/* Generated by tools/wiki/build-wiki.js — do not edit */
window.WIKI_BUNDLE={"manifest":{"version":2,"standalone":true,"generatedAt":"2026-07-20T00:03:19.510Z","defaultPage":"wiki--README","categories":["Home","Getting started","Installation","Storage & USB","API reference","Integration","Audio","Reference"],"pages":[{"id":"README","title":"HighAsCG documentation","file":"README.md","category":"Home","description":"Operator and integrator docs live at the **top level** of this folder. Deeper material is grouped by audience.","order":0},{"id":"wiki--README","title":"HighAsCG wiki","file":"wiki/README.md","category":"Home","description":"Operator and integrator documentation beyond the top-level [docs/](../README.md) guides.","order":1},{"id":"ARCHITECTURE","title":"HighAsCG architecture — unified playout stack","file":"ARCHITECTURE.md","category":"Getting started","description":"**Status:** Canonical reference (2026)","order":10},{"id":"PLAN_SERVER_CLIENT_SPLIT","title":"Plan: Permanent server / client split","file":"PLAN_SERVER_CLIENT_SPLIT.md","category":"Getting started","description":"> **Superseded for production (2026):** **One unified repo** — **`client/`** (canonical UI) + **`src/`** (API) on the **same playout machine**. Node serves **AP","order":11},{"id":"MODULES","title":"Optional modules (Previs / Tracking / Auto-follow / CG Studio)","file":"MODULES.md","category":"Getting started","description":"HighAsCG ships with a small set of optional feature modules that can be enabled or deleted independently of the base server. The packaging rules below are the o","order":12},{"id":"MANUAL_INSTALL","title":"Manual install: `casparcg` user, `/opt/casparcg`, X11, DeckLink, NDI","file":"MANUAL_INSTALL.md","category":"Installation","description":"> **Playout host (recommended):** use the ordered setup scripts and **[`scripts/setup/MANUAL_INSTALL.md`](../scripts/setup/MANUAL_INSTALL.md)** — kernel 117, NV","order":20},{"id":"LIVE_USB_IMAGE","title":"Creating a Live USB Image from a Running HighAsCG / CasparCG Server","file":"LIVE_USB_IMAGE.md","category":"Installation","description":"> **Goal:** Capture the entire running production server — Ubuntu 24.04 LTS, NVIDIA","order":21},{"id":"CALAMARES_INSTALL_TO_DISK","title":"Install HighAsCG to internal disk (Calamares)","file":"CALAMARES_INSTALL_TO_DISK.md","category":"Installation","description":"Graphical **install-to-disk** from the live USB stick using **Calamares** (eggs integration). Use this for a **permanent playout machine** — not for preparing t","order":21.5},{"id":"wiki--install--calamares-install-to-disk","title":"Calamares install to disk","file":"wiki/install/calamares-install-to-disk.md","category":"Installation","description":"Install HighAsCG from the **live USB stick** onto an **internal SSD/HDD/NVMe** using the graphical Calamares wizard.","order":21.6},{"id":"ISO_CONTENTS","title":"HighAsCG live ISO — contents reference","file":"ISO_CONTENTS.md","category":"Installation","description":"What is inside the **penguins-eggs** hybrid ISO produced by **`tools/eggs/live-usb/build-highascg-egg.sh`** (or **`make-dev-github-release-iso-quick.sh`**).","order":22},{"id":"DEV_RELEASE_GITHUB","title":"Dev GitHub releases (ISO + portable tarball)","file":"DEV_RELEASE_GITHUB.md","category":"Installation","description":"Use this flow when you have a **build host or workstation** running the repo (typically with `node_modules` already installed). Two modes:","order":23},{"id":"BOOT_EMERGENCY_RECOVERY","title":"Boot emergency mode (Ctrl+D to continue)","file":"BOOT_EMERGENCY_RECOVERY.md","category":"Installation","description":"If the machine drops into **emergency mode** during boot and you must press **Ctrl+D** to continue, check the journal on the playout PC:","order":24},{"id":"openbox_autostart","title":"Openbox autostart — X session only (layout, NVIDIA). Caspar + scanner = systemd (WO-73).","file":"openbox_autostart.md","category":"Installation","description":"**Current installer:** `scripts/setup/09-openbox-autostart.sh` writes `~/.config/openbox/autostart` for the Caspar user. It does **not** start `casparcg-scanner","order":25},{"id":"HIGHASCG_PASSWORDLESS_SUDO","title":"HighAsCG passwordless sudo (`NOPASSWD`)","file":"HIGHASCG_PASSWORDLESS_SUDO.md","category":"Installation","description":"HighAsCG’s Node service often runs as **`casparcg`** (see installer). Several features call **`sudo -n`** so the Web UI works **without an interactive TTY passw","order":26},{"id":"BRIDGE_DISK_AND_USB_EXFAT","title":"Bridge disk (HIGHASCGDAT) and USB stick (HIGHASCGEXF)","file":"BRIDGE_DISK_AND_USB_EXFAT.md","category":"Storage & USB","description":"WO-52 splits operator storage into two volumes.","order":30},{"id":"WO47_ISO_VS_EXFAT","title":"WO‑47: What stays in the Eggs squashfs vs what rides on exFAT","file":"WO47_ISO_VS_EXFAT.md","category":"Storage & USB","description":"This matches **`tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list`** and the boot chain **`highascg-exfat-server-update`** → **`highascg-exfat-sy","order":31},{"id":"EXFAT_SERVER_UPDATE","title":"exFAT server updates (`drop-update/`)","file":"EXFAT_SERVER_UPDATE.md","category":"Storage & USB","description":"For a **closed ISO** (no `src/` in squashfs), operators refresh the **Node server + operator UI** by dropping files on the stick — without reflashing the image.","order":32},{"id":"USB_AUTO_MOUNT_UBUNTU","title":"Ubuntu: auto-mount USB drives for the `casparcg` user (HighAsCG import)","file":"USB_AUTO_MOUNT_UBUNTU.md","category":"Storage & USB","description":"This walkthrough is for a **playout** machine where:","order":33},{"id":"casparcg-linux-usb-guide","title":"CasparCG Linux Bootable USB — Comprehensive Build Guide","file":"casparcg-linux-usb-guide.md","category":"Storage & USB","description":"A complete guide to building a bootable Ubuntu Server USB drive running CasparCG, with live boot capability, optional local installation via Calamares, and a de","order":34},{"id":"CASPAR_IMAGE_VS_HIGHASCG_OVERLAY","title":"CasparCG-only image vs HighAsCG on exFAT (overlay mental model)","file":"CASPAR_IMAGE_VS_HIGHASCG_OVERLAY.md","category":"Storage & USB","description":"**Authoritative matrix** (what the ISO keeps vs what lives on **`HIGHASCGEXF`**, boot order, knobs): **[`WO47_ISO_VS_EXFAT.md`](WO47_ISO_VS_EXFAT.md)**.","order":35},{"id":"wiki--api--README","title":"HighAsCG HTTP API","file":"wiki/api/README.md","category":"API reference","group":"Overview","description":"HighAsCG exposes a **JSON REST API** on the playout server. The operator UI (served from **`dist-web/`** on the same host at `:4200`) and Companion modules call","order":40},{"id":"wiki--api--scene-take","title":"`POST /api/scene/take` — scene / look take","file":"wiki/api/scene-take.md","category":"API reference","group":"Scene & playback","description":"Takes a **look** (scene JSON with layers) to **program** or **preview** on Caspar, using the LBG pipeline (`runSceneTakeLbg`). This is the main air chain used b","order":41},{"id":"wiki--api--playback","title":"Playback API — PLAY, LOAD, LOADBG, transport","file":"wiki/api/playback.md","category":"API reference","group":"Scene & playback","description":"Layer-level CasparCG AMCP commands exposed as JSON POST routes. These are the **direct** playout controls (single channel/layer). Full **look takes** use [`POST","order":42},{"id":"wiki--api--playback-and-amcp","title":"AMCP bridge & server control","file":"wiki/api/playback-and-amcp.md","category":"API reference","group":"Scene & playback","description":"**Caspar:** required for all routes on this page.","order":43},{"id":"wiki--api--mixer","title":"Mixer API — `MIXER` AMCP over HTTP","file":"wiki/api/mixer.md","category":"API reference","group":"Mixer & CG","description":"CasparCG layer and channel mixer commands: opacity, fill, crop, color, audio volume, deferred tweens, and inspector effects.","order":44},{"id":"wiki--api--mixer-cg-scene","title":"CG, scene & overlays","file":"wiki/api/mixer-cg-scene.md","category":"API reference","group":"Mixer & CG","description":"**Caspar:** required unless noted.","order":45},{"id":"wiki--api--cg","title":"CG API — HTML templates (`CG` AMCP)","file":"wiki/api/cg.md","category":"API reference","group":"Mixer & CG","description":"CasparCG **HTML/template CG** commands on a channel layer. Templates live under the Caspar template path; data payloads are XML/JSON strings passed on add/updat","order":46},{"id":"wiki--api--project","title":"Project API — save, load, sync","file":"wiki/api/project.md","category":"API reference","group":"Project & state","description":"Persist show data (looks/scenes, routing, timelines in project JSON, settings mirrors) on the playout host disk. **Does not require Caspar** for core save/load/","order":47},{"id":"wiki--api--project-state-media","title":"Project, state & media (index)","file":"wiki/api/project-state-media.md","category":"API reference","group":"Project & state","description":"Split into detailed pages:","order":48},{"id":"wiki--api--state-and-media","title":"State, variables, selection & media","file":"wiki/api/state-and-media.md","category":"API reference","group":"Project & state","description":"Query and update server snapshots for the operator UI, Companion variables, and the media library.","order":49},{"id":"wiki--api--timelines","title":"Timelines API","file":"wiki/api/timelines.md","category":"API reference","group":"Timelines & streaming","description":"Multi-track timelines with clip keyframes, playhead transport, and Caspar output on **layers 200+** (above look stacks 1–199). The server holds timelines in the","order":50},{"id":"wiki--api--timelines-multiview","title":"Multiview & NDI","file":"wiki/api/timelines-multiview.md","category":"API reference","group":"Timelines & streaming","description":"**Full reference:** [timelines.md](timelines.md) — CRUD, transport (`play`/`pause`/`stop`/`seek`), `sendTo`, `take`, data model, WebSocket.","order":51},{"id":"wiki--api--streaming-audio-osc","title":"Streaming, audio & OSC","file":"wiki/api/streaming-audio-osc.md","category":"API reference","group":"Timelines & streaming","description":"| Method | Path | Caspar | Description |","order":52},{"id":"wiki--api--system-settings-hardware","title":"System, settings & hardware","file":"wiki/api/system-settings-hardware.md","category":"API reference","group":"System & hardware","description":"**Caspar:** not required.","order":53},{"id":"wiki--api--network-tailscale","title":"Tailscale network API","file":"wiki/api/network-tailscale.md","category":"API reference","group":"System & hardware","description":"**Caspar:** not required.","order":53.5},{"id":"wiki--api--ingest-usb-plugins","title":"Ingest, USB, plugins & modules","file":"wiki/api/ingest-usb-plugins.md","category":"API reference","group":"System & hardware","description":"**Caspar:** not required for upload/download.","order":54},{"id":"api-reference","title":"AMCP REST API Reference (legacy)","file":"api-reference.md","category":"API reference","group":"Overview","description":"> **Canonical docs:** [wiki/api/README.md](wiki/api/README.md) — full endpoint list, Caspar gate, OpenAPI [`openapi.yaml`](wiki/api/openapi.yaml), and per-topic","order":55},{"id":"osc-integration","title":"OSC integration (CasparCG → HighAsCG)","file":"osc-integration.md","category":"Integration","description":"HighAsCG listens for **Open Sound Control (OSC)** over **UDP** from CasparCG Server. **Production setups assume Caspar sends OSC to HighAsCG**; the UDP listener","order":60},{"id":"wiki--integration--usb-v4l2-input","title":"USB / V4L2 video input","file":"wiki/integration/usb-v4l2-input.md","category":"Integration","description":"Capture UVC webcams and USB switchers (e.g. **ATEM Mini Pro ISO**) on a dedicated Caspar host channel. By default Caspar opens the device directly via **`PLAY …","order":60},{"id":"companion-websocket-catalog-bootstrap","title":"Companion module: WebSocket slim bootstrap & catalog loading","file":"companion-websocket-catalog-bootstrap.md","category":"Integration","description":"HighAsCG can send a **small first `state` frame** on WebSocket connect when the operator enables slim bootstrap on the server. Companion modules that assumed **","order":61},{"id":"companion-module-ui-selection","title":"Companion module: UI selection variables (`ui_selection_*`)","file":"companion-module-ui-selection.md","category":"Integration","description":"HighAsCG pushes **inspector selection** from the web UI into the same **Companion-style variable map** used for OSC, channel INFO, and app stats. A Bitfocus **C","order":62},{"id":"caspar_config_explained","title":"Caspar Config Explained","file":"caspar_config_explained.md","category":"Integration","description":"<?xml version=\"1.0\" encoding=\"utf-8\"?> <-- The opening XML declaration -->","order":63},{"id":"wiki--guides--shader-fx","title":"Shader FX — audio-reactive shader templates","file":"wiki/guides/shader-fx.md","category":"Audio","description":"Paste a **Shadertoy-style GLSL shader**, save it, and play it like any other template. Saved shaders export as self-contained Caspar HTML templates in **`templa","order":69},{"id":"guides--audio--audio_features_walkthrough","title":"Audio Playout & Monitoring Walkthrough","file":"guides/audio/audio_features_walkthrough.md","category":"Audio","description":"This update enhances HighAsCG with \"CasparCG Enhanced\" features, specifically focusing on PortAudio support, multi-channel layouts (8ch/16ch), and hardware-leve","order":70},{"id":"guides--audio--audio-setup-guide","title":"Audio setup guide (CasparCG on Ubuntu)","file":"guides/audio/audio-setup-guide.md","category":"Audio","description":"Single entry point for **multi-channel playout**: follow one path below, then use [Audio routing reference](../../reference/audio/audio-routing-reference.md) fo","order":70},{"id":"reference--audio--alsa-device-enumeration","title":"ALSA device enumeration (Linux)","file":"reference/audio/alsa-device-enumeration.md","category":"Audio","description":"This guide explains how to **list and interpret** ALSA playback devices on Ubuntu (and similar distributions). Use it when wiring CasparCG **FFmpeg** or **syste","order":71},{"id":"reference--audio--alsa-mixer-api","title":"ALSA mixer API (server handoff)","file":"reference/audio/alsa-mixer-api.md","category":"Audio","description":"**Server:** `src/audio/alsa-mixer.js` · **Routes:** `src/api/routes-audio.js`","order":71},{"id":"reference--audio--audio-routing-reference","title":"Audio routing reference (CasparCG + HighAsCG)","file":"reference/audio/audio-routing-reference.md","category":"Audio","description":"Quick reference for **layer/channel audio levels**, **layouts**, **routing between channels**, and **FFmpeg audio filters**. For Linux device setup, see [Audio ","order":71},{"id":"reference--audio--caspar-audio-consumers","title":"CasparCG audio consumers (config reference)","file":"reference/audio/caspar-audio-consumers.md","category":"Audio","description":"How to declare **audio outputs** in `casparcg.config`: system default sink, FFmpeg to ALSA/Pulse, NDI, audio-only channels, and **channel layouts**. Complements","order":71},{"id":"reference--audio--caspar-outputs-nvidia-stereo-usb","title":"CasparCG audio outputs on Linux: NVIDIA, 3.5 mm stereo, USB","file":"reference/audio/caspar-outputs-nvidia-stereo-usb.md","category":"Audio","description":"Practical guide for the three most common **local** playout paths on Ubuntu:","order":71},{"id":"reference--audio--jack-audio-routing","title":"JACK audio routing (Linux alternative to PipeWire graph)","file":"reference/audio/jack-audio-routing.md","category":"Audio","description":"**JACK** (JACK Audio Connection Kit) is a low-latency audio server with a patchable **client graph**. It fits when you want **deterministic** routing between ap","order":71},{"id":"reference--audio--network-audio-protocols","title":"Network audio protocols (Linux + CasparCG context)","file":"reference/audio/network-audio-protocols.md","category":"Audio","description":"Options for moving **audio over the network** or bridging **Dante / AES67** to Linux. Use with local I/O guides: [ALSA](./alsa-device-enumeration.md), [Caspar a","order":71},{"id":"reference--audio--pipewire-multichannel-routing","title":"PipeWire multi-channel routing (Linux)","file":"reference/audio/pipewire-multichannel-routing.md","category":"Audio","description":"[PipeWire](https://pipewire.org/) is the default audio server on **Ubuntu 24.04+** (with **WirePlumber** as the session/policy manager). Use it to route **multi","order":71},{"id":"reference--amcp-mapping","title":"AMCP Architecture Mapping","file":"reference/amcp-mapping.md","category":"Reference","description":"This document maps out the CasparCG TCP AMCP commands, their respective HighAsCG REST endpoints, and the underlying modular JS methods.","order":80},{"id":"reference--amcp-clean-look-fade","title":"AMCP Clean Look Fade","file":"reference/amcp-clean-look-fade.md","category":"Reference","description":"This is the working banked look-to-look fade pattern used by HighAsCG for clean program transitions.","order":80},{"id":"reference--amcp-migration-qa-checklist","title":"AMCP migration — manual QA checklist (T7.4)","file":"reference/amcp-migration-qa-checklist.md","category":"Reference","description":"Run on **each playout PC** with CasparCG and HighAsCG on the build that includes `casparcg-connection` (default transport). Record date, machine, and pass/fail.","order":80},{"id":"reference--amcp-migration","title":"AMCP transport: `casparcg-connection` migration","file":"reference/amcp-migration.md","category":"Reference","description":"HighAsCG uses the [casparcg-connection](https://www.npmjs.com/package/casparcg-connection) npm package (v6.x, CommonJS) as the default AMCP transport. The publi","order":80},{"id":"reference--audio_output_architecture","title":"Audio Output Architecture Redesign & Remaining Issues","file":"reference/audio_output_architecture.md","category":"Reference","description":"**Before:** Every PortAudio / ALSA device was auto-generated as a rear panel connector, flooding the panel with ~40 entries for 3 physical cards.","order":80},{"id":"reference--cef-interactive-bridge","title":"CEF interactive bridge (removed, WO-257)","file":"reference/cef-interactive-bridge.md","category":"Reference","description":"**Status:** Removed. This page is kept as a pointer for anyone following old links or work","order":80},{"id":"reference--companion-satellite-api","title":"Companion Satellite API (HighAsCG reference)","file":"reference/companion-satellite-api.md","category":"Reference","description":"HighAsCG uses Bitfocus Companion’s **Satellite API** for **button preview bitmaps** (WO-75). Timeline **press/trigger** stays on the **HTTP Remote Control API**","order":80},{"id":"reference--GPU_SCREEN_CONSUMER_AND_XRANDR","title":"GPU output, screen consumers, and X11 (xrandr) — intended interaction","file":"reference/GPU_SCREEN_CONSUMER_AND_XRANDR.md","category":"Reference","description":"This document describes how **screen destinations**, **CasparCG channel / screen consumers**, **Device View cabling**, and **OS-level display layout (xrandr)** ","order":80},{"id":"reference--SUDO_UBUNTU_SETUP","title":"HighAsCG sudo (no-password) setup on Ubuntu","file":"reference/SUDO_UBUNTU_SETUP.md","category":"Reference","description":"This guide configures **only the minimum commands** that HighAsCG may run as root without a password when the app runs as `casparcg:casparcg`.","order":80},{"id":"reference--hot-backup-replication","title":"Hot backup replication (WO-54 / WO-61)","file":"reference/hot-backup-replication.md","category":"Reference","description":"Two playout machines run as a **leader/follower** pair for hot backup.","order":80},{"id":"reference--amcp-pgm-look-take-pipeline","title":"PGM look take — AMCP pipeline","file":"reference/amcp-pgm-look-take-pipeline.md","category":"Reference","description":"`POST /api/scene/take` runs `runSceneTakeLbg` on the Caspar program channel (and optionally the paired preview channel). This document is the **command order** ","order":80},{"id":"reference--pip-overlay-outside-border","title":"PIP overlay — outside border (canonical model)","file":"reference/pip-overlay-outside-border.md","category":"Reference","description":"**Status:** Canonical reference (2026-06-29). Do not regress this without updating this doc and `tools/smoke/smoke-pip-overlay-placement.test.js`.","order":80},{"id":"reference--PIXELHUE_API","title":"PixelHue / Unico HTTP / WebSocket API (Companion + PixelFlow OpenAPI V2.0.1)","file":"reference/PIXELHUE_API.md","category":"Reference","description":"This document combines:","order":80},{"id":"reference--polling-vs-osc","title":"Polling vs OSC in HighAsCG","file":"reference/polling-vs-osc.md","category":"Reference","description":"CasparCG exposes rich state in two ways: **AMCP** (TCP, request/response) and **OSC** (UDP, push). HighAsCG uses OSC for real-time mixer, layer, playback, profi","order":80},{"id":"reference--private-volume-sync","title":"Private volume sync (machine secrets on USB / bridge)","file":"reference/private-volume-sync.md","category":"Reference","description":"Per-machine folder for **Tailscale**, **Syncthing**, and **replication pairing** — kept off the public `configs/` and `projects/` sync paths.","order":80},{"id":"reference--README","title":"Reference documentation","file":"reference/README.md","category":"Reference","description":"Technical deep dives for integrators and developers. Operators should start at [../README.md](../README.md).","order":80},{"id":"reference--config_from_server","title":"Sample `casparcg.config` from production (custom resolution)","file":"reference/config_from_server.md","category":"Reference","description":"**NVIDIA screen consumer vsync:** use **Sync to VBlank off** in NVIDIA Settings and **`<vsync>true</vsync>`** on the Caspar screen consumer. See [screen-consume","order":80},{"id":"reference--screen-consumer-vsync-nvidia","title":"Screen consumer vsync (NVIDIA)","file":"reference/screen-consumer-vsync-nvidia.md","category":"Reference","description":"Normative setup for **smooth, tear-free** output on CasparCG **screen** consumers when the playout GPU is **NVIDIA**.","order":80},{"id":"reference--sudo_and_audio_setup","title":"Sudo Setup for CasparCG / HighAsCG","file":"reference/sudo_and_audio_setup.md","category":"Reference","description":"**Avoid `NOPASSWD: ALL` for `casparcg`.** Use narrow **`/etc/sudoers.d/`** fragments (see **`docs/HIGHASCG_PASSWORDLESS_SUDO.md`**): media mount helper, optiona","order":80},{"id":"reference--tailscale-integration","title":"Tailscale integration (WO-91)","file":"reference/tailscale-integration.md","category":"Reference","description":"HighAsCG controls Tailscale from **Settings → Tailscale** and exposes HTTP APIs for status, login, and operator-monitor browser assist.","order":80},{"id":"reference--web-browser-and-server-logs","title":"Web Browser And Server Logs","file":"reference/web-browser-and-server-logs.md","category":"Reference","description":"[2026-04-05 09:07:18.294] [info]    async_event_server[:5250] Client 127.0.0.1 disconnected (0 connections).","order":80},{"id":"reference--xrandr-gpu-screen-mapping","title":"xrandr GPU ↔ screen mapping (Device View)","file":"reference/xrandr-gpu-screen-mapping.md","category":"Reference","description":"This document is the **implementation-facing** companion to the product narrative in [GPU_SCREEN_CONSUMER_AND_XRANDR.md](GPU_SCREEN_CONSUMER_AND_XRANDR.md). It ","order":80},{"id":"decoupled_architecture_list","title":"HighAsCG Decoupled Service Architecture","file":"decoupled_architecture_list.md","category":"Reference","description":"> **Production (WO-52):** API and UI both run on the **playout machine** at **`http://<host>:4200/`** (`dist-web/` + `/api/*`). The diagram below is the **logic","order":81},{"id":"ARTNET_PIXEL_MAPPING","title":"Art-Net & pixel mapping — capability landscape (WO-241, updated WO-242)","file":"ARTNET_PIXEL_MAPPING.md","category":"Reference","description":"**Status:** Canonical reference (2026-07) | **Binary:** `bin/casparcg` highascg-build-v1, deployed 2026-07-15 — see [CASPARCG_BUILD_PROVENANCE.md](CASPARCG_BUIL","order":900},{"id":"CASPARCG_BUILD_PROVENANCE","title":"CasparCG highascg-build-v1 — provenance manifest","file":"CASPARCG_BUILD_PROVENANCE.md","category":"Reference","description":"Every component of the final binary, mapped to its source. Keep current through assembly/build.","order":900},{"id":"STICK_QUICK_START","title":"HighAsCG playout stick — quick start","file":"STICK_QUICK_START.md","category":"Reference","description":"Prepare a **bootable HighAsCG USB stick** on **Windows** or **macOS**: flash the live ISO, add the operator **exFAT** partition, copy the starter folder layout,","order":900},{"id":"SECURITY","title":"HighAsCG security model","file":"SECURITY.md","category":"Reference","description":"HighAsCG exposes an HTTP API and operator WebSocket on the playout server. By default **authentication is not enforced** so existing deployments keep working. W","order":900},{"id":"wiki--integration--virtual-camera-output","title":"Virtual camera output (v4l2loopback)","file":"wiki/integration/virtual-camera-output.md","category":"Reference","description":"Feed Caspar program output to **Zoom**, **OBS**, or other apps as a **V4L2 webcam** plus an optional **ALSA virtual mic**. HighAsCG uses the same safe JPEG buff","order":900},{"id":"WALKTHROUGH_ARTNET_LED_WALL","title":"Walkthrough — LED wall on the native Art-Net consumer","file":"WALKTHROUGH_ARTNET_LED_WALL.md","category":"Reference","description":"**Badge:** [CONFIG+CASPAR-RESTART] — every step in this doc that changes fixtures requires a Caspar restart.","order":900},{"id":"WALKTHROUGH_PIXELMAP_FIXTURES","title":"Walkthrough — live-editable pixel-map fixtures (HighAsCG JS pipeline)","file":"WALKTHROUGH_PIXELMAP_FIXTURES.md","category":"Reference","description":"**Badge:** [DEPRECATED — legacyJsPixelmap flag] · [LIVE-EDITABLE] — everything here re-applies on save with **no Caspar restart** and no config regen.","order":900}]},"content":{"README":"<h1 id=\"highascg-documentation\">HighAsCG documentation</h1>\n<p>Operator and integrator docs live at the <strong>top level</strong> of this folder. Deeper material is grouped by audience.</p>\n<h2 id=\"start-here-operators-installers\">Start here (operators &amp; installers)</h2>\n<table><thead><tr><th>Document</th><th>Topic</th></tr></thead><tbody>\n<tr><td><a href=\"#/ARCHITECTURE\" data-wiki-page=\"1\">ARCHITECTURE.md</a></td><td><strong>Unified playout stack</strong> — API + <code>dist-web/</code> on <code>:4200</code>, same machine</td></tr>\n<tr><td><a href=\"../map\">Interactive Map</a></td><td><strong>Project Map</strong> — Visual architecture graph (served at <code>/map</code> at runtime)</td></tr>\n<tr><td><a href=\"#/PLAN_SERVER_CLIENT_SPLIT\" data-wiki-page=\"1\">PLAN_SERVER_CLIENT_SPLIT.md</a></td><td>Historical WO-51 headless plan (superseded by WO-52)</td></tr>\n<tr><td><a href=\"../../from_client/AGENT_SERVER_CLIENT_MERGE.md\">../from_client/AGENT_SERVER_CLIENT_MERGE.md</a></td><td><strong>WO-52</strong> deploy checklist — API + UI on playout</td></tr>\n<tr><td><a href=\"#/MANUAL_INSTALL\" data-wiki-page=\"1\">MANUAL_INSTALL.md</a></td><td>Production install on Ubuntu (<code>scripts/install.sh</code>)</td></tr>\n<tr><td><a href=\"#/STICK_QUICK_START\" data-wiki-page=\"1\">STICK_QUICK_START.md</a></td><td><strong>Operator stick prep</strong> — download ISO, Etcher, exFAT, starter zip, BIOS</td></tr>\n<tr><td><a href=\"#/LIVE_USB_IMAGE\" data-wiki-page=\"1\">LIVE_USB_IMAGE.md</a></td><td>Build / flash / boot a live USB from a running host</td></tr>\n<tr><td><a href=\"#/CALAMARES_INSTALL_TO_DISK\" data-wiki-page=\"1\">CALAMARES_INSTALL_TO_DISK.md</a></td><td><strong>Install to internal disk</strong> — <strong>disable CSM</strong>, Calamares, UEFI ESP, <strong><code>bios_grub</code></strong> + <code>/</code>, troubleshooting</td></tr>\n<tr><td><a href=\"#/ISO_CONTENTS\" data-wiki-page=\"1\">ISO_CONTENTS.md</a></td><td>What is inside the Eggs live ISO (OS → Caspar → HighAsCG)</td></tr>\n<tr><td><a href=\"#/DEV_RELEASE_GITHUB\" data-wiki-page=\"1\">DEV_RELEASE_GITHUB.md</a></td><td>GitHub prereleases (alpha tarball, full ISO+tarball)</td></tr>\n<tr><td><a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">WO47_ISO_VS_EXFAT.md</a></td><td>ISO squashfs vs exFAT stick payload (modular updates)</td></tr>\n<tr><td><a href=\"#/BRIDGE_DISK_AND_USB_EXFAT\" data-wiki-page=\"1\">BRIDGE_DISK_AND_USB_EXFAT.md</a></td><td>Internal bridge disk (HIGHASCGDAT) vs USB stick (HIGHASCGEXF)</td></tr>\n<tr><td><a href=\"#/BOOT_EMERGENCY_RECOVERY\" data-wiki-page=\"1\">BOOT_EMERGENCY_RECOVERY.md</a></td><td>Emergency boot (Ctrl+D): stale EFI fstab + optional USB mount blocking local-fs</td></tr>\n<tr><td><a href=\"#/CASPAR_IMAGE_VS_HIGHASCG_OVERLAY\" data-wiki-page=\"1\">CASPAR_IMAGE_VS_HIGHASCG_OVERLAY.md</a></td><td>Caspar-only ISO shell + HighAsCG from exFAT</td></tr>\n<tr><td><a href=\"#/HIGHASCG_PASSWORDLESS_SUDO\" data-wiki-page=\"1\">HIGHASCG_PASSWORDLESS_SUDO.md</a></td><td>Narrow <code>sudo</code> rules for media mount, NVIDIA, etc.</td></tr>\n<tr><td><a href=\"#/openbox_autostart\" data-wiki-page=\"1\">openbox_autostart.md</a></td><td>nodm + Openbox + Caspar autostart chain</td></tr>\n<tr><td><a href=\"#/casparcg-linux-usb-guide\" data-wiki-page=\"1\">casparcg-linux-usb-guide.md</a></td><td>USB stick usage on Linux</td></tr>\n<tr><td><a href=\"#/USB_AUTO_MOUNT_UBUNTU\" data-wiki-page=\"1\">USB_AUTO_MOUNT_UBUNTU.md</a></td><td>Auto-mount removable media (udisks / polkit)</td></tr>\n</tbody></table>\n<h2 id=\"application-integration\">Application &amp; integration</h2>\n<table><thead><tr><th>Document</th><th>Topic</th></tr></thead><tbody>\n<tr><td><a href=\"index.html\">&lt;strong&gt;HTML wiki&lt;/strong&gt;</a></td><td><strong>Standalone browser UI</strong> — open <code>docs/wiki-site/index.html</code> (<code>npm run wiki:build</code> after doc edits)</td></tr>\n<tr><td><a href=\"#/wiki--api--README\" data-wiki-page=\"1\">wiki/api/README.md</a></td><td><strong>HTTP API wiki</strong> (all endpoints, examples, OpenAPI YAML)</td></tr>\n<tr><td><a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">wiki/api/scene-take.md</a></td><td><strong><code>POST /api/scene/take</code></strong> (detailed)</td></tr>\n<tr><td><a href=\"#/wiki--api--playback\" data-wiki-page=\"1\">wiki/api/playback.md</a></td><td><strong>Playback</strong> — play/load/stop/clear, etc.</td></tr>\n<tr><td><a href=\"#/wiki--api--timelines\" data-wiki-page=\"1\">wiki/api/timelines.md</a></td><td><strong>Timelines</strong> — CRUD, transport, take</td></tr>\n<tr><td><a href=\"#/wiki--api--mixer\" data-wiki-page=\"1\">wiki/api/mixer.md</a></td><td><strong>Mixer</strong> — <code>/api/mixer/{command}</code></td></tr>\n<tr><td><a href=\"#/wiki--api--cg\" data-wiki-page=\"1\">wiki/api/cg.md</a></td><td><strong>CG</strong> templates</td></tr>\n<tr><td><a href=\"#/wiki--api--project\" data-wiki-page=\"1\">wiki/api/project.md</a></td><td><strong>Project</strong> save/load</td></tr>\n<tr><td><a href=\"#/wiki--api--state-and-media\" data-wiki-page=\"1\">wiki/api/state-and-media.md</a></td><td><strong>State</strong>, media, variables</td></tr>\n<tr><td><a href=\"#/api-reference\" data-wiki-page=\"1\">api-reference.md</a></td><td>Legacy AMCP REST overview (partial — see wiki)</td></tr>\n<tr><td><a href=\"#/MODULES\" data-wiki-page=\"1\">MODULES.md</a></td><td>Feature flags and optional modules</td></tr>\n<tr><td><a href=\"#/osc-integration\" data-wiki-page=\"1\">osc-integration.md</a></td><td>OSC from CasparCG into HighAsCG</td></tr>\n<tr><td><a href=\"#/caspar_config_explained\" data-wiki-page=\"1\">caspar_config_explained.md</a></td><td>Caspar XML / config concepts</td></tr>\n<tr><td><a href=\"#/companion-websocket-catalog-bootstrap\" data-wiki-page=\"1\">companion-websocket-catalog-bootstrap.md</a></td><td>Bitfocus Companion + slim WS catalog</td></tr>\n<tr><td><a href=\"#/companion-module-ui-selection\" data-wiki-page=\"1\">companion-module-ui-selection.md</a></td><td>Companion module UI notes</td></tr>\n</tbody></table>\n<h2 id=\"audio-operator-guides\">Audio (operator guides)</h2>\n<table><thead><tr><th>Document</th><th>Topic</th></tr></thead><tbody>\n<tr><td><a href=\"#/guides--audio--audio-setup-guide\" data-wiki-page=\"1\">guides/audio/audio-setup-guide.md</a></td><td>Audio routing entry point</td></tr>\n<tr><td><a href=\"#/guides--audio--audio_features_walkthrough\" data-wiki-page=\"1\">guides/audio/audio_features_walkthrough.md</a></td><td>Audio features tour</td></tr>\n</tbody></table>\n<h2 id=\"other-folders\">Other folders</h2>\n<table><thead><tr><th>Folder</th><th>Audience</th></tr></thead><tbody>\n<tr><td><a href=\"../reference\">reference/</a></td><td>AMCP mapping, GPU/xrandr design, <a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">screen consumer vsync (NVIDIA)</a>, PixelHue API, deep audio routing</td></tr>\n<tr><td><a href=\"../internal\">internal/</a></td><td>Custom Caspar builds, image consolidation, architecture notes</td></tr>\n<tr><td><a href=\"../../work/work-orders\">../work/work-orders/</a></td><td>Engineering work orders (WO-*) — <strong>not shipped runtime</strong></td></tr>\n<tr><td><a href=\"../../work/references\">../work/references/</a></td><td>Design prototypes — <strong>not part of the program</strong></td></tr>\n</tbody></table>\n<p><strong>On playout server:</strong> <strong><code>dist-web/</code></strong> (built from in-repo <strong><code>client/</code></strong>, served on <code>:4200</code> on the <strong>same machine</strong> as the API), <strong><code>index.js</code></strong>, <strong><code>src/</code></strong>, <strong><code>config/</code></strong>, <strong><code>template/</code></strong>. <strong>Not on playout as sources:</strong> raw <strong><code>client/</code></strong> tree (ship <strong><code>dist-web/</code></strong> only), <strong><code>work/</code></strong>. <strong>Canonical UI development:</strong> edit <strong><code>client/</code></strong> in this repo → <code>npm run build:client</code>. <strong>highascg-client</strong> (optional): Electron packaging extract (<code>client/tools/electron-launcher/</code>) — simulator, multiserver, modules; opens browser to playout <code>:4200</code>; <strong>not</strong> the operator UI source tree.</p>","wiki--README":"<h1 id=\"highascg-wiki\">HighAsCG wiki</h1>\n<p>Operator and integrator documentation beyond the top-level <a href=\"#/README\" data-wiki-page=\"1\">docs/</a> guides.</p>\n<h2 id=\"html-wiki-browser-ui\">HTML wiki (browser UI)</h2>\n<p>Standalone static site — <strong>not served by the playout server</strong>. Open in any browser:</p>\n<pre><code class=\"language-text\">docs/wiki-site/index.html</code></pre>\n<p>From the repo root:</p>\n<pre><code class=\"language-bash\">npm run wiki:build    # after editing markdown under docs/\nnpm run wiki:open     # Linux/macOS shortcut</code></pre>\n<p>Or double-click <code>docs/wiki-site/index.html</code>. All page content is embedded in <code>assets/wiki-bundle.js</code> at build time (works with <code>file://</code> — no server required).</p>\n<h2 id=\"installation-from-live-usb\">Installation (from live USB)</h2>\n<table><thead><tr><th>Page</th><th>Contents</th></tr></thead><tbody>\n<tr><td><a href=\"#/wiki--install--calamares-install-to-disk\" data-wiki-page=\"1\">&lt;strong&gt;install/calamares-install-to-disk.md&lt;/strong&gt;</a></td><td><strong>Calamares</strong> — <strong>disable CSM</strong>, launch, pick disk, <strong>Erase</strong> vs manual <strong><code>bios_grub</code></strong> + <code>/</code></td></tr>\n<tr><td><a href=\"#/CALAMARES_INSTALL_TO_DISK\" data-wiki-page=\"1\">../CALAMARES_INSTALL_TO_DISK.md</a></td><td>Full install guide (partition tables, rsync 11, session.log)</td></tr>\n</tbody></table>\n<h2 id=\"api-reference-openapi-style\">API reference (OpenAPI-style)</h2>\n<table><thead><tr><th>Page</th><th>Contents</th></tr></thead><tbody>\n<tr><td><a href=\"#/wiki--api--README\" data-wiki-page=\"1\">api/README.md</a></td><td>Base URL, auth, Caspar gate, response format, WebSocket</td></tr>\n<tr><td><a href=\"../wiki/api/openapi.yaml\">api/openapi.yaml</a></td><td>OpenAPI 3.1 (core routes fully specified)</td></tr>\n<tr><td><a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">&lt;strong&gt;api/scene-take.md&lt;/strong&gt;</a></td><td><strong><code>POST /api/scene/take</code></strong> — request/response schemas, PGM/PRV, examples</td></tr>\n<tr><td><a href=\"#/wiki--api--playback\" data-wiki-page=\"1\">&lt;strong&gt;api/playback.md&lt;/strong&gt;</a></td><td><strong>Playback</strong> — play/load/loadbg, transport, swap/call, playbackMatrix</td></tr>\n<tr><td><a href=\"#/wiki--api--playback-and-amcp\" data-wiki-page=\"1\">api/playback-and-amcp.md</a></td><td>AMCP batch/raw, print, restart, server control</td></tr>\n<tr><td><a href=\"#/wiki--api--mixer\" data-wiki-page=\"1\">&lt;strong&gt;api/mixer.md&lt;/strong&gt;</a></td><td><strong>Mixer</strong> — opacity/fill/crop, defer/commit, effects</td></tr>\n<tr><td><a href=\"#/wiki--api--mixer-cg-scene\" data-wiki-page=\"1\">api/mixer-cg-scene.md</a></td><td>FTB, LED test card, PiP</td></tr>\n<tr><td><a href=\"#/wiki--api--cg\" data-wiki-page=\"1\">&lt;strong&gt;api/cg.md&lt;/strong&gt;</a></td><td><strong>CG</strong> — <code>/api/cg/{command}</code></td></tr>\n<tr><td><a href=\"#/wiki--api--project\" data-wiki-page=\"1\">&lt;strong&gt;api/project.md&lt;/strong&gt;</a></td><td><strong>Project</strong> — save/load/bundle/sync, <strong>USB/bridge volume catalog</strong></td></tr>\n<tr><td><a href=\"#/wiki--api--state-and-media\" data-wiki-page=\"1\">&lt;strong&gt;api/state-and-media.md&lt;/strong&gt;</a></td><td><strong>State</strong>, variables, media, thumbnails</td></tr>\n<tr><td><a href=\"#/wiki--api--project-state-media\" data-wiki-page=\"1\">api/project-state-media.md</a></td><td>Index → project + state pages</td></tr>\n<tr><td><a href=\"#/wiki--api--timelines\" data-wiki-page=\"1\">&lt;strong&gt;api/timelines.md&lt;/strong&gt;</a></td><td><strong>Timelines</strong> — CRUD, play/seek/take, sendTo</td></tr>\n<tr><td><a href=\"#/wiki--api--timelines-multiview\" data-wiki-page=\"1\">api/timelines-multiview.md</a></td><td>Multiview, NDI</td></tr>\n<tr><td><a href=\"#/wiki--api--streaming-audio-osc\" data-wiki-page=\"1\">api/streaming-audio-osc.md</a></td><td>WebRTC streams, RTMP/record, audio routing, OSC</td></tr>\n<tr><td><a href=\"#/wiki--api--system-settings-hardware\" data-wiki-page=\"1\">api/system-settings-hardware.md</a></td><td>Settings, GPU, Caspar config, logs, exFAT sync, device view</td></tr>\n<tr><td><a href=\"#/wiki--api--network-tailscale\" data-wiki-page=\"1\">&lt;strong&gt;api/network-tailscale.md&lt;/strong&gt;</a></td><td><strong>Tailscale</strong> — status, login, operator-monitor UI</td></tr>\n<tr><td><a href=\"#/wiki--api--ingest-usb-plugins\" data-wiki-page=\"1\">api/ingest-usb-plugins.md</a></td><td>Ingest, USB import, plugins, optional modules</td></tr>\n</tbody></table>\n<h2 id=\"operator-guides\">Operator guides</h2>\n<table><thead><tr><th>Page</th><th>Contents</th></tr></thead><tbody>\n<tr><td><a href=\"#/wiki--guides--shader-fx\" data-wiki-page=\"1\">&lt;strong&gt;guides/shader-fx.md&lt;/strong&gt;</a></td><td><strong>Shader FX</strong> — audio-reactive Shadertoy-style shader templates: create, put on air, edit, limits</td></tr>\n</tbody></table>\n<h2 id=\"integration\">Integration</h2>\n<table><thead><tr><th>Page</th><th>Contents</th></tr></thead><tbody>\n<tr><td><a href=\"#/wiki--integration--usb-v4l2-input\" data-wiki-page=\"1\">&lt;strong&gt;integration/usb-v4l2-input.md&lt;/strong&gt;</a></td><td><strong>USB / V4L2</strong> — ATEM, webcams, FFmpeg bridge, operator setup</td></tr>\n</tbody></table>\n<p>Legacy single-file overview (partial): <a href=\"#/api-reference\" data-wiki-page=\"1\">../api-reference.md</a> — prefer the wiki for completeness.</p>","ARCHITECTURE":"<h1 id=\"highascg-architecture-unified-playout-stack\">HighAsCG architecture — unified playout stack</h1>\n<p><strong>Status:</strong> Canonical reference (2026) <strong>Related:</strong> <a href=\"../../client/README.md\">&lt;code&gt;../client/README.md&lt;/code&gt;</a>, <a href=\"../../from_client/AGENT_SERVER_CLIENT_MERGE.md\">&lt;code&gt;../from_client/AGENT_SERVER_CLIENT_MERGE.md&lt;/code&gt;</a>, <a href=\"#/PLAN_SERVER_CLIENT_SPLIT\" data-wiki-page=\"1\">&lt;code&gt;PLAN_SERVER_CLIENT_SPLIT.md&lt;/code&gt;</a> (historical WO-51), <a href=\"../../work/BACKEND_AND_CLIENT_SPLIT.md\">&lt;code&gt;work/BACKEND_AND_CLIENT_SPLIT.md&lt;/code&gt;</a>, <a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">&lt;code&gt;WO47_ISO_VS_EXFAT.md&lt;/code&gt;</a>, <a href=\"#/CASPARCG_BUILD_PROVENANCE\" data-wiki-page=\"1\">&lt;code&gt;CASPARCG_BUILD_PROVENANCE.md&lt;/code&gt;</a> (<strong>which PR/repo every piece of our custom CasparCG binary comes from</strong>), <a href=\"#/ARTNET_PIXEL_MAPPING\" data-wiki-page=\"1\">&lt;code&gt;ARTNET_PIXEL_MAPPING.md&lt;/code&gt;</a> (the three Art-Net/pixel-mapping engines), <a href=\"#/WALKTHROUGH_ARTNET_LED_WALL\" data-wiki-page=\"1\">&lt;code&gt;WALKTHROUGH_ARTNET_LED_WALL.md&lt;/code&gt;</a> (native artnet/pixel consumer walkthrough), <a href=\"#/WALKTHROUGH_PIXELMAP_FIXTURES\" data-wiki-page=\"1\">&lt;code&gt;WALKTHROUGH_PIXELMAP_FIXTURES.md&lt;/code&gt;</a> (live-editable JS pixel-map fixtures)</p>\n<hr>\n<h2 id=\"one-line-model\">One-line model</h2>\n<blockquote><p><strong>One repo, one playout machine:</strong> <code>client/</code> → <code>dist-web/</code> → Node serves API + UI on <code>:4200</code>. Optional Electron launcher (highascg-client) = stick prep / sim / multiserver — not the UI source tree.</p></blockquote>\n<hr>\n<h2 id=\"repository-model-post-merge\">Repository model (post-merge)</h2>\n<p>Everything ships from <strong>this repository</strong> (<a href=\"https://github.com/mko1989/highascg\">highascg</a>):</p>\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody>\n<tr><td><strong><code>client/</code></strong></td><td><strong>Canonical operator UI sources</strong> (dashboard, scenes, device view, timeline, settings)</td></tr>\n<tr><td><strong><code>client/map.html</code></strong></td><td><strong>Interactive Project Map</strong> sources (served at <code>/map</code>)</td></tr>\n<tr><td><strong><code>dist-web/</code></strong></td><td>Vite production build — <strong>what playout serves</strong> at <code>/</code></td></tr>\n<tr><td><strong><code>src/</code></strong>, <strong><code>index.js</code></strong></td><td>Node API bridge (Caspar, OS, WebSocket, config)</td></tr>\n<tr><td><strong><code>client/tools/electron-launcher/</code></strong></td><td>Electron hub sources — packaged separately as <a href=\"https://github.com/mko1989/highascg-client\">&lt;strong&gt;highascg-client&lt;/strong&gt;</a></td></tr>\n</tbody></table>\n<p><strong>highascg-client</strong> is <strong>not</strong> a second UI development repo. It is an <strong>optional Electron packaging extract</strong> (simulator, multiserver workflow, modules such as CG Studio) built from <code>client/tools/electron-launcher/</code> and related tooling. Operators still control playout via the <strong>browser</strong> at <code>http://&lt;playout-ip&gt;:4200/</code>.</p>\n<p><strong>Develop UI here:</strong> edit <code>client/</code> → <code>npm run build:client</code> → refresh browser. <strong>Do not</strong> treat GitHub highascg-client as the canonical UI tree.</p>\n<hr>\n<h2 id=\"what-runs-where\">What runs where</h2>\n<table><thead><tr><th>Component</th><th>Runs on</th><th>In production playout deploy?</th></tr></thead><tbody>\n<tr><td><strong><code>index.js</code> + <code>src/</code></strong></td><td>Playout Linux host</td><td><strong>Yes</strong></td></tr>\n<tr><td><strong><code>dist-web/</code></strong></td><td>Playout Linux host (served by Node)</td><td><strong>Yes</strong> — required for operator UI</td></tr>\n<tr><td><strong><code>config/</code>, <code>template/</code>, <code>scripts/</code></strong></td><td>Playout host</td><td><strong>Yes</strong></td></tr>\n<tr><td><strong><code>tools/runtime/</code></strong></td><td>Playout host</td><td><strong>Yes</strong> (exFAT sync CLI, staged Caspar start)</td></tr>\n<tr><td><strong><code>client/</code></strong> (UI sources)</td><td>Dev / build only</td><td><strong>No</strong> as sources — ship <strong><code>dist-web/</code></strong></td></tr>\n<tr><td><strong>Electron hub</strong> (<a href=\"https://github.com/mko1989/highascg-client\">highascg-client</a>)</td><td>Optional Mac/Windows/Linux</td><td><strong>No</strong> on playout — sim, multiserver, stick prep; opens browser to <code>:4200</code></td></tr>\n<tr><td><strong><code>work/</code></strong></td><td>Developers only</td><td><strong>No</strong></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"connection-diagram\">Connection diagram</h2>\n<pre><code class=\"language-mermaid\">flowchart TB\n  subgraph playout[&quot;Playout machine (Linux) — this repo&quot;]\n    Browser[&quot;Browser\\nhttp://host:4200/&quot;]\n    SVC[&quot;highascg.service\\nAPI + dist-web :4200&quot;]\n    Caspar[&quot;casparcg-server\\nAMCP :5250&quot;]\n    OS[&quot;Ubuntu OS\\nGPU xrandr exFAT USB&quot;]\n    Build[&quot;npm run build:client\\nclient/ → dist-web/&quot;]\n    Build -.-&gt; SVC\n    Browser --&gt;|&quot;same origin\\n/api /api/ws&quot;| SVC\n    SVC --&gt;|&quot;AMCP&quot;| Caspar\n    SVC --&gt;|&quot;shell APIs sudo&quot;| OS\n  end\n\n  subgraph optional[&quot;Optional — highascg-client Electron&quot;]\n    EL[&quot;Hub · sim · multiserver · modules&quot;]\n    EL --&gt;|&quot;opens system browser&quot;| Browser\n  end\n\n  LAN[&quot;LAN browser&quot;] --&gt;|&quot;http://playout-ip:4200/&quot;| SVC</code></pre>\n<hr>\n<h2 id=\"server-responsibilities-bridge\">Server responsibilities (bridge)</h2>\n<table><thead><tr><th>Direction</th><th>Examples</th></tr></thead><tbody>\n<tr><td><strong>→ CasparCG</strong></td><td>Scene take, mixer, playlists, multiview, global border AMCP, <code>casparcg.config</code> generation, template sync</td></tr>\n<tr><td><strong>← CasparCG</strong></td><td>OSC state, INFO/CLS polling, playback tracker</td></tr>\n<tr><td><strong>→ OS</strong></td><td><code>xrandr</code> layout, NVIDIA driver apply (incl. <strong>Sync to VBlank off</strong> for screen consumers — see <a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">reference/screen-consumer-vsync-nvidia.md</a>), exFAT mtime sync, USB mount/ingest, systemd unit writes</td></tr>\n<tr><td><strong>↔ UI (browser)</strong></td><td>Settings persistence, device graph, project/scenes JSON, WebSocket state broadcast</td></tr>\n<tr><td><strong>→ Browser</strong></td><td>Serve <strong><code>dist-web/</code></strong> SPA, <code>/templates/</code>, optional <code>/vendor/</code></td></tr>\n</tbody></table>\n<p>With <strong><code>HIGHASCG_HEADLESS=true</code></strong> (debug/API-only only), non-API HTTP returns 404 JSON and <strong><code>dist-web/</code></strong> is not served. <strong>Production default:</strong> headless <strong>off</strong>.</p>\n<hr>\n<h2 id=\"ui-responsibilities-browser-spa\">UI responsibilities (browser SPA)</h2>\n<table><thead><tr><th>Area</th><th>Browser UI (<code>dist-web/</code> from <code>client/</code>)</th><th>Server (<code>src/</code>)</th></tr></thead><tbody>\n<tr><td>Panels, drag-drop, forms</td><td>Yes</td><td>Validates + executes</td></tr>\n<tr><td>Live state</td><td>WS subscribe, local mirrors</td><td>Owns truth, broadcasts</td></tr>\n<tr><td>Scene edit UX</td><td>Local edit, save via API</td><td>AMCP take sequences</td></tr>\n<tr><td>Settings UI</td><td>Collect payloads</td><td>Merge config, restart services</td></tr>\n<tr><td>Media browser tree</td><td>Thumbnails, tree UI</td><td>Disk scan, ffmpeg probe</td></tr>\n<tr><td>Preview video</td><td>WebRTC <code>&lt;video&gt;</code></td><td>Caspar/FFmpeg consumers</td></tr>\n</tbody></table>\n<p>No direct AMCP from the browser (except dev tooling). No OSC UDP, no systemd, no GPU driver install in the SPA.</p>\n<hr>\n<h2 id=\"build-deploy-workflow\">Build &amp; deploy workflow</h2>\n<pre><code class=\"language-bash\"># On playout or dev machine (repo root)\nnpm install\nnpm run build:client    # client/ → dist-web/\nnpm start               # :4200 serves API + dist-web/</code></pre>\n<p><strong>UI dev with HMR:</strong> <code>npm run dev:client</code> (<code>:4350</code> → API on <code>:4200</code>).</p>\n<p><strong>Deploy:</strong> <code>npm run deploy:dev</code> includes <strong><code>dist-web/</code></strong> when present (see <a href=\"../../scripts/deploy/dev-push.sh\">&lt;code&gt;scripts/deploy/dev-push.sh&lt;/code&gt;</a>).</p>\n<p>When counting lines or auditing “server code”, exclude <strong><code>work/</code></strong>, <strong><code>tools/eggs/</code></strong>, and <strong><code>cef-cache/</code></strong>. UI sources in <strong><code>client/</code></strong> are part of this program but not deployed as raw sources on playout.</p>\n<hr>\n<h2 id=\"server-appctx-and-state-ownership-wo-100\">Server <code>appCtx</code> and state ownership (WO-100)</h2>\n<p>The Node process builds one <strong><code>appCtx</code></strong> via <code>src/app-context.js</code> → <code>createAppContext()</code> (wired from <code>index.js</code>). Subsystems receive this bag; core references (<code>config</code>, <code>state</code>, <code>persistence</code>, <code>configManager</code>) are sealed after boot.</p>\n<table><thead><tr><th>Data</th><th>Owner</th><th>Notes</th></tr></thead><tbody>\n<tr><td>Media / template catalog</td><td><code>StateManager</code> (<code>src/state/state-manager.js</code>)</td><td>CLS/TLS → <code>updateFromCLS</code> / <code>updateFromTLS</code> only</td></tr>\n<tr><td><code>CHOICES_MEDIAFILES</code> / <code>CHOICES_TEMPLATES</code> on <code>appCtx</code></td><td><strong>Getters</strong> over <code>StateManager</code></td><td>Legacy readers; do not mutate</td></tr>\n<tr><td>Program scene map</td><td><code>live-scene-state.js</code></td><td>Serialized RMW via <code>async-serial-queue</code></td></tr>\n<tr><td><code>programLayerBankByChannel</code>, <code>sceneDeck</code></td><td><code>LiveDeckState</code> (<code>src/state/live-deck-state.js</code>) on <code>appCtx.liveDeck</code></td><td>Back-compat via <code>appCtx</code> getters/setters; persist via module API</td></tr>\n<tr><td>Operator snapshot</td><td><code>get-state.js</code></td><td>Merges <code>appCtx</code> + <code>StateManager</code> at request time</td></tr>\n</tbody></table>\n<pre><code class=\"language-mermaid\">flowchart LR\n  CLS[AMCP CLS/TLS] --&gt; SM[StateManager]\n  SM --&gt; GET[get-state / WS]\n  appCtx[appCtx getters] --&gt; GET\n  take[scene take] --&gt; LSS[live-scene-state]\n  LSS --&gt; persistence[(persistence)]</code></pre>\n<hr>\n<h2 id=\"distribution\">Distribution</h2>\n<table><thead><tr><th>Artifact</th><th>Contents</th></tr></thead><tbody>\n<tr><td><strong>Playout ISO squashfs</strong></td><td>Caspar shell, systemd, drivers — often <strong>no</strong> full <code>src/</code> until exFAT bootstrap</td></tr>\n<tr><td><strong>exFAT <code>drop-update/</code></strong></td><td><code>highascg-server_*.tar.gz</code> — <strong><code>index.js</code>, <code>src/</code>, <code>scripts/</code>, <code>tools/runtime/</code>, <code>dist-web/</code></strong></td></tr>\n<tr><td><strong>UI-only hotfix</strong></td><td>rsync <strong><code>dist-web/</code></strong> only, or rebuild on playout with <code>npm run build:client</code></td></tr>\n</tbody></table>\n<p>Server + UI updates can land via <strong><code>drop-update/</code></strong> without reflashing the ISO. Operator entry URL: <strong><code>http://&lt;playout-ip&gt;:4200/</code></strong>.</p>\n<hr>\n<h2 id=\"historical-note-wo-51-wo-52\">Historical note (WO-51 → WO-52)</h2>\n<p>WO-51 ran the API headless on playout and hosted the UI in Electron on <code>:4350</code>. <strong>Current model:</strong> UI is built from in-repo <strong><code>client/</code></strong> and served on playout <strong><code>:4200</code></strong>. See <a href=\"#/PLAN_SERVER_CLIENT_SPLIT\" data-wiki-page=\"1\">&lt;code&gt;PLAN_SERVER_CLIENT_SPLIT.md&lt;/code&gt;</a> for the old plan only.</p>","PLAN_SERVER_CLIENT_SPLIT":"<h1 id=\"plan-permanent-server-client-split\">Plan: Permanent server / client split</h1>\n<blockquote><p><strong>Superseded for production (2026):</strong> <strong>One unified repo</strong> — <strong><code>client/</code></strong> (canonical UI) + <strong><code>src/</code></strong> (API) on the <strong>same playout machine</strong>. Node serves <strong>API + web GUI</strong> (<code>dist-web/</code>) on <strong><code>:4200</code></strong>. <a href=\"https://github.com/mko1989/highascg-client\">&lt;strong&gt;highascg-client&lt;/strong&gt;</a> is <strong>optional Electron packaging only</strong> (simulator, multiserver, modules) — not the UI source tree. This document records the <strong>WO-51 headless + remote Electron</strong> plan only. <strong>Current model:</strong> <a href=\"#/ARCHITECTURE\" data-wiki-page=\"1\">&lt;code&gt;ARCHITECTURE.md&lt;/code&gt;</a> · <a href=\"../../client/README.md\">&lt;code&gt;../client/README.md&lt;/code&gt;</a>.</p></blockquote>\n<p><strong>Status:</strong> Historical (WO-51 shipped; WO-52 merged UI onto playout) <strong>Goal (original):</strong> Node server = bridge via API/WS; UI on operator workstation. <strong>WO-52 update:</strong> UI is <strong><code>dist-web/</code></strong> on playout <code>:4200</code>; Electron is hub-only.</p>\n<p><strong>Canonical reference:</strong> <a href=\"#/ARCHITECTURE\" data-wiki-page=\"1\">&lt;code&gt;ARCHITECTURE.md&lt;/code&gt;</a></p>\n<p><strong>Related:</strong> <a href=\"../../work/BACKEND_AND_CLIENT_SPLIT.md\">&lt;code&gt;work/BACKEND_AND_CLIENT_SPLIT.md&lt;/code&gt;</a>, WO‑51 <a href=\"../../work/work-orders/51_WO_DECOUPLED_FRONTEND_ARCHITECTURE.md\">&lt;code&gt;work/work-orders/51_WO_DECOUPLED_FRONTEND_BACKEND_ARCHITECTURE.md&lt;/code&gt;</a>, <a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">&lt;code&gt;docs/WO47_ISO_VS_EXFAT.md&lt;/code&gt;</a>, <a href=\"../../client/tools/electron-launcher/\">&lt;code&gt;client/tools/electron-launcher/&lt;/code&gt;</a>.</p>\n<hr>\n<h2 id=\"1-target-architecture\">1. Target architecture</h2>\n<pre><code class=\"language-mermaid\">flowchart TB\n  subgraph playout[&quot;Playout machine (Linux ISO / rack)&quot;]\n    Caspar[&quot;casparcg-server-2.5\\nOpenbox autostart&quot;]\n    Svc[&quot;highascg.service\\nnode index.js&quot;]\n    Svc --&gt;|&quot;AMCP :5250&quot;| Caspar\n  end\n\n  subgraph operator[&quot;Operator machine (Mac / Windows / Linux)&quot;]\n    EL[&quot;Electron launcher\\nclient/tools/electron-launcher&quot;]\n    UI[&quot;Web UI\\ndist-web/ in app bundle&quot;]\n    EL --&gt;|&quot;serves static&quot;| UI\n    EL --&gt;|&quot;optional child:\\nlocal sim&quot;| SvcSim[&quot;node index.js\\n--no-caspar&quot;]\n  end\n\n  UI --&gt;|&quot;HTTP /api/*\\nWebSocket /api/ws&quot;| Svc\n  UI -.-&gt;|&quot;sim only&quot;| SvcSim\n  EL --&gt;|&quot;production:\\nno local server&quot;| Svc</code></pre>\n<table><thead><tr><th>Role</th><th>Process</th><th>Hosts</th><th>Does <strong>not</strong> do</th></tr></thead><tbody>\n<tr><td><strong>Server (bridge)</strong></td><td><code>node index.js</code> (<code>highascg.service</code> on playout)</td><td>REST <code>/api/<em></code>, WS <code>/api/ws</code>, <code>/templates/</em></code>, OS/GPU hooks</td><td>Serve operator UI (<code>client/</code>, <code>dist-web/</code>, Electron)</td></tr>\n<tr><td><strong>Client</strong></td><td>Static SPA inside <strong>Electron</strong> on operator laptop</td><td>Scene editor, settings forms, device view</td><td>AMCP, Caspar XML, OSC UDP, systemd, GPU drivers</td></tr>\n<tr><td><strong>Launcher</strong></td><td>Electron <code>main.js</code> (highascg-client)</td><td>Stick prep, sim, embedded <code>dist-web/</code>, API base URL</td><td>Playout (except optional local sim child)</td></tr>\n</tbody></table>\n<p><strong>Connection rule:</strong> The browser/Electron renderer always knows <strong><code>HIGHASCG_API_ORIGIN</code></strong> (e.g. <code>http://192.168.0.42:4200</code>). All API, WS, <code>/templates/</code>, and <code>/vendor/</code> URLs are resolved against that origin—not <code>location.origin</code> of the UI page.</p>\n<hr>\n<h2 id=\"2-why-decision-record\">2. Why (decision record)</h2>\n<table><thead><tr><th>Concern</th><th>Same-origin server UI</th><th>Split (this plan)</th></tr></thead><tbody>\n<tr><td>GPU on playout</td><td>Browser + Caspar compete</td><td>Caspar-only on playout</td></tr>\n<tr><td>ISO size</td><td><code>dist-web/</code> + <code>node_modules</code> bloat</td><td>Server tarball only</td></tr>\n<tr><td>Update cadence</td><td>Reflash ISO for UI tweaks</td><td>Client release without touching playout</td></tr>\n<tr><td>Operator workflow</td><td>Keyboard/mouse on playout head</td><td>Control from laptop (Electron)</td></tr>\n<tr><td>Security</td><td>Wide HTTP static surface on playout</td><td>API surface only (<code>HIGHASCG_HEADLESS</code>)</td></tr>\n</tbody></table>\n<p>WO‑47 <strong>exFAT <code>update/server/</code></strong> already assumes server-only drops; this plan makes <strong>headless the default on ISO</strong> and moves UI hosting into <strong>Electron</strong> instead of “open browser to playout :4200”.</p>\n<hr>\n<h2 id=\"3-environment-and-configuration-matrix\">3. Environment and configuration matrix</h2>\n<h3 id=\"31-server-playout-sim-child-process\">3.1 Server (playout + sim child process)</h3>\n<table><thead><tr><th>Variable</th><th>Where set</th><th>Values</th><th>Effect</th></tr></thead><tbody>\n<tr><td><strong><code>HIGHASCG_HEADLESS</code></strong></td><td><strong>Required</strong> on playout: systemd drop-in <code>/etc/systemd/system/highascg.service.d/10-headless.conf</code></td><td><code>true</code> \\</td><td><code>1</code> \\</td><td><code>yes</code> (normalize in code)</td><td>Non-API HTTP → 404 JSON; no <code>serveWebApp</code></td></tr>\n<tr><td><strong><code>NODE_ENV</code></strong></td><td>systemd</td><td><code>production</code></td><td>Standard Node</td></tr>\n<tr><td><strong><code>HTTP_PORT</code></strong> / <strong><code>PORT</code></strong></td><td>systemd / <code>config/server.json</code></td><td>default <strong><code>4200</code></strong> (<code>src/config/defaults.js</code>)</td><td>API listen port</td></tr>\n<tr><td><strong><code>BIND_ADDRESS</code></strong></td><td>systemd / config</td><td><code>0.0.0.0</code> on playout</td><td>API bind</td></tr>\n<tr><td><strong><code>WS_PORT</code></strong></td><td>config (future split)</td><td>today same as HTTP</td><td>Reserved WO‑51</td></tr>\n<tr><td><strong><code>HIGHASCG_CONFIG_PATH</code></strong></td><td>optional</td><td>path to monolithic JSON</td><td>Overrides modular <code>config/</code></td></tr>\n<tr><td><strong><code>HIGHASCG_EXFAT_SYNC_MAP</code></strong></td><td><code>/etc/highascg/exfat-sync.json</code></td><td>path</td><td>WO‑47 sync</td></tr>\n<tr><td><strong><code>CASPAR_CONFIG_PATH</code></strong></td><td>rare override</td><td>path</td><td>Caspar XML path</td></tr>\n<tr><td>*<em><code>HIGHASCG_WS_</em></code><strong>, </strong><code>HIGHASCG_OSC_<em></code></em>*, etc.</td><td>env</td><td>see README</td><td>Unchanged</td></tr>\n</tbody></table>\n<p><strong>Remove / deprecate on server:</strong></p>\n<table><thead><tr><th>Variable</th><th>Action</th></tr></thead><tbody>\n<tr><td><strong><code>HIGHASCG_WEB_DIR</code></strong></td><td>Deprecate; ignored when <code>HIGHASCG_HEADLESS</code> (eventually delete <code>resolveWebDir</code> from <code>index.js</code>)</td></tr>\n<tr><td><strong><code>HIGHASCG_ISO_BUILD_WEB</code></strong></td><td>Remove from <code>install-iso-defaults.sh</code> after split (no <code>dist-web</code> on ISO)</td></tr>\n<tr><td>Default <strong><code>HIGHASCG_ISO_EMBED_SERVER=1</code></strong></td><td>Keep embedding <strong><code>src/</code></strong>, <strong><code>node_modules/</code></strong>, <strong><code>package.json</code></strong>; never embed <strong><code>dist-web/</code></strong></td></tr>\n</tbody></table>\n<p><strong>systemd (playout) — new drop-in:</strong></p>\n<pre><code class=\"language-ini\"># /etc/systemd/system/highascg.service.d/10-headless.conf\n[Service]\nEnvironment=HIGHASCG_HEADLESS=true</code></pre>\n<p>Update <strong><code>scripts/write-highascg-systemd-unit.sh</code></strong> to install this drop-in by default.</p>\n<h3 id=\"32-client-vite-build-electron\">3.2 Client (Vite build + Electron)</h3>\n<table><thead><tr><th>Variable</th><th>Where set</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><strong><code>VITE_HIGHASCG_API_ORIGIN</code></strong></td><td><code>.env.production</code>, Electron build, CI</td><td>Build-time default API origin (e.g. <code>http://127.0.0.1:4200</code>)</td></tr>\n<tr><td><strong><code>window.__HIGHASCG_API_ORIGIN__</code></strong></td><td>Electron <code>preload.js</code> at runtime</td><td><strong>Overrides</strong> Vite default when operator sets “Server IP”</td></tr>\n<tr><td><strong><code>VITE_HIGHASCG_WS_ORIGIN</code></strong></td><td>optional</td><td>Explicit WS origin if ever split from HTTP</td></tr>\n</tbody></table>\n<p><strong>New module:</strong> <code>client/lib/api-origin.js</code> (single source of truth):</p>\n<pre><code class=\"language-js\">// Pseudocode — implement in Phase 2\nexport function getApiOrigin() {\n  const runtime = globalThis.__HIGHASCG_API_ORIGIN__\n  if (runtime) return String(runtime).replace(/\\/$/, '')\n  const built = import.meta.env.VITE_HIGHASCG_API_ORIGIN\n  if (built) return String(built).replace(/\\/$/, '')\n  return '' // '' =&gt; legacy same-origin relative /api (dev only)\n}\nexport function getApiBase() { /* Companion /instance/ prefix on pathname OR api origin */ }\nexport function getWsUrl() { /* ws(s) from getApiOrigin() + /api/ws */ }\nexport function assetUrl(path) { /* getApiOrigin() + /vendor/... /templates/... */ }</code></pre>\n<p>Refactor <strong><code>client/lib/api-client.js</code></strong> and <strong><code>client/lib/ws-client.js</code></strong> to use <code>api-origin.js</code>.</p>\n<p><strong>Electron launcher (<code>client/tools/electron-launcher/</code>):</strong></p>\n<table><thead><tr><th>Variable</th><th>Set by</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><strong><code>HIGHASCG_API_ORIGIN</code></strong></td><td>main → preload → renderer</td><td>Target playout API (production connect)</td></tr>\n<tr><td><strong><code>HIGHASCG_UI_ROOT</code></strong></td><td>main (packaged <code>dist-web/</code>)</td><td>Path to static UI inside <code>.asar</code> / <code>resources/</code></td></tr>\n<tr><td><strong><code>HIGHASCG_LAUNCH_MODE</code></strong></td><td>main</td><td><code>connect</code> \\</td><td><code>sim</code> \\</td><td><code>prep-only</code></td></tr>\n<tr><td><strong><code>HIGHASCG_LAUNCH_NO_BROWSER</code></strong></td><td>main</td><td><code>1</code> — Electron hosts UI; do not <code>xdg-open</code></td></tr>\n<tr><td><strong><code>HIGHASCG_OFFLINE_MODE</code></strong></td><td>sim only</td><td>passed to child <code>index.js</code></td></tr>\n<tr><td><strong><code>HTTP_PORT</code></strong></td><td>sim child env</td><td>local headless server port</td></tr>\n</tbody></table>\n<p><strong>Deprecate for production UI path:</strong></p>\n<table><thead><tr><th>Variable</th><th>Action</th></tr></thead><tbody>\n<tr><td>Opening external browser to <code>http://{ip}:{port}/</code></td><td>Replace with <strong>Electron <code>BrowserWindow</code></strong> loading packaged UI + <code>HIGHASCG_API_ORIGIN</code></td></tr>\n</tbody></table>\n<h3 id=\"33-dev-workstation-repo-checkout\">3.3 Dev workstation (repo checkout)</h3>\n<table><thead><tr><th>Workflow</th><th>Server</th><th>Client UI</th></tr></thead><tbody>\n<tr><td><strong>Split dev (canonical)</strong></td><td><code>HIGHASCG_HEADLESS=true npm start</code></td><td><code>npm run dev:client</code> (Vite <code>:3000</code>)</td></tr>\n<tr><td><strong>Vite proxy</strong></td><td>Fix <strong><code>vite.config.js</code></strong> proxy target → <strong><code>http://localhost:4200</code></strong> (today wrongly <code>8000</code>)</td><td>Proxies <code>/api</code>, <code>/ws</code>, <code>/vendor</code>, <code>/templates</code></td></tr>\n<tr><td><strong>Legacy monolith</strong></td><td><code>npm start</code> without headless</td><td>Same port serves UI — <strong>dev-only</strong>, document as deprecated</td></tr>\n</tbody></table>\n<h3 id=\"34-cors-server\">3.4 CORS (server)</h3>\n<p>Today: <code>Access-Control-Allow-Origin: *</code> in <code>src/server/cors.js</code>.</p>\n<table><thead><tr><th>Variable</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><strong><code>HIGHASCG_CORS_ORIGINS</code></strong></td><td>Comma-separated allowed origins (e.g. <code>http://127.0.0.1:3000,file://,app://</code>)</td></tr>\n<tr><td>Default when headless</td><td><code>http://localhost:3000</code>, Electron <code>file://</code> / custom protocol</td></tr>\n</tbody></table>\n<p>Required once UI origin ≠ API origin.</p>\n<hr>\n<h2 id=\"4-path-and-artifact-layout-canonical\">4. Path and artifact layout (canonical)</h2>\n<h3 id=\"41-repository-development\">4.1 Repository (development)</h3>\n<table><thead><tr><th>Path</th><th>Role after split</th></tr></thead><tbody>\n<tr><td><code>index.js</code>, <code>src/</code></td><td>Server only</td></tr>\n<tr><td><code>package.json</code> (root)</td><td>Server deps + Vite <strong>devDependencies</strong></td></tr>\n<tr><td><code>client/</code></td><td>UI sources</td></tr>\n<tr><td><code>dist-web/</code></td><td>Vite output — <strong>client artifact</strong>, packaged into Electron, <strong>not</strong> deployed to playout</td></tr>\n<tr><td><code>config/casparcg.config.iso</code></td><td>ISO Caspar default (unchanged)</td></tr>\n<tr><td><code>tools/runtime/</code></td><td>On playout via server tarball</td></tr>\n<tr><td><code>tools/eggs/</code></td><td>Build host only</td></tr>\n<tr><td><code>client/tools/electron-launcher/</code></td><td>Operator app + <strong>bundled <code>dist-web/</code></strong></td></tr>\n</tbody></table>\n<h3 id=\"42-playout-machine-homecasparcghighascg\">4.2 Playout machine (<code>/home/casparcg/highascg</code>)</h3>\n<table><thead><tr><th>Path</th><th>On ISO / after install</th></tr></thead><tbody>\n<tr><td><code>index.js</code>, <code>src/</code>, <code>scripts/</code>, <code>tools/runtime/</code>, <code>node_modules/</code>, <code>package.json</code></td><td>Yes (embed or exFAT <code>update/server/</code>)</td></tr>\n<tr><td><code>config/casparcg.config</code></td><td>Yes (from <code>casparcg.config.iso</code> on prepare)</td></tr>\n<tr><td><code>template/</code>, <code>lib/</code>, <code>media/</code>, stubs</td><td>Yes</td></tr>\n<tr><td><code>dist-web/</code>, <code>client/</code></td><td><strong>No</strong></td></tr>\n<tr><td><code>highascg.service</code> + <code>10-headless.conf</code></td><td>Yes</td></tr>\n</tbody></table>\n<h3 id=\"43-exfat-stick-highascgexf\">4.3 exFAT stick (<code>HIGHASCGEXF</code>)</h3>\n<table><thead><tr><th>Path</th><th>Contents</th></tr></thead><tbody>\n<tr><td><code>update/server/</code></td><td><code>highascg-server_*.tar.gz</code> extract — <strong>backend only</strong></td></tr>\n<tr><td><code>sim/highascg/</code></td><td><strong>Deprecated for Linux playout</strong>; optional Win/Mac legacy sim tree (server only, no UI)</td></tr>\n<tr><td><code>media/</code>, <code>templates/</code>, <code>configs/</code>, <code>drop-config/</code></td><td>Unchanged</td></tr>\n</tbody></table>\n<p><strong>Client on stick:</strong> Not required if Electron ships UI. Optional future: <code>sim/client/dist-web/</code> for air-gapped extract-only workflows.</p>\n<h3 id=\"44-github-releases\">4.4 GitHub releases</h3>\n<table><thead><tr><th>Script</th><th>Artifact</th><th>Contents</th></tr></thead><tbody>\n<tr><td><code>npm run release:github-server</code></td><td><code>highascg-server_*.tar.gz</code></td><td><code>index.js</code>, <code>src/</code>, <code>scripts/</code>, <code>tools/runtime/</code>, <code>config/</code>, <code>package.json</code>, <code>node_modules/</code> (optional) — <strong>no <code>dist-web/</code></strong></td></tr>\n<tr><td><code>npm run release:github-client</code></td><td><code>highascg-client_*.tar.gz</code></td><td><code>dist-web/</code> only</td></tr>\n<tr><td><strong>New:</strong> <code>npm run release:launcher</code> (TBD)</td><td><code>HighAsCG-Launcher_*.{dmg,exe,AppImage}</code></td><td>Electron + embedded <code>dist-web/</code> + version pin file</td></tr>\n</tbody></table>\n<h3 id=\"45-eggs-iso-excludes-after-split\">4.5 Eggs / ISO excludes (after split)</h3>\n<p>Use <strong><code>penguins-eggs-exclude-highascg-fragment.list</code></strong> (WO‑47 server omit) <strong>plus</strong> always exclude:</p>\n<pre><code>home/casparcg/highascg/dist-web\nhome/casparcg/highascg/dist-web/*</code></pre>\n<p>Revert default <strong><code>HIGHASCG_ISO_EMBED_SERVER</code></strong> to embed <strong>server only</strong> (no <code>npm run build:client</code> in <code>install-iso-defaults.sh</code>).</p>\n<hr>\n<h2 id=\"5-electron-launcher-design-ui-host\">5. Electron launcher design (UI host)</h2>\n<h3 id=\"51-process-model\">5.1 Process model</h3>\n<table><thead><tr><th>Mode</th><th>Child processes</th><th>UI window</th></tr></thead><tbody>\n<tr><td><strong>Connect to playout</strong></td><td>None (or optional health-ping script)</td><td><code>BrowserWindow</code> → <code>file://</code> / <code>app://</code> bundled <code>dist-web/index.html</code> with <code>preload</code> setting <code>__HIGHASCG_API_ORIGIN__</code> = <code>http://{Server IP}:{port}</code></td></tr>\n<tr><td><strong>Local simulation</strong></td><td><code>node launch-sim-from-exfat.js</code> (headless server on laptop)</td><td>Same window; API origin <code>http://127.0.0.1:{simPort}</code></td></tr>\n<tr><td><strong>Prep / flash tabs</strong></td><td>None</td><td>Existing launcher chrome only</td></tr>\n</tbody></table>\n<p><strong>Remove:</strong> <code>btn-open-webui</code> → external system browser for production path (keep as advanced fallback behind “Open in browser”).</p>\n<h3 id=\"52-packaging-layout-electron-builder\">5.2 Packaging layout (Electron builder)</h3>\n<pre><code>HighAsCG-Launcher.app/\n  resources/\n    app/dist-web/          # from npm run build:client\n    app/preload.js         # sets __HIGHASCG_API_ORIGIN__\n    server-stub/           # optional: pin compatible server version text only</code></pre>\n<p><strong>Do not</strong> bundle full <code>node_modules</code> for playout server inside Electron (size, duplication). Sim mode spawns server from <code>sim/highascg</code> on stick or dev repo.</p>\n<h3 id=\"53-ui-api-wiring\">5.3 UI ↔ API wiring</h3>\n<ol>\n<li>Operator enters <strong>Server IP</strong> + <strong>port</strong> (default <strong>4200</strong>).</li>\n<li>On <strong>Connect</strong>, main process stores <code>HIGHASCG_API_ORIGIN</code>, opens app window, preload injects origin.</li>\n<li>Renderer <code>fetch(getApiOrigin() + '/api/state')</code>, <code>new WebSocket(getWsUrl())</code>.</li>\n<li><strong>Import map</strong> in <code>client/index.html</code> must use <strong>runtime-generated</strong> vendor URLs:</li>\n</ol>\n<pre><code class=\"language-html\">&lt;!-- Today (same-origin only): --&gt;\n&quot;three&quot;: &quot;/vendor/three/build/three.module.js&quot;\n&lt;!-- After split: generated at load from getApiOrigin() + '/vendor/three/...' --&gt;</code></pre>\n<p>Implement via small inline script in <code>index.html</code> or Vite plugin <code>transformIndexHtml</code>.</p>\n<h3 id=\"54-files-to-change-launcher\">5.4 Files to change (launcher)</h3>\n<table><thead><tr><th>File</th><th>Change</th></tr></thead><tbody>\n<tr><td><code>client/tools/electron-launcher/main.js</code></td><td>Add <code>createAppWindow()</code>, <code>protocol</code>/<code>preload</code>, pack <code>dist-web</code> path</td></tr>\n<tr><td><code>client/tools/electron-launcher/preload.js</code></td><td><strong>New</strong> — expose <code>setApiOrigin</code>, read env</td></tr>\n<tr><td><code>client/tools/electron-launcher/renderer.js</code></td><td>“Connect” starts app window; sim sets local origin</td></tr>\n<tr><td><code>client/tools/electron-launcher/package.json</code></td><td>electron-builder config, <code>extraResources: dist-web</code></td></tr>\n<tr><td><code>package.json</code></td><td><code>&quot;launcher:package&quot;: &quot;...&quot;</code> script</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"6-server-code-changes\">6. Server code changes</h2>\n<table><thead><tr><th>File</th><th>Change</th></tr></thead><tbody>\n<tr><td><code>src/server/http-server.js</code></td><td>Treat <code>HIGHASCG_HEADLESS</code> as true for <code>1</code>/<code>yes</code>; skip <code>webDir</code> branch entirely when headless</td></tr>\n<tr><td><code>index.js</code></td><td>Do not call <code>resolveWebDir</code> when headless; log <code>API-only</code></td></tr>\n<tr><td><code>src/repo-paths.js</code></td><td>Mark <code>resolveWebDir</code> <strong>deprecated</strong>; dev-only guard</td></tr>\n<tr><td><code>scripts/write-highascg-systemd-unit.sh</code></td><td>Write <code>highascg.service.d/10-headless.conf</code></td></tr>\n<tr><td><code>tools/eggs/live-usb/install-iso-defaults.sh</code></td><td>Remove <code>build:client</code>; only Caspar config + <code>npm ci --omit=dev</code> for server</td></tr>\n<tr><td><code>tools/eggs/live-usb/prepare-eggs-clone-with-exfat.sh</code></td><td>Default <code>HIGHASCG_ISO_BUILD_WEB=0</code>; document headless</td></tr>\n<tr><td><code>tools/release/make-github-release-server.sh</code></td><td>Notes: never bundle UI; require client/launcher</td></tr>\n<tr><td><code>README.md</code>, <code>docs/DEV_RELEASE_GITHUB.md</code>, <code>docs/ISO_CONTENTS.md</code></td><td>Port <strong>4200</strong>, headless default, Electron operator path</td></tr>\n</tbody></table>\n<p><strong>Optional hardening (Phase 4):</strong> Remove <code>serveWebApp</code> code paths behind <code>NODE_ENV === 'development'</code> flag so production builds cannot accidentally serve UI.</p>\n<hr>\n<h2 id=\"7-client-code-changes\">7. Client code changes</h2>\n<h3 id=\"71-api-ws-assets-critical\">7.1 API / WS / assets (critical)</h3>\n<table><thead><tr><th>File</th><th>Change</th></tr></thead><tbody>\n<tr><td><strong><code>client/lib/api-origin.js</code></strong></td><td><strong>New</strong> — <code>getApiOrigin</code>, <code>assetUrl</code></td></tr>\n<tr><td><code>client/lib/api-client.js</code></td><td><code>getApiBase()</code> = Companion prefix <strong>or</strong> <code>${getApiOrigin()}</code></td></tr>\n<tr><td><code>client/lib/ws-client.js</code></td><td><code>getWsUrl()</code> from API origin</td></tr>\n<tr><td><code>client/lib/thumbnail-url.js</code></td><td>Already uses <code>getApiBase()</code> — verify after refactor</td></tr>\n<tr><td><code>client/index.html</code></td><td>Dynamic import map for <code>/vendor/*</code></td></tr>\n<tr><td><code>client/components/sources-panel-helpers.js</code></td><td>Replace <code>location.origin</code> + <code>/templates/...</code> with <code>assetUrl('/templates/...')</code></td></tr>\n<tr><td><code>client/components/publish-modal.js</code></td><td><code>myOrigin</code> → API origin for ingest preview</td></tr>\n<tr><td>Grep targets</td><td><code>/vendor/</code>, <code>/templates/</code>, <code>location.origin</code> in <code>client/</code></td></tr>\n</tbody></table>\n<h3 id=\"72-vite\">7.2 Vite</h3>\n<table><thead><tr><th>File</th><th>Change</th></tr></thead><tbody>\n<tr><td><code>vite.config.js</code></td><td>Proxy → <code>http://127.0.0.1:4200</code>; add <code>define</code> for optional env</td></tr>\n<tr><td><code>.env.development</code></td><td><strong>New</strong> — <code>VITE_HIGHASCG_API_ORIGIN=http://127.0.0.1:4200</code></td></tr>\n<tr><td><code>.env.production</code></td><td><strong>New</strong> — empty or placeholder; Electron injects at runtime</td></tr>\n</tbody></table>\n<h3 id=\"73-build-outputs\">7.3 Build outputs</h3>\n<table><thead><tr><th>Output</th><th>Consumer</th></tr></thead><tbody>\n<tr><td><code>dist-web/</code></td><td>Electron <code>extraResources</code> only</td></tr>\n<tr><td><code>highascg-client_*.tar.gz</code></td><td>Manual extract / CI; optional stick <code>sim/client/</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"8-cross-origin-and-vendor-templates\">8. Cross-origin and <code>/vendor</code> / <code>/templates</code></h2>\n<p>Caspar HTML templates and previs/CG-studio depend on server-mounted paths:</p>\n<table><thead><tr><th>URL (on API server)</th><th>Served by</th><th>Client must use</th></tr></thead><tbody>\n<tr><td><code>/api/*</code></td><td><code>src/api/router.js</code></td><td><code>{API_ORIGIN}/api/...</code></td></tr>\n<tr><td><code>/api/ws</code></td><td><code>ws-server.js</code></td><td><code>ws://{host}:{port}/api/ws</code></td></tr>\n<tr><td><code>/templates/*</code></td><td><code>http-server.js</code> <code>templatesDir</code></td><td><code>{API_ORIGIN}/templates/...</code></td></tr>\n<tr><td><code>/vendor/three/<em></code>, <code>/vendor/grapesjs/</em></code></td><td><code>vendorDirs</code> in <code>index.js</code></td><td><code>{API_ORIGIN}/vendor/...</code></td></tr>\n</tbody></table>\n<p><strong>Smoke test (new):</strong> Headless server + Vite dev UI on <code>:3000</code> — verify previs module loads Three from API origin.</p>\n<hr>\n<h2 id=\"9-implementation-phases\">9. Implementation phases</h2>\n<h3 id=\"phase-0-docs-and-flags-1-day\">Phase 0 — Docs and flags (1 day)</h3>\n<ul>\n<li>[x] Accept this plan; link from <code>README.md</code></li>\n<li>[x] Add <code>HIGHASCG_HEADLESS=true</code> to <code>write-highascg-systemd-unit.sh</code> drop-in on imaging host</li>\n<li>[x] Set <code>HIGHASCG_ISO_BUILD_WEB=0</code> default; exclude <code>dist-web/</code> from ISO embed fragment</li>\n<li>[x] Fix <code>vite.config.js</code> proxy ports to <strong>4200</strong></li>\n</ul>\n<h3 id=\"phase-1-api-origin-in-client-23-days\">Phase 1 — API origin in client (2–3 days)</h3>\n<ul>\n<li>[x] Add <code>client/lib/api-origin.js</code></li>\n<li>[x] Refactor <code>api-client.js</code>, <code>ws-client.js</code></li>\n<li>[x] Fix <code>location.origin</code> / <code>/templates</code> / import map (<code>index.html</code> + Vite plugin)</li>\n<li>[x] CORS: <code>HIGHASCG_CORS_ORIGINS</code> + default Vite origins when headless</li>\n<li>[x] <code>npm run smoke:api-origin</code>; split dev: <code>HIGHASCG_HEADLESS=true npm start</code> + <code>npm run dev:client</code></li>\n</ul>\n<h3 id=\"phase-2-electron-hosts-ui-35-days\">Phase 2 — Electron hosts UI (3–5 days)</h3>\n<ul>\n<li>[x] <code>preload-app.js</code> + second <code>BrowserWindow</code> for control UI</li>\n<li>[x] <code>npm run launcher:prepare</code> syncs <code>dist-web/</code> into launcher folder</li>\n<li>[x] Connect flow: Server IP + port → <code>__HIGHASCG_API_ORIGIN__</code> → <code>highascg://app/</code></li>\n<li>[x] Sim flow: headless child + auto-open embedded UI on <code>127.0.0.1:{port}</code></li>\n<li>[x] System browser demoted to secondary button</li>\n</ul>\n<h3 id=\"phase-3-release-and-iso-alignment-2-days\">Phase 3 — Release and ISO alignment (2 days)</h3>\n<ul>\n<li>[x] Revert embed-server defaults (no dist-web in squashfs)</li>\n<li>[x] Update <code>smoke-wo47-manifest.mjs</code>, <code>EGGS_EXCLUDE_LIST.md</code>, <code>WO47_ISO_VS_EXFAT.md</code></li>\n<li>[x] <code>release:github-launcher</code> script; <code>build:launcher</code> runs <code>launcher:prepare</code></li>\n<li>[x] Server tarball excludes <code>client/</code>, <code>dist-web/</code> (<code>archive_common_server_tar_excludes</code>)</li>\n<li>[x] Update <code>electron-launcher</code> README; <code>DEV_RELEASE_GITHUB.md</code></li>\n</ul>\n<h3 id=\"phase-4-cleanup-1-day\">Phase 4 — Cleanup (1 day)</h3>\n<ul>\n<li>[x] <code>HIGHASCG_HEADLESS</code> accepts <code>1</code>/<code>true</code>/<code>yes</code></li>\n<li>[x] Deprecation warning when Node serves UI without headless; <code>HIGHASCG_WEB_DIR</code> warned</li>\n<li>[x] <code>HIGHASCG_ISO_BUILD_WEB</code> default <strong>0</strong> (legacy opt-in <code>=1</code> only)</li>\n<li>[x] Archive monolith <code>release:github-app</code> docs as legacy</li>\n<li>[x] WO‑51 work log updated (Electron launcher canonical for UI)</li>\n</ul>\n<hr>\n<h2 id=\"10-migration-checklist-operators\">10. Migration checklist (operators)</h2>\n<table><thead><tr><th>Step</th><th>Action</th></tr></thead><tbody>\n<tr><td>1</td><td>Install <strong>HighAsCG Launcher</strong> (Electron) on operator laptop</td></tr>\n<tr><td>2</td><td>Flash ISO (server-only squashfs)</td></tr>\n<tr><td>3</td><td>Put <code>highascg-server_*.tar.gz</code> in <code>update/server/</code> if not embedded</td></tr>\n<tr><td>4</td><td>On playout: confirm <code>systemctl show highascg.service</code> has <code>HIGHASCG_HEADLESS=true</code></td></tr>\n<tr><td>5</td><td>Launcher → Server IP = playout LAN address, port = <strong>4200</strong> (or your <code>config/server.json</code>)</td></tr>\n<tr><td>6</td><td>Do <strong>not</strong> browse to <code>http://playout:4200/</code> for UI (API JSON only)</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"11-testing-matrix\">11. Testing matrix</h2>\n<table><thead><tr><th>Test</th><th>Pass criteria</th></tr></thead><tbody>\n<tr><td>Playout ISO boot</td><td><code>highascg.service</code> active; <code>curl http://127.0.0.1:4200/api/health</code> OK; <code>curl http://127.0.0.1:4200/</code> → headless 404 JSON</td></tr>\n<tr><td>Electron → remote playout</td><td>UI loads; WS connects; Caspar status updates</td></tr>\n<tr><td>Electron → sim</td><td>Child headless server; UI offline mode works</td></tr>\n<tr><td>Vite dev</td><td><code>:3000</code> UI → <code>:4200</code> API; previs Three loads</td></tr>\n<tr><td>exFAT server update</td><td><code>update/server/</code> without dist-web does not break service</td></tr>\n<tr><td>CORS</td><td>No browser console CORS errors from Electron or Vite</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"12-open-decisions-resolve-before-phase-2\">12. Open decisions (resolve before Phase 2)</h2>\n<table><thead><tr><th>#</th><th>Question</th><th>Recommendation</th></tr></thead><tbody>\n<tr><td>1</td><td>Default API port in operator docs</td><td><strong>4200</strong> (matches <code>defaults.js</code>, <code>config/server.json</code>, and sim launcher)</td></tr>\n<tr><td>2</td><td>Bundle server in Electron for offline sim?</td><td><strong>No</strong> — spawn from stick <code>sim/highascg</code> or repo</td></tr>\n<tr><td>3</td><td><code>file://</code> vs <code>app://</code> protocol for UI?</td><td><strong><code>app://</code> custom protocol</strong> (avoids <code>file://</code> CORS/storage limits)</td></tr>\n<tr><td>4</td><td>Keep <code>npm start</code> serving UI for dev?</td><td><strong>Yes</strong>, only when <code>HIGHASCG_HEADLESS</code> unset — print deprecation warning</td></tr>\n<tr><td>5</td><td>Linux playout operator without Electron?</td><td>Optional: <code>nginx</code> serving client tarball pointing at localhost API (document in MANUAL_INSTALL)</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"13-file-index-quick-reference\">13. File index (quick reference)</h2>\n<pre><code>Server\n  index.js\n  src/server/http-server.js\n  src/server/cors.js\n  src/repo-paths.js\n  scripts/write-highascg-systemd-unit.sh\n  /etc/systemd/system/highascg.service.d/10-headless.conf\n\nClient\n  client/lib/api-origin.js          (new)\n  client/lib/api-client.js\n  client/lib/ws-client.js\n  client/index.html\n  vite.config.js\n  dist-web/                         (Electron + client release only)\n\nLauncher\n  client/tools/electron-launcher/main.js\n  client/tools/electron-launcher/preload.js   (new)\n  client/tools/electron-launcher/renderer.js\n\nISO / eggs\n  tools/eggs/live-usb/install-iso-defaults.sh\n  tools/eggs/live-usb/prepare-eggs-clone-with-exfat.sh\n  tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list\n  config/casparcg.config.iso\n\nReleases\n  tools/release/make-github-release-server.sh\n  client/tools/release/make-github-release-client.sh</code></pre>\n<hr>\n<h2 id=\"work-log\">Work log</h2>\n<h3 id=\"2026-05-20-phase-0\">2026-05-20 — Phase 0</h3>\n<ul>\n<li><code>write-highascg-systemd-unit.sh</code> installs <code>highascg.service.d/10-headless.conf</code>.</li>\n<li><code>HIGHASCG_ISO_BUILD_WEB</code> default <strong>0</strong>; embed exclude list omits <code>dist-web/</code>.</li>\n<li><code>vite.config.js</code> dev proxy targets <strong>127.0.0.1:4200</strong>.</li>\n<li>README + WO47 + ISO_CONTENTS + plan checklist updated.</li>\n</ul>\n<h3 id=\"2026-05-20-phase-1\">2026-05-20 — Phase 1</h3>\n<ul>\n<li><code>client/lib/api-origin.js</code>; <code>api-client</code> / <code>ws-client</code> / <code>webrtc-client</code> use explicit API origin.</li>\n<li><code>src/server/headless-mode.js</code>; CORS reflects <code>HIGHASCG_CORS_ORIGINS</code> or Vite dev origins.</li>\n<li><code>.env.development</code>, Vite import-map plugin, <code>index.html</code> bootstrap script.</li>\n<li><code>npm run smoke:api-origin</code>.</li>\n</ul>\n<h3 id=\"2026-05-20-phase-2\">2026-05-20 — Phase 2</h3>\n<ul>\n<li><code>electron-launcher/main.js</code>: <code>highascg://</code> protocol, control UI window, headless sim spawn.</li>\n<li><code>preload-app.js</code>, <code>sync-dist-web.sh</code>, <code>npm run launcher:prepare</code>.</li>\n</ul>\n<p><em>Aligned with repo layout May 2026.</em></p>","MODULES":"<h1 id=\"optional-modules-previs-tracking-auto-follow-cg-studio\">Optional modules (Previs / Tracking / Auto-follow / CG Studio)</h1>\n<p>HighAsCG ships with a small set of optional feature modules that can be enabled or deleted independently of the base server. The packaging rules below are the ones enforced by <a href=\"../../work/30_WO_PREVIS_TRACKING_MODULE.md\">WO-30</a>; everything in the Previs / Tracking / Stage Auto-follow WOs (17 / 19 / 31) is built on top of them.</p>\n<p><strong>CG Studio</strong> (WO-32) is also an optional module but runs a <strong>separate HTTP server</strong> on port 4300 — see <a href=\"../../src/cg-studio/README.md\">src/cg-studio/README.md</a>.</p>\n<p>Acceptance test for the &quot;lean build&quot;: delete the module directories listed below and the base server must still boot normally.</p>\n<h2 id=\"directory-layout\">Directory layout</h2>\n<pre><code>src/\n├── module-registry.js          ← core\n├── api/\n│   └── routes-modules.js       ← core (GET /api/modules)\n├── previs/                     ← optional module (delete to remove)\n│   └── register.js\n├── tracking/                   ← optional module (delete to remove)\n│   └── register.js\n└── autofollow/                 ← optional module (delete to remove)\n    └── register.js\n└── cg-studio/                  ← optional module (delete to remove)\n    └── register.js\n\nweb/\n├── lib/optional-modules.js     ← core loader\n└── components/\n    ├── previs-*.js             ← optional (delete to remove)\n    ├── tracking-*.js           ← optional (delete to remove)\n    └── autofollow-*.js         ← optional (delete to remove)</code></pre>\n<p>Core code must never <code>require('./previs/...')</code>, <code>require('./tracking/...')</code>, <code>require('./autofollow/...')</code> or statically <code>import</code> from <code>client/components/{previs,tracking,autofollow}-*.js</code>. Every entry into a module goes through the registry.</p>\n<h2 id=\"enabling-the-module\">Enabling the module</h2>\n<p>Either set the environment variable:</p>\n<pre><code class=\"language-bash\">HIGHASCG_PREVIS=1 node index.js</code></pre>\n<p>…or flip the runtime config:</p>\n<pre><code class=\"language-json\">{\n  &quot;features&quot;: {\n    &quot;previs3d&quot;: true\n  }\n}</code></pre>\n<p>When the flag is off, <code>index.js</code> logs a single &quot;Previs/tracking module disabled&quot; line and skips the <code>tryLoad</code> calls entirely. Nothing from <code>src/previs/</code>, <code>src/tracking/</code>, or <code>src/autofollow/</code> is touched.</p>\n<p>When the flag is on, <code>index.js</code> calls <code>moduleRegistry.tryLoad('previs' | 'tracking' | 'autofollow')</code>. Each <code>tryLoad</code> swallows <code>MODULE_NOT_FOUND</code> and descriptor-level errors, so an incomplete or half-deleted install degrades gracefully: the other modules still load.</p>\n<h2 id=\"installing-optional-dependencies\">Installing optional dependencies</h2>\n<p>The heavy native/runtime deps are marked <code>optionalDependencies</code> so a base install never pays for them:</p>\n<table><thead><tr><th>Package</th><th>Purpose</th><th>Size on disk</th></tr></thead><tbody>\n<tr><td><code>three</code></td><td>WebGL2 scene for 3D previs</td><td>~2 MB JS</td></tr>\n<tr><td><code>naudiodon</code></td><td>PortAudio device capture</td><td>native addon</td></tr>\n</tbody></table>\n<p>Browser-only deps for Previs (<code>three</code>) live in optionalDependencies. CG Studio ships as vanilla static files under <code>src/cg-studio/public/</code> (no GrapesJS, no client build). <code>onnxruntime-node</code> (WO-19 person tracking) will be added when that module ships — not in the base server install.</p>\n<p>Two install scripts:</p>\n<pre><code class=\"language-bash\"># Lean: skip optional deps (recommended for playout servers)\nnpm run install:base\n\n# Full: include optional deps (three, naudiodon)\nnpm run install:previs</code></pre>\n<p><code>npm install --include=optional</code> is also fine; <code>install:previs</code> is a thin alias that keeps the intent visible in shell history / CI logs.</p>\n<h2 id=\"registration-api\">Registration API</h2>\n<p>Each module exports a descriptor from <code>src/&lt;name&gt;/register.js</code>. All fields except <code>name</code> are optional.</p>\n<pre><code class=\"language-js\">// src/previs/register.js  (example)\nmodule.exports = {\n  name: 'previs',\n\n  // Called once after the WebSocket server is up and appCtx._wsBroadcast is wired.\n  onBoot(ctx) { /* start workers, subscribe to WS, seed state, … */ },\n\n  // Called on SIGINT / SIGTERM before HTTP close.\n  async onShutdown() { /* stop workers, release GPU handles, … */ },\n\n  // Any incoming HTTP path matching one of these prefixes is dispatched to handleApi.\n  apiPathPrefixes: ['/api/previs'],\n\n  // Signature: ({ method, path, body, ctx, req, query }) =&gt; response | null\n  handleApi: async ({ method, path, body, ctx }) =&gt; { /* … */ },\n\n  // WebSocket event name prefixes this module broadcasts on. Purely informational\n  // (exposed through GET /api/modules). The registry does not enforce them.\n  wsNamespaces: ['previs:'],\n\n  // URLs served by the core web server. The web client dynamically imports each\n  // bundle and, if it has a default export, awaits default(sharedContext).\n  webBundles: ['/assets/modules/previs/entry.js'],\n  webStyles:  ['/assets/modules/previs/previs.css'],\n}</code></pre>\n<p>The web client fetches <code>GET /api/modules</code> during <code>init()</code> and forwards a shared context (<code>{ stateStore, ws, api, sceneState, settingsState, streamState }</code>) to each module's default export. A module that only extends the server-side API does not need a web bundle at all.</p>\n<h2 id=\"get-apimodules\"><code>GET /api/modules</code></h2>\n<pre><code class=\"language-jsonc\">{\n  &quot;enabled&quot;:      [&quot;previs&quot;, &quot;tracking&quot;, &quot;autofollow&quot;],\n  &quot;bundles&quot;:      [&quot;/assets/modules/previs/entry.js&quot;, &quot;…&quot;],\n  &quot;styles&quot;:       [&quot;/assets/modules/previs/previs.css&quot;],\n  &quot;wsNamespaces&quot;: [&quot;previs:&quot;, &quot;tracking:&quot;, &quot;autofollow:&quot;]\n}</code></pre>\n<p>Always returns <code>200</code> — empty arrays when no modules are registered.</p>\n<h2 id=\"websocket-namespaces\">WebSocket namespaces</h2>\n<p>Reserved event prefixes (see individual WOs for payload shapes):</p>\n<table><thead><tr><th>Prefix</th><th>Owner</th><th>Example events</th></tr></thead><tbody>\n<tr><td><code>previs:</code></td><td>WO-17</td><td><code>previs:scene</code>, <code>previs:camera</code></td></tr>\n<tr><td><code>tracking:</code></td><td>WO-19</td><td><code>tracking:persons</code>, <code>tracking:stats</code></td></tr>\n<tr><td><code>autofollow:</code></td><td>WO-31</td><td><code>autofollow:device</code>, <code>autofollow:state</code></td></tr>\n</tbody></table>\n<p>Core code never broadcasts on these prefixes. If a module is deleted, the prefix simply goes silent.</p>\n<h2 id=\"stage-coordinate-system\">Stage coordinate system</h2>\n<p>Shared reference frame used by all three modules so data can flow between them (tracking → auto-follow; previs floor plane; device calibration) without per-module conversions.</p>\n<ul>\n<li>Units: metres.</li>\n<li>Handedness: right-handed.</li>\n<li>Axes, relative to the operator standing in the audience looking at the stage:</li>\n<li><code>+X</code> → <strong>stage right</strong> (audience-left; positive to the operator's left when facing the stage is a common pitfall — we mirror this once at the UI level and never in the data).</li>\n<li><code>+Y</code> → <strong>upstage</strong> (away from the audience).</li>\n<li><code>+Z</code> → <strong>up</strong> (vertical, gravity-opposite).</li>\n<li>Origin: user-defined during calibration. The calibration wizard picks a visible mark (centre of downstage edge is the recommended default) and fits the transform from there.</li>\n<li>Z = 0 is the stage floor. Ceiling-mounted devices have positive Z; traps / pit lifts have negative Z.</li>\n</ul>\n<p>Positions emitted on the wire (<code>tracking:persons[].stagePosition</code>, <code>autofollow:device[].position</code>, etc.) all obey this frame. The Previs 3D scene renders with the same axes so no conversion is needed when aiming a tracked person at a modelled device.</p>\n<h2 id=\"deletion-checklist\">Deletion checklist</h2>\n<p>To remove the entire optional-module stack:</p>\n<pre><code class=\"language-bash\">rm -rf src/previs src/tracking src/autofollow\nrm -f  client/components/previs-*.js client/components/tracking-*.js client/components/autofollow-*.js\nrm -rf web/assets/modules\n# Optional — shrink the lockfile:\nnpm uninstall three onnxruntime-node</code></pre>\n<p>Then boot. <code>GET /api/modules</code> returns <code>{ enabled: [] }</code>, the web client logs <code>loaded: [—]</code>, and the base app runs untouched.</p>\n<h2 id=\"cg-studio-launcher-hosted-module\">CG Studio (launcher-hosted module)</h2>\n<p>CG Studio does <strong>not</strong> run on the playout server in normal production. Source is <code>src/cg-studio/</code> in <strong>this repo</strong>. The optional Electron launcher (<a href=\"https://github.com/mko1989/highascg-client\">highascg-client</a>) — packaged from <code>client/tools/electron-launcher/</code> — starts it locally on port <strong>4300</strong> when enabled in the Modules tab.</p>\n<p><strong>Local dev:</strong> <code>npm run cg-studio</code> from this repo checkout.</p>\n<p>The <strong>operator UI</strong> (dashboard, scenes, device view) is always <strong><code>client/</code></strong> in this repo → <code>dist-web/</code> on playout <code>:4200</code>. Do not develop that UI in highascg-client.</p>\n<p>Exported templates land in <code>template/studio/</code> on the linked HighAsCG checkout and appear in <code>GET /api/lower-thirds/templates</code> after playout picks up the files.</p>","MANUAL_INSTALL":"<h1 id=\"manual-install-casparcg-user-optcasparcg-x11-decklink-ndi\">Manual install: <code>casparcg</code> user, <code>/opt/casparcg</code>, X11, DeckLink, NDI</h1>\n<blockquote><p><strong>Playout host (recommended):</strong> use the ordered setup scripts and <strong><a href=\"../../scripts/setup/MANUAL_INSTALL.md\">&lt;code&gt;scripts/setup/MANUAL_INSTALL.md&lt;/code&gt;</a></strong> — kernel 117, NVIDIA open 595, <code>~/highascg</code>, nodm/Openbox.</p></blockquote>\n<p>This guide mirrors the intent of <strong><code>scripts/install-phase2.sh</code></strong>, <strong><code>install-phase3.sh</code></strong>, and <strong><code>install-helpers.sh</code></strong>, but every step is <strong>manual</strong> (no <code>install.sh</code>). Adjust paths and versions to your machine.</p>\n<p><strong>Privileges:</strong> Steps below assume a normal user with <strong><code>sudo</code></strong>. Commands that need root are prefixed with <code>sudo</code>. Alternatively, open a root shell once: <code>sudo -i</code>, then run the same commands <strong>without</strong> the <code>sudo</code> prefix.</p>\n<hr>\n<h2 id=\"1-create-user-casparcg-and-own-optcasparcg\">1. Create user <code>casparcg</code> and own <code>/opt/casparcg</code></h2>\n<h3 id=\"11-user-service-style-matches-scripts\">1.1 User (service-style, matches scripts)</h3>\n<p>No interactive shell; home directory exists for nodm/X11:</p>\n<pre><code class=\"language-bash\">USER_CASPAR=casparcg\nUSER_SHELL=$(command -v nologin || command -v false || echo &quot;/usr/sbin/nologin&quot;)\n\nsudo useradd -r -m -s &quot;$USER_SHELL&quot; &quot;$USER_CASPAR&quot;</code></pre>\n<p>If the user already exists, skip <code>useradd</code>.</p>\n<h3 id=\"12-directory-layout-and-ownership\">1.2 Directory layout and ownership</h3>\n<pre><code class=\"language-bash\">sudo mkdir -p /opt/casparcg/{media,log,template,data,cef-cache,config}\nsudo chown -R casparcg:casparcg /opt/casparcg\nsudo chmod -R 775 /opt/casparcg</code></pre>\n<p>Deploy binaries, <code>config/casparcg.config</code>, <code>run.sh</code>, and <code>lib/</code> (CEF, etc.) into <code>/opt/casparcg</code>, then re-run <code>sudo chown</code> if root created files.</p>\n<hr>\n<h2 id=\"2-groups-video-audio-render\">2. Groups (<code>video</code>, <code>audio</code>, <code>render</code>, …)</h2>\n<p>Only add groups that exist on your system (same list as <strong><code>install-phase3.sh</code></strong>):</p>\n<pre><code class=\"language-bash\">for GRP in video audio render plugdev dialout input; do\n  if getent group &quot;$GRP&quot; &amp;&gt;/dev/null; then\n    sudo usermod -aG &quot;$GRP&quot; casparcg\n  fi\ndone</code></pre>\n<table><thead><tr><th>Group</th><th>Typical use</th></tr></thead><tbody>\n<tr><td><code>video</code></td><td><code>/dev/dri</code>, GPU</td></tr>\n<tr><td><code>render</code></td><td>DRM/KMS (e.g. kmsgrab / GPU)</td></tr>\n<tr><td><code>audio</code></td><td>ALSA / Pulse / PipeWire</td></tr>\n<tr><td><code>plugdev</code></td><td>Some USB devices</td></tr>\n<tr><td><code>dialout</code></td><td>Serial ports</td></tr>\n<tr><td><code>input</code></td><td>Input devices</td></tr>\n</tbody></table>\n<p>Reboot or log out so membership applies to new sessions.</p>\n<hr>\n<h2 id=\"3-openbox-nodm\">3. Openbox + nodm</h2>\n<p>Packages (see <strong><code>install-phase3.sh</code></strong>):</p>\n<pre><code class=\"language-bash\">sudo apt update\nsudo apt install -y nodm openbox unclutter xterm util-linux</code></pre>\n<h3 id=\"31-nodm\">3.1 nodm</h3>\n<pre><code class=\"language-bash\">sudo tee /etc/default/nodm &gt;/dev/null &lt;&lt;'EOF'\nNODM_ENABLED=true\nNODM_USER=casparcg\nNODM_X_OPTIONS='-s 0 -dpms -nolisten tcp'\nEOF</code></pre>\n<h3 id=\"32-session\">3.2 Session</h3>\n<pre><code class=\"language-bash\">sudo mkdir -p /home/casparcg\necho 'exec openbox-session' | sudo tee /home/casparcg/.xsession &gt;/dev/null\nsudo chmod +x /home/casparcg/.xsession\nsudo chown casparcg:casparcg /home/casparcg/.xsession</code></pre>\n<h3 id=\"33-openbox-autostart\">3.3 Openbox autostart</h3>\n<pre><code class=\"language-bash\"></code></pre>\n<p>mkdir -p /home/casparcg/.config/openbox</p>\n<h1 id=\"create-and-edit-autostart-see-openbox_autostartmd-install-phase3sh-for-a-full-example\">Create and edit autostart — see openbox_autostart.md / install-phase3.sh for a full example</h1>\n<p>chmod +x /home/casparcg/.config/openbox/autostart chown -R casparcg:casparcg /home/casparcg/.config</p>\n<pre><code>\nRestart:\n</code></pre>\n<p>sudo systemctl restart nodm</p>\n<pre><code>\n---\n\n## 4. DeckLink (Blackmagic Desktop Video)\n\nAligned with **`install-phase2.sh`** (§2.2) and **`fetch_decklink_tarball`** in **`install-helpers.sh`**.\n\n1. Download **Desktop Video for Linux** from [Blackmagic Capture &amp; Playback](https://www.blackmagicdesign.com/support/family/capture-and-playback) (license acceptance required). You can also set a tarball path via `HIGHASCG_DECKLINK_TAR` or place a copy at `/tmp/decklink.tar.gz` when using the automated helper.\n\n2. Install:\n</code></pre>\n<p>cd /tmp https://swr.cloud.blackmagicdesign.com/DesktopVideo/v16.0/Blackmagic_Desktop_Video_Linux_16.0.tar.gz?verify=1776791815-QyDaLVBq%2FvE14glhLV6tJb33C2Aw%2Fs4NEIRpOu%2BIPsE%3D tar -xzf decklink.tar.gz   # use your actual filename sudo dpkg -i /tmp/Blackmagic_Desktop_Video_Linux_16.0/deb/x86_64/desktopvideo_16.0a14_amd64.deb sudo dpkg -i /tmp/Blackmagic_Desktop_Video_Linux_16.0/deb/x86_64/desktopvideo-gui_16.0a14_amd64.deb sudo apt install -f -y sudo modprobe blackmagic_io 2&gt;/dev/null || true</p>\n<pre><code>\n3. Verify:\n</code></pre>\n<p>dpkg -l desktopvideo command -v desktopvideo_setup   # optional GUI</p>\n<pre><code>\n4. Reboot if the installer or Blackmagic docs require it.\n\n---\n\n## 5. NDI SDK (Linux v6)\n\nAligned with **`install-phase2.sh`** (§2.3) and **`fetch_ndi_sdk_tarball`** in **`install-helpers.sh`**.\n\n1. Download **NDI SDK for Linux** from NDI (account / license). The automated installer script is typically named `Install_NDI_SDK_v6_Linux.sh`.\n\n2. Run the installer (needs root to install system-wide):\n</code></pre>\n<p>cd /tmp wget https://downloads.ndi.tv/SDK/NDI_SDK_Linux/Install_NDI_SDK_v6_Linux.tar.gz tar -xzf Install_NDI_SDK_v6_Linux.tar.gz chmod +x Install_NDI_SDK_v6_Linux.sh sudo ./Install_NDI_SDK_v6_Linux.sh --accept-license</p>\n<pre><code>\n3. Install the versioned library and symlinks (layout as in **`install-phase2.sh`**):\n</code></pre>\n<p>NDI_LIB_SRC=$(find &quot;NDI SDK for Linux/lib/x86_64-linux-gnu&quot; -maxdepth 1 -type f -name 'libndi.so.6.*' 2&gt;/dev/null | head -1) if [ -n &quot;$NDI_LIB_SRC&quot; ] &amp;&amp; [ -f &quot;$NDI_LIB_SRC&quot; ]; then sudo install -m 0644 &quot;$NDI_LIB_SRC&quot; /usr/lib/x86_64-linux-gnu/ NDI_BASE=$(basename &quot;$NDI_LIB_SRC&quot;) sudo ln -sf &quot;$NDI_BASE&quot; /usr/lib/x86_64-linux-gnu/libndi.so.6 sudo ln -sf libndi.so.6 /usr/lib/x86_64-linux-gnu/libndi.so sudo ldconfig fi</p>\n<pre><code>\nIf paths differ, locate `libndi.so.6.*` inside the extracted SDK and adjust.\n\n4. Optional (same idea as **`install-phase3.sh`**): copy next to Caspar for loaders that search the app directory:\n</code></pre>\n<p>sudo cp /usr/lib/x86_64-linux-gnu/libndi.so.6 /opt/casparcg/ 2&gt;/dev/null || true sudo chown casparcg:casparcg /opt/casparcg/libndi.so.6 2&gt;/dev/null || true</p>\n<pre><code>\n---\n\n## 6. CEF / dynamic libraries for Caspar\n\nIf Caspar loads bundled CEF from `/opt/casparcg/lib`:\n</code></pre>\n<p>echo /opt/casparcg/lib | sudo tee /etc/ld.so.conf.d/casparcg.conf &gt;/dev/null sudo ldconfig</p>\n<pre><code>\nAlternatively set `LD_LIBRARY_PATH=/opt/casparcg/lib` in the **environment that starts Caspar** (value must be the **directory**, not the `.so` path). That does not require `sudo` if you only change a user’s systemd unit or shell script.\n\n---\n\n## 7. Optional HighAsCG-related bits (from scripts)\n\n- **USB media ingest (WO-29)**: install **`udisks2`** and **`policykit-1`**; copy **`scripts/polkit/50-highascg-udisks.rules`** to **`/etc/polkit-1/rules.d/`** so the `casparcg` user (group **`plugdev`**) can run **`udisksctl unmount` / `power-off`** without a password. The full installer does this in **`install-phase4.sh`** (section 4.3b). If a stick does not auto-mount under **`/media/…`**, check **`udev`/`udisks2`** logs and that the volume is not already mounted elsewhere.\n- **Sudoers for `/etc/asound.conf`**: **optional** — only for Web UI **system-wide** ALSA default. Default installer skips this; set **`HIGHASCG_INSTALL_ASOUND_SUDOERS=1`** during install (see **`install-phase3.sh`**). Per-user **`~/.asoundrc`** needs no sudo.\n- **NVIDIA X session tweaks**: only for NVIDIA GPUs — see **`install-phase2.sh`** (**Sync to VBlank off**). Pair with Caspar screen consumer **vsync on** — [reference/screen-consumer-vsync-nvidia.md](reference/screen-consumer-vsync-nvidia.md).\n- **Durable config/state/media (exFAT only):** use WO-47 — mount **`HIGHASCGEXF`** at **`/home/casparcg/exfat`**, sync via **`highascg-exfat-sync.service`** and **`config/exfat-sync.json`** (see **`docs/WO47_ISO_VS_EXFAT.md`**, **`docs/EXFAT_SERVER_UPDATE.md`**). WO-38 **`media/drive`** partition mount was removed (no startup wait, no Settings mount UI).\n\n---\n\n## 8. Checklist\n\n| Step | Done |\n|------|------|\n| User `casparcg` exists with home | ☐ |\n| `sudo chown -R casparcg:casparcg /opt/casparcg` | ☐ |\n| Groups: `video`, `audio`, `render`, `plugdev`, `dialout`, `input` | ☐ |\n| `nodm` configured; `.xsession` → `openbox-session` | ☐ |\n| Openbox autostart + Caspar `run.sh` or equivalent | ☐ |\n| DeckLink `desktopvideo` packages installed | ☐ |\n| NDI SDK + `libndi.so.6` + `ldconfig` | ☐ |\n| `ldconfig` or `LD_LIBRARY_PATH` for `/opt/casparcg/lib` | ☐ |\n| HighAsCG cloned to `/opt/highascg`, `npm install`, service (optional) | ☐ |\n| Reboot and verify X + Caspar | ☐ |\n\n---\n\n## 9. HighAsCG — fetch from GitHub and install\n\nReplace **`YOUR_REPO`** with your GitHub URL (HTTPS or SSH), for example **`https://github.com/mko1989/highascg.git`** (same default as `HIGHASCG_GIT_URL` in `scripts/install-config.sh`).\n\n**Requirements:** Node.js **≥ 20** (see [NodeSource](https://github.com/nodesource/distributions) or `install-phase4.sh`). Ubuntu’s default `nodejs` package is often too old.\n\n### 9.1 Clone into `/opt/highascg` and install dependencies\n</code></pre>\n<p>sudo rm -rf /opt/highascg sudo mkdir -p /opt/highascg sudo chown casparcg:casparcg /opt/highascg</p>\n<p>sudo -u casparcg git clone --depth 1 https://github.com/mko1989/highascg.git /opt/highascg</p>\n<p>cd /opt/highascg sudo -u casparcg npm install --omit=dev</p>\n<pre><code>\n\n\n### 9.2 Configuration\n</code></pre>\n<p>cd /opt/highascg if [ ! -f highascg.config.json ] &amp;&amp; [ -f highascg.config.example.json ]; then sudo -u casparcg cp highascg.config.example.json highascg.config.json fi sudo chown -R casparcg:casparcg /opt/highascg sudo chmod -R 775 /opt/highascg</p>\n<pre><code>\nEdit `highascg.config.json` (or use env vars — see root **`README.md`**) as needed.\n\n### 9.2.1 Optional 3D Previs (`three`)\n\nThe browser-side **3D Previs** feature (PGM cell 2D/3D toggle, imported stage models, LED-style video mapping) is gated behind **`HIGHASCG_PREVIS=1`** or **`config.features.previs3d === true`** (see root `index.js` and `docs/MODULES.md`).\n\nThe **`three`** package is listed under **`optionalDependencies`** in `package.json`. A minimal install such as `npm install --omit=dev` **without** optional packages does **not** install it.\n\nTo pull optional dependencies (including **`three`**):\n</code></pre>\n<p>cd /opt/highascg sudo -u casparcg npm run install:previs</p>\n<h1 id=\"equivalent-npm-install---includeoptional\">equivalent: npm install --include=optional</h1>\n<pre><code>\nEnable the server-side feature flag (pick one):\n\n**systemd `Environment=`**\n</code></pre>\n<p>Environment=HIGHASCG_PREVIS=1</p>\n<pre><code>\n**Or** in `highascg.config.json`:\n</code></pre>\n<p>&quot;features&quot;: { &quot;previs3d&quot;: true }</p>\n<pre><code>\nIn the web UI (with Previs enabled), operators can: toggle the **PGM** preview cell to **3D**, import a **`.glb`** stage, tag meshes as screens, and use **Scene settings** (virtual canvas size, video texture cap, lights). The inspector **Screen mapping** section includes a **virtual-canvas region** control (drag / corner resize) to crop how PGM video maps onto a screen; that layout is stored in the browser (with the rest of previs state in `localStorage`, key `highascg.previs.state.v1`). It is optional workflow polish, not required for a minimal playout install.\n\n### 9.3 systemd service (optional, matches `install-phase4.sh`)\n</code></pre>\n<p>sudo tee /etc/systemd/system/highascg.service &gt;/dev/null &lt;&lt;'EOF' [Unit] Description=HighAsCG Playout Control Server After=network.target</p>\n<p>[Service] Type=simple User=casparcg Group=casparcg UMask=002 WorkingDirectory=/opt/highascg ExecStart=/usr/bin/node /opt/highascg/index.js Restart=always RestartSec=5 Environment=NODE_ENV=production</p>\n<p>[Install] WantedBy=multi-user.target EOF</p>\n<p>sudo systemctl daemon-reload sudo systemctl enable highascg.service sudo systemctl start highascg.service</p>\n<pre><code>\nCheck: `systemctl status highascg`, then open **`http://SERVER_IP:4200/`** (or the port in your config).\n\n### 9.3.1 npm: `go2rtc-static` download timeout\n\nThe `go2rtc-static` package runs `node dist/install.js` after install and **downloads** a go2rtc binary from GitHub (`got` request timeout **30s**). Slow links, firewalls, or GitHub blocks produce:\n\n`TimeoutError: Timeout awaiting 'request' for 30000ms`\n\n**Workaround A — system binary + skip scripts (typical on restricted servers)**\n\n1. Install a compatible `go2rtc` binary on the server (e.g. [AlexxIT/go2rtc releases](https://github.com/AlexxIT/go2rtc/releases) for Linux amd64), place it at e.g. `/usr/local/bin/go2rtc`, `chmod +x`.\n2. Deploy Node modules without running the post-install download:\n</code></pre>\n<p>cd /opt/highascg npm ci --ignore-scripts</p>\n<pre><code>\n3. Point HighAsCG at that binary (either is fine):\n\n   - Environment: `HIGHASCG_GO2RTC_BINARY=/usr/local/bin/go2rtc`  \n   - Or: `GO2RTC_BIN=/usr/local/bin/go2rtc` (also respected by the `go2rtc-static` package if present)\n\n   In **systemd**, add e.g. `Environment=HIGHASCG_GO2RTC_BINARY=/usr/local/bin/go2rtc` under `[Service]`.\n\n**Workaround B — proxy:** if GitHub is only reachable via proxy, set `HTTPS_PROXY` / `https_proxy` during `npm install` (the download script uses `https-proxy-agent`).\n\n**Workaround C:** run `npm ci` on a machine with reliable GitHub access, then **rsync `node_modules`** to the server (or use `npm pack` / offline mirror).\n\n### 9.4 Full automated installer (Caspar + HighAsCG + more)\n\nIf you want the **entire** stack (Caspar debs, nodm, Tailscale, Syncthing, etc.), clone the repo to a path with the full `scripts/` tree and run:\n</code></pre>\n<p>git clone https://github.com/YOUR_ORG/highascg.git cd highascg sudo ./scripts/install.sh</p>\n<pre><code>\nThat uses **`scripts/install-config.sh`** (including `HIGHASCG_GIT_URL`) and **`install-phase4.sh`** to deploy to `/opt/highascg`; for a machine that already has the repo cloned, phase 4 can sync from `SCRIPT_DIR` instead of cloning again.\n\n### 9.5 Dev deploy from your laptop\n\n**`client/scripts/dev-push.sh`** / `npm run deploy:dev` — rsync or scp to a remote host (see **`scripts/README.md`** and `.env.deploy`).\n\n---\n\n## Script reference (repo)\n\n| Topic | File |\n|--------|------|\n| User, groups, nodm, openbox, `/opt/casparcg` | `scripts/install-phase3.sh` |\n| FFmpeg base, NVIDIA, DeckLink, NDI | `scripts/install-phase2.sh` |\n| Tarball fetch helpers | `scripts/install-helpers.sh` |\n| Openbox autostart notes | `openbox_autostart.md` (repo root) |\n</code></pre>","LIVE_USB_IMAGE":"<h1 id=\"creating-a-live-usb-image-from-a-running-highascg-casparcg-server\">Creating a Live USB Image from a Running HighAsCG / CasparCG Server</h1>\n<blockquote><p><strong>Goal:</strong> Capture the entire running production server — Ubuntu 24.04 LTS, NVIDIA drivers, DeckLink, NDI, CasparCG (custom CEF build), HighAsCG, autostart configuration — into a bootable ISO that can:  1. <strong>Boot live from USB</strong> (test / emergency playout — durable data on <strong>exFAT</strong> <code>HIGHASCGEXF</code>, see <strong>§7</strong>) 2. <strong>Install to an internal drive</strong> (permanent deployment on new hardware)</p></blockquote>\n<hr>\n<h2 id=\"table-of-contents\">Table of Contents</h2>\n<ol>\n<li><a href=\"#/LIVE_USB_IMAGE#1-overview--strategy\" data-wiki-page=\"1\">Overview &amp;amp; Strategy</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#2-prerequisites\" data-wiki-page=\"1\">Prerequisites</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#3-pre-image-cleanup\" data-wiki-page=\"1\">Pre-Image Cleanup</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#4-method-a--penguins-eggs-recommended\" data-wiki-page=\"1\">Method A — penguins-eggs (Recommended)</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#5-method-b--manual-squashfs--grub-iso\" data-wiki-page=\"1\">Method B — Manual SquashFS + GRUB ISO</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#6-writing-the-iso-to-usb\" data-wiki-page=\"1\">Writing the ISO to USB</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#7-booting-the-live-usb\" data-wiki-page=\"1\">Booting the Live USB</a> — <strong>§7 durable data on exFAT</strong> (not union persistence)</li>\n<li><a href=\"#/LIVE_USB_IMAGE#8-installing-to-a-drive-from-live-usb\" data-wiki-page=\"1\">Installing to a Drive from Live USB</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#9-post-install-steps-on-new-hardware\" data-wiki-page=\"1\">Post-Install Steps on New Hardware</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#10-troubleshooting\" data-wiki-page=\"1\">Troubleshooting</a></li>\n<li><a href=\"#/LIVE_USB_IMAGE#11-reference-whats-on-the-server\" data-wiki-page=\"1\">Reference: What's on the Server</a></li>\n</ol>\n<hr>\n<h2 id=\"1-overview-strategy\">1. Overview &amp; Strategy</h2>\n<p>The server is a <strong>headless Ubuntu 24.04 LTS</strong> box running a minimal X11 stack (nodm + Openbox) with CasparCG, CasparCG Scanner, HighAsCG, and professional broadcast I/O (DeckLink, NDI). There is no full desktop environment.</p>\n<p><strong>penguins-eggs</strong> is the recommended tool because it was purpose-built to snapshot a <em>running</em> Linux system into a redistributable live ISO. It handles the kernel, initramfs, squashfs compression, GRUB/EFI boot, and bundles a text-based installer (Krill) — all in one command.</p>\n<p>The <strong>manual method</strong> (Section 5) is a fallback for environments where penguins-eggs cannot be installed or when you need full control over the ISO layout.</p>\n<blockquote><p>[!IMPORTANT] <strong>Hardware-specific drivers</strong> (NVIDIA, DeckLink DKMS modules) are compiled for the running kernel. The live image will work best on identical or similar hardware. On different hardware, generic VESA/nouveau will be used for video output, and DeckLink modules may need to be rebuilt (see §9).</p></blockquote>\n<hr>\n<h2 id=\"2-prerequisites\">2. Prerequisites</h2>\n<p>All commands run on the <strong>live production server</strong> (the source machine) as <code>root</code> or via <code>sudo</code>.</p>\n<h3 id=\"21-disk-space\">2.1 Disk Space</h3>\n<p>You need roughly <strong>2× the used space</strong> on the system partition available somewhere for the build:</p>\n<pre><code class=\"language-bash\"># Check used space\ndf -h /\ndu -sh /opt/casparcg /opt/highascg\n\n# Typical: ~8–15 GB used → need ~16–30 GB free for build\n# If / is too small, mount external storage at e.g. /mnt/build</code></pre>\n<h3 id=\"22-kernel-headers-for-dkms-modules-in-the-image\">2.2 Kernel Headers (for DKMS modules in the image)</h3>\n<pre><code class=\"language-bash\">apt install -y linux-headers-$(uname -r)</code></pre>\n<h3 id=\"23-required-packages\">2.3 Required Packages</h3>\n<pre><code class=\"language-bash\">apt update\napt install -y git rsync xorriso squashfs-tools dosfstools mtools grub-efi-amd64-bin grub-pc-bin</code></pre>\n<hr>\n<h2 id=\"3-pre-image-cleanup\">3. Pre-Image Cleanup</h2>\n<p>Before creating the image, clean up ephemeral data to keep the ISO small and avoid leaking secrets.</p>\n<pre><code class=\"language-bash\"># Stop CasparCG (gracefully)\npkill -f '/usr/bin/casparcg-server-2.5' || true\nsleep 3\n\n# Clear CasparCG logs and CEF cache\nrm -rf /opt/casparcg/log/*\nrm -rf /opt/casparcg/cef-cache/*\nrm -f /tmp/caspar.log\n\n# Clear HighAsCG logs (if any)\nrm -rf /opt/highascg/logs/* 2&gt;/dev/null || true\n\n# Clear apt cache\napt clean\n\n# Clear temp files\nrm -rf /tmp/* /var/tmp/*\n\n# Clear bash history (optional — remove if you want to keep it)\n&gt; /root/.bash_history\n&gt; /home/casparcg/.bash_history 2&gt;/dev/null || true\n\n# Clear journal logs older than 1 day\njournalctl --vacuum-time=1d\n\n# Remove SSH host keys (regenerated on first boot of the new install)\n# SKIP THIS if you want to keep them for live use\n# rm -f /etc/ssh/ssh_host_*\n\n# Remove Tailscale state if you don't want it cloned (optional)\n# systemctl stop tailscaled\n# rm -rf /var/lib/tailscale\n\n# Remove Syncthing state if you don't want it cloned (optional)\n# systemctl --user -M casparcg@ stop syncthing 2&gt;/dev/null || true\n# rm -rf /home/casparcg/.config/syncthing\n\n# Optional: remove large media you do not need in the image\n# (full playout media tree is often huge — exclude via §4.2.1 or clear selectively)\n# rm -rf /opt/casparcg/media/*\n# or only heavy files: rm -f /opt/casparcg/media/clip_xyz.mp4\n\n# Optional: another local user's home — large project or CI artifacts\n# rm -rf /home/otheruser/your-build-dir</code></pre>\n<blockquote><p>[!TIP] Keep <code>/opt/casparcg/media/</code> if you want a &quot;ready to playout&quot; image that includes all media files. For a system-only or thin image, remove media, clear other users’ build trees (see §4.2.1), and use penguins-eggs excludes for paths you cannot delete.</p></blockquote>\n<hr>\n<h2 id=\"4-method-a-penguins-eggs-recommended\">4. Method A — penguins-eggs (Recommended)</h2>\n<h3 id=\"40-nodejs-22-required-before-install\">4.0 Node.js 22+ (required before install)</h3>\n<p>Current <strong>penguins-eggs</strong> packages declare <code>Depends: nodejs (&gt;= 22)</code> on the <strong>Debian <code>nodejs</code> package</strong> — not merely a <code>node</code> binary on <code>PATH</code>. If you use nvm, a manual tarball, or another copy of Node, <code>node -v</code> can show 22 while <code>apt</code> still has Ubuntu’s <code>nodejs</code> <strong>18.x</strong>, and the eggs <code>.deb</code> will keep failing until the <strong>apt</strong> package is upgraded.</p>\n<p>Check what apt will use:</p>\n<pre><code class=\"language-bash\">apt-cache policy nodejs</code></pre>\n<p>If the only install candidate is <code>18.x</code> from <code>archive.ubuntu.com</code>, continue below.</p>\n<p><strong>Install Node 22 as a system package</strong> — for example with <a href=\"https://github.com/nodesource/distributions\">NodeSource</a>:</p>\n<pre><code class=\"language-bash\">curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -\nsudo apt-get install -y nodejs</code></pre>\n<p><strong>Verify the package, not only the CLI</strong> (all should show 22+ from NodeSource or equivalent, not 18 from Ubuntu):</p>\n<pre><code class=\"language-bash\">dpkg -l nodejs\napt-cache policy nodejs\nnode -v</code></pre>\n<p><strong>If the NodeSource script errors with <code>Failed to run 'apt update'</code></strong> (or any <code>apt update</code> failure), a broken or obsolete third-party repo is often the cause — for example a PPA with no <em>Release</em> for your Ubuntu version (HTTP 404). The script cannot add its repo until <code>apt update</code> is clean. Find and fix the bad source (remove the PPA, or delete/comment the <code>.list</code> under <code>/etc/apt/sources.list.d/</code>), then:</p>\n<pre><code class=\"language-bash\">sudo apt update</code></pre>\n<p>Re-run the <code>curl … setup_22.x</code> line and <code>sudo apt-get install -y nodejs</code> again.</p>\n<p>If you already have <code>nodejs</code> 18 from Ubuntu and only need eggs for this one-off ISO build, replacing it with 22+ is usually fine. <strong>HighAsCG</strong> itself expects a current Node (≥ 20; see the project README) — re-test <code>systemctl status highascg</code> after changing the system <code>node</code> if the service is installed.</p>\n<h3 id=\"41-install-penguins-eggs\">4.1 Install penguins-eggs</h3>\n<pre><code class=\"language-bash\">cd /tmp\ngit clone https://github.com/pieroproietti/fresh-eggs\ncd fresh-eggs\nsudo ./fresh-eggs.sh</code></pre>\n<p>This installs <code>eggs</code> and all dependencies (including <code>calamares</code> components if a GUI is available, and <code>krill</code> for TUI install).</p>\n<p>Verify:</p>\n<pre><code class=\"language-bash\">sudo eggs --version</code></pre>\n<h3 id=\"42-configure-eggs\">4.2 Configure eggs</h3>\n<p>Run the interactive configuration wizard:</p>\n<pre><code class=\"language-bash\">sudo eggs dad -d</code></pre>\n<p>This sets defaults for the ISO name, compression, and output directory. You can accept all defaults — ISOs will be stored in <code>/home/eggs/</code>.</p>\n<p>Alternatively, edit <code>/etc/penguins-eggs.d/eggs.yaml</code> directly:</p>\n<pre><code class=\"language-yaml\"># Key settings to verify/adjust:\nsnapshot_basename: highascg-server\nsnapshot_prefix: &quot;&quot;\ncompression: zstd           # fast compression, good ratio</code></pre>\n<h3 id=\"421-highascg-empty-mount-points-in-the-squashfs-casparcg\">4.2.1 HighAsCG: empty mount points in the squashfs (<code>casparcg</code>)</h3>\n<p>This repository ships a <strong>mksquashfs exclude fragment</strong> and helper scripts in <code>tools/eggs/live-usb/</code>. The goal: <strong>runtime trees under <code>/home/casparcg/highascg</code> are empty in the live image</strong> (media, logs, cache, etc.), while <strong>WO‑47</strong> mount point <strong><code>/home/casparcg/exfat</code></strong> still exists as an <strong>empty directory</strong> so systemd can mount the operator exFAT volume after boot.</p>\n<p>penguins-eggs calls <code>mksquashfs</code> with <strong><code>-wildcards</code></strong>. The patterns use *<em><code>/</em></code><strong> where needed so </strong>only the contents<strong> of each directory are excluded; </strong>parent<strong> directory nodes remain in the squash when they were created on the build host. Paths in the fragment are </strong>relative to the imaged root, no leading slash** (see upstream <code>penguins-eggs</code> <code>conf/exclude.list.d/master.list</code> for the same style).</p>\n<p><strong>Produced files (copy or clone the repo on the build host):</strong></p>\n<table><thead><tr><th>File</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list</code></td><td>Paths under <code>home/casparcg/highascg/…</code> (and caches) to omit from squashfs — merge into eggs</td></tr>\n<tr><td><code>tools/eggs/live-usb/ensure-empty-live-usb-dirs.sh</code></td><td><code>mkdir</code> + optional <code>chown</code> for <strong><code>casparcg</code></strong> on <strong><code>/home/casparcg/exfat</code></strong> before imaging</td></tr>\n<tr><td><code>tools/eggs/live-usb/merge-penguins-eggs-exclude-highascg.sh</code></td><td>Appends the fragment to <code>/etc/penguins-eggs.d/exclude.list</code> (idempotent)</td></tr>\n<tr><td><code>tools/eggs/live-usb/prepare-eggs-clone-with-exfat.sh</code></td><td><strong>Preferred one-shot:</strong> WO-47 systemd units, <strong><code>exfat-sync</code></strong>, <strong><code>highascg.service</code></strong> deps, <strong><code>exfatprogs</code></strong>, empty mount stubs, merge excludes (<strong><a href=\"../../tools/eggs/live-usb/EXFAT_DATA_ZERO_TOUCH.md\">&lt;code&gt;EXFAT_DATA_ZERO_TOUCH.md&lt;/code&gt;</a></strong>)</td></tr>\n</tbody></table>\n<p><strong>Shortcut (recommended):</strong> run <strong><code>sudo bash tools/eggs/live-usb/prepare-eggs-clone-with-exfat.sh</code></strong> once <strong><code>/etc/penguins-eggs.d/exclude.list</code></strong> exists; it performs steps 1–2 below (<code>ensure-empty-live-usb-dirs</code> + merge fragment) and bakes WO-47 into <strong><code>/etc</code></strong>. <strong><code>tools/eggs/live-usb/build-highascg-egg.sh</code></strong> invokes it automatically.</p>\n<p>Alternatively, do the steps manually:</p>\n<p><strong>On the source host (as root), before the final <code>eggs produce</code>:</strong></p>\n<ol>\n<li><strong>Empty mount points on disk</strong> (so the squash has those folder nodes). On a</li>\n</ol>\n<p>machine that already has <strong><code>casparcg</code></strong>:</p>\n<pre><code class=\"language-bash\">   cd /path/to/highascg   # this repo checkout\n   sudo ./tools/eggs/live-usb/ensure-empty-live-usb-dirs.sh</code></pre>\n<p>If <strong><code>casparcg</code></strong> does not exist yet, the script still creates the directories (owned by root); fix ownership after user creation if needed.</p>\n<ol>\n<li><strong>Merge the fragment</strong> into the eggs config file. This requires</li>\n</ol>\n<p><code>/etc/penguins-eggs.d/exclude.list</code> to <strong>already exist</strong> — on most installs, the first <code>eggs produce</code> without <code>--excludes static</code> (or a documented config pass) creates it. If the merge script errors with “file not found”, run a first normal <code>eggs produce</code> once, <em>then</em> merge, <em>then</em> the final produce in step 3.</p>\n<pre><code class=\"language-bash\">   sudo ./tools/eggs/live-usb/merge-penguins-eggs-exclude-highascg.sh</code></pre>\n<ol>\n<li><strong>Build the live image</strong> with <strong><code>--excludes static</code></strong> so eggs does <strong>not</strong></li>\n</ol>\n<p>overwrite <code>exclude.list</code> and remove the HighAsCG block:</p>\n<pre><code class=\"language-bash\">   sudo eggs produce --clone --excludes static --basename &quot;highascg-server&quot;</code></pre>\n<p>After you adopt this workflow, use <strong><code>--excludes static</code> whenever you</strong> run <code>eggs produce</code> and want to keep the fragment.</p>\n<p><strong>Optional / notes</strong></p>\n<ul>\n<li>The stock flags <strong><code>--excludes homes</code></strong> / <strong><code>--excludes home</code></strong> add eggs’</li>\n</ul>\n<p>own templates; they are <strong>not</strong> a replacement for the HighAsCG fragment.</p>\n<ul>\n<li><strong>Revert:</strong> in <code>/etc/penguins-eggs.d/exclude.list</code>, delete the block that</li>\n</ul>\n<p>starts with the marker line <code># --- HighAsCG tools/eggs/live-usb: merge-penguins-eggs-exclude-highascg.sh ---</code>.</p>\n<ul>\n<li><strong>Other large paths</strong> can still be cleared in §3, or you can append more</li>\n</ul>\n<p>lines to <code>exclude.list</code> (same format) before a <code>--excludes static</code> run.</p>\n<ul>\n<li>On a <strong>very old mksquashfs</strong>, if <code>/*</code> does not do what you expect, delete</li>\n</ul>\n<p>everything <em>under</em> those dirs on the source (§3) and keep only the empty directories, then run <code>eggs produce</code> without changing the list.</p>\n<h3 id=\"43-produce-the-iso\">4.3 Produce the ISO</h3>\n<pre><code class=\"language-bash\"># --clone  : include all user accounts and data (see §4.2.1 for the HighAsCG\n#             fragment + ensure-empty dirs so casparcg mount points stay empty)\n# --excludes static : do not rebuild exclude.list (required after merge script)\n# --basename : name for the ISO file\n# --prefix : prefix (empty = cleaner name)\n\nsudo eggs produce --clone --excludes static --basename &quot;highascg-server&quot;</code></pre>\n<blockquote><p>[!WARNING] <strong><code>--clone</code> copies the whole system and user tree</strong> by default, including every user under <code>/home/<em></code> and all of <code>/opt/casparcg</code> and <code>/opt/highascg</code>. Use §4.2.1 (merge the HighAsCG fragment, then use <strong><code>--excludes static</code></strong>) so <strong><code>casparcg</code></strong> HighAsCG runtime dirs and mount parents are present but empty in the image. If you want a &quot;clean&quot; image without </em>any* user data (only packages and system config), omit <code>--clone</code> instead and do not rely on the fragment.</p></blockquote>\n<p>This process:</p>\n<ol>\n<li>Creates a squashfs of the entire filesystem</li>\n<li>Builds an initramfs with casper/live hooks</li>\n<li>Generates GRUB config for BIOS + UEFI boot</li>\n<li>Packages everything into an ISO</li>\n</ol>\n<p><strong>Expected output:</strong> <code>/home/eggs/highascg-server-amd64.iso</code></p>\n<p><strong>Time:</strong> 10–30 minutes depending on disk size and CPU.</p>\n<h3 id=\"44-verify-the-iso\">4.4 Verify the ISO</h3>\n<pre><code class=\"language-bash\">ls -lh /home/eggs/*.iso\n\n# Quick sanity check — mount and inspect\nmkdir -p /mnt/iso\nmount -o loop /home/eggs/highascg-server-*.iso /mnt/iso\nls /mnt/iso/\n# Should see: boot/ casper/ EFI/ (or live/) etc.\nls /mnt/iso/casper/\n# Should see: filesystem.squashfs, vmlinuz, initrd\numount /mnt/iso</code></pre>\n<hr>\n<h2 id=\"5-method-b-manual-squashfs-grub-iso\">5. Method B — Manual SquashFS + GRUB ISO</h2>\n<p>Use this if penguins-eggs is not available or you want full control.</p>\n<h3 id=\"51-create-working-directories\">5.1 Create Working Directories</h3>\n<pre><code class=\"language-bash\">export WORK=/mnt/build/live-build    # adjust to a location with enough space\nmkdir -p $WORK/{staging/{EFI/boot,boot/grub/x86_64-efi,isolinux,live},tmp}</code></pre>\n<h3 id=\"52-create-the-squashfs\">5.2 Create the SquashFS</h3>\n<p>This compresses the entire root filesystem into a single file:</p>\n<pre><code class=\"language-bash\">mksquashfs / $WORK/staging/live/filesystem.squashfs \\\n  -comp zstd \\\n  -Xcompression-level 15 \\\n  -e /boot \\\n  -e /proc \\\n  -e /sys \\\n  -e /dev \\\n  -e /run \\\n  -e /tmp \\\n  -e /mnt \\\n  -e /media \\\n  -e /lost+found \\\n  -e /swapfile \\\n  -e /home/eggs \\\n  -e $WORK \\\n  -wildcards -e '*.log' \\\n  -no-recovery \\\n  -info</code></pre>\n<blockquote><p>[!NOTE] <code>-e</code> excludes pseudo-filesystems and the build directory itself. Add more exclusions as needed (e.g., <code>-e /opt/casparcg/media</code> to exclude media files).</p></blockquote>\n<h3 id=\"53-copy-kernel-and-initramfs\">5.3 Copy Kernel and Initramfs</h3>\n<pre><code class=\"language-bash\"># Copy the currently running kernel and initramfs\ncp /boot/vmlinuz-$(uname -r) $WORK/staging/live/vmlinuz\ncp /boot/initrd.img-$(uname -r) $WORK/staging/live/initrd\n\n# Also need the kernel for the boot directory\ncp /boot/vmlinuz-$(uname -r) $WORK/staging/boot/vmlinuz\ncp /boot/initrd.img-$(uname -r) $WORK/staging/boot/initrd</code></pre>\n<h3 id=\"54-install-casper-live-boot-support\">5.4 Install casper (Live Boot Support)</h3>\n<p>The system needs <code>casper</code> to boot as a live system. If not already installed:</p>\n<pre><code class=\"language-bash\">apt install -y casper lupin-casper</code></pre>\n<p>Regenerate the initramfs with casper support:</p>\n<pre><code class=\"language-bash\">update-initramfs -u -k $(uname -r)\n# Re-copy the updated initramfs\ncp /boot/initrd.img-$(uname -r) $WORK/staging/live/initrd\ncp /boot/initrd.img-$(uname -r) $WORK/staging/boot/initrd</code></pre>\n<h3 id=\"55-create-grub-configuration\">5.5 Create GRUB Configuration</h3>\n<pre><code class=\"language-bash\">cat &gt; $WORK/staging/boot/grub/grub.cfg &lt;&lt; 'GRUB_EOF'\ninsmod all_video\n\nset default=&quot;0&quot;\nset timeout=10\n\nmenuentry &quot;HighAsCG Server — Live (RAM)&quot; {\n    linux /live/vmlinuz boot=casper quiet splash ---\n    initrd /live/initrd\n}\n\nmenuentry &quot;HighAsCG Server — Live (toram — full copy to RAM)&quot; {\n    linux /live/vmlinuz boot=casper toram quiet splash ---\n    initrd /live/initrd\n}\n\nmenuentry &quot;HighAsCG Server — Live (nomodeset — safe graphics)&quot; {\n    linux /live/vmlinuz boot=casper nomodeset quiet splash ---\n    initrd /live/initrd\n}\nGRUB_EOF</code></pre>\n<h3 id=\"56-create-efi-boot-image\">5.6 Create EFI Boot Image</h3>\n<pre><code class=\"language-bash\">cat &gt; $WORK/tmp/grub-standalone.cfg &lt;&lt; 'EOF'\nsearch --set=root --file /live/vmlinuz\nset prefix=($root)/boot/grub/\nconfigfile /boot/grub/grub.cfg\nEOF\n\ngrub-mkstandalone \\\n  --format=x86_64-efi \\\n  --output=$WORK/tmp/bootx64.efi \\\n  --locales=&quot;&quot; \\\n  --fonts=&quot;&quot; \\\n  &quot;boot/grub/grub.cfg=$WORK/tmp/grub-standalone.cfg&quot;\n\n# Create a FAT12 EFI system partition image\ndd if=/dev/zero of=$WORK/staging/EFI/boot/efiboot.img bs=1M count=20\nmkfs.vfat $WORK/staging/EFI/boot/efiboot.img\nmmd -i $WORK/staging/EFI/boot/efiboot.img EFI EFI/BOOT\nmcopy -i $WORK/staging/EFI/boot/efiboot.img \\\n  $WORK/tmp/bootx64.efi ::EFI/BOOT/BOOTX64.EFI</code></pre>\n<h3 id=\"57-create-bios-boot-image-legacy\">5.7 Create BIOS Boot Image (Legacy)</h3>\n<pre><code class=\"language-bash\">grub-mkstandalone \\\n  --format=i386-pc \\\n  --output=$WORK/tmp/core.img \\\n  --install-modules=&quot;linux normal iso9660 biosdisk memdisk search tar ls&quot; \\\n  --modules=&quot;linux normal iso9660 biosdisk search&quot; \\\n  --locales=&quot;&quot; \\\n  --fonts=&quot;&quot; \\\n  &quot;boot/grub/grub.cfg=$WORK/tmp/grub-standalone.cfg&quot;\n\ncat /usr/lib/grub/i386-pc/cdboot.img $WORK/tmp/core.img &gt; \\\n  $WORK/staging/boot/grub/bios.img</code></pre>\n<h3 id=\"58-build-the-iso\">5.8 Build the ISO</h3>\n<pre><code class=\"language-bash\">xorriso \\\n  -as mkisofs \\\n  -iso-level 3 \\\n  -o $WORK/highascg-server-live.iso \\\n  -full-iso9660-filenames \\\n  -volid &quot;HIGHASCG_LIVE&quot; \\\n  --grub2-mbr /usr/lib/grub/i386-pc/boot_hybrid.img \\\n  -partition_offset 16 \\\n  --mbr-force-bootable \\\n  -append_partition 2 28732ac11ff8d211ba4b00a0c93ec93b \\\n    $WORK/staging/EFI/boot/efiboot.img \\\n  -appended_part_as_gpt \\\n  -iso_mbr_part_type a2a0d0ebe5b9334487c068b6b72699c7 \\\n  -eltorito-boot boot/grub/bios.img \\\n    -no-emul-boot -boot-load-size 4 -boot-info-table --grub2-boot-info \\\n  -eltorito-catalog boot/grub/boot.cat \\\n  -eltorito-alt-boot \\\n  -e '--interval:appended_partition_2:::' \\\n    -no-emul-boot \\\n  $WORK/staging\n\necho &quot;ISO created: $WORK/highascg-server-live.iso&quot;\nls -lh $WORK/highascg-server-live.iso</code></pre>\n<hr>\n<h2 id=\"6-writing-the-iso-to-usb\">6. Writing the ISO to USB</h2>\n<h3 id=\"from-the-server-or-any-linux-machine\">From the Server (or any Linux machine)</h3>\n<pre><code class=\"language-bash\"># Identify your USB device (BE CAREFUL — wrong device = data loss!)\nlsblk\n\n# Write (replace /dev/sdX with your USB device — NOT a partition like /dev/sdX1)\nsudo dd if=/path/to/highascg-server-*.iso of=/dev/sdX bs=4M status=progress oflag=sync\n\n# Or use a safer alternative:\nsudo cp /path/to/highascg-server-*.iso /dev/sdX\nsync\nsudo partprobe /dev/sdX</code></pre>\n<p><strong>HighAsCG — after <code>dd</code>, add the exFAT data partition</strong> (label <strong><code>HIGHASCGEXF</code></strong>) for configs, media, and server drops — see <strong><code>docs/WO47_ISO_VS_EXFAT.md</code></strong>, <strong><code>tools/eggs/live-usb/EXFAT_DATA_ZERO_TOUCH.md</code></strong>. Boot plain <strong>Live</strong> (no union <strong><code>/ union</code></strong> persistence partition).</p>\n<h3 id=\"from-macos\">From macOS</h3>\n<pre><code class=\"language-bash\"># List disks\ndiskutil list\n\n# Unmount the USB disk (replace diskN)\ndiskutil unmountDisk /dev/diskN\n\n# Write (use rdiskN for faster raw writes)\nsudo dd if=/path/to/highascg-server-*.iso of=/dev/rdiskN bs=4m status=progress\n\n# Eject\ndiskutil eject /dev/diskN</code></pre>\n<h3 id=\"from-windows\">From Windows</h3>\n<p>Use <strong><a href=\"https://etcher.balena.io/\">balenaEtcher</a></strong> or <strong>Rufus</strong> (select &quot;DD Image mode&quot; in Rufus).</p>\n<hr>\n<h2 id=\"7-booting-the-live-usb\">7. Booting the Live USB</h2>\n<ol>\n<li>Plug the USB into the target machine.</li>\n<li>Enter BIOS/UEFI (usually <code>F2</code>, <code>F12</code>, <code>Del</code>, or <code>Esc</code> at POST).</li>\n<li><strong>Disable Secure Boot</strong> (NVIDIA and DeckLink DKMS modules are unsigned).</li>\n<li>Set USB as first boot device, or use the boot menu (<code>F12</code> on most boards).</li>\n<li>In GRUB, pick the default <strong>Live</strong> entry (disposable RAM overlay is fine for the OS root; operator data lives on exFAT).</li>\n</ol>\n<h3 id=\"what-happens-on-boot\">What Happens on Boot</h3>\n<p>The live system will:</p>\n<ol>\n<li>Load the kernel and initramfs from the USB.</li>\n<li>Mount <code>filesystem.squashfs</code> as a read-only root via overlayfs.</li>\n<li>Start <code>nodm</code> → Openbox → CasparCG autostart (if display-mode is <code>normal</code>).</li>\n<li>Start the <code>highascg.service</code> (HighAsCG web GUI on port 4200).</li>\n</ol>\n<blockquote><p>[!TIP] If using <strong>penguins-eggs</strong>, the default live credentials are: - <strong>User:</strong> <code>live</code> / <strong>Password:</strong> <code>evolution</code> - <strong>Root:</strong> <code>root</code> / <strong>Password:</strong> <code>evolution</code>  If you used <code>--clone</code>, your original <code>casparcg</code> user and SSH keys are also present.</p></blockquote>\n<h3 id=\"71-why-data-vanishes-on-a-plain-live-session\">7.1 Why data vanishes on a plain Live session</h3>\n<p>A normal <strong>live session</strong> stacks a <strong>writable layer</strong> on top of the read-only <strong><code>filesystem.squashfs</code></strong>. That layer is usually <strong>RAM</strong> (<code>tmpfs</code>), so <strong>reboot clears</strong> anything written only under <strong><code>/home</code></strong>, <strong><code>/etc</code></strong>, or <strong><code>~/highascg</code></strong> during the session.</p>\n<p><strong>Operator rule:</strong> keep anything that must survive on the <strong>exFAT volume</strong> (<code>HIGHASCGEXF</code> → <strong><code>/home/casparcg/exfat</code></strong>), not on the live overlay.</p>\n<h3 id=\"72-durable-storage-exfat-only-current-product\">7.2 Durable storage (exFAT only — current product)</h3>\n<table><thead><tr><th>What</th><th>Where</th></tr></thead><tbody>\n<tr><td>Modular config + Caspar XML</td><td>exFAT <strong><code>configs/</code></strong> ↔ <strong><code>~/highascg/config/</code></strong> (boot sync)</td></tr>\n<tr><td>Optional monolithic config</td><td><strong><code>drop-config/highascg.config.json</code></strong></td></tr>\n<tr><td>UI/state JSON</td><td><strong><code>configs/.highascg-state.json</code></strong>, <strong><code>.module-state.json</code></strong></td></tr>\n<tr><td>Media / templates</td><td>exFAT <strong><code>media/</code></strong>, <strong><code>templates/</code></strong> (bind under playout tree when prepared)</td></tr>\n<tr><td>Server code drops</td><td><strong><code>drop-update/</code></strong> → applied by <strong><code>highascg-exfat-server-update.service</code></strong></td></tr>\n</tbody></table>\n<p>See <strong><code>docs/WO47_ISO_VS_EXFAT.md</code></strong>, <strong><code>docs/EXFAT_SERVER_UPDATE.md</code></strong>, <strong><code>config/exfat-sync.json</code></strong>.</p>\n<p><strong>Removed (do not use on new sticks):</strong></p>\n<ul>\n<li>Union <strong><code>/ union</code></strong> ext4 <strong><code>persistence</code></strong> partition + GRUB <strong>Live with persistence</strong></li>\n<li>WO-38 <strong><code>media/drive</code></strong> internal partition mount + Settings <strong>Mount</strong> UI</li>\n</ul>\n<p>HighAsCG <strong>no longer waits</strong> on partition mount at startup — <strong><code>systemctl restart highascg</code></strong> should complete in seconds.</p>\n<h3 id=\"73-install-to-internal-disk-optional\">7.3 Install to internal disk (optional)</h3>\n<p>For a permanent machine, use §8 (Calamares) instead of relying on live overlay or exFAT. <strong>Full guide:</strong> <a href=\"#/CALAMARES_INSTALL_TO_DISK\" data-wiki-page=\"1\">CALAMARES_INSTALL_TO_DISK.md</a>.</p>\n<hr>\n<h2 id=\"8-installing-to-a-drive-from-live-usb\">8. Installing to a Drive from Live USB</h2>\n<p>Once booted into the live environment, you can install the system permanently.</p>\n<p><strong>Canonical guide:</strong> <a href=\"#/CALAMARES_INSTALL_TO_DISK\" data-wiki-page=\"1\">CALAMARES_INSTALL_TO_DISK.md</a> — <strong>disable CSM</strong> (UEFI install), disk selection, <strong>Erase disk</strong>, manual <strong>UEFI ESP</strong> vs Legacy <strong><code>bios_grub</code></strong> + <strong><code>/</code></strong>, common failures (<code>rsync</code> 11, bootloader error 1).</p>\n<h3 id=\"81-calamares-recommended-on-live-iso\">8.1 Calamares (recommended on live ISO)</h3>\n<p><strong>Before opening Calamares:</strong> in firmware, <strong>disable CSM</strong> (Compatibility Support Module / Legacy boot) and boot the stick from the <strong>UEFI:</strong> entry. With CSM on, Calamares often runs in Legacy mode and <strong>GRUB install fails</strong> (error code 1). Confirm: <code>[ -d /sys/firmware/efi ] &amp;&amp; echo UEFI</code>.</p>\n<p>From the operator UI: <strong>Settings → Nuclear → Install to disk</strong>, or:</p>\n<pre><code class=\"language-bash\">sudo -n /usr/local/bin/launch-calamares.sh</code></pre>\n<p><strong>Pick the internal disk</strong> (Kingston, NVMe, …) — <strong>not</strong> the USB stick or its <strong>exFAT</strong> partition (<code>HIGHASCGEXF</code>, often <code>sda3</code> on the stick). Use <code>lsblk -o NAME,SIZE,MODEL,TRAN</code> and <code>sudo /usr/local/lib/highascg/probe-internal-storage.sh --check</code> before opening Calamares.</p>\n<p><strong>Easiest:</strong> <strong>Erase disk and install</strong> on the internal drive. Swap: <strong>none</strong>.</p>\n<p><strong>Manual partitioning (Legacy BIOS + GPT):</strong> one <strong>1–2 MiB</strong> partition, <strong>unformatted</strong>, flag <strong><code>bios_grub</code></strong>, plus <strong>ext4</strong> mounted <strong><code>/</code></strong>. Without <code>bios_grub</code>, partition/GRUB steps often fail.</p>\n<p><strong>Manual partitioning (UEFI):</strong> <strong>fat32</strong> <strong>≥ 300 MiB</strong> → <strong><code>/boot/efi</code></strong> (flags <strong>boot</strong>, <strong>esp</strong>), plus <strong>ext4</strong> <strong><code>/</code></strong>.</p>\n<h3 id=\"82-krill-text-installer-eggs-build-host-only\">8.2 Krill (text installer — eggs build host only)</h3>\n<p><code>eggs krill</code> is available on the <strong>eggs build machine</strong>, not on the live ISO (<code>penguins-eggs</code> is excluded from squashfs). On the stick, use <strong>Calamares</strong> (§8.1).</p>\n<h3 id=\"83-manual-installation-for-the-manual-iso-method\">8.3 Manual Installation (for the manual ISO method)</h3>\n<p>If your ISO was built manually (Method B), there is no bundled installer. Use this procedure:</p>\n<pre><code class=\"language-bash\"># 1. Partition the target disk\n#    Example: /dev/sda with GPT, 512MB EFI + rest ext4\nparted /dev/sda -- mklabel gpt\nparted /dev/sda -- mkpart ESP fat32 1MiB 513MiB\nparted /dev/sda -- set 1 esp on\nparted /dev/sda -- mkpart primary ext4 513MiB 100%\n\nmkfs.fat -F32 /dev/sda1\nmkfs.ext4 /dev/sda2\n\n# 2. Mount target\nmount /dev/sda2 /mnt\nmkdir -p /mnt/boot/efi\nmount /dev/sda1 /mnt/boot/efi\n\n# 3. Extract the squashfs to the target\nunsquashfs -f -d /mnt /path/to/live/filesystem.squashfs\n\n# 4. Bind-mount for chroot\nmount --bind /dev /mnt/dev\nmount --bind /dev/pts /mnt/dev/pts\nmount --bind /proc /mnt/proc\nmount --bind /sys /mnt/sys\n\n# 5. Chroot and install bootloader\nchroot /mnt /bin/bash\n\n# Inside chroot:\n# Update fstab with the new disk UUIDs\nblkid   # note the UUIDs\ncat &gt; /etc/fstab &lt;&lt; EOF\nUUID=&lt;root-uuid&gt;  /          ext4  errors=remount-ro  0 1\nUUID=&lt;efi-uuid&gt;   /boot/efi  vfat  umask=0077         0 1\nEOF\n\n# Install GRUB\ngrub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=GRUB\nupdate-grub\n\n# Regenerate initramfs\nupdate-initramfs -u -k all\n\n# Remove casper (not needed for installed system)\napt remove -y casper lupin-casper\n\n# Regenerate SSH host keys\ndpkg-reconfigure openssh-server\n\nexit  # exit chroot\n\n# 6. Unmount and reboot\numount -R /mnt\nreboot</code></pre>\n<hr>\n<h2 id=\"9-post-install-steps-on-new-hardware\">9. Post-Install Steps on New Hardware</h2>\n<p>After installing on a new machine, run these checks:</p>\n<h3 id=\"91-gpu-drivers\">9.1 GPU Drivers</h3>\n<p>Each live ISO is built with <strong>one</strong> NVIDIA branch (<strong>535</strong>, <strong>580</strong>, or <strong>595</strong>). Set <strong><code>HIGHASCG_NVIDIA_DRIVER</code></strong> on the build host before <strong><code>install.sh</code></strong> and <strong><code>eggs produce</code></strong> — see <strong><code>work/work-orders/WO_single-nvidia-driver-per-iso.md</code></strong>. <strong><code>GET /api/system/gpu-nvidia</code></strong> shows the baked branch and a GPU recommendation guide; driver switching in Settings was removed.</p>\n<pre><code class=\"language-bash\"># Check NVIDIA\nnvidia-smi\n\n# If NVIDIA drivers are missing (different GPU), reinstall:\napt install -y ubuntu-drivers-common\nubuntu-drivers autoinstall\nreboot</code></pre>\n<h3 id=\"92-decklink-drivers\">9.2 DeckLink Drivers</h3>\n<pre><code class=\"language-bash\"># Check DeckLink\nlsmod | grep blackmagic\n\n# If module missing (different kernel), rebuild DKMS:\ndpkg-reconfigure desktopvideo\n# or reinstall:\ndpkg -i /path/to/desktopvideo_*.deb\napt install -f -y\nsudo modprobe blackmagic_io</code></pre>\n<h3 id=\"93-casparcg-cef-verification\">9.3 CasparCG &amp; CEF Verification</h3>\n<pre><code class=\"language-bash\"># Verify CasparCG binary\n/usr/bin/casparcg-server-2.5 --version 2&gt;/dev/null || echo &quot;Check caspar install&quot;\n\n# Verify CEF libraries are correctly linked\nldd /usr/bin/casparcg-server-2.5 | grep -i cef\n\n# Verify ld.so config\ncat /etc/ld.so.conf.d/casparcg.conf\nldconfig -v 2&gt;/dev/null | grep -i cef</code></pre>\n<h3 id=\"94-highascg-service\">9.4 HighAsCG Service</h3>\n<pre><code class=\"language-bash\">systemctl status highascg\njournalctl -u highascg --no-pager -n 20\n\n# If not installed as service:\ncd /opt/highascg &amp;&amp; node index.js</code></pre>\n<h3 id=\"95-network-firewall\">9.5 Network &amp; Firewall</h3>\n<pre><code class=\"language-bash\"># Check UFW rules\nufw status\n\n# Re-apply if needed\nufw allow 5250/tcp comment &quot;CasparCG AMCP&quot;\nufw allow 8000/tcp comment &quot;CasparCG Scanner&quot;\nufw allow 4200/tcp comment &quot;HighAsCG Web GUI&quot;\nufw allow 6250/udp comment &quot;CasparCG OSC&quot;</code></pre>\n<h3 id=\"96-regenerate-machine-specific-config\">9.6 Regenerate Machine-Specific Config</h3>\n<pre><code class=\"language-bash\"># Generate new SSH host keys (if removed during cleanup)\nsudo dpkg-reconfigure openssh-server\n\n# Re-join Tailscale (if applicable)\nsudo tailscale up\n\n# Reconfigure Syncthing (if applicable)\n# Each machine needs its own Syncthing identity</code></pre>\n<hr>\n<h2 id=\"10-troubleshooting\">10. Troubleshooting</h2>\n<h3 id=\"iso-doesnt-boot-black-screen-hangs\">ISO doesn't boot (black screen / hangs)</h3>\n<ul>\n<li>Try the <strong>&quot;nomodeset&quot;</strong> GRUB entry — bypasses GPU driver issues</li>\n<li>Check BIOS: disable Secure Boot, enable Legacy/CSM if UEFI-only fails</li>\n<li>Verify the USB was written correctly: <code>dd</code> checksum vs ISO checksum</li>\n</ul>\n<h3 id=\"casparcg-wont-start-on-live-boot\">CasparCG won't start on live boot</h3>\n<pre><code class=\"language-bash\"># Check logs\ncat /tmp/caspar.log\njournalctl -b | grep -i caspar\n\n# Common causes:\n# - GPU driver mismatch → use nomodeset or reinstall drivers\n# - CEF cache corruption → already cleaned if you followed §3\n# - Port 5250 already in use → check for duplicate processes\npgrep -af casparcg</code></pre>\n<h3 id=\"squashfs-too-large-out-of-memory\">SquashFS too large / out of memory</h3>\n<ul>\n<li>Shrink the source tree: remove or exclude <code>/opt/casparcg/media</code>, large dirs</li>\n</ul>\n<p>under other users’ homes, etc. (§4.2.1, §3).</p>\n<ul>\n<li>Use <code>toram</code> GRUB option only if the machine has enough RAM (≥ squashfs size × 2)</li>\n<li>Use <code>zstd</code> compression (default) for the best size/speed ratio</li>\n</ul>\n<h3 id=\"penguins-eggs-produce-fails\">penguins-eggs <code>produce</code> fails</h3>\n<pre><code class=\"language-bash\"># Check disk space\ndf -h /home/eggs\n\n# Try with verbose output\nsudo eggs produce --clone --basename &quot;highascg-server&quot; --verbose\n\n# Alternative: use the simpler &quot;love&quot; command\nsudo eggs love</code></pre>\n<h3 id=\"penguins-eggs-will-not-install-depends-nodejs-22-but-18-is-to-be-installed\"><code>penguins-eggs</code> will not install: <code>Depends: nodejs (&gt;= 22) but 18... is to be installed</code></h3>\n<p>The <code>.deb</code> from <code>fresh-eggs</code> needs the <strong><code>nodejs</code> apt package</strong> ≥ 22. Ubuntu’s default <code>nodejs</code> is 18, so you must install Node 22+ via apt (§4.0). If <code>node -v</code> already shows 22 but the error persists, you only upgraded a non-Debian <code>node</code> (e.g. nvm) — <code>apt-cache policy nodejs</code> will still show 18; fix that first.</p>\n<p>If the NodeSource script failed (often because <strong><code>apt update</code> hit a 404</strong> on an old PPA), <code>apt</code> never picked up NodeSource, so <code>apt install</code> still points at 18.x. Remove or fix the broken repository, run <code>sudo apt update</code>, then repeat the NodeSource steps in §4.0, then re-run <code>sudo ./fresh-eggs.sh</code>.</p>\n<h3 id=\"no-network-after-live-boot\">No network after live boot</h3>\n<pre><code class=\"language-bash\"># Check interfaces\nip link show\nip addr show\n\n# If using DHCP\ndhclient eth0   # or your interface name\n\n# Check NetworkManager / systemd-networkd\nsystemctl status NetworkManager\nsystemctl status systemd-networkd</code></pre>\n<hr>\n<h2 id=\"11-reference-whats-on-the-server\">11. Reference: What's on the Server</h2>\n<p>This section documents the full software stack that the image captures.</p>\n<h3 id=\"operating-system\">Operating System</h3>\n<table><thead><tr><th>Component</th><th>Details</th></tr></thead><tbody>\n<tr><td>OS</td><td>Ubuntu 24.04 LTS (Noble Numbat)</td></tr>\n<tr><td>Kernel</td><td>linux-generic (amd64)</td></tr>\n<tr><td>Display</td><td>nodm → Openbox (minimal X11, no DE)</td></tr>\n<tr><td>Init</td><td>systemd</td></tr>\n</tbody></table>\n<h3 id=\"casparcg-stack\">CasparCG Stack</h3>\n<table><thead><tr><th>Component</th><th>Location</th><th>Notes</th></tr></thead><tbody>\n<tr><td>CasparCG Server 2.5</td><td><code>/usr/bin/casparcg-server-2.5</code></td><td>Custom build (PRs #1718–#1720)</td></tr>\n<tr><td>CasparCG CEF</td><td><code>/usr/lib/casparcg-cef-*</code></td><td>Patched Chromium Embedded Framework</td></tr>\n<tr><td>CasparCG Scanner</td><td><code>/usr/bin/casparcg-scanner</code></td><td>Media scanner (port 8000)</td></tr>\n<tr><td>Config</td><td><code>/opt/casparcg/config/casparcg.config</code></td><td>Generated by HighAsCG</td></tr>\n<tr><td>Media</td><td><code>/opt/casparcg/media/</code></td><td>Playout media</td></tr>\n<tr><td>Templates</td><td><code>/opt/casparcg/template/</code></td><td>HTML templates</td></tr>\n<tr><td>CEF cache</td><td><code>/opt/casparcg/cef-cache/</code></td><td>Cleared on each restart</td></tr>\n<tr><td>Logs</td><td><code>/opt/casparcg/log/</code></td><td>Server logs</td></tr>\n</tbody></table>\n<h3 id=\"highascg\">HighAsCG</h3>\n<table><thead><tr><th>Component</th><th>Location</th><th>Notes</th></tr></thead><tbody>\n<tr><td>Application</td><td><code>/opt/highascg/</code></td><td>Node.js playout control</td></tr>\n<tr><td>Config</td><td><code>/opt/highascg/highascg.config.json</code></td><td>Application config</td></tr>\n<tr><td>Service</td><td><code>highascg.service</code></td><td>systemd unit</td></tr>\n<tr><td>Web GUI</td><td><code>http://&lt;IP&gt;:4200</code></td><td>Browser-based control</td></tr>\n</tbody></table>\n<h3 id=\"drivers-libraries\">Drivers &amp; Libraries</h3>\n<table><thead><tr><th>Component</th><th>Notes</th></tr></thead><tbody>\n<tr><td>NVIDIA driver</td><td><code>nvidia-driver-550+</code> via <code>ubuntu-drivers</code></td></tr>\n<tr><td>nvidia-persistenced</td><td>Enabled, persistence mode on</td></tr>\n<tr><td>DeckLink Desktop Video</td><td><code>desktopvideo</code> + <code>desktopvideo-gui</code> (DKMS: <code>blackmagic_io</code>)</td></tr>\n<tr><td>NDI SDK v6</td><td><code>/usr/lib/x86_64-linux-gnu/libndi.so.6</code> + symlinks</td></tr>\n<tr><td>FFmpeg</td><td>System package (x11grab, kmsgrab)</td></tr>\n</tbody></table>\n<h3 id=\"autostart-chain\">Autostart Chain</h3>\n<pre><code>systemd → nodm → X11 (:0) → Openbox\n  └─ ~/.config/openbox/autostart\n       ├─ xset (disable screensaver/DPMS)\n       ├─ unclutter (hide cursor)\n       ├─ highascg-nvidia-x-apply.sh (GPU perf settings)\n       ├─ /usr/bin/casparcg-scanner &amp;\n       └─ while true; do\n            casparcg-server-2.5 /opt/casparcg/config/casparcg.config\n            # respawn on exit\n          done &amp;\n\nsystemd → highascg.service\n  └─ node /opt/highascg/index.js (port 4200)</code></pre>\n<h3 id=\"system-hardening\">System Hardening</h3>\n<table><thead><tr><th>Setting</th><th>How</th></tr></thead><tbody>\n<tr><td>No screen blanking</td><td><code>xset s off; xset -dpms</code> in autostart</td></tr>\n<tr><td>No sleep/suspend</td><td><code>systemctl mask sleep.target suspend.target hibernate.target</code></td></tr>\n<tr><td>GRUB quiet</td><td><code>GRUB_CMDLINE_LINUX_DEFAULT=&quot;quiet splash&quot;</code></td></tr>\n<tr><td>UFW firewall</td><td>Ports 5250, 6250, 8000, 4200 open</td></tr>\n<tr><td>Single CasparCG instance</td><td><code>flock</code> in autostart (prevents duplicate starts)</td></tr>\n</tbody></table>\n<h3 id=\"key-user\">Key User</h3>\n<table><thead><tr><th>User</th><th>Shell</th><th>Home</th><th>Groups</th></tr></thead><tbody>\n<tr><td><code>casparcg</code></td><td>nologin</td><td><code>/home/casparcg</code></td><td>video, audio, render, plugdev, dialout, input</td></tr>\n</tbody></table>\n<h3 id=\"key-paths\">Key Paths</h3>\n<pre><code>/opt/casparcg/               # CasparCG working directory (media, config, logs)\n/opt/highascg/               # HighAsCG application\n/etc/default/nodm            # nodm configuration (NODM_USER=casparcg)\n/etc/highascg/display-mode   # &quot;normal&quot; or &quot;x11-only&quot;\n/home/casparcg/.config/openbox/autostart  # CasparCG launch script\n/home/casparcg/.xsession     # exec openbox-session\n/etc/ld.so.conf.d/casparcg.conf  # CEF library path\n/etc/highascg/nvidia-iso-driver  # Baked driver branch (535|580|595) for this ISO\n/etc/sudoers.d/highascg-asound   # optional: system ALSA via tee (install with HIGHASCG_INSTALL_ASOUND_SUDOERS=1)\n/usr/local/bin/highascg-display-mode      # normal/x11-only switcher\n/usr/local/bin/highascg-nvidia-x-apply.sh # GPU perf on X start</code></pre>\n<hr>\n<h2 id=\"quick-reference-one-liner-workflow\">Quick Reference: One-Liner Workflow</h2>\n<pre><code class=\"language-bash\"># 1. Clean up\nsudo bash -c 'rm -rf /opt/casparcg/{log/*,cef-cache/*} /tmp/caspar.log &amp;&amp; apt clean'\n\n# 2. Install eggs\ncd /tmp &amp;&amp; git clone https://github.com/pieroproietti/fresh-eggs &amp;&amp; cd fresh-eggs &amp;&amp; sudo ./fresh-eggs.sh\n\n# 3. Produce ISO\nsudo eggs produce --clone --basename &quot;highascg-server&quot;\n\n# 4. Write to USB (on the server or copy ISO to another machine first)\nsudo dd if=/home/eggs/highascg-server-amd64.iso of=/dev/sdX bs=4M status=progress oflag=sync\n\n# 5. Boot USB on target → GRUB → Live\n# 6. Install: sudo eggs krill</code></pre>\n<hr>\n<p><em>Document created: 2026-04-22 | Related: <a href=\"#/MANUAL_INSTALL\" data-wiki-page=\"1\">MANUAL_INSTALL.md</a>, <a href=\"../../12_WO_PRODUCTION_INSTALLER.md\">WO-12</a>, <a href=\"../../openbox_autostart.md\">openbox_autostart.md</a></em></p>","CALAMARES_INSTALL_TO_DISK":"<h1 id=\"install-highascg-to-internal-disk-calamares\">Install HighAsCG to internal disk (Calamares)</h1>\n<p>Graphical <strong>install-to-disk</strong> from the live USB stick using <strong>Calamares</strong> (eggs integration). Use this for a <strong>permanent playout machine</strong> — not for preparing the USB stick itself.</p>\n<p><strong>Related:</strong> <a href=\"#/LIVE_USB_IMAGE\" data-wiki-page=\"1\">LIVE_USB_IMAGE.md</a> (build/flash stick), <a href=\"#/STICK_QUICK_START\" data-wiki-page=\"1\">STICK_QUICK_START.md</a> (Windows/macOS stick prep), <a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">WO47_ISO_VS_EXFAT.md</a> (exFAT operator data after install).</p>\n<hr>\n<h2 id=\"launch-the-installer\">Launch the installer</h2>\n<p>From the live session (operator display or SSH):</p>\n<pre><code class=\"language-bash\"># Web UI: Settings → Nuclear → Install to disk\n# Or:\nsudo -n /usr/local/bin/launch-calamares.sh</code></pre>\n<p>Requires passwordless sudo for <code>launch-calamares.sh</code> (baked on factory ISO). Calamares opens on <strong><code>:0</code></strong>.</p>\n<p><strong>Note:</strong> <code>eggs krill</code> (text installer) exists on the <strong>eggs build host</strong>, not on the live ISO (<code>penguins-eggs</code> is excluded from squashfs). On the stick, use <strong>Calamares</strong>.</p>\n<hr>\n<h2 id=\"before-partitioning\">Before partitioning</h2>\n<h3 id=\"1-pick-the-correct-disk\">1. Pick the correct disk</h3>\n<pre><code class=\"language-bash\">lsblk -o NAME,TYPE,SIZE,MODEL,TRAN,FSTYPE,PARTLABEL</code></pre>\n<table><thead><tr><th>What you see</th><th>Install here?</th></tr></thead><tbody>\n<tr><td><code>TRAN=usb</code>, SanDisk ~57 G, <code>iso9660</code></td><td><strong>No</strong> — live USB stick</td></tr>\n<tr><td>Internal SSD/HDD/NVMe (Kingston, Samsung, <code>nvme0n1</code>, SATA <code>sdX</code> without <code>usb</code>)</td><td><strong>Yes</strong></td></tr>\n</tbody></table>\n<p>Device letters (<code>sda</code> / <code>sdb</code>) <strong>swap</strong> depending on USB port order. Use <strong>MODEL</strong> and <strong>TRAN</strong>, not the letter alone.</p>\n<p><strong>Never</strong> install to the stick’s <strong>exFAT</strong> partition (<code>HIGHASCGEXF</code>, often <code>sda3</code> on the USB layout). That partition is operator data only.</p>\n<h3 id=\"2-make-the-internal-disk-visible\">2. Make the internal disk visible</h3>\n<pre><code class=\"language-bash\">sudo /usr/local/lib/highascg/probe-internal-storage.sh --check</code></pre>\n<ul>\n<li><strong>NVMe missing, dmesg mentions RST/RAID:</strong> set BIOS storage to <strong>AHCI</strong> (not Intel RST/RAID), reboot from USB, confirm <code>nvme0n1</code> in <code>lsblk</code>.</li>\n<li><strong>SATA SSD</strong> (e.g. Kingston SKC600): should appear as <code>/dev/sda</code> or <code>/dev/sdb</code> without <code>TRAN=usb</code>.</li>\n</ul>\n<h3 id=\"3-firmware-mode-disable-csm-recommended\">3. Firmware mode — disable CSM (recommended)</h3>\n<p><strong>CSM</strong> (Compatibility Support Module, also <strong>Legacy boot</strong>, <strong>Launch CSM</strong>) lets UEFI PCs boot in old BIOS mode. With CSM <strong>enabled</strong>, the live USB often boots as <strong>Legacy BIOS</strong> even though the machine is UEFI-capable. That leads to <strong><code>bios_grub</code></strong> layouts, missing <strong><code>grub-pc</code></strong> on older ISOs, and <strong>“The bootloader could not be installed” (error code 1)</strong>.</p>\n<p><strong>Recommended before Calamares:</strong></p>\n<ol>\n<li>Enter firmware setup (<code>F2</code>, <code>Del</code>, <code>F12</code>, or <code>Esc</code> at POST).</li>\n<li><strong>Disable CSM</strong> / set boot mode to <strong>UEFI only</strong> (wording varies by vendor).</li>\n<li><strong>Disable Secure Boot</strong> if the stick does not boot (some boards require this for unsigned live ISOs).</li>\n<li>Save and reboot; pick the USB entry labeled <strong>UEFI:</strong> … (not plain USB/Legacy).</li>\n<li>Confirm from the live session:</li>\n</ol>\n<pre><code class=\"language-bash\">[ -d /sys/firmware/efi ] &amp;&amp; echo 'Firmware: UEFI — OK for Erase disk' || echo 'Firmware: Legacy BIOS — disable CSM and reboot'</code></pre>\n<p>With <strong>UEFI</strong> boot, use <strong>Erase disk and install</strong> — Calamares creates <strong>ESP + <code>/</code></strong> automatically. You do <strong>not</strong> need manual <strong><code>bios_grub</code></strong>.</p>\n<table><thead><tr><th>Boot mode</th><th>Partition table</th><th>Boot slice needed</th></tr></thead><tbody>\n<tr><td><strong>UEFI</strong> (recommended — <strong>CSM off</strong>)</td><td><strong>GPT</strong></td><td><strong>EFI System Partition</strong> — FAT32, ~300 MiB+, mount <strong><code>/boot/efi</code></strong>, flags <strong>boot</strong> + <strong>esp</strong></td></tr>\n<tr><td>Legacy BIOS + GPT (CSM on — avoid if possible)</td><td><strong>GPT</strong></td><td><strong><code>bios_grub</code></strong> — <strong>1–2 MiB</strong>, <strong>leave unformatted</strong> (no filesystem), flag <strong><code>bios_grub</code></strong> only</td></tr>\n<tr><td>Legacy BIOS + MBR</td><td>msdos</td><td>Often no extra slice; Calamares <strong>Erase disk</strong> is easiest</td></tr>\n</tbody></table>\n<p>If the firmware shows <strong>“missing GPT”</strong> or <strong>“create an EFI partition”</strong> after a failed install, you likely booted <strong>Legacy</strong> (CSM on) while using a <strong>UEFI</strong> layout, or skipped <strong>ESP</strong> / <strong><code>bios_grub</code></strong> in manual layout.</p>\n<hr>\n<h2 id=\"recommended-erase-disk\">Recommended: Erase disk</h2>\n<p><strong>Prerequisite:</strong> firmware <strong>CSM disabled</strong>, USB booted in <strong>UEFI</strong> mode (see §3).</p>\n<p>On the Calamares <strong>partition</strong> page:</p>\n<ol>\n<li>Select the <strong>internal</strong> disk (e.g. <strong>KINGSTON …</strong>, <strong>nvme0n1</strong> — not the USB stick).</li>\n<li>Choose <strong>Erase disk and install</strong> (or equivalent).</li>\n<li>Calamares creates <strong>GPT</strong>, <strong>ESP</strong> (UEFI) or correct layout for firmware, and <strong>ext4</strong> <code>/</code>.</li>\n<li><strong>Swap:</strong> <strong>none</strong> (matches ISO defaults).</li>\n</ol>\n<p>This avoids manual <code>bios_grub</code> / ESP mistakes.</p>\n<hr>\n<h2 id=\"manual-partitioning\">Manual partitioning</h2>\n<p>Use <strong>Manual partitioning</strong> when Windows/other OS partitions must stay, or <strong>Erase</strong> fails.</p>\n<h3 id=\"uefi-gpt-most-new-pcs\">UEFI + GPT (most new PCs)</h3>\n<table><thead><tr><th>Partition</th><th>Size</th><th>Filesystem</th><th>Mount point</th><th>Flags</th></tr></thead><tbody>\n<tr><td>EFI</td><td><strong>≥ 300 MiB</strong> (512 MiB is fine)</td><td><strong>fat32</strong></td><td><strong><code>/boot/efi</code></strong></td><td><strong>boot</strong> and <strong>esp</strong> (set both in Calamares; “EFI System Partition” preset is OK)</td></tr>\n<tr><td>root</td><td>rest (≥ 32 GiB recommended)</td><td><strong>ext4</strong></td><td><strong><code>/</code></strong></td><td>(none)</td></tr>\n</tbody></table>\n<p>Partition table: <strong>GPT</strong>. Do <strong>not</strong> use ext4 on the EFI slice — only <strong>fat32</strong> on <strong><code>/boot/efi</code></strong>.</p>\n<h3 id=\"legacy-bios-gpt\">Legacy BIOS + GPT</h3>\n<p>GRUB on GPT without UEFI needs a <strong><code>bios_grub</code></strong> slice (this is what fixed installs that failed with partition / rsync errors):</p>\n<table><thead><tr><th>Partition</th><th>Size</th><th>Filesystem</th><th>Mount point</th><th>Flags</th></tr></thead><tbody>\n<tr><td>GRUB BIOS</td><td><strong>1–2 MiB</strong></td><td><strong>unformatted</strong> (do <strong>not</strong> assign ext4/xfs)</td><td><em>(none)</em></td><td><strong><code>bios_grub</code></strong></td></tr>\n<tr><td>root</td><td>rest (≥ 32 GiB recommended)</td><td><strong>ext4</strong></td><td><strong><code>/</code></strong></td><td>(none)</td></tr>\n</tbody></table>\n<p>Partition table: <strong>GPT</strong>.</p>\n<p>In Calamares manual editor:</p>\n<ol>\n<li><strong>New partition table</strong> → <strong>GPT</strong> (wipes target disk — confirm correct disk).</li>\n<li>Add <strong>1–2 MiB</strong> partition → leave filesystem <strong>unformatted</strong> → set flag <strong><code>bios_grub</code></strong>.</li>\n<li>Add large <strong>ext4</strong> partition → mount <strong><code>/</code></strong>.</li>\n<li>Do <strong>not</strong> mount Windows NTFS partitions; do <strong>not</strong> use stick <code>sda3</code> exFAT.</li>\n</ol>\n<h3 id=\"alongside-windows\">Alongside Windows</h3>\n<p>Only if you have <strong>unallocated</strong> space or you shrink Windows deliberately. Prefer <strong>manual</strong> layout: keep Windows partitions, add ESP + <code>/</code> (UEFI) or <code>bios_grub</code> + <code>/</code> (BIOS) in free space. <strong>Do not</strong> install <code>/</code> onto an existing NTFS volume.</p>\n<h3 id=\"swap\">Swap</h3>\n<p>Choose <strong>none</strong> unless you have a specific reason to add swap.</p>\n<hr>\n<h2 id=\"after-installation\">After installation</h2>\n<ol>\n<li>Remove the USB stick when prompted.</li>\n<li>Boot from internal disk (UEFI boot order: internal SSD/NVMe first).</li>\n<li>First boot: nodm → Openbox → HighAsCG on <code>:4200</code>, Caspar via systemd.</li>\n<li>Operator exFAT on the <strong>stick</strong> (<code>HIGHASCGEXF</code>) is optional for updates; internal playout does not require the stick.</li>\n</ol>\n<hr>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<h3 id=\"read-the-log\">Read the log</h3>\n<pre><code class=\"language-bash\">grep -iE 'partition|unpackfs|rsync|fail|error' ~/.cache/calamares/session.log | tail -40</code></pre>\n<h3 id=\"failed-to-create-partition-on-disk\">“Failed to create partition on disk …”</h3>\n<ul>\n<li>Wrong disk selected, or disk locked (BitLocker).</li>\n<li><strong>Manual:</strong> missing <strong><code>bios_grub</code></strong> (BIOS+GPT) or <strong>ESP</strong> (UEFI).</li>\n<li>Try <strong>Erase disk</strong> on the internal drive after backup, or <code>wipefs</code> + new GPT (see below).</li>\n</ul>\n<h3 id=\"rsync-failed-with-error-code-11-failed-to-unpack-filesystemsquashfs\"><code>rsync failed with error code 11</code> / “Failed to unpack filesystem.squashfs”</h3>\n<p>Rsync <strong>exit 11</strong> = <strong>file I/O error</strong> while copying the ~4 GiB live image to <code>/</code>. Common causes:</p>\n<ul>\n<li><strong><code>/</code></strong> partition too small (need <strong>≥ ~8 GiB</strong> minimum; <strong>32 GiB+</strong> recommended).</li>\n<li><strong><code>/</code></strong> on wrong filesystem (exFAT/NTFS instead of ext4).</li>\n<li>Target is a <strong>USB stick partition</strong> (<code>sda3</code> exFAT), not the internal SSD.</li>\n<li>Failing SATA cable / SSD errors — check <code>dmesg</code> for I/O errors.</li>\n</ul>\n<p>Verify live image on stick:</p>\n<pre><code class=\"language-bash\">ls -lh /run/live/medium/live/filesystem.squashfs\nunsquashfs -s /run/live/medium/live/filesystem.squashfs</code></pre>\n<h3 id=\"wipe-internal-disk-and-retry-destructive\">Wipe internal disk and retry (destructive)</h3>\n<p><strong>Double-check MODEL= your internal SSD, not USB:</strong></p>\n<pre><code class=\"language-bash\">lsblk -o NAME,SIZE,MODEL,TRAN\n# Example: /dev/sdb is Kingston, /dev/sda is USB\nsudo wipefs -a /dev/sdX\nsudo parted /dev/sdX mklabel gpt</code></pre>\n<p>Then run Calamares again with <strong>Erase disk</strong> on that device.</p>\n<h3 id=\"calamares-shellprocess-exit-127-at-end-of-install\">Calamares shellprocess / exit 127 at end of install</h3>\n<p>If install fails at the last step with exit <strong>127</strong>, the stick ISO may need Calamares shellprocess patches — on stick:</p>\n<pre><code class=\"language-bash\">sudo bash ~/highascg/tools/eggs/live-usb/fix-calamares-shellprocess.sh</code></pre>\n<p>(Rebuild ISO with latest <code>patch-iso-squashfs-calamares.sh</code> for a permanent fix.)</p>\n<h3 id=\"the-bootloader-could-not-be-installed-error-code-1\">“The bootloader could not be installed” (error code <strong>1</strong>)</h3>\n<p>Calamares failed running <strong><code>grub-install</code></strong> on the target disk. Read the exact command:</p>\n<pre><code class=\"language-bash\">grep -iE 'bootloader|grub-install|grub' ~/.cache/calamares/session.log | tail -40</code></pre>\n<p>Check how the USB stick booted:</p>\n<pre><code class=\"language-bash\">[ -d /sys/firmware/efi ] &amp;&amp; echo 'Firmware: UEFI' || echo 'Firmware: Legacy BIOS'\ndpkg -l grub-pc grub-efi-amd64 2&gt;/dev/null | grep ^ii\nlsblk -o NAME,SIZE,FSTYPE,PARTTYPE,PARTFLAGS,MOUNTPOINT,MODEL,TRAN</code></pre>\n<table><thead><tr><th>Situation</th><th>Fix</th></tr></thead><tbody>\n<tr><td><strong>CSM enabled</strong> — live shows <code>Firmware: Legacy BIOS</code></td><td><strong>Disable CSM</strong> in firmware, reboot from <strong>UEFI:</strong> USB entry, <strong>Erase disk</strong> (ESP + <code>/</code>). This fixed <strong>bootloader error 1</strong> on several playout PCs.</td></tr>\n<tr><td><strong>Legacy BIOS</strong> + GPT manual layout</td><td>Need <strong>1–2 MiB unformatted <code>bios_grub</code></strong> + <strong>ext4</strong> <code>/</code>. Bootloader target must be the <strong>whole internal disk</strong> (<code>/dev/sdX</code>), not a partition.</td></tr>\n<tr><td><strong>Legacy BIOS</strong>, log shows <code>grub-install --target i386-pc</code> failed</td><td>ISO may lack <strong><code>grub-pc</code></strong> (older sticks). <strong>Preferred:</strong> disable <strong>CSM</strong>, boot USB in <strong>UEFI</strong>, <strong>Erase disk</strong>. <strong>Permanent:</strong> rebuild ISO after <code>install-grub-for-calamares-iso.sh</code> + <code>fix-calamares-shellprocess.sh</code>.</td></tr>\n<tr><td><strong>UEFI</strong> boot</td><td>Need <strong>fat32 ESP</strong> mounted <strong><code>/boot/efi</code></strong> (flags <strong>boot</strong>, <strong>esp</strong>). Do <strong>not</strong> use <code>bios_grub</code> only.</td></tr>\n<tr><td>Bootloader installed to <strong>USB stick</strong></td><td>Wrong disk — pick <strong>Kingston / internal</strong> in Calamares; <code>TRAN</code> must not be <code>usb</code>.</td></tr>\n<tr><td>Files copied but bootloader step failed</td><td>See <strong>manual GRUB recovery</strong> below.</td></tr>\n</tbody></table>\n<p>On the live stick, apply Calamares patches (includes BIOS <code>grub-pc</code> install step):</p>\n<pre><code class=\"language-bash\">sudo bash ~/highascg/tools/eggs/live-usb/fix-calamares-shellprocess.sh</code></pre>\n<p>If <strong><code>grub-pc</code> is missing</strong> on the stick (<code>dpkg -l grub-pc</code> empty) and you must stay on Legacy BIOS, you need a <strong>rebuilt ISO</strong> — the squashfs image does not include BIOS GRUB on older builds.</p>\n<h4 id=\"manual-grub-recovery-install-copied-bootloader-failed\">Manual GRUB recovery (install copied, bootloader failed)</h4>\n<p>Only if Calamares finished <strong>unpackfs</strong> (system files on internal disk) but failed at bootloader:</p>\n<pre><code class=\"language-bash\"># Replace sdX with internal disk; sdX2 = ext4 / ; sdX1 = ESP (UEFI) or bios_grub slice\nlsblk -o NAME,SIZE,FSTYPE,MOUNTPOINT,MODEL,TRAN\nsudo mount /dev/sdX2 /mnt\n# UEFI only:\nsudo mkdir -p /mnt/boot/efi &amp;&amp; sudo mount /dev/sdX1 /mnt/boot/efi\nsudo mount --bind /dev /mnt/dev\nsudo mount --bind /proc /mnt/proc\nsudo mount --bind /sys /mnt/sys\nsudo chroot /mnt bash -c '\n  export DEBIAN_FRONTEND=noninteractive\n  apt-get update\n  apt-get install -y grub-pc grub-pc-bin grub-efi-amd64-signed\n'\n# Legacy BIOS + GPT (bios_grub present):\nsudo chroot /mnt grub-install --target=i386-pc /dev/sdX\n# UEFI:\nsudo chroot /mnt grub-install --target=x86_64-efi --efi-directory=/boot/efi --bootloader-id=GRUB\nsudo chroot /mnt update-grub\nsudo umount -R /mnt</code></pre>\n<p>Requires <strong>network</strong> in the live session for <code>apt-get</code> if <code>grub-pc</code> was not baked into the install image.</p>\n<h3 id=\"external-command-finished-with-errors-logs-helper-exit-1\">“External command finished with errors” / <strong>logs-helper exit 1</strong></h3>\n<p>Often appears <strong>at the very end</strong> after partition copy, GRUB, and cleanup — the failing step is usually <strong><code>calamares-logs-helper.sh</code></strong> (archives session log to <code>/var/log/installer/</code> on the target). Eggs’ default helper uses <code>set -ex</code> and <strong>aborts</strong> if <strong><code>/var/log/syslog</code></strong> or <strong><code>.disk/info</code></strong> is missing on the live session (common on our ISO).</p>\n<p><strong>The install may still have succeeded.</strong> Check:</p>\n<pre><code class=\"language-bash\">grep -iE 'logs-helper|shellprocess@logs|external command' ~/.cache/calamares/session.log | tail -20\nlsblk -o NAME,FSTYPE,MOUNTPOINT,MODEL,TRAN\n# If Kingston/NVMe has ext4 + fat32 EFI partitions with data, try booting without USB:\nsudo reboot</code></pre>\n<p>If only the logs step failed, internal disk boot often works. Remove USB, set firmware to boot <strong>internal UEFI</strong> first.</p>\n<p><strong>On the stick (before retry or for next machine):</strong></p>\n<pre><code class=\"language-bash\">sudo bash ~/highascg/tools/eggs/live-usb/fix-calamares-shellprocess.sh</code></pre>\n<p>That installs an offline-safe <code>calamares-logs-helper.sh</code>. Rebuild ISO with <code>patch-iso-squashfs-calamares.sh</code> for a permanent fix.</p>\n<p><strong>If earlier steps failed</strong> (not just logs), <code>session.log</code> will also show <code>bootloader</code>, <code>unpackfs</code>, or <code>mkinitramfs</code> errors — read the full tail:</p>\n<pre><code class=\"language-bash\">tail -80 ~/.cache/calamares/session.log</code></pre>\n<hr>\n<h2 id=\"quick-checklist\">Quick checklist</h2>\n<ul>\n<li>[ ] Firmware: <strong>CSM disabled</strong>, USB booted as <strong>UEFI</strong> (<code>/sys/firmware/efi</code> exists)</li>\n<li>[ ] <code>lsblk</code> — internal disk visible, USB identified</li>\n<li>[ ] <code>probe-internal-storage.sh --check</code> passes (NVMe/AHCI if applicable)</li>\n<li>[ ] Calamares target = <strong>internal disk</strong>, not USB</li>\n<li>[ ] <strong>UEFI (recommended):</strong> <strong>Erase disk</strong> → ESP <strong>fat32</strong> <code>/boot/efi</code> + <strong>ext4</strong> <code>/</code></li>\n<li>[ ] <strong>Legacy only (CSM on):</strong> <strong>1–2 MiB unformatted <code>bios_grub</code></strong> + <strong>ext4</strong> <code>/</code></li>\n<li>[ ] Swap <strong>none</strong></li>\n<li>[ ] Root partition <strong>≥ 32 GiB</strong> recommended</li>\n</ul>","wiki--install--calamares-install-to-disk":"<h1 id=\"calamares-install-to-disk\">Calamares install to disk</h1>\n<p>Install HighAsCG from the <strong>live USB stick</strong> onto an <strong>internal SSD/HDD/NVMe</strong> using the graphical Calamares wizard.</p>\n<p><strong>Full guide (partition layouts, <code>bios_grub</code>, UEFI ESP, troubleshooting):</strong> <a href=\"#/CALAMARES_INSTALL_TO_DISK\" data-wiki-page=\"1\">CALAMARES_INSTALL_TO_DISK.md</a></p>\n<h2 id=\"launch\">Launch</h2>\n<ul>\n<li><strong>Web UI:</strong> Settings → Nuclear → <strong>Install to disk</strong></li>\n<li><strong>Terminal:</strong> <code>sudo -n /usr/local/bin/launch-calamares.sh</code></li>\n</ul>\n<h2 id=\"pick-the-right-disk\">Pick the right disk</h2>\n<pre><code class=\"language-bash\">lsblk -o NAME,TYPE,SIZE,MODEL,TRAN,FSTYPE</code></pre>\n<p>Install on the <strong>internal</strong> drive (Kingston, NVMe, …). <strong>Do not</strong> install on the <strong>USB stick</strong> or its <strong>exFAT</strong> partition (<code>HIGHASCGEXF</code>, often <code>sda3</code>).</p>\n<h2 id=\"disable-csm-uefi-install\">Disable CSM (UEFI install)</h2>\n<p><strong>CSM</strong> (Compatibility Support Module / <strong>Legacy boot</strong>) makes the stick boot in BIOS mode. That often causes <strong>bootloader could not be installed (error 1)</strong> on HighAsCG ISOs.</p>\n<ol>\n<li>Firmware setup → <strong>disable CSM</strong> (UEFI only).</li>\n<li>Boot the USB entry labeled <strong>UEFI:</strong> … (not Legacy USB).</li>\n<li>Confirm: <code>[ -d /sys/firmware/efi ] &amp;&amp; echo UEFI</code></li>\n</ol>\n<p>Then use <strong>Erase disk</strong> below — no manual <strong><code>bios_grub</code></strong> needed.</p>\n<h2 id=\"easiest-path\">Easiest path</h2>\n<p><strong>Erase disk and install</strong> on the internal drive. Swap: <strong>none</strong>.</p>\n<h2 id=\"manual-partitioning-when-erase-fails\">Manual partitioning (when Erase fails)</h2>\n<h3 id=\"uefi-typical-csm-off\">UEFI (typical — <strong>CSM off</strong>)</h3>\n<table><thead><tr><th>Size</th><th>FS</th><th>Mount</th><th>Flags</th></tr></thead><tbody>\n<tr><td><strong>≥ 300 MiB</strong></td><td><strong>fat32</strong></td><td><strong><code>/boot/efi</code></strong></td><td><strong>boot</strong> + <strong>esp</strong></td></tr>\n<tr><td>rest</td><td>ext4</td><td><code>/</code></td><td>—</td></tr>\n</tbody></table>\n<p>GPT partition table. This layout (with CSM disabled) fixed bootloader install on several playout PCs.</p>\n<h3 id=\"legacy-bios-gpt\">Legacy BIOS + GPT</h3>\n<table><thead><tr><th>Size</th><th>FS</th><th>Mount</th><th>Flags</th></tr></thead><tbody>\n<tr><td><strong>1–2 MiB</strong></td><td><strong>unformatted</strong></td><td>—</td><td><strong><code>bios_grub</code></strong></td></tr>\n<tr><td>rest</td><td>ext4</td><td><code>/</code></td><td>—</td></tr>\n</tbody></table>\n<p>The <strong><code>bios_grub</code></strong> slice must stay <strong>unformatted</strong> — no ext4 on that 1–2 MiB partition. Without it, GRUB/partition steps often fail on BIOS machines.</p>\n<h2 id=\"common-errors\">Common errors</h2>\n<table><thead><tr><th>Message</th><th>Likely fix</th></tr></thead><tbody>\n<tr><td>Failed to create partition</td><td>Wrong disk; add <strong>ESP</strong> or <strong><code>bios_grub</code></strong>; try Erase</td></tr>\n<tr><td>rsync error <strong>11</strong> / unpackfs failed</td><td><code>/</code> too small or wrong FS; not internal ext4; check <code>dmesg</code></td></tr>\n<tr><td><strong>Bootloader could not be installed</strong> (code <strong>1</strong>)</td><td><strong>Disable CSM</strong>, boot USB in <strong>UEFI</strong>, <strong>fat32 <code>/boot/efi</code></strong> (≥300 MiB, <strong>boot</strong>+<strong>esp</strong>) + ext4 <code>/</code></td></tr>\n<tr><td><strong>logs-helper</strong> / external command exit <strong>1</strong> at end</td><td>Install often <strong>OK</strong> — remove USB and boot internal disk; run <code>fix-calamares-shellprocess.sh</code> on stick</td></tr>\n<tr><td>No disk in Calamares</td><td>BIOS <strong>AHCI</strong>; run <code>probe-internal-storage.sh --check</code></td></tr>\n</tbody></table>\n<p>Log: <code>~/.cache/calamares/session.log</code></p>\n<h2 id=\"krill\">Krill?</h2>\n<p><strong>Calamares</strong> is on the live ISO. <strong><code>eggs krill</code></strong> is on the eggs <strong>build host</strong> only, not the stick.</p>","ISO_CONTENTS":"<h1 id=\"highascg-live-iso-contents-reference\">HighAsCG live ISO — contents reference</h1>\n<p>What is inside the <strong>penguins-eggs</strong> hybrid ISO produced by <strong><code>tools/eggs/live-usb/build-highascg-egg.sh</code></strong> (or <strong><code>make-dev-github-release-iso-quick.sh</code></strong>).</p>\n<p>The image is a <strong><code>eggs produce --clone --max --excludes static</code></strong> snapshot of the <strong>build host</strong> at produce time. It is <strong>not</strong> a minimal net-install recipe: whatever was installed and configured on that machine (via <strong><code>scripts/install.sh</code></strong> and WO‑47 prep) is what gets cloned—minus paths listed in <a href=\"../../tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list\">&lt;strong&gt;&lt;code&gt;tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list&lt;/code&gt;&lt;/strong&gt;</a>.</p>\n<p><strong>Related:</strong> <a href=\"#/LIVE_USB_IMAGE\" data-wiki-page=\"1\">&lt;code&gt;LIVE_USB_IMAGE.md&lt;/code&gt;</a> (workflow), <a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">&lt;code&gt;WO47_ISO_VS_EXFAT.md&lt;/code&gt;</a> (ISO vs stick payload), <a href=\"../../tools/eggs/live-usb/BUILD_AND_FLASH.md\">&lt;code&gt;BUILD_AND_FLASH.md&lt;/code&gt;</a> (build &amp; flash).</p>\n<hr>\n<h2 id=\"usb-stick-layout-after-operator-prep\">USB stick layout (after operator prep)</h2>\n<table><thead><tr><th>Region</th><th>What it is</th></tr></thead><tbody>\n<tr><td><strong>Hybrid ISO partition(s)</strong></td><td>Bootable live system (this document).</td></tr>\n<tr><td><strong>Operator data (exFAT)</strong></td><td>Label <strong><code>HIGHASCGEXF</code></strong> — configs, media, server drops; survives reboot when the stick is prepared with WO-47. No union <strong><code>/ union</code></strong> persistence partition. See <a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">&lt;code&gt;WO47_ISO_VS_EXFAT.md&lt;/code&gt;</a>.</td></tr>\n<tr><td><strong>exFAT <code>HIGHASCGEXF</code></strong></td><td>Operator data: <strong><code>update/server/</code></strong>, media, templates, configs — <strong>not</strong> inside the squashfs. See <a href=\"#/ISO_CONTENTS#wo-47-what-is-on-the-iso-vs-on-exfat\" data-wiki-page=\"1\">§ WO‑47 split</a>.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"stack-overview-bottom-top\">Stack overview (bottom → top)</h2>\n<pre><code>┌─────────────────────────────────────────────────────────────┐\n│  HighAsCG — highascg.service → :4200 (API + dist-web/ UI, WO-52) │\n│  Browser UI ↔ Caspar AMCP + UI ↔ Ubuntu OS via REST/WS (same host) │\n│  (full app tree often from exFAT after WO‑47 bootstrap)         │\n├─────────────────────────────────────────────────────────────┤\n│  CasparCG Server 2.5 + CEF + casparcg-scanner               │\n│  Openbox autostart (respawn) — config under ~/highascg/    │\n├─────────────────────────────────────────────────────────────┤\n│  X11 :0 — nodm → openbox-session (user casparcg)            │\n├─────────────────────────────────────────────────────────────┤\n│  Drivers: NVIDIA (one branch per ISO — 535 / 580 / 595)     │\n│           DeckLink desktopvideo (DKMS blackmagic_io)        │\n│           NDI (libndi in tree + ~/highascg/lib copies)     │\n├─────────────────────────────────────────────────────────────┤\n│  FFmpeg, systemd-networkd, optional NetworkManager stack    │\n│  v4l-utils (`v4l2-ctl`) — USB/V4L2 webcam enumeration       │\n├─────────────────────────────────────────────────────────────┤\n│  Ubuntu 24.04 LTS (Noble) — linux-generic, systemd, GRUB    │\n└─────────────────────────────────────────────────────────────┘</code></pre>\n<hr>\n<h2 id=\"1-operating-system\">1. Operating system</h2>\n<table><thead><tr><th>Item</th><th>Typical content</th></tr></thead><tbody>\n<tr><td><strong>Distribution</strong></td><td>Ubuntu <strong>24.04 LTS</strong> (Noble Numbat) — target of <strong><code>scripts/install.sh</code></strong></td></tr>\n<tr><td><strong>Architecture</strong></td><td><strong>amd64</strong></td></tr>\n<tr><td><strong>Kernel</strong></td><td><strong>linux-generic</strong> (whatever the build host was running when <code>eggs produce</code> ran)</td></tr>\n<tr><td><strong>Init</strong></td><td><strong>systemd</strong></td></tr>\n<tr><td><strong>Boot</strong></td><td>GRUB / EFI hybrid layout from eggs (live + optional installer entries per eggs config)</td></tr>\n<tr><td><strong>User</strong></td><td><strong><code>casparcg</code></strong> — playout user (<code>video</code>, <code>audio</code>, <code>render</code>, <code>plugdev</code>, …); home <strong><code>/home/casparcg</code></strong></td></tr>\n</tbody></table>\n<p><strong>Added at ISO build time</strong> (<code>build-highascg-egg.sh</code>, not necessarily on a minimal Ubuntu):</p>\n<ul>\n<li><strong>systemd-networkd</strong> + <strong>netplan</strong> (renderer <code>networkd</code>) for wired DHCP on live boot</li>\n<li>Packages: <code>network-manager</code>, <code>wpasupplicant</code>, <code>iproute2</code>, <code>linux-firmware</code>, <code>pciutils</code>, <code>usbutils</code>, etc. (see script)</li>\n<li><strong><code>exfatprogs</code></strong>, <strong><code>parted</code></strong>, <strong><code>rsync</code></strong>, <strong><code>python3</code></strong> — WO‑47 stick prep (<code>prepare-eggs-clone-with-exfat.sh</code>)</li>\n</ul>\n<hr>\n<h2 id=\"2-display-nodm-openbox-no-desktop-environment\">2. Display — nodm + Openbox (no desktop environment)</h2>\n<table><thead><tr><th>Component</th><th>Role</th></tr></thead><tbody>\n<tr><td><strong>nodm</strong></td><td>Auto-starts X11 on <strong><code>DISPLAY=:0</code></strong> as <strong><code>casparcg</code></strong> (no GDM/lightdm)</td></tr>\n<tr><td><strong>openbox</strong></td><td>Minimal window manager only</td></tr>\n<tr><td><strong>unclutter</strong></td><td>Hides mouse cursor on playout head</td></tr>\n<tr><td><strong>xterm</strong></td><td>Used in <strong><code>x11-only</code></strong> display mode (DeckLink setup)</td></tr>\n<tr><td><strong>X input</strong></td><td><code>xserver-xorg-input-all</code> / <strong>libinput</strong> (USB kb/mouse on <code>:0</code>)</td></tr>\n</tbody></table>\n<p><strong>Key paths</strong></p>\n<table><thead><tr><th>Path</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><code>/etc/default/nodm</code></td><td><code>NODM_USER=casparcg</code>, <code>NODM_ENABLED=true</code>, X options <code>-s 0 -dpms</code></td></tr>\n<tr><td><code>/home/casparcg/.xsession</code></td><td><code>exec openbox-session</code></td></tr>\n<tr><td><code>/home/casparcg/.config/openbox/autostart</code></td><td>CasparCG + scanner + NVIDIA X tweaks; <strong><code>flock</code></strong> single-instance lock</td></tr>\n<tr><td><code>/etc/highascg/display-mode</code></td><td><strong><code>normal</code></strong> (Caspar autostart) or <strong><code>x11-only</code></strong> (DeckLink GUI only)</td></tr>\n<tr><td><code>/usr/local/bin/highascg-display-mode</code></td><td>Switch display mode + restart nodm</td></tr>\n</tbody></table>\n<p><strong>Autostart chain (<code>display-mode=normal</code>)</strong></p>\n<ol>\n<li><code>xset</code> — disable screensaver / DPMS</li>\n<li><code>highascg-nvidia-x-apply.sh</code> — PowerMizer / <strong>Sync to VBlank off</strong> (if NVIDIA); Caspar screen consumer <strong>vsync on</strong> — <a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">reference/screen-consumer-vsync-nvidia.md</a></li>\n<li><code>casparcg-scanner</code> (background)</li>\n<li>Loop: clear <strong><code>cef-cache</code></strong>, run <strong><code>casparcg-server-2.5</code></strong> with <strong><code>~/highascg/config/casparcg.config</code></strong> (respawn on crash)</li>\n</ol>\n<p>Stock <strong><code>casparcg-server.service</code></strong> is <strong>disabled</strong> — Caspar is started from Openbox, not systemd.</p>\n<hr>\n<h2 id=\"3-nvidia\">3. NVIDIA</h2>\n<table><thead><tr><th>Item</th><th>Notes</th></tr></thead><tbody>\n<tr><td><strong>Driver on build host</strong></td><td>Set **`HIGHASCG_NVIDIA_DRIVER=535\\</td><td>580\\</td><td>595<code>** before install + </code>eggs produce` (one branch per ISO)</td></tr>\n<tr><td><strong><code>/etc/highascg/nvidia-iso-driver</code></strong></td><td>Stamp file baked into the image (e.g. <code>595</code>)</td></tr>\n<tr><td><strong><code>GET /api/system/gpu-nvidia</code></strong></td><td>Read-only status + GPU → branch recommendation (<code>data/nvidia-driver-guide.json</code>)</td></tr>\n<tr><td><strong>No</strong> <code>/opt/nvidia-pool</code></td><td>Multi-driver pool removed — see <strong><code>work/work-orders/WO_single-nvidia-driver-per-iso.md</code></strong></td></tr>\n<tr><td><strong>X session</strong></td><td><code>__GL_SYNC_TO_VBLANK=0</code>, <code>highascg-nvidia-x-apply.sh</code>, <strong><code>nvidia-settings</code></strong></td></tr>\n<tr><td><strong>Screen consumer vsync</strong></td><td>NVIDIA <strong>Sync to VBlank off</strong> + Caspar <strong>screen consumer vsync on</strong> — <a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">reference/screen-consumer-vsync-nvidia.md</a></td></tr>\n</tbody></table>\n<p>Settings UI: <strong>Application Settings → system</strong> can apply another branch from the pool (WO‑39) when HighAsCG is deployed.</p>\n<hr>\n<h2 id=\"4-blackmagic-decklink\">4. Blackmagic DeckLink</h2>\n<table><thead><tr><th>Item</th><th>Notes</th></tr></thead><tbody>\n<tr><td><strong>Packages</strong></td><td><strong><code>desktopvideo</code></strong> + <strong><code>desktopvideo-gui</code></strong> (from Blackmagic Desktop Video <code>.deb</code> on build host — <strong>required</strong> for ISO)</td></tr>\n<tr><td><strong>License on ISO</strong></td><td><code>licenses/third-party/blackmagic-desktopvideo-EULA.txt</code> → <code>/usr/share/doc/highascg/licenses/</code></td></tr>\n<tr><td><strong>Kernel module</strong></td><td><strong>DKMS <code>blackmagic_io</code></strong> (must be present on build host at clone time)</td></tr>\n<tr><td><strong>Setup GUI</strong></td><td><code>desktopvideo_setup</code> — autostart in <strong><code>x11-only</code></strong> mode; optional in normal mode after delay</td></tr>\n<tr><td><strong>Build warning</strong></td><td><code>build-operator-stick.sh</code> / docs warn if *<em><code>desktopvideo</em></code>** missing on clone source</td></tr>\n</tbody></table>\n<p>DeckLink I/O is configured in Caspar / HighAsCG config (e.g. <strong><code>config/caspar_server.json</code></strong>), not baked as fixed channel maps in the ISO.</p>\n<hr>\n<h2 id=\"5-casparcg-stack\">5. CasparCG stack</h2>\n<table><thead><tr><th>Component</th><th>Path / binary</th></tr></thead><tbody>\n<tr><td><strong>CasparCG Server 2.5</strong></td><td><code>/usr/bin/casparcg-server-2.5</code></td></tr>\n<tr><td><strong>CEF (Caspar build)</strong></td><td><code>/usr/lib/casparcg-cef-*</code> (<code>.deb</code> from CasparCG/server releases)</td></tr>\n<tr><td><strong>Media scanner</strong></td><td><code>/usr/bin/casparcg-scanner</code> (port <strong>8000</strong>)</td></tr>\n<tr><td><strong>Working tree</strong></td><td><strong><code>/home/casparcg/highascg/</code></strong> (not <code>/opt/casparcg</code> in current installer)</td></tr>\n<tr><td><strong>Server config</strong></td><td><strong><code>/home/casparcg/highascg/config/casparcg.config</code></strong></td></tr>\n<tr><td><strong>Templates / media stubs</strong></td><td><strong><code>~/highascg/template/</code></strong>, <strong><code>~/highascg/media/</code></strong> (empty in ISO; media often from exFAT bind)</td></tr>\n<tr><td><strong>CEF cache</strong></td><td><strong><code>~/highascg/cef-cache/</code></strong> — cleared on each Caspar start in autostart</td></tr>\n<tr><td><strong>Logs</strong></td><td><strong><code>~/highascg/log/</code></strong>, <strong><code>/tmp/caspar.log</code></strong></td></tr>\n<tr><td><strong>NDI copies</strong></td><td>*<em><code>~/highascg/lib/libndi.so.6</em></code>** (from system NDI install)</td></tr>\n</tbody></table>\n<p><strong>Also installed on build host (Phase 2–3)</strong></p>\n<ul>\n<li><strong>FFmpeg</strong> + <strong>libdrm2</strong> — kmsgrab / x11grab for capture paths</li>\n<li><strong>NDI SDK</strong> — <code>/usr/lib/x86_64-linux-gnu/libndi.so.6</code></li>\n</ul>\n<p>Caspar is launched with config under <strong><code>~/highascg</code></strong>, matching HighAsCG’s generated <strong><code>casparcg.config</code></strong>.</p>\n<hr>\n<h2 id=\"6-highascg-nodejs-control-server\">6. HighAsCG (Node.js control server)</h2>\n<table><thead><tr><th>Item</th><th>Notes</th></tr></thead><tbody>\n<tr><td><strong>Service</strong></td><td><strong><code>highascg.service</code></strong> — <code>node index.js</code> (see <strong><code>scripts/write-highascg-systemd-unit.sh</code></strong>)</td></tr>\n<tr><td><strong>HTTP</strong></td><td>Default <strong>:4200</strong> — REST *<em><code>/api/</em></code><strong>, WS </strong><code>/api/ws</code><strong>, operator UI </strong><code>dist-web/</code><strong> at </strong><code>/</code><strong> (WO-52). </strong><code>HIGHASCG_HEADLESS=true</code>** = API-only debug (optional)</td></tr>\n<tr><td><strong>Deploy path on playout host</strong></td><td><strong><code>/home/casparcg/highascg</code></strong></td></tr>\n<tr><td><strong>Depends on</strong></td><td><strong><code>package.json</code></strong> present (WO‑47: from exFAT <strong><code>update/server/</code></strong> apply)</td></tr>\n</tbody></table>\n<p><strong>WO‑47:</strong> <strong><code>src/</code></strong>, <strong><code>scripts/</code></strong>, <strong><code>package.json</code></strong>, and <strong><code>tools/</code></strong> (playout gets <strong><code>tools/runtime/</code></strong> only via server tarball) are <strong>excluded from the squashfs</strong> and applied from <strong><code>exfat/update/server/</code></strong>. The ISO carries a <strong>minimal Caspar shell</strong> under <strong><code>~/highascg</code></strong> (see next section).</p>\n<p><strong>Optional on image (if install.sh ran on build host)</strong></p>\n<ul>\n<li>Passwordless <strong>sudo</strong> fragments for media mount, NVIDIA apply, ALSA (<code>docs/HIGHASCG_PASSWORDLESS_SUDO.md</code>)</li>\n<li><strong>Polkit</strong> rules for headless udisks (USB ingest)</li>\n</ul>\n<hr>\n<h2 id=\"wo47-what-is-on-the-iso-vs-on-exfat\">WO‑47: what is on the ISO vs on exFAT</h2>\n<h3 id=\"baked-into-squashfs-minimal-playout-shell\">Baked into squashfs (minimal playout shell)</h3>\n<p>Kept under <strong><code>/home/casparcg/highascg</code></strong> for Caspar + mounts:</p>\n<table><thead><tr><th>Path</th><th>In ISO</th></tr></thead><tbody>\n<tr><td><strong><code>bin/</code></strong></td><td>Optional site helpers</td></tr>\n<tr><td><strong><code>config/</code></strong></td><td><strong><code>casparcg.config</code></strong> from <strong><code>casparcg.config.iso</code></strong> (720p50 windowed borderless screen) when prepare embed is on</td></tr>\n<tr><td><strong><code>lib/</code></strong></td><td>e.g. <strong>NDI</strong> copies</td></tr>\n<tr><td><strong><code>media/</code></strong>, <strong><code>log/</code></strong>, <strong><code>template/</code></strong>, <strong><code>data/</code></strong>, <strong><code>cef-cache/</code></strong></td><td><strong>Empty stubs</strong> (contents excluded)</td></tr>\n<tr><td><strong><code>~/exfat</code></strong></td><td>Empty mountpoint; *<em><code>exfat/</em></code>** excluded from squashfs</td></tr>\n</tbody></table>\n<p><strong>Not in squashfs</strong> (excluded — see fragment list):</p>\n<ul>\n<li><strong><code>client/</code></strong>, <strong><code>work/</code></strong>, <strong><code>work/references/</code></strong>, <strong><code>tools/</code></strong>, <strong><code>scripts/</code></strong>, <strong><code>examples/</code></strong>, …</li>\n<li><strong><code>package.json</code></strong>, <strong><code>index.js</code></strong>, <strong><code>node_modules/</code></strong></li>\n<li>Builder <strong><code>media/</code></strong> scratch, <strong><code>.git</code></strong>, IDE caches</li>\n</ul>\n<h3 id=\"on-operator-stick-exfat-label-highascgexf\">On operator stick (exFAT label <strong><code>HIGHASCGEXF</code></strong>)</h3>\n<table><thead><tr><th>Path on stick</th><th>Role</th></tr></thead><tbody>\n<tr><td><strong><code>update/server/</code></strong></td><td>Server drop: *<em><code>highascg-server_</em>.tar.gz</code><strong> (<code>src/</code>, <code>scripts/</code>, </strong><code>tools/runtime/</code>**, …)</td></tr>\n<tr><td><strong><code>media/</code></strong>, <strong><code>templates/</code></strong>, <strong><code>configs/</code></strong>, …</td><td>Portable operator data</td></tr>\n<tr><td><strong><code>drop-config/</code></strong></td><td>Optional monolithic config</td></tr>\n</tbody></table>\n<p><strong>Boot order (simplified)</strong></p>\n<ol>\n<li><strong><code>home-casparcg-exfat.mount</code></strong> — mount <strong><code>HIGHASCGEXF</code></strong> → <strong><code>/home/casparcg/exfat</code></strong></li>\n<li><strong><code>highascg-exfat-media-prep</code></strong> + bind <strong><code>exfat/media</code></strong> → <strong><code>~/highascg/media/exfat</code></strong></li>\n<li><strong><code>highascg-exfat-server-update</code></strong> — apply <strong><code>update/server/</code></strong> → <strong><code>~/highascg</code></strong> when pending</li>\n<li><strong><code>highascg-exfat-sync</code></strong> — mtime sync <strong><code>drop-config/</code></strong> (and configured pairs)</li>\n<li><strong><code>highascg.service</code></strong> (no NVIDIA first-boot picker)</li>\n<li><strong><code>nodm</code></strong> → Openbox → Caspar</li>\n<li><strong><code>highascg.service</code></strong> — Node app when <strong><code>package.json</code></strong> exists</li>\n</ol>\n<hr>\n<h2 id=\"systemd-units-wo47-playout\">Systemd units (WO‑47 + playout)</h2>\n<table><thead><tr><th>Unit</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><code>home-casparcg-exfat.mount</code></td><td>exFAT by label <strong>HIGHASCGEXF</strong></td></tr>\n<tr><td><code>highascg-exfat-media-prep.service</code></td><td>Prepare media bind targets</td></tr>\n<tr><td><code>home-casparcg-highascg-media-exfat.mount</code></td><td>Bind exFAT media into tree</td></tr>\n<tr><td><code>highascg-exfat-server-update.service</code></td><td>Apply <strong><code>update/server/</code></strong> server drop</td></tr>\n<tr><td><code>highascg-exfat-sync.service</code></td><td>Boot sync via <strong><code>exfat-sync-cli.js</code></strong></td></tr>\n<tr><td><code>highascg.service</code></td><td>HighAsCG Node server</td></tr>\n<tr><td><code>nodm.service</code></td><td>X11 for <strong>casparcg</strong></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"network-firewall-typical\">Network &amp; firewall (typical)</h2>\n<table><thead><tr><th>Item</th><th>Notes</th></tr></thead><tbody>\n<tr><td><strong>Live networking</strong></td><td><strong>systemd-networkd</strong> + DHCP on <code>en<em></code> / <code>eth</em></code> (egg build)</td></tr>\n<tr><td><strong>UFW</strong> (if configured on build host)</td><td>Often <strong>5250</strong> AMCP, <strong>6250</strong>, <strong>8000</strong> scanner, <strong>4200</strong> HighAsCG</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"third-party-licenses-wo-90\">Third-party licenses (WO-90)</h2>\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody>\n<tr><td><strong><code>/usr/share/doc/highascg/licenses/</code></strong></td><td>NVIDIA, NDI, Blackmagic, FFmpeg, … notices + <code>manifest.json</code></td></tr>\n<tr><td><strong><code>~/highascg/licenses</code></strong></td><td>Symlink to system path when <code>15-licenses-install.sh</code> ran</td></tr>\n<tr><td><strong><code>licenses/COMPLIANCE-ISO.md</code></strong> (in repo)</td><td><strong>Read before shipping ISO</strong> — NVIDIA (OK with notice), NDI (OK with EULA + UI attribution), DeckLink (<strong>caution</strong>)</td></tr>\n</tbody></table>\n<p>Regenerate before <code>eggs produce</code>:</p>\n<pre><code class=\"language-bash\">bash tools/release/collect-third-party-licenses.sh\nsudo bash scripts/setup/15-licenses-install.sh</code></pre>\n<hr>\n<h2 id=\"explicitly-omitted-from-squashfs-eggs-excludes\">Explicitly omitted from squashfs (eggs excludes)</h2>\n<p>Summary of <a href=\"../../tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list\">&lt;strong&gt;&lt;code&gt;penguins-eggs-exclude-highascg-fragment.list&lt;/code&gt;&lt;/strong&gt;</a>:</p>\n<ul>\n<li>Full HighAsCG app tree (see WO‑47 above)</li>\n<li><strong><code>~/highascg/media</code></strong> contents, *<em><code>~/exfat/</em></code>**</li>\n<li><strong>Tailscale</strong> state under <strong><code>/var/lib/tailscale</code></strong>, <strong><code>/var/snap/tailscale</code></strong></li>\n<li><strong>casparcg</strong> shell history, <strong><code>.cursor</code></strong>, caches</li>\n</ul>\n<p>Re-merge after editing the fragment: <strong><code>sudo bash tools/eggs/live-usb/merge-penguins-eggs-exclude-highascg.sh</code></strong>, then rebuild ISO.</p>\n<hr>\n<h2 id=\"how-this-list-is-produced-on-the-build-host\">How this list is produced on the build host</h2>\n<table><thead><tr><th>Step</th><th>Script</th></tr></thead><tbody>\n<tr><td>Production stack (Caspar, nodm, NVIDIA, DeckLink, HighAsCG, …)</td><td><strong><code>sudo ./scripts/install.sh</code></strong> (phases 1–5)</td></tr>\n<tr><td>WO‑47 units + excludes + empty stubs</td><td><strong><code>sudo bash tools/eggs/live-usb/prepare-eggs-clone-with-exfat.sh</code></strong></td></tr>\n<tr><td>NVIDIA pool + live network (full build only)</td><td><strong><code>sudo bash tools/eggs/live-usb/build-highascg-egg.sh</code></strong></td></tr>\n<tr><td>Squashfs + ISO</td><td><strong><code>eggs produce --nointeractive --clone --max --excludes static --basename highascg</code></strong></td></tr>\n</tbody></table>\n<p><strong>ISO-only rebuild</strong> (host already prepared):</p>\n<pre><code class=\"language-bash\">sudo bash deprecated/tools/release/make-dev-github-release-iso-quick.sh</code></pre>\n<hr>\n<h2 id=\"quick-checklist-is-it-supposed-to-be-on-the-iso\">Quick checklist — “is it supposed to be on the ISO?”</h2>\n<table><thead><tr><th>Question</th><th>Answer</th></tr></thead><tbody>\n<tr><td>Ubuntu + kernel + systemd?</td><td><strong>Yes</strong> (clone of build host)</td></tr>\n<tr><td>nodm + Openbox + X?</td><td><strong>Yes</strong>, if install.sh ran</td></tr>\n<tr><td>NVIDIA driver per ISO?</td><td>Set <strong><code>HIGHASCG_NVIDIA_DRIVER</code></strong> on build host; <strong><code>npm run eggs:build</code></strong></td></tr>\n<tr><td>DeckLink <strong>desktopvideo</strong>?</td><td><strong>Yes</strong>, if installed on build host before produce</td></tr>\n<tr><td>Caspar + scanner binaries?</td><td><strong>Yes</strong>, if install.sh ran</td></tr>\n<tr><td><strong><code>~/highascg/config/casparcg.config</code></strong> stub?</td><td><strong>Yes</strong> (minimal shell)</td></tr>\n<tr><td>Full HighAsCG <strong><code>src/</code> / <code>node_modules</code>?</strong></td><td><strong>Yes</strong> when <strong><code>HIGHASCG_ISO_EMBED_SERVER=1</code></strong> (default). <strong><code>client/</code></strong> sources excluded from ISO; <strong><code>dist-web/</code></strong> via <strong><code>drop-update/</code></strong> (WO-52)</td></tr>\n<tr><td>Operator media / templates?</td><td><strong>No</strong> in squashfs — <strong>exFAT</strong></td></tr>\n<tr><td>GitHub alpha tarball contents?</td><td>Same as <strong>exFAT app tree</strong>, not the minimal ISO shell</td></tr>\n</tbody></table>\n<hr>\n<p><em>Last aligned with WO‑47 modular layout and <code>build-highascg-egg.sh</code> / <code>prepare-eggs-clone-with-exfat.sh</code>.</em></p>","DEV_RELEASE_GITHUB":"<h1 id=\"dev-github-releases-iso-portable-tarball\">Dev GitHub releases (ISO + portable tarball)</h1>\n<p>Use this flow when you have a <strong>build host or workstation</strong> running the repo (typically with <code>node_modules</code> already installed). Two modes:</p>\n<ul>\n<li><strong>Full image (rare)</strong> — Eggs ISO + tarball: <strong><code>npm run release:dev-github</code></strong> (needs <strong><code>sudo</code></strong> / eggs).</li>\n<li><strong>Server drop (canonical)</strong> — playout tarball includes API + <strong><code>dist-web/</code></strong> (built from in-repo <strong><code>client/</code></strong>):</li>\n<li><strong><code>npm run release:github-server</code></strong> → <code>highascg-server_&lt;UTC&gt;.tar.gz</code> (<code>src/</code> + <strong><code>dist-web/</code></strong>; <code>BUILD_STAMP</code> + <code>package.json</code> <code>version</code> = UTC stamp in tarball; working tree restored after pack)</li>\n<li><strong><code>npm run release:github-launcher</code></strong> → Electron hub (sim, multiserver, modules — packaged from <code>client/tools/electron-launcher/</code>)</li>\n<li><strong>Alpha / app-only (legacy monolith)</strong> — <strong><code>npm run release:github-app</code></strong> — full repo tarball; prefer <strong><code>release:github-server</code></strong> for playout.</li>\n</ul>\n<p><strong>Playout stick:</strong> extract *<em><code>highascg-server_</em>.tar.gz</code><strong> into </strong><code>drop-update/</code><strong> (API + UI on </strong><code>http://&lt;playout-ip&gt;:4200/</code>**). Optional Electron hub (<a href=\"https://github.com/mko1989/highascg-client\">highascg-client</a>) for simulator / multiserver / stick prep — opens browser to playout.</p>\n<h2 id=\"what-gets-published\">What gets published</h2>\n<p><strong>Full</strong> run (<code>release:dev-github</code>) produces:</p>\n<table><thead><tr><th>Asset</th><th>Produced by</th></tr></thead><tbody>\n<tr><td><code>highascg_*.iso</code> under <code>/home/eggs/</code></td><td><strong>Full:</strong> <code>sudo npm run eggs:build</code> — merges <a href=\"../../tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list\">&lt;code&gt;penguins-eggs-exclude-highascg-fragment.list&lt;/code&gt;</a> via <code>prepare-eggs-clone-with-exfat.sh</code>. <strong>Quick (deprecated):</strong> <code>sudo bash deprecated/tools/release/make-dev-github-release-iso-quick.sh</code></td></tr>\n</tbody></table>\n<p><strong>Every</strong> run produces:</p>\n<table><thead><tr><th>Asset</th><th>Produced by</th></tr></thead><tbody>\n<tr><td><code>dist/highascg_&lt;UTC&gt;.tar.gz</code></td><td><strong>Legacy</strong> monolith (<code>release:github-app</code>). <strong>WO-52 server</strong> tarball includes <strong><code>dist-web/</code></strong>.</td></tr>\n</tbody></table>\n<p><strong>Legacy alpha</strong> (<code>npm run release:github-app</code>): monolith tarball only — prefer <strong><code>release:github-server</code></strong> + <strong><code>release:github-launcher</code></strong> for new deployments.</p>\n<p>GitHub Releases have a soft <strong>per-asset ~2 GiB</strong> limit. If the server tarball is too large, use <code>--zip-exclude-node-modules</code> and run <code>npm ci</code> under <code>~/highascg</code> after the stick applies <code>update/server/</code>.</p>\n<h3 id=\"why-is-the-server-tarball-so-big\">Why is the <strong>server</strong> tarball so big?</h3>\n<p>The <strong>server</strong> asset (<code>release:github-server</code>) includes <strong><code>dist-web/</code></strong> (WO-52) but not in-tree <strong><code>client/</code></strong> sources. Size still adds up because:</p>\n<table><thead><tr><th>Component</th><th>Typical share</th></tr></thead><tbody>\n<tr><td><strong><code>node_modules/</code></strong></td><td>Largest — all runtime deps (optional packages too if installed). Use <code>--zip-exclude-node-modules</code> + <code>npm ci</code> on the stick.</td></tr>\n<tr><td><strong><code>tools/runtime/</code></strong></td><td>Only tools subtree in server tarball (<code>exfat-sync-cli</code>, staged Caspar).</td></tr>\n<tr><td><strong><code>src/</code></strong>, <strong><code>dist-web/</code></strong></td><td>Node bridge + operator UI bundle</td></tr>\n<tr><td><strong><code>scripts/</code></strong>, <strong><code>config/</code></strong>, <strong><code>template/</code></strong></td><td>Install helpers and Caspar templates.</td></tr>\n</tbody></table>\n<p>The <strong>client</strong> tarball (if published separately) is <code>dist-web/</code> only. The <strong>launcher</strong> tarball ships <code>client/tools/electron-launcher/</code> (optional Electron packaging — not the canonical UI tree). <strong><code>release:github-app</code></strong> remains a legacy monolith.</p>\n<p>Shared rules: <a href=\"../../scripts/archive-common.sh\">&lt;code&gt;scripts/archive-common.sh&lt;/code&gt;</a> (used by deploy + release scripts).</p>\n<h2 id=\"prerequisites\">Prerequisites</h2>\n<ol>\n<li><strong><code>gh</code></strong> installed and <strong><code>gh auth login</code></strong> finished for the repo you push to.</li>\n<li><strong><code>tar</code></strong> (gzip) — standard on Ubuntu/Debian. <strong><code>sudo</code></strong> only for <strong>full</strong> image builds (<strong><code>npm run release:dev-github</code></strong>).</li>\n<li><strong>penguins‑eggs</strong> only when you build a <strong>full</strong> ISO (same expectations as <a href=\"../../tools/eggs/live-usb/BUILD_AND_FLASH.md\">&lt;strong&gt;&lt;code&gt;BUILD_AND_FLASH.md&lt;/code&gt;&lt;/strong&gt;</a>).</li>\n<li>Repo root checkout (for <code>npm run …</code> wrappers below).</li>\n</ol>\n<h2 id=\"commands\">Commands</h2>\n<h3 id=\"split-server-launcher-canonical\">Split — server + launcher (canonical)</h3>\n<pre><code class=\"language-bash\">npm run release:github-server:dry\nnpm run release:github-launcher:dry\nnpm run release:github-server\nnpm run release:github-launcher</code></pre>\n<p>On the playout stick (WO-52):</p>\n<pre><code class=\"language-bash\">mkdir -p &lt;mount&gt;/drop-update\ntar -xzf highascg-server_&lt;stamp&gt;.tar.gz -C &lt;mount&gt;/drop-update</code></pre>\n<p>After boot: open <strong><code>http://&lt;playout-ip&gt;:4200/</code></strong>. Optional Electron hub (<a href=\"https://github.com/mko1989/highascg-client\">highascg-client</a>) opens the same URL for simulator / multiserver / modules. <strong><code>HIGHASCG_HEADLESS=true</code></strong> is API-only debug, not production default.</p>\n<h3 id=\"legacy-monolith-tarball\">Legacy — monolith tarball</h3>\n<pre><code class=\"language-bash\">npm run release:github-app:dry\nnpm run release:github-app</code></pre>\n<h3 id=\"full-image-eggs-iso-tarball-rare\">Full image — Eggs ISO + tarball (rare)</h3>\n<p>Dry run:</p>\n<pre><code class=\"language-bash\">npm run release:dev-github:dry</code></pre>\n<p>Full prerelease with default full ISO:</p>\n<pre><code class=\"language-bash\">npm run release:dev-github</code></pre>\n<p>Direct script equivalents:</p>\n<pre><code class=\"language-bash\">./deprecated/tools/release/make-dev-github-release.sh --app-only --dry-run\n./deprecated/tools/release/make-dev-github-release.sh --app-only\n./deprecated/tools/release/make-dev-github-release.sh --dry-run\n./deprecated/tools/release/make-dev-github-release.sh\n./deprecated/tools/release/make-dev-github-release.sh --quick-iso --tag dev-smoke-$(date -u +%Y%m%d)</code></pre>\n<p>See <code>./deprecated/tools/release/make-dev-github-release.sh --help</code> for every flag.</p>\n<p>Useful variants:</p>\n<table><thead><tr><th>Need</th><th>Flags</th></tr></thead><tbody>\n<tr><td><strong>Server + launcher prereleases</strong></td><td><strong><code>release:github-server</code></strong> + <strong><code>release:github-launcher</code></strong></td></tr>\n<tr><td><strong>Legacy monolith tarball</strong></td><td><strong><code>npm run release:github-app</code></strong> or <strong><code>--app-only</code></strong></td></tr>\n<tr><td>Rebuild tarball + attach <strong>existing</strong> ISO</td><td><code>--no-iso</code> (still expects an ISO under <code>/home/eggs/</code>).</td></tr>\n<tr><td>Smaller archive</td><td><code>--zip-exclude-node-modules</code></td></tr>\n<tr><td>Repeat same tag during testing</td><td><code>--replace</code></td></tr>\n<tr><td>Custom tag</td><td><code>--tag name</code></td></tr>\n<tr><td>Custom output directory</td><td><code>--out-dir /tmp/rel</code></td></tr>\n<tr><td>Historical source in archive</td><td><code>--zip-with-git</code> and/or <code>--zip-with-work</code></td></tr>\n</tbody></table>\n<h2 id=\"iso-discovery\">ISO discovery</h2>\n<p>The helper uses <strong><code>find_latest_iso</code></strong> from <a href=\"../../tools/eggs/live-usb/flash-stick-common.sh\">&lt;code&gt;flash-stick-common.sh&lt;/code&gt;</a> (same rule as flashing tools — typically <strong><code>/home/eggs/</strong>/<em>.iso</code><strong> newest). Align </strong><code>BASENAME</code></em>* with Eggs output if you renamed the image.</p>\n<h2 id=\"operator-path-stick-studio-release-tarball\">Operator path: Stick Studio + release tarball</h2>\n<p>Designed to match WO‑47: live system on hybrid ISO plus <strong><code>HIGHASCGEXF</code></strong> exFAT for data.</p>\n<h3 id=\"1-download-release-assets\">1. Download release assets</h3>\n<p>From the GitHub <strong>Releases</strong> page, download:</p>\n<ul>\n<li>The <strong>ISO</strong> (from a <strong>full</strong> <code>release:dev-github</code> run, or reuse an older ISO you keep on file).</li>\n<li><strong><code>highascg_&lt;timestamp&gt;.tar.gz</code></strong> — <strong>every</strong> prerelease has this; <strong>alpha</strong> / <strong><code>release:github-app</code></strong> releases have <strong>only</strong> this file (unless you used <code>--zip-exclude-node-modules</code>).</li>\n</ul>\n<h3 id=\"2-flash-iso-and-carve-exfat-desktop\">2. Flash ISO and carve exFAT (desktop)</h3>\n<p>On a workstation with UI:</p>\n<pre><code class=\"language-bash\">npm run stick-studio</code></pre>\n<p>Documentation: <strong><code>client/tools/stick-tools/README.md</code></strong>.</p>\n<p>Rough order in the GUI:</p>\n<ol>\n<li>Point <strong>ISO</strong> at the downloaded file and <strong>whole-disk USB</strong>.</li>\n<li>Enable <strong>Erase stick with ISO</strong>, run <strong>pkexec pipeline</strong> (or flash first, then exFAT-only if you skipped dd).</li>\n<li>Enable <strong>Append exFAT … HIGHASCGEXF</strong> where appropriate.</li>\n<li>Mount the exFAT volume and set <strong>mount path</strong> in Stick Studio; create <strong><code>update/server/</code></strong> (and operator dirs).</li>\n</ol>\n<h3 id=\"3-lay-out-server-drop-on-the-stick\">3. Lay out server drop on the stick</h3>\n<p>With the exFAT volume mounted at <code>&lt;mount&gt;</code>:</p>\n<pre><code class=\"language-bash\">mkdir -p &lt;mount&gt;/update/server\ntar -xzf /path/to/highascg-server_&lt;stamp&gt;.tar.gz -C &lt;mount&gt;/update/server</code></pre>\n<p>so that <code>&lt;mount&gt;/update/server/package.json</code> exists (includes <strong><code>tools/runtime/</code></strong>).</p>\n<p>If the archive omitted <code>node_modules</code>, run <strong><code>npm ci</code></strong> on the playout host after first boot apply (or bake deps into the tarball).</p>\n<p><strong>Monolith / alpha tarball (deprecated):</strong> see <code>deprecated/tools/release/make-dev-github-release.sh</code> — prefer split server + client releases.</p>\n<p>(Use <strong><code>install:base</code></strong> / <strong><code>install:previs</code></strong> from <code>package.json</code> if you mirror production optional deps.)</p>\n<h3 id=\"4-simulation-mode\">4. Simulation mode</h3>\n<p>Stick Studio runs <strong><code>npm run portable:sim</code></strong> against the <strong>HighAsCG repo path</strong> configured at the top of the window (point this at your <strong>local git checkout</strong> on the workstation, not necessarily the stick copy). That script uses <a href=\"../../client/tools/portable-desktop/launch-sim-from-exfat.js\">&lt;code&gt;client/tools/portable-desktop/launch-sim-from-exfat.js&lt;/code&gt;</a> to drive simulation using exFAT paths.</p>\n<p>For headless / CI testing, from a repo that has the same mount layout available:</p>\n<pre><code class=\"language-bash\">npm run portable:sim</code></pre>\n<h3 id=\"5-reference-docs\">5. Reference docs</h3>\n<ul>\n<li><a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">&lt;strong&gt;&lt;code&gt;WO47_ISO_VS_EXFAT.md&lt;/code&gt;&lt;/strong&gt;</a> — split between squashfs and exFAT.</li>\n<li><a href=\"../../tools/eggs/live-usb/BUILD_AND_FLASH.md\">&lt;strong&gt;&lt;code&gt;BUILD_AND_FLASH.md&lt;/code&gt;&lt;/strong&gt;</a> — full build and flash runbook.</li>\n<li><a href=\"../../tools/eggs/live-usb/EXFAT_DATA_ZERO_TOUCH.md\">&lt;strong&gt;&lt;code&gt;EXFAT_DATA_ZERO_TOUCH.md&lt;/code&gt;&lt;/strong&gt;</a> — mount and data layout on the stick.</li>\n</ul>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<ul>\n<li><strong>Eggs prints “finished” but the release script says there is no ISO</strong> — the image is often at *<em><code>/home/eggs/mnt/highascg_</em>.iso</code><strong>. The release script now </strong><code>chmod</code><strong>s that tree after a full/quick build so your user can read it; if you built ISO separately, run </strong><code>sudo chmod a+rx /home/eggs/mnt</code><strong> and </strong><code>sudo chmod a+r /home/eggs/mnt/<em>.iso</code><strong>, or set </strong><code>HIGHASCG_ISO=/home/eggs/mnt/….iso</code></em>*.</li>\n<li><strong>Exit code 141</strong> — usually <strong>SIGPIPE</strong> (e.g. a broken pipe or the terminal closing while a long <strong><code>tar</code></strong>/upload is still running). Re-run after the ISO step finishes, or run <strong><code>./deprecated/tools/release/make-dev-github-release.sh --no-iso</code></strong> once the ISO already exists (still uploads that ISO + a fresh tarball).</li>\n<li><strong><code>HIGHASCG_ISO</code></strong> is <strong>ignored</strong> when using <strong><code>--app-only</code></strong> / <strong><code>release:github-app</code></strong> (those releases intentionally have no ISO asset).</li>\n</ul>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<table><thead><tr><th>Variable</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>BASENAME</code></td><td>Passed to Eggs / build scripts (default <code>highascg</code>).</td></tr>\n<tr><td><code>HIGHASCG_ISO</code></td><td>For <strong>full</strong> releases: explicit <code>.iso</code> path (skips <strong><code>find_latest_iso</code></strong> under <strong><code>/home/eggs/</code></strong>). <strong>Ignored</strong> with <strong><code>--app-only</code></strong>.</td></tr>\n<tr><td><code>GITHUB_REPOSITORY</code></td><td><code>owner/name</code> if <code>gh</code> cannot infer from <code>git remote</code>.</td></tr>\n<tr><td><code>GH_TOKEN</code></td><td>Token for non-interactive <code>gh</code> (CI).</td></tr>\n</tbody></table>\n<h2 id=\"ci-note\">CI note</h2>\n<p><code>eggs produce</code> and full ISO builds usually need <strong>root</strong> and a prepared Linux host. <strong>Alpha</strong> uploads (<strong><code>--app-only</code></strong>) avoid that path entirely. For GitHub Actions you may run <strong><code>release:github-app</code></strong>, <strong><code>--dry-run</code></strong>, or attach prebuilt ISO artifacts separately; keep secrets (<code>GH_TOKEN</code>) in encrypted vars.</p>","BOOT_EMERGENCY_RECOVERY":"<h1 id=\"boot-emergency-mode-ctrld-to-continue\">Boot emergency mode (Ctrl+D to continue)</h1>\n<p>If the machine drops into <strong>emergency mode</strong> during boot and you must press <strong>Ctrl+D</strong> to continue, check the journal on the playout PC:</p>\n<pre><code class=\"language-bash\">journalctl -b -p err --no-pager | tail -40\njournalctl -b -g 'emergency|local-fs|HIGHASCG|boot-efi' --no-pager | tail -30</code></pre>\n<h2 id=\"common-causes-wo-47-wo-52-playout-installs\">Common causes (WO-47 / WO-52 playout installs)</h2>\n<table><thead><tr><th>Symptom in journal</th><th>Cause</th><th>Fix</th></tr></thead><tbody>\n<tr><td><code>Timed out waiting for device dev-disk-by-uuid-1E9C-3CEB</code></td><td><code>/etc/fstab</code> <strong>/boot/efi</strong> points at an old ESP UUID (disk reinstall / imaging)</td><td>Run fix script (updates fstab to current vfat UUID)</td></tr>\n<tr><td><code>Timed out waiting for device dev-disk-by-label-HIGHASCGEXF</code></td><td><strong>USB stick not plugged in</strong> but <code>home-casparcg-exfat.mount</code> was <strong>enabled</strong> at boot</td><td>Fix script <strong>disables</strong> USB mount at <code>local-fs</code>; <code>highascg-exfat-boot.service</code> mounts when stick appears</td></tr>\n<tr><td><code>Dependency failed for local-fs.target</code></td><td>Combination of the above</td><td>Same fix script</td></tr>\n</tbody></table>\n<p>USB and bridge volumes are <strong>optional at boot</strong>. Missing stick or bridge disk must not block <code>local-fs.target</code>.</p>\n<h2 id=\"one-command-fix-run-on-each-affected-pc\">One-command fix (run on each affected PC)</h2>\n<pre><code class=\"language-bash\">cd ~/highascg &amp;&amp; sudo bash scripts/fix-boot-emergency-recovery.sh\nsudo reboot</code></pre>\n<p>After reboot, confirm:</p>\n<pre><code class=\"language-bash\">systemctl is-enabled home-casparcg-exfat.mount   # disabled\nsystemctl is-enabled highascg-exfat-boot.service # enabled\nfindmnt /boot/efi || echo &quot;EFI not mounted (check fstab)&quot;</code></pre>\n<h2 id=\"manual-efi-fstab-check\">Manual EFI fstab check</h2>\n<pre><code class=\"language-bash\">grep /boot/efi /etc/fstab\nlsblk -f | grep vfat\nblkid | grep vfat</code></pre>\n<p>The UUID in fstab must match the vfat ESP on the boot drive (often <code>nvme0n1p1</code>).</p>","openbox_autostart":"<h1 id=\"openbox-autostart-x-session-only-layout-nvidia-caspar-scanner-systemd-wo-73\">Openbox autostart — X session only (layout, NVIDIA). Caspar + scanner = systemd (WO-73).</h1>\n<p><strong>Current installer:</strong> <code>scripts/setup/09-openbox-autostart.sh</code> writes <code>~/.config/openbox/autostart</code> for the Caspar user. It does <strong>not</strong> start <code>casparcg-scanner</code> or <code>run.sh</code> when <code>casparcg-scanner.service</code> / <code>casparcg-server.service</code> are enabled (<code>scripts/setup/13-caspar-systemd-units.sh</code>).</p>\n<ul>\n<li><strong><code>casparcg-scanner.service</code></strong> — <code>After=network.target</code> only (no X, no nodm).</li>\n<li><strong><code>casparcg-server.service</code></strong> — runs <code>run.sh</code> under systemd.</li>\n</ul>\n<p>Sync wiring: <code>sudo bash scripts/setup/sync-caspar-supervisor-wiring.sh</code></p>\n<hr>\n<h2 id=\"why-sudo-systemctl-restart-nodm-did-not-stop-caspar-legacy\">Why <code>sudo systemctl restart nodm</code> did not stop Caspar (legacy)</h2>\n<ol>\n<li><strong>Background <code>&amp;</code></strong> — The <code>while true; do … done &amp;</code> block detaches from Openbox. After a nodm/X restart, those processes are <strong>not always</strong> in the same process tree as the new session; old loops can <strong>keep running</strong> (orphaned or still under a surviving user manager).</li>\n<li><strong>Autostart runs again</strong> — Each new X session executes <code>autostart</code> again, so you get <strong>another</strong> <code>while true</code> loop → <strong>another</strong> Caspar main process while the old one may still be alive.</li>\n<li><strong>Caspar spawns many children</strong> — Only the “main” lines without <code>--type=</code> are extra servers; killing X does not guarantee all of them exit together if multiple mains were started.</li>\n</ol>\n<p><strong>Operational rule:</strong> before or after a nodm restart, assume you may need to <strong>stop Caspar explicitly</strong> once:</p>\n<pre><code class=\"language-bash\"># See main server processes (no CEF --type= helpers)\npgrep -af 'casparcg-server-2.5 /opt/casparcg/config/casparcg.config' | grep -v -- '--type='\n\n# Stop them (run as user that owns the processes, or use sudo if needed)\npkill -f '/usr/bin/casparcg-server-2.5 /opt/casparcg/config/casparcg.config'\n# Wait until none left, then restart nodm or let autostart start a single instance.</code></pre>\n<p>Use <strong><code>pkill -f</code></strong> carefully: it matches the full command line; adjust if your config path differs.</p>\n<hr>\n<h2 id=\"recommended-autostart-single-loop-single-caspar\">Recommended autostart (single loop, single Caspar)</h2>\n<p>Use a <strong>file lock</strong> so only <strong>one</strong> autostart loop can run, even if Openbox runs <code>autostart</code> multiple times.</p>\n<pre><code class=\"language-bash\">#!/bin/bash\nexport DISPLAY=:0\nexport XAUTHORITY=/home/casparcg/.Xauthority\n\nxset s off\nxset s noblank\nxset -dpms\nunclutter -idle 1 -root &amp;\n# Optional (production installer): NVIDIA Sync to VBlank off — see install-phase2.sh / docs/reference/screen-consumer-vsync-nvidia.md\n# [ -x /usr/local/bin/highascg-nvidia-x-apply.sh ] &amp;&amp; /usr/local/bin/highascg-nvidia-x-apply.sh\n\nif [ -f /etc/highascg/display-mode ] &amp;&amp; grep -q '^x11-only$' /etc/highascg/display-mode; then\n  if command -v desktopvideo_setup &gt;/dev/null 2&gt;&amp;1; then\n    (sleep 2 &amp;&amp; desktopvideo_setup) &amp;\n  fi\n  if command -v xterm &gt;/dev/null 2&gt;&amp;1; then\n    (xterm -e 'bash -c &quot;echo X11-only: CasparCG not started.; echo Open Desktop Video Setup from the menu.; echo Resume: sudo highascg-display-mode normal; read&quot;') &amp;\n  fi\nelse\n  # --- Single instance: second autostart exits immediately (nodm/X restart, duplicate runs) ---\n  (\n    exec 9&gt;/tmp/caspar-openbox-autostart.lock\n    if ! flock -n 9; then\n      exit 0\n    fi\n\n    cd /opt/casparcg || exit 1\n    /usr/bin/casparcg-scanner &amp;\n    export CASPAR_RESPAWN=1\n    ./run.sh &gt;&gt; /tmp/caspar.log 2&gt;&amp;1\n  ) &amp;\nfi</code></pre>\n<p><strong>Installer:</strong> <code>scripts/install-phase3.sh</code> writes this logic to <code>~/.config/openbox/autostart</code> for the Caspar user (with <code>XAUTHORITY=/home/&lt;user&gt;/.Xauthority</code>). It inserts the <strong>NVIDIA</strong> line above after <code>unclutter</code> when <code>/usr/local/bin/highascg-nvidia-x-apply.sh</code> exists (Phase 2).</p>\n<p>Notes:</p>\n<ul>\n<li><strong><code>flock -n</code></strong> — If another session already holds <code>/tmp/caspar-openbox-autostart.lock</code>, this autostart <strong>does nothing</strong> (no second Caspar loop).</li>\n<li><strong>CEF cache</strong> — Each restart clears <strong>everything</strong> under <code>/opt/casparcg/cef-cache</code> (<code>find … -mindepth 1 -delete</code>) so stale Chromium profile data does not accumulate.</li>\n<li><strong>Port check</strong> — <code>grep -qE ':5250\\b'</code> is a bit stricter than <code>grep 5250</code>.</li>\n</ul>\n<hr>\n<h2 id=\"restart-workflow-suggested\">Restart workflow (suggested)</h2>\n<ol>\n<li><strong>Planned restart:</strong></li>\n</ol>\n<p><code>pkill -f '/usr/bin/casparcg-server-2.5 /opt/casparcg/config/casparcg.config'</code> → wait until <code>pgrep</code> shows none → <code>sudo systemctl restart nodm</code> (or reboot).</p>\n<ol>\n<li><strong>After unclean restart:</strong></li>\n</ol>\n<p>If you see <strong>more than one</strong> main <code>casparcg-server-2.5 … casparcg.config</code> without <code>--type=</code>, kill all mains as above once, then let <strong>one</strong> autostart loop respawn Caspar (or restart nodm after the kill).</p>\n<ol>\n<li><strong>Long term (current):</strong> Caspar runs as <strong>systemd</strong> units (<code>casparcg-scanner.service</code>, <code>casparcg-server.service</code>); Openbox handles X utilities and display layout only.</li>\n</ol>\n<hr>\n<h2 id=\"previous-inline-script-for-diff-history\">Previous inline script (for diff / history)</h2>\n<pre><code class=\"language-bash\">#!/bin/bash\nexport DISPLAY=:0\nexport XAUTHORITY=/home/casparcg/.Xauthority\n\nxset s off\nxset s noblank\nxset -dpms\nunclutter -idle 1 -root &amp;\n\nif [ -f /etc/highascg/display-mode ] &amp;&amp; grep -q '^x11-only$' /etc/highascg/display-mode; then\n  if command -v desktopvideo_setup &gt;/dev/null 2&gt;&amp;1; then\n    (sleep 2 &amp;&amp; desktopvideo_setup) &amp;\n  fi\n  if command -v xterm &gt;/dev/null 2&gt;&amp;1; then\n    (xterm -e 'bash -c &quot;echo X11-only: CasparCG not started.; echo Open Desktop Video Setup from the menu.; echo Resume: sudo highascg-display-mode normal; read&quot;') &amp;\n  fi\nelse\n  cd /opt/casparcg\n  /usr/bin/casparcg-scanner &amp;\n  while true; do\n    cd /opt/casparcg\n    mkdir -p /opt/casparcg/cef-cache\n    find /opt/casparcg/cef-cache -mindepth 1 -delete 2&gt;/dev/null || true\n    /usr/bin/casparcg-server-2.5 /opt/casparcg/config/casparcg.config &gt;&gt; /tmp/caspar.log 2&gt;&amp;1\n    while ss -tlnp | grep -q 5250; do sleep 1; done\n    sleep 2\n  done &amp;\nfi</code></pre>\n<p>Problem: no lock → every new Openbox autostart could start <strong>another</strong> background loop → multiple Caspar mains.</p>","HIGHASCG_PASSWORDLESS_SUDO":"<h1 id=\"highascg-passwordless-sudo-nopasswd\">HighAsCG passwordless sudo (<code>NOPASSWD</code>)</h1>\n<p>HighAsCG’s Node service often runs as <strong><code>casparcg</code></strong> (see installer). Several features call <strong><code>sudo -n</code></strong> so the Web UI works <strong>without an interactive TTY password</strong>.</p>\n<p>This page lists <strong>exact commands</strong> that should be mirrored in <strong><code>/etc/sudoers.d/</code></strong> (or one consolidated file). After edits, run <strong><code>sudo visudo -c</code></strong> or <strong><code>visudo -cf /etc/sudoers.d/…</code></strong> to validate syntax.</p>\n<p>Always prefer <strong>one fixed path per action</strong> (wrapper script with <strong>no user-controlled arguments</strong>) over broad rules like <code>NOPASSWD: ALL</code>.</p>\n<hr>\n<h2 id=\"installed-by-highascg-installer-reference\">Installed by HighAsCG installer (reference)</h2>\n<table><thead><tr><th>Fragment / rule</th><th>User</th><th>Command</th><th>Feature</th></tr></thead><tbody>\n</tbody></table>\n<p>Source templates in the repo:</p>\n<ul>\n<li><code>scripts/install-phase4.sh</code> (installs helpers + sudoers fragments)</li>\n</ul>\n<p><strong>Removed (2026-05):</strong> WO-38 <strong><code>highascg-media-mount</code></strong> / <strong><code>media/drive</code></strong> partition mount — durable config/state/media use <strong>exFAT only</strong> (<code>HIGHASCGEXF</code>, WO-47). See <strong><code>work/work-orders/WO_remove-persistence-partition-workflow_exfat-only.md</code></strong>.</p>\n<p><strong>Removed (2026-05):</strong> Multi-branch <strong><code>/opt/nvidia-pool</code></strong> and <strong><code>nvidia-apply-from-pool</code></strong> — one NVIDIA driver per live ISO (<code>HIGHASCG_NVIDIA_DRIVER=535|580|595</code>). See <strong><code>work/work-orders/WO_single-nvidia-driver-per-iso.md</code></strong>.</p>\n<h3 id=\"optional-alsa-highascg-asound-off-by-default\">Optional ALSA — <strong><code>highascg-asound</code></strong> (off by default)</h3>\n<p><strong>You usually do not need this on a PortAudio-first / device-name reference system.</strong> Caspar’s PortAudio consumer uses <strong>device indices or names</strong> from the server; HighAsCG already supports a <strong>per-user ALSA default</strong> via <strong><code>~/.asoundrc</code></strong> with <strong>no sudo</strong> (<code>scope: user</code> on <code>POST /api/audio/default-device</code> — see <code>src/audio/audio-devices.js</code>).</p>\n<p><strong><code>/etc/sudoers.d/highascg-asound</code></strong> (NOPASSWD <strong><code>tee</code> → <code>/etc/asound.conf</code></strong>) is only for <strong><code>scope: system</code></strong>, i.e. forcing a <strong>global</strong> default ALSA PCM for <strong>non-Caspar</strong> “system audio”. Install it only if you really use that path:</p>\n<pre><code class=\"language-bash\">HIGHASCG_INSTALL_ASOUND_SUDOERS=1 sudo -E ./scripts/install.sh</code></pre>\n<p>Otherwise leave it <strong>unset</strong> (default <strong><code>0</code></strong>) to keep the eggs image minimal. The fragment is emitted by <strong><code>install-phase3.sh</code></strong> only when that variable is <strong><code>1</code></strong>.</p>\n<hr>\n<h2 id=\"used-by-node-but-not-always-installed-automatically\">Used by Node but not always installed automatically</h2>\n<p>These appear in <strong><code>sudo -n</code></strong> call sites. If the Nuclear / setup actions fail with a password prompt error, add matching <strong><code>NOPASSWD</code></strong> lines for <strong><code>casparcg</code></strong> (or whichever user runs <code>node</code>):</p>\n<table><thead><tr><th>Binary (typical path)</th><th>Arguments</th><th>Source</th></tr></thead><tbody>\n<tr><td><strong><code>/bin/systemctl</code></strong></td><td><code>restart nodm</code></td><td><code>src/api/routes-system-setup.js</code>, <code>src/utils/os-config.js</code></td></tr>\n<tr><td><strong><code>/usr/bin/systemctl</code></strong></td><td><code>restart nodm</code></td><td>Same (path varies by distro)</td></tr>\n<tr><td><strong><code>/sbin/reboot</code></strong></td><td><em>(none)</em></td><td><code>src/api/routes-system-setup.js</code></td></tr>\n<tr><td><strong><code>/usr/sbin/reboot</code></strong></td><td><em>(none)</em></td><td>Same</td></tr>\n<tr><td><strong><code>/bin/systemctl</code></strong></td><td><code>reboot</code></td><td>Same</td></tr>\n<tr><td><strong><code>/usr/bin/systemctl</code></strong></td><td><code>reboot</code></td><td>Same</td></tr>\n<tr><td><strong><code>/usr/local/lib/highascg/highascg-webui-server-update.sh</code></strong></td><td><code>--source &lt;extract-dir&gt;</code></td><td>Web UI server update (WO-66)</td></tr>\n<tr><td><strong><code>/usr/bin/eggs</code></strong></td><td><code>calamares</code></td><td><code>src/api/routes-system-setup.js</code> (DISPLAY often <code>:0</code>)</td></tr>\n<tr><td><strong><code>/usr/local/lib/highascg/highascg-network-apply.sh</code></strong></td><td>fixed <code>dhcp</code> / <code>static</code> + allow-listed iface args</td><td>WO-59 <strong>Apply network</strong> (<code>POST /api/system/network/apply</code>)</td></tr>\n<tr><td><strong><code>/usr/local/lib/highascg/highascg-network-reset.sh</code></strong></td><td>optional <code>[iface]</code></td><td><strong>Reset network</strong> (<code>POST /api/system/network/reset</code>) — DHCP renew / reconnect</td></tr>\n<tr><td><strong><code>/usr/local/lib/highascg/highascg-tailscale-up.sh</code></strong></td><td><em>(none — reads <code>config/tailscale.json</code>)</em></td><td>WO-91 / WO-97 pinned Tailscale bring-up</td></tr>\n<tr><td><strong><code>/usr/local/lib/highascg/highascg-vcam-modules-up.sh</code></strong></td><td><em>(none — reads <code>/run/highascg/vcam-modules.conf</code>)</em></td><td>WO-137 virtual camera — <code>v4l2loopback</code> + <code>snd-aloop</code></td></tr>\n<tr><td><strong><code>/usr/bin/tailscale</code></strong></td><td><code>logout</code></td><td><code>src/network/tailscale-service.js</code></td></tr>\n<tr><td><strong><code>/snap/bin/tailscale</code></strong></td><td><code>logout</code></td><td>Same</td></tr>\n</tbody></table>\n<p><strong>Removed (WO-97, 2026-07-02):</strong> <code>tailscale up</code> and <code>tailscale up <em></code> NOPASSWD wildcards — use <strong><code>highascg-tailscale-up.sh</code></strong> only. </em><em><code>eggs calamares --install </em></code>** removed (build-host root task only).</p>\n<p><strong>WO-66 Web UI server update</strong> (install helper via <code>install-exfat-systemd-units.sh</code>, then sudoers):</p>\n<pre><code class=\"language-bash\">echo 'casparcg ALL=(root) NOPASSWD: /usr/local/lib/highascg/highascg-webui-server-update.sh' | sudo tee /etc/sudoers.d/highascg-webui-server-update\nsudo visudo -cf /etc/sudoers.d/highascg-webui-server-update</code></pre>\n<p><strong>WO-59 network helpers</strong> (install manually until wired in <code>install-phase4.sh</code>):</p>\n<pre><code class=\"language-bash\">sudo bash scripts/runtime/install-network-apply.sh casparcg</code></pre>\n<p>Installs <strong><code>highascg-network-apply.sh</code></strong> and <strong><code>highascg-network-reset.sh</code></strong> with matching sudoers.</p>\n<p><strong>Optional power button</strong> (short press = network reset, hold 3s = shutdown):</p>\n<pre><code class=\"language-bash\">sudo bash scripts/setup/14-power-button-network-reset.sh</code></pre>\n<p>Uses <strong><code>evtest</code></strong> + <strong><code>logind</code> <code>HandlePowerKey=ignore</code></strong>. Only enable on kiosk/playout boxes where accidental shutdown is acceptable.</p>\n<p>Uses <strong>NetworkManager (<code>nmcli</code>)</strong> on the live image (NM active; netplan files present but NM owns connections).</p>\n<p><strong>WO-137 virtual camera modules</strong> (install via setup script):</p>\n<pre><code class=\"language-bash\">sudo bash scripts/setup/12-passwordless-sudo.sh</code></pre>\n<p>Installs <strong><code>highascg-vcam-modules-up.sh</code></strong> with matching sudoers. HighAsCG writes <strong><code>/run/highascg/vcam-modules.conf</code></strong> then runs <strong><code>sudo -n /usr/local/lib/highascg/highascg-vcam-modules-up.sh</code></strong> on virtual cam start. See <a href=\"#/wiki--integration--virtual-camera-output\" data-wiki-page=\"1\">wiki/integration/virtual-camera-output.md</a>.</p>\n<p><strong>Not <code>sudo -n</code> today (interactive sudo):</strong></p>\n<ul>\n<li><strong><code>os-config.js</code></strong> — persisting X11 layout writes <strong><code>/etc/highascg/apply-layout.sh</code></strong> and <strong><code>/etc/X11/Xsession.d/99highascg-layout</code></strong> via <strong><code>sudo tee</code></strong> without <code>-n</code>. Operators need a password session or future passwordless rules / a small wrapper (out of scope unless you add WO-39 helpers).</li>\n</ul>\n<hr>\n<h2 id=\"settings-system-decklink-tailscale-wo-39-wo-91\">Settings → <strong>system</strong> / <strong>decklink</strong> / <strong>Tailscale</strong> (WO-39 / WO-91)</h2>\n<ul>\n<li><strong>NVIDIA:</strong> read-only <strong><code>GET /api/system/gpu-nvidia</code></strong> (ISO branch stamp + GPU guide). Driver switching via Settings was removed — use the correct single-driver ISO. <strong>Screen consumer vsync:</strong> NVIDIA <strong>Sync to VBlank off</strong> + Caspar <strong>vsync on</strong> — <a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">reference/screen-consumer-vsync-nvidia.md</a>.</li>\n<li><strong>GUI launch:</strong> <strong><code>POST /api/system/gui-launch</code></strong> spawns allow-listed apps on <strong><code>:0</code></strong> with <strong><code>XAUTHORITY</code></strong> from <strong><code>getXAuthority()</code></strong> — <strong>no sudo</strong> when binaries are executable for the service user.</li>\n<li><strong>Tailscale (WO-91):</strong> <strong><code>GET /api/network/tailscale/status</code></strong>, <strong><code>POST /api/network/tailscale/login</code></strong>, <strong><code>POST /api/network/tailscale/enable</code></strong>, etc. — see <a href=\"#/reference--tailscale-integration\" data-wiki-page=\"1\">reference/tailscale-integration.md</a>. Requires passwordless <strong><code>sudo tailscale up</code></strong>, <strong><code>sudo tailscale logout</code></strong>, and <strong><code>systemctl … tailscaled</code></strong> / <strong><code>snap.tailscale.tailscaled</code></strong>.</li>\n</ul>\n<hr>\n<h2 id=\"operational-commands-that-stay-password-protected-examples\">Operational commands that stay password-protected (examples)</h2>\n<p>These are <strong>supposed</strong> to stay interactive or polkit-gated:</p>\n<ul>\n<li><strong><code>sudo apt upgrade</code></strong> (general system changes)</li>\n<li><strong><code>sudo eggs produce</code></strong> (image build — run on a build host, not from the playout Web UI)</li>\n</ul>\n<hr>\n<h2 id=\"verification-snippets\">Verification snippets</h2>\n<pre><code class=\"language-bash\"># Service user\nid casparcg\n\n# Non-interactive check (should succeed when rules exist)\nsudo -n -u casparcg /usr/bin/true</code></pre>\n<hr>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><code>tools/eggs/live-usb/build-flash-and-persist.sh</code> — build + flash USB (run as <strong>root</strong>, not via Web UI)</li>\n<li><code>scripts/README.md</code> — install overview</li>\n<li><a href=\"#/reference--SUDO_UBUNTU_SETUP\" data-wiki-page=\"1\">reference/SUDO_UBUNTU_SETUP.md</a> — historical / audio-related sudo notes</li>\n</ul>","BRIDGE_DISK_AND_USB_EXFAT":"<h1 id=\"bridge-disk-highascgdat-and-usb-stick-highascgexf\">Bridge disk (HIGHASCGDAT) and USB stick (HIGHASCGEXF)</h1>\n<p>WO-52 splits operator storage into two volumes.</p>\n<h2 id=\"bridge-partition-main-internal-disk\">Bridge partition (main — internal disk)</h2>\n<table><thead><tr><th>Item</th><th>Value</th></tr></thead><tbody>\n<tr><td><strong>Label</strong></td><td><code>HIGHASCGDAT</code> (exFAT, ≤11 characters)</td></tr>\n<tr><td><strong>Linux mount</strong></td><td><code>/home/casparcg/bridge</code></td></tr>\n<tr><td><strong>Media library</strong></td><td><code>bridge/media/</code> bind-mounted to <strong><code>/home/casparcg/highascg/media/bridge</code></strong> (local files under <code>media/</code> stay visible)</td></tr>\n<tr><td><strong>Configs</strong></td><td><code>bridge/configs/</code> ↔ <code>~/highascg/config/</code> + state JSON (bidirectional, boot prefers bridge)</td></tr>\n<tr><td><strong>Projects</strong></td><td><code>bridge/projects/*.json</code> ↔ <code>~/highascg/projects/</code> (bidirectional mtime sync)</td></tr>\n</tbody></table>\n<p>Create on Windows (Disk Management) or Linux:</p>\n<pre><code class=\"language-bash\">sudo mkfs.exfat -L HIGHASCGDAT /dev/nvme0n1p3\n\nAfter the partition exists with that label, install units and sync map on the playout host:\n</code></pre>\n<p>cd ~/highascg &amp;&amp; sudo bash scripts/apply-bridge-label-highascgdat.sh</p>\n<pre><code>sudo bash tools/eggs/live-usb/seed-bridge-operator-layout.sh /mount/point</code></pre>\n<p>Seed layout only:</p>\n<pre><code class=\"language-bash\">sudo bash tools/eggs/live-usb/seed-bridge-operator-layout.sh /home/casparcg/bridge</code></pre>\n<h2 id=\"usb-stick-field-kit-live-iso-tail\">USB stick (field kit — live ISO tail)</h2>\n<table><thead><tr><th>Item</th><th>Value</th></tr></thead><tbody>\n<tr><td><strong>Label</strong></td><td><code>HIGHASCGEXF</code></td></tr>\n<tr><td><strong>Linux mount</strong></td><td><code>/home/casparcg/exfat</code></td></tr>\n<tr><td><strong>Media</strong></td><td><code>exfat/media/</code> → <strong>one-way ingest</strong> into <code>~/highascg/media/bridge</code> (USB → bridge library only)</td></tr>\n<tr><td><strong>Configs</strong></td><td><code>exfat/configs/</code> ↔ project (bidirectional; boot prefers USB when stick present)</td></tr>\n<tr><td><strong>Projects</strong></td><td><code>exfat/projects/*.json</code> — stick catalog; boot pulls to <code>~/highascg/projects/</code>; saves push <strong>only the active slug</strong> while stick mounted</td></tr>\n<tr><td><strong>Server drops</strong></td><td><code>drop-update/</code> (unchanged)</td></tr>\n<tr><td><strong>Private (per machine)</strong></td><td><code>.private/&lt;machine-id&gt;/</code> on USB and bridge — Tailscale/Syncthing/replication pairing; <strong>not</strong> in <code>configs/</code> sync</td></tr>\n</tbody></table>\n<p>Use <a href=\"../../dist/HIGHASCGEXF-starter-layout.zip\">&lt;code&gt;HIGHASCGEXF-starter-layout.zip&lt;/code&gt;</a> for USB folder stubs.</p>\n<h2 id=\"boot-order\">Boot order</h2>\n<ol>\n<li><code>highascg-bridge-boot.service</code> — mount <code>HIGHASCGDAT</code>, bind media</li>\n<li><code>highascg-exfat-boot.service</code> — mount <code>HIGHASCGEXF</code>, server-update, sync</li>\n<li><code>highascg-exfat-sync.service</code> — bridge configs, then USB configs, then USB media ingest</li>\n<li><code>highascg.service</code></li>\n</ol>\n<h2 id=\"sync-map\">Sync map</h2>\n<p>Installed from <code>config/exfat-sync.json</code> → <code>/etc/highascg/exfat-sync.json</code>.</p>\n<p>Legacy <strong>USB <code>media/exfat</code> bind</strong> is disabled by default. Enable only for old sticks:</p>\n<pre><code class=\"language-bash\">sudo HIGHASCG_LEGACY_USB_MEDIA_BIND=1 bash scripts/install-exfat-systemd-units.sh</code></pre>\n<h2 id=\"missing-volume-behavior-graceful\">Missing-volume behavior (graceful)</h2>\n<table><thead><tr><th>Scenario</th><th>Boot</th><th><code>highascg.service</code></th><th>Media library</th><th>Config sync</th><th>USB media ingest</th></tr></thead><tbody>\n<tr><td><strong>Both absent</strong></td><td>Continues</td><td>Starts if <code>package.json</code> exists</td><td>Empty local <code>~/highascg/media/</code></td><td>Skipped (log only)</td><td>Skipped</td></tr>\n<tr><td><strong>Bridge only</strong></td><td>Continues</td><td>Starts</td><td><code>bridge/media</code> → <code>~/highascg/media</code></td><td>Bridge pairs</td><td>Skipped</td></tr>\n<tr><td><strong>USB only</strong></td><td>Continues</td><td>Starts</td><td>Local <code>~/highascg/media/</code> unless legacy bind enabled</td><td>USB pairs (boot prefer USB)</td><td>USB → <code>~/highascg/media</code></td></tr>\n<tr><td><strong>Both present</strong></td><td>Full pipeline</td><td>Starts</td><td>Bridge bind wins for library</td><td>Bridge then USB on boot</td><td>USB → library</td></tr>\n</tbody></table>\n<ul>\n<li>Mount units use <strong><code>nofail</code></strong> and <strong><code>ConditionPathExists</code></strong> on the label — no partition → unit does not run, boot does not hang.</li>\n<li><strong><code>highascg.service</code></strong> does <strong>not</strong> <code>Wants</code> exFAT/bridge mounts (avoids ~90s device timeout).</li>\n<li><strong><code>highascg-exfat-sync.service</code></strong> skips pairs whose volume is not mounted; exits <strong>0</strong> when neither volume is present.</li>\n<li><strong><code>highascg-exfat-server-update</code></strong> exits <strong>0</strong> if USB not mounted or no <code>drop-update/package.json</code>.</li>\n<li><strong>Hotplug</strong> (<code>highascg-exfat-arrive</code>): no-op if stick absent; legacy <code>media/exfat</code> bind only with <code>legacy-usb-media-bind</code>.</li>\n</ul>\n<h2 id=\"next-eggs-iso-auto-mount-highascgdat\">Next eggs ISO (auto-mount HIGHASCGDAT)</h2>\n<p>On the <strong>imaging host</strong> before <code>eggs produce</code>:</p>\n<pre><code class=\"language-bash\">cd ~/highascg\nsudo bash tools/eggs/live-usb/prepare-eggs-clone-with-exfat.sh\nsudo bash tools/eggs/live-usb/build-highascg-egg.sh   # or your eggs produce flow</code></pre>\n<p>That bakes WO-52 units plus:</p>\n<ul>\n<li><strong>45s</strong> wait for <code>HIGHASCGDAT</code> at boot (<code>HIGHASCG_BRIDGE_BOOT_WAIT_SEC</code> in <code>20-live-boot.conf</code>)</li>\n<li><strong>udev</strong> <code>99-highascg-bridge-arrive.rules</code> — mounts bridge when NVMe appears after USB boot</li>\n</ul>\n<p>After flashing the new ISO, verify once:</p>\n<pre><code class=\"language-bash\">findmnt /home/casparcg/bridge\ntail /var/log/highascg-bridge-boot.log</code></pre>\n<h2 id=\"live-usb-operator-stick\">Live USB (operator stick)</h2>\n<table><thead><tr><th>Label</th><th>On stick?</th><th>On live boot</th></tr></thead><tbody>\n<tr><td><strong>HIGHASCGEXF</strong></td><td>Yes — MBR <strong>slot 3</strong> after <code>create-operator-stick-from-dd.sh</code></td><td>Should mount at <strong><code>/home/casparcg/exfat</code></strong></td></tr>\n<tr><td><strong>HIGHASCGDAT</strong></td><td><strong>No</strong> — internal NVMe/SATA bridge disk only</td><td>Absent unless you created that partition on the <strong>machine’s internal drive</strong></td></tr>\n</tbody></table>\n<p>Settings showing <code>map: /etc/highascg/exfat-sync.json</code> with <strong>bridge not mounted</strong> is normal on USB-only boots. Bridge pairs in the table stay <strong>volume not mounted</strong> until <code>HIGHASCGDAT</code> exists.</p>\n<p><strong>Diagnostics on the live system:</strong></p>\n<pre><code class=\"language-bash\">sudo bash ~/highascg/tools/eggs/live-usb/diagnose-exfat-volumes.sh\nsudo systemctl start highascg-exfat-boot.service\nlsblk -f</code></pre>\n<p>If <strong><code>HIGHASCGEXF</code></strong> is missing from <code>lsblk</code>, the stick has no data partition (plain <code>dd</code> without <code>finish-operator-stick</code> / <code>create-operator-stick-from-dd.sh</code> step 2). Re-flash with the full operator script.</p>\n<h2 id=\"related\">Related</h2>\n<ul>\n<li><a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">&lt;code&gt;WO47_ISO_VS_EXFAT.md&lt;/code&gt;</a> — ISO vs stick payload</li>\n<li><a href=\"../../work/work-orders/52_WO_BRIDGE_DISK_PARTITION_AND_USB_SYNC.md\">&lt;code&gt;work/work-orders/52_WO_BRIDGE_DISK_PARTITION_AND_USB_SYNC.md&lt;/code&gt;</a> — full WO</li>\n</ul>","WO47_ISO_VS_EXFAT":"<h1 id=\"wo47-what-stays-in-the-eggs-squashfs-vs-what-rides-on-exfat\">WO‑47: What stays in the Eggs squashfs vs what rides on exFAT</h1>\n<p>This matches <strong><code>tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list</code></strong> and the boot chain <strong><code>highascg-exfat-server-update</code></strong> → <strong><code>highascg-exfat-sync</code></strong> → <strong><code>highascg.service</code></strong>.</p>\n<p><strong>Server drops:</strong> <a href=\"#/EXFAT_SERVER_UPDATE\" data-wiki-page=\"1\">&lt;code&gt;EXFAT_SERVER_UPDATE.md&lt;/code&gt;</a> (<code>exfat/drop-update/</code> — contents of *<em><code>highascg-server_</em>.tar.gz</code><strong>, including </strong><code>tools/runtime/</code>** only).</p>\n<h2 id=\"intended-layout-on-the-iso-minimal-caspar-shell\">Intended layout on the ISO (minimal “Caspar shell”)</h2>\n<p>Keep these under <strong><code>/home/casparcg/highascg</code></strong> in the clone source so live/Caspar starts:</p>\n<table><thead><tr><th>Path</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><strong><code>bin/</code></strong></td><td>Site / helper binaries (optional; Caspar may expect scripts here)</td></tr>\n<tr><td><strong><code>cef-cache/</code></strong></td><td>Empty in image; Caspar recreates at runtime</td></tr>\n<tr><td><strong><code>config/</code></strong></td><td><strong><code>casparcg.config</code></strong> at minimum</td></tr>\n<tr><td><strong><code>data/</code></strong></td><td>Empty stub</td></tr>\n<tr><td><strong><code>lib/</code></strong></td><td><strong><code>libndi.so</code></strong> copies etc. (Phase 3 installer)</td></tr>\n<tr><td><strong><code>log/</code></strong></td><td>Empty stub</td></tr>\n<tr><td><strong><code>media/</code></strong></td><td>Empty stub (WO‑47 bind mounts <strong><code>exfat/media</code></strong> → <strong><code>media/exfat</code></strong> when present)</td></tr>\n<tr><td><strong><code>template/</code></strong></td><td>Empty stub unless your Caspar config references templates in-tree</td></tr>\n</tbody></table>\n<p><strong>Openbox</strong> autostart still <strong><code>cd /home/casparcg/highascg</code></strong> for Caspar + scanner.</p>\n<h2 id=\"omitted-from-squashfs-restored-from-stick\">Omitted from squashfs (restored from stick)</h2>\n<p>Excluded from the snapshot; applied from <strong><code>exfat/drop-update/</code></strong> when the stick is present:</p>\n<ul>\n<li><strong><code>index.js</code></strong>, <strong><code>src/</code></strong>, <strong><code>scripts/</code></strong>, <strong><code>package.json</code></strong>, <strong><code>package-lock.json</code></strong></li>\n<li><strong><code>tools/</code></strong> (entire tree) — playout receives <strong><code>tools/runtime/</code></strong> only inside the server tarball (<code>exfat-sync-cli.js</code>, staged Caspar helpers)</li>\n<li><strong><code>client/</code></strong>, <strong><code>dist-web/</code></strong>, <strong><code>work/</code></strong>, <strong><code>deprecated/</code></strong>, <strong><code>node_modules/</code></strong>, dev trees</li>\n<li><strong><code>.private/</code></strong> — device identity/auth material (Syncthing device ID, Tailscale status, replication pairing); <strong><code>projects/_trash/</code></strong> — deleted-project tombstones; *<em><code>config/</em>.bak<em></code></em>* / <code>casparcg copy.config</code> — config backups (WO-168; see <code>tools/eggs/live-usb/EGGS_EXCLUDE_LIST.md</code> for the full exclude/reset/verify matrix)</li>\n</ul>\n<p><strong>First boot / hotfix:</strong> <strong><code>highascg-exfat-server-update.service</code></strong> rsyncs <strong><code>drop-update/</code></strong> → <strong><code>~/highascg/</code></strong> when <strong><code>drop-update/package.json</code></strong> exists (stops <strong><code>highascg.service</code></strong>, optional <strong><code>npm ci</code></strong>). <strong>Live USB:</strong> drop <strong>stays</strong> in <strong><code>drop-update/</code></strong> (retain — <code>~/highascg/</code> is reset each boot). <strong>Persistent install:</strong> drop may move to <strong><code>drop-update/applied/&lt;UTC&gt;/</code></strong>. Legacy <strong><code>update/server/</code></strong> is still accepted once.</p>\n<p>Then <strong><code>highascg-exfat-sync.service</code></strong> runs <strong><code>node tools/runtime/exfat-sync-cli.js</code></strong> (<strong><code>configs/</code> ↔ <code>config/</code></strong>, <strong><code>drop-config/</code></strong>, state JSON per <strong><code>/etc/highascg/exfat-sync.json</code></strong>; post-save debounced sync from the Node app).</p>\n<p><strong><code>sim/highascg/</code></strong> is not used on playout sticks (simulation runs from the <strong>Electron launcher</strong>). Legacy <strong><code>highascg-exfat-bootstrap</code></strong> is disabled by default.</p>\n<h2 id=\"safeguards-knobs\">Safeguards (knobs)</h2>\n<table><thead><tr><th>File / env</th><th>Effect</th></tr></thead><tbody>\n<tr><td><strong><code>/etc/highascg/disable-exfat-server-update</code></strong></td><td>Skip server drop apply</td></tr>\n<tr><td><strong><code>/etc/highascg/disable-exfat-bootstrap</code></strong></td><td>Skip legacy bootstrap (if unit still enabled)</td></tr>\n<tr><td><strong><code>HIGHASCG_SERVER_UPDATE_DRY_RUN=1</code></strong></td><td>Log only (server update)</td></tr>\n<tr><td><strong><code>ConditionPathExists=…/package.json</code></strong> on <strong><code>highascg.service</code></strong></td><td>Node app does not start until tree exists</td></tr>\n</tbody></table>\n<h2 id=\"standalone-iso-embedded-server\">Standalone ISO (embedded server)</h2>\n<p>By default <strong><code>prepare-eggs-clone-with-exfat.sh</code></strong> sets <strong><code>HIGHASCG_ISO_EMBED_SERVER=1</code></strong> and <strong><code>HIGHASCG_ISO_BUILD_WEB=0</code></strong>:</p>\n<ul>\n<li>Resets *<em><code>config/</em>.json</code><strong> from </strong><code>src/config/defaults.js</code><strong> (not the eggs build host’s saved settings) via </strong><code>reset-iso-operator-config.sh</code><strong>; installs </strong><code>config/casparcg.config</code><strong> from </strong><code>config/casparcg.config.iso</code><strong> (single </strong>720p50<strong> windowed borderless screen consumer); copies </strong><code>.env.example</code> → <code>.env</code><strong> (headless stub; build-host </strong><code>.env</code>** is excluded from squashfs).</li>\n<li>Runs <strong><code>npm ci --omit=dev</code></strong> so <strong><code>package.json</code></strong>, <strong><code>src/</code></strong>, <strong><code>node_modules/</code></strong> are in the squashfs — <strong><code>dist-web/</code></strong> is <strong>not</strong> baked into squashfs; it arrives via <strong><code>exfat/drop-update/</code></strong> (WO-52: server serves web GUI on <code>:4200</code> on the same machine).</li>\n<li><strong><code>highascg.service.d/10-headless.conf</code></strong> is <strong>not</strong> installed by default (WO-52). Use <strong><code>HIGHASCG_INSTALL_HEADLESS=1</code></strong> only for API-only debug.</li>\n<li>Merges <strong><code>penguins-eggs-exclude-highascg-embed-server.list</code></strong> (excludes <strong><code>client/</code></strong>, <strong><code>dist-web/</code></strong>, dev trees).</li>\n</ul>\n<p>Set <strong><code>HIGHASCG_ISO_EMBED_SERVER=0</code></strong> for Caspar shell only (Node app from <strong><code>exfat/drop-update/</code></strong>). Set <strong><code>HIGHASCG_ISO_BUILD_WEB=1</code></strong> only for legacy monolith ISO experiments.</p>\n<h2 id=\"operator-workflow\">Operator workflow</h2>\n<ol>\n<li>Build host: <strong><code>sudo npm run eggs:prepare</code></strong> (or <strong><code>sudo bash tools/eggs/live-usb/prepare-eggs-clone-with-exfat.sh</code></strong>) — WO‑47 units + exclude merge (+ ISO defaults when embed is on)</li>\n<li>Eggs <strong><code>--clone</code></strong>: squashfs honors <strong><code>exclude.list</code></strong> fragment (re-merge after edits: <strong><code>sudo bash tools/eggs/live-usb/merge-penguins-eggs-exclude-highascg.sh</code></strong>)</li>\n<li>Stick: extract *<em><code>highascg-server_</em>.tar.gz</code><strong> into </strong><code>drop-update/</code><strong> (must include </strong><code>dist-web/</code><strong>, </strong><code>package.json</code><strong>, </strong><code>src/</code><strong>, </strong><code>tools/runtime/</code>**, …)</li>\n<li>Boot with <strong><code>HIGHASCGEXF</code></strong>: server-update applies when pending; sync runs when <strong><code>tools/runtime/exfat-sync-cli.js</code></strong> exists; <strong><code>highascg.service</code></strong> starts when <strong><code>package.json</code></strong> is present; open <strong><code>http://&lt;playout-ip&gt;:4200/</code></strong> for operator UI (same machine as API)</li>\n<li><strong>Electron launcher</strong> (optional, Mac/Windows/Linux): multiplatform hub for stick setup, links, sim — opens browser to <code>:4200</code>; not required on playout</li>\n</ol>\n<h2 id=\"prerequisites-on-image\">Prerequisites on image</h2>\n<p><strong><code>rsync</code></strong> must be installed (<strong><code>prepare-eggs-clone-with-exfat.sh</code></strong> installs <strong><code>rsync</code></strong>, <strong><code>parted</code></strong>, <strong><code>exfatprogs</code></strong>).</p>","EXFAT_SERVER_UPDATE":"<h1 id=\"exfat-server-updates-drop-update\">exFAT server updates (<code>drop-update/</code>)</h1>\n<p>For a <strong>closed ISO</strong> (no <code>src/</code> in squashfs), operators refresh the <strong>Node server + operator UI</strong> by dropping files on the stick — without reflashing the image.</p>\n<p><strong>WO-52:</strong> Server drops include <strong><code>dist-web/</code></strong> so stick updates refresh the browser UI on playout.</p>\n<h2 id=\"stick-layout\">Stick layout</h2>\n<table><thead><tr><th>Path on <code>HIGHASCGEXF</code></th><th>Purpose</th></tr></thead><tbody>\n<tr><td><strong><code>drop-update/</code></strong></td><td>Server drop — extract *<em><code>highascg-server_</em>.tar.gz</code><strong> here (must include </strong><code>dist-web/</code>** for UI)</td></tr>\n<tr><td><code>drop-update/applied/&lt;UTC&gt;/</code></td><td>Optional audit copies (live USB <strong>retain</strong> mode); <strong>consumed</strong> drops on persistent installs</td></tr>\n<tr><td><code>drop-update/.applied-stamp</code></td><td>Last successfully applied build stamp (retain mode)</td></tr>\n<tr><td><code>drop-config/</code></td><td>Optional <code>highascg.config.json</code> (mtime sync)</td></tr>\n<tr><td><code>media/</code>, <code>templates/</code>, <code>configs/</code>, …</td><td>Operator data</td></tr>\n</tbody></table>\n<p><strong>Not on playout stick:</strong> <code>sim/highascg/</code> (simulation from <strong>Electron hub</strong> on Mac/Windows), in-repo <strong><code>client/</code></strong> UI sources.</p>\n<p><strong>Legacy:</strong> <code>update/server/</code> is still applied once if present (log asks you to use <code>drop-update/</code>).</p>\n<h2 id=\"drop-workflow\">Drop workflow</h2>\n<ol>\n<li>On a workstation, extract *<em><code>highascg-server_</em>.tar.gz</code><strong> from <a href=\"#/DEV_RELEASE_GITHUB\" data-wiki-page=\"1\">&lt;code&gt;release:github-server&lt;/code&gt;</a> into <code>drop-update/</code> on the exFAT volume (must include <code>package.json</code> and </strong><code>dist-web/index.html</code>** at the top of that folder).</li>\n<li>Boot the live system (or reboot).</li>\n<li><strong><code>highascg-exfat-server-update.service</code></strong> runs <strong>before</strong> <code>highascg.service</code>:</li>\n</ol>\n<ul>\n<li>Stops <code>highascg.service</code></li>\n<li>Validates drop (<code>package.json</code>, <code>src/</code>, <strong><code>dist-web/index.html</code></strong>, <code>tools/runtime/</code>)</li>\n<li><code>rsync</code> from <code>exfat/drop-update/</code> → <code>/home/casparcg/highascg/</code> (includes <strong><code>dist-web/</code></strong>; does not deploy <strong><code>client/</code></strong> sources)</li>\n<li>Runs <code>npm ci --omit=dev</code> when <code>package-lock.json</code> is in the drop</li>\n<li><strong>Live USB (retain):</strong> leaves the drop in <strong><code>drop-update/</code></strong> — required on every cold boot because <code>~/highascg/</code> is not durable on the stick image</li>\n<li><strong>Persistent install (consume):</strong> moves the drop to <code>drop-update/applied/&lt;UTC&gt;/</code> after success</li>\n<li>Starts <code>highascg.service</code></li>\n</ul>\n<h2 id=\"what-the-server-tarball-contains\">What the server tarball contains</h2>\n<table><thead><tr><th>Included</th><th>Excluded</th></tr></thead><tbody>\n<tr><td><code>index.js</code>, <code>src/</code>, <code>config/</code>, <code>template/</code>, <code>scripts/</code>, <strong><code>dist-web/</code></strong></td><td><strong><code>client/</code></strong> (sources only)</td></tr>\n<tr><td><strong><code>tools/runtime/</code></strong> only</td><td><code>tools/smoke/</code>, <code>tools/eggs/</code>, <code>tools/release/</code></td></tr>\n</tbody></table>\n<h2 id=\"operator-ui\">Operator UI</h2>\n<p>Open <strong><code>http://&lt;playout-ip&gt;:4200/</code></strong> on the playout machine or LAN after deploy. Same origin — no separate client install on playout.</p>\n<p>Optional <strong>Electron launcher</strong> on Mac/Windows (<a href=\"https://github.com/mko1989/highascg-client\">&lt;strong&gt;highascg-client&lt;/strong&gt;</a>) — simulator, multiserver, modules packaging from <code>client/tools/electron-launcher/</code>; opens the system browser to playout <code>:4200</code>. Operator UI is <strong><code>client/</code></strong> → <code>dist-web/</code> in this repo.</p>\n<h2 id=\"disable-test\">Disable / test</h2>\n<table><thead><tr><th>Knob</th><th>Effect</th></tr></thead><tbody>\n<tr><td><code>/etc/highascg/disable-exfat-server-update</code></td><td>Skip apply</td></tr>\n<tr><td><code>/etc/highascg/server-update-retain-drop</code></td><td>Force <strong>retain</strong> (installed on live-USB images)</td></tr>\n<tr><td><code>/etc/highascg/server-update-consume-drop</code></td><td>Force <strong>consume</strong> (move to <code>applied/</code>)</td></tr>\n<tr><td><code>HIGHASCG_SERVER_UPDATE_RETAIN_DROP=1</code></td><td>Force retain</td></tr>\n<tr><td><code>HIGHASCG_SERVER_UPDATE_RETAIN_DROP=0</code></td><td>Force consume</td></tr>\n<tr><td><code>HIGHASCG_SERVER_UPDATE_DRY_RUN=1</code></td><td>Log only</td></tr>\n<tr><td><code>HIGHASCG_SERVER_UPDATE_NPM_CI=0</code></td><td>Skip <code>npm ci</code> after rsync</td></tr>\n<tr><td><code>HIGHASCG_SERVER_UPDATE_ARCHIVE_COPY=1</code></td><td>Retain mode: optional <code>cp -a</code> audit under <code>applied/</code></td></tr>\n</tbody></table>\n<p>Manual run (root): <code>/usr/local/lib/highascg/highascg-exfat-server-update.sh</code> Apply helper: <code>/usr/local/lib/highascg/highascg-apply-server-drop.sh</code></p>\n<h2 id=\"boot-order\">Boot order</h2>\n<pre><code>exfat mount → server-update (drop-update/) → exfat-sync (drop-config/) → highascg.service</code></pre>\n<p>If the USB stick enumerates <strong>after</strong> <code>local-fs.target</code> (slow hub/port), <strong><code>highascg-exfat-arrive.service</code></strong> runs when udev sees <code>LABEL=HIGHASCGEXF</code>. Disable with <code>/etc/highascg/disable-exfat-arrive</code>.</p>\n<p>See also: <a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">&lt;code&gt;WO47_ISO_VS_EXFAT.md&lt;/code&gt;</a>, <a href=\"../../from_client/AGENT_SERVER_CLIENT_MERGE.md\">&lt;code&gt;from_client/AGENT_SERVER_CLIENT_MERGE.md&lt;/code&gt;</a>, <a href=\"../../tools/eggs/live-usb/EXFAT_DATA_ZERO_TOUCH.md\">&lt;code&gt;tools/eggs/live-usb/EXFAT_DATA_ZERO_TOUCH.md&lt;/code&gt;</a>.</p>","USB_AUTO_MOUNT_UBUNTU":"<h1 id=\"ubuntu-auto-mount-usb-drives-for-the-casparcg-user-highascg-import\">Ubuntu: auto-mount USB drives for the <code>casparcg</code> user (HighAsCG import)</h1>\n<p>This walkthrough is for a <strong>playout</strong> machine where:</p>\n<ul>\n<li>Linux user <strong><code>casparcg</code></strong> owns the unified <strong><code>/home/casparcg/highascg</code></strong> playout tree (Caspar + HighAsCG), runs <strong>CasparCG</strong> (and usually X / displays), and runs the <strong>HighAsCG</strong> <code>systemd</code> service.</li>\n<li>You plug in a <strong>USB stick</strong> and want it <strong>mounted without manual <code>sudo</code></strong>, at a path the <strong>HighAsCG</strong> process (running as <code>casparcg</code>) can read, so the web UI can <strong>browse</strong> the volume and <strong>import</strong> files into <strong><code>/home/casparcg/highascg/media/</code></strong>.</li>\n</ul>\n<p>The HighAsCG app does <strong>not</strong> implement its own block-device driver. It lists volumes via <strong><code>lsblk</code></strong> (and related logic in <code>src/media/usb-drives.js</code>) and only sees devices that already have a <strong><code>MOUNTPOINT</code></strong>. So the OS must <strong>mount</strong> the stick (almost always via <strong>udisks2</strong> on Ubuntu).</p>\n<hr>\n<h2 id=\"1-install-packages-and-groups-once\">1. Install packages and groups (once)</h2>\n<p>On Ubuntu:</p>\n<pre><code class=\"language-bash\">sudo apt update\nsudo apt install -y udisks2 policykit-1</code></pre>\n<p>Ensure <strong><code>casparcg</code></strong> is in <strong><code>plugdev</code></strong> (the production installer does this in <code>scripts/install-phase4.sh</code>):</p>\n<pre><code class=\"language-bash\">sudo usermod -aG plugdev casparcg</code></pre>\n<p>Log out and back in <strong>or</strong> start a new session so group membership applies to running services. After changing groups, <strong>restart</strong> the HighAsCG service so the worker picks up the new supplementary groups if it was already running:</p>\n<pre><code class=\"language-bash\">sudo systemctl restart highascg</code></pre>\n<p>(Exact unit name may differ; check <code>systemctl list-units '<em>highascg</em>'</code>.)</p>\n<hr>\n<h2 id=\"2-polkit-allow-casparcg-to-mount-unmount-power-off-without-a-password\">2. Polkit: allow <code>casparcg</code> to mount / unmount / power-off without a password</h2>\n<p>The repo ships a rule for <strong><code>plugdev</code></strong> users who have an <strong>“active”</strong> polkit session (typical of desktop logins). On a <strong>headless</strong> or service-only box, <code>subject.active</code> is often <strong>false</strong> when you <code>ssh</code> in or when only <code>systemd</code> user sessions run. Then you also need the <strong>headless</strong> rule.</p>\n<ol>\n<li><strong>Base rule (desktop-friendly)</strong> — from the repo:</li>\n</ol>\n<pre><code class=\"language-text\">   scripts/polkit/50-highascg-udisks.rules</code></pre>\n<p>Install:</p>\n<pre><code class=\"language-bash\">   sudo cp /home/casparcg/highascg/scripts/polkit/50-highascg-udisks.rules /etc/polkit-1/rules.d/\n   sudo chmod 644 /etc/polkit-1/rules.d/50-highascg-udisks.rules</code></pre>\n<ol>\n<li><strong>Headless / dedicated <code>casparcg</code> rule (recommended on servers)</strong> — does <strong>not</strong> require <code>subject.active</code> but only allows the service user (default <strong><code>casparcg</code></strong>) in <strong><code>plugdev</code></strong>:</li>\n</ol>\n<pre><code class=\"language-text\">   scripts/polkit/51-highascg-udisks-casparcg-headless.rules</code></pre>\n<p>The production <strong>Phase 4</strong> install copies this file, substitutes <strong><code>USER_CASPAR</code></strong> (from <a href=\"../../scripts/install-config.sh\">install-config.sh</a>), and restarts polkit. For a <strong>manual</strong> install, copy the file and ensure <code>subject.user == 'casparcg'</code> (or your service account name) matches the user that runs HighAsCG, then set mode <code>644</code>.</p>\n<ol>\n<li>Reload polkit (or reboot):</li>\n</ol>\n<pre><code class=\"language-bash\">   sudo systemctl restart polkit\n   # or: sudo systemctl try-restart polkit.service</code></pre>\n<hr>\n<h2 id=\"3-systemd-linger-often-fixes-empty-mount-list-on-first-plug\">3. Systemd “linger” (often fixes empty mount list on first plug)</h2>\n<p>For <strong>user</strong> <code>casparcg</code>, allow user <code>@</code> services and a stable session so udev/udisks can attach mounts to that user (especially if nobody logs in on the console):</p>\n<pre><code class=\"language-bash\">sudo loginctl enable-linger casparcg</code></pre>\n<p>Reboot once after this if udisks still does not list mounts for that user.</p>\n<hr>\n<h2 id=\"4-what-path-will-the-stick-use\">4. What path will the stick use?</h2>\n<p>On current Ubuntu, udisks usually mounts removable volumes under one of:</p>\n<ul>\n<li><code>/media/casparcg/&lt;LABEL-or-uuid&gt;/</code></li>\n<li><code>/run/media/casparcg/&lt;LABEL-or-uuid&gt;/</code></li>\n</ul>\n<p>HighAsCG’s USB module only allows paths under those prefixes (see <code>isAllowedMountpoint</code> in <code>src/media/usb-drives.js</code>). If your distro uses only <code>/run/media/...</code>, that is already accepted.</p>\n<hr>\n<h2 id=\"5-verify-as-casparcg-before-using-the-web-ui\">5. Verify as <code>casparcg</code> (before using the web UI)</h2>\n<p>List block devices and mounts:</p>\n<pre><code class=\"language-bash\">sudo -u casparcg -- lsblk -J -o NAME,LABEL,SIZE,TYPE,MOUNTPOINT,FSTYPE,RM,TRAN</code></pre>\n<p>If the stick is inserted but <strong>MOUNTPOINT</strong> is empty, try mounting the partition manually <strong>as the same user</strong> the service uses:</p>\n<pre><code class=\"language-bash\">sudo -u casparcg -- udisksctl mount -b /dev/sdX1</code></pre>\n<p>Replace <code>/dev/sdX1</code> with the correct partition. If that succeeds, polkit and permissions are in good shape; auto-mount on hotplug may still depend on the next section.</p>\n<p>Eject / power when done (optional):</p>\n<pre><code class=\"language-bash\">sudo -u casparcg -- udisksctl unmount -b /dev/sdX1\nsudo -u casparcg -- udisksctl power-off -b /dev/sdX</code></pre>\n<hr>\n<h2 id=\"6-if-the-stick-still-does-not-auto-mount-on-insert\">6. If the stick still does not auto-mount on insert</h2>\n<ol>\n<li><strong>Confirm udisks is running:</strong> <code>systemctl status udisks2</code> (or <code>udisksd</code> depending on the release).</li>\n<li><strong>Kernel / uaccess:</strong> most consumer USB storage gets a <code>TAGS</code> including <code>uaccess</code> so udisks is allowed to mount. Exotic or broken devices may need a different port or a manual mount once to test.</li>\n<li><strong>Keep using manual mount</strong> for debugging: as long as <strong><code>casparcg</code></strong> runs <code>udisksctl mount</code> (or the desktop auto-mounts to <code>/run/media/casparcg/...</code>), HighAsCG will list the path after <strong>refresh</strong> in the UI.</li>\n<li><strong>Do not</strong> point Caspar or HighAsCG at raw `/dev/sd<em> paths<strong> — the app is built around </strong>directory<strong> paths on a </strong>mounted</em>* filesystem.</li>\n</ol>\n<hr>\n<h2 id=\"7-highascg-import-destination\">7. HighAsCG: import destination</h2>\n<ul>\n<li>Default Caspar <strong>media</strong> tree is <strong><code>/home/casparcg/highascg/media</code></strong>. The installer chowns that tree to <strong><code>casparcg</code></strong>.</li>\n<li>In the web app: <strong>Settings → media/usb</strong> — enable import if needed; the copy target comes from the configured media path (same as Caspar’s media folder for imports).</li>\n<li><strong>Sources → + → Import from USB…</strong> — browse the mounted volume, select files, import. Files land under <strong><code>/home/casparcg/highascg/media/</code></strong> (or a subfolder per your <code>usbIngest</code> settings).</li>\n</ul>\n<p>If imports fail with permission errors, fix ownership of <strong><code>/home/casparcg/highascg/media</code></strong>, not the USB (USB is only read for copy):</p>\n<pre><code class=\"language-bash\">sudo chown -R casparcg:casparcg /home/casparcg/highascg/media\nsudo chmod -R 775 /home/casparcg/highascg/media</code></pre>\n<hr>\n<h2 id=\"8-quick-checklist\">8. Quick checklist</h2>\n<table><thead><tr><th>Step</th><th>Action</th></tr></thead><tbody>\n<tr><td>1</td><td><code>apt install udisks2 policykit-1</code></td></tr>\n<tr><td>2</td><td><code>usermod -aG plugdev casparcg</code> + restart service / re-login</td></tr>\n<tr><td>3</td><td>Install polkit rules <strong>50</strong> and, on headless, <strong>51</strong> from <code>scripts/polkit/</code></td></tr>\n<tr><td>4</td><td><code>loginctl enable-linger casparcg</code> (recommended)</td></tr>\n<tr><td>5</td><td>Test: <code>sudo -u casparcg udisksctl mount -b /dev/sdX1</code> and check <code>MOUNTPOINT</code> in <code>lsblk</code></td></tr>\n<tr><td>6</td><td>Open HighAsCG <strong>Import from USB</strong> and refresh drives</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"9-alternative-lightweight-automount-via-udev-systemd-ubuntu-server\">9. Alternative: Lightweight Automount via Udev + Systemd (Ubuntu Server)</h2>\n<p>If you are running a headless Ubuntu Server and prefer not to use <code>udisks2</code> and Polkit, you can use a modern <code>udev</code> rule combined with <code>systemd-mount</code>. This is lighter and doesn't require an active user session.</p>\n<ol>\n<li>Create a udev rule file:</li>\n</ol>\n<pre><code class=\"language-bash\">   sudo nano /etc/udev/rules.d/99-usb-automount.rules</code></pre>\n<ol>\n<li>Add the following line:</li>\n</ol>\n<pre><code class=\"language-udev\">   ACTION==&quot;add&quot;, SUBSYSTEMS==&quot;usb&quot;, SUBSYSTEM==&quot;block&quot;, ENV{ID_FS_USAGE}==&quot;filesystem&quot;, RUN{program}+=&quot;/usr/bin/systemd-mount --no-block --automount=yes --collect $devnode&quot;</code></pre>\n<ol>\n<li>Reload rules:</li>\n</ol>\n<pre><code class=\"language-bash\">   sudo udevadm control --reload-rules\n   sudo udevadm trigger</code></pre>\n<p>This will automatically mount USB drives under <code>/run/media/</code> (or similar) when plugged in, which HighAsCG will detect.</p>\n<hr>\n<h2 id=\"10-related-files-in-this-repository\">10. Related files in this repository</h2>\n<table><thead><tr><th>File</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>scripts/polkit/50-highascg-udisks.rules</code></td><td>Base udisks2 allow for <code>plugdev</code> (active session)</td></tr>\n<tr><td><code>scripts/polkit/51-highascg-udisks-casparcg-headless.rules</code></td><td>Headless: service user + <code>plugdev</code>, no <code>active</code> requirement</td></tr>\n<tr><td><code>scripts/install-phase4.sh</code></td><td>Installs packages, <code>plugdev</code>, copies <strong>50</strong> and <strong>51</strong>, <code>USER_CASPAR</code> rewrite, <code>polkit</code> restart</td></tr>\n<tr><td><code>src/media/usb-drives.js</code></td><td>Detection, path sandbox, copy to media folder</td></tr>\n<tr><td><code>config/default.js</code> <code>usbIngest</code></td><td>App-side toggles (not the same as OS auto-mount)</td></tr>\n</tbody></table>\n<p><em>Last updated: 2026-04-22 (aligned with <code>USER_CASPAR=&quot;casparcg&quot;</code> in <code>scripts/install-config.sh</code>.)</em></p>","casparcg-linux-usb-guide":"<h1 id=\"casparcg-linux-bootable-usb-comprehensive-build-guide\">CasparCG Linux Bootable USB — Comprehensive Build Guide</h1>\n<p>A complete guide to building a bootable Ubuntu Server USB drive running CasparCG, with live boot capability, optional local installation via Calamares, and a dedicated ext4 media partition on the internal drive.</p>\n<hr>\n<h2 id=\"overview-architecture\">Overview &amp; Architecture</h2>\n<pre><code>[ USB Drive ]                    [ Internal 2TB Drive ]\n  Ubuntu Server                    Windows Partition (untouched)\n  CasparCG + services              ----------------------------\n  Penguins' Eggs + Calamares       ext4 Media Partition (1TB)\n  NVIDIA DKMS drivers              Logs, media assets, config\n        |                                     |\n        └──── boots live ──────────────────── reads/writes ──────┘</code></pre>\n<p><strong>What this guide covers:</strong></p>\n<ol>\n<li>Preparing the media partition in Windows</li>\n<li>Installing and configuring Ubuntu Server on a reference machine</li>\n<li>Installing NVIDIA drivers via DKMS</li>\n<li>Installing and configuring CasparCG with autostart</li>\n<li>Preparing the ext4 data partition for live boot use</li>\n<li>Configuring Eggs exclusions and building the ISO</li>\n<li>Flashing to USB and verifying the result</li>\n</ol>\n<hr>\n<h2 id=\"part-1-prepare-the-media-partition-in-windows\">Part 1 — Prepare the Media Partition in Windows</h2>\n<p>Do this first, before touching Linux. You only need to do it once.</p>\n<h3 id=\"11-shrink-the-windows-ntfs-partition\">1.1 Shrink the Windows NTFS partition</h3>\n<ol>\n<li>Press <code>Win + X</code> → select <strong>Disk Management</strong></li>\n<li>Right-click your 2TB NTFS partition → <strong>Shrink Volume</strong></li>\n<li>In the <em>&quot;Enter the amount of space to shrink in MB&quot;</em> field enter:</li>\n</ol>\n<pre><code>   1048576</code></pre>\n<p>(that is exactly 1TB in MB)</p>\n<ol>\n<li>Click <strong>Shrink</strong></li>\n<li>You will now see <strong>1TB of Unallocated space</strong> at the end of the disk</li>\n<li><strong>Do not format it</strong> — leave it as unallocated. Linux will handle the rest.</li>\n</ol>\n<blockquote><p>⚠️ If Windows says it cannot shrink by the full amount, it means unmovable files (hibernate file, page file) are blocking it. Disable hibernation first: ``<code> powershell (run as Administrator): powercfg /h off </code>`` Then retry the shrink.</p></blockquote>\n<hr>\n<h2 id=\"part-2-set-up-ubuntu-server-on-the-reference-machine\">Part 2 — Set Up Ubuntu Server on the Reference Machine</h2>\n<p>This is the machine you will use to build the master image. It should have the same GPU as your target machines (or at minimum the same NVIDIA GPU generation).</p>\n<h3 id=\"21-install-ubuntu-server-2204-lts\">2.1 Install Ubuntu Server 22.04 LTS</h3>\n<p>Download Ubuntu Server 22.04 LTS from ubuntu.com and install it normally. During installation:</p>\n<ul>\n<li>Choose <strong>minimal installation</strong> — no desktop environment needed</li>\n<li>Create a user account (e.g. <code>caspar</code>)</li>\n<li>Enable <strong>OpenSSH server</strong> for remote access convenience</li>\n<li>Do <strong>not</strong> install any snaps</li>\n</ul>\n<h3 id=\"22-update-the-system\">2.2 Update the system</h3>\n<pre><code class=\"language-bash\">sudo apt update &amp;&amp; sudo apt upgrade -y\nsudo apt install -y build-essential curl wget git software-properties-common</code></pre>\n<h3 id=\"23-set-up-automatic-login-on-boot-no-password-prompt\">2.3 Set up automatic login on boot (no password prompt)</h3>\n<p>CasparCG needs to start without human interaction. Configure auto-login:</p>\n<pre><code class=\"language-bash\">sudo mkdir -p /etc/systemd/system/getty@tty1.service.d/\nsudo nano /etc/systemd/system/getty@tty1.service.d/override.conf</code></pre>\n<p>Paste this content:</p>\n<pre><code class=\"language-ini\">[Service]\nExecStart=\nExecStart=-/sbin/agetty --autologin caspar --noclear %I $TERM</code></pre>\n<hr>\n<h2 id=\"part-3-install-nvidia-drivers-via-dkms\">Part 3 — Install NVIDIA Drivers via DKMS</h2>\n<p>Using DKMS ensures the driver kernel module is automatically recompiled when the image is booted on a machine with a different kernel version or minor hardware variation.</p>\n<h3 id=\"31-install-dkms-and-kernel-headers\">3.1 Install DKMS and kernel headers</h3>\n<pre><code class=\"language-bash\">sudo apt install -y dkms linux-headers-$(uname -r) linux-headers-generic</code></pre>\n<h3 id=\"32-add-the-nvidia-ppa-and-install-the-driver\">3.2 Add the NVIDIA PPA and install the driver</h3>\n<pre><code class=\"language-bash\">sudo add-apt-repository ppa:graphics-drivers/ppa -y\nsudo apt update\nsudo apt install -y nvidia-driver-550 nvidia-dkms-550</code></pre>\n<blockquote><p>Replace <code>550</code> with the current recommended driver version for your GPU. To check which version is recommended: ``<code>bash ubuntu-drivers devices </code>``</p></blockquote>\n<h3 id=\"33-verify-dkms-registration\">3.3 Verify DKMS registration</h3>\n<p>After installation, confirm the driver is registered with DKMS:</p>\n<pre><code class=\"language-bash\">dkms status</code></pre>\n<p>Expected output (version numbers will vary):</p>\n<pre><code>nvidia/550.xx.xx, 6.x.x-xx-generic, x86_64: installed</code></pre>\n<h3 id=\"34-reboot-and-verify\">3.4 Reboot and verify</h3>\n<pre><code class=\"language-bash\">sudo reboot\n# After reboot:\nnvidia-smi</code></pre>\n<p>You should see your GPU listed with driver version confirmed.</p>\n<hr>\n<h2 id=\"part-4-install-and-configure-casparcg\">Part 4 — Install and Configure CasparCG</h2>\n<h3 id=\"41-install-dependencies\">4.1 Install dependencies</h3>\n<pre><code class=\"language-bash\">sudo apt install -y \\\n  ffmpeg \\\n  fonts-liberation \\\n  libgles2 \\\n  libgles2-mesa \\\n  libx11-6 \\\n  libxrandr2 \\\n  libxinerama1 \\\n  libxi6 \\\n  libxcursor1 \\\n  openjdk-17-jre</code></pre>\n<h3 id=\"42-install-blackmagic-decklink-drivers\">4.2 Install Blackmagic DeckLink drivers</h3>\n<p>Download the Desktop Video package from the Blackmagic Design website:</p>\n<pre><code>https://www.blackmagicdesign.com/support/family/capture-and-playback</code></pre>\n<p>Install it:</p>\n<pre><code class=\"language-bash\">sudo dpkg -i desktopvideo_*.deb\nsudo apt --fix-broken install -y\nsudo reboot</code></pre>\n<p>After reboot, verify DeckLink is detected:</p>\n<pre><code class=\"language-bash\">BlackmagicFirmwareUpdater status</code></pre>\n<h3 id=\"43-download-and-install-casparcg-server\">4.3 Download and install CasparCG Server</h3>\n<pre><code class=\"language-bash\">sudo mkdir -p /home/casparcg/highascg\ncd /home/casparcg/highascg\n\n# Download latest CasparCG release (check github.com/CasparCG/server for current version)\nwget https://github.com/CasparCG/server/releases/download/v2.3.3/casparcg_server-2.3.3-Linux.tar.gz\ntar -xzf casparcg_server-*.tar.gz --strip-components=1\nsudo chown -R caspar:caspar /home/casparcg/highascg</code></pre>\n<h3 id=\"44-configure-casparcg-to-use-the-media-partition\">4.4 Configure CasparCG to use the media partition</h3>\n<p>Edit the CasparCG configuration file:</p>\n<pre><code class=\"language-bash\">nano /home/casparcg/highascg/config/casparcg.config</code></pre>\n<p>Set the media and log paths to point to the data partition mount point (which we will set up in Part 5):</p>\n<pre><code class=\"language-xml\">&lt;configuration&gt;\n  &lt;paths&gt;\n    &lt;media-path&gt;/mnt/caspar-data/media/&lt;/media-path&gt;\n    &lt;log-path&gt;/mnt/caspar-data/logs/&lt;/log-path&gt;\n    &lt;data-path&gt;/mnt/caspar-data/data/&lt;/data-path&gt;\n    &lt;template-path&gt;/mnt/caspar-data/templates/&lt;/template-path&gt;\n  &lt;/paths&gt;\n\n  &lt;!-- Your channel and consumer configuration below --&gt;\n  &lt;channels&gt;\n    &lt;channel&gt;\n      &lt;video-mode&gt;1080i5000&lt;/video-mode&gt;\n      &lt;consumers&gt;\n        &lt;decklink&gt;\n          &lt;device&gt;1&lt;/device&gt;\n          &lt;key-device&gt;2&lt;/key-device&gt;\n        &lt;/decklink&gt;\n      &lt;/consumers&gt;\n    &lt;/channel&gt;\n  &lt;/channels&gt;\n&lt;/configuration&gt;</code></pre>\n<blockquote><p>Adjust <code>video-mode</code> and DeckLink device numbers for your setup.</p></blockquote>\n<h3 id=\"45-create-required-directories-on-the-data-partition\">4.5 Create required directories on the data partition</h3>\n<p>These will be created again in Part 5 when we prepare the partition, but define them here for reference:</p>\n<pre><code>/mnt/caspar-data/\n  media/\n  logs/\n  data/\n  templates/</code></pre>\n<h3 id=\"46-create-a-systemd-service-for-casparcg-autostart\">4.6 Create a systemd service for CasparCG autostart</h3>\n<pre><code class=\"language-bash\">sudo nano /etc/systemd/system/casparcg.service</code></pre>\n<p>Paste the following:</p>\n<pre><code class=\"language-ini\">[Unit]\nDescription=CasparCG Playout Server\nAfter=network.target caspar-data.mount\nRequires=caspar-data.mount\n\n[Service]\nType=simple\nUser=caspar\nWorkingDirectory=/home/casparcg/highascg\nExecStart=/home/casparcg/highascg/run.sh\nRestart=on-failure\nRestartSec=5\nStandardOutput=journal\nStandardError=journal\n\n[Install]\nWantedBy=multi-user.target</code></pre>\n<p>Enable the service:</p>\n<pre><code class=\"language-bash\">sudo systemctl daemon-reload\nsudo systemctl enable casparcg.service</code></pre>\n<hr>\n<h2 id=\"part-5-prepare-the-ext4-data-partition\">Part 5 — Prepare the ext4 Data Partition</h2>\n<p>This is done from the Ubuntu system (either booted from USB or installed). The unallocated space created in Part 1 will now become a formatted, auto-mounting ext4 partition.</p>\n<h3 id=\"51-identify-the-disk\">5.1 Identify the disk</h3>\n<pre><code class=\"language-bash\">lsblk</code></pre>\n<p>Look for your 2TB drive — it will show the Windows NTFS partition and the unallocated space. Identify the device name (e.g. <code>/dev/sda</code>) and note the partition number of the unallocated space.</p>\n<p>Example output:</p>\n<pre><code>sda           2TB\n├─sda1        100MB   vfat    (Windows EFI)\n├─sda2        128MB           (Windows MSR)\n├─sda3        931GB   ntfs    (Windows C:)\n└─                    free    (your 1TB unallocated space)</code></pre>\n<h3 id=\"52-create-the-new-partition\">5.2 Create the new partition</h3>\n<pre><code class=\"language-bash\">sudo parted /dev/sda</code></pre>\n<p>Inside parted:</p>\n<pre><code>(parted) print free          # confirm free space location\n(parted) mkpart primary ext4 932GB 2000GB   # adjust start/end to match your free space\n(parted) print               # confirm new partition\n(parted) quit</code></pre>\n<blockquote><p>Use the exact start and end values shown by <code>print free</code> — do not guess them.</p></blockquote>\n<h3 id=\"53-format-the-new-partition-as-ext4\">5.3 Format the new partition as ext4</h3>\n<pre><code class=\"language-bash\"># Refresh partition table\nsudo partprobe /dev/sda\n\n# Format (replace sda4 with your actual new partition number)\nsudo mkfs.ext4 -L caspar-data /dev/sda4</code></pre>\n<p>The <code>-L caspar-data</code> sets a volume label — we'll use this for reliable mounting.</p>\n<h3 id=\"54-create-the-mount-point\">5.4 Create the mount point</h3>\n<pre><code class=\"language-bash\">sudo mkdir -p /mnt/caspar-data</code></pre>\n<h3 id=\"55-configure-automatic-mounting-via-etcfstab\">5.5 Configure automatic mounting via /etc/fstab</h3>\n<p>Using the label means the partition mounts correctly regardless of whether it appears as sda4, sdb4, etc. on different machines:</p>\n<pre><code class=\"language-bash\">sudo nano /etc/fstab</code></pre>\n<p>Add this line at the end:</p>\n<pre><code>LABEL=caspar-data  /mnt/caspar-data  ext4  defaults,nofail,x-systemd.automount  0  2</code></pre>\n<blockquote><p><code>nofail</code> is critical — it tells the system to boot normally even if this partition is not present (important for live USB scenarios on machines where the internal drive may be different).</p></blockquote>\n<h3 id=\"56-create-the-directory-structure-on-the-partition\">5.6 Create the directory structure on the partition</h3>\n<pre><code class=\"language-bash\">sudo mount /mnt/caspar-data\nsudo mkdir -p /mnt/caspar-data/{media,logs,data,templates}\nsudo chown -R caspar:caspar /mnt/caspar-data</code></pre>\n<h3 id=\"57-create-a-systemd-mount-unit-belt-and-suspenders\">5.7 Create a systemd mount unit (belt and suspenders)</h3>\n<p>This ensures the partition is mounted before CasparCG starts, even if fstab is slow:</p>\n<pre><code class=\"language-bash\">sudo nano /etc/systemd/system/caspar-data.mount</code></pre>\n<pre><code class=\"language-ini\">[Unit]\nDescription=CasparCG Data Partition\nBefore=casparcg.service\n\n[Mount]\nWhat=LABEL=caspar-data\nWhere=/mnt/caspar-data\nType=ext4\nOptions=defaults,nofail\n\n[Install]\nWantedBy=multi-user.target</code></pre>\n<pre><code class=\"language-bash\">sudo systemctl daemon-reload\nsudo systemctl enable caspar-data.mount</code></pre>\n<h3 id=\"58-handle-the-case-where-the-partition-is-absent-live-boot-on-unknown-hardware\">5.8 Handle the case where the partition is absent (live boot on unknown hardware)</h3>\n<p>Add a startup check script so CasparCG falls back gracefully if the data partition isn't found:</p>\n<pre><code class=\"language-bash\">sudo nano /home/casparcg/highascg/run.sh</code></pre>\n<pre><code class=\"language-bash\">#!/bin/bash\n\nMOUNT=/mnt/caspar-data\n\n# Wait up to 10 seconds for the data partition to mount\nfor i in $(seq 1 10); do\n  if mountpoint -q &quot;$MOUNT&quot;; then\n    echo &quot;Data partition mounted at $MOUNT&quot;\n    break\n  fi\n  echo &quot;Waiting for data partition... ($i/10)&quot;\n  sleep 1\ndone\n\n# If partition never mounted, use local fallback paths\nif ! mountpoint -q &quot;$MOUNT&quot;; then\n  echo &quot;WARNING: Data partition not found. Using local fallback paths.&quot;\n  mkdir -p /home/casparcg/highascg/fallback/{media,logs,data,templates}\n  export CASPAR_MEDIA=/home/casparcg/highascg/fallback/media\nelse\n  export CASPAR_MEDIA=$MOUNT/media\nfi\n\nexec /home/casparcg/highascg/casparcg</code></pre>\n<pre><code class=\"language-bash\">chmod +x /home/casparcg/highascg/run.sh</code></pre>\n<hr>\n<h2 id=\"part-6-install-penguins-eggs-and-configure-exclusions\">Part 6 — Install Penguins' Eggs and Configure Exclusions</h2>\n<h3 id=\"61-install-penguins-eggs\">6.1 Install Penguins' Eggs</h3>\n<pre><code class=\"language-bash\"># Official apt repo (PPA KEY.gpg is empty — use penguins-eggs.net instead)\ncurl -fsSL https://penguins-eggs.net/repos/KEY.asc | sudo gpg --dearmor -o /usr/share/keyrings/penguins-eggs-repos.gpg\n\necho &quot;deb [signed-by=/usr/share/keyrings/penguins-eggs-repos.gpg] https://penguins-eggs.net/repos/deb stable main&quot; | \\\n  sudo tee /etc/apt/sources.list.d/penguins-eggs-repos.list\n\nsudo apt update\nsudo apt install -y penguins-eggs</code></pre>\n<h3 id=\"62-initialise-eggs\">6.2 Initialise Eggs</h3>\n<pre><code class=\"language-bash\">sudo eggs dad -d</code></pre>\n<p>This creates the Eggs configuration directory at <code>/etc/penguins-eggs.d/</code> and sets up defaults.</p>\n<h3 id=\"63-configure-exclusions\">6.3 Configure exclusions</h3>\n<p>Eggs uses an exclusion file to skip directories when building the ISO. This is how you keep the image lean and avoid including unwanted content.</p>\n<p>Edit the exclusions file:</p>\n<pre><code class=\"language-bash\">sudo nano /etc/penguins-eggs.d/exclude.list</code></pre>\n<p>Add the following — each line is a path that will be <strong>excluded from the ISO</strong>:</p>\n<pre><code># System runtime directories (always exclude)\n/proc\n/sys\n/dev\n/run\n/tmp\n\n# Swap\n/swapfile\n*.swap\n\n# CasparCG data partition mount point\n# (this is on the internal drive, not the USB — exclude entirely)\n/mnt/caspar-data\n\n# CasparCG media, logs, and data (exclude from image)\n# These live on the data partition, not the OS\n/home/casparcg/highascg/media\n/home/casparcg/highascg/logs\n/home/casparcg/highascg/data\n/home/casparcg/highascg/fallback\n\n# Large or machine-specific files\n/var/log\n/var/cache/apt/archives\n/var/tmp\n/home/caspar/.cache\n/home/caspar/Downloads\n/root/.cache\n\n# NVIDIA compiled module cache (will recompile via DKMS on target hardware)\n/var/lib/dkms/*/build\n/usr/src/nvidia-*/*/dkms.conf.orig\n\n# Any test media you may have had locally during setup\n/home/casparcg/highascg/test-media</code></pre>\n<blockquote><p><strong>Important:</strong> The <code>/mnt/caspar-data</code> exclusion is critical. Without it, Eggs would try to include whatever is currently mounted there (your internal drive's media partition) into the ISO — potentially gigabytes of media files.</p></blockquote>\n<h3 id=\"64-verify-the-exclusions-file-is-being-picked-up\">6.4 Verify the exclusions file is being picked up</h3>\n<pre><code class=\"language-bash\">sudo eggs produce --dry-run 2&gt;&amp;1 | grep -i exclud</code></pre>\n<p>You should see your excluded paths listed in the output.</p>\n<hr>\n<h2 id=\"part-7-build-the-iso-with-penguins-eggs\">Part 7 — Build the ISO with Penguins' Eggs</h2>\n<h3 id=\"71-run-a-preflight-check\">7.1 Run a preflight check</h3>\n<pre><code class=\"language-bash\">sudo eggs dad --help\nsudo eggs status</code></pre>\n<p>Confirm everything looks correct — partition layout, user accounts, services.</p>\n<h3 id=\"72-produce-the-iso-use---clone-required-for-casparcg\">7.2 Produce the ISO (use <code>--clone</code> — required for <code>casparcg</code>)</h3>\n<p>HighAsCG and the surrounding install assume the <strong><code>casparcg</code></strong> system user: systemd units, file ownership under <code>/opt</code>, and the home tree under <code>/home/casparcg</code>. A plain <code>sudo eggs produce</code> (without <code>--clone</code>) <strong>removes all host users from the live root</strong> and replaces them with a generic <strong><code>live</code></strong> account. That is why the build log shows lines like <code>Removing user: casparcg</code> — it is normal for the default mode, but <strong>wrong for this stack</strong>.</p>\n<p>Always build with <strong><code>--clone</code></strong> so accounts and home directories from this machine are included in the squashfs. On a dedicated image-build host with generic passwords, the larger ISO and copied home trees are usually acceptable.</p>\n<pre><code class=\"language-bash\">sudo eggs produce --clone --max --basename casparcg-live</code></pre>\n<p>Optional before the build: remove accounts you do not want in the image at all (for example an extra desktop user). <strong>Do not</strong> remove <code>casparcg</code> on the build host before <code>produce</code>, or the clone will not contain that user.</p>\n<pre><code class=\"language-bash\"># Example only — removes user coolux from the SOURCE machine before imaging\n# sudo deluser coolux</code></pre>\n<p>Flags explained:</p>\n<ul>\n<li><strong><code>--clone</code></strong> — include user accounts and user data in the live filesystem (keeps <code>casparcg</code> and <code>/home/casparcg</code>)</li>\n<li><code>--max</code> — maximum compression (slower build, smaller ISO)</li>\n<li><code>--basename</code> — sets the output filename</li>\n</ul>\n<p>The build will take longer than a non-clone image and the ISO will be <strong>larger</strong> (rough guide: often <strong>~8–25+ GB</strong> depending on what is under <code>/home</code> and what your <code>exclude.list</code> still strips). The ISO will be saved to:</p>\n<pre><code>/home/eggs/</code></pre>\n<h3 id=\"73-check-the-iso-size\">7.3 Check the ISO size</h3>\n<pre><code class=\"language-bash\">ls -lh /home/eggs/*.iso</code></pre>\n<p>Without <code>--clone</code>, a lean Caspar-only root might land around <strong>3–6 GB</strong>. With <strong><code>--clone</code></strong>, expect a <strong>much larger</strong> file; that is expected.</p>\n<hr>\n<h2 id=\"part-8-flash-the-iso-to-usb\">Part 8 — Flash the ISO to USB</h2>\n<h3 id=\"on-linux-from-the-build-machine-or-any-linux-machine\">On Linux (from the build machine or any Linux machine):</h3>\n<pre><code class=\"language-bash\"># Identify your USB drive - BE CAREFUL to select the right device\nlsblk\n\n# Flash (replace sdX with your USB device - NOT a partition, the whole device)\nsudo dd if=/home/eggs/casparcg-live.iso of=/dev/sdX bs=4M status=progress oflag=sync</code></pre>\n<h3 id=\"on-windows\">On Windows:</h3>\n<p>Use <strong>Balena Etcher</strong> (free, from etcher.balena.io):</p>\n<ol>\n<li>Select the <code>.iso</code> file</li>\n<li>Select the USB drive</li>\n<li>Click Flash</li>\n</ol>\n<h3 id=\"on-macos\">On macOS:</h3>\n<pre><code class=\"language-bash\">sudo dd if=casparcg-live.iso of=/dev/rdiskX bs=4m</code></pre>\n<hr>\n<h2 id=\"part-9-boot-and-verify\">Part 9 — Boot and Verify</h2>\n<h3 id=\"91-boot-from-usb\">9.1 Boot from USB</h3>\n<ol>\n<li>Insert the USB drive into the target machine</li>\n<li>Power on and enter the BIOS boot menu (usually <code>F12</code>, <code>F8</code>, or <code>Del</code> at POST)</li>\n<li>Select the USB drive</li>\n<li>Ubuntu boots — CasparCG starts automatically via systemd</li>\n</ol>\n<h3 id=\"92-verify-services-on-first-boot\">9.2 Verify services on first boot</h3>\n<pre><code class=\"language-bash\"># Check CasparCG service status\nsystemctl status casparcg.service\n\n# Check data partition is mounted\nmountpoint /mnt/caspar-data\ndf -h /mnt/caspar-data\n\n# Check NVIDIA driver loaded correctly on this hardware\nnvidia-smi\n\n# Check DeckLink detected\nBlackmagicFirmwareUpdater status</code></pre>\n<h3 id=\"93-dkms-recompilation-first-boot-on-new-hardware\">9.3 DKMS recompilation (first boot on new hardware)</h3>\n<p>On first boot with different hardware, DKMS will automatically recompile the NVIDIA kernel module. This happens in the background and may take 1–3 minutes. You can monitor it:</p>\n<pre><code class=\"language-bash\">journalctl -f -u dkms\n# or\ndkms status</code></pre>\n<p>CasparCG's systemd service will wait for the system to be ready before starting.</p>\n<hr>\n<h2 id=\"part-10-installing-to-the-local-partition-optional\">Part 10 — Installing to the Local Partition (Optional)</h2>\n<p>If the user wants to install the system permanently to the local machine (faster than USB, no USB required to boot):</p>\n<h3 id=\"101-launch-calamares-the-graphical-installer\">10.1 Launch Calamares (the graphical installer)</h3>\n<p>Calamares is included in the Eggs-built ISO:</p>\n<pre><code class=\"language-bash\">sudo calamares</code></pre>\n<p>Or if a desktop environment is available, it may appear as an &quot;Install System&quot; icon.</p>\n<h3 id=\"102-in-the-calamares-installer\">10.2 In the Calamares installer</h3>\n<ol>\n<li>Select <strong>language and timezone</strong></li>\n<li>At the <strong>partitioning step</strong> — choose <strong>Manual partitioning</strong></li>\n<li>Select the free space or the ext4 partition you want to install the OS onto</li>\n</ol>\n<ul>\n<li><strong>Do not touch the Windows NTFS partition</strong></li>\n<li><strong>Do not touch the caspar-data ext4 partition</strong> (that's your media storage)</li>\n<li>If you have a separate unallocated area for the OS, create a new ext4 partition there and set it as <code>/</code> (root)</li>\n</ul>\n<ol>\n<li>Set the bootloader to install to the <strong>internal drive</strong> (e.g. <code>/dev/sda</code>)</li>\n<li>At the user step, align with your cloned layout (<strong><code>casparcg</code></strong> if the installer asks you to reconcile users — many clone installs keep the cloned accounts)</li>\n<li>Complete the installation</li>\n</ol>\n<h3 id=\"103-after-installation-reboot\">10.3 After installation reboot</h3>\n<p>Remove the USB drive. The machine will boot from the internal drive. CasparCG will autostart, and the caspar-data partition will mount automatically via fstab.</p>\n<hr>\n<h2 id=\"quick-reference-key-paths\">Quick Reference — Key Paths</h2>\n<table><thead><tr><th>Purpose</th><th>Path</th></tr></thead><tbody>\n<tr><td>CasparCG installation</td><td><code>/home/casparcg/highascg/</code></td></tr>\n<tr><td>CasparCG config</td><td><code>/home/casparcg/highascg/config/casparcg.config</code></td></tr>\n<tr><td>CasparCG startup script</td><td><code>/home/casparcg/highascg/run.sh</code></td></tr>\n<tr><td>Systemd service</td><td><code>/etc/systemd/system/casparcg.service</code></td></tr>\n<tr><td>Data partition mount</td><td><code>/mnt/caspar-data/</code></td></tr>\n<tr><td>Media files</td><td><code>/mnt/caspar-data/media/</code></td></tr>\n<tr><td>Logs</td><td><code>/mnt/caspar-data/logs/</code></td></tr>\n<tr><td>Templates</td><td><code>/mnt/caspar-data/templates/</code></td></tr>\n<tr><td>Eggs ISO output</td><td><code>/home/eggs/</code></td></tr>\n<tr><td>Eggs exclusions</td><td><code>/etc/penguins-eggs.d/exclude.list</code></td></tr>\n<tr><td>fstab</td><td><code>/etc/fstab</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<p><strong>CasparCG doesn't start on boot</strong></p>\n<pre><code class=\"language-bash\">journalctl -u casparcg.service -n 50</code></pre>\n<p><strong>Data partition not mounting</strong></p>\n<pre><code class=\"language-bash\">journalctl -u caspar-data.mount\nlsblk -f   # check if partition is visible and has correct label</code></pre>\n<p><strong>NVIDIA driver not loaded after boot on new hardware</strong></p>\n<pre><code class=\"language-bash\">dkms status\njournalctl -b | grep -i nvidia\n# If missing, trigger recompile manually:\nsudo dkms autoinstall</code></pre>\n<p><strong>Eggs ISO too large</strong></p>\n<pre><code class=\"language-bash\"># Check what is taking up space before building:\nsudo du -sh /* | sort -rh | head -20\n# Add any large unexpected directories to exclude.list</code></pre>\n<p><strong>Build log says <code>Removing user: casparcg</code></strong> You ran <code>eggs produce</code> <strong>without</strong> <code>--clone</code>. Cancel if still safe, then rebuild with <code>sudo eggs produce --clone ...</code> (see §7.2). Default Eggs behaviour strips host users from the ISO workspace and creates only the <code>live</code> user; that breaks a <code>casparcg</code>-centric install.</p>\n<p><strong>DeckLink not detected</strong></p>\n<pre><code class=\"language-bash\"># Check kernel module is loaded\nlsmod | grep blackmagic\n# Reload if needed\nsudo modprobe blackmagic\nBlackmagicFirmwareUpdater status</code></pre>\n<hr>\n<p><em>Guide covers Ubuntu Server 22.04 LTS, CasparCG 2.3.x, Penguins' Eggs latest, NVIDIA driver 550 series. Adjust version numbers as needed for current releases.</em></p>","CASPAR_IMAGE_VS_HIGHASCG_OVERLAY":"<h1 id=\"casparcg-only-image-vs-highascg-on-exfat-overlay-mental-model\">CasparCG-only image vs HighAsCG on exFAT (overlay mental model)</h1>\n<p><strong>Authoritative matrix</strong> (what the ISO keeps vs what lives on <strong><code>HIGHASCGEXF</code></strong>, boot order, knobs): <strong><a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">&lt;code&gt;WO47_ISO_VS_EXFAT.md&lt;/code&gt;</a></strong>.</p>\n<p>This project uses <strong>one directory</strong> on the playout machine: <strong><code>/home/casparcg/highascg</code></strong> — Caspar’s working tree <em>and</em> the Node app when present.</p>\n<h2 id=\"caspar-nodm-openbox-without-the-node-app\">Caspar + nodm + openbox without the Node app</h2>\n<p><strong>Yes.</strong> Phase 3 (<code>scripts/install-phase3.sh</code>) starts <strong>CasparCG</strong> + <strong>scanner</strong> from <strong>Openbox</strong> with <strong><code>cwd</code></strong> under <strong><code>/home/casparcg/highascg</code></strong> and <strong><code>config/casparcg.config</code></strong>. That does <strong>not</strong> require <strong><code>package.json</code></strong>.</p>\n<p>Keep on disk (or empty stubs in the squashfs): <strong><code>bin/</code></strong>, <strong><code>config/</code></strong> (at least <strong><code>casparcg.config</code></strong>), <strong><code>lib/</code></strong>, <strong><code>cef-cache/</code></strong>, <strong><code>data/</code></strong>, <strong><code>log/</code></strong>, <strong><code>media/</code></strong>, <strong><code>template/</code></strong> as your config references.</p>\n<h2 id=\"eggs-excludes-bootstrap-we-are-not-deleting-anything-at-runtime\">Eggs excludes + bootstrap (we are not deleting anything at runtime)</h2>\n<p>The <strong>penguins-eggs</strong> fragment <strong><code>tools/eggs/live-usb/penguins-eggs-exclude-highascg-fragment.list</code></strong> <strong>omits</strong> large Node trees from the <strong>squashfs snapshot</strong> only. On boot, when the stick carries <strong><code>sim/highascg/</code></strong>:</p>\n<ol>\n<li><strong><code>highascg-exfat-bootstrap.service</code></strong> — <strong><code>rsync</code></strong> seed if <strong><code>~/highascg/package.json</code></strong> is missing (protects <strong><code>config/casparcg.config</code></strong> and <strong><code>lib/</code></strong> via <strong><code>/etc/highascg/bootstrap-rsync-excludes.txt</code></strong>).</li>\n<li><strong><code>highascg-exfat-sync.service</code></strong> — node mtime sync; <strong>ConditionPathExists</strong> <strong><code>tools/runtime/exfat-sync-cli.js</code></strong> so the unit <strong>skips</strong> cleanly if the tree was never seeded (no failed oneshot).</li>\n<li><strong><code>highascg.service</code></strong> — <strong><code>ConditionPathExists=package.json</code></strong>.</li>\n</ol>\n<p>Safeguards: <strong><code>/etc/highascg/disable-exfat-bootstrap</code></strong>, <strong><code>/etc/highascg/force-exfat-bootstrap-once</code></strong>, <strong><code>HIGHASCG_BOOTSTRAP_DRY_RUN</code></strong>. See <strong><code>scripts/highascg-exfat-bootstrap.sh</code></strong>.</p>\n<p>Regression check: <strong><code>npm run smoke:wo47-manifest</code></strong>.</p>\n<h2 id=\"stick-studio\">Stick Studio</h2>\n<p><strong><code>npm run stick-studio</code></strong> — operator GUI for flash / exFAT / folder seed / portable sim (workstation).</p>\n<h2 id=\"summary\">Summary</h2>\n<table><thead><tr><th>Layer</th><th>Caspar-only OK?</th><th>Notes</th></tr></thead><tbody>\n<tr><td>nodm + openbox</td><td>Yes</td><td><code>~/.config/openbox</code></td></tr>\n<tr><td>Caspar + scanner</td><td>Yes</td><td>Needs Caspar shell under <strong><code>~/highascg</code></strong></td></tr>\n<tr><td>Bootstrap + sync</td><td>When stick present</td><td><strong><code>install-exfat-systemd-units.sh</code></strong></td></tr>\n<tr><td><strong><code>highascg.service</code></strong></td><td>If <strong><code>package.json</code></strong> exists</td><td>Skipped otherwise</td></tr>\n</tbody></table>","wiki--api--README":"<h1 id=\"highascg-http-api\">HighAsCG HTTP API</h1>\n<p>HighAsCG exposes a <strong>JSON REST API</strong> on the playout server. The operator UI (served from <strong><code>dist-web/</code></strong> on the same host at <code>:4200</code>) and Companion modules call these endpoints over HTTP; live state also flows over <strong>WebSocket</strong> on the same port.</p>\n<h2 id=\"base-url\">Base URL</h2>\n<pre><code class=\"language-text\">http://&lt;playout-host&gt;:4200/api/...</code></pre>\n<table><thead><tr><th>Setting</th><th>Default</th><th>Override</th></tr></thead><tbody>\n<tr><td>HTTP port</td><td><code>4200</code></td><td><code>HTTP_PORT</code>, <code>PORT</code>, or <code>node index.js --port</code></td></tr>\n<tr><td>Bind</td><td><code>0.0.0.0</code></td><td><code>BIND_ADDRESS</code>, <code>--bind</code></td></tr>\n<tr><td>Caspar AMCP</td><td><code>127.0.0.1:5250</code></td><td><code>CASPAR_HOST</code>, <code>CASPAR_PORT</code></td></tr>\n</tbody></table>\n<h3 id=\"instance-prefix\">Instance prefix</h3>\n<p>When the server is mounted under a path prefix (multi-instance dev), routes also work as:</p>\n<pre><code class=\"language-text\">http://host:4200/instance/&lt;id&gt;/api/...</code></pre>\n<p>The router strips <code>/instance/&lt;id&gt;/</code> before dispatch.</p>\n<h2 id=\"request-format\">Request format</h2>\n<table><thead><tr><th>Header</th><th>Value</th></tr></thead><tbody>\n<tr><td><code>Content-Type</code></td><td><code>application/json</code> for POST/PUT bodies</td></tr>\n<tr><td>Body</td><td>JSON object (empty <code>{}</code> allowed)</td></tr>\n</tbody></table>\n<p>Query parameters are used on some GET routes (e.g. mixer reads, thumbnails).</p>\n<h2 id=\"response-format\">Response format</h2>\n<p>Successful responses use <strong><code>application/json</code></strong>. Typical shapes:</p>\n<pre><code class=\"language-json\">{ &quot;ok&quot;: true }</code></pre>\n<pre><code class=\"language-json\">{ &quot;data&quot;: &quot;…&quot;, &quot;playbackMatrix&quot;: { } }</code></pre>\n<p>Errors:</p>\n<pre><code class=\"language-json\">{ &quot;error&quot;: &quot;human-readable message&quot; }</code></pre>\n<table><thead><tr><th>Status</th><th>Meaning</th></tr></thead><tbody>\n<tr><td><code>200</code></td><td>Success</td></tr>\n<tr><td><code>400</code></td><td>Bad request / validation</td></tr>\n<tr><td><code>404</code></td><td>Unknown route or resource</td></tr>\n<tr><td><code>410</code></td><td>Removed API (e.g. legacy <code>/api/data/*</code>)</td></tr>\n<tr><td><code>502</code></td><td>Upstream/Caspar command failed</td></tr>\n<tr><td><code>503</code></td><td><strong>Caspar not connected</strong> (see below)</td></tr>\n</tbody></table>\n<h2 id=\"caspar-connection-gate\">Caspar connection gate</h2>\n<p>Most playout routes require an active AMCP connection to CasparCG. If Caspar is down or the server was started with <code>--no-caspar</code>, those routes return:</p>\n<pre><code class=\"language-http\">HTTP/1.1 503 Service Unavailable</code></pre>\n<pre><code class=\"language-json\">{ &quot;error&quot;: &quot;Caspar not connected&quot; }</code></pre>\n<p><strong>Works without Caspar</strong> (non-exhaustive): settings, hardware displays, audio device list, streams status, streaming toggle, OSC, ingest/USB, project save/load on disk, logs, host stats, exFAT sync, system/GPU helpers, optional modules that do not call AMCP.</p>\n<p>Implementation: <a href=\"../../src/api/router.js\">&lt;code&gt;src/api/router.js&lt;/code&gt;</a> — routes registered before the gate are documented per page with <strong>Caspar: optional</strong> where applicable.</p>\n<h2 id=\"websocket\">WebSocket</h2>\n<p>Same host/port as HTTP (default <strong>4200</strong>). Clients subscribe for:</p>\n<ul>\n<li>Aggregated <strong>state</strong> (channels, playback, OSC, etc.)</li>\n<li><strong>Scene live</strong> updates</li>\n<li>Optional periodic broadcast (<code>HIGHASCG_WS_BROADCAST_MS</code>)</li>\n</ul>\n<p>WebSocket message shapes are not duplicated here; see <a href=\"#/osc-integration\" data-wiki-page=\"1\">osc-integration.md</a> and client <code>lib/ws-client.js</code> in this repo.</p>\n<h2 id=\"openapi-file\">OpenAPI file</h2>\n<p><a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a> lists all paths for tooling; <strong>scene take</strong>, <strong>playback</strong>, <strong>timelines</strong>, <strong>mixer</strong>, <strong>CG</strong>, <strong>project</strong>, and <strong>state/media</strong> routes include schemas and examples. Narrative docs with <strong>curl</strong> and <strong>fetch</strong> examples live in the topic pages linked from <a href=\"#/wiki--README\" data-wiki-page=\"1\">../README.md</a>.</p>\n<table><thead><tr><th>Endpoint group</th><th>Detailed doc</th></tr></thead><tbody>\n<tr><td>Scene take</td><td><a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">scene-take.md</a></td></tr>\n<tr><td>Playback</td><td><a href=\"#/wiki--api--playback\" data-wiki-page=\"1\">playback.md</a></td></tr>\n<tr><td>Timelines</td><td><a href=\"#/wiki--api--timelines\" data-wiki-page=\"1\">timelines.md</a></td></tr>\n<tr><td>Mixer</td><td><a href=\"#/wiki--api--mixer\" data-wiki-page=\"1\">mixer.md</a></td></tr>\n<tr><td>CG</td><td><a href=\"#/wiki--api--cg\" data-wiki-page=\"1\">cg.md</a></td></tr>\n<tr><td>Project</td><td><a href=\"#/wiki--api--project\" data-wiki-page=\"1\">project.md</a></td></tr>\n<tr><td>State &amp; media</td><td><a href=\"#/wiki--api--state-and-media\" data-wiki-page=\"1\">state-and-media.md</a></td></tr>\n<tr><td>Tailscale</td><td><a href=\"#/wiki--api--network-tailscale\" data-wiki-page=\"1\">network-tailscale.md</a></td></tr>\n</tbody></table>\n<h2 id=\"quick-example\">Quick example</h2>\n<pre><code class=\"language-bash\"># Server health / config snapshot (needs Caspar for full channel list)\ncurl -s http://127.0.0.1:4200/api/state | jq .\n\n# Settings (no Caspar required)\ncurl -s http://127.0.0.1:4200/api/settings | jq .\n\n# Play a clip on channel 1, layer 10\ncurl -s -X POST http://127.0.0.1:4200/api/play \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:10,&quot;clip&quot;:&quot;AMB&quot;,&quot;loop&quot;:true}'</code></pre>\n<h2 id=\"source-of-truth\">Source of truth</h2>\n<p>Route dispatch: <a href=\"../../src/api/router.js\">&lt;code&gt;src/api/router.js&lt;/code&gt;</a>. Per-area handlers: <code>src/api/routes-*.js</code>. Optional modules register extra prefixes via <a href=\"../../src/module-registry.js\">&lt;code&gt;src/module-registry.js&lt;/code&gt;</a>.</p>","wiki--api--scene-take":"<h1 id=\"post-apiscenetake-scene-look-take\"><code>POST /api/scene/take</code> — scene / look take</h1>\n<p>Takes a <strong>look</strong> (scene JSON with layers) to <strong>program</strong> or <strong>preview</strong> on Caspar, using the LBG pipeline (<code>runSceneTakeLbg</code>). This is the main air chain used by the scenes editor and Companion.</p>\n<p><strong>Caspar:</strong> required (<code>503</code> if AMCP is down).</p>\n<p><strong>Implementation:</strong> <a href=\"../../src/api/routes-scene.js\">&lt;code&gt;src/api/routes-scene.js&lt;/code&gt;</a> · AMCP order: <a href=\"#/reference--amcp-pgm-look-take-pipeline\" data-wiki-page=\"1\">&lt;code&gt;docs/reference/amcp-pgm-look-take-pipeline.md&lt;/code&gt;</a></p>\n<hr>\n<h2 id=\"endpoint\">Endpoint</h2>\n<pre><code class=\"language-http\">POST /api/scene/take\nContent-Type: application/json</code></pre>\n<p>Default base: <code>http://127.0.0.1:4200/api/scene/take</code></p>\n<hr>\n<h2 id=\"behaviour-summary\">Behaviour summary</h2>\n<table><thead><tr><th>Mode</th><th>How to request</th><th>What happens</th></tr></thead><tbody>\n<tr><td><strong>Program take</strong></td><td><code>channel</code> = PGM (e.g. <code>1</code>), omit <code>target</code></td><td>Incoming look airs on PGM; previous PGM look moves to PRV (when routing has a preview bus)</td></tr>\n<tr><td><strong>Preview-only</strong></td><td><code>target</code>: <code>&quot;preview&quot;</code>, <code>&quot;prv&quot;</code>, or <code>&quot;bus1&quot;</code></td><td>Incoming look built on PRV only (hard cut); PGM unchanged</td></tr>\n<tr><td><strong>Direct program</strong></td><td>PGM channel not in <code>programChannels</code> map</td><td>Take runs on requested <code>channel</code> only (no PGM↔PRV exchange)</td></tr>\n<tr><td><strong>Timeline-only look</strong></td><td>Every content layer has <code>source.type === &quot;timeline&quot;</code></td><td>Delegates to timeline engine (not LBG bank swap)</td></tr>\n</tbody></table>\n<p>Takes on the <strong>same <code>channel</code> are serialized</strong> (<code>_sceneTakeChainByChannel</code>) so rapid UI clicks queue instead of racing AMCP.</p>\n<p><strong>Timeout:</strong> 120 seconds → <code>504</code> with <code>{ &quot;error&quot;: &quot;Scene take timed out&quot; }</code>.</p>\n<p>On success, server updates <strong>live scene state</strong>, broadcasts WebSocket <code>sceneLive</code>, and returns <strong><code>playbackMatrix</code></strong>.</p>\n<hr>\n<h2 id=\"request-body\">Request body</h2>\n<h3 id=\"required\">Required</h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>channel</code></td><td>number</td><td>Caspar <strong>program</strong> channel for this take (e.g. <code>1</code>). Must be ≥ 1.</td></tr>\n<tr><td><code>incomingScene</code></td><td>object</td><td>Full look/scene JSON, including <strong><code>layers</code> array</strong> with at least one layer that has content.</td></tr>\n</tbody></table>\n<p>A layer has content when <code>layer.source.value</code> is non-empty (<code>layerHasContent</code>).</p>\n<h3 id=\"incomingscene-shape\"><code>incomingScene</code> shape</h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>id</code></td><td>string</td><td>Look/scene id (stored in live state after take)</td></tr>\n<tr><td><code>name</code></td><td>string</td><td>Display name (logging)</td></tr>\n<tr><td><code>mainScope</code></td><td>string</td><td>Screen scope (<code>&quot;0&quot;</code>, <code>&quot;1&quot;</code>, <code>&quot;all&quot;</code>, …)</td></tr>\n<tr><td><code>layers</code></td><td>array</td><td>Layer objects (see below)</td></tr>\n<tr><td><code>defaultTransition</code></td><td>object</td><td>Optional global transition when per-layer transition omitted</td></tr>\n</tbody></table>\n<h3 id=\"layer-object-typical\">Layer object (typical)</h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>layerNumber</code></td><td>number</td><td>Logical layer (e.g. <code>10</code> → Caspar <code>1-10</code> / <code>1-110</code> bank pair)</td></tr>\n<tr><td><code>source</code></td><td>object</td><td>`{ &quot;type&quot;: &quot;media&quot; \\</td><td>&quot;route&quot; \\</td><td>&quot;timeline&quot; \\</td><td>…, &quot;value&quot;: &quot;clip.mov&quot; \\</td><td>route string \\</td><td>timeline id }`</td></tr>\n<tr><td><code>transition</code></td><td>object</td><td>Optional <code>{ &quot;type&quot;: &quot;MIX&quot;, &quot;duration&quot;: 25, &quot;tween&quot;: &quot;linear&quot; }</code> (frames at channel fps)</td></tr>\n<tr><td><code>loop</code></td><td>boolean</td><td>Loop media (when applicable)</td></tr>\n<tr><td><code>opacity</code>, <code>fill</code>, …</td><td>various</td><td>Mixer/geometry — passed through to AMCP pipeline</td></tr>\n<tr><td><code>playSeekFrames</code></td><td>number</td><td>Optional seek frame for timeline sync (stripped from persisted live state)</td></tr>\n</tbody></table>\n<p><strong>Alternative to full <code>incomingScene</code>:</strong> provide <code>sceneId</code> (or <code>lookId</code> / <code>incomingSceneId</code>) and the server resolves the scene from the loaded project:</p>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;sceneId&quot;: &quot;sc_1778329614472_gbdse4w&quot;\n}</code></pre>\n<p>If the id is missing from the project, you still get <code>incomingScene object required</code>.</p>\n<h3 id=\"optional-control-fields\">Optional control fields</h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>target</code> / <code>bus</code></td><td>string</td><td>—</td><td><code>&quot;preview&quot;</code>, <code>&quot;prv&quot;</code>, or <code>&quot;bus1&quot;</code> → preview-only path</td></tr>\n<tr><td><code>forceCut</code></td><td>boolean</td><td><code>false</code></td><td>Skip crossfade; cut take</td></tr>\n<tr><td><code>framerate</code></td><td>number</td><td>from Caspar INFO</td><td>Override fps for mixer tween duration</td></tr>\n<tr><td><code>useServerLive</code></td><td>boolean</td><td><code>true</code></td><td>When <code>false</code> and <code>currentScene</code> sent, use client’s <code>currentScene</code> instead of server live JSON</td></tr>\n<tr><td><code>currentScene</code></td><td>object</td><td>server live</td><td>Outgoing look (with <code>useServerLive: false</code>)</td></tr>\n<tr><td><code>stageOnPreview</code></td><td>boolean</td><td><code>true</code></td><td>PGM/PRV: stage incoming on PRV before PGM mix</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"response\">Response</h2>\n<h3 id=\"200-ok\"><code>200 OK</code></h3>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;sceneLive&quot;: {\n    &quot;1&quot;: { &quot;sceneId&quot;: &quot;sc_abc&quot;, &quot;scene&quot;: { &quot;id&quot;: &quot;sc_abc&quot;, &quot;name&quot;: &quot;Look 1&quot;, &quot;layers&quot;: [] } },\n    &quot;2&quot;: { &quot;sceneId&quot;: &quot;sc_prev&quot;, &quot;scene&quot;: { } }\n  },\n  &quot;playbackMatrix&quot;: { }\n}</code></pre>\n<table><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>ok</code></td><td>Always <code>true</code> on success</td></tr>\n<tr><td><code>sceneLive</code></td><td>Per-channel live look ids + stripped scene JSON (no ephemeral <code>playSeekFrames</code>)</td></tr>\n<tr><td><code>playbackMatrix</code></td><td>Playback tracker snapshot (same family as <code>/api/play</code> responses)</td></tr>\n</tbody></table>\n<p>Clients should merge <code>sceneLive</code> into UI state; WebSocket also emits scene live updates.</p>\n<h3 id=\"error-responses\">Error responses</h3>\n<table><thead><tr><th>Status</th><th>Condition</th><th>Example body</th></tr></thead><tbody>\n<tr><td><code>400</code></td><td>Missing/invalid <code>channel</code></td><td><code>{ &quot;error&quot;: &quot;channel required&quot; }</code></td></tr>\n<tr><td><code>400</code></td><td>No <code>incomingScene</code></td><td><code>{ &quot;error&quot;: &quot;incomingScene object required …&quot; }</code></td></tr>\n<tr><td><code>400</code></td><td><code>layers</code> not an array</td><td><code>{ &quot;error&quot;: &quot;incomingScene.layers must be an array&quot; }</code></td></tr>\n<tr><td><code>400</code></td><td>No layer with <code>source.value</code></td><td><code>{ &quot;error&quot;: &quot;incomingScene has no layers with sources …&quot; }</code></td></tr>\n<tr><td><code>503</code></td><td>Caspar not connected</td><td><code>{ &quot;error&quot;: &quot;Caspar not connected&quot; }</code></td></tr>\n<tr><td><code>500</code></td><td>AMCP/engine failure</td><td><code>{ &quot;error&quot;: &quot;…&quot; }</code></td></tr>\n<tr><td><code>504</code></td><td>Exceeded 120s</td><td><code>{ &quot;error&quot;: &quot;Scene take timed out&quot; }</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"examples\">Examples</h2>\n<h3 id=\"minimal-program-take-cut\">Minimal program take (cut)</h3>\n<p>From <a href=\"../../tools/smoke/smoke-scene-take.js\">&lt;code&gt;tools/smoke/smoke-scene-take.js&lt;/code&gt;</a>:</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/scene/take \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    &quot;channel&quot;: 1,\n    &quot;forceCut&quot;: true,\n    &quot;incomingScene&quot;: {\n      &quot;id&quot;: &quot;sc_1778329614472_gbdse4w&quot;,\n      &quot;name&quot;: &quot;Look 1&quot;,\n      &quot;layers&quot;: [\n        {\n          &quot;layerNumber&quot;: 10,\n          &quot;source&quot;: { &quot;type&quot;: &quot;media&quot;, &quot;value&quot;: &quot;led-grid-3840x1024.png&quot; }\n        }\n      ]\n    }\n  }'</code></pre>\n<h3 id=\"program-take-with-crossfade\">Program take with crossfade</h3>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/scene/take \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    &quot;channel&quot;: 1,\n    &quot;framerate&quot;: 50,\n    &quot;incomingScene&quot;: {\n      &quot;id&quot;: &quot;look_b&quot;,\n      &quot;name&quot;: &quot;Look B&quot;,\n      &quot;defaultTransition&quot;: { &quot;type&quot;: &quot;MIX&quot;, &quot;duration&quot;: 25, &quot;tween&quot;: &quot;easeInOutCubic&quot; },\n      &quot;layers&quot;: [\n        {\n          &quot;layerNumber&quot;: 10,\n          &quot;source&quot;: { &quot;type&quot;: &quot;media&quot;, &quot;value&quot;: &quot;clip.mov&quot; },\n          &quot;transition&quot;: { &quot;type&quot;: &quot;MIX&quot;, &quot;duration&quot;: 25, &quot;tween&quot;: &quot;linear&quot; }\n        }\n      ]\n    }\n  }'</code></pre>\n<h3 id=\"preview-bus-only-stage-on-prv\">Preview bus only (stage on PRV)</h3>\n<p>Uses routing: <code>channel</code> = PGM (<code>1</code>), preview on <code>switcherBus1Channels[0]</code> (often <code>2</code>).</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/scene/take \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    &quot;channel&quot;: 1,\n    &quot;target&quot;: &quot;preview&quot;,\n    &quot;incomingScene&quot;: {\n      &quot;id&quot;: &quot;look_prv&quot;,\n      &quot;layers&quot;: [\n        { &quot;layerNumber&quot;: 10, &quot;source&quot;: { &quot;type&quot;: &quot;media&quot;, &quot;value&quot;: &quot;preview-still.png&quot; } }\n      ]\n    }\n  }'</code></pre>\n<h3 id=\"javascript-operator-client\">JavaScript (operator client)</h3>\n<pre><code class=\"language-javascript\">const res = await fetch(`${apiBase}/api/scene/take`, {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({\n    channel: programChannel,\n    incomingScene: sceneFromEditor,\n    forceCut: false,\n    framerate: programFps,\n  }),\n})\nconst data = await res.json()\nif (!res.ok) throw new Error(data.error || res.statusText)\n// data.sceneLive, data.playbackMatrix</code></pre>\n<hr>\n<h2 id=\"routing-pgm-prv\">Routing (PGM / PRV)</h2>\n<p>Channel numbers come from persisted config (<code>channel_map</code> / routing). Typical 2-main layout:</p>\n<table><thead><tr><th>Role</th><th>Config field</th><th>Example Caspar ch</th></tr></thead><tbody>\n<tr><td>PGM screen 1</td><td><code>programChannels[0]</code> or <code>programCh(1)</code></td><td><code>1</code></td></tr>\n<tr><td>PRV screen 1</td><td><code>switcherBus1Channels[0]</code> or <code>previewChannels[0]</code></td><td><code>2</code></td></tr>\n</tbody></table>\n<p><code>POST /api/scene/take</code> always passes <strong><code>channel</code> = PGM</strong>. Preview path uses the mapped PRV channel internally.</p>\n<p>Probe routing + live state:</p>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/settings | jq '.channel_map // .routing'\ncurl -s http://127.0.0.1:4200/api/scene/live | jq .</code></pre>\n<hr>\n<h2 id=\"related-scene-endpoints\">Related scene endpoints</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/scene/live</code></td><td>Current live scene per channel (no take)</td></tr>\n<tr><td>POST</td><td><code>/api/scene/border-lines</code></td><td>Returns AMCP lines for global border CG (layer 998)</td></tr>\n<tr><td>POST</td><td><code>/api/scene/border-preset-crossfade</code></td><td>Border crossfade between preset layers</td></tr>\n</tbody></table>\n<p>See <a href=\"#/wiki--api--mixer-cg-scene\" data-wiki-page=\"1\">mixer-cg-scene.md</a> for FTB, LED test card, PiP.</p>\n<hr>\n<h2 id=\"openapi\">OpenAPI</h2>\n<p>Machine-readable schema for this operation: <a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a> → path <code>/api/scene/take</code>.</p>","wiki--api--playback":"<h1 id=\"playback-api-play-load-loadbg-transport\">Playback API — PLAY, LOAD, LOADBG, transport</h1>\n<p>Layer-level CasparCG AMCP commands exposed as JSON POST routes. These are the <strong>direct</strong> playout controls (single channel/layer). Full <strong>look takes</strong> use <a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">&lt;code&gt;POST /api/scene/take&lt;/code&gt;</a> instead.</p>\n<p><strong>Caspar:</strong> required (<code>503</code> if AMCP is down).</p>\n<p><strong>Implementation:</strong> <a href=\"../../src/api/routes-amcp.js\">&lt;code&gt;src/api/routes-amcp.js&lt;/code&gt;</a> · AMCP client: <a href=\"../../src/caspar/amcp-basic.js\">&lt;code&gt;src/caspar/amcp-basic.js&lt;/code&gt;</a> · Command builder: <a href=\"../../src/caspar/amcp-command-plan.js\">&lt;code&gt;src/caspar/amcp-command-plan.js&lt;/code&gt;</a></p>\n<p><strong>AMCP mapping table:</strong> <a href=\"#/reference--amcp-mapping\" data-wiki-page=\"1\">&lt;code&gt;docs/reference/amcp-mapping.md&lt;/code&gt;</a></p>\n<p>For batched/raw AMCP, logging, restart, and diagnostics see <a href=\"#/wiki--api--playback-and-amcp#amcp-bridge\" data-wiki-page=\"1\">playback-and-amcp.md</a>.</p>\n<hr>\n<h2 id=\"base-url\">Base URL</h2>\n<pre><code class=\"language-http\">POST http://127.0.0.1:4200/api/&lt;endpoint&gt;\nContent-Type: application/json</code></pre>\n<hr>\n<h2 id=\"common-request-fields\">Common request fields</h2>\n<p>Most layer commands accept:</p>\n<table><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>channel</code></td><td>number</td><td><code>1</code></td><td>Caspar channel</td></tr>\n<tr><td><code>layer</code></td><td>number</td><td>—</td><td>Caspar layer (<code>channel-layer</code>, e.g. <code>10</code> → <code>1-10</code>). Omit only where AMCP allows channel-only (e.g. <code>PRINT</code>)</td></tr>\n</tbody></table>\n<p>Clip commands (<code>play</code>, <code>load</code>, <code>loadbg</code>) add:</p>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>clip</code></td><td>string</td><td>Media path, <code>route://…</code>, <code>ndi://…</code>, <code>[HTML] …</code>, or URL (see <strong>Clip strings</strong>)</td></tr>\n<tr><td><code>loop</code></td><td>boolean</td><td>Append <code>LOOP</code> to AMCP</td></tr>\n<tr><td><code>transition</code></td><td>string</td><td><code>MIX</code>, <code>PUSH</code>, <code>WIPE</code>, <code>SLIDE</code>, <code>STING</code>, … (<code>CUT</code> omitted on wire)</td></tr>\n<tr><td><code>duration</code></td><td>number</td><td>Transition length in <strong>frames</strong></td></tr>\n<tr><td><code>tween</code></td><td>string</td><td>Easing name (e.g. <code>linear</code>, <code>easeInOutCubic</code>)</td></tr>\n<tr><td><code>auto</code></td><td>boolean</td><td><code>AUTO</code> — background plays when foreground ends (<code>loadbg</code> / <code>play</code>)</td></tr>\n<tr><td><code>parameters</code></td><td>string</td><td>Extra AMCP tokens (STING template path, etc.)</td></tr>\n<tr><td><code>audioFilter</code></td><td>string</td><td>Raw FFmpeg <code>AF</code> filter string</td></tr>\n<tr><td><code>audioRoute</code></td><td>string</td><td>Shorthand: <code>1+2</code> (default), <code>3+4</code>, <code>5+6</code>, … → mapped to <code>AF</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"responses\">Responses</h2>\n<h3 id=\"with-playbackmatrix\">With <code>playbackMatrix</code></h3>\n<p><code>POST /api/play</code>, <code>/api/stop</code>, <code>/api/clear</code>, and <code>/api/amcp/batch</code> return the AMCP result object <strong>plus</strong> a server-side tracker snapshot:</p>\n<pre><code class=\"language-json\">{\n  &quot;data&quot;: &quot;…&quot;,\n  &quot;playbackMatrix&quot;: {\n    &quot;1-10&quot;: {\n      &quot;channel&quot;: 1,\n      &quot;layer&quot;: 10,\n      &quot;clip&quot;: &quot;AMB&quot;,\n      &quot;startedAt&quot;: 1717512345678,\n      &quot;durationMs&quot;: 120000,\n      &quot;playing&quot;: true,\n      &quot;loop&quot;: false,\n      &quot;isRoute&quot;: false\n    }\n  }\n}</code></pre>\n<p>The tracker updates on <strong>play</strong> (records clip) and <strong>stop/clear</strong> (removes layer). OSC mode can also feed the matrix; see <a href=\"../../src/state/playback-tracker.js\">&lt;code&gt;src/state/playback-tracker.js&lt;/code&gt;</a>.</p>\n<p>WebSocket clients receive <code>playback.matrix</code> state changes when the matrix updates.</p>\n<h3 id=\"without-playbackmatrix\">Without <code>playbackMatrix</code></h3>\n<p><code>POST /api/load</code>, <code>/api/loadbg</code>, <code>/api/pause</code>, <code>/api/resume</code>, <code>/api/call</code>, <code>/api/swap</code>, <code>/api/add</code>, <code>/api/remove</code> return the raw AMCP JSON only (no matrix merge).</p>\n<h3 id=\"errors\">Errors</h3>\n<table><thead><tr><th>Status</th><th>When</th></tr></thead><tbody>\n<tr><td><code>400</code></td><td>Missing <code>commands</code> on batch routes, missing <code>cmd</code> on <code>/api/raw</code>, etc.</td></tr>\n<tr><td><code>502</code></td><td>Caspar rejected the command (upstream failure)</td></tr>\n<tr><td><code>503</code></td><td>Caspar not connected</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"side-effects\">Side effects</h2>\n<table><thead><tr><th>Endpoint</th><th>Playback tracker</th><th>Live scene broadcast</th></tr></thead><tbody>\n<tr><td><code>play</code></td><td>Records layer</td><td>May invalidate/broadcast if channel is a <strong>program</strong> channel</td></tr>\n<tr><td><code>stop</code>, <code>clear</code></td><td>Clears layer</td><td>Same</td></tr>\n<tr><td><code>load</code>, <code>loadbg</code></td><td>—</td><td>Same</td></tr>\n<tr><td>Others</td><td>—</td><td>—</td></tr>\n</tbody></table>\n<p>Direct AMCP on a program channel can desync UI “live look” state from Caspar; the server broadcasts <code>sceneLive</code> when routing marks the channel as program.</p>\n<hr>\n<h2 id=\"post-apiplay\"><code>POST /api/play</code></h2>\n<p>Maps to <strong><code>PLAY &lt;channel&gt;-&lt;layer&gt; &lt;clip&gt; …</code></strong>.</p>\n<h3 id=\"request\">Request</h3>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;clip&quot;: &quot;AMB&quot;,\n  &quot;loop&quot;: true,\n  &quot;transition&quot;: &quot;MIX&quot;,\n  &quot;duration&quot;: 25,\n  &quot;tween&quot;: &quot;linear&quot;\n}</code></pre>\n<p><strong>Empty clip:</strong> <code>PLAY</code> with no clip string is valid (resume / transition on existing layer) — omit <code>clip</code> or use <code>&quot;&quot;</code>.</p>\n<h3 id=\"example\">Example</h3>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/play \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:10,&quot;clip&quot;:&quot;AMB&quot;,&quot;loop&quot;:true}'</code></pre>\n<pre><code class=\"language-javascript\">const res = await fetch(`${apiBase}/api/play`, {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({ channel: 1, layer: 10, clip: 'AMB', loop: true }),\n})\nconst data = await res.json()\n// data.playbackMatrix</code></pre>\n<h3 id=\"amcp-examples-generated\">AMCP examples (generated)</h3>\n<table><thead><tr><th>Body</th><th>Approximate AMCP</th></tr></thead><tbody>\n<tr><td><code>{ &quot;channel&quot;:1, &quot;layer&quot;:10, &quot;clip&quot;:&quot;AMB&quot;, &quot;loop&quot;:true }</code></td><td><code>PLAY 1-10 AMB LOOP</code></td></tr>\n<tr><td><code>{ &quot;channel&quot;:1, &quot;layer&quot;:10, &quot;clip&quot;:&quot;clip.mov&quot;, &quot;transition&quot;:&quot;MIX&quot;, &quot;duration&quot;:25, &quot;tween&quot;:&quot;linear&quot; }</code></td><td><code>PLAY 1-10 clip.mov MIX 25 linear</code></td></tr>\n<tr><td><code>{ &quot;channel&quot;:1, &quot;layer&quot;:10, &quot;clip&quot;:&quot;[HTML] http://host/page&quot;, &quot;audioRoute&quot;:&quot;3+4&quot; }</code></td><td><code>PLAY 1-10 [HTML] &quot;http://host/page&quot; AF …</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"post-apiload\"><code>POST /api/load</code></h2>\n<p>Maps to <strong><code>LOAD</code></strong> — loads into <strong>foreground</strong> without necessarily starting playback (Caspar semantics).</p>\n<p>Same clip/transition/audio fields as <strong>play</strong>, except <strong><code>auto</code></strong> is not passed from the HTTP handler.</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/load \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:10,&quot;clip&quot;:&quot;still.png&quot;,&quot;loop&quot;:true}'</code></pre>\n<p>Typical workflow: <code>load</code> or <code>loadbg</code> → <code>play</code> or <code>call</code> to air.</p>\n<hr>\n<h2 id=\"post-apiloadbg\"><code>POST /api/loadbg</code></h2>\n<p>Maps to <strong><code>LOADBG</code></strong> — loads into <strong>background</strong> stack for later <code>CALL</code> or mixed <code>PLAY</code>.</p>\n<p>Supports <strong><code>auto</code></strong> (background auto-starts when foreground ends).</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/loadbg \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    &quot;channel&quot;: 1,\n    &quot;layer&quot;: 20,\n    &quot;clip&quot;: &quot;next.mov&quot;,\n    &quot;transition&quot;: &quot;MIX&quot;,\n    &quot;duration&quot;: 25,\n    &quot;tween&quot;: &quot;easeInOutCubic&quot;,\n    &quot;auto&quot;: true\n  }'</code></pre>\n<p>Then take background to air:</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/call \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:20}'</code></pre>\n<p>Or crossfade with play on the same layer after loadbg.</p>\n<hr>\n<h2 id=\"post-apipause-post-apiresume\"><code>POST /api/pause</code> · <code>POST /api/resume</code></h2>\n<p>Maps to <strong><code>PAUSE</code></strong> / <strong><code>RESUME</code></strong>.</p>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10 }</code></pre>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/pause \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:10}'</code></pre>\n<hr>\n<h2 id=\"post-apistop-post-apiclear\"><code>POST /api/stop</code> · <code>POST /api/clear</code></h2>\n<p>Maps to <strong><code>STOP</code></strong> / <strong><code>CLEAR</code></strong>.</p>\n<p>Both update <strong><code>playbackMatrix</code></strong> (layer removed from tracker).</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/stop \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:10}'</code></pre>\n<p><code>CLEAR</code> removes producers and mixer state on the layer (stronger than stop).</p>\n<hr>\n<h2 id=\"post-apicall\"><code>POST /api/call</code></h2>\n<p>Maps to <strong><code>CALL &lt;channel&gt;-&lt;layer&gt; [function] [params]</code></strong> — brings background to foreground (Caspar CALL semantics).</p>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>fn</code></td><td>string</td><td>Optional CALL function name</td></tr>\n<tr><td><code>params</code></td><td>string</td><td>Optional parameter string appended to command</td></tr>\n</tbody></table>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/call \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:20}'</code></pre>\n<hr>\n<h2 id=\"post-apiswap\"><code>POST /api/swap</code></h2>\n<p>Maps to <strong><code>SWAP &lt;ch1&gt;-&lt;layer1&gt; &lt;ch2&gt;-&lt;layer2&gt; [TRANSFORMS]</code></strong>.</p>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>channel</code></td><td>number</td><td>First channel (default <code>1</code>)</td></tr>\n<tr><td><code>layer</code></td><td>number</td><td>First layer</td></tr>\n<tr><td><code>channel2</code></td><td>number</td><td>Second channel</td></tr>\n<tr><td><code>layer2</code></td><td>number</td><td>Second layer</td></tr>\n<tr><td><code>transforms</code></td><td>boolean</td><td>Append <code>TRANSFORMS</code> when true</td></tr>\n</tbody></table>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;channel2&quot;: 1,\n  &quot;layer2&quot;: 20,\n  &quot;transforms&quot;: true\n}</code></pre>\n<hr>\n<h2 id=\"post-apiadd-post-apiremove\"><code>POST /api/add</code> · <code>POST /api/remove</code></h2>\n<p>Channel <strong>consumer</strong> producers (outputs), not clip layers.</p>\n<h3 id=\"post-apiadd\"><code>POST /api/add</code></h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>channel</code></td><td>number</td><td>Caspar channel</td></tr>\n<tr><td><code>consumer</code></td><td>string</td><td>Consumer type (e.g. <code>decklink</code>, <code>screen</code>)</td></tr>\n<tr><td><code>params</code></td><td>string</td><td>Consumer parameter string</td></tr>\n<tr><td><code>index</code></td><td>number</td><td>Optional <code>ADD &lt;channel&gt;-&lt;index&gt;</code></td></tr>\n</tbody></table>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;consumer&quot;: &quot;decklink&quot;,\n  &quot;params&quot;: &quot;device 1&quot;,\n  &quot;index&quot;: 0\n}</code></pre>\n<h3 id=\"post-apiremove\"><code>POST /api/remove</code></h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>channel</code></td><td>number</td><td>Caspar channel</td></tr>\n<tr><td><code>consumer</code></td><td>string</td><td>Consumer id (when not using index)</td></tr>\n<tr><td><code>index</code></td><td>number</td><td><code>REMOVE &lt;channel&gt;-&lt;index&gt;</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"clip-strings\">Clip strings</h2>\n<table><thead><tr><th>Form</th><th>Transport</th><th>Notes</th></tr></thead><tbody>\n<tr><td><code>clip.mov</code>, <code>media/still.png</code></td><td>Usually <strong>typed</strong> <code>casparcg-connection</code></td><td>Quoted if spaces</td></tr>\n<tr><td><code>[HTML] …</code></td><td><strong>Raw</strong> AMCP</td><td>Browser/HTML producer</td></tr>\n<tr><td><code>ndi://…</code></td><td><strong>Raw</strong></td><td>NDI source</td></tr>\n<tr><td><code>route://1-10</code></td><td><strong>Raw</strong></td><td>Routed layer</td></tr>\n<tr><td><code>http(s)://…</code></td><td>Typed or <code>[HTML]</code></td><td>Non-video extensions forced to HTML</td></tr>\n</tbody></table>\n<p>STING transitions require <code>transition: &quot;STING&quot;</code> and <code>parameters</code> with template path.</p>\n<hr>\n<h2 id=\"websocket-parity\">WebSocket parity</h2>\n<p>Structured AMCP over WebSocket uses the same fields as HTTP bodies (<code>type: &quot;play&quot;</code>, <code>channel</code>, <code>layer</code>, <code>clip</code>, …). See smoke test <code>dispatchStructuredAmcp</code> in <a href=\"../../tools/smoke/highascg-health-api-amcp.test.js\">&lt;code&gt;tools/smoke/highascg-health-api-amcp.test.js&lt;/code&gt;</a>.</p>\n<hr>\n<h2 id=\"related\">Related</h2>\n<table><thead><tr><th>Doc</th><th>Content</th></tr></thead><tbody>\n<tr><td><a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">scene-take.md</a></td><td>Multi-layer look take (LOADBG/PLAY pipeline)</td></tr>\n<tr><td><a href=\"#/wiki--api--playback-and-amcp\" data-wiki-page=\"1\">playback-and-amcp.md</a></td><td><code>/api/amcp/batch</code>, raw, print, restart, logs</td></tr>\n<tr><td><a href=\"#/wiki--api--mixer-cg-scene\" data-wiki-page=\"1\">mixer-cg-scene.md</a></td><td><code>MIXER</code>, CG, FTB</td></tr>\n<tr><td><a href=\"#/wiki--api--project-state-media\" data-wiki-page=\"1\">project-state-media.md</a></td><td><code>/api/state</code>, <code>/api/channels</code>, media list</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"openapi\">OpenAPI</h2>\n<p><a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a> — schemas <code>PlayRequest</code>, <code>LayerTransportRequest</code>, <code>PlaybackResponse</code>, <code>AmcpBatchRequest</code>.</p>","wiki--api--playback-and-amcp":"<h1 id=\"amcp-bridge-server-control\">AMCP bridge &amp; server control</h1>\n<p><strong>Caspar:</strong> required for all routes on this page.</p>\n<p>Base: <code>http://&lt;host&gt;:4200</code></p>\n<h2 id=\"playback-layer-commands\">Playback (layer commands)</h2>\n<p><strong>Full reference:</strong> <a href=\"#/wiki--api--playback\" data-wiki-page=\"1\">playback.md</a> — <code>POST /api/play</code>, <code>/api/load</code>, <code>/api/loadbg</code>, pause/resume/stop/clear, call/swap, add/remove, <code>playbackMatrix</code>, clip/transition fields.</p>\n<h2 id=\"amcp-bridge-amcp-bridge\">AMCP bridge {#amcp-bridge}</h2>\n<h3 id=\"post-apiamcpbatch\"><code>POST /api/amcp/batch</code></h3>\n<p>Preferred for multiple lines. <strong>Do not</strong> wrap in <code>BEGIN</code>/<code>COMMIT</code> in the array — the client adds batching.</p>\n<pre><code class=\"language-json\">{\n  &quot;commands&quot;: [\n    &quot;CLEAR 1&quot;,\n    &quot;PLAY 1-10 AMB&quot;,\n    &quot;MIXER 1 COMMIT&quot;\n  ]\n}</code></pre>\n<p><code>MIXER {ch} COMMIT</code> must be <strong>outside</strong> BEGIN/COMMIT batches (send as its own line or after batch completes).</p>\n<h3 id=\"post-apiamcpraw-batch-post-apiraw-post-apiamcpraw\"><code>POST /api/amcp/raw-batch</code> · <code>POST /api/raw</code> · <code>POST /api/amcp/raw</code></h3>\n<p>Sequential raw AMCP (max 4000 lines per raw-batch). Single line for <code>/api/raw</code>.</p>\n<h3 id=\"post-apiprint-post-apiamcpprint\"><code>POST /api/print</code> · <code>POST /api/amcp/print</code></h3>\n<p>Body: <code>{ &quot;channel&quot;: 1 }</code>.</p>\n<h3 id=\"logging-server\">Logging &amp; server</h3>\n<table><thead><tr><th>Method</th><th>Path</th><th>Body / notes</th></tr></thead><tbody>\n<tr><td>POST</td><td><code>/api/log/level</code></td><td><code>{ &quot;level&quot;: &quot;trace&quot; }</code></td></tr>\n<tr><td>POST</td><td><code>/api/log/category</code></td><td><code>{ &quot;category&quot;: &quot;calltrace&quot;, &quot;enable&quot;: true }</code></td></tr>\n<tr><td>POST</td><td><code>/api/ping</code></td><td>—</td></tr>\n<tr><td>POST</td><td><code>/api/restart</code></td><td>Restart Caspar</td></tr>\n<tr><td>POST</td><td><code>/api/kill</code></td><td>Kill Caspar</td></tr>\n<tr><td>POST</td><td><code>/api/diag</code></td><td>Diagnostics</td></tr>\n<tr><td>POST</td><td><code>/api/gl/gc</code></td><td>GL garbage collect</td></tr>\n<tr><td>POST</td><td><code>/api/channel-grid</code></td><td>Channel grid</td></tr>\n<tr><td>POST</td><td><code>/api/set</code></td><td>Channel SET</td></tr>\n<tr><td>POST</td><td><code>/api/lock</code></td><td>Channel lock</td></tr>\n</tbody></table>\n<h2 id=\"related\">Related</h2>\n<ul>\n<li><a href=\"#/wiki--api--mixer-cg-scene\" data-wiki-page=\"1\">Mixer, CG &amp;amp; scene</a></li>\n<li><a href=\"#/wiki--api--project-state-media#state--query\" data-wiki-page=\"1\">State &amp;amp; channels</a></li>\n</ul>","wiki--api--mixer":"<h1 id=\"mixer-api-mixer-amcp-over-http\">Mixer API — <code>MIXER</code> AMCP over HTTP</h1>\n<p>CasparCG layer and channel mixer commands: opacity, fill, crop, color, audio volume, deferred tweens, and inspector effects.</p>\n<p><strong>Caspar:</strong> required (<code>503</code> if AMCP is down).</p>\n<p><strong>Implementation:</strong> <a href=\"../../src/api/routes-mixer.js\">&lt;code&gt;src/api/routes-mixer.js&lt;/code&gt;</a> · AMCP: <a href=\"../../src/caspar/amcp-mixer.js\">&lt;code&gt;src/caspar/amcp-mixer.js&lt;/code&gt;</a></p>\n<p><strong>AMCP table:</strong> <a href=\"#/reference--amcp-mapping\" data-wiki-page=\"1\">&lt;code&gt;docs/reference/amcp-mapping.md&lt;/code&gt;</a></p>\n<p>CG, scene take, and PiP are separate: <a href=\"#/wiki--api--mixer-cg-scene\" data-wiki-page=\"1\">mixer-cg-scene.md</a>.</p>\n<hr>\n<h2 id=\"endpoints\">Endpoints</h2>\n<pre><code class=\"language-http\">POST /api/mixer/{command}\nGET  /api/mixer/{command}?channel=1&amp;layer=10</code></pre>\n<table><thead><tr><th><code>{command}</code></th><th>Layer required</th><th>Query (GET)</th><th>Set (POST)</th></tr></thead><tbody>\n<tr><td><code>opacity</code></td><td>yes</td><td>current opacity</td><td><code>opacity</code>, optional tween</td></tr>\n<tr><td><code>fill</code></td><td>yes</td><td>FILL tuple</td><td>position/scale + optional <code>stretch</code></td></tr>\n<tr><td><code>clip</code></td><td>yes</td><td>CLIP tuple</td><td><code>x</code>, <code>y</code>, <code>xScale</code>, <code>yScale</code></td></tr>\n<tr><td><code>anchor</code></td><td>yes</td><td>anchor</td><td><code>x</code>, <code>y</code></td></tr>\n<tr><td><code>crop</code></td><td>yes</td><td>crop</td><td><code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code></td></tr>\n<tr><td><code>rotation</code></td><td>yes</td><td>rotation</td><td><code>degrees</code></td></tr>\n<tr><td><code>blend</code></td><td>yes</td><td>blend mode</td><td><code>mode</code> (e.g. <code>normal</code>, <code>add</code>)</td></tr>\n<tr><td><code>keyer</code></td><td>yes</td><td>keyer on/off</td><td><code>keyer</code> <code>0</code> \\</td><td><code>1</code></td></tr>\n<tr><td><code>chroma</code></td><td>yes</td><td>chroma</td><td>Caspar chroma fields (see below)</td></tr>\n<tr><td><code>invert</code></td><td>yes</td><td>invert</td><td><code>invert</code> bool</td></tr>\n<tr><td><code>brightness</code></td><td>yes</td><td>brightness</td><td><code>value</code></td></tr>\n<tr><td><code>saturation</code></td><td>yes</td><td>saturation</td><td><code>value</code></td></tr>\n<tr><td><code>contrast</code></td><td>yes</td><td>contrast</td><td><code>value</code></td></tr>\n<tr><td><code>levels</code></td><td>yes</td><td>levels</td><td><code>minInput</code>, <code>maxInput</code>, <code>gamma</code>, <code>minOutput</code>, <code>maxOutput</code></td></tr>\n<tr><td><code>volume</code></td><td>yes</td><td>volume</td><td><code>volume</code></td></tr>\n<tr><td><code>mastervolume</code></td><td>channel only</td><td>master</td><td><code>volume</code></td></tr>\n<tr><td><code>straight_alpha</code></td><td>channel only</td><td>straight alpha</td><td><code>enable</code> bool</td></tr>\n<tr><td><code>grid</code></td><td>channel only</td><td>grid</td><td><code>resolution</code></td></tr>\n<tr><td><code>commit</code></td><td>channel only</td><td>—</td><td>applies deferred mixer</td></tr>\n<tr><td><code>clear</code></td><td>optional layer</td><td>—</td><td>clear mixer state</td></tr>\n<tr><td><code>effect</code></td><td>yes</td><td>—</td><td>inspector <code>effectType</code> + <code>params</code></td></tr>\n</tbody></table>\n<p><strong>Not a top-level path:</strong> <code>perspective</code> — use <strong><code>POST /api/mixer/effect</code></strong> with <code>effectType: &quot;perspective&quot;</code>.</p>\n<hr>\n<h2 id=\"common-post-fields\">Common POST fields</h2>\n<table><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>channel</code></td><td>number</td><td><code>1</code></td><td>Caspar channel</td></tr>\n<tr><td><code>layer</code></td><td>number</td><td>—</td><td>Caspar layer (<code>channel-layer</code>)</td></tr>\n<tr><td><code>duration</code></td><td>number</td><td>—</td><td>Tween length in <strong>frames</strong></td></tr>\n<tr><td><code>tween</code></td><td>string</td><td>—</td><td>Easing (<code>linear</code>, <code>easeInOutCubic</code>, …)</td></tr>\n<tr><td><code>defer</code></td><td>boolean</td><td>—</td><td>Append <code>DEFER</code> — apply on <code>commit</code></td></tr>\n</tbody></table>\n<h3 id=\"deferred-multi-change-looks-batches\">Deferred multi-change (looks / batches)</h3>\n<ol>\n<li>Send several mixer POSTs with <code>&quot;defer&quot;: true</code>.</li>\n<li><strong><code>POST /api/mixer/commit</code></strong> with <code>{ &quot;channel&quot;: 1 }</code>.</li>\n</ol>\n<p>Do <strong>not</strong> wrap <code>MIXER … COMMIT</code> inside <code>/api/amcp/batch</code> <code>BEGIN</code>/<code>COMMIT</code> — send commit as its own line (<a href=\"#/wiki--api--playback-and-amcp\" data-wiki-page=\"1\">playback-and-amcp.md</a>).</p>\n<hr>\n<h2 id=\"ui-selection-integration\">UI selection integration</h2>\n<p>When the operator has a <strong>look layer</strong> selected (<code>POST /api/selection</code> with <code>context: &quot;scene_layer&quot;</code>), <strong><code>opacity</code></strong> and <strong><code>fill</code></strong> can omit <code>channel</code> / <code>layer</code>:</p>\n<ul>\n<li>Server reads <code>ui_selection_look_preview_channel</code> and <code>ui_selection_look_caspar_layer</code> from variables.</li>\n<li>Updates the matching layer in the persisted <strong>web project</strong> (debounced save).</li>\n<li>Broadcasts WebSocket <code>mixer_update</code> with <code>{ lookId, layerIdx, updatedValues }</code>.</li>\n</ul>\n<p><strong>Relative nudges:</strong> pass <code>&quot;+0.01&quot;</code> or <code>&quot;-0.05&quot;</code> for <code>opacity</code> or fill <code>x</code> / <code>y</code> / <code>scaleX</code> / <code>scaleY</code> — server applies delta from current project/UI state.</p>\n<p><strong>Aspect lock:</strong> when the look layer has <code>aspectLocked</code>, changing <code>scaleX</code> or <code>scaleY</code> adjusts the other axis to preserve aspect.</p>\n<hr>\n<h2 id=\"commands-detail\">Commands (detail)</h2>\n<h3 id=\"post-apimixeropacity\"><code>POST /api/mixer/opacity</code></h3>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;opacity&quot;: 0.5,\n  &quot;duration&quot;: 25,\n  &quot;tween&quot;: &quot;linear&quot;,\n  &quot;defer&quot;: false\n}</code></pre>\n<p>AMCP: <code>MIXER 1-10 OPACITY 0.5 25 linear</code></p>\n<h3 id=\"post-apimixerfill\"><code>POST /api/mixer/fill</code></h3>\n<p>Normalized position and scale (0–1 style, Caspar FILL semantics):</p>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;x&quot;: 0,\n  &quot;y&quot;: 0,\n  &quot;xScale&quot;: 1,\n  &quot;yScale&quot;: 1,\n  &quot;duration&quot;: 0\n}</code></pre>\n<p><strong>Content-aware stretch</strong> (queries layer <code>INFO</code> for width/height):</p>\n<table><thead><tr><th><code>stretch</code></th><th>Behaviour</th></tr></thead><tbody>\n<tr><td><code>none</code></td><td>Native scale; clip if larger than layer box</td></tr>\n<tr><td><code>fit</code></td><td>Letterbox inside layer rect</td></tr>\n<tr><td><code>fill-h</code></td><td>Full width, height by aspect</td></tr>\n<tr><td><code>fill-v</code></td><td>Full height, width by aspect</td></tr>\n<tr><td><code>stretch</code></td><td>Fill layer rect; resets clip to full frame</td></tr>\n</tbody></table>\n<p>Optional geometry hints: <code>channelW</code>, <code>channelH</code>, <code>layerW</code>, <code>layerH</code>, <code>layerX</code>, <code>layerY</code>.</p>\n<h3 id=\"post-apimixerclip\"><code>POST /api/mixer/clip</code></h3>\n<p>Mask rectangle (same tuple shape as FILL in AMCP):</p>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;x&quot;: 0,\n  &quot;y&quot;: 0,\n  &quot;xScale&quot;: 0.5,\n  &quot;yScale&quot;: 0.5\n}</code></pre>\n<p>Inspector <strong>clip_mask</strong> effect maps <code>params.left/top/width/height</code> → <code>x</code>, <code>y</code>, <code>xScale</code>, <code>yScale</code>.</p>\n<h3 id=\"post-apimixercrop\"><code>POST /api/mixer/crop</code></h3>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;left&quot;: 0,\n  &quot;top&quot;: 0,\n  &quot;right&quot;: 0,\n  &quot;bottom&quot;: 0\n}</code></pre>\n<h3 id=\"post-apimixerlevels\"><code>POST /api/mixer/levels</code></h3>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;minInput&quot;: 0,\n  &quot;maxInput&quot;: 1,\n  &quot;gamma&quot;: 1,\n  &quot;minOutput&quot;: 0,\n  &quot;maxOutput&quot;: 1,\n  &quot;duration&quot;: 0,\n  &quot;defer&quot;: true\n}</code></pre>\n<p>Inspector effect uses <code>minIn</code> / <code>maxIn</code> / <code>minOut</code> / <code>maxOut</code> aliases inside <code>effect</code> → mapped to <code>minInput</code> etc.</p>\n<h3 id=\"post-apimixerchroma\"><code>POST /api/mixer/chroma</code></h3>\n<p>Pass Caspar chroma options on the body (forwarded to AMCP):</p>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;enable&quot;: true,\n  &quot;targetHue&quot;: 120,\n  &quot;hueWidth&quot;: 0.1,\n  &quot;minSaturation&quot;: 0.3,\n  &quot;minBrightness&quot;: 0.2,\n  &quot;softness&quot;: 0.1,\n  &quot;spillSuppress&quot;: 0.5\n}</code></pre>\n<h3 id=\"post-apimixerblend\"><code>POST /api/mixer/blend</code></h3>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10, &quot;mode&quot;: &quot;normal&quot; }</code></pre>\n<h3 id=\"post-apimixerkeyer\"><code>POST /api/mixer/keyer</code></h3>\n<p>Layer straight-alpha keyer (not DeckLink hardware key):</p>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10, &quot;keyer&quot;: 1 }</code></pre>\n<h3 id=\"post-apimixervolume-post-apimixermastervolume\"><code>POST /api/mixer/volume</code> · <code>POST /api/mixer/mastervolume</code></h3>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10, &quot;volume&quot;: 1, &quot;duration&quot;: 0 }</code></pre>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;volume&quot;: 0.8 }</code></pre>\n<h3 id=\"post-apimixerstraight_alpha\"><code>POST /api/mixer/straight_alpha</code></h3>\n<p>Channel output straight alpha:</p>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;enable&quot;: true }</code></pre>\n<h3 id=\"post-apimixergrid\"><code>POST /api/mixer/grid</code></h3>\n<p>Safe-area / grid overlay on channel:</p>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;resolution&quot;: 1 }</code></pre>\n<h3 id=\"post-apimixercommit\"><code>POST /api/mixer/commit</code></h3>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1 }</code></pre>\n<h3 id=\"post-apimixerclear\"><code>POST /api/mixer/clear</code></h3>\n<p>Per layer or whole channel:</p>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10 }</code></pre>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1 }</code></pre>\n<hr>\n<h2 id=\"post-apimixereffect\"><code>POST /api/mixer/effect</code></h2>\n<p>Single inspector effect (same types as the client effect registry):</p>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;effectType&quot;: &quot;brightness&quot;,\n  &quot;params&quot;: { &quot;value&quot;: 1.2 },\n  &quot;duration&quot;: 25,\n  &quot;tween&quot;: &quot;linear&quot;,\n  &quot;defer&quot;: true\n}</code></pre>\n<table><thead><tr><th><code>effectType</code></th><th><code>params</code> (typical)</th><th>AMCP routed to</th></tr></thead><tbody>\n<tr><td><code>blend_mode</code></td><td><code>mode</code></td><td><code>blend</code></td></tr>\n<tr><td><code>brightness</code></td><td><code>value</code></td><td><code>brightness</code></td></tr>\n<tr><td><code>contrast</code></td><td><code>value</code></td><td><code>contrast</code></td></tr>\n<tr><td><code>saturation</code></td><td><code>value</code></td><td><code>saturation</code></td></tr>\n<tr><td><code>levels</code></td><td><code>minIn</code>, <code>maxIn</code>, <code>gamma</code>, <code>minOut</code>, <code>maxOut</code></td><td><code>levels</code></td></tr>\n<tr><td><code>chroma_key</code></td><td><code>key</code>, <code>threshold</code>, <code>softness</code>, <code>spill</code>, <code>blur</code></td><td><code>chroma</code></td></tr>\n<tr><td><code>crop</code></td><td><code>left</code>, <code>top</code>, <code>right</code>, <code>bottom</code></td><td><code>crop</code></td></tr>\n<tr><td><code>clip_mask</code></td><td><code>left</code>, <code>top</code>, <code>width</code>, <code>height</code></td><td><code>clip</code></td></tr>\n<tr><td><code>perspective</code></td><td><code>ulX</code>, <code>ulY</code>, <code>urX</code>, <code>urY</code>, <code>lrX</code>, <code>lrY</code>, <code>llX</code>, <code>llY</code></td><td><code>perspective</code></td></tr>\n<tr><td><code>grid</code></td><td><code>resolution</code></td><td><code>grid</code></td></tr>\n<tr><td><code>keyer</code></td><td><code>enabled</code></td><td><code>keyer</code></td></tr>\n<tr><td><code>rotation</code></td><td><code>degrees</code></td><td><code>rotation</code></td></tr>\n<tr><td><code>anchor</code></td><td><code>x</code>, <code>y</code></td><td><code>anchor</code></td></tr>\n</tbody></table>\n<p>Alias: <code>type</code> instead of <code>effectType</code>.</p>\n<p><strong>400</strong> from handler: <code>{ &quot;error&quot;: &quot;Unknown inspector effectType: …&quot; }</code> <strong>502</strong> / <strong>503</strong> from <code>handleMixerSafe</code> on AMCP/connection errors.</p>\n<hr>\n<h2 id=\"get-query-current-value\">GET — query current value</h2>\n<p>Omit value fields in POST; GET runs the AMCP query form (no arguments → read current state).</p>\n<pre><code class=\"language-bash\">curl -s 'http://127.0.0.1:4200/api/mixer/opacity?channel=1&amp;layer=10'</code></pre>\n<p>Typical response:</p>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;data&quot;: &quot;1&quot;\n}</code></pre>\n<p><code>data</code> is the raw AMCP payload (often a string or string array).</p>\n<hr>\n<h2 id=\"examples\">Examples</h2>\n<h3 id=\"fade-layer-in-over-1-second-50-fps\">Fade layer in over 1 second @ 50 fps</h3>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/mixer/opacity \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:10,&quot;opacity&quot;:1,&quot;duration&quot;:50,&quot;tween&quot;:&quot;easeInOutCubic&quot;}'</code></pre>\n<h3 id=\"deferred-look-tweak-commit\">Deferred look tweak + commit</h3>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/mixer/fill \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:10,&quot;x&quot;:0.1,&quot;y&quot;:0,&quot;xScale&quot;:0.8,&quot;yScale&quot;:0.8,&quot;defer&quot;:true}'\n\ncurl -s -X POST http://127.0.0.1:4200/api/mixer/opacity \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:10,&quot;opacity&quot;:1,&quot;duration&quot;:25,&quot;tween&quot;:&quot;linear&quot;,&quot;defer&quot;:true}'\n\ncurl -s -X POST http://127.0.0.1:4200/api/mixer/commit \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1}'</code></pre>\n<h3 id=\"javascript\">JavaScript</h3>\n<pre><code class=\"language-javascript\">await fetch(`${apiBase}/api/mixer/fill`, {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({\n    channel: 1,\n    layer: 10,\n    x: 0,\n    y: 0,\n    xScale: 1,\n    yScale: 1,\n    stretch: 'fit',\n    channelW: 1920,\n    channelH: 1080,\n  }),\n})</code></pre>\n<hr>\n<h2 id=\"responses-and-errors\">Responses and errors</h2>\n<table><thead><tr><th>Status</th><th>Meaning</th></tr></thead><tbody>\n<tr><td><code>200</code></td><td>AMCP success — body is passthrough Caspar JSON (<code>ok</code>, <code>data</code>, …)</td></tr>\n<tr><td><code>400</code></td><td>Unknown <code>{command}</code></td></tr>\n<tr><td><code>502</code></td><td>Caspar/command failure</td></tr>\n<tr><td><code>503</code></td><td>Not connected / socket errors</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"related\">Related</h2>\n<table><thead><tr><th>Doc</th><th>Topic</th></tr></thead><tbody>\n<tr><td><a href=\"#/wiki--api--playback\" data-wiki-page=\"1\">playback.md</a></td><td><code>PLAY</code> / <code>LOAD</code> / layer transport</td></tr>\n<tr><td><a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">scene-take.md</a></td><td>Look take applies mixer + LOADBG/PLAY</td></tr>\n<tr><td><a href=\"#/wiki--api--timelines\" data-wiki-page=\"1\">timelines.md</a></td><td>Keyframed mixer during timeline tick</td></tr>\n<tr><td><a href=\"#/wiki--api--playback-and-amcp\" data-wiki-page=\"1\">playback-and-amcp.md</a></td><td>Batch AMCP + <code>COMMIT</code> rules</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"openapi\">OpenAPI</h2>\n<p><a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a> — <code>MixerCommand</code> path param, <code>MixerCommonFields</code>, per-command request notes, examples for <code>opacity</code>, <code>fill</code>, <code>commit</code>, <code>effect</code>.</p>","wiki--api--mixer-cg-scene":"<h1 id=\"cg-scene-overlays\">CG, scene &amp; overlays</h1>\n<p><strong>Caspar:</strong> required unless noted.</p>\n<h2 id=\"mixer\">Mixer</h2>\n<p><strong>Full reference:</strong> <a href=\"#/wiki--api--mixer\" data-wiki-page=\"1\">mixer.md</a> — all <code>/api/mixer/{command}</code> routes, <code>defer</code>/<code>commit</code>, <code>fill</code> stretch modes, <code>effect</code> types, UI selection sync.</p>\n<h2 id=\"cg-html-templates\">CG (HTML templates)</h2>\n<p><strong>Full reference:</strong> <a href=\"#/wiki--api--cg\" data-wiki-page=\"1\">cg.md</a></p>\n<h2 id=\"scene\">Scene</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/scene/live</code></td><td>Live program/preview scene IDs per channel</td></tr>\n<tr><td>POST</td><td><code>/api/scene/take</code></td><td><strong>Full reference:</strong> <a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">scene-take.md</a></td></tr>\n<tr><td>POST</td><td><code>/api/scene/border-lines</code></td><td>Returns AMCP lines for border preset</td></tr>\n<tr><td>POST</td><td><code>/api/scene/border-preset-crossfade</code></td><td>Crossfade border lines</td></tr>\n</tbody></table>\n<h3 id=\"get-apiscenelive\"><code>GET /api/scene/live</code></h3>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/scene/live | jq .</code></pre>\n<h2 id=\"ftb-led-test-pip\">FTB · LED test · PiP</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Notes</th></tr></thead><tbody>\n<tr><td>POST</td><td><code>/api/ftb</code></td><td>Fade to black</td></tr>\n<tr><td>POST</td><td><code>/api/led-test-card</code></td><td>Startup/output test pattern</td></tr>\n<tr><td>GET</td><td><code>/api/pip-overlay/templates</code></td><td>PiP template list</td></tr>\n<tr><td>POST</td><td><code>/api/pip-overlay/apply</code></td><td>Apply PiP</td></tr>\n<tr><td>POST</td><td><code>/api/pip-overlay/update</code></td><td>Update PiP</td></tr>\n<tr><td>POST</td><td><code>/api/pip-overlay/remove</code></td><td>Remove PiP</td></tr>\n</tbody></table>\n<h2 id=\"multiview\">Multiview</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>POST</td><td><code>/api/multiview/apply</code></td></tr>\n</tbody></table>\n<p>See <a href=\"#/wiki--api--timelines\" data-wiki-page=\"1\">timelines.md</a> and <a href=\"#/wiki--api--timelines-multiview\" data-wiki-page=\"1\">timelines-multiview.md</a>.</p>","wiki--api--cg":"<h1 id=\"cg-api-html-templates-cg-amcp\">CG API — HTML templates (<code>CG</code> AMCP)</h1>\n<p>CasparCG <strong>HTML/template CG</strong> commands on a channel layer. Templates live under the Caspar template path; data payloads are XML/JSON strings passed on add/update.</p>\n<p><strong>Caspar:</strong> required (<code>503</code> if AMCP is down).</p>\n<p><strong>Implementation:</strong> <a href=\"../../src/api/routes-cg.js\">&lt;code&gt;src/api/routes-cg.js&lt;/code&gt;</a> · AMCP: <a href=\"../../src/caspar/amcp-cg.js\">&lt;code&gt;src/caspar/amcp-cg.js&lt;/code&gt;</a></p>\n<p><strong>AMCP table:</strong> <a href=\"#/reference--amcp-mapping\" data-wiki-page=\"1\">&lt;code&gt;docs/reference/amcp-mapping.md&lt;/code&gt;</a></p>\n<p>Mixer and scene APIs: <a href=\"#/wiki--api--mixer\" data-wiki-page=\"1\">mixer.md</a>, <a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">scene-take.md</a>.</p>\n<hr>\n<h2 id=\"endpoint-pattern\">Endpoint pattern</h2>\n<pre><code class=\"language-http\">POST /api/cg/{command}\nContent-Type: application/json</code></pre>\n<table><thead><tr><th><code>{command}</code></th><th>AMCP</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>add</code></td><td><code>CG ADD</code></td><td>Register template on layer</td></tr>\n<tr><td><code>remove</code></td><td><code>CG REMOVE</code></td><td>Remove template instance</td></tr>\n<tr><td><code>clear</code></td><td><code>CG CLEAR</code></td><td>Clear all CG on layer</td></tr>\n<tr><td><code>play</code></td><td><code>CG PLAY</code></td><td>Play template</td></tr>\n<tr><td><code>stop</code></td><td><code>CG STOP</code></td><td>Stop template</td></tr>\n<tr><td><code>next</code></td><td><code>CG NEXT</code></td><td>Next step/page</td></tr>\n<tr><td><code>goto</code></td><td><code>CG GOTO</code></td><td>Jump to label</td></tr>\n<tr><td><code>update</code></td><td><code>CG UPDATE</code></td><td>Push new template data</td></tr>\n<tr><td><code>invoke</code></td><td><code>CG INVOKE</code></td><td>Call template method</td></tr>\n<tr><td><code>info</code></td><td><code>CG INFO</code></td><td>Query template state</td></tr>\n</tbody></table>\n<p>Unknown command → <strong>400</strong> <code>{ &quot;error&quot;: &quot;Unknown CG command: …&quot; }</code>.</p>\n<hr>\n<h2 id=\"common-request-fields\">Common request fields</h2>\n<table><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>channel</code></td><td>number</td><td><code>1</code></td><td>Caspar channel</td></tr>\n<tr><td><code>layer</code></td><td>number</td><td>—</td><td>Caspar layer hosting CG (<code>channel-layer</code>)</td></tr>\n<tr><td><code>templateHostLayer</code></td><td>number</td><td><code>1</code></td><td>CG sub-layer index on that layer (Caspar “template layer”)</td></tr>\n<tr><td><code>template</code></td><td>string</td><td>—</td><td>Template name (for <code>add</code>)</td></tr>\n<tr><td><code>playOnLoad</code></td><td>boolean</td><td><code>false</code></td><td><code>1</code> / <code>0</code> on <code>CG ADD</code></td></tr>\n<tr><td><code>data</code></td><td>string</td><td>—</td><td>Template data XML/JSON (add/update)</td></tr>\n<tr><td><code>label</code></td><td>string</td><td>—</td><td>Goto label (<code>goto</code>)</td></tr>\n<tr><td><code>method</code></td><td>string</td><td>—</td><td>Method name (<code>invoke</code>)</td></tr>\n</tbody></table>\n<p>AMCP shape: <code>CG {channel}-{layer} {SUBCMD} {templateHostLayer} …</code></p>\n<p>Example: channel <code>1</code>, layer <code>10</code>, template host <code>1</code> → <code>CG 1-10 ADD 1 &quot;my_template&quot; 1 &lt;data…&gt;</code>.</p>\n<hr>\n<h2 id=\"commands\">Commands</h2>\n<h3 id=\"post-apicgadd\"><code>POST /api/cg/add</code></h3>\n<p>Load a template onto the layer.</p>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;templateHostLayer&quot;: 1,\n  &quot;template&quot;: &quot;lower_third&quot;,\n  &quot;playOnLoad&quot;: true,\n  &quot;data&quot;: &quot;&lt;template&gt;&lt;text id=\\&quot;title\\&quot;&gt;Hello&lt;/text&gt;&lt;/template&gt;&quot;\n}</code></pre>\n<p>When <code>data</code> is non-empty, the server sends <strong>raw</strong> AMCP (escaping handled in <a href=\"../../src/caspar/amcp-cg.js\">&lt;code&gt;amcp-cg.js&lt;/code&gt;</a>). Empty data uses typed <code>cgAdd</code>.</p>\n<h3 id=\"post-apicgupdate\"><code>POST /api/cg/update</code></h3>\n<p>Replace template data without re-adding:</p>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;templateHostLayer&quot;: 1,\n  &quot;data&quot;: &quot;&lt;template&gt;&lt;text id=\\&quot;title\\&quot;&gt;Updated&lt;/text&gt;&lt;/template&gt;&quot;\n}</code></pre>\n<h3 id=\"post-apicgplay-stop-next\"><code>POST /api/cg/play</code> · <code>stop</code> · <code>next</code></h3>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10, &quot;templateHostLayer&quot;: 1 }</code></pre>\n<h3 id=\"post-apicggoto\"><code>POST /api/cg/goto</code></h3>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;templateHostLayer&quot;: 1,\n  &quot;label&quot;: &quot;slide2&quot;\n}</code></pre>\n<h3 id=\"post-apicginvoke\"><code>POST /api/cg/invoke</code></h3>\n<pre><code class=\"language-json\">{\n  &quot;channel&quot;: 1,\n  &quot;layer&quot;: 10,\n  &quot;templateHostLayer&quot;: 1,\n  &quot;method&quot;: &quot;refresh&quot;\n}</code></pre>\n<h3 id=\"post-apicgremove\"><code>POST /api/cg/remove</code></h3>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10, &quot;templateHostLayer&quot;: 1 }</code></pre>\n<h3 id=\"post-apicgclear\"><code>POST /api/cg/clear</code></h3>\n<p>Clears <strong>all</strong> CG templates on the layer (no <code>templateHostLayer</code> in AMCP):</p>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10 }</code></pre>\n<h3 id=\"post-apicginfo\"><code>POST /api/cg/info</code></h3>\n<pre><code class=\"language-json\">{ &quot;channel&quot;: 1, &quot;layer&quot;: 10, &quot;templateHostLayer&quot;: 1 }</code></pre>\n<p>Omit <code>templateHostLayer</code> to query layer-level CG info.</p>\n<hr>\n<h2 id=\"examples\">Examples</h2>\n<pre><code class=\"language-bash\"># Add lower third and auto-play\ncurl -s -X POST http://127.0.0.1:4200/api/cg/add \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    &quot;channel&quot;: 1,\n    &quot;layer&quot;: 998,\n    &quot;templateHostLayer&quot;: 1,\n    &quot;template&quot;: &quot;lt&quot;,\n    &quot;playOnLoad&quot;: true,\n    &quot;data&quot;: &quot;&lt;template&gt;&lt;text&gt;ON AIR&lt;/text&gt;&lt;/template&gt;&quot;\n  }'\n\n# Update text\ncurl -s -X POST http://127.0.0.1:4200/api/cg/update \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    &quot;channel&quot;: 1,\n    &quot;layer&quot;: 998,\n    &quot;templateHostLayer&quot;: 1,\n    &quot;data&quot;: &quot;&lt;template&gt;&lt;text&gt;OFF AIR&lt;/text&gt;&lt;/template&gt;&quot;\n  }'\n\n# Remove\ncurl -s -X POST http://127.0.0.1:4200/api/cg/remove \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;channel&quot;:1,&quot;layer&quot;:998,&quot;templateHostLayer&quot;:1}'</code></pre>\n<pre><code class=\"language-javascript\">await fetch(`${apiBase}/api/cg/add`, {\n  method: 'POST',\n  headers: { 'Content-Type': 'application/json' },\n  body: JSON.stringify({\n    channel: 1,\n    layer: 10,\n    template: 'clock',\n    playOnLoad: true,\n    data: '&lt;template&gt;&lt;/template&gt;',\n  }),\n})</code></pre>\n<hr>\n<h2 id=\"responses\">Responses</h2>\n<p><strong>200</strong> — Passthrough Caspar JSON (<code>ok</code>, <code>data</code>, …).</p>\n<p><strong>502</strong> — AMCP/command failure.</p>\n<p><strong>503</strong> — Caspar not connected.</p>\n<hr>\n<h2 id=\"templates-list\">Templates list</h2>\n<p>Discover template names via <strong><code>GET /api/templates</code></strong> (requires Caspar) or <strong><code>GET /api/state</code></strong> → <code>templates</code> array.</p>\n<hr>\n<h2 id=\"related\">Related</h2>\n<table><thead><tr><th>Doc</th><th>Topic</th></tr></thead><tbody>\n<tr><td><a href=\"#/wiki--api--state-and-media\" data-wiki-page=\"1\">state-and-media.md</a></td><td><code>/api/state</code>, media index</td></tr>\n<tr><td><a href=\"#/wiki--api--mixer\" data-wiki-page=\"1\">mixer.md</a></td><td>Layer opacity/fill (often combined with CG overlays)</td></tr>\n<tr><td><a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">scene-take.md</a></td><td>Look layers may use <code>source.type: &quot;cg&quot;</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"openapi\">OpenAPI</h2>\n<p><a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a> — <code>CgCommand</code>, <code>CgAddRequest</code>, shared <code>CgLayerRequest</code>.</p>","wiki--api--project":"<h1 id=\"project-api-save-load-sync\">Project API — save, load, sync</h1>\n<p>Persist show data (looks/scenes, routing, timelines in project JSON, settings mirrors) on the playout host disk. <strong>Does not require Caspar</strong> for core save/load/list/get.</p>\n<p><strong>Implementation:</strong> <a href=\"../../src/api/routes-data.js\">&lt;code&gt;src/api/routes-data.js&lt;/code&gt;</a> · store: <a href=\"../../src/engine/project-store.js\">&lt;code&gt;src/engine/project-store.js&lt;/code&gt;</a></p>\n<p>Advanced sync/bundle: <a href=\"../../src/api/routes-project.js\">&lt;code&gt;src/api/routes-project.js&lt;/code&gt;</a></p>\n<hr>\n<h2 id=\"caspar-gate\">Caspar gate</h2>\n<table><thead><tr><th>Route</th><th>Caspar</th></tr></thead><tbody>\n<tr><td><code>GET /api/project</code>, <code>/list</code>, <code>POST save/load/autosave</code></td><td><strong>Not required</strong></td></tr>\n<tr><td><code>GET /api/project/bundle</code></td><td><strong>Not required</strong></td></tr>\n<tr><td><code>POST /api/project/reconcile</code>, <code>/sync</code>, <code>/diff</code>, <code>/apply-bundle</code></td><td><strong>Required</strong></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"endpoints\">Endpoints</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/project</code></td><td>Active merged project (or <code>{}</code> if none)</td></tr>\n<tr><td>GET</td><td><code>/api/project/list</code></td><td>Saved slugs + <code>activeSlug</code></td></tr>\n<tr><td>POST</td><td><code>/api/project/save</code></td><td>Save project to disk</td></tr>\n<tr><td>POST</td><td><code>/api/project/load</code></td><td>Load project by slug</td></tr>\n<tr><td>POST</td><td><code>/api/project/autosave</code></td><td>Autosave with stricter validation</td></tr>\n<tr><td>GET</td><td><code>/api/project/bundle</code></td><td>Export bundle (config + state + Caspar XML + media manifest)</td></tr>\n<tr><td>POST</td><td><code>/api/project/reconcile</code></td><td>Check live looks vs media/template index</td></tr>\n<tr><td>POST</td><td><code>/api/project/sync</code></td><td>Force scene take for all live channels</td></tr>\n<tr><td>POST</td><td><code>/api/project/diff</code></td><td>Manifest diff (body)</td></tr>\n<tr><td>POST</td><td><code>/api/project/apply-bundle</code></td><td>Apply remote bundle, restart</td></tr>\n</tbody></table>\n<p>Legacy *<em><code>/api/data/</em></code><strong> → </strong>410 Gone** — use project routes instead.</p>\n<hr>\n<h2 id=\"on-disk-layout\">On-disk layout</h2>\n<table><thead><tr><th>Location</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>~/highascg/projects/&lt;slug&gt;.json</code></td><td>Working copy on the playout host</td></tr>\n<tr><td><code>~/highascg/projects/_autosave/&lt;slug&gt;.json</code></td><td>Autosave for active slug</td></tr>\n<tr><td><strong>USB stick</strong> <code>HIGHASCGEXF</code> → <code>projects/<em>.json</code> + <code>projects/_autosave/</em>.json</code></td><td><strong>Field catalog</strong> — listed when stick is mounted; boot pulls stick → working dir</td></tr>\n<tr><td><strong>Bridge disk</strong> <code>HIGHASCGDAT</code> → <code>projects/*.json</code> + <code>_autosave/</code></td><td>Production sync — bidirectional mtime sync with working dir</td></tr>\n</tbody></table>\n<p><strong>Save behaviour:</strong> each save/autosave writes the working copy and <code>_autosave/</code>, then pushes <strong>only that slug</strong> (main + autosave files) to USB and/or bridge when those volumes are mounted.</p>\n<p><strong>Boot behaviour:</strong> <code>exfat-sync --boot</code> pulls <code>projects/</code> <strong>including <code>_autosave/</code></strong> from the stick (and bridge when mounted). On load, <code>readAutosaveFile</code> refreshes from the newest volume copy so reboot restores where the operator left off.</p>\n<p><strong>List behaviour:</strong> when USB is mounted, <code>GET /api/project/list</code> reads the catalog from the stick (<code>source: &quot;usb&quot;</code>). Without USB, local + bridge entries are merged (newest <code>savedAt</code> wins).</p>\n<p>Sync map pairs: <code>usb-projects</code> (<code>direction: to_project</code>, boot pull only) and <code>bridge-projects</code> (<code>direction: both</code>).</p>\n<h3 id=\"project-scoped-media-wo-62\">Project-scoped media (WO-62)</h3>\n<p>When <code>projectScopedMedia.enabled</code> is true (Settings → Media), each active project gets a dedicated folder under the Caspar media root:</p>\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>&lt;media_root&gt;/projects/&lt;slug&gt;/</code></td><td>Default upload/import target while project is active</td></tr>\n<tr><td><code>&lt;media_root&gt;/stock/</code>, other folders</td><td>Shared library — still browsable and usable in looks</td></tr>\n</tbody></table>\n<p><strong>Portable show kit:</strong> copy <code>~/highascg/projects/&lt;slug&gt;.json</code> plus <code>media/projects/&lt;slug&gt;/</code> to another machine, load the project — clips referenced with project-relative ids resolve under the same folder layout.</p>\n<p><code>GET /api/project/list</code> returns <code>activeProjectMedia: { relId, absPath }</code> and each project row may include <code>mediaFolder: &quot;projects/&lt;slug&gt;&quot;</code>.</p>\n<hr>\n<p>The <code>project</code> object is the same envelope the operator UI saves:</p>\n<table><thead><tr><th>Area</th><th>Typical keys</th></tr></thead><tbody>\n<tr><td>Metadata</td><td><code>name</code>, <code>slug</code>, <code>savedAt</code>, <code>version</code></td></tr>\n<tr><td>Scenes</td><td><code>scenes.scenes[]</code> — looks with <code>layers</code>, transitions</td></tr>\n<tr><td><strong><code>hardwareConfig</code></strong></td><td>Injected on <strong>save/autosave</strong> from live server config (v2)</td></tr>\n<tr><td>Timelines</td><td>May be client-localStorage; sync via <a href=\"#/wiki--api--timelines\" data-wiki-page=\"1\">&lt;code&gt;/api/timelines&lt;/code&gt;</a> separately</td></tr>\n</tbody></table>\n<h3 id=\"hardwareconfig-server-injected-v2\"><code>hardwareConfig</code> (server-injected, v2)</h3>\n<p>Written by the server on <code>POST /api/project/save</code> and <code>POST /api/project/autosave</code>; restored on <code>POST /api/project/load</code> into <code>highascg.config.json</code>.</p>\n<table><thead><tr><th>Key</th><th>Contents</th></tr></thead><tbody>\n<tr><td><code>version</code></td><td><code>2</code></td></tr>\n<tr><td><code>deviceGraph</code></td><td>Device View graph</td></tr>\n<tr><td><code>screenDestinations</code></td><td>PGM/PRV/multiview bindings</td></tr>\n<tr><td><code>osDisplay</code></td><td>GPU xrandr layout (<code>screen_N_os_<em></code>, <code>multiview_os_</em></code>, <code>screen_count</code>, …)</td></tr>\n<tr><td><code>gpuPhysicalTopology</code></td><td>Physical GPU port map (when present)</td></tr>\n<tr><td><code>casparServer</code></td><td>Caspar generator settings dict</td></tr>\n<tr><td><code>audioRouting</code>, <code>streamingChannel</code>, <code>dmx</code>, <code>*Outputs</code></td><td>Routing extras</td></tr>\n<tr><td><code>multiviewLayout</code></td><td>Multiview editor layout</td></tr>\n<tr><td><code>fingerprint</code></td><td><code>{ hostname }</code> — informational for client mismatch UI</td></tr>\n</tbody></table>\n<p><strong>Load does not</strong> run <code>apply-os</code>, regenerate <code>casparcg.config</code>, or restart Caspar — client should call those after applying hardware when heads changed.</p>\n<p>Implementation: <a href=\"../../src/engine/project-hardware-config.js\">&lt;code&gt;src/engine/project-hardware-config.js&lt;/code&gt;</a></p>\n<hr>\n<h2 id=\"get-apiproject\"><code>GET /api/project</code></h2>\n<p>Startup probe — always <strong>200</strong>:</p>\n<ul>\n<li>Existing project → full JSON</li>\n<li>None yet → <code>{}</code> (avoids client boot errors)</li>\n</ul>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/project | jq '.name, .scenes'</code></pre>\n<hr>\n<h2 id=\"get-apiprojectlist\"><code>GET /api/project/list</code></h2>\n<p>Returns the project catalog merged from mounted volumes. Each entry includes a <code>source</code> field indicating which volume supplied the winning copy.</p>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>activeSlug</code></td><td>string \\</td><td>null</td><td>Last loaded slug (persistence)</td></tr>\n<tr><td><code>projects[]</code></td><td>array</td><td>Catalog entries (newest <code>savedAt</code> per slug)</td></tr>\n<tr><td><code>projects[].source</code></td><td><code>&quot;usb&quot;</code> \\</td><td><code>&quot;bridge&quot;</code> \\</td><td><code>&quot;local&quot;</code></td><td>Origin of the listed entry</td></tr>\n<tr><td><code>volumes.usb</code></td><td>object</td><td><code>{ mount, mounted }</code> for <code>HIGHASCGEXF</code></td></tr>\n<tr><td><code>volumes.bridge</code></td><td>object</td><td><code>{ mount, mounted }</code> for <code>HIGHASCGDAT</code></td></tr>\n</tbody></table>\n<p><strong>Catalog rules:</strong></p>\n<ul>\n<li>USB mounted → scan stick <code>projects/</code> as the primary catalog; merge bridge entries when bridge is also mounted.</li>\n<li>USB absent → scan local <code>~/highascg/projects/</code> and merge bridge when mounted.</li>\n<li>Tie on <code>savedAt</code> → USB wins when the stick is mounted (field catalog).</li>\n</ul>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/project/list | jq .</code></pre>\n<pre><code class=\"language-json\">{\n  &quot;activeSlug&quot;: &quot;show-a&quot;,\n  &quot;volumes&quot;: {\n    &quot;usb&quot;: { &quot;mount&quot;: &quot;/home/casparcg/exfat&quot;, &quot;mounted&quot;: true },\n    &quot;bridge&quot;: { &quot;mount&quot;: &quot;/home/casparcg/bridge&quot;, &quot;mounted&quot;: false }\n  },\n  &quot;projects&quot;: [\n    {\n      &quot;slug&quot;: &quot;show-a&quot;,\n      &quot;name&quot;: &quot;Show A&quot;,\n      &quot;savedAt&quot;: &quot;2026-06-07T12:00:00.000Z&quot;,\n      &quot;path&quot;: &quot;/home/casparcg/exfat/projects/show-a.json&quot;,\n      &quot;source&quot;: &quot;usb&quot;\n    }\n  ]\n}</code></pre>\n<p><strong>Load side effect:</strong> <code>POST /api/project/load</code> (and any read by slug) calls <code>pullProjectSlugFromUsbIfNewer</code> — if the stick copy has a newer mtime, it refreshes the working file under <code>~/highascg/projects/</code> before returning JSON.</p>\n<hr>\n<h2 id=\"post-apiprojectsave\"><code>POST /api/project/save</code></h2>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>project</code></td><td>object</td><td><strong>Required</strong> full project</td></tr>\n<tr><td><code>force</code> / <code>allowReplace</code></td><td>boolean</td><td>Bypass stale/replace guards</td></tr>\n<tr><td><code>broadcastProject</code></td><td>boolean</td><td>default true — WebSocket <code>project_sync</code></td></tr>\n</tbody></table>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/project/save \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;project&quot;:{&quot;name&quot;:&quot;Demo Show&quot;,&quot;scenes&quot;:{&quot;scenes&quot;:[]}}}'</code></pre>\n<p><strong>200:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;slug&quot;: &quot;demo-show&quot;,\n  &quot;activeSlug&quot;: &quot;demo-show&quot;,\n  &quot;created&quot;: true\n}</code></pre>\n<p><strong>409</strong> — save rejected (stale tab, unrelated scene set, older <code>savedAt</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;error&quot;: &quot;Project save rejected: …&quot;,\n  &quot;reason&quot;: &quot;stale_saved_at&quot;,\n  &quot;incomingSavedAt&quot;: &quot;…&quot;,\n  &quot;storedSavedAt&quot;: &quot;…&quot;\n}</code></pre>\n<p>Reasons: <code>stale_saved_at</code>, <code>unrelated_scene_set</code>, <code>empty_over_nonempty</code> (autosave).</p>\n<p>Side effects: persists via <code>persistProject</code>, optional Art-Net reconfigure, debounced <strong><code>project_sync</code></strong> WebSocket broadcast.</p>\n<hr>\n<h2 id=\"post-apiprojectload\"><code>POST /api/project/load</code></h2>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>slug</code></td><td>string</td><td>Project file slug; omit to load <strong>active</strong> slug</td></tr>\n<tr><td><code>applyHardware</code></td><td>boolean</td><td>When <code>true</code>, write <code>hardwareConfig</code> into server config (default <strong>false</strong> — verify Device View and apply manually)</td></tr>\n</tbody></table>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/project/load \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;slug&quot;:&quot;demo-show&quot;}'</code></pre>\n<p><strong>200</strong> — full project object. <strong>404</strong> — <code>{ &quot;error&quot;: &quot;No project stored&quot; }</code>.</p>\n<p>Sets active slug in persistence. Does <strong>not</strong> apply project hardware to the server unless <code>applyHardware: true</code>.</p>\n<p><strong>Autosave merge:</strong></p>\n<table><thead><tr><th>Request</th><th>Merges <code>_autosave/&lt;slug&gt;.json</code>?</th></tr></thead><tbody>\n<tr><td>Omit <code>slug</code> (active project / reboot bootstrap)</td><td><strong>Yes</strong> — same as <code>GET /api/project</code> (<code>pickNewerFullProject</code>)</td></tr>\n<tr><td><code>slug</code> set (explicit load of another show)</td><td><strong>No</strong> — main <code>projects/&lt;slug&gt;.json</code> only</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"post-apiprojectautosave\"><code>POST /api/project/autosave</code></h2>\n<p>Same body as save (<code>project</code> required). Stricter validation — rejects empty projects that would wipe stored looks.</p>\n<p><strong>409</strong> messages tuned for stale browser tabs. Expected <strong><code>stale_saved_at</code></strong> when <code>scene_deck_sync</code> already wrote a newer copy — client should refresh from <code>GET /api/project</code> or <code>project_sync</code> WS (see <code>from_client/project-persistence-client-agent.md</code>).</p>\n<p>Triggers USB/bridge push on success. <strong><code>scene_deck_sync</code></strong> (WebSocket) debounces local disk writes (default 750ms, <code>HIGHASCG_SCENE_DECK_SYNC_DEBOUNCE_MS</code>) and does <strong>not</strong> push to USB.</p>\n<hr>\n<h2 id=\"get-apiprojectbundle\"><code>GET /api/project/bundle</code></h2>\n<p>Offline/export snapshot:</p>\n<pre><code class=\"language-json\">{\n  &quot;config&quot;: { },\n  &quot;state&quot;: { },\n  &quot;casparcgConfig&quot;: &quot;&lt;configuration&gt;…&lt;/configuration&gt;&quot;,\n  &quot;mediaManifest&quot;: [\n    { &quot;path&quot;: &quot;clip.mov&quot;, &quot;size&quot;: 12345, &quot;mtime&quot;: 1717512345678 }\n  ]\n}</code></pre>\n<hr>\n<h2 id=\"post-apiprojectreconcile\"><code>POST /api/project/reconcile</code></h2>\n<p>Compares <strong>live scene</strong> layers to server media/template catalogs.</p>\n<p><strong>200:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;reconciliation&quot;: {\n    &quot;usedMedia&quot;: [],\n    &quot;usedTemplates&quot;: [],\n    &quot;missingMedia&quot;: [],\n    &quot;missingTemplates&quot;: [],\n    &quot;isClean&quot;: true\n  }\n}</code></pre>\n<hr>\n<h2 id=\"post-apiprojectsync\"><code>POST /api/project/sync</code></h2>\n<p>Runs <strong>hard-cut scene take</strong> (<code>forceCut: true</code>) for every channel in live scene state (draft → air). Returns per-channel results array.</p>\n<hr>\n<h2 id=\"post-apiprojectapply-bundle\"><code>POST /api/project/apply-bundle</code></h2>\n<p>Applies bundle from pre-show tooling; may rewrite config, exit process, restart Caspar. Body: bundle payload (see internal sync tools).</p>\n<p><strong>200:</strong> <code>{ &quot;ok&quot;: true, &quot;message&quot;: &quot;Bundle applied. HighAsCG and CasparCG restarting.&quot; }</code></p>\n<hr>\n<h2 id=\"websocket\">WebSocket</h2>\n<table><thead><tr><th>Event</th><th>When</th></tr></thead><tbody>\n<tr><td><code>project_sync</code></td><td>After successful save (debounced ~150 ms, <code>HIGHASCG_PROJECT_SYNC_DEBOUNCE_MS</code>)</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"related\">Related</h2>\n<table><thead><tr><th>Doc</th><th>Topic</th></tr></thead><tbody>\n<tr><td><a href=\"#/wiki--api--state-and-media\" data-wiki-page=\"1\">state-and-media.md</a></td><td><code>/api/state</code> snapshot includes <code>scene.deck</code></td></tr>\n<tr><td><a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">scene-take.md</a></td><td>Resolve <code>sceneId</code> from loaded project</td></tr>\n<tr><td><a href=\"#/wiki--api--timelines\" data-wiki-page=\"1\">timelines.md</a></td><td>Timeline CRUD separate from project file</td></tr>\n<tr><td><a href=\"#/wiki--api--system-settings-hardware\" data-wiki-page=\"1\">system-settings-hardware.md</a></td><td>exFAT sync pairs (<code>usb-projects</code>, <code>bridge-projects</code>)</td></tr>\n<tr><td><a href=\"#/BRIDGE_DISK_AND_USB_EXFAT\" data-wiki-page=\"1\">BRIDGE_DISK_AND_USB_EXFAT.md</a></td><td>Volume layout, boot order, seed scripts</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"openapi\">OpenAPI</h2>\n<p><a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a> — <code>ProjectSaveRequest</code>, <code>ProjectLoadRequest</code>, <code>ProjectListResponse</code>.</p>","wiki--api--project-state-media":"<h1 id=\"project-state-media-index\">Project, state &amp; media (index)</h1>\n<p>Split into detailed pages:</p>\n<table><thead><tr><th>Topic</th><th>Doc</th></tr></thead><tbody>\n<tr><td>Project save/load/bundle/sync</td><td><a href=\"#/wiki--api--project\" data-wiki-page=\"1\">&lt;strong&gt;project.md&lt;/strong&gt;</a></td></tr>\n<tr><td>State, variables, selection, media, thumbnails</td><td><a href=\"#/wiki--api--state-and-media\" data-wiki-page=\"1\">&lt;strong&gt;state-and-media.md&lt;/strong&gt;</a></td></tr>\n</tbody></table>\n<p>CG templates: <a href=\"#/wiki--api--cg\" data-wiki-page=\"1\">&lt;strong&gt;cg.md&lt;/strong&gt;</a>.</p>","wiki--api--state-and-media":"<h1 id=\"state-variables-selection-media\">State, variables, selection &amp; media</h1>\n<p>Query and update server snapshots for the operator UI, Companion variables, and the media library.</p>\n<p><strong>Implementation:</strong> <a href=\"../../src/api/routes-state.js\">&lt;code&gt;src/api/routes-state.js&lt;/code&gt;</a> · <a href=\"../../src/api/get-state.js\">&lt;code&gt;src/api/get-state.js&lt;/code&gt;</a> · <a href=\"../../src/api/routes-media.js\">&lt;code&gt;src/api/routes-media.js&lt;/code&gt;</a> · <a href=\"../../src/api/media-catalog.js\">&lt;code&gt;src/api/media-catalog.js&lt;/code&gt;</a></p>\n<p>Project persistence: <a href=\"#/wiki--api--project\" data-wiki-page=\"1\">project.md</a>.</p>\n<hr>\n<h2 id=\"caspar-gate-summary\">Caspar gate summary</h2>\n<table><thead><tr><th>Route</th><th>Caspar required?</th></tr></thead><tbody>\n<tr><td><code>GET /api/variables</code>, <code>/variables/batch</code>, <code>/variables/custom</code></td><td><strong>No</strong></td></tr>\n<tr><td><code>POST /api/variables/batch</code>, <code>/variables/custom</code></td><td><strong>No</strong></td></tr>\n<tr><td><code>POST /api/selection</code></td><td><strong>No</strong></td></tr>\n<tr><td><code>GET /api/project</code>, <code>/media</code>, <code>POST /api/media/cinf</code> (ffprobe fallback)</td><td><strong>No</strong> (cinf prefers AMCP when up)</td></tr>\n<tr><td><code>GET /api/state</code>, <code>/channels</code>, <code>/templates</code>, <code>/fonts</code>, <code>/server/*</code>, <code>/help</code></td><td><strong>Yes</strong></td></tr>\n<tr><td>Thumbnails via Caspar <code>THUMBNAIL RETRIEVE</code></td><td><strong>Yes</strong> (local PNG cache may work without)</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"get-apistate\"><code>GET /api/state</code></h2>\n<p>Full UI bootstrap snapshot (requires Caspar).</p>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/state | jq 'keys'</code></pre>\n<p>Query: <code>?full_cinf=1</code> — enrich all media entries with CINF (can be heavy).</p>\n<h3 id=\"top-level-fields-typical\">Top-level fields (typical)</h3>\n<table><thead><tr><th>Key</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>variables</code></td><td>Companion/UI variable map</td></tr>\n<tr><td><code>channels</code></td><td>Channel id list</td></tr>\n<tr><td><code>channelStatus</code></td><td>Status lines per channel</td></tr>\n<tr><td><code>media</code></td><td>Media catalog (id, label, duration, resolution, …)</td></tr>\n<tr><td><code>templates</code></td><td>CG template list</td></tr>\n<tr><td><code>caspar</code></td><td><code>{ connected, host, port }</code></td></tr>\n<tr><td><code>channelMap</code></td><td>PGM/PRV routing resolved for UI</td></tr>\n<tr><td><code>scene</code></td><td><code>live</code>, <code>deck</code>, <code>programLayerBankByChannel</code>, <code>globalBorders</code></td></tr>\n<tr><td><code>timeline</code></td><td><code>list</code>, <code>playback</code></td></tr>\n<tr><td><code>playback.matrix</code></td><td>Layer play tracker</td></tr>\n<tr><td><code>screenDestinations</code></td><td>Output routing metadata</td></tr>\n<tr><td><code>osc</code></td><td>OSC snapshot when enabled</td></tr>\n</tbody></table>\n<p>WebSocket may send slim catalog first (<code>catalogDeferred: true</code>) then chunk media separately.</p>\n<hr>\n<h2 id=\"variables\">Variables</h2>\n<h3 id=\"get-apivariables\"><code>GET /api/variables</code></h3>\n<p>All variables. Query <code>?prefix=ui_</code> filters keys.</p>\n<h3 id=\"get-apivariablesbatchcategoriesuichannel\"><code>GET /api/variables/batch?categories=ui,channel</code></h3>\n<p>Returns variables whose keys start with <code>category_</code> (adds <code>_</code> if missing).</p>\n<h3 id=\"post-apivariablesbatch\"><code>POST /api/variables/batch</code></h3>\n<pre><code class=\"language-json\">{ &quot;keys&quot;: [&quot;ui_selection_context&quot;, &quot;channel_1_framerate&quot;] }</code></pre>\n<p>Max <strong>2000</strong> keys — <strong>400</strong> if exceeded. Returns only keys that exist.</p>\n<h3 id=\"get-apivariablescustom\"><code>GET /api/variables/custom</code></h3>\n<pre><code class=\"language-json\">{ &quot;labels&quot;: { &quot;my_var&quot;: &quot;Friendly name&quot; } }</code></pre>\n<h3 id=\"post-apivariablescustom\"><code>POST /api/variables/custom</code></h3>\n<pre><code class=\"language-json\">{ &quot;labels&quot;: { &quot;my_var&quot;: &quot;New label&quot;, &quot;old_var&quot;: null } }</code></pre>\n<p><code>null</code> or <code>&quot;&quot;</code> removes a label. Persisted on disk.</p>\n<hr>\n<h2 id=\"post-apiselection\"><code>POST /api/selection</code></h2>\n<p>Sync UI inspector selection into variables (<strong>no Caspar</strong>).</p>\n<pre><code class=\"language-json\">{\n  &quot;context&quot;: &quot;scene_layer&quot;,\n  &quot;lookId&quot;: &quot;sc_abc&quot;,\n  &quot;layerIndex&quot;: 2,\n  &quot;previewChannel&quot;: 2,\n  &quot;casparLayer&quot;: 10\n}</code></pre>\n<p>Drives mixer <a href=\"#/wiki--api--mixer\" data-wiki-page=\"1\">UI integration</a> (<code>ui_selection_*</code> keys). Debounced ~100 ms in the client.</p>\n<p><strong>200:</strong> <code>{ &quot;ok&quot;: true }</code></p>\n<hr>\n<h2 id=\"channels-server-introspection\">Channels &amp; server introspection</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/channels</code></td><td><code>ids</code>, <code>status</code>, <code>channelXml</code></td></tr>\n<tr><td>GET</td><td><code>/api/channels/{n}</code></td><td><code>INFO</code> channel</td></tr>\n<tr><td>GET</td><td><code>/api/channels/{n}/delay</code></td><td><code>INFO</code> delay</td></tr>\n<tr><td>GET</td><td><code>/api/templates</code></td><td>Template catalog</td></tr>\n<tr><td>GET</td><td><code>/api/config</code></td><td>Caspar <code>INFO CONFIG</code> XML (text/xml)</td></tr>\n<tr><td>GET</td><td><code>/api/fonts</code></td><td><code>FLS</code> font list</td></tr>\n<tr><td>GET</td><td><code>/api/server</code></td><td>Server info</td></tr>\n<tr><td>GET</td><td><code>/api/server/queues</code></td><td>Queue stats</td></tr>\n<tr><td>GET</td><td><code>/api/server/threads</code></td><td>Thread stats</td></tr>\n<tr><td>GET</td><td><code>/api/server/gl</code></td><td>OpenGL info</td></tr>\n<tr><td>GET</td><td><code>/api/help</code></td><td>AMCP HELP</td></tr>\n<tr><td>GET</td><td><code>/api/help/{command}</code></td><td>HELP for one command</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"media-catalog\">Media catalog</h2>\n<h3 id=\"get-apimedia\"><code>GET /api/media</code></h3>\n<p>Media index (CLS + disk scan). Works <strong>without Caspar</strong>.</p>\n<pre><code class=\"language-bash\">curl -s 'http://127.0.0.1:4200/api/media?full_cinf=1' | jq '.[0]'</code></pre>\n<p>Entries typically include <code>id</code>, <code>label</code>, optional <code>durationMs</code>, <code>resolution</code>, <code>fps</code> after enrichment.</p>\n<h3 id=\"post-apimediacinf\"><code>POST /api/media/cinf</code></h3>\n<p>Duration/metadata for one file (timeline drop):</p>\n<pre><code class=\"language-json\">{ &quot;id&quot;: &quot;media/clip.mov&quot; }</code></pre>\n<p>Also accepts <code>filename</code>. Uses Caspar <code>CINF</code> when connected; <strong>ffprobe</strong> fallback on disk when AMCP is down.</p>\n<p><strong>200:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;id&quot;: &quot;media/clip.mov&quot;,\n  &quot;durationMs&quot;: 120000,\n  &quot;cinf&quot;: &quot;…&quot;,\n  &quot;source&quot;: &quot;cinf&quot;\n}</code></pre>\n<h3 id=\"post-apimediarefresh\"><code>POST /api/media/refresh</code></h3>\n<p>Triggers CLS/TLS media scan cycle.</p>\n<h3 id=\"post-apimediadelete\"><code>POST /api/media/delete</code></h3>\n<pre><code class=\"language-json\">{ &quot;id&quot;: &quot;media/old.mov&quot; }</code></pre>\n<p>Prefer over <code>DELETE /api/local-media/...</code> when paths contain slashes.</p>\n<h3 id=\"post-apimediamkdir-post-apimediamove\"><code>POST /api/media/mkdir</code> · <code>POST /api/media/move</code></h3>\n<pre><code class=\"language-json\">{ &quot;path&quot;: &quot;folder/sub&quot; }</code></pre>\n<pre><code class=\"language-json\">{ &quot;sourceId&quot;: &quot;a.mov&quot;, &quot;targetId&quot;: &quot;folder/a.mov&quot; }</code></pre>\n<h3 id=\"get-apilocal-mediapath\"><code>GET /api/local-media/{path}</code></h3>\n<p>Serve file from configured local media root.</p>\n<h3 id=\"delete-apilocal-mediapath\"><code>DELETE /api/local-media/{path}</code></h3>\n<p>Delete by URL path (encoding pitfalls — prefer <code>POST /api/media/delete</code>).</p>\n<hr>\n<h2 id=\"thumbnails\">Thumbnails</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/thumbnails</code></td><td>Caspar thumbnail index</td></tr>\n<tr><td>GET</td><td><code>/api/thumbnail?filename=</code></td><td>One image (query)</td></tr>\n<tr><td>GET</td><td><code>/api/thumbnail/{filename}</code></td><td>One image (path)</td></tr>\n<tr><td>GET</td><td><code>/api/thumbnail/live/{channel}</code></td><td>Cached PRINT still</td></tr>\n<tr><td>POST</td><td><code>/api/thumbnails/generate</code></td><td>Generate one</td></tr>\n<tr><td>POST</td><td><code>/api/thumbnails/generate-all</code></td><td>Generate all</td></tr>\n<tr><td>POST</td><td><code>/api/thumbnail/live/capture</code></td><td>Capture from PRINT</td></tr>\n<tr><td>POST</td><td><code>/api/thumbnail/live/upload</code></td><td>Upload still</td></tr>\n</tbody></table>\n<p>Thumbnail GET tries <strong>local ffmpeg cache</strong> first (<code>hq=1</code> default); Caspar retrieve with <code>?fallback=1</code>.</p>\n<p>Query params: <code>w</code> (max width), <code>t</code> (seek seconds), <code>hq</code>, <code>fallback</code>.</p>\n<hr>\n<h2 id=\"examples\">Examples</h2>\n<pre><code class=\"language-bash\"># Bootstrap UI (Caspar up)\ncurl -s http://127.0.0.1:4200/api/state | jq '{caspar, scene: .scene.live, timelines: .timeline.list | length}'\n\n# Media without Caspar\ncurl -s http://127.0.0.1:4200/api/media | jq 'length'\n\n# Clip duration for timeline\ncurl -s -X POST http://127.0.0.1:4200/api/media/cinf \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;id&quot;:&quot;AMB&quot;}' | jq .\n\n# Companion variable batch\ncurl -s -X POST http://127.0.0.1:4200/api/variables/batch \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;keys&quot;:[&quot;channel_1_framerate&quot;,&quot;ui_selection_context&quot;]}'</code></pre>\n<hr>\n<h2 id=\"legacy\">Legacy</h2>\n<p><code>POST/GET /api/data/store|retrieve|list|remove</code> → <strong>410</strong> with message to use project save/load.</p>\n<hr>\n<h2 id=\"openapi\">OpenAPI</h2>\n<p><a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a> — <code>StateSnapshot</code>, <code>VariablesBatchRequest</code>, <code>MediaCinfRequest</code>, <code>SelectionPayload</code>.</p>","wiki--api--timelines":"<h1 id=\"timelines-api\">Timelines API</h1>\n<p>Multi-track timelines with clip keyframes, playhead transport, and Caspar output on <strong>layers 200+</strong> (above look stacks 1–199). The server holds timelines in the <strong>timeline engine</strong> (in-memory); the operator UI syncs edits via <code>PUT</code> / <code>POST</code> before transport.</p>\n<p><strong>Caspar:</strong> all <code>/api/timelines/*</code> routes are registered <strong>after</strong> the AMCP gate — Caspar must be connected (<code>503</code> otherwise).</p>\n<p><strong>Engine:</strong> if <code>timelineEngine</code> is not initialized, every timeline route returns <code>503</code> <code>{ &quot;error&quot;: &quot;Timeline engine not ready&quot; }</code>.</p>\n<p><strong>Implementation:</strong> <a href=\"../../src/api/routes-timeline.js\">&lt;code&gt;src/api/routes-timeline.js&lt;/code&gt;</a> · <a href=\"../../src/engine/timeline-engine.js\">&lt;code&gt;src/engine/timeline-engine.js&lt;/code&gt;</a> · AMCP sync: <a href=\"../../src/engine/timeline-playback-amcp.js\">&lt;code&gt;src/engine/timeline-playback-amcp.js&lt;/code&gt;</a></p>\n<p><strong>Related:</strong> scene take with timeline-only looks → <a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">&lt;code&gt;scene-take.md&lt;/code&gt;</a>. Multiview / NDI → <a href=\"#/wiki--api--timelines-multiview\" data-wiki-page=\"1\">timelines-multiview.md</a>.</p>\n<hr>\n<h2 id=\"base-url\">Base URL</h2>\n<pre><code class=\"language-http\">http://127.0.0.1:4200/api/timelines\nContent-Type: application/json</code></pre>\n<p>Path actions use <strong><code>POST /api/timelines/{id}/{action}</code></strong> (not separate top-level paths).</p>\n<hr>\n<h2 id=\"overview\">Overview</h2>\n<table><thead><tr><th>Concern</th><th>Detail</th></tr></thead><tbody>\n<tr><td>Storage</td><td>Server <code>TimelineEngine.timelines</code> map (lost on restart unless clients re-<code>PUT</code>)</td></tr>\n<tr><td>Caspar layers</td><td>Layer index <code>i</code> → Caspar <code>200 + i</code> on each target channel</td></tr>\n<tr><td>Routing</td><td><code>sendTo</code>: <code>{ preview, program, screenIdx }</code> → PGM/PRV channels from <code>channel_map</code></td></tr>\n<tr><td>State</td><td><code>GET /api/state</code> includes <code>timelines[]</code> and <code>timelinePlayback</code></td></tr>\n<tr><td>WebSocket</td><td><code>timeline.playback</code>, throttled <code>timeline.tick</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"timeline-document-model\">Timeline document model</h2>\n<h3 id=\"timeline-object\">Timeline object</h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>id</code></td><td>string</td><td>auto <code>tl…</code></td><td>Unique id</td></tr>\n<tr><td><code>name</code></td><td>string</td><td><code>&quot;Timeline&quot;</code></td><td>Display name</td></tr>\n<tr><td><code>duration</code></td><td>number</td><td><code>30000</code></td><td>Timeline length (ms)</td></tr>\n<tr><td><code>fps</code></td><td>number</td><td><code>25</code></td><td>Timecode / frame math</td></tr>\n<tr><td><code>flags</code></td><td>array</td><td><code>[]</code></td><td>Playhead markers (pause/play/jump/companion)</td></tr>\n<tr><td><code>layers</code></td><td>array</td><td>3 empty layers</td><td>Stack of timeline layers</td></tr>\n</tbody></table>\n<h3 id=\"layer\">Layer</h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>id</code></td><td>string</td><td>Layer id</td></tr>\n<tr><td><code>name</code></td><td>string</td><td>e.g. <code>&quot;Layer 1&quot;</code></td></tr>\n<tr><td><code>clips</code></td><td>array</td><td>Clips on this track</td></tr>\n</tbody></table>\n<h3 id=\"clip\">Clip</h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>id</code></td><td>string</td><td>Clip id</td></tr>\n<tr><td><code>source</code></td><td>object</td><td><code>{ type, value, resolution? }</code> — media path, route, etc.</td></tr>\n<tr><td><code>startTime</code></td><td>number</td><td>Start on timeline (ms)</td></tr>\n<tr><td><code>duration</code></td><td>number</td><td>Clip length on timeline (ms)</td></tr>\n<tr><td><code>inPoint</code></td><td>number</td><td>Media in-point (ms)</td></tr>\n<tr><td><code>outPoint</code></td><td>number \\</td><td>null</td><td>Media out-point</td></tr>\n<tr><td><code>keyframes</code></td><td>array</td><td><code>{ time, property, value, easing }</code> — local ms inside clip</td></tr>\n<tr><td><code>audioRoute</code></td><td>string</td><td><code>1+2</code>, <code>3+4</code>, …</td></tr>\n<tr><td><code>volume</code>, <code>muted</code></td><td>various</td><td>Audio</td></tr>\n<tr><td><code>fillPx</code></td><td>object</td><td><code>{ x, y, w, h }</code> pixel rect on program canvas</td></tr>\n<tr><td><code>contentFit</code>, <code>aspectLocked</code></td><td>various</td><td>Layout</td></tr>\n<tr><td><code>startBehaviour</code></td><td>string</td><td><code>beginning</code> \\</td><td><code>relativeToPrevious</code> (scene take sync)</td></tr>\n<tr><td><code>loop</code>, <code>loopAlways</code></td><td>boolean</td><td>Loop playback</td></tr>\n</tbody></table>\n<h3 id=\"flag-marker\">Flag (marker)</h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>id</code></td><td>string</td><td>Flag id</td></tr>\n<tr><td><code>timeMs</code></td><td>number</td><td>Position on timeline</td></tr>\n<tr><td><code>type</code></td><td>string</td><td><code>pause</code>, <code>play</code>, <code>jump</code>, <code>companion_press</code></td></tr>\n<tr><td><code>jumpTimeMs</code></td><td>number</td><td>Jump target (for <code>jump</code>)</td></tr>\n<tr><td><code>jumpFlagId</code></td><td>string</td><td>Jump to another flag’s time</td></tr>\n<tr><td><code>companionPage</code>, <code>companionRow</code>, <code>companionColumn</code></td><td>numbers</td><td>For <code>companion_press</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"crud\">CRUD</h2>\n<h3 id=\"get-apitimelines\"><code>GET /api/timelines</code></h3>\n<p>Returns <strong>array</strong> of all timeline objects.</p>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/timelines | jq .</code></pre>\n<h3 id=\"post-apitimelines\"><code>POST /api/timelines</code></h3>\n<ul>\n<li><strong>No <code>id</code> or unknown id:</strong> <code>create(body)</code> — new timeline returned.</li>\n<li><strong><code>id</code> present and exists:</strong> <code>update(id, body)</code> — replaces stored timeline.</li>\n</ul>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/timelines \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    &quot;name&quot;: &quot;Show open&quot;,\n    &quot;duration&quot;: 60000,\n    &quot;fps&quot;: 50,\n    &quot;layers&quot;: [\n      {\n        &quot;id&quot;: &quot;layer1&quot;,\n        &quot;name&quot;: &quot;Layer 1&quot;,\n        &quot;clips&quot;: [\n          {\n            &quot;id&quot;: &quot;c1&quot;,\n            &quot;source&quot;: { &quot;type&quot;: &quot;media&quot;, &quot;value&quot;: &quot;opener.mov&quot; },\n            &quot;startTime&quot;: 0,\n            &quot;duration&quot;: 10000\n          }\n        ]\n      }\n    ]\n  }'</code></pre>\n<h3 id=\"get-apitimelinesid\"><code>GET /api/timelines/{id}</code></h3>\n<p>Single timeline or <strong><code>404</code></strong> <code>{ &quot;error&quot;: &quot;Not found&quot; }</code>.</p>\n<h3 id=\"put-apitimelinesid\"><code>PUT /api/timelines/{id}</code></h3>\n<p>Update existing id, or <strong>create</strong> with that id if missing (upsert).</p>\n<pre><code class=\"language-bash\">curl -s -X PUT http://127.0.0.1:4200/api/timelines/tl_abc123 \\\n  -H 'Content-Type: application/json' \\\n  -d @timeline.json</code></pre>\n<p>Client sync pattern (editor): <code>PUT</code> first, <code>POST /api/timelines</code> fallback.</p>\n<h3 id=\"delete-apitimelinesid\"><code>DELETE /api/timelines/{id}</code></h3>\n<p>Stops playback if this timeline was active, removes from engine.</p>\n<pre><code class=\"language-json\">{ &quot;ok&quot;: true }</code></pre>\n<hr>\n<h2 id=\"playback-state\">Playback state</h2>\n<h3 id=\"get-apitimelinesidstate\"><code>GET /api/timelines/{id}/state</code></h3>\n<p>Current engine playback (any timeline id in path; returns global playback state):</p>\n<pre><code class=\"language-json\">{\n  &quot;timelineId&quot;: &quot;tl_abc123&quot;,\n  &quot;position&quot;: 4520,\n  &quot;playing&quot;: true,\n  &quot;loop&quot;: false,\n  &quot;sendTo&quot;: {\n    &quot;preview&quot;: true,\n    &quot;program&quot;: false,\n    &quot;screenIdx&quot;: 0\n  }\n}</code></pre>\n<p><code>position</code> is computed live when <code>playing: true</code>.</p>\n<p>Also available on <strong><code>GET /api/state</code></strong> as <code>timelinePlayback</code>.</p>\n<hr>\n<h2 id=\"transport-actions\">Transport actions</h2>\n<p>All are <strong><code>POST /api/timelines/{id}/&lt;action&gt;</code></strong>.</p>\n<h3 id=\"play\"><code>play</code></h3>\n<p>Start or resume playback.</p>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>from</code></td><td>number</td><td>Start position (ms); default resume position or <code>0</code></td></tr>\n<tr><td><code>sendTo</code></td><td>object</td><td>Optional — set routing before play (same shape as <code>sendto</code>)</td></tr>\n</tbody></table>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/timelines/tl_abc123/play \\\n  -H 'Content-Type: application/json' \\\n  -d '{\n    &quot;from&quot;: 0,\n    &quot;sendTo&quot;: { &quot;preview&quot;: true, &quot;program&quot;: false, &quot;screenIdx&quot;: 0 }\n  }'</code></pre>\n<p>Response: <code>{ &quot;ok&quot;: true }</code>.</p>\n<p>Applies active clips to Caspar on target channels (layers 200+). Ticker runs every <strong>40 ms</strong>; AMCP updates on clip changes / keyframe segments, not every tick.</p>\n<h3 id=\"pause\"><code>pause</code></h3>\n<p>Pauses ticker and Caspar layers on timeline stack.</p>\n<h3 id=\"stop\"><code>stop</code></h3>\n<p>Stops ticker, resets position to <strong>0</strong>, clears timeline Caspar layers (<code>_stopAll</code>).</p>\n<h3 id=\"seek\"><code>seek</code></h3>\n<table><thead><tr><th>Field</th><th>Type</th><th>Required</th></tr></thead><tbody>\n<tr><td><code>ms</code></td><td>number</td><td>yes, <code>&gt;= 0</code></td></tr>\n</tbody></table>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/timelines/tl_abc123/seek \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;ms&quot;: 5000}'</code></pre>\n<p><strong>400</strong> if <code>ms</code> missing or invalid: <code>{ &quot;error&quot;: &quot;ms required (number &gt;= 0)&quot; }</code>.</p>\n<h3 id=\"loop\"><code>loop</code></h3>\n<pre><code class=\"language-json\">{ &quot;loop&quot;: true }</code></pre>\n<p>Sets loop flag for active playback of this timeline id.</p>\n<h3 id=\"sendto\"><code>sendto</code></h3>\n<p>Update preview/program routing without starting transport.</p>\n<pre><code class=\"language-json\">{\n  &quot;preview&quot;: true,\n  &quot;program&quot;: false,\n  &quot;screenIdx&quot;: 0\n}</code></pre>\n<table><thead><tr><th><code>screenIdx</code></th><th>Meaning</th></tr></thead><tbody>\n<tr><td><code>0</code>, <code>1</code>, …</td><td>Single main screen (0-based)</td></tr>\n<tr><td><code>null</code></td><td>All screens (used internally when take uses <code>all</code>)</td></tr>\n</tbody></table>\n<p>Removes AMCP from channels dropped when toggling <code>sendTo</code> off.</p>\n<hr>\n<h2 id=\"post-apitimelinesidtake\"><code>POST /api/timelines/{id}/take</code></h2>\n<p>Air timeline to <strong>program</strong> (and preview during staging): clears look stacks on PGM/PRV, then plays from current or resumed position.</p>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>screenIdx</code></td><td>number \\</td><td><code>&quot;all&quot;</code> \\</td><td><code>null</code></td><td><code>0</code> = first screen; <code>&quot;all&quot;</code> or <code>null</code> = every <code>screenCount</code> screen</td></tr>\n</tbody></table>\n<p><strong>Per screen:</strong> clears scene look layers <strong>1–199</strong> on program + preview channels, <code>MIXER COMMIT</code>, clears live scene state, then:</p>\n<pre><code class=\"language-javascript\">eng.setSendTo({ preview: true, program: true, screenIdx })\neng.play(id, currentPosition)\nliveSceneState.broadcastSceneLive(ctx)</code></pre>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/timelines/tl_abc123/take \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;screenIdx&quot;: 0}'</code></pre>\n<p>All screens:</p>\n<pre><code class=\"language-json\">{ &quot;screenIdx&quot;: &quot;all&quot; }</code></pre>\n<table><thead><tr><th>Status</th><th>Condition</th></tr></thead><tbody>\n<tr><td><code>404</code></td><td>Unknown timeline id</td></tr>\n<tr><td><code>503</code></td><td>Caspar not connected (inside take handler)</td></tr>\n</tbody></table>\n<p><strong>Note:</strong> The UI may send <code>transition</code>, <code>duration</code>, <code>tween</code> on take; the server handler <strong>does not</strong> use them today — take always uses the engine’s direct play path after stack clear.</p>\n<hr>\n<h2 id=\"sendto-and-channel-mapping\"><code>sendTo</code> and channel mapping</h2>\n<p>Resolved in <a href=\"../../src/engine/timeline-playback-amcp.js\">&lt;code&gt;timeline-playback-amcp.js&lt;/code&gt;</a> <code>_channelsFor</code>:</p>\n<ul>\n<li><code>preview: true</code> → <code>previewCh(screen)</code> or bus mapping</li>\n<li><code>program: true</code> → <code>programCh(screen)</code></li>\n<li><code>screenIdx: null</code> → iterate all <code>screenCount</code> screens</li>\n</ul>\n<p>Default when playback starts: <code>{ preview: true, program: false, screenIdx: 0 }</code>.</p>\n<hr>\n<h2 id=\"scene-integration\">Scene integration</h2>\n<table><thead><tr><th>Path</th><th>Behaviour</th></tr></thead><tbody>\n<tr><td>Look layer <code>source.type === &quot;timeline&quot;</code></td><td><a href=\"#/wiki--api--scene-take\" data-wiki-page=\"1\">&lt;code&gt;POST /api/scene/take&lt;/code&gt;</a> runs timeline engine instead of LBG</td></tr>\n<tr><td>Timeline-only look</td><td>All content layers reference the same timeline id</td></tr>\n<tr><td><code>startBehaviour</code></td><td><code>relativeToPrevious</code> — playhead frame passed as <code>playSeekFrames</code> on take</td></tr>\n</tbody></table>\n<p>Timeline ids in looks must exist on the server (<code>eng.get(tlId)</code>).</p>\n<hr>\n<h2 id=\"websocket-events\">WebSocket events</h2>\n<table><thead><tr><th>Event</th><th>Payload</th></tr></thead><tbody>\n<tr><td><code>timeline.playback</code></td><td>Full <code>getPlayback()</code> snapshot on play/pause/stop/seek/sendto</td></tr>\n<tr><td><code>timeline.tick</code></td><td><code>{ timelineId, position }</code> ~165 ms while playing</td></tr>\n</tbody></table>\n<p>Clients extrapolate playhead between ticks.</p>\n<hr>\n<h2 id=\"examples-operator-flow\">Examples (operator flow)</h2>\n<ol>\n<li><strong>Upload timeline JSON</strong></li>\n</ol>\n<pre><code class=\"language-bash\">curl -s -X PUT http://127.0.0.1:4200/api/timelines/my_show \\\n  -H 'Content-Type: application/json' \\\n  -d @my_show_timeline.json</code></pre>\n<ol>\n<li><strong>Preview on PRV</strong></li>\n</ol>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/timelines/my_show/play \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;from&quot;:0,&quot;sendTo&quot;:{&quot;preview&quot;:true,&quot;program&quot;:false,&quot;screenIdx&quot;:0}}'</code></pre>\n<ol>\n<li><strong>Take to PGM</strong></li>\n</ol>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/timelines/my_show/take \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;screenIdx&quot;:0}'</code></pre>\n<ol>\n<li><strong>Poll position</strong></li>\n</ol>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/timelines/my_show/state | jq .position</code></pre>\n<hr>\n<h2 id=\"openapi\">OpenAPI</h2>\n<p>Machine-readable schemas: <a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a> — <code>Timeline</code>, <code>TimelinePlayback</code>, transport request bodies.</p>","wiki--api--timelines-multiview":"<h1 id=\"multiview-ndi\">Multiview &amp; NDI</h1>\n<h2 id=\"timelines\">Timelines</h2>\n<p><strong>Full reference:</strong> <a href=\"#/wiki--api--timelines\" data-wiki-page=\"1\">timelines.md</a> — CRUD, transport (<code>play</code>/<code>pause</code>/<code>stop</code>/<code>seek</code>), <code>sendTo</code>, <code>take</code>, data model, WebSocket.</p>\n<h2 id=\"multiview\">Multiview</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>POST</td><td><code>/api/multiview/apply</code></td></tr>\n</tbody></table>\n<p>Applies multiview layout from persisted project/config (see <code>routes-multiview.js</code>).</p>\n<h2 id=\"ndi\">NDI</h2>\n<p><strong>Caspar:</strong> required.</p>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/ndi/list</code></td></tr>\n</tbody></table>\n<p>Lists NDI sources available to Caspar/FFmpeg consumers.</p>","wiki--api--streaming-audio-osc":"<h1 id=\"streaming-audio-osc\">Streaming, audio &amp; OSC</h1>\n<h2 id=\"webrtc-preview-streams\">WebRTC / preview streams</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Caspar</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/streams</code></td><td>optional</td><td>Pipeline status, ports</td></tr>\n<tr><td>GET</td><td><code>/api/streaming/ndi-sources</code></td><td>optional</td><td>NDI source names for streaming UI</td></tr>\n<tr><td>POST</td><td><code>/api/streaming/toggle</code></td><td>optional</td><td>Start/stop consumers</td></tr>\n<tr><td>POST</td><td><code>/api/streaming/restart</code></td><td>optional</td><td>Restart streaming</td></tr>\n</tbody></table>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/streams | jq .</code></pre>\n<h2 id=\"streaming-channel-rtmp-record\">Streaming channel (RTMP / record)</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/streaming-channel</code></td></tr>\n<tr><td>POST</td><td><code>/api/streaming-channel/rtmp</code></td></tr>\n<tr><td>POST</td><td><code>/api/streaming-channel/record</code></td></tr>\n</tbody></table>\n<h2 id=\"audio\">Audio</h2>\n<p>Most routes work <strong>without Caspar</strong> for enumeration; routing to Caspar needs AMCP when applying filters.</p>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/audio/devices</code></td><td>ALSA/PipeWire devices</td></tr>\n<tr><td>GET</td><td><code>/api/audio/portaudio-devices</code></td><td>PortAudio enumeration</td></tr>\n<tr><td>GET</td><td><code>/api/audio/live-inputs</code></td><td>Live input config</td></tr>\n<tr><td>POST</td><td><code>/api/audio/config</code></td><td>Persist <code>audioRouting</code></td></tr>\n<tr><td>POST</td><td><code>/api/audio/volume</code></td><td>Layer/master volume</td></tr>\n<tr><td>POST</td><td><code>/api/audio/route</code></td><td>Route buses</td></tr>\n<tr><td>POST</td><td><code>/api/audio/default-device</code></td><td>Default output device</td></tr>\n<tr><td>POST</td><td><code>/api/audio/monitor-source</code></td><td>Browser monitor bus</td></tr>\n<tr><td>POST</td><td><code>/api/audio/solo</code></td><td>Solo layers</td></tr>\n<tr><td>POST</td><td><code>/api/audio/live-inputs/apply</code></td><td>Apply live inputs to Caspar</td></tr>\n<tr><td>POST</td><td><code>/api/audio/live-inputs/config</code></td><td>Save live input mapping</td></tr>\n</tbody></table>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/audio/devices | jq .</code></pre>\n<h2 id=\"osc-caspar-highascg\">OSC (Caspar → HighAsCG)</h2>\n<p>UDP listener on <strong><code>OSC_LISTEN_PORT</code></strong> (default <strong>6251</strong>). HTTP exposes aggregated state:</p>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/osc/state</code></td></tr>\n<tr><td>GET</td><td><code>/api/osc/diagnostics</code></td></tr>\n<tr><td>GET</td><td><code>/api/osc/profiler</code></td></tr>\n<tr><td>GET</td><td><code>/api/osc/outputs</code></td></tr>\n<tr><td>GET</td><td><code>/api/osc/config-hint</code></td></tr>\n</tbody></table>\n<p>See <a href=\"#/osc-integration\" data-wiki-page=\"1\">osc-integration.md</a>.</p>\n<h2 id=\"artnet\">Artnet</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/artnet/input</code></td></tr>\n</tbody></table>","wiki--api--system-settings-hardware":"<h1 id=\"system-settings-hardware\">System, settings &amp; hardware</h1>\n<h2 id=\"settings\">Settings</h2>\n<p><strong>Caspar:</strong> not required.</p>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/settings</code></td><td>Full settings JSON</td></tr>\n<tr><td>POST</td><td><code>/api/settings</code></td><td>Persist settings</td></tr>\n<tr><td>POST</td><td><code>/api/settings/apply-os</code></td><td>Apply OS-related settings (GPU, etc.)</td></tr>\n</tbody></table>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/settings | jq .caspar.host</code></pre>\n<h2 id=\"config-highascg-app-config\">Config (HighAsCG app config)</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>POST</td><td><code>/api/config/apply</code></td></tr>\n<tr><td>POST</td><td><code>/api/config/reset</code></td></tr>\n</tbody></table>\n<h2 id=\"caspar-xml-config\">Caspar XML config</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/caspar-config/generate</code></td></tr>\n<tr><td>GET</td><td><code>/api/caspar-config/mode-choices</code></td></tr>\n<tr><td>GET</td><td><code>/api/caspar-config/override</code></td></tr>\n<tr><td>POST</td><td><code>/api/caspar-config/apply</code></td></tr>\n<tr><td>POST</td><td><code>/api/caspar-config/override</code></td></tr>\n</tbody></table>\n<h2 id=\"gpu-hardware\">GPU &amp; hardware</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/hardware/displays</code></td><td>Display names (xrandr)</td></tr>\n<tr><td>GET</td><td><code>/api/hardware/modeline-preview</code></td><td>Modeline preview</td></tr>\n<tr><td>GET</td><td><code>/api/system/gpu-nvidia</code></td><td>NVIDIA probe</td></tr>\n<tr><td>GET</td><td><code>/api/system/gpu-layout</code></td><td>GPU port layout</td></tr>\n<tr><td>GET</td><td><code>/api/system/decklink</code></td><td>Decklink status</td></tr>\n<tr><td>GET</td><td><code>/api/system/v4l2-devices</code></td><td>V4L2 capture devices (<code>?refresh=1</code>)</td></tr>\n<tr><td>GET</td><td><code>/api/v4l2-inputs</code></td><td>USB video input config + bridge status</td></tr>\n<tr><td>POST</td><td><code>/api/v4l2-inputs/config</code></td><td>Persist V4L2 slot settings</td></tr>\n<tr><td>POST</td><td><code>/api/v4l2-inputs/apply</code></td><td>Start bridges + PLAY on host channels</td></tr>\n<tr><td>GET</td><td><code>/api/virtual-camera</code></td><td>Virtual cam config + runtime status</td></tr>\n<tr><td>GET</td><td><code>/api/virtual-camera/status</code></td><td>Alias</td></tr>\n<tr><td>POST</td><td><code>/api/virtual-camera/config</code></td><td>Persist virtual cam settings</td></tr>\n<tr><td>POST</td><td><code>/api/virtual-camera/start</code></td><td>Start v4l2loopback bridge (+ optional ALSA mic)</td></tr>\n<tr><td>POST</td><td><code>/api/virtual-camera/stop</code></td><td>Stop bridge</td></tr>\n<tr><td>POST</td><td><code>/api/system/gpu-nvidia/apply</code></td><td>Apply NVIDIA layout</td></tr>\n<tr><td>POST</td><td><code>/api/system/gpu-ports-reset</code></td><td>Reset port mapping</td></tr>\n<tr><td>POST</td><td><code>/api/system/gui-launch</code></td><td>Launch GUI helper</td></tr>\n</tbody></table>\n<h2 id=\"device-view-snapshot\">Device view &amp; snapshot</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/device-view</code></td></tr>\n<tr><td>GET</td><td><code>/api/device-view/gpu-map-debug</code></td></tr>\n<tr><td>POST</td><td><code>/api/device-view</code></td></tr>\n<tr><td>GET</td><td><code>/api/device-snapshot/schema</code></td></tr>\n<tr><td>GET</td><td><code>/api/device-snapshot/build</code></td></tr>\n<tr><td>POST</td><td><code>/api/device-snapshot/apply</code></td></tr>\n</tbody></table>\n<h2 id=\"system-setup-caspar-staging\">System setup &amp; Caspar staging</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Notes</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/system/setup</code></td><td>Setup wizard state (includes compact <code>tailscale</code> block)</td></tr>\n<tr><td>POST</td><td><code>/api/system/setup/restart-window-manager</code></td><td>nodm restart (password)</td></tr>\n<tr><td>POST</td><td><code>/api/system/setup/restart-app</code></td><td>Restart HighAsCG Node process</td></tr>\n<tr><td>POST</td><td><code>/api/system/setup/reboot</code></td><td>Reboot host (password)</td></tr>\n<tr><td>POST</td><td><code>/api/system/setup/install</code></td><td>Launch Calamares installer — see <a href=\"#/CALAMARES_INSTALL_TO_DISK\" data-wiki-page=\"1\">Calamares install guide</a></td></tr>\n<tr><td>GET</td><td><code>/api/system/caspar-arm</code></td><td>Staged Caspar armed?</td></tr>\n<tr><td>POST</td><td><code>/api/system/caspar-arm</code></td><td>Arm Caspar start</td></tr>\n<tr><td>DELETE</td><td><code>/api/system/caspar-arm</code></td><td>Disarm</td></tr>\n</tbody></table>\n<h2 id=\"exfat-sync-logs-stats\">exFAT sync · logs · stats</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/system/exfat-sync</code></td></tr>\n<tr><td>POST</td><td><code>/api/system/exfat-sync/run</code></td></tr>\n<tr><td>GET</td><td><code>/api/logs</code></td></tr>\n<tr><td>POST</td><td><code>/api/logs/clear</code></td></tr>\n<tr><td>GET</td><td><code>/api/support/bundle</code></td><td>Download diagnostics ZIP (configs, logs, GPU/xrandr, network)</td></tr>\n<tr><td>POST</td><td><code>/api/support/bundle</code></td><td>Same; optional JSON <code>{ &quot;operatorNote&quot;: &quot;…&quot; }</code></td></tr>\n<tr><td>GET</td><td><code>/api/host-stats</code></td></tr>\n</tbody></table>\n<p><strong>Caspar:</strong> not required.</p>\n<h3 id=\"exfat-sync-dashboard-get-apisystemexfat-sync\">exFAT sync dashboard (<code>GET /api/system/exfat-sync</code>)</h3>\n<p>Returns the installed sync map (<code>/etc/highascg/exfat-sync.json</code> or repo <code>config/exfat-sync.json</code>) with per-pair mount status, paths, and existence flags. Useful before/after inserting the operator USB stick or bridge disk.</p>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/system/exfat-sync | jq '.volumes, .pairs[] | select(.id | test(&quot;project&quot;))'</code></pre>\n<p>Key pairs (see <a href=\"#/BRIDGE_DISK_AND_USB_EXFAT\" data-wiki-page=\"1\">BRIDGE_DISK_AND_USB_EXFAT.md</a>):</p>\n<table><thead><tr><th>Pair id</th><th>Volume</th><th>Direction</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>bridge-modular-config</code></td><td>bridge</td><td>both</td><td><code>configs/</code> ↔ <code>~/highascg/config/</code></td></tr>\n<tr><td><code>usb-modular-config</code></td><td>usb</td><td>to_project</td><td>Stick configs → host at boot</td></tr>\n<tr><td><code>usb-media-ingest</code></td><td>usb</td><td>to_project</td><td><code>media/</code> → <code>~/highascg/media/bridge</code> (one-way)</td></tr>\n<tr><td><code>bridge-projects</code></td><td>bridge</td><td>both</td><td><code>projects/</code> ↔ <code>~/highascg/projects/</code> (mtime sync)</td></tr>\n<tr><td><code>usb-projects</code></td><td>usb</td><td>to_project</td><td>Stick <code>projects/</code> → working dir at boot only</td></tr>\n</tbody></table>\n<p><strong>Project saves</strong> use a separate code path (<code>project-volume-sync.js</code>): each <code>POST /api/project/save</code> pushes <strong>only the saved slug</strong> to mounted USB/bridge volumes. The <code>usb-projects</code> pair does <strong>not</strong> use <code>pushOnSave</code> — stick catalog updates happen on explicit project save, not on Settings save.</p>\n<h3 id=\"run-sync-post-apisystemexfat-syncrun\">Run sync (<code>POST /api/system/exfat-sync/run</code>)</h3>\n<p>Preview (no writes):</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/system/exfat-sync/run \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;dryRun&quot;: true}' | jq .</code></pre>\n<p>Commit mtime sync (overwrites older side per pair direction):</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/system/exfat-sync/run \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;dryRun&quot;: false, &quot;confirm&quot;: &quot;EXFAT_SYNC&quot;}' | jq .</code></pre>\n<p><strong>400</strong> without <code>dryRun: true</code> or <code>confirm: &quot;EXFAT_SYNC&quot;</code>.</p>\n<p>Implementation: <a href=\"../../src/api/routes-exfat-sync.js\">&lt;code&gt;src/api/routes-exfat-sync.js&lt;/code&gt;</a> · <a href=\"../../src/system/exfat-sync.js\">&lt;code&gt;src/system/exfat-sync.js&lt;/code&gt;</a></p>\n<h3 id=\"server-logs-get-apilogs\">Server logs (<code>GET /api/logs</code>)</h3>\n<p>HighAsCG in-memory log buffer (also streamed live on WebSocket <code>log_line</code>). Filter by category and level:</p>\n<pre><code class=\"language-bash\">curl -s 'http://127.0.0.1:4200/api/logs?lines=200&amp;categories=artnet,os-display&amp;levels=warn,error&amp;caspar=0'</code></pre>\n<p>Categories include <code>system</code>, <code>config</code>, <code>os-display</code>, <code>amcp</code>, <code>playback</code>, <code>streaming</code>, <code>audio</code>, <code>network</code>, <code>artnet</code>, <code>replication</code>, <code>websocket</code>, <code>device</code>, <code>sync</code>, <code>debug</code>. Open the <strong>connection eye</strong> in the Web UI for a live view with category dropdown and level checkboxes.</p>\n<h3 id=\"support-bundle-get-apisupportbundle\">Support bundle (<code>GET /api/support/bundle</code>)</h3>\n<p>One ZIP for post-mortems and bug reports — redacted settings, Caspar XML, project summary, system inventory, network, GPU/xrandr layout, log tails, host stats. Filename pattern: <code>highascg-support_&lt;hostname&gt;_&lt;ISO-timestamp&gt;.zip</code>. Max size defaults to 25 MB (<code>HIGHASCG_SUPPORT_BUNDLE_MAX_BYTES</code>). Sensitive config keys (token, password, secret, …) are replaced with <code>[REDACTED]</code>; see <code>manifest.json</code> → <code>redactedKeyPatterns</code>.</p>\n<pre><code class=\"language-bash\">curl -s -o support.zip 'http://127.0.0.1:4200/api/support/bundle?logLines=2000&amp;casparLines=500'\nunzip -l support.zip</code></pre>\n<p>Same bundle as the <strong>Support bundle</strong> button in the connection-eye logs modal, or <strong>Settings → Diagnostics</strong>. Use after a failure or before contacting support.</p>\n<h2 id=\"host-live-sources-operator-fullscreen\">Host live sources &amp; operator fullscreen</h2>\n<p><strong>Caspar:</strong> required.</p>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/host-live/operator-fullscreen</code></td><td>Operator fullscreen video route state</td></tr>\n<tr><td>POST</td><td><code>/api/host-live/operator-fullscreen</code></td><td>Toggle fullscreen route (<code>{ sourceId, action }</code>)</td></tr>\n<tr><td>GET</td><td><code>/api/host-live/migration</code></td><td>Preview legacy NDI/browser migration</td></tr>\n<tr><td>POST</td><td><code>/api/host-live/migration</code></td><td>Apply migration (<code>{ migrateHostLiveSources: true }</code>)</td></tr>\n</tbody></table>\n<p>WO-257 removed the CEF interactive bridge (<code>/api/cef-interactive/*</code>, <code>/api/cef/arm-input</code>, <code>/api/cef/release-input</code>) and its keyboard/mouse/eval forwarding into embedded CEF HTML. Operator fullscreen still routes a webpage host source onto the operator display (multiview or an interactive-enabled screen consumer) — it just doesn't forward input into it anymore. Templates that expected input (e.g. <code>template/mario</code>) still play; they no longer receive clicks/keys.</p>\n<h2 id=\"tailscale\">Tailscale</h2>\n<p><strong>Caspar:</strong> not required.</p>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/network/tailscale/status</code></td><td>Status + <code>config/tailscale.json</code></td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/enable</code></td><td><code>{ enabled: boolean }</code></td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/login</code></td><td>Start interactive login</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/login-operator-ui</code></td><td>Login + Firefox on operator <code>:0</code></td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/prefs</code></td><td>Save tailscale preferences</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/logout</code></td><td>Log out of tailnet</td></tr>\n</tbody></table>\n<p>Full request/response examples: <a href=\"#/wiki--api--network-tailscale\" data-wiki-page=\"1\">&lt;strong&gt;network-tailscale.md&lt;/strong&gt;</a>.</p>\n<hr>\n<pre><code class=\"language-bash\">curl -s 'http://127.0.0.1:4200/api/logs?lines=100&amp;source=highascg' | jq .</code></pre>","wiki--api--network-tailscale":"<h1 id=\"tailscale-network-api\">Tailscale network API</h1>\n<p><strong>Caspar:</strong> not required.</p>\n<p>Configure Tailscale from the Web UI (<strong>Settings → Tailscale</strong>) or these routes. Privileged POST actions use the same <strong>nuclear password</strong> gate as reboot/Calamares when <code>ui.nuclearRequirePassword</code> is enabled.</p>\n<p>Implementation: <a href=\"../../src/network/tailscale-service.js\">&lt;code&gt;src/network/tailscale-service.js&lt;/code&gt;</a> · <a href=\"../../src/api/routes-network-tailscale.js\">&lt;code&gt;src/api/routes-network-tailscale.js&lt;/code&gt;</a> · config: <a href=\"../../config/tailscale.json\">&lt;code&gt;config/tailscale.json&lt;/code&gt;</a></p>\n<p>Operator guide: <a href=\"#/reference--tailscale-integration\" data-wiki-page=\"1\">reference/tailscale-integration.md</a> · WO-91</p>\n<hr>\n<h2 id=\"routes\">Routes</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/network/tailscale/status</code></td><td>Full status + persisted config</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/enable</code></td><td>Start/stop <code>tailscaled</code> (<code>{ enabled: boolean }</code>)</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/login</code></td><td>Start interactive login; returns <code>authUrl</code> when pending</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/login-operator-ui</code></td><td>Login + open auth URL in Firefox on operator <code>:0</code></td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/prefs</code></td><td>Save <code>config/tailscale.json</code> preferences</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/logout</code></td><td><code>tailscale logout</code></td></tr>\n</tbody></table>\n<p><code>GET /api/system/setup</code> includes a compact <code>tailscale</code> object from the same service (IPv4, <code>needsLogin</code>, <code>authUrl</code>, <code>backendState</code>, etc.).</p>\n<hr>\n<h2 id=\"get-apinetworktailscalestatus\">GET <code>/api/network/tailscale/status</code></h2>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/network/tailscale/status | jq .</code></pre>\n<p><strong>Response (example, connected):</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;installed&quot;: true,\n  &quot;cli&quot;: &quot;/snap/bin/tailscale&quot;,\n  &quot;daemon&quot;: { &quot;unit&quot;: &quot;snap.tailscale.tailscaled.service&quot;, &quot;state&quot;: &quot;active&quot; },\n  &quot;socketPath&quot;: null,\n  &quot;backendState&quot;: &quot;Running&quot;,\n  &quot;authUrl&quot;: null,\n  &quot;ipv4&quot;: &quot;100.87.189.107&quot;,\n  &quot;hostname&quot;: &quot;highascg-nvidia-595&quot;,\n  &quot;dnsName&quot;: &quot;highascg-nvidia-595.tailc30860.ts.net&quot;,\n  &quot;needsLogin&quot;: false,\n  &quot;connected&quot;: true,\n  &quot;statusLine&quot;: &quot;highascg-nvidia-595  100.87.189.107&quot;,\n  &quot;adminUrl&quot;: &quot;https://login.tailscale.com/admin/machines&quot;,\n  &quot;config&quot;: {\n    &quot;enabled&quot;: true,\n    &quot;autoLoginOnBoot&quot;: false,\n    &quot;hostname&quot;: &quot;&quot;,\n    &quot;acceptRoutes&quot;: false,\n    &quot;operatorLoginAssist&quot;: true\n  }\n}</code></pre>\n<table><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody>\n<tr><td><code>backendState</code></td><td>Tailscale daemon state (<code>Running</code>, <code>NeedsLogin</code>, …)</td></tr>\n<tr><td><code>authUrl</code></td><td>OAuth URL when login is required (also shown in Settings)</td></tr>\n<tr><td><code>connected</code></td><td><code>true</code> when <code>backendState</code> is <code>Running</code> and tailnet IPv4 is assigned</td></tr>\n<tr><td><code>config</code></td><td>Persisted preferences from <code>config/tailscale.json</code></td></tr>\n</tbody></table>\n<p>Status is read via Tailscale <strong>Local API</strong> (<code>/run/tailscale/tailscaled.sock</code>) when available, else <code>tailscale status --json</code>.</p>\n<hr>\n<h2 id=\"post-apinetworktailscaleenable\">POST <code>/api/network/tailscale/enable</code></h2>\n<p>Body:</p>\n<pre><code class=\"language-json\">{ &quot;enabled&quot;: true, &quot;password&quot;: &quot;optional-nuclear-password&quot; }</code></pre>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/network/tailscale/enable \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;enabled&quot;: true}'</code></pre>\n<p>Runs <code>systemctl enable --now tailscaled</code> and/or <code>systemctl start snap.tailscale.tailscaled.service</code> (whichever applies). Disable stops both unit names.</p>\n<p><strong>502</strong> if passwordless <code>sudo</code> is not configured for <code>systemctl</code>.</p>\n<hr>\n<h2 id=\"post-apinetworktailscalelogin\">POST <code>/api/network/tailscale/login</code></h2>\n<p>Starts interactive login (<code>login-interactive</code> Local API or <code>sudo tailscale up</code> with config hostname/routes).</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/network/tailscale/login \\\n  -H 'Content-Type: application/json' \\\n  -d '{}'</code></pre>\n<p><strong>Response:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;authUrl&quot;: &quot;https://login.tailscale.com/a/…&quot;,\n  &quot;state&quot;: &quot;NeedsLogin&quot;,\n  &quot;connected&quot;: false,\n  &quot;status&quot;: { }\n}</code></pre>\n<p>Poll <code>GET …/status</code> every 3s until <code>connected</code> is true (Settings UI does this automatically for 120s).</p>\n<hr>\n<h2 id=\"post-apinetworktailscalelogin-operator-ui\">POST <code>/api/network/tailscale/login-operator-ui</code></h2>\n<p>Same as login, then spawns Firefox on <strong><code>DISPLAY=:0</code></strong> with the server-generated auth URL (never pass arbitrary URLs from the client).</p>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/network/tailscale/login-operator-ui \\\n  -H 'Content-Type: application/json' \\\n  -d '{}'</code></pre>\n<p><strong>Response:</strong></p>\n<pre><code class=\"language-json\">{\n  &quot;ok&quot;: true,\n  &quot;spawned&quot;: true,\n  &quot;authUrl&quot;: &quot;https://login.tailscale.com/a/…&quot;,\n  &quot;bin&quot;: &quot;/usr/local/lib/highascg/highascg-launch-operator-firefox.sh&quot;,\n  &quot;url&quot;: &quot;https://login.tailscale.com/a/…&quot;\n}</code></pre>\n<p>Requires nodm/X on the operator monitor. Uses <a href=\"../../tools/runtime/highascg-launch-operator-firefox.sh\">&lt;code&gt;highascg-launch-operator-firefox.sh&lt;/code&gt;</a>.</p>\n<p><strong>409</strong> when no auth URL is available yet — retry after <code>login</code> or wait and poll status.</p>\n<hr>\n<h2 id=\"post-apinetworktailscaleprefs\">POST <code>/api/network/tailscale/prefs</code></h2>\n<p>Persist advanced options (no systemd changes).</p>\n<pre><code class=\"language-json\">{\n  &quot;autoLoginOnBoot&quot;: false,\n  &quot;acceptRoutes&quot;: false,\n  &quot;operatorLoginAssist&quot;: true,\n  &quot;hostname&quot;: &quot;playout-box-1&quot;\n}</code></pre>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/network/tailscale/prefs \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;autoLoginOnBoot&quot;: true, &quot;operatorLoginAssist&quot;: true}'</code></pre>\n<table><thead><tr><th>Key</th><th>Default</th><th>Effect</th></tr></thead><tbody>\n<tr><td><code>autoLoginOnBoot</code></td><td><code>false</code></td><td>~4s after <code>highascg</code> start, trigger login if needed</td></tr>\n<tr><td><code>acceptRoutes</code></td><td><code>false</code></td><td>Pass <code>--accept-routes</code> on <code>tailscale up</code></td></tr>\n<tr><td><code>operatorLoginAssist</code></td><td><code>true</code></td><td>Allow operator-monitor browser spawn</td></tr>\n<tr><td><code>hostname</code></td><td><code>&quot;&quot;</code></td><td>Optional <code>--hostname</code> for <code>tailscale up</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"post-apinetworktailscalelogout\">POST <code>/api/network/tailscale/logout</code></h2>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/network/tailscale/logout \\\n  -H 'Content-Type: application/json' \\\n  -d '{}'</code></pre>\n<p>Runs <code>sudo tailscale logout</code>. <strong>502</strong> without passwordless sudo.</p>\n<hr>\n<h2 id=\"sudo-requirements\">Sudo requirements</h2>\n<p>Documented in <a href=\"#/HIGHASCG_PASSWORDLESS_SUDO\" data-wiki-page=\"1\">HIGHASCG_PASSWORDLESS_SUDO.md</a>:</p>\n<ul>\n<li><code>tailscale up</code>, <code>tailscale logout</code> (deb or snap path)</li>\n<li><code>systemctl start|stop|enable --now tailscaled</code></li>\n<li><code>systemctl start|stop snap.tailscale.tailscaled.service</code></li>\n</ul>\n<hr>\n<h2 id=\"related-wo\">Related WO</h2>\n<p>Tailscale control shipped in <a href=\"../../work/work-orders/91_WO_TAILSCALE_SETTINGS_AND_OPERATOR_UI.md\">WO-91</a>. Broader <strong>Network sync</strong> tab (Syncthing, rsync) remains <a href=\"../../work/work-orders/61_WO_RSYNC_PEER_SYNC_AND_NETWORK_SETTINGS.md\">WO-61</a>.</p>","wiki--api--ingest-usb-plugins":"<h1 id=\"ingest-usb-plugins-modules\">Ingest, USB, plugins &amp; modules</h1>\n<h2 id=\"media-ingest\">Media ingest</h2>\n<p><strong>Caspar:</strong> not required for upload/download.</p>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>POST</td><td><code>/api/ingest/upload</code></td><td>Multipart/stream upload</td></tr>\n<tr><td>POST</td><td><code>/api/ingest/download</code></td><td>Queue remote download</td></tr>\n<tr><td>GET</td><td><code>/api/ingest/download-status</code></td><td>Download job status</td></tr>\n<tr><td>GET</td><td><code>/api/ingest/preview</code></td><td>Preview URL/metadata (query)</td></tr>\n</tbody></table>\n<h2 id=\"usb-import-wo-29\">USB import (WO-29)</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/usb/drives</code></td></tr>\n<tr><td>POST</td><td><code>/api/usb/mount</code></td></tr>\n<tr><td>GET</td><td><code>/api/usb/browse</code></td></tr>\n<tr><td>GET</td><td><code>/api/usb/import-status</code></td></tr>\n<tr><td>POST</td><td><code>/api/usb/import</code></td></tr>\n<tr><td>POST</td><td><code>/api/usb/import-cancel</code></td></tr>\n<tr><td>POST</td><td><code>/api/usb/eject</code></td></tr>\n</tbody></table>\n<h3 id=\"list-usb-drives\">List USB drives</h3>\n<p>Returns mounted volumes (<code>drives</code>) and, on Linux, removable partitions that are plugged in but not yet mounted (<code>unmounted</code>).</p>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/usb/drives | jq .</code></pre>\n<h3 id=\"mount-a-removable-volume-linux\">Mount a removable volume (Linux)</h3>\n<pre><code class=\"language-bash\">curl -s -X POST http://127.0.0.1:4200/api/usb/mount \\\n  -H 'Content-Type: application/json' \\\n  -d '{&quot;blockDevice&quot;:&quot;/dev/sdb1&quot;}' | jq .</code></pre>\n<p>Uses <code>udisksctl</code> (polkit / <code>plugdev</code> — no sudo). The Import from USB modal calls this when you click <strong>Mount</strong>.</p>\n<h2 id=\"plugins\">Plugins</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/plugins</code></td></tr>\n<tr><td>POST</td><td><code>/api/plugins/add</code></td></tr>\n</tbody></table>\n<h2 id=\"optional-modules\">Optional modules</h2>\n<p>Enabled modules register extra API prefixes. Discover enabled modules:</p>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/modules | jq .</code></pre>\n<p>Built-in module API prefixes (when enabled in config):</p>\n<table><thead><tr><th>Prefix</th><th>Module</th></tr></thead><tbody>\n<tr><td><code>/api/previs</code></td><td>Previs / 3D</td></tr>\n<tr><td><code>/api/tracking</code></td><td>Tracking</td></tr>\n<tr><td><code>/api/autofollow</code></td><td>Auto-follow</td></tr>\n<tr><td><code>/api/cg-studio</code></td><td>CG studio</td></tr>\n</tbody></table>\n<p>Each module implements its own paths under that prefix; see <code>src/*/register.js</code> and <a href=\"#/MODULES\" data-wiki-page=\"1\">MODULES.md</a>.</p>","api-reference":"<h1 id=\"amcp-rest-api-reference-legacy\">AMCP REST API Reference (legacy)</h1>\n<blockquote><p><strong>Canonical docs:</strong> <a href=\"#/wiki--api--README\" data-wiki-page=\"1\">wiki/api/README.md</a> — full endpoint list, Caspar gate, OpenAPI <a href=\"../wiki/api/openapi.yaml\">&lt;code&gt;openapi.yaml&lt;/code&gt;</a>, and per-topic examples.</p></blockquote>\n<p>HighAsCG maps subset functionality of the underlying CasparCG AMCP protocol into an easy-to-use HTTP REST JSON API. All endpoints are accessible via <code>http://localhost:&lt;PORT&gt;/api</code>.</p>\n<p>All server responses include standard headers and use the <code>application/json</code> format.</p>\n<h2 id=\"table-of-contents\">Table of Contents</h2>\n<ol>\n<li><a href=\"#/api-reference#1-basic-playback-commands\" data-wiki-page=\"1\">Basic Playback Commands</a></li>\n<li><a href=\"#/api-reference#2-mixer-commands\" data-wiki-page=\"1\">Mixer Commands</a></li>\n<li><a href=\"#/api-reference#3-cg-template-commands\" data-wiki-page=\"1\">CG (Template) Commands</a></li>\n<li><a href=\"#/api-reference#4-data--project-commands\" data-wiki-page=\"1\">Data &amp;amp; Project Commands</a></li>\n<li><a href=\"#/api-reference#5-state--query-commands\" data-wiki-page=\"1\">State &amp;amp; Query Commands</a></li>\n<li><a href=\"#/api-reference#6-thumbnail--media-commands\" data-wiki-page=\"1\">Thumbnail &amp;amp; Media Commands</a></li>\n<li><a href=\"#/api-reference#7-system--control-commands\" data-wiki-page=\"1\">System &amp;amp; Control Commands</a></li>\n</ol>\n<hr>\n<h2 id=\"1-basic-playback-commands\">1. Basic Playback Commands</h2>\n<h3 id=\"post-apiplay\"><code>POST /api/play</code></h3>\n<p>Play a clip on a channel and layer.</p>\n<ul>\n<li><strong>Body:</strong></li>\n</ul>\n<pre><code class=\"language-json\">  {\n    &quot;channel&quot;: 1,\n    &quot;layer&quot;: 10,\n    &quot;clip&quot;: &quot;AMB&quot;,\n    &quot;loop&quot;: true,\n    &quot;transition&quot;: &quot;MIX&quot;,\n    &quot;duration&quot;: 50,\n    &quot;tween&quot;: &quot;easeInCubic&quot;,\n    &quot;auto&quot;: false,\n    &quot;parameters&quot;: &quot;&quot;\n  }</code></pre>\n<ul>\n<li><strong>Example:</strong></li>\n</ul>\n<pre><code class=\"language-bash\">  curl -X POST -H &quot;Content-Type: application/json&quot; -d '{&quot;clip&quot;:&quot;AMB&quot;,&quot;loop&quot;:true,&quot;channel&quot;:1,&quot;layer&quot;:10}' http://localhost:4200/api/play</code></pre>\n<pre><code class=\"language-javascript\">  fetch('/api/play', { method: 'POST', body: JSON.stringify({ clip: 'AMB', channel: 1, layer: 10, loop: true }) })</code></pre>\n<h3 id=\"post-apiload-post-apiloadbg\"><code>POST /api/load</code>, <code>POST /api/loadbg</code></h3>\n<p>Loads a clip (or loads it into the background) onto a channel and layer. Wait for <code>POST /api/play</code> to trigger if BG.</p>\n<ul>\n<li><strong>Body Schema:</strong> Same as <code>/api/play</code>.</li>\n</ul>\n<h3 id=\"post-apipause-post-apiresume-post-apistop-post-apiclear\"><code>POST /api/pause</code>, <code>POST /api/resume</code>, <code>POST /api/stop</code>, <code>POST /api/clear</code></h3>\n<p>Controls the playback state or clears the layer content entirely.</p>\n<ul>\n<li><strong>Body:</strong> <code>{ &quot;channel&quot;: 1, &quot;layer&quot;: 10 }</code></li>\n</ul>\n<h3 id=\"post-apiprint-post-apiloglevel-post-apilogcategory\"><code>POST /api/print</code>, <code>POST /api/log/level</code>, <code>POST /api/log/category</code></h3>\n<p>Controls CasparCG disk writing/logging settings.</p>\n<ul>\n<li><strong>Bodies:</strong></li>\n<li><code>print</code>: <code>{ &quot;channel&quot;: 1 }</code></li>\n<li><code>log/level</code>: <code>{ &quot;level&quot;: &quot;trace&quot; }</code></li>\n<li><code>log/category</code>: <code>{ &quot;category&quot;: &quot;calltrace&quot;, &quot;enable&quot;: true }</code></li>\n</ul>\n<hr>\n<h2 id=\"2-mixer-commands\">2. Mixer Commands</h2>\n<p>Mixer commands can be accessed via <code>POST</code> (to mutate) or <code>GET</code> (to query properties).</p>\n<h3 id=\"post-apimixercommand\"><code>POST /api/mixer/:command</code></h3>\n<p>Changes mixer configurations for a layer.</p>\n<ul>\n<li><strong>Path parameters:</strong> <code>command</code> can be <code>keyer</code>, <code>blend</code>, <code>chroma</code>, <code>invert</code>, <code>straight_alpha</code>, <code>opacity</code>, <code>brightness</code>, <code>saturation</code>, <code>contrast</code>, <code>levels</code>, <code>fill</code>, <code>clip</code>, <code>anchor</code>, <code>crop</code>, <code>rotation</code>, <code>volume</code>, <code>mastervolume</code>, <code>grid</code>, <code>commit</code>, <code>clear</code>.</li>\n<li><strong><code>keyer</code>:</strong> per-layer blend mode in Caspar’s mixer — not the same as DeckLink “external keyer” in hardware logs. See <a href=\"#/reference--amcp-mapping\" data-wiki-page=\"1\">reference/amcp-mapping.md</a> (Mixer) for detail.</li>\n<li><strong>Body Defaults:</strong> <code>{ &quot;channel&quot;: 1, &quot;layer&quot;: 10 }</code></li>\n<li><strong>Example (<code>opacity</code>):</strong></li>\n</ul>\n<pre><code class=\"language-json\">  {\n    &quot;channel&quot;: 1, \n    &quot;layer&quot;: 10,\n    &quot;opacity&quot;: 0.5,\n    &quot;duration&quot;: 25,\n    &quot;tween&quot;: &quot;linear&quot;\n  }</code></pre>\n<h3 id=\"get-apimixercommandchannelxlayery\"><code>GET /api/mixer/:command?channel=X&amp;layer=Y</code></h3>\n<p>Queries the value of the mixer property (e.g. <code>GET /api/mixer/opacity?channel=1&amp;layer=10</code>).</p>\n<ul>\n<li><strong>Response:</strong></li>\n</ul>\n<pre><code class=\"language-json\">  {\n    &quot;ok&quot;: true,\n    &quot;data&quot;: &quot;0.5&quot;\n  }</code></pre>\n<hr>\n<h2 id=\"3-cg-template-commands\">3. CG (Template) Commands</h2>\n<p>Controls HTML and Flash templates running in the CG layer.</p>\n<h3 id=\"post-apicgadd\"><code>POST /api/cg/add</code></h3>\n<p>Adds a template to a specific host layer.</p>\n<ul>\n<li><strong>Body:</strong></li>\n</ul>\n<pre><code class=\"language-json\">  {\n    &quot;channel&quot;: 1,\n    &quot;layer&quot;: 20,\n    &quot;templateHostLayer&quot;: 1,\n    &quot;template&quot;: &quot;my-template&quot;,\n    &quot;playOnLoad&quot;: true,\n    &quot;data&quot;: &quot;&lt;templateData&gt;&lt;componentData id=\\&quot;f0\\&quot;&gt;&lt;data id=\\&quot;text\\&quot; value=\\&quot;Hello World\\&quot;/&gt;&lt;/componentData&gt;&lt;/templateData&gt;&quot;\n  }</code></pre>\n<h3 id=\"post-apicgupdate\"><code>POST /api/cg/update</code></h3>\n<p>Updates template parameters.</p>\n<ul>\n<li><strong>Body:</strong> <code>{ &quot;channel&quot;: 1, &quot;layer&quot;: 20, &quot;templateHostLayer&quot;: 1, &quot;data&quot;: &quot;&lt;xml...&gt;&quot; }</code></li>\n</ul>\n<h3 id=\"post-apicgplay-post-apicgstop-post-apicgnext-post-apicginvoke\"><code>POST /api/cg/play</code>, <code>POST /api/cg/stop</code>, <code>POST /api/cg/next</code>, <code>POST /api/cg/invoke</code></h3>\n<p>Controls playback of templates, triggers animations, or invokes JS methods.</p>\n<hr>\n<h2 id=\"4-data-project-commands\">4. Data &amp; Project Commands</h2>\n<p>Stores and retrieves raw strings to CasparCG disk (for memory caching) as well as handles project states.</p>\n<h3 id=\"post-apidatastore\"><code>POST /api/data/store</code></h3>\n<ul>\n<li><strong>Body:</strong> <code>{ &quot;name&quot;: &quot;my_dataset&quot;, &quot;data&quot;: &quot;&lt;dataset&gt;...&quot; }</code></li>\n</ul>\n<h3 id=\"post-apidataretrieve-post-apidataremove\"><code>POST /api/data/retrieve</code>, <code>POST /api/data/remove</code></h3>\n<ul>\n<li><strong>Body:</strong> <code>{ &quot;name&quot;: &quot;my_dataset&quot; }</code></li>\n</ul>\n<h3 id=\"post-apidatalist\"><code>POST /api/data/list</code></h3>\n<p>Lists datasets stored in CasparCG.</p>\n<h3 id=\"post-apiprojectsave-post-apiprojectload\"><code>POST /api/project/save</code>, <code>POST /api/project/load</code></h3>\n<p>Saves and loads a full HighAsCG project payload into a Caspar dataset (<code>casparcg_web_project</code>).</p>\n<hr>\n<h2 id=\"5-state-query-commands\">5. State &amp; Query Commands</h2>\n<p>Extracts core statuses and telemetry from CasparCG.</p>\n<h3 id=\"get-apistate\"><code>GET /api/state</code></h3>\n<p>Returns the merged state tree of the HighAsCG app + Caspar variables and channels.</p>\n<h3 id=\"get-apiserver-get-apiserverqueues-get-apiserverthreads-get-apiservergl\"><code>GET /api/server</code>, <code>GET /api/server/queues</code>, <code>GET /api/server/threads</code>, <code>GET /api/server/gl</code></h3>\n<p>Gets information about the underlying CasparCG running server.</p>\n<h3 id=\"get-apifonts-get-apihelpcommand\"><code>GET /api/fonts</code>, <code>GET /api/help/:command</code></h3>\n<p>Returns all fonts available and documentation on commands.</p>\n<h3 id=\"get-apichannelsid-get-apichannelsiddelay\"><code>GET /api/channels/:id</code>, <code>GET /api/channels/:id/delay</code></h3>\n<p>Fetches parsed XML info arrays of what is loaded inside a channel (<code>INFO X</code>).</p>\n<hr>\n<h2 id=\"6-thumbnail-media-commands\">6. Thumbnail &amp; Media Commands</h2>\n<h3 id=\"get-apithumbnails\"><code>GET /api/thumbnails</code></h3>\n<p>Lists all available thumbnails.</p>\n<h3 id=\"get-apithumbnailfilename\"><code>GET /api/thumbnail/:filename</code></h3>\n<p>Returns a binary PNG response of the requested media thumbnail.</p>\n<h3 id=\"post-apithumbnailsgenerate-post-apithumbnailsgenerate-all\"><code>POST /api/thumbnails/generate</code>, <code>POST /api/thumbnails/generate-all</code></h3>\n<p>Commands Caspar to regenerate base64 thumb streams.</p>\n<ul>\n<li><strong>Generate Body:</strong> <code>{ &quot;filename&quot;: &quot;AMB&quot; }</code></li>\n</ul>\n<hr>\n<h2 id=\"7-system-control-commands\">7. System &amp; Control Commands</h2>\n<h3 id=\"post-apirestart-post-apikill\"><code>POST /api/restart</code>, <code>POST /api/kill</code></h3>\n<p>Restarts the underlying AMCP connections and internal structures, or sends <code>KILL</code> to shut off the CasparCG instance entirely.</p>\n<ul>\n<li><strong>Body:</strong> <code>{}</code></li>\n</ul>\n<h3 id=\"post-apidiag-post-apiglgc\"><code>POST /api/diag</code>, <code>POST /api/gl/gc</code></h3>\n<p>Show diagnostic screens or trigger OpenGL Garbage Collection.</p>\n<ul>\n<li><strong>Body:</strong> <code>{}</code></li>\n</ul>\n<h3 id=\"post-apiraw\"><code>POST /api/raw</code></h3>\n<p>Arbitrary AMCP payload dispatching.</p>\n<ul>\n<li><strong>Body:</strong> <code>{ &quot;cmd&quot;: &quot;PLAY 1-1 AMB LOOP&quot; }</code></li>\n</ul>","osc-integration":"<h1 id=\"osc-integration-casparcg-highascg\">OSC integration (CasparCG → HighAsCG)</h1>\n<p>HighAsCG listens for <strong>Open Sound Control (OSC)</strong> over <strong>UDP</strong> from CasparCG Server. <strong>Production setups assume Caspar sends OSC to HighAsCG</strong>; the UDP listener is always on except when the server is started with <strong><code>--no-osc</code></strong> (development or emergency). Caspar pushes channel mixer levels, stage/layer state, FFmpeg file timing, profiler timings, and output consumer status at frame rate. The Node app aggregates this in <code>OscState</code>, merges it into the main state manager, exposes <strong>REST</strong> and <strong>WebSocket</strong> APIs, and drives the web UI (VU strip, playback timer, etc.).</p>\n<p><strong>Related:</strong> <a href=\"#/reference--polling-vs-osc\" data-wiki-page=\"1\">polling-vs-osc.md</a></p>\n<hr>\n<h2 id=\"architecture\">Architecture</h2>\n<pre><code>CasparCG Server\n│\n├── AMCP (TCP :5250)          commands / replies\n│\n└── OSC (UDP)  ──────────►  HighAsCG :listenPort (default **6251**; Caspar `&lt;default-port&gt;` stays **6250**)\n         │\n         ▼\n    src/osc/osc-listener.js     UDP receive, parse bundles/messages\n         │\n         ▼\n    src/osc/osc-state.js        aggregate per channel/layer, peak hold, throttled `change`\n         │\n         ├──► src/state/state-manager.js   updateFromOscSnapshot / clearOscMirror\n         ├──► WebSocket `{ type: 'osc', data }`  (often ~20 Hz after throttle; optional delta payloads)\n         ├──► src/osc/osc-variables.js     Companion-style variables when enabled\n         └──► REST  /api/osc/*\n\n         │  Browser\n         ▼\n    client/lib/osc-client.js       merges WS OSC payloads (full or delta)\n         │\n         └──► footer VU, playback-timer, now-playing, profiler-display, output-status, …</code></pre>\n<h3 id=\"playback-timer-data-flow-summary\">Playback timer data flow (summary)</h3>\n<ol>\n<li>Caspar sends e.g. <code>/channel/1/stage/layer/10/file/time</code> with elapsed and total seconds.</li>\n<li><code>osc-state</code> stores <code>file.elapsed</code>, <code>file.duration</code>, computes <code>remaining</code> / <code>progress</code>.</li>\n<li>Server broadcasts OSC snapshot (or delta) over WebSocket.</li>\n<li>UI components subscribe via <code>OscClient</code> (<code>onLayerState</code>, <code>onAudioLevels</code>, …).</li>\n</ol>\n<hr>\n<h2 id=\"casparcg-server-configuration\">CasparCG server configuration</h2>\n<p>Caspar must <strong>send</strong> OSC toward the machine running HighAsCG. Two common approaches:</p>\n<ol>\n<li><strong>Predefined client</strong> (persistent, no AMCP session required) — recommended for dedicated HighAsCG hosts.</li>\n<li><strong>AMCP-associated client</strong> — Caspar may also send OSC to the IP of an AMCP client (see Caspar wiki); HighAsCG’s generated config snippet prefers a predefined client.</li>\n</ol>\n<h3 id=\"xml-snippet-conceptual\">XML snippet (conceptual)</h3>\n<p>HighAsCG can emit a full <code>&lt;osc&gt;</code> block from the config generator (<code>buildOscConfigurationXml</code> in <code>src/config/config-generator.js</code>) when Companion/module <code>osc_port &gt; 0</code>, including:</p>\n<ul>\n<li><code>&lt;default-port&gt;</code> — Port Caspar uses for <strong>its own</strong> OSC server (incoming control). <strong>Must not be the same UDP port</strong> as HighAsCG’s listener on the <strong>same machine</strong>, or one process will fail to bind and you will see <strong>no</strong> OSC in the UI (<code>udpPacketsReceived</code> stays 0 in diagnostics).</li>\n<li><code>&lt;disable-send-to-amcp-clients&gt;</code> — <code>false</code> if you want OSC mirrored to AMCP clients as well.</li>\n<li><code>&lt;predefined-clients&gt;&lt;predefined-client&gt;</code> — <code>&lt;address&gt;</code> must be the <strong>IP</strong> of the HighAsCG host (hostnames are not reliable per Caspar docs). <code>&lt;port&gt;</code> must equal HighAsCG <strong><code>osc.listenPort</code></strong> (where this app’s UDP listener binds — default <strong>6251</strong>).</li>\n</ul>\n<p>Use <strong><code>GET /api/osc/config-hint</code></strong> for an XML fragment where <code>default-port</code> and predefined <code>port</code> are <strong>split</strong> correctly. Use <strong><code>GET /api/osc/diagnostics</code></strong> to confirm <code>udpPacketsReceived</code> increases while Caspar is running.</p>\n<p><strong>Common mistake:</strong> using the <strong>same</strong> UDP port for Caspar’s <code>&lt;default-port&gt;</code> (Caspar’s OSC server, typically <strong>6250</strong>) and HighAsCG’s listener — they must differ on one host. Defaults: Caspar <strong>6250</strong>, HighAsCG <strong>6251</strong>.</p>\n<h3 id=\"firewall\">Firewall</h3>\n<ul>\n<li>Allow <strong>UDP</strong> inbound to HighAsCG’s <code>listenPort</code> from the CasparCG host.</li>\n<li>Ensure no other process binds the same UDP port on the HighAsCG machine.</li>\n</ul>\n<hr>\n<h2 id=\"highascg-configuration\">HighAsCG configuration</h2>\n<p>Defined in <code>config/default.js</code>, overridden by persisted <code>appSettings</code>, environment variables, CLI, and <strong>Application Settings → Audio / OSC</strong> in the web UI.</p>\n<table><thead><tr><th>Setting (concept)</th><th>Env / notes</th></tr></thead><tbody>\n<tr><td>Listener on/off</td><td><strong>On by default.</strong> Only <strong><code>--no-osc</code></strong> disables the UDP bind (not configurable via env/UI)</td></tr>\n<tr><td>Listen port</td><td><code>OSC_LISTEN_PORT</code> (default 6251)</td></tr>\n<tr><td>Bind address</td><td><code>OSC_BIND_ADDRESS</code> (default <code>0.0.0.0</code>)</td></tr>\n<tr><td>Peak hold</td><td><code>peakHoldMs</code> in config (UI)</td></tr>\n<tr><td>WS delta mode</td><td><code>HIGHASCG_OSC_WS_DELTA</code> / <code>config.osc.wsDeltaBroadcast</code> — partial channel updates on WS</td></tr>\n</tbody></table>\n<p>Implementation: <code>src/osc/osc-config.js</code> (<code>normalizeOscConfig</code>).</p>\n<hr>\n<h2 id=\"rest-api-osc-aggregate\">REST API (OSC aggregate)</h2>\n<p>Base path: <code>/api/osc/</code></p>\n<table><thead><tr><th>Method</th><th>Path</th><th>Description</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/osc/state</code></td><td>Full <code>OscState</code> snapshot (<code>channels</code>, <code>updatedAt</code>, …)</td></tr>\n<tr><td>GET</td><td><code>/api/osc/audio/:ch</code></td><td>Audio mixer block for channel <code>ch</code></td></tr>\n<tr><td>GET</td><td><code>/api/osc/layer/:ch/:layer</code></td><td>Layer aggregate for channel/layer</td></tr>\n<tr><td>GET</td><td><code>/api/osc/profiler</code></td><td>Per-channel profiler summaries</td></tr>\n<tr><td>GET</td><td><code>/api/osc/outputs</code></td><td>Per-channel output port info</td></tr>\n<tr><td>GET</td><td><code>/api/osc/config-hint</code></td><td><code>text/xml</code> snippet for Caspar <code>casparcg.config</code></td></tr>\n</tbody></table>\n<p>If the OSC listener is disabled, most endpoints return a small JSON payload indicating OSC is off; <code>config-hint</code> may still be generated for documentation.</p>\n<p>Main HTTP state also includes <code>osc</code> and <code>ui</code> where applicable (<code>src/api/get-state.js</code>).</p>\n<hr>\n<h2 id=\"websocket\">WebSocket</h2>\n<p>Messages with <code>type: </code>'osc'<code> carry the aggregate snapshot or, when delta mode is enabled, a mergeable partial (</code>delta: true<code>, </code>channels: { &quot;1&quot;: … }<code>). Clients should deep-merge by channel id when </code>delta` is set.</p>\n<hr>\n<h2 id=\"osc-message-reference-caspar-highascg\">OSC message reference (Caspar → HighAsCG)</h2>\n<p>Paths are OSC address patterns; <code>N</code> = channel (1-based), <code>L</code> = layer, <code>M</code> = mixer audio index (0-based), <code>P</code> = output port.</p>\n<h3 id=\"channel-level-channeln\">Channel-level — <code>/channel/N/…</code></h3>\n<table><thead><tr><th>Address suffix</th><th>Args</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>format</code></td><td>string</td><td>Video format (e.g. <code>1080p5000</code>)</td></tr>\n<tr><td><code>profiler/time</code></td><td>float float</td><td>Actual vs expected frame time (s)</td></tr>\n<tr><td><code>output/port/P/type</code></td><td>string</td><td>Consumer type: <code>screen</code>, <code>system-audio</code>, <code>decklink</code>, …</td></tr>\n<tr><td><code>output/port/P/frame</code></td><td>int int</td><td>Frames written / max (file/stream)</td></tr>\n</tbody></table>\n<h3 id=\"audio-mixer-channelnmixeraudio\">Audio mixer — <code>/channel/N/mixer/audio/…</code></h3>\n<table><thead><tr><th>Address suffix</th><th>Args</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>nb_channels</code></td><td>int</td><td>Audio channel count</td></tr>\n<tr><td><code>M/dBFS</code></td><td>float</td><td>Level for mixer channel <code>M</code> (~25–60 updates/sec)</td></tr>\n</tbody></table>\n<h3 id=\"stage-layer-channelnstagelayerl\">Stage / layer — <code>/channel/N/stage/layer/L/…</code></h3>\n<table><thead><tr><th>Address suffix</th><th>Args</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>time</code></td><td>float</td><td>Seconds layer active</td></tr>\n<tr><td><code>frame</code></td><td>int</td><td>Frames since layer started</td></tr>\n<tr><td><code>type</code></td><td>string</td><td>Producer type (<code>ffmpeg</code>, <code>empty</code>, …)</td></tr>\n<tr><td><code>background/type</code></td><td>string</td><td>LOADBG producer type</td></tr>\n<tr><td><code>profiler/time</code></td><td>float float</td><td>Layer render actual/expected</td></tr>\n<tr><td><code>paused</code></td><td>bool</td><td>Paused</td></tr>\n</tbody></table>\n<h3 id=\"ffmpeg-producer-channelnstagelayerl\">FFmpeg producer — <code>/channel/N/stage/layer/L/…</code></h3>\n<table><thead><tr><th>Address suffix</th><th>Args</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>file/name</code></td><td>string</td><td>Media name (relative)</td></tr>\n<tr><td><code>file/path</code></td><td>string</td><td>Absolute path on server</td></tr>\n<tr><td><strong><code>file/time</code></strong></td><td><strong>float float</strong></td><td><strong>Elapsed s, total s</strong> — primary playback progress</td></tr>\n<tr><td><code>file/{stream-id}/fps</code></td><td>float</td><td>Stream FPS</td></tr>\n</tbody></table>\n<p>Legacy / extra FFmpeg paths (still seen on some builds): <code>file/frame</code> (elapsed/total frames), <code>file/fps</code>, <code>file/video/<em></code>, <code>file/audio/</em></code>, <code>loop</code>, etc.</p>\n<h3 id=\"html-template-channelnstagelayerlhost\">HTML template — <code>/channel/N/stage/layer/L/host/…</code></h3>\n<table><thead><tr><th>Address suffix</th><th>Args</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>path</code>, <code>width</code>, <code>height</code>, <code>fps</code></td><td>various</td><td>Template metadata</td></tr>\n<tr><td><code>buffer</code></td><td><em>(varies)</em></td><td>Buffer state</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table><thead><tr><th>Symptom</th><th>Things to check</th></tr></thead><tbody>\n<tr><td>No OSC in UI / empty <code>osc</code> in state</td><td>Listener disabled (<code>--no-osc</code>, env, UI); Caspar not targeting this host:port; <strong>UDP</strong> firewall; wrong <strong>IP</strong> in predefined client (use numeric IP).</td></tr>\n<tr><td>Port bind error</td><td>Another app using <code>listenPort</code>; try another port in UI + Caspar + firewall.</td></tr>\n<tr><td>Stale or jumpy meters</td><td>Network loss; compare <code>emitIntervalMs</code> / <code>staleTimeoutMs</code>; verify Caspar is actually outputting (<code>nb_channels</code>, <code>dBFS</code>).</td></tr>\n<tr><td>Playback timer blank or <strong>stuck on last clip</strong></td><td>OSC not reaching app (<code>/api/osc/diagnostics</code>); wrong PGM channel. Newer Caspar uses <code>…/foreground/file/time</code>. Some codecs send sparse <code>file/time</code>; HighAsCG clears stale elapsed when <strong><code>foreground/producer</code></strong> or <strong><code>file/name</code></strong> / <strong><code>file/path</code></strong> changes so the header does not keep the previous clip’s time.</td></tr>\n<tr><td>High CPU</td><td>Normal at high message rates; WS delta mode and throttling reduce broadcast size.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"reference-files\">Reference files</h2>\n<table><thead><tr><th>Path</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>src/osc/osc-listener.js</code></td><td>UDP + parsing</td></tr>\n<tr><td><code>src/osc/osc-state.js</code></td><td>Aggregation, peak hold, computed fields</td></tr>\n<tr><td><code>src/osc/osc-config.js</code></td><td>Normalized config</td></tr>\n<tr><td><code>src/osc/osc-variables.js</code></td><td>Variable bridge</td></tr>\n<tr><td><code>src/api/routes-osc.js</code></td><td>REST</td></tr>\n<tr><td><code>src/config/config-generator.js</code></td><td>Caspar <code>&lt;osc&gt;</code> XML for generated server config</td></tr>\n<tr><td><code>client/lib/osc-client.js</code></td><td>Browser merge + subscriptions</td></tr>\n</tbody></table>\n<p>Caspar wiki (repo): <code>.reference/casparcg-wiki/Protocols/OSC-Protocol.md</code></p>","wiki--integration--usb-v4l2-input":"<h1 id=\"usb-v4l2-video-input\">USB / V4L2 video input</h1>\n<p>Capture UVC webcams and USB switchers (e.g. <strong>ATEM Mini Pro ISO</strong>) on a dedicated Caspar host channel. By default Caspar opens the device directly via <strong><code>PLAY … v4l2:///dev/videoN LOOP</code></strong> (ffmpeg producer). An optional FFmpeg UDP bridge (<code>v4l2_capture_bridge: true</code>) remains for legacy setups.</p>\n<h2 id=\"requirements\">Requirements</h2>\n<ul>\n<li><strong><code>v4l-utils</code></strong> on the playout host (<code>v4l2-ctl --list-devices</code>). Included in <code>scripts/setup/05-caspar-deps.sh</code> and live ISO prep.</li>\n<li>Device must expose a <strong>capture</strong> node (not metadata-only). Loopback outputs such as <strong>CasparCG Out</strong> (<code>/dev/video10</code>) are excluded automatically.</li>\n</ul>\n<h2 id=\"quick-setup-atem-mini-pro-iso\">Quick setup (ATEM Mini Pro ISO)</h2>\n<ol>\n<li><strong>Settings → USB video</strong> — set count = 1, pick <code>/dev/video0</code> (not <code>/dev/video1</code>).</li>\n<li>Format <strong>mjpeg</strong>, 1920×1080, <strong>50</strong> fps if auto fails.</li>\n<li>Optional audio: use a separate <strong>live-audio</strong> slot on the same ALSA card (V4L2 direct capture is video-only).</li>\n<li><strong>Save</strong> → <strong>Apply Caspar config + restart</strong> (required when count increases).</li>\n<li><strong>Apply PLAY</strong> (Settings tab or Sources → Live tile).</li>\n<li>Drag <strong><code>route://hostCh-N</code></strong> from <strong>Sources → Live</strong> onto PGM or multiview.</li>\n</ol>\n<p>Or: <strong>Sources → Live → +</strong> → <strong>USB Video (V4L2)</strong> — adds a slot and starts capture when channels exist.</p>\n<h2 id=\"architecture\">Architecture</h2>\n<table><thead><tr><th>Piece</th><th>Role</th></tr></thead><tbody>\n<tr><td>Direct (default)</td><td>Caspar ffmpeg producer → <code>v4l2:///dev/videoN</code></td></tr>\n<tr><td>Optional bridge</td><td><code>v4l2-input-bridge.js</code> → UDP <code>52400 + slot</code> when <code>v4l2_capture_bridge</code> is true</td></tr>\n<tr><td>Caspar channel</td><td>One dedicated host channel per slot (<code>kind: v4l2</code>)</td></tr>\n<tr><td>On-air</td><td><code>route://</code> only — do not PLAY V4L2 directly on program layers</td></tr>\n</tbody></table>\n<h2 id=\"api\">API</h2>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/system/v4l2-devices?refresh=1</code></td></tr>\n<tr><td>GET</td><td><code>/api/v4l2-inputs</code></td></tr>\n<tr><td>POST</td><td><code>/api/v4l2-inputs/config</code></td></tr>\n<tr><td>POST</td><td><code>/api/v4l2-inputs/apply</code></td></tr>\n</tbody></table>\n<p>Status is also exposed as <code>v4l2InputsStatus</code> in <code>GET /api/state</code>.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table><thead><tr><th>Symptom</th><th>Check</th></tr></thead><tbody>\n<tr><td>Empty device list</td><td><code>v4l2-ctl --list-devices</code>; install <code>v4l-utils</code></td></tr>\n<tr><td>ATEM missing</td><td>USB cable; <code>ls -l /dev/video*</code></td></tr>\n<tr><td>Black picture after route</td><td><strong>Apply PLAY</strong>; AMCP connected; <code>INFO hostCh-1</code> shows <code>v4l2://</code> and advancing time</td></tr>\n<tr><td><code>non-existing PPS</code> in Caspar log</td><td>Disable UDP bridge — use direct <code>v4l2://</code> (default)</td></tr>\n<tr><td>Duplicate device warning</td><td>Only one slot per <code>/dev/videoN</code></td></tr>\n<tr><td>ALSA conflict warning</td><td>Same <code>hw:C,D</code> cannot be live-audio and V4L2 mux simultaneously</td></tr>\n</tbody></table>\n<h2 id=\"related\">Related</h2>\n<ul>\n<li>Work order: <code>work/work-orders/121_WO_USB_WEBCAM_V4L2_INPUT.md</code></li>\n<li>Virtual camera <strong>output</strong> (loopback): <a href=\"#/wiki--integration--virtual-camera-output\" data-wiki-page=\"1\">virtual-camera-output.md</a> — do not use <code>/dev/video10</code> as input</li>\n</ul>","companion-websocket-catalog-bootstrap":"<h1 id=\"companion-module-websocket-slim-bootstrap-catalog-loading\">Companion module: WebSocket slim bootstrap &amp; catalog loading</h1>\n<p>HighAsCG can send a <strong>small first <code>state</code> frame</strong> on WebSocket connect when the operator enables slim bootstrap on the server. Companion modules that assumed <strong><code>state.media</code></strong> and <strong><code>state.templates</code></strong> were always populated in that first message need a small adaptation.</p>\n<p>This document is for <strong>maintainers of Bitfocus Companion modules</strong> (or any HTTP/WS client) that connect to HighAsCG’s WebSocket API.</p>\n<p><strong>Related:</strong> variable/topic docs in <a href=\"#/companion-module-ui-selection\" data-wiki-page=\"1\">&lt;code&gt;companion-module-ui-selection.md&lt;/code&gt;</a>; performance context in <a href=\"../../work/work-orders/performance/PF-01-big-snapshots-ws-state.md\">&lt;code&gt;work/work-orders/performance/PF-01-big-snapshots-ws-state.md&lt;/code&gt;</a>.</p>\n<hr>\n<h2 id=\"1-when-this-applies\">1. When this applies</h2>\n<p>Slim bootstrap is active on the server when <strong><code>HIGHASCG_WS_SLIM_BOOTSTRAP</code></strong> is set to <strong><code>1</code></strong> or <strong><code>true</code></strong> (case-insensitive). If unset, the server behaves like the legacy style: the first WS <strong><code>state</code></strong> message usually includes full <strong><code>media</code></strong> and <strong><code>templates</code></strong> arrays (subject to other caps such as CINF limits on HTTP-style snapshots).</p>\n<p>There is <strong>no separate capability flag</strong> beyond inspecting the first <strong><code>state</code></strong> payload.</p>\n<hr>\n<h2 id=\"2-how-to-detect-deferred-catalog-mode\">2. How to detect “deferred catalog” mode</h2>\n<p>On the first inbound WebSocket message with <strong><code>type: &quot;state&quot;</code></strong>, check <strong><code>data</code></strong> (the parsed JSON body):</p>\n<table><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody>\n<tr><td><strong><code>catalogDeferred: true</code></strong></td><td>Catalog bodies are omitted from this <strong><code>state</code></strong>; fill them via HTTP or WS catalog messages (below).</td></tr>\n<tr><td><strong><code>mediaCount</code></strong></td><td>Integer — number of media rows on the server (CLS + app merge).</td></tr>\n<tr><td><strong><code>templateCount</code></strong></td><td>Integer — template count.</td></tr>\n<tr><td><strong><code>media</code></strong>, <strong><code>templates</code></strong></td><td>Typically <strong>empty arrays</strong> when <code>catalogDeferred</code> is true.</td></tr>\n</tbody></table>\n<p>If <strong><code>catalogDeferred</code></strong> is <strong>missing or false</strong>, treat the snapshot as <strong>full</strong> (existing module logic can stay as-is).</p>\n<hr>\n<h2 id=\"3-recovery-strategies-pick-one\">3. Recovery strategies (pick one)</h2>\n<h3 id=\"option-a-single-http-full-state-smallest-code-change\">Option A — <strong>Single HTTP full state</strong> (smallest code change)</h3>\n<p>After connect, if <strong><code>data.catalogDeferred</code></strong>:</p>\n<ol>\n<li><strong><code>GET /api/state</code></strong> (same host/port as WS).</li>\n<li>Replace your cached snapshot with the JSON body (it includes <strong><code>media</code></strong>, <strong><code>templates</code></strong>, <strong><code>variables</code></strong>, <strong><code>channelMap</code></strong>, etc.).</li>\n</ol>\n<p>This matches the HighAsCG web client <strong>fallback</strong> and is the most compatible path for modules that already poll or merge <strong><code>GET /api/state</code></strong>.</p>\n<p><strong>Caveat:</strong> On installations with <strong>very large</strong> media lists, this request is heavier than chunked loading or <strong><code>GET /api/media</code></strong> alone.</p>\n<hr>\n<h3 id=\"option-b-http-catalog-only-lighter-than-full-state\">Option B — <strong>HTTP catalog only</strong> (lighter than full state)</h3>\n<p>If you only need file/template pickers:</p>\n<ol>\n<li><strong><code>GET /api/media</code></strong> — returns the media list (optional query <strong><code>?full_cinf=1</code></strong> or <strong><code>?fullCinf=1</code></strong> for full CINF parsing server-side).</li>\n<li><strong><code>GET /api/templates</code></strong> — returns <code>{ id, label }[]</code>.</li>\n</ol>\n<p>Merge <strong><code>media</code></strong> and <strong><code>templates</code></strong> into whatever structure your module expects (often the same shape as inside <strong><code>GET /api/state</code></strong>).</p>\n<hr>\n<h3 id=\"option-c-websocket-catalog-chunks-best-for-huge-catalogs\">Option C — <strong>WebSocket catalog chunks</strong> (best for huge catalogs)</h3>\n<p>Use JSON messages on the <strong>same</strong> WebSocket as the rest of your traffic. All client messages are JSON objects; the server responds with <strong><code>type: &quot;catalog_chunk&quot;</code></strong> (and <strong><code>catalog_error</code></strong> on failure).</p>\n<h4 id=\"c1-templates-one-request\">C.1 — Templates (one request)</h4>\n<p>Send:</p>\n<pre><code class=\"language-json\">{\n  &quot;type&quot;: &quot;catalog_request&quot;,\n  &quot;slice&quot;: &quot;templates&quot;,\n  &quot;offset&quot;: 0,\n  &quot;limit&quot;: 100000,\n  &quot;id&quot;: &quot;companion-templates-1&quot;\n}</code></pre>\n<ul>\n<li><strong><code>id</code></strong> — your correlation id; echoed as <strong><code>requestId</code></strong> on the matching <strong><code>catalog_chunk</code></strong> (recommended).</li>\n</ul>\n<p>You will receive exactly one chunk (templates are small):</p>\n<pre><code class=\"language-json\">{\n  &quot;type&quot;: &quot;catalog_chunk&quot;,\n  &quot;data&quot;: {\n    &quot;slice&quot;: &quot;templates&quot;,\n    &quot;offset&quot;: 0,\n    &quot;total&quot;: 42,\n    &quot;items&quot;: [{ &quot;id&quot;: &quot;...&quot;, &quot;label&quot;: &quot;...&quot; }],\n    &quot;done&quot;: true,\n    &quot;requestId&quot;: &quot;companion-templates-1&quot;\n  }\n}</code></pre>\n<p>Use <strong><code>data.items</code></strong> as <strong><code>templates</code></strong>.</p>\n<h4 id=\"c2-media-subscribe-server-streams-chunks\">C.2 — Media — <strong>subscribe</strong> (server streams chunks)</h4>\n<p>Send:</p>\n<pre><code class=\"language-json\">{\n  &quot;type&quot;: &quot;catalog_subscribe&quot;,\n  &quot;slice&quot;: &quot;media&quot;,\n  &quot;id&quot;: &quot;companion-media-1&quot;,\n  &quot;limit&quot;: 600,\n  &quot;fullCinf&quot;: false\n}</code></pre>\n<ul>\n<li><strong><code>slice</code></strong> — must be <strong><code>&quot;media&quot;</code></strong> for subscribe (templates use <strong><code>catalog_request</code></strong> only).</li>\n<li><strong><code>limit</code></strong> — optional; server clamps to a safe maximum. Default server chunk size comes from <strong><code>HIGHASCG_WS_CATALOG_CHUNK_LIMIT</code></strong> (typically <strong>600</strong>).</li>\n<li><strong><code>fullCinf</code></strong> — optional boolean (or <strong><code>&quot;1&quot;</code></strong>). When true, disables CINF cap for <strong>each chunk</strong> (can be expensive on large rigs).</li>\n</ul>\n<p>The server sends <strong>multiple</strong> <strong><code>catalog_chunk</code></strong> messages with the <strong>same</strong> <strong><code>requestId</code></strong> until <strong><code>done: true</code></strong>:</p>\n<pre><code class=\"language-json\">{\n  &quot;type&quot;: &quot;catalog_chunk&quot;,\n  &quot;data&quot;: {\n    &quot;slice&quot;: &quot;media&quot;,\n    &quot;offset&quot;: 0,\n    &quot;total&quot;: 15000,\n    &quot;items&quot;: [ /* … rows for this slice … */ ],\n    &quot;done&quot;: false,\n    &quot;requestId&quot;: &quot;companion-media-1&quot;,\n    &quot;streamId&quot;: &quot;cat-1700000000000-abc123&quot;\n  }\n}</code></pre>\n<p><strong>Reassembly rule:</strong> allocate an array of length <strong><code>total</code></strong>, then for each chunk assign:</p>\n<p><code>array[data.offset + i] = data.items[i]</code></p>\n<p>When <strong><code>data.done</code></strong> is <strong>true</strong>, the catalog is complete. Use <strong><code>array</code></strong> as <strong><code>media</code></strong> in your snapshot.</p>\n<h4 id=\"c3-media-single-chunk-catalog_request\">C.3 — Media — <strong>single chunk</strong> (<strong><code>catalog_request</code></strong>)</h4>\n<p>For manual paging, send <strong><code>catalog_request</code></strong> with <strong><code>slice: &quot;media&quot;</code></strong>, <strong><code>offset</code></strong>, and <strong><code>limit</code></strong>. The server returns one <strong><code>catalog_chunk</code></strong>. Repeat with increasing <strong><code>offset</code></strong> until <strong><code>done</code></strong> or <strong><code>offset &gt;= total</code></strong>.</p>\n<hr>\n<h2 id=\"4-errors\">4. Errors</h2>\n<pre><code class=\"language-json\">{\n  &quot;type&quot;: &quot;catalog_error&quot;,\n  &quot;data&quot;: {\n    &quot;message&quot;: &quot;…&quot;,\n    &quot;requestId&quot;: &quot;companion-media-1&quot;,\n    &quot;streamId&quot;: &quot;cat-…&quot;\n  },\n  &quot;id&quot;: &quot;companion-media-1&quot;\n}</code></pre>\n<p>Correlate failures with your <strong><code>id</code></strong> / <strong><code>requestId</code></strong>. Fall back to <strong>Option A</strong> or <strong>B</strong> if needed.</p>\n<hr>\n<h2 id=\"5-server-tuning-operator-deployment\">5. Server tuning (operator / deployment)</h2>\n<table><thead><tr><th>Variable</th><th>Effect</th></tr></thead><tbody>\n<tr><td><strong><code>HIGHASCG_WS_SLIM_BOOTSTRAP</code></strong></td><td>Enables slim first <strong><code>state</code></strong> + <strong><code>catalogDeferred</code></strong>.</td></tr>\n<tr><td><strong><code>HIGHASCG_WS_CATALOG_CHUNK_LIMIT</code></strong></td><td>Max rows per <strong>media</strong> chunk (clamped).</td></tr>\n<tr><td><strong><code>HIGHASCG_WS_CATALOG_CHUNK_ENRICH</code></strong></td><td><strong><code>0</code></strong> / <strong><code>false</code></strong> — raw CLS-style rows in chunks (fastest WS path). Default enriches chunks similarly to <strong><code>GET /api/media</code></strong> (probe + CINF rules, per chunk).</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"6-other-websocket-behavior-changes-fyi\">6. Other WebSocket behavior changes (FYI)</h2>\n<p>These affect <strong>all</strong> WS clients, not only slim bootstrap:</p>\n<table><thead><tr><th>Topic</th><th>Behavior</th></tr></thead><tbody>\n<tr><td><strong><code>change</code></strong> events</td><td>May be <strong>coalesced</strong> within <strong><code>HIGHASCG_WS_CHANGE_COALESCE_MS</code></strong> (default ~75 ms). <strong>Last write wins</strong> per <code>path</code> within the window — do not rely on receiving every intermediate <code>change</code> if your UI animates on each tick.</td></tr>\n<tr><td><strong><code>log_line</code></strong></td><td>Rate-limited by <strong><code>HIGHASCG_WS_LOG_LINE_MAX_HZ</code></strong> (default <strong>50</strong>/s; <strong><code>0</code></strong> disables).</td></tr>\n<tr><td><strong>Periodic full <code>state</code></strong></td><td>If the server is configured with a WS broadcast interval, those ticks may use a <strong>full</strong> catalog snapshot (implementation detail). Do not assume periodic <strong><code>state</code></strong> is always slim.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"7-websocket-url\">7. WebSocket URL</h2>\n<p>Same patterns as today:</p>\n<ul>\n<li><strong><code>ws://&lt;host&gt;:&lt;port&gt;/api/ws</code></strong></li>\n<li><strong><code>ws://&lt;host&gt;:&lt;port&gt;/ws</code></strong></li>\n<li><strong><code>ws://&lt;host&gt;:&lt;port&gt;/instance/&lt;instanceId&gt;/api/ws</code></strong> (when using instance-prefixed HTTP as well)</li>\n</ul>\n<p>Messages are <strong>JSON text</strong> frames: <strong><code>{&quot;type&quot;:&quot;…&quot;, …}</code></strong>.</p>\n<hr>\n<h2 id=\"8-minimal-implementation-checklist\">8. Minimal implementation checklist</h2>\n<ul>\n<li>[ ] After first <strong><code>state</code></strong>, if <strong><code>catalogDeferred</code></strong>, do <strong>not</strong> assume <strong><code>media.length &gt; 0</code></strong>.</li>\n<li>[ ] Implement at least <strong>Option A</strong> (<code>GET /api/state</code>) or <strong>Option B</strong> (<code>/api/media</code> + <code>/api/templates</code>).</li>\n<li>[ ] For large lists, prefer <strong>Option C</strong> ( <strong><code>catalog_subscribe</code></strong> for <strong>media</strong> + one <strong><code>catalog_request</code></strong> for <strong>templates</strong>).</li>\n<li>[ ] Handle <strong><code>catalog_error</code></strong> and fall back to HTTP if WS catalog fails.</li>\n<li>[ ] If your module depended on <strong>every</strong> <strong><code>change</code></strong> message, verify behavior with coalescing (§6).</li>\n</ul>\n<hr>\n<h2 id=\"9-source-map-for-code-archeology\">9. Source map (for code archeology)</h2>\n<table><thead><tr><th>Area</th><th>Path</th></tr></thead><tbody>\n<tr><td>Slim snapshot shape</td><td><code>src/api/get-state.js</code> (<code>slimCatalog</code>)</td></tr>\n<tr><td>WS catalog dispatch</td><td><code>src/server/ws-catalog-handlers.js</code></td></tr>\n<tr><td>WS attach / <code>change</code> coalescing</td><td><code>src/server/ws-server.js</code></td></tr>\n<tr><td>Raw catalog merge</td><td><code>src/api/media-catalog.js</code></td></tr>\n<tr><td>Web UI reference client</td><td><code>client/lib/deferred-catalog-ws.js</code>, <code>client/lib/app-ws-handlers.js</code></td></tr>\n</tbody></table>","companion-module-ui-selection":"<h1 id=\"companion-module-ui-selection-variables-ui_selection_\">Companion module: UI selection variables (<code>ui_selection_*</code>)</h1>\n<p>HighAsCG pushes <strong>inspector selection</strong> from the web UI into the same <strong>Companion-style variable map</strong> used for OSC, channel INFO, and app stats. A Bitfocus <strong>Companion module</strong> that talks to HighAsCG over HTTP/WebSocket can mirror these into Companion’s own variables for buttons, triggers, and expressions.</p>\n<h2 id=\"behaviour-already-implemented-in-highascg\">Behaviour (already implemented in HighAsCG)</h2>\n<ol>\n<li>When the user selects something in the inspector (looks editor layer, timeline clip, multiview cell), the browser calls <strong><code>POST /api/selection</code></strong> with a JSON body (debounced ~100 ms). See <code>client/lib/selection-sync.js</code> (<code>buildSelectionPayload</code>, <code>scheduleSelectionSync</code>).</li>\n<li>The server applies that payload with <code>applyUiSelectionPayloadToVariables</code> (<code>src/api/apply-ui-selection-variables.js</code>), which calls <code>state.setVariable(key, stringValue)</code> for each field.</li>\n<li>Variables appear in <strong><code>GET /api/variables</code></strong> (full map) and <strong><code>GET /api/variables?prefix=ui_selection_</code></strong> (subset). Updates are also emitted on the WebSocket as <strong><code>variable_update</code></strong> with a partial <code>{ [key]: value }</code> object (see <code>src/server/ws-server.js</code>).</li>\n</ol>\n<p>Optional: WebSocket message <strong><code>{ &quot;type&quot;: &quot;selection_sync&quot;, &quot;data&quot;: &lt;same shape as POST body&gt; }</code></strong> calls <code>ctx.setUiSelection</code> (<code>index.js</code>) and runs the same mapper—useful if a client prefers WS over HTTP.</p>\n<h2 id=\"implementing-in-a-companion-module\">Implementing in a Companion module</h2>\n<ol>\n<li><strong>Subscribe to changes</strong> (recommended): open <code>ws://&lt;highascg-host&gt;:&lt;port&gt;/api/ws</code> (or <code>/instance/&lt;id&gt;/api/ws</code> when using the instance URL prefix). On <strong><code>variable_update</code></strong>, copy any keys starting with <code>ui_selection_</code> into Companion via <code>setVariableValues</code> / your module’s variable API.</li>\n<li><strong>Or poll</strong>: periodically <strong><code>GET /api/variables?prefix=ui_selection_</code></strong> and diff against the previous snapshot; push updates when values change.</li>\n<li><strong>Define variables</strong> in the module so they appear in Companion’s UI: register definitions for the keys you care about (or a dynamic prefix-based approach if your framework supports it). All values are <strong>strings</strong> (booleans use <code>'true'</code> / <code>'false'</code> / <code>''</code>).</li>\n<li><strong><code>GET /api/state</code></strong> includes a <code>variables</code> object with the full snapshot; heavier than <code>prefix=</code> query but useful for initial sync after connect.</li>\n</ol>\n<h2 id=\"ui_selection_context\"><code>ui_selection_context</code></h2>\n<table><thead><tr><th><code>ui_selection_context</code></th><th>Meaning</th><th>Variable prefix for detail fields</th></tr></thead><tbody>\n<tr><td><code>none</code></td><td>No actionable selection (details cleared)</td><td>—</td></tr>\n<tr><td><code>scene_layer</code></td><td>Look editor / scene layer selected</td><td><code>ui_selection_look_*</code></td></tr>\n<tr><td><code>timeline_clip</code></td><td>Timeline clip selected</td><td><code>ui_selection_tl_*</code></td></tr>\n<tr><td><code>multiview</code></td><td>Multiview cell selected</td><td><code>ui_selection_mv_*</code></td></tr>\n</tbody></table>\n<p>Always read <strong><code>ui_selection_context</code></strong> first; when it changes, ignore stale keys from another prefix until new values arrive (HighAsCG clears all owned keys on each update).</p>\n<h2 id=\"useful-keys-looks-editor\">Useful keys (looks editor)</h2>\n<ul>\n<li><strong><code>ui_selection_label</code></strong> — short label (e.g. <code>L10</code>).</li>\n<li><strong><code>ui_selection_look_id</code></strong> / <strong><code>ui_selection_look_name</code></strong> — look id and display name.</li>\n<li><strong><code>ui_selection_look_layer_index</code></strong>, <strong><code>ui_selection_look_layer_number</code></strong>, <strong><code>ui_selection_look_caspar_layer</code></strong>, <strong><code>ui_selection_look_preview_channel</code></strong> — routing indices.</li>\n<li>*<em><code>ui_selection_look_fill_</em></code><strong>, </strong><code>rotation</code><strong>, </strong><code>opacity</code><strong>, </strong><code>ui_selection_look_source_<em></code></em>* — geometry and source.</li>\n<li><strong><code>ui_selection_look_layer_json</code></strong> — full JSON snapshot of the selected layer object (effects, transition, PIP overlays, etc.). Prefer individual keys when possible; use JSON for exhaustive access.</li>\n</ul>\n<p>Full key list is the <code>ALL_UI_SELECTION_KEYS</code> array in <code>src/api/apply-ui-selection-variables.js</code>.</p>\n<h2 id=\"companion-style-url-prefix\">Companion-style URL prefix</h2>\n<p>If the UI is loaded as <code>http://host:port/instance/myid/</code>, API paths are <strong><code>/instance/myid/api/variables</code></strong>, <strong><code>/instance/myid/api/selection</code></strong>, <strong><code>/instance/myid/api/ws</code></strong> — the server accepts these without stripping the prefix.</p>\n<h2 id=\"related-files\">Related files</h2>\n<p><strong>See also:</strong> <a href=\"#/companion-websocket-catalog-bootstrap\" data-wiki-page=\"1\">WebSocket slim bootstrap &amp;amp; catalog loading</a> — first <code>state</code> may omit <code>media</code> / <code>templates</code> when slim bootstrap is enabled.</p>\n<table><thead><tr><th>Area</th><th>File</th></tr></thead><tbody>\n<tr><td>Client payload</td><td><code>client/lib/selection-sync.js</code></td></tr>\n<tr><td>Server → variables</td><td><code>src/api/apply-ui-selection-variables.js</code></td></tr>\n<tr><td>HTTP route</td><td><code>src/api/router.js</code> (<code>POST /api/selection</code>)</td></tr>\n<tr><td>Variable HTTP API</td><td><code>src/api/routes-state.js</code></td></tr>\n<tr><td>WS broadcast</td><td><code>src/server/ws-server.js</code></td></tr>\n</tbody></table>","caspar_config_explained":"<p>&lt;?xml version=&quot;1.0&quot; encoding=&quot;utf-8&quot;?&gt; &lt;-- The opening XML declaration --&gt; &lt;configuration&gt;  &lt;-- The opening element is configuration --&gt; &lt;paths&gt; &lt;-- Here we define all the paths to various resources the Server would need to access --&gt; &lt;-- Note that all the paths presented in this default configuration file are relative to where you run CasparCG.exe If CasparCG.exe is in c:\\CasparCG\\Server, then the media-path is c:\\CasparCG\\Server\\media --&gt; &lt;media-path&gt;media/&lt;/media-path&gt; &lt;-- This is the path to media files (images and videos) --&gt; &lt;log-path&gt;log/&lt;/log-path&gt; &lt;-- The Server creates logfiles in this location --&gt; &lt;data-path&gt;data/&lt;/data-path&gt; &lt;-- CasparCG supports saving and retrieving data from the Server --&gt; &lt;template-path&gt;template/&lt;/template-path&gt; &lt;-- The path to HTML and Flash templates and the resources they use --&gt; &lt;/paths&gt; &lt;-- The end tag for the paths element --&gt; &lt;lock-clear-phrase&gt;secret&lt;/lock-clear-phrase&gt; &lt;-- You can lock control of CasparCG and this would be the secret to unlock it --&gt; &lt;channels&gt; &lt;-- Here we configure all the channels that CasparCG will run --&gt; &lt;channel&gt; &lt;-- You can have multiple &lt;channel&gt;'s.  This would be channel number 1 --&gt; &lt;video-mode&gt;720p5000&lt;/video-mode&gt; &lt;-- Here the resolution and frame rate is set to 1280x720 at 50 frames pr. second --&gt; &lt;consumers&gt; &lt;-- Consumers are the ouputs for the channel, you can only have one for each channel, but it can include multiple child-elements --&gt; &lt;screen /&gt; &lt;-- A screen consumer with default values --&gt; &lt;system-audio /&gt; &lt;-- System audio output with default values --&gt; &lt;/consumers&gt; &lt;-- The end tag for the consumer element --&gt; &lt;/channel&gt; &lt;-- The end tag for the channel element --&gt; &lt;-- Here you could define more channels, just like above, they're parsed sequentially, so a new channel here would be number 2 --&gt; &lt;/channels&gt; &lt;-- The end tag for the channels element --&gt; &lt;controllers&gt; &lt;-- Controllers are ports and protocols used to control CasparCG --&gt; &lt;tcp&gt; &lt;-- This defines a new TCP controller --&gt; &lt;port&gt;5250&lt;/port&gt; &lt;-- It will listen on all IP address of the server running CasparCG at port 5250 --&gt; &lt;protocol&gt;AMCP&lt;/protocol&gt; &lt;-- The command protocol is AMCP --&gt; &lt;/tcp&gt; &lt;-- The end tag for the tcp element --&gt; &lt;/controllers&gt; &lt;-- The end tag for the controllers element --&gt; &lt;amcp&gt; &lt;-- This is new for version 2.2 and declares the handler for AMCP request related to media --&gt; &lt;media-server&gt; &lt;-- Define media-server --&gt; &lt;host&gt;localhost&lt;/host&gt; &lt;-- Here we tell CasparCG that the media-server is running on the localhost, this could also be an IP address like 127.0.0.1 --&gt; &lt;port&gt;8000&lt;/port&gt; &lt;-- The media-server is listening for request at port 8000 --&gt; &lt;/media-server&gt; &lt;-- The end tag for the media-server element --&gt; &lt;/amcp&gt; &lt;-- The end tag for the amcp elment --&gt; &lt;/configuration&gt; &lt;-- The end tag for the configuration element --&gt;</p>\n<p>&lt;-- Then the example section of the casparcg.config file starts: --&gt; &lt;-- For this demonstration I'm purposefully not commenting this section out, please note that you can not copy/paste this code and expect CasparCG to run correctly --&gt; &lt;-- EVERYTHING BELOW ARE EXAMPLES AND WILL NOT FUNCTION CORRECTLY --&gt;</p>\n<p>&lt;log-level&gt; info  [trace|debug|info|warning|error|fatal]&lt;/log-level&gt; &lt;-- log-level sets the verbosity of the Servers logs. The default option is trace, and if you wouldn't include a &lt;log-level&gt; element in your config, that would be used. Other possible options are then defined between the square brackets as trace, debug, info, warning, error and fatal. Using &lt;log-level&gt;trace&lt;log-level&gt; would log all activity of the Server, resulting in some very big log files. --&gt; &lt;template-hosts&gt; &lt;-- The Flash producer uses template-hosts to run and control your templates --&gt; &lt;template-host&gt; &lt;video-mode /&gt; &lt;filename /&gt; &lt;width /&gt; &lt;height /&gt; &lt;/template-host&gt; &lt;/template-hosts&gt; &lt;flash&gt; &lt;buffer-depth&gt;auto [auto|1..]&lt;/buffer-depth&gt; &lt;-- The Flash producer has a configurable buffer-depth (a positive integer) or auto (which is recomended) --&gt; &lt;/flash&gt; &lt;html&gt; &lt;-- Since CasparCG uses the Chromium Embedded Framework for HTML templates it comes with DevTools built-in. If you define a port below you can run a HTML template and connect to it by simply opening up a Chrome browser and going to http://localhost:PORT/ to debug your template --&gt; &lt;remote-debugging-port&gt;0 [0|1024-65535]&lt;/remote-debugging-port&gt; &lt;-- Set to zero, remote debugging is disabled --&gt; &lt;enable-gpu&gt; false [true|false]&lt;/enable-gpu&gt; &lt;-- Here you can enable using GPU accelerated rendering in the HTML producers (it might cause issues, so is off by default) --&gt; &lt;/html&gt; &lt;ndi&gt; &lt;-- The latest builds of CasparCG support NDI inputs and outputs --&gt; &lt;auto-load&gt;false [true|false]&lt;/auto-load&gt; &lt;-- This loads the NDI library when the Server is started --&gt; &lt;/ndi&gt; &lt;channels&gt; &lt;channel&gt; &lt;-- Here we define a channel --&gt; &lt;video-mode&gt;PAL [PAL|NTSC|576p2500|720p2398|720p2400|720p2500|720p5000|720p2997|720p5994|720p3000|720p6000|1080p2398|1080p2400|1080i5000|1080i5994|1080i6000|1080p2500|1080p2997</p>\n<p>|1080p3000|1080p5000|1080p5994|1080p6000|1556p2398|1556p2400|1556p2500|dci1080p2398|dci1080p2400|dci1080p2500|2160p2398|2160p2400|2160p2500|2160p2997|2160p3000|2160p5000|2160p5994</p>\n<p>|2160p6000|dci2160p2398|dci2160p2400|dci2160p2500]&lt;/video-mode&gt;</p>\n<p>&lt;-- The default mode is PAL and the other supported resolutions and framerates are found within the square brackets --&gt; &lt;consumers&gt; &lt;decklink&gt; &lt;device&gt;[1..]&lt;/device&gt;&lt;-- Here you define the number of the output you want to use on the Decklink card --&gt; &lt;key-device&gt;device + 1 [1..]&lt;/key-device&gt; &lt;-- CasparCG supports sending the key output on separate output for cards that don't support key+fill outputs natively.  Key-device must be a number other than device. --&gt; &lt;embedded-audio&gt;false [true|false]&lt;/embedded-audio&gt; &lt;-- If CasparCG should embedd audio on the SDI, it results in 1 extra frame of delay --&gt; &lt;latency&gt;normal [normal|low|default]&lt;/latency&gt; &lt;-- The latency of the processing on the Decklink card, there normal/default is 1 frame slower than low, but is more stable --&gt; &lt;keyer&gt;external [external|external_separate_device|internal|default]&lt;/keyer&gt; &lt;-- Configure the keyer of the card.  If your card support internal key-fill processing (like the Extreme models) you would use external.  If you are using another channel for the key you would use external_separate_device.  Internal keyer will use the input signal on the card and add the keyed signal from CasparCG (passthrough). -&gt; &lt;key-only&gt;false [true|false]&lt;/key-only&gt; &lt;-- Output only the key signal, you would use this when you define a Decklink consumer as the key for another Decklink consumer --&gt; &lt;buffer-depth&gt;3 [1..]&lt;/buffer-depth&gt; &lt;-- This sets the buffer of the frames sent to the Decklink card.  Newer 4K models can use a buffer-depth of 2, older models usually need a buffer-depth of 3, increasing the buffer-depth results in higher latency.  There is an upper limit to the buffer-depth, but that depends on the card, system memory and other factors. --&gt; &lt;/decklink&gt; &lt;bluefish&gt; &lt;device&gt;[1..]&lt;/device&gt; &lt;-- The number of Bluefish card  to use--&gt; &lt;sdi-stream&gt;1[1..] &lt;/sdi-stream&gt; &lt;-- The number of the output to use on the card --&gt; &lt;embedded-audio&gt;false [true|false]&lt;/embedded-audio&gt; &lt;-- Embedd audio on the SDI output --&gt; &lt;keyer&gt;disabled [external|internal|disabled] (external only supported on channels 1 and 3, using 3 requires 4 out connectors) ( internal only available on devices with a hardware keyer) &lt;/keyer&gt; &lt;internal-keyer-audio-source&gt; videooutputchannel [videooutputchannel|sdivideoinput] ( only valid when using internal keyer option)&lt;/internal-keyer-audio-source&gt; &lt;-- Much like the Decklink above --&gt; &lt;watchdog&gt;2[0..] ( set to 0 to disable the HW watchdog functionality, otherwise this value indicates how many frames to wait after a crash, before enabling the bypass relay's on the card - only works on sdi-stream 1) &lt;/watchdog&gt; &lt;/bluefish&gt; &lt;system-audio&gt; &lt;channel-layout&gt;stereo [mono|stereo|matrix]&lt;/channel-layout&gt; &lt;-- The channel-layout for the audio from the default Windows or Linux soundcard --&gt; &lt;latency&gt;200 [0..]&lt;/latency&gt; &lt;-- Configurable latency, some use this to try and sync system-audio with the video from another output --&gt; &lt;/system-audio&gt; &lt;screen&gt; &lt;device&gt;1 [1..]&lt;/device&gt; &lt;-- Which screen to use if windowed is disabled below --&gt; &lt;aspect-ratio&gt;default [default|4:3|16:9]&lt;/aspect-ratio&gt;  &lt;-- Set the aspect-ratio--&gt; &lt;stretch&gt;fill [none|fill|uniform|uniform_to_fill]&lt;/stretch&gt; &lt;-- How to fill the window --&gt; &lt;windowed&gt;true [true|false]&lt;/windowed&gt; &lt;-- If true the output will be in a controllable window, otherwise fullscreen --&gt; &lt;key-only&gt;false [true|false]&lt;/key-only&gt; &lt;-- Output only the key --&gt; &lt;vsync&gt;false [true|false]&lt;/vsync&gt; &lt;-- V-sync on the screen consumer. On NVIDIA playout hosts use true here AND Sync to VBlank off in nvidia-settings; see docs/reference/screen-consumer-vsync-nvidia.md --&gt; &lt;borderless&gt;false [true|false]&lt;/borderless&gt; &lt;-- Disable border/window-chrome --&gt; &lt;interactive&gt;true [true|false]&lt;/interactive&gt;  &lt;-- If the screen consumer should send mouse interaction to CEF.  You could open up a website and interact withit like it would be running in your browser --&gt; &lt;always-on-top&gt;false [true|false]&lt;&lt;/always-on-top&gt; &lt;-- Fix window so it's always on top of other windows --&gt; &lt;x&gt;0&lt;/x&gt; &lt;-- Location --&gt; &lt;y&gt;0&lt;/y&gt; &lt;-- Location --&gt; &lt;width&gt;0 (0=not set)&lt;/width&gt; &lt;-- Width --&gt; &lt;height&gt;0 (0=not set)&lt;/height&gt; &lt;-- Height --&gt; &lt;sbs-key&gt;false [true|false]&lt;/sbs-key&gt; &lt;-- This enables a side-by-side key which can be used with a DataVideo vision switcher --&gt; &lt;colour-space&gt;RGB [RGB|datavideo-full|datavideo-limited] (Enables colour space convertion for DataVideo TC-100 / TC-200)&lt;/colour-space&gt; &lt;/screen&gt; &lt;newtek-ivga&gt;&lt;/newtek-ivga&gt; &lt;-- Enable the original iVGA/NDI --&gt; &lt;ndi&gt; &lt;name&gt;[custom name]&lt;/name&gt; &lt;-- Defines the name of the NDI output --&gt; &lt;allow-fields&gt;false [true|false]&lt;/allow-fields&gt; &lt;-- If NDI output should be interlaced or progessive --&gt; &lt;/ndi&gt; &lt;ffmpeg&gt; &lt;path&gt;[file|url]&lt;/path&gt; &lt;--  --&gt; &lt;args&gt;[most ffmpeg arguments related to filtering and output codecs]&lt;/args&gt; &lt;--  --&gt; &lt;/ffmpeg&gt; &lt;/consumers&gt; &lt;/channel&gt; &lt;/channels&gt; &lt;osc&gt; &lt;default-port&gt;6250&lt;/default-port&gt; &lt;--  --&gt; &lt;disable-send-to-amcp-clients&gt;false [true|false]&lt;/disable-send-to-amcp-clients&gt; &lt;--  --&gt; &lt;predefined-clients&gt; &lt;--  --&gt; &lt;predefined-client&gt; &lt;--  --&gt; &lt;address&gt;127.0.0.1&lt;/address&gt; &lt;--  --&gt; &lt;port&gt;5253&lt;/port&gt; &lt;--  --&gt; &lt;/predefined-client&gt; &lt;/predefined-clients&gt; &lt;/osc&gt; --&gt;</p>\n<hr>\n<h2 id=\"custom-video-mode-when-id-is-widthheight-not-a-preset\">Custom <code>&lt;video-mode&gt;</code> (when <code>id</code> is width×height, not a preset)</h2>\n<p>Caspar expects a matching block under <code>&lt;video-modes&gt;</code> with:</p>\n<table><thead><tr><th>Element</th><th>Meaning</th></tr></thead><tbody>\n<tr><td><code>&lt;time-scale&gt;</code></td><td><strong><code>fps × 1000</code></strong> (e.g. 50 → 50000, 60 → 60000).</td></tr>\n<tr><td><code>&lt;duration&gt;</code></td><td><strong>Always <code>1000</code></strong> in generated configs.</td></tr>\n<tr><td><code>&lt;cadence&gt;</code></td><td><strong><code>48000 / fps</code></strong> rounded — 48 kHz sample rate divided by frame rate (e.g. <strong>50 fps → 960</strong>, <strong>60 fps → 800</strong>, <strong>25 fps → 1920</strong>).</td></tr>\n</tbody></table>\n<p>HighAsCG emits <code>time-scale = round(fps × 1000)</code>, <code>duration = 1000</code>, and <code>cadence = calculateCadence(fps)</code> (<code>src/config/config-generator.js</code> / <code>config-modes.js</code>).</p>\n<p>See <code>work/config_from_server.md</code> for a live example (<code>3072x1728</code> @ 60 Hz: <code>cadence</code> 800, <code>time-scale</code> 60000).</p>","wiki--guides--shader-fx":"<h1 id=\"shader-fx-audio-reactive-shader-templates\">Shader FX — audio-reactive shader templates</h1>\n<p>Paste a <strong>Shadertoy-style GLSL shader</strong>, save it, and play it like any other template. Saved shaders export as self-contained Caspar HTML templates in <strong><code>template/shaders/</code></strong> — fullscreen visuals or transparent overlays, optionally driven by live audio.</p>\n<h2 id=\"create-a-shader\">Create a shader</h2>\n<ol>\n<li><strong>Sources → Media</strong> tab → <strong><code>+</code></strong> button (bottom of the media list) → <strong>New shader…</strong> — opens the <strong>Shader FX</strong> modal.</li>\n<li>Click <strong>+ New shader</strong> and enter a <strong>Name</strong>. The saved id becomes <code>sh-&lt;name-slug&gt;</code> (Caspar path <code>shaders/sh-&lt;name-slug&gt;</code>).</li>\n<li>Tick <strong>audio reactive</strong> if the shader should react to sound; tick <strong>alpha</strong> for a transparent background (overlay use). Default is opaque black (fullscreen use).</li>\n<li>Paste the Shadertoy <strong>Image</strong> tab GLSL into <strong>Image (required)</strong>. Optional: paste the <strong>Common</strong> tab into Common, and <strong>Buffer A–D</strong> tabs into their sections (multipass).</li>\n<li>Wire each pass's inputs: <strong>iCh0–iCh3</strong> dropdowns map <code>iChannel0–3</code> to a buffer output (<strong>A/B/C/D</strong>) or the <strong>audio</strong> texture. Leave <code>—</code> for unused channels.</li>\n<li><strong>Save &amp; export</strong> — the status line shows the Caspar template path and the preview pane renders the exported page.</li>\n<li><strong>Templates</strong> tab → <strong>Refresh</strong> — the shader appears with an <strong>FX</strong> pill (and a ♪ badge in the modal list when audio-reactive).</li>\n</ol>\n<h2 id=\"put-it-on-air\">Put it on air</h2>\n<p>Drag the shader from the <strong>Templates</strong> tab onto PGM/PRV like any other template — no special playback path.</p>\n<p><strong>CG path needs GPU:</strong> Caspar's CEF templates render shaders only when GPU is enabled. In the Caspar device view (Settings), tick <strong>&quot;Enable GPU in CEF templates (WebGL / Shader FX)&quot;</strong>, then <strong>Apply Caspar config + restart</strong>. Without it the template has no WebGL2 and the layer stays black. Alternative for best audio: play the exported URL (<code>/templates/shaders/sh-&lt;id&gt;.html</code>) via a <strong>browser_display</strong> source.</p>\n<h2 id=\"editing-and-deleting\">Editing and deleting</h2>\n<ul>\n<li><strong>Templates</strong> tab → <strong>Edit</strong> button on any FX row opens the shader in the modal; or open the modal from the <code>+</code> menu and pick it from the list.</li>\n<li><strong>Save &amp; export</strong> with the same shader re-exports in place (same id/path). The config is baked into the exported template at save time — re-save after any change.</li>\n<li><strong>Delete</strong> removes both the config (<code>data/shaders/&lt;id&gt;.json</code>) and the exported template (<code>template/shaders/&lt;id&gt;.html</code>).</li>\n</ul>\n<h2 id=\"what-a-shader-can-use\">What a shader can use</h2>\n<p>Standard Shadertoy entry point <code>mainImage(out vec4 fragColor, in vec2 fragCoord)</code> and uniforms:</p>\n<table><thead><tr><th>Uniform</th><th>Notes</th></tr></thead><tbody>\n<tr><td><code>iResolution</code>, <code>iTime</code>, <code>iTimeDelta</code>, <code>iFrameRate</code>, <code>iFrame</code></td><td>as on Shadertoy</td></tr>\n<tr><td><code>iChannel0–3</code></td><td>only <strong>buffer A–D outputs</strong> or the <strong>audio</strong> texture (wired via the iCh dropdowns)</td></tr>\n<tr><td><code>iChannelTime[4]</code>, <code>iChannelResolution[4]</code>, <code>iDate</code></td><td>as on Shadertoy</td></tr>\n<tr><td><code>iMouse</code></td><td>exists but there is no mouse on air — treat as zero</td></tr>\n</tbody></table>\n<p><strong>Audio texture</strong> (channel wired to <code>audio</code>): 512×2, same layout as Shadertoy's audio input — row 0 = FFT spectrum, row 1 = waveform; sample the <code>.x</code>/<code>.r</code> component (e.g. <code>texture(iChannel0, vec2(f, 0.25)).x</code> for spectrum, <code>y ≈ 0.75</code> for waveform).</p>\n<p><strong>Audio reactivity by playback path</strong> (auto-selected):</p>\n<table><thead><tr><th>Path</th><th>Audio quality</th></tr></thead><tbody>\n<tr><td>browser_display source</td><td><strong>Real FFT</strong> via getUserMedia — picks an ALSA monitor/loopback input automatically; <code>?audioDev=&lt;substring&gt;</code> overrides</td></tr>\n<tr><td>Caspar CG / CEF template</td><td><strong>Coarse</strong> — level from the playout OSC meters, synthesized into a plausible spectrum; <code>?ch=&lt;caspar channel&gt;</code> picks the meter source (default 1)</td></tr>\n<tr><td>No audio available</td><td>Shader still renders; audio texture stays silent</td></tr>\n</tbody></table>\n<p><strong>Not supported</strong> (v1): Shadertoy texture/video/cubemap/keyboard/webcam channel assets (channels are buffers + audio only), sound shaders, VR, mouse interaction. CG <code>UPDATE</code> accepts <code>{ &quot;paused&quot;: true|false }</code> only — everything else is baked in at export.</p>\n<h2 id=\"limits-and-gotchas\">Limits and gotchas</h2>\n<ul>\n<li><strong>Name</strong> and a non-empty <strong>Image</strong> pass are required to save.</li>\n<li>Each pass source (and Common) is limited to <strong>256 KB</strong>.</li>\n<li>Rendering needs <strong>WebGL2</strong> — see the GPU note above for the CG path.</li>\n<li>CEF GPU is a global Caspar setting — turn it off if <strong>other channels stutter or tear</strong>, and use browser_display for shaders instead.</li>\n<li>New/renamed shaders show in the Templates tab only after a <strong>Refresh</strong> (Caspar TLS).</li>\n</ul>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table><thead><tr><th>Symptom</th><th>Check</th></tr></thead><tbody>\n<tr><td>Layer black on air</td><td>CEF GPU disabled — enable + Apply Caspar config + restart, or play via browser_display</td></tr>\n<tr><td>Shader missing from Templates</td><td><strong>Refresh</strong> the Templates tab (Caspar TLS)</td></tr>\n<tr><td>No audio reaction</td><td>A channel must be wired to <strong>audio</strong> <em>and</em> <strong>audio reactive</strong> ticked; CG path gives coarse levels only — use browser_display for real FFT</td></tr>\n<tr><td>Save fails</td><td>Missing name, empty Image pass, or a pass over 256 KB — the status line shows the reason</td></tr>\n<tr><td>Preview blank in the modal</td><td>Shader compile error — check the browser console for the GLSL error</td></tr>\n</tbody></table>\n<h2 id=\"related\">Related</h2>\n<ul>\n<li>CG template contract: <a href=\"#/wiki--api--cg\" data-wiki-page=\"1\">../api/cg.md</a></li>\n<li>Work orders: <code>work/work-orders/266_WO_SHADER_FX_AUDIO_REACTIVE.md</code>, <code>268_WO_SHADER_CEF_WEBGL_AND_CG_CONTINUITY.md</code></li>\n</ul>","guides--audio--audio_features_walkthrough":"<h1 id=\"audio-playout-monitoring-walkthrough\">Audio Playout &amp; Monitoring Walkthrough</h1>\n<p>This update enhances HighAsCG with &quot;CasparCG Enhanced&quot; features, specifically focusing on PortAudio support, multi-channel layouts (8ch/16ch), and hardware-level monitoring.</p>\n<h2 id=\"1-portaudio-output-setup\">1. PortAudio Output Setup</h2>\n<p>You can now assign physical audio outputs to your GPU (DP/HDMI) ports or dedicated audio cards.</p>\n<ol>\n<li>Open <strong>Device View</strong>.</li>\n<li>Click on a <strong>GPU</strong> or <strong>Audio</strong> port.</li>\n<li>In the inspector:</li>\n</ol>\n<ul>\n<li>Check <strong>Enable PortAudio</strong>.</li>\n<li>Select the physical <strong>Device</strong> from the dropdown (e.g., <code>NVidia HDMI 0</code>).</li>\n<li>Choose the <strong>Layout</strong> (Stereo, 4ch, 8ch, 16ch).</li>\n<li><strong>Save Audio Settings</strong>.</li>\n</ul>\n<h2 id=\"2-channel-patching-cross-patching\">2. Channel Patching (Cross-Patching)</h2>\n<p>If you need to route specific internal CasparCG channels to different physical outputs:</p>\n<ol>\n<li>In the same Audio Inspector, use the <strong>Pair Mapper</strong>.</li>\n<li>Example: To send internal channels <strong>Mix 7-8</strong> to physical <strong>Out 1-2</strong>:</li>\n</ol>\n<ul>\n<li>Set <code>Out 1-2 ← Mix 7-8</code>.</li>\n</ul>\n<ol>\n<li>Click <strong>Save Audio Settings</strong>.</li>\n<li>Apply the Caspar configuration (Settings → Apply).</li>\n</ol>\n<blockquote><p>[!TIP] In your Timeline or Looks, set the <strong>Audio Route</strong> for a layer to <code>7+8</code>. It will now play out of the physical channels 1-2 on that port.</p></blockquote>\n<h2 id=\"3-operator-hardware-monitor\">3. Operator Hardware Monitor</h2>\n<p>You can now have a dedicated &quot;Headphones&quot; or &quot;Monitor&quot; jack on the server that you control from the UI.</p>\n<h3 id=\"configuration\">Configuration</h3>\n<ol>\n<li>Go to <strong>Device View</strong> -&gt; <strong>Caspar host setup</strong> (the server icon).</li>\n<li>Under the <strong>Audio</strong> section:</li>\n</ol>\n<ul>\n<li>Check <strong>Enable Monitor Channel</strong>.</li>\n<li>Select the <strong>Monitor device</strong> (e.g., your motherboard's line-out).</li>\n<li>Save and Apply.</li>\n</ul>\n<h3 id=\"usage\">Usage</h3>\n<ol>\n<li>In the Top Bar, click the <strong>Headphones</strong> icon.</li>\n<li>Under <strong>Server Hardware Monitor</strong>, select the source:</li>\n</ol>\n<ul>\n<li><strong>PGM</strong>: Listen to the main program output.</li>\n<li><strong>PRV</strong>: Listen to the preview bus.</li>\n<li><strong>Multiview</strong>: Listen to the multiview audio.</li>\n</ul>\n<h2 id=\"4-troubleshooting\">4. Troubleshooting</h2>\n<ul>\n<li><strong>No audio?</strong> Ensure the <code>casparcg</code> user has permission to use the audio hardware. See the <a href=\"#/reference--sudo_and_audio_setup\" data-wiki-page=\"1\">Sudo and Audio Setup</a> guide.</li>\n<li><strong>PortAudio missing?</strong> Ensure your CasparCG build supports PortAudio and the <code>custom_live</code> profile is selected in HighAsCG settings.</li>\n</ul>","guides--audio--audio-setup-guide":"<h1 id=\"audio-setup-guide-casparcg-on-ubuntu\">Audio setup guide (CasparCG on Ubuntu)</h1>\n<p>Single entry point for <strong>multi-channel playout</strong>: follow one path below, then use <a href=\"#/reference--audio--audio-routing-reference\" data-wiki-page=\"1\">Audio routing reference</a> for MIXER / <code>route://</code> / FFmpeg mapping details.</p>\n<p><strong>Related:</strong> WO-06 Audio playout · Phase 5 (T5.1)</p>\n<table><thead><tr><th>Topic</th><th>Deep dive</th></tr></thead><tbody>\n<tr><td>List ALSA devices</td><td><a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA device enumeration</a></td></tr>\n<tr><td>NVIDIA HDMI/DP, 3.5 mm, USB</td><td><a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb\" data-wiki-page=\"1\">Caspar outputs: NVIDIA, stereo, USB</a></td></tr>\n<tr><td>Consumers in <code>casparcg.config</code></td><td><a href=\"#/reference--audio--caspar-audio-consumers\" data-wiki-page=\"1\">Caspar audio consumers</a></td></tr>\n<tr><td>AES67, Dante bridges, NDI tools, NetJACK, ROC</td><td><a href=\"#/reference--audio--network-audio-protocols\" data-wiki-page=\"1\">Network audio protocols</a></td></tr>\n<tr><td>PipeWire sinks / graph</td><td><a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">PipeWire multi-channel routing</a></td></tr>\n<tr><td>JACK alternative</td><td><a href=\"#/reference--audio--jack-audio-routing\" data-wiki-page=\"1\">JACK audio routing</a></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"before-you-start\">Before you start</h2>\n<ol>\n<li><strong>Sample rate:</strong> Caspar channels typically run at <strong>48 kHz</strong>; FFmpeg <code>-ar 48000</code> and hardware must agree.</li>\n<li><strong>Layout vs hardware:</strong> <code>channel-layout</code> on the channel (and any custom names in <code>&lt;audio&gt;&lt;channel-layouts&gt;</code>) must match what you send with <strong><code>-ac</code></strong> and what the sink can play.</li>\n<li><strong>Find <code>hw:N,M</code>:</strong> <code>aplay -l</code> (see enumeration doc). HighAsCG can list devices via <strong><code>GET /api/audio/devices</code></strong> when the app runs on the same machine as Caspar.</li>\n</ol>\n<hr>\n<h2 id=\"1-nvidia-hdmi-displayport\">1. NVIDIA HDMI / DisplayPort</h2>\n<p><strong>Goal:</strong> Send program audio over the GPU’s digital audio device (often <strong>2–8 channels</strong> on one link).</p>\n<ol>\n<li>Confirm driver: <code>lsmod | grep snd_hda_intel</code> (see <a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb#11-verify-kernel-audio-stack\" data-wiki-page=\"1\">§1.1</a>).</li>\n<li>Map the connector: <code>aplay -l | grep -iE 'nvidia|hdmi|display'</code> → note <strong>card</strong> and <strong>device</strong> → <code>hw:N,M</code>.</li>\n<li>Test: <code>aplay -D hw:N,M /usr/share/sounds/alsa/Front_Center.wav</code> (or any short WAV).</li>\n<li>Multi-channel: <code>speaker-test -D hw:N,M -c 8</code> if you need to validate <strong>&gt;2ch</strong> (see enumeration doc).</li>\n<li>Caspar: add <strong>FFmpeg consumer</strong> with <code>-f alsa hw:N,M</code> and <code>-ac</code> / codec matching your layout, or <strong><code>&lt;system-audio/&gt;</code></strong> if the default desktop sink should follow HDMI (less deterministic on headless servers).</li>\n</ol>\n<p><strong>Also read:</strong> <a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb#16-hdmi--displayport-into-a-video-switcher-embedded-audio-as-main-output\" data-wiki-page=\"1\">§1.6 — HDMI/DP into a video switcher</a> (embedded audio into switchers that de-embed / bridge to Dante).</p>\n<hr>\n<h2 id=\"2-usb-audio-interface\">2. USB audio interface</h2>\n<p><strong>Goal:</strong> Class-compliant <strong>multi-channel</strong> output (often <strong>8–32+</strong> channels).</p>\n<ol>\n<li>Plug in the interface; <code>aplay -l</code> should show the product name on a <strong>USB</strong> card.</li>\n<li>Match <strong>rate</strong> and <strong>channel count</strong> to the device spec (see <a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb#3-usb-audio-interface\" data-wiki-page=\"1\">§3</a>).</li>\n<li>Probe channels: <code>speaker-test -D hw:N,M -c 8</code> (adjust <code>-c</code>).</li>\n<li>Caspar: <strong>FFmpeg consumer</strong> <code>-f alsa hw:N,M</code> with <code>-codec:a pcm_s16le</code> or <code>pcm_s24le</code>, <code>-ar 48000</code>, <code>-ac N</code>.</li>\n</ol>\n<p><strong>Dante on the network:</strong> Linux has <strong>no</strong> Dante Virtual Soundcard; use a <strong>USB–Dante hardware bridge</strong> or AES67 mode — <a href=\"#/reference--audio--network-audio-protocols\" data-wiki-page=\"1\">Network audio protocols § T3.1</a>.</p>\n<hr>\n<h2 id=\"3-aes67-dante-bridge\">3. AES67 / Dante bridge</h2>\n<p><strong>Goal:</strong> Get audio into a Dante domain without DVS on Linux.</p>\n<ol>\n<li>Prefer a <strong>hardware bridge</strong> (e.g. USB interface with Dante ports): it appears as <strong>ALSA</strong> on Linux; route Caspar FFmpeg to <code>hw:N,M</code> like any USB device.</li>\n<li><strong>AES67 mode</strong> on Dante endpoints: configure in <strong>Dante Controller</strong> (Windows/Mac), then use a <strong>Linux AES67/RAVENNA</strong> stack or PipeWire module as described in <a href=\"#/reference--audio--network-audio-protocols\" data-wiki-page=\"1\">§ T3.1</a>.</li>\n</ol>\n<hr>\n<h2 id=\"4-ndi-network\">4. NDI (network)</h2>\n<p><strong>Goal:</strong> Send video + <strong>embedded audio</strong> to the LAN.</p>\n<ol>\n<li>Enable <strong>NDI consumer</strong> on the Caspar channel; set <code>&lt;name&gt;</code> uniquely (<a href=\"#/reference--audio--caspar-audio-consumers#t23--ndi-consumer-network-video--embedded-audio\" data-wiki-page=\"1\">§ T2.3</a>).</li>\n<li>Downstream: NDI Studio Monitor, FFmpeg with <code>libndi_newtek</code>, or hardware — <a href=\"#/reference--audio--network-audio-protocols\" data-wiki-page=\"1\">§ T3.2</a>.</li>\n</ol>\n<hr>\n<h2 id=\"5-pipewire-multi-channel-routing\">5. PipeWire multi-channel routing</h2>\n<p><strong>Goal:</strong> Virtual sinks, graph routing, or ROC — when a single <code>hw:</code> device is not enough.</p>\n<ol>\n<li>Install <strong>PipeWire</strong> + <strong>WirePlumber</strong> (default on recent Ubuntu).</li>\n<li>Optional: drop-in <strong>clock/quantum</strong> under <code>/etc/pipewire/pipewire.conf.d/</code> (<a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">PipeWire doc</a>).</li>\n<li>Create <strong>null-sinks</strong> or named sinks for each Caspar “bus”; point Caspar FFmpeg to <strong><code>-f pulse sink_name</code></strong>.</li>\n<li>Use <strong><code>qpwgraph</code></strong> to verify edges from Caspar’s process to the right sink.</li>\n</ol>\n<hr>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table><thead><tr><th>Symptom</th><th>Things to check</th></tr></thead><tbody>\n<tr><td><strong>No sound</strong></td><td>Wrong <code>hw:N,M</code>; cable/port; monitor/switcher input selected; <code>PULSE_SINK</code> / default sink for <code>&lt;system-audio/&gt;</code>.</td></tr>\n<tr><td><strong>Only 2 channels work</strong></td><td>Layout is <code>stereo</code> but you need <code>8ch</code> / custom; HDMI EDID limits; <code>-ac</code> too small.</td></tr>\n<tr><td><strong>Crackle / xruns</strong></td><td>Increase buffer (<code>-buffer_size</code> in FFmpeg args, or PipeWire quantum); avoid competing exclusive <code>hw</code> users.</td></tr>\n<tr><td><strong>Device busy</strong></td><td>Another app holds ALSA <code>hw</code> exclusively; use Pulse/PipeWire path or stop the other app.</td></tr>\n<tr><td><strong>Dante not in OS</strong></td><td>Expected on Linux without a bridge — use AES67 + Linux stack or USB–Dante hardware.</td></tr>\n</tbody></table>\n<h3 id=\"diagnostic-commands\">Diagnostic commands</h3>\n<pre><code class=\"language-bash\">aplay -l\npactl list short sinks          # PipeWire/Pulse sinks\npw-cli list-objects Node       # PipeWire nodes (best-effort)\nspeaker-test -D hw:N,M -c N</code></pre>\n<hr>\n<h2 id=\"highascg-settings\">HighAsCG settings</h2>\n<p>Application <strong>Settings → Audio / OSC</strong> stores <strong><code>audioRouting</code></strong> (program layout, ALSA/NDI/system/custom FFmpeg, extra buses). The <strong>config generator</strong> merges this into flat Caspar keys — see <code>src/config/config-generator.js</code> (<code>mergeAudioRoutingIntoConfig</code>).</p>","reference--audio--alsa-device-enumeration":"<h1 id=\"alsa-device-enumeration-linux\">ALSA device enumeration (Linux)</h1>\n<p>This guide explains how to <strong>list and interpret</strong> ALSA playback devices on Ubuntu (and similar distributions). Use it when wiring CasparCG <strong>FFmpeg</strong> or <strong>system-audio</strong> consumers to specific hardware (HDMI, USB interfaces, onboard jack) for multi-channel audio playout (WO-06).</p>\n<p><strong>Package:</strong> <code>alsa-utils</code> (<code>aplay</code>, <code>arecord</code>). Install if needed: <code>sudo apt install alsa-utils</code>.</p>\n<hr>\n<h2 id=\"list-playback-hardware-aplay--l\">List playback hardware: <code>aplay -l</code></h2>\n<p>Prints <strong>hardware</strong> PCM devices: one block per <strong>card</strong>, with <strong>device</strong> indices.</p>\n<pre><code class=\"language-bash\">aplay -l</code></pre>\n<p>Example (illustrative):</p>\n<pre><code>**** List of PLAYBACK Hardware Devices ****\ncard 0: PCH [HDA Intel PCH], device 0: ALC892 Analog [ALC892 Analog]\n  Subdevices: 1/1\n  Subdevice #0: subdevice #0\ncard 1: NVidia [HDA NVidia], device 3: HDMI 0 [HDMI 0]\n  Subdevices: 1/1\n  Subdevice #0: subdevice #0\ncard 2: USB [Scarlett 18i20 USB], device 0: USB Audio [USB Audio]\n  Subdevices: 0/1\n  ...</code></pre>\n<h3 id=\"how-to-read-it\">How to read it</h3>\n<table><thead><tr><th>Field</th><th>Meaning</th></tr></thead><tbody>\n<tr><td><strong>card N</strong></td><td>ALSA card index (0, 1, 2, …).</td></tr>\n<tr><td><strong>device M</strong></td><td>Device index on that card (0, 1, …).</td></tr>\n<tr><td>Bracket name</td><td>Human-readable name (chip, HDMI port, USB product).</td></tr>\n</tbody></table>\n<p><strong>ALSA PCM hardware ID:</strong> <code>hw:N,M</code> = card <strong>N</strong>, device <strong>M</strong>.</p>\n<p>Examples:</p>\n<ul>\n<li>Onboard analog: <code>hw:0,0</code></li>\n<li>NVIDIA HDMI on card 1, device 3: <code>hw:1,3</code></li>\n<li>USB interface on card 2, device 0: <code>hw:2,0</code></li>\n</ul>\n<p>Use these IDs in CasparCG FFmpeg paths such as <code>-f alsa hw:1,3</code> (see <code>config-generator</code> / WO-06 Phase 2).</p>\n<hr>\n<h2 id=\"list-pcm-names-including-plugins-aplay--l\">List PCM names (including plugins): <code>aplay -L</code></h2>\n<p>Prints <strong>all PCM device names</strong> ALSA knows, including <strong>soft aliases</strong> and <strong>plug</strong> devices (<code>plughw:</code>, <code>default</code>, <code>pulse</code>, <code>pipewire</code>, etc.).</p>\n<pre><code class=\"language-bash\">aplay -L</code></pre>\n<p>Typical patterns:</p>\n<table><thead><tr><th>Name</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>default</code></td><td>Often PipeWire/PulseAudio default sink (session-dependent).</td></tr>\n<tr><td><code>sysdefault:CARD=...</code></td><td>Card-specific default.</td></tr>\n<tr><td><code>hw:CARD=...,DEV=...</code></td><td>Named access to hardware (see <code>aplay -l</code> for indices).</td></tr>\n<tr><td><code>plughw:N,M</code></td><td>Hardware with automatic format conversion.</td></tr>\n</tbody></table>\n<p><strong>Rule:</strong> For <strong>low-latency, fixed-format</strong> routing from CasparCG, <strong>FFmpeg → ALSA</strong> usually targets <strong><code>hw:N,M</code></strong> or a <strong><code>plughw:N,M</code></strong> if sample format must be converted. For <strong>desktop default speaker</strong>, <code>default</code> or PipeWire may be easier but less deterministic on servers.</p>\n<hr>\n<h2 id=\"identify-device-types\">Identify device types</h2>\n<table><thead><tr><th>Clue in <code>aplay -l</code></th><th>Likely device</th></tr></thead><tbody>\n<tr><td><code>HDA Intel</code>, <code>Realtek</code>, <code>ALC</code>, <code>Analog</code></td><td>Onboard <strong>3.5 mm</strong> / internal codec.</td></tr>\n<tr><td><code>NVidia</code>, <code>HDMI</code>, <code>DP</code>, <code>DisplayPort</code></td><td><strong>GPU HDMI/DP</strong> audio (often <strong>up to 8ch</strong> on one HDMI).</td></tr>\n<tr><td><code>USB</code>, vendor name (Focusrite, RME, MOTU, …)</td><td><strong>USB audio interface</strong> (channel count varies).</td></tr>\n<tr><td><code>HD-Audio Generic</code></td><td>Could be onboard or secondary HDA; check <code>device</code> name.</td></tr>\n</tbody></table>\n<p>NVIDIA often exposes <strong>multiple HDMI/DP devices</strong> as separate <strong>device</strong> numbers on one <strong>card</strong>—pick the connector you use.</p>\n<p>That same HDMI/DP output is often used as the <strong>main</strong> multi-channel program feed into a <strong>video switcher</strong> that de-embeds audio and bridges to Dante or other networks—see <a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb\" data-wiki-page=\"1\">Caspar outputs: NVIDIA, stereo, USB</a> §1.6.</p>\n<hr>\n<h2 id=\"channel-capabilities\">Channel capabilities</h2>\n<p><code>aplay -l</code> does <strong>not</strong> always show max channel count. To probe:</p>\n<ol>\n<li><strong>Vendor specs</strong> for USB interfaces (e.g. 8-out, 16-out).</li>\n<li><strong>Test playback</strong> with a known multi-channel WAV and FFmpeg/ALSA (e.g. <code>-ac 8</code>), or use <code>speaker-test</code> if installed:</li>\n</ol>\n<pre><code class=\"language-bash\">   speaker-test -D hw:2,0 -c 8 -t wav</code></pre>\n<ol>\n<li>Inspect card details (if available):</li>\n</ol>\n<pre><code class=\"language-bash\">   cat /proc/asound/cards</code></pre>\n<ol>\n<li>For PipeWire, use <code>pw-cli</code>, <code>pw-dump</code>, or <code>wpctl status</code> to see <strong>node</strong> channel counts (see WO-06 Phase 1 T1.2 PipeWire section).</li>\n</ol>\n<p><strong>Caspar:</strong> match channel <strong>layout</strong> (<code>stereo</code>, <code>8ch</code>, custom <code>live-8ch</code>, etc.) to <strong>consumer</strong> <code>ac</code> / device capability.</p>\n<hr>\n<h2 id=\"quick-reference-card-index-vs-hw\">Quick reference: card index vs <code>hw</code></h2>\n<table><thead><tr><th>Goal</th><th>Command</th></tr></thead><tbody>\n<tr><td>Playback devices</td><td><code>aplay -l</code></td></tr>\n<tr><td>All PCM names</td><td><code>aplay -L</code></td></tr>\n<tr><td>Card summary</td><td><code>cat /proc/asound/cards</code></td></tr>\n<tr><td>Record devices (same cards, capture side)</td><td><code>arecord -l</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"mapping-checklist-for-casparcg-highascg\">Mapping checklist for CasparCG / HighAsCG</h2>\n<ol>\n<li>Run <code>aplay -l</code> on the <strong>same machine</strong> that runs CasparCG.</li>\n<li>Note <strong>card</strong> and <strong>device</strong> for each output you need.</li>\n<li>Build FFmpeg ALSA path: <code>-f alsa hw:N,M</code> (or <code>plughw:N,M</code> if required).</li>\n<li>Align <strong>sample rate</strong> (<code>-ar</code>) and <strong>channel count</strong> (<code>-ac</code>) with the Caspar channel <strong>video-mode</strong> cadence and <strong>channel-layout</strong>.</li>\n<li>Document the chosen <code>hw:N,M</code> in your station runbook (firewall and WO-06 <strong>T1.4</strong> NVIDIA notes apply separately).</li>\n</ol>\n<hr>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><a href=\"#/reference--audio--jack-audio-routing\" data-wiki-page=\"1\">JACK audio routing</a></li>\n<li><a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb\" data-wiki-page=\"1\">Caspar outputs: NVIDIA, 3.5 mm stereo, USB</a></li>\n<li>Work Order 06 — Audio playout (internal WO, optional local <code>work/</code> copy)</li>\n<li>Caspar wiki: FFmpeg consumer, system-audio consumer</li>\n<li><code>.reference/</code> in repo (if present) for Caspar audio consumer XML</li>\n</ul>","reference--audio--alsa-mixer-api":"<h1 id=\"alsa-mixer-api-server-handoff\">ALSA mixer API (server handoff)</h1>\n<p><strong>Server:</strong> <code>src/audio/alsa-mixer.js</code> · <strong>Routes:</strong> <code>src/api/routes-audio.js</code></p>\n<p>Client UI: <strong>Application Settings → Live audio → ALSA mixer</strong> (<code>client/components/settings-alsa-mixer-panel.js</code>).</p>\n<p>The web client exposes alsamixer-style sliders (card picker, Playback/Capture/All view, volume faders, mute, enum/switch controls). It does <strong>not</strong> shell out to <code>amixer</code> in the browser — the playout server must wrap ALSA on Linux.</p>\n<p>Fallback: <strong>Launch alsamixer</strong> calls <code>POST /api/system/gui-launch</code> with <code>action: &quot;alsamixer&quot;</code> (same pattern as <code>nvidia-settings</code>). That path is optional but recommended when the REST API is unavailable or for power users.</p>\n<hr>\n<h2 id=\"requirements\">Requirements</h2>\n<table><thead><tr><th>Item</th><th>Notes</th></tr></thead><tbody>\n<tr><td>OS</td><td>Linux with ALSA (<code>/dev/snd/*</code>)</td></tr>\n<tr><td>Binaries</td><td><code>amixer</code> in <code>PATH</code> (alsa-utils). <code>alsamixer</code> only needed for gui-launch fallback.</td></tr>\n<tr><td>User</td><td>Process user must be allowed to open mixer controls (typically member of <code>audio</code> group).</td></tr>\n<tr><td>Non-Linux</td><td>Return <strong>501</strong> or <strong>503</strong> with a clear JSON error; client shows a warning and offers Launch alsamixer only where applicable.</td></tr>\n</tbody></table>\n<p>Existing <code>GET /api/audio/devices</code> (ALSA card list) is already used elsewhere; card names in the mixer API should match those entries when possible.</p>\n<hr>\n<h2 id=\"endpoints\">Endpoints</h2>\n<h3 id=\"get-apiaudioalsa-mixer\"><code>GET /api/audio/alsa-mixer</code></h3>\n<p>Query parameters:</p>\n<table><thead><tr><th>Param</th><th>Type</th><th>Default</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>card</code></td><td>int</td><td><code>0</code></td><td>ALSA card index (<code>-c</code> for amixer)</td></tr>\n<tr><td><code>refresh</code></td><td><code>1</code></td><td>off</td><td>Bypass server cache and re-run <code>amixer</code></td></tr>\n</tbody></table>\n<p><strong>Response <code>200</code></strong> — JSON object:</p>\n<pre><code class=\"language-json\">{\n  &quot;card&quot;: 2,\n  &quot;cards&quot;: [\n    { &quot;card&quot;: 0, &quot;name&quot;: &quot;HDA Intel PCH&quot; },\n    { &quot;card&quot;: 2, &quot;name&quot;: &quot;USB Audio Device&quot; }\n  ],\n  &quot;controls&quot;: [\n    {\n      &quot;name&quot;: &quot;Master&quot;,\n      &quot;index&quot;: 0,\n      &quot;type&quot;: &quot;volume&quot;,\n      &quot;playback&quot;: true,\n      &quot;capture&quot;: false,\n      &quot;min&quot;: 0,\n      &quot;max&quot;: 100,\n      &quot;value&quot;: 87,\n      &quot;percent&quot;: 87,\n      &quot;dB&quot;: -12.5,\n      &quot;mute&quot;: false,\n      &quot;channels&quot;: [&quot;Front Left&quot;, &quot;Front Right&quot;]\n    },\n    {\n      &quot;name&quot;: &quot;Mic&quot;,\n      &quot;type&quot;: &quot;volume&quot;,\n      &quot;playback&quot;: false,\n      &quot;capture&quot;: true,\n      &quot;min&quot;: 0,\n      &quot;max&quot;: 100,\n      &quot;value&quot;: 60,\n      &quot;percent&quot;: 60,\n      &quot;mute&quot;: false,\n      &quot;channels&quot;: [&quot;Capture&quot;]\n    },\n    {\n      &quot;name&quot;: &quot;Mic Boost&quot;,\n      &quot;type&quot;: &quot;enum&quot;,\n      &quot;items&quot;: [\n        { &quot;value&quot;: &quot;0dB&quot;, &quot;label&quot;: &quot;0dB&quot; },\n        { &quot;value&quot;: &quot;20dB&quot;, &quot;label&quot;: &quot;20dB&quot; }\n      ],\n      &quot;item&quot;: &quot;0dB&quot;\n    },\n    {\n      &quot;name&quot;: &quot;Auto Gain Control&quot;,\n      &quot;type&quot;: &quot;boolean&quot;,\n      &quot;value&quot;: 1,\n      &quot;on&quot;: true\n    }\n  ]\n}</code></pre>\n<p><strong>Alternative shape</strong> (client also accepts):</p>\n<pre><code class=\"language-json\">{\n  &quot;card&quot;: 0,\n  &quot;cards&quot;: [ ... ],\n  &quot;playback&quot;: [ /* controls with playback: true */ ],\n  &quot;capture&quot;: [ /* controls with capture: true */ ]\n}</code></pre>\n<p>Or <code>elements</code> instead of <code>controls</code>.</p>\n<h4 id=\"control-object-fields\">Control object fields</h4>\n<table><thead><tr><th>Field</th><th>Required</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>name</code></td><td>yes</td><td>Simple control name (amixer <code>sname</code>), e.g. <code>&quot;Master&quot;</code>, <code>&quot;PCM&quot;</code>, <code>&quot;Mic&quot;</code></td></tr>\n<tr><td><code>type</code></td><td>yes</td><td><code>&quot;volume&quot;</code> (default), <code>&quot;enum&quot;</code> / <code>&quot;enumerated&quot;</code>, <code>&quot;boolean&quot;</code> / <code>&quot;switch&quot;</code></td></tr>\n<tr><td><code>playback</code></td><td>volume</td><td><code>true</code> if control has playback capability</td></tr>\n<tr><td><code>capture</code></td><td>volume</td><td><code>true</code> if control has capture capability</td></tr>\n<tr><td><code>min</code>, <code>max</code></td><td>volume</td><td>Raw ALSA range (optional if <code>percent</code> is always 0–100)</td></tr>\n<tr><td><code>value</code></td><td>volume</td><td>Raw ALSA value <strong>or</strong> 0–100 percent (see below)</td></tr>\n<tr><td><code>percent</code></td><td>volume</td><td><strong>Preferred:</strong> 0–100 for UI slider</td></tr>\n<tr><td><code>dB</code></td><td>no</td><td>Display only, e.g. <code>-18.06</code></td></tr>\n<tr><td><code>mute</code> / <code>muted</code></td><td>volume</td><td><code>true</code> when playback/capture is off</td></tr>\n<tr><td><code>channels</code></td><td>no</td><td>Human labels, e.g. <code>[&quot;Front Left&quot;, &quot;Front Right&quot;]</code></td></tr>\n<tr><td><code>items</code></td><td>enum</td><td>Array of strings or <code>{ value, label }</code></td></tr>\n<tr><td><code>item</code></td><td>enum</td><td>Currently selected item string</td></tr>\n<tr><td><code>index</code></td><td>no</td><td>ALSA control index; use if names are ambiguous</td></tr>\n</tbody></table>\n<p><strong>View filtering (client-side):</strong> Playback view shows controls with <code>playback: true</code> (or name heuristics). Capture view shows <code>capture: true</code>. Enum/boolean controls appear in all views.</p>\n<p><strong>Errors:</strong></p>\n<table><thead><tr><th>Status</th><th>Body</th><th>When</th></tr></thead><tbody>\n<tr><td>400</td><td><code>{ &quot;error&quot;: &quot;invalid card&quot; }</code></td><td>Bad <code>card</code> query</td></tr>\n<tr><td>404</td><td><code>{ &quot;error&quot;: &quot;card not found&quot; }</code></td><td>No such ALSA card</td></tr>\n<tr><td>501/503</td><td><code>{ &quot;error&quot;: &quot;ALSA mixer not available on this host&quot; }</code></td><td>Non-Linux or no <code>amixer</code></td></tr>\n<tr><td>500</td><td><code>{ &quot;error&quot;: &quot;...&quot; }</code></td><td><code>amixer</code> failed</td></tr>\n</tbody></table>\n<hr>\n<h3 id=\"post-apiaudioalsa-mixer\"><code>POST /api/audio/alsa-mixer</code></h3>\n<p>Set one control. Body (JSON):</p>\n<pre><code class=\"language-json\">{ &quot;card&quot;: 2, &quot;name&quot;: &quot;Mic&quot;, &quot;percent&quot;: 75 }</code></pre>\n<table><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>card</code></td><td>int</td><td>ALSA card index (required)</td></tr>\n<tr><td><code>name</code></td><td>string</td><td>Control name (required unless <code>index</code> given)</td></tr>\n<tr><td><code>index</code></td><td>int</td><td>Optional ALSA control index</td></tr>\n<tr><td><code>percent</code></td><td>int 0–100</td><td>Set volume by percentage (volume controls)</td></tr>\n<tr><td><code>value</code></td><td>number</td><td>Raw ALSA value <strong>or</strong> <code>0</code>/<code>1</code> for boolean</td></tr>\n<tr><td><code>mute</code></td><td>bool</td><td><code>true</code> → mute, <code>false</code> → unmute (volume controls)</td></tr>\n<tr><td><code>item</code></td><td>string</td><td>Enum item name (enum controls)</td></tr>\n</tbody></table>\n<p>The client sends:</p>\n<ul>\n<li>Slider drag: <code>{ card, name, percent }</code> (debounced ~100 ms)</li>\n<li>Mute toggle: <code>{ card, name, mute: true|false }</code></li>\n<li>Enum change: <code>{ card, name, item: &quot;20dB&quot; }</code></li>\n<li>Switch: <code>{ card, name, value: 0|1 }</code></li>\n</ul>\n<p><strong>Response <code>200</code>:</strong></p>\n<pre><code class=\"language-json\">{ &quot;ok&quot;: true, &quot;card&quot;: 2, &quot;name&quot;: &quot;Mic&quot;, &quot;percent&quot;: 75, &quot;mute&quot;: false }</code></pre>\n<p>Optional: return the updated control object in the same shape as GET.</p>\n<p><strong>Errors:</strong> same as GET; add <code>404</code> if control name not found, <code>409</code> if control is read-only.</p>\n<hr>\n<h2 id=\"suggested-server-implementation-amixer\">Suggested server implementation (amixer)</h2>\n<h3 id=\"list-cards\">List cards</h3>\n<pre><code class=\"language-bash\"># Card names (pick one approach)\naplay -l\n# or parse /proc/asound/cards</code></pre>\n<p>Reuse logic from existing <code>GET /api/audio/devices</code> if it already enumerates ALSA cards.</p>\n<h3 id=\"list-controls-get\">List controls (GET)</h3>\n<pre><code class=\"language-bash\">amixer -c &lt;card&gt; scontents</code></pre>\n<p>Parse “Simple mixer control” blocks:</p>\n<ul>\n<li>Name from <code>Simple mixer control 'Master',0</code></li>\n<li>Capabilities: <code>pvolume</code>, <code>cvolume</code>, <code>pswitch</code>, <code>cswitch</code>, <code>penum</code>, <code>cenum</code>, …</li>\n<li>Limits: <code>Limits: Playback 0 - 65536</code> / <code>Limits: Capture 0 - 65536</code></li>\n<li>Values: <code>Front Left: Playback 32768 [50%] [-18.06dB] [on]</code></li>\n</ul>\n<p>Map to JSON:</p>\n<ul>\n<li><code>type: &quot;volume&quot;</code> when <code>pvolume</code> or <code>cvolume</code></li>\n<li><code>playback: true</code> if <code>pvolume</code> or <code>pswitch</code> in capabilities</li>\n<li><code>capture: true</code> if <code>cvolume</code> or <code>cswitch</code></li>\n<li><code>percent</code> from <code>[50%]</code> in amixer output (simplest for the client)</li>\n<li><code>mute: true</code> when channel shows <code>[off]</code></li>\n<li><code>type: &quot;enum&quot;</code> when <code>penum</code>/<code>cenum</code>; items from <code>Item0: '0dB'</code> lines</li>\n<li><code>type: &quot;boolean&quot;</code> for on/off switches without volume range</li>\n</ul>\n<p><strong>Mono controls:</strong> single channel line still yields one <code>percent</code> / <code>mute</code>.</p>\n<p><strong>Caching:</strong> optional short TTL (1–2 s) unless <code>refresh=1</code>.</p>\n<h3 id=\"set-volume-post-percent\">Set volume (POST percent)</h3>\n<pre><code class=\"language-bash\">amixer -c &lt;card&gt; sset '&lt;name&gt;' &lt;percent&gt;%</code></pre>\n<h3 id=\"set-mute-post-mute\">Set mute (POST mute)</h3>\n<pre><code class=\"language-bash\">amixer -c &lt;card&gt; sset '&lt;name&gt;' mute\namixer -c &lt;card&gt; sset '&lt;name&gt;' unmute\n# or toggle: amixer -c &lt;card&gt; sset '&lt;name&gt;' toggle</code></pre>\n<h3 id=\"set-enum-post-item\">Set enum (POST item)</h3>\n<pre><code class=\"language-bash\">amixer -c &lt;card&gt; sset '&lt;name&gt;' '&lt;item&gt;'</code></pre>\n<h3 id=\"set-boolean-post-value\">Set boolean (POST value)</h3>\n<pre><code class=\"language-bash\">amixer -c &lt;card&gt; sset '&lt;name&gt;' on\namixer -c &lt;card&gt; sset '&lt;name&gt;' off</code></pre>\n<p>Use <code>name</code> with proper quoting; prefer <code>sset 'Mic'</code> over numeric index unless names collide.</p>\n<hr>\n<h2 id=\"post-apisystemgui-launch-alsamixer\"><code>POST /api/system/gui-launch</code> — <code>alsamixer</code></h2>\n<p>Client payload:</p>\n<pre><code class=\"language-json\">{ &quot;action&quot;: &quot;alsamixer&quot;, &quot;password&quot;: &quot;&lt;optional nuclear password&gt;&quot; }</code></pre>\n<p>Expected behaviour (mirror <code>nvidia-settings</code>):</p>\n<ol>\n<li>Honour nuclear password gate if configured.</li>\n<li>Run on display <code>:0</code>, e.g. <code>DISPLAY=:0 alsamixer -c &lt;defaultCard&gt;</code> in background.</li>\n<li>Response: <code>{ &quot;ok&quot;: true, &quot;exe&quot;: &quot;alsamixer&quot; }</code> or similar.</li>\n</ol>\n<p>If only one card matters, default to card <code>0</code> or the card from server settings / <code>default_alsa_card</code>.</p>\n<hr>\n<h2 id=\"security-and-ops\">Security and ops</h2>\n<ul>\n<li>Mixer changes affect <strong>machine-wide</strong> ALSA levels (capture gain, headphone volume). Restrict API to the same trust boundary as other <code>/api/audio/*</code> routes.</li>\n<li>Avoid shell injection: never interpolate unsanitized <code>name</code> or <code>item</code> into a shell string; use <code>execFile('amixer', ['-c', String(card), 'sset', name, ...])</code>.</li>\n<li>Log set operations at info level (<code>card</code>, <code>name</code>, <code>percent</code>/<code>mute</code>/<code>item</code>) for support.</li>\n<li>PipeWire/Pulse systems: <code>amixer</code> may target a different layer than PipeWire volume; document host behaviour if both are present. Prefer the card USB devices use for Caspar <code>alsa://hw:N,M</code> capture.</li>\n</ul>\n<hr>\n<h2 id=\"client-files-reference\">Client files (reference)</h2>\n<table><thead><tr><th>File</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>client/lib/alsa-mixer-api.js</code></td><td>GET/POST wrappers + response normalization</td></tr>\n<tr><td><code>client/components/settings-alsa-mixer-panel.js</code></td><td>Settings UI</td></tr>\n<tr><td><code>client/components/settings-live-audio-panel.js</code></td><td>Mounts mixer under Live audio tab</td></tr>\n<tr><td><code>client/components/settings-modal.js</code></td><td><strong>Live audio</strong> settings tab</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"test-checklist\">Test checklist</h2>\n<ol>\n<li><code>GET /api/audio/alsa-mixer?card=0</code> returns <code>cards</code> + non-empty <code>controls</code> on a Linux box with USB audio.</li>\n<li><code>POST</code> with <code>{ &quot;percent&quot;: 50 }</code> moves hardware level; <code>GET</code> reflects new <code>percent</code>.</li>\n<li><code>POST</code> with <code>{ &quot;mute&quot;: true }</code> mutes; UI shows <strong>M</strong> on refresh.</li>\n<li>Capture view: <code>Mic</code> / <code>Capture</code> controls visible when <code>capture: true</code>.</li>\n<li>Invalid card → 404 JSON error (client shows warning).</li>\n<li>macOS/Windows dev → 501 (client shows fallback message).</li>\n<li><code>gui-launch</code> + <code>alsamixer</code> opens terminal UI on <code>:0</code> when REST API missing.</li>\n</ol>\n<hr>\n<h2 id=\"minimal-stub-for-integration-tests\">Minimal stub (for integration tests)</h2>\n<p>Until full parsing exists, a stub that returns one fake control will render UI but sliders will fail on POST. Minimum viable product:</p>\n<ol>\n<li>Parse <code>amixer -c N scontents</code> into <code>controls[]</code> with <code>name</code>, <code>type</code>, <code>percent</code>, <code>mute</code>, <code>playback</code>, <code>capture</code>.</li>\n<li>Implement POST <code>percent</code> + <code>mute</code> only (covers Master/PCM/Mic).</li>\n</ol>\n<p>Enum/boolean can follow in a second pass; the client already renders them when present.</p>","reference--audio--audio-routing-reference":"<h1 id=\"audio-routing-reference-casparcg-highascg\">Audio routing reference (CasparCG + HighAsCG)</h1>\n<p>Quick reference for <strong>layer/channel audio levels</strong>, <strong>layouts</strong>, <strong>routing between channels</strong>, and <strong>FFmpeg audio filters</strong>. For Linux device setup, see <a href=\"#/guides--audio--audio-setup-guide\" data-wiki-page=\"1\">Audio setup guide</a>.</p>\n<p><strong>Related:</strong> WO-06 Audio playout · Phase 5 (T5.2) · <a href=\"#/reference--audio--caspar-audio-consumers\" data-wiki-page=\"1\">Caspar audio consumers</a></p>\n<hr>\n<h2 id=\"amcp-mixer-audio\">AMCP: MIXER audio</h2>\n<p>Audio on a channel is controlled with <strong>MIXER</strong> subcommands. Layer index follows Caspar conventions (<code>channel-layer</code>).</p>\n<table><thead><tr><th>Action</th><th>AMCP (typical)</th><th>Notes</th></tr></thead><tbody>\n<tr><td><strong>Layer volume</strong></td><td><code>MIXER 1-10 VOLUME 0.5</code></td><td><code>0</code> = mute, <code>1</code> = unity (check your Caspar version for exact semantics).</td></tr>\n<tr><td><strong>Layer volume + tween</strong></td><td><code>MIXER 1-10 VOLUME 0.5 25 linear</code></td><td>Duration in frames, tween name.</td></tr>\n<tr><td><strong>Channel master</strong></td><td><code>MIXER 1 MASTERVOLUME 0.5</code></td><td>Affects the whole channel mix (no layer index on <code>MASTERVOLUME</code>).</td></tr>\n<tr><td><strong>Query</strong></td><td><code>MIXER 1-10 VOLUME</code></td><td>No value → query current (if supported).</td></tr>\n</tbody></table>\n<p><strong>HighAsCG HTTP:</strong> <code>POST /api/audio/volume</code> with JSON, e.g. <code>{ &quot;channel&quot;: 1, &quot;layer&quot;: 10, &quot;volume&quot;: 0.5 }</code> or <code>{ &quot;channel&quot;: 1, &quot;master&quot;: true, &quot;volume&quot;: 0.8 }</code> — see <code>src/api/routes-audio.js</code>.</p>\n<hr>\n<h2 id=\"channel-layouts-summary\">Channel layouts (summary)</h2>\n<p>Standard and custom layouts are set on the <strong>channel</strong> in <code>casparcg.config</code> and may require entries under <strong><code>&lt;audio&gt;&lt;channel-layouts&gt;</code></strong> for custom names (<code>4ch</code>, <code>16ch</code>, <code>live-8ch</code>, …). Full table: <a href=\"#/reference--audio--caspar-audio-consumers#t25--channel-layouts-and-audiochannel-layouts\" data-wiki-page=\"1\">T2.5</a>.</p>\n<table><thead><tr><th>Channels</th><th>Typical layout id</th><th>Notes</th></tr></thead><tbody>\n<tr><td>1</td><td><code>mono</code></td><td></td></tr>\n<tr><td>2</td><td><code>stereo</code></td><td>Default stereo bus</td></tr>\n<tr><td>4</td><td><code>4ch</code> (custom)</td><td>Define in <code>&lt;audio&gt;</code> if your build requires it</td></tr>\n<tr><td>6</td><td><code>dts</code> / 5.1 layouts</td><td></td></tr>\n<tr><td>8</td><td><code>8ch</code>, <code>live-8ch</code>, <code>dolby-e</code></td><td><code>live-8ch</code> often needs explicit channel-order in config</td></tr>\n<tr><td>16</td><td><code>16ch</code> (custom)</td><td>Stems / multi-bus</td></tr>\n</tbody></table>\n<p><strong>Runtime layout change (if enabled on server):</strong> <code>SET 1 CHANNEL_LAYOUT stereo</code> — verify against your Caspar build; not all deployments expose this.</p>\n<hr>\n<h2 id=\"multi-bus-routing-concept\">Multi-bus routing (concept)</h2>\n<p>Program audio is mixed on <strong>channel 1</strong> (example). <strong>Extra channels</strong> act as <strong>buses</strong>: you <strong>play</strong> a <code>route://</code> source into a layer on the bus channel, then send that bus to ALSA/NDI/etc.</p>\n<pre><code class=\"language-mermaid\">flowchart LR\n  subgraph pgm [Program channel e.g. ch1]\n    L1[Layers with clips]\n  end\n  subgraph bus [Audio bus channel e.g. ch5]\n    R[Layer: route:// from PGM]\n    M[MIXER VOLUME on bus]\n    F[FFmpeg consumer to hw or Pulse]\n  end\n  L1 --&gt;|&quot;PLAY bus-layer route://&quot;| R\n  R --&gt; M --&gt; F</code></pre>\n<ol>\n<li>Configure an <strong>audio-only</strong> (or minimal video-mode) <strong>channel</strong> with the right <strong>channel-layout</strong> and consumers (<a href=\"#/reference--audio--caspar-audio-consumers#t24--audio-only-channels-extra-channels\" data-wiki-page=\"1\">T2.4</a>).</li>\n<li>From AMCP: load/play <strong><code>route://</code></strong> (or equivalent) pointing at the program channel’s audio mix — exact producer string depends on Caspar version; consult your server docs.</li>\n<li>Use <strong>MIXER VOLUME</strong> on the <strong>routed layer</strong> for sub-mix level.</li>\n</ol>\n<hr>\n<h2 id=\"ffmpeg-audio-filters-af-on-play\">FFmpeg audio filters (<code>AF</code> on PLAY)</h2>\n<p>Caspar can pass <strong>FFmpeg audio filters</strong> on <strong>PLAY</strong> for per-clip remapping (see WO-06 Context: <code>PLAY 1-10 clip AF &quot;...&quot;</code>).</p>\n<p><strong>Stereo → duplicate to two buses (illustrative <code>pan</code>):</strong></p>\n<pre><code class=\"language-text\">pan=stereo|c0=c0|c1=c1</code></pre>\n<p><strong>4-output matrix example (adjust channel indices to your layout):</strong></p>\n<pre><code class=\"language-text\">pan=4c|c0=0.5*c0+0.5*c1|c1=c2|c2=c3|c3=c0</code></pre>\n<p>Always validate filter graphs against <strong>FFmpeg</strong> docs for your deployed version; start with <strong>unity</strong> routing and add gain <strong>coefficients</strong> in small steps to avoid clipping.</p>\n<hr>\n<h2 id=\"osc\">OSC</h2>\n<p>Real-time meters and transport feedback use <strong>OSC</strong> from Caspar to clients (see <strong><code>docs/osc-integration.md</code></strong>). This does not replace MIXER for level control unless you add automation that sends AMCP in response.</p>","reference--audio--caspar-audio-consumers":"<h1 id=\"casparcg-audio-consumers-config-reference\">CasparCG audio consumers (config reference)</h1>\n<p>How to declare <strong>audio outputs</strong> in <code>casparcg.config</code>: system default sink, FFmpeg to ALSA/Pulse, NDI, audio-only channels, and <strong>channel layouts</strong>. Complements the Linux stack guides (<a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA</a>, <a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">PipeWire</a>, <a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb\" data-wiki-page=\"1\">NVIDIA / stereo / USB</a>).</p>\n<p><strong>Consolidated:</strong> <a href=\"#/guides--audio--audio-setup-guide\" data-wiki-page=\"1\">Audio setup guide</a> · <a href=\"#/reference--audio--audio-routing-reference\" data-wiki-page=\"1\">Audio routing reference</a> (MIXER, buses, FFmpeg <code>AF</code>).</p>\n<p><strong>Context:</strong> WO-06 Audio playout · Phase 2 (T2.1–T2.5)</p>\n<hr>\n<h2 id=\"t21-system-audio-consumer\">T2.1 — <code>&lt;system-audio&gt;</code> consumer</h2>\n<p>Sends the channel mix to the <strong>default</strong> desktop audio sink (PipeWire / PulseAudio / ALSA default, depending on session).</p>\n<pre><code class=\"language-xml\">&lt;consumers&gt;\n  &lt;system-audio&gt;\n    &lt;channel-layout&gt;stereo&lt;/channel-layout&gt;\n  &lt;/system-audio&gt;\n&lt;/consumers&gt;</code></pre>\n<table><thead><tr><th>Topic</th><th>Notes</th></tr></thead><tbody>\n<tr><td><strong>Layout</strong></td><td>Match <code>&lt;channel-layout&gt;</code> on the channel (e.g. <code>stereo</code>, <code>5.1</code>) to what the sink supports.</td></tr>\n<tr><td><strong>Count</strong></td><td><strong>At most one</strong> <code>&lt;system-audio&gt;</code> per Caspar <strong>channel</strong>.</td></tr>\n<tr><td><strong>Choosing the device</strong></td><td>Caspar follows the process default. Set the session <strong>default output</strong> in OS settings, or set <strong><code>PULSE_SINK=&lt;sink_name&gt;</code></strong> (PulseAudio compatibility layer used by PipeWire) so the default sink is the intended HDMI / 3.5 mm / USB device.</td></tr>\n</tbody></table>\n<p>FFmpeg consumers are <strong>not</strong> required for basic “play through speakers” if this consumer is enough.</p>\n<hr>\n<h2 id=\"t22-ffmpeg-consumer-explicit-device-or-pulse-sink\">T2.2 — FFmpeg consumer (explicit device or Pulse sink)</h2>\n<p>Use when you need a <strong>specific</strong> ALSA card/device or a <strong>named</strong> Pulse/PipeWire sink (see <a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">virtual sinks</a>).</p>\n<h3 id=\"alsa-hardware\">ALSA hardware</h3>\n<pre><code class=\"language-xml\">&lt;ffmpeg-consumer&gt;\n  &lt;path&gt;-f alsa hw:N,M&lt;/path&gt;\n  &lt;args&gt;-codec:a pcm_s24le -ar 48000 -ac 8&lt;/args&gt;\n&lt;/ffmpeg-consumer&gt;</code></pre>\n<p>Replace <code>N,M</code> using <code>aplay -l</code>. Align <strong><code>-ar</code></strong> and <strong><code>-ac</code></strong> with the channel <strong>video-mode</strong> cadence and <strong>channel-layout</strong>.</p>\n<h3 id=\"pulse-pipewire-named-sink\">Pulse / PipeWire named sink</h3>\n<pre><code class=\"language-xml\">&lt;ffmpeg-consumer&gt;\n  &lt;path&gt;-f pulse caspar_audio_bus_1&lt;/path&gt;\n  &lt;args&gt;-codec:a pcm_s24le -ar 48000 -ac 2&lt;/args&gt;\n&lt;/ffmpeg-consumer&gt;</code></pre>\n<p>Create the sink first (<code>pactl load-module module-null-sink sink_name=caspar_audio_bus_1 …</code>). Multiple <strong><code>&lt;ffmpeg-consumer&gt;</code></strong> blocks on the <strong>same</strong> channel are allowed—each can target a different sink or device.</p>\n<p><strong>Note:</strong> In HighAsCG’s config generator, FFmpeg blocks use element name <code>&lt;ffmpeg-consumer&gt;</code> (see <code>src/config/config-generator.js</code>); upstream Caspar examples sometimes say <code>&lt;ffmpeg&gt;</code>—follow your Caspar build’s schema.</p>\n<hr>\n<h2 id=\"t23-ndi-consumer-network-video-embedded-audio\">T2.3 — NDI consumer (network video + embedded audio)</h2>\n<p>NDI carries <strong>embedded audio</strong> with the stream; receivers on the LAN can decode both.</p>\n<pre><code class=\"language-xml\">&lt;ndi&gt;\n  &lt;name&gt;CasparCG-Audio-PGM&lt;/name&gt;\n&lt;/ndi&gt;</code></pre>\n<p>Audio channel count follows the Caspar <strong>channel layout</strong> for that channel. Useful when a downstream tool or hardware takes <strong>NDI</strong> and splits or meters audio.</p>\n<hr>\n<h2 id=\"t24-audio-only-channels-extra-channels\">T2.4 — Audio-only channels (extra channels)</h2>\n<p>Use <strong>extra</strong> channels with <strong>no</strong> screen consumer as <strong>buses</strong>: route program audio with <strong><code>PLAY … route://</code></strong> (or equivalent routing) and adjust <strong>MIXER</strong> / <strong>MASTERVOLUME</strong> on routed layers.</p>\n<p>Example shape (video mode and layout must exist in your Caspar build; <code>PAL</code> is a common minimal mode for audio-only channels):</p>\n<pre><code class=\"language-xml\">&lt;channel&gt;\n  &lt;video-mode&gt;PAL&lt;/video-mode&gt;\n  &lt;channel-layout&gt;16ch&lt;/channel-layout&gt;\n  &lt;consumers&gt;\n    &lt;ffmpeg-consumer&gt;\n      &lt;path&gt;-f alsa hw:USB_AUDIO&lt;/path&gt;\n      &lt;args&gt;-codec:a pcm_s24le -ar 48000 -ac 16&lt;/args&gt;\n    &lt;/ffmpeg-consumer&gt;\n  &lt;/consumers&gt;\n&lt;/channel&gt;</code></pre>\n<p>Replace <code>hw:USB_AUDIO</code> with your real <code>hw:N,M</code> or Pulse sink. HighAsCG’s <strong>audio mixer</strong> / routing UI (when enabled) uses the same idea: <strong>extra audio channels</strong> in generated config.</p>\n<hr>\n<h2 id=\"t25-channel-layouts-and-audiochannel-layouts\">T2.5 — Channel layouts and <code>&lt;audio&gt;&lt;channel-layouts&gt;</code></h2>\n<p>Caspar ships standard layouts (<code>stereo</code>, <code>5.1</code>, <code>7.1</code>, …). For <strong>4 / 8 / 16</strong> discrete buses you often define <strong>custom</strong> layouts in the global <strong><code>&lt;audio&gt;</code></strong> section:</p>\n<pre><code class=\"language-xml\">&lt;audio&gt;\n  &lt;channel-layouts&gt;\n    &lt;channel-layout name=&quot;8ch&quot; type=&quot;8ch&quot; num-channels=&quot;8&quot;\n      channel-order=&quot;FL FR FC LFE BL BR SL SR&quot;/&gt;\n    &lt;channel-layout name=&quot;16ch&quot; type=&quot;16ch&quot; num-channels=&quot;16&quot;\n      channel-order=&quot;c0 c1 c2 c3 c4 c5 c6 c7 c8 c9 c10 c11 c12 c13 c14 c15&quot;/&gt;\n  &lt;/channel-layouts&gt;\n&lt;/audio&gt;</code></pre>\n<p>Then reference them on a channel:</p>\n<pre><code class=\"language-xml\">&lt;channel&gt;\n  &lt;video-mode&gt;1080p5000&lt;/video-mode&gt;\n  &lt;channel-layout&gt;8ch&lt;/channel-layout&gt;\n  &lt;consumers&gt;\n    &lt;!-- … --&gt;\n  &lt;/consumers&gt;\n&lt;/channel&gt;</code></pre>\n<table><thead><tr><th>Channels</th><th>Typical layout id</th><th>Use</th></tr></thead><tbody>\n<tr><td>2</td><td><code>stereo</code></td><td>L/R</td></tr>\n<tr><td>4</td><td>custom <code>4ch</code> (define in XML)</td><td>Stereo + aux, etc.</td></tr>\n<tr><td>8</td><td><code>7.1</code> or custom <code>8ch</code></td><td>Surround or multi-bus</td></tr>\n<tr><td>16</td><td>custom <code>16ch</code></td><td>Stems / buses</td></tr>\n</tbody></table>\n<p>HighAsCG <strong>config generator</strong> can emit <code>live-8ch</code> and other layouts when needed (<code>buildAudioLayoutsXml</code> in <code>src/config/config-generator.js</code>).</p>\n<hr>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><a href=\"#/reference--audio--network-audio-protocols\" data-wiki-page=\"1\">Network audio protocols (AES67/Dante, NDI, NetJACK, ROC)</a></li>\n<li>WO-06 Phase 4 &amp; 5 — HighAsCG settings API / config generator integration when implemented</li>\n<li><a href=\"https://github.com/CasparCG/help/wiki\">Caspar wiki / server docs</a> (consumer syntax varies slightly by version)</li>\n</ul>","reference--audio--caspar-outputs-nvidia-stereo-usb":"<h1 id=\"casparcg-audio-outputs-on-linux-nvidia-35-mm-stereo-usb\">CasparCG audio outputs on Linux: NVIDIA, 3.5 mm stereo, USB</h1>\n<p>Practical guide for the three most common <strong>local</strong> playout paths on Ubuntu:</p>\n<ol>\n<li><strong>NVIDIA GPU</strong> — HDMI / DisplayPort embedded audio (often <strong>up to 8 channels</strong> on one link). On many installs this is the <strong>main</strong> multi-channel program output, not only a local monitor feed—see §1.6.</li>\n<li><strong>3.5 mm jack</strong> — onboard <strong>stereo</strong> analog (headphones / line out).</li>\n<li><strong>USB audio interface</strong> — class-compliant <strong>multi-channel</strong> (2–32+ outputs depending on hardware).</li>\n</ol>\n<p><strong>Prerequisite:</strong> know how to read <code>aplay -l</code> and map <strong><code>hw:N,M</code></strong> — see <a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA device enumeration</a>. For PipeWire routing, see <a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">PipeWire multi-channel routing</a>.</p>\n<p><strong>Context:</strong> WO-06 Audio playout</p>\n<hr>\n<h2 id=\"quick-pick\">Quick pick</h2>\n<table><thead><tr><th>Goal</th><th>Typical ALSA clue (<code>aplay -l</code>)</th><th>Caspar channel layout</th><th>FFmpeg consumer hint</th></tr></thead><tbody>\n<tr><td><strong>Monitor / TV</strong> over HDMI</td><td><code>NVidia</code>, <code>HDMI</code>, <code>DP</code></td><td><code>stereo</code> … <code>8ch</code> (HDMI 2.0 often <strong>8ch</strong> capable)</td><td><code>-f alsa hw:N,M</code> + <code>-ar 48000</code> <code>-ac 2</code> … <code>8</code></td></tr>\n<tr><td><strong>Switch / router</strong> (HDMI/DP → de-embed / Dante / buses)</td><td>Same — cable to switcher input</td><td><code>stereo</code> … <code>8ch</code> (match switcher embed path)</td><td>Same <code>hw:N,M</code> path; downstream device handles network audio and routing</td></tr>\n<tr><td><strong>Simple stereo</strong> room / cue</td><td><code>Analog</code>, <code>ALC</code>, onboard HDA</td><td><code>stereo</code></td><td><code>-ac 2</code></td></tr>\n<tr><td><strong>Multi-bus / stems</strong></td><td>USB vendor name, many channels</td><td>custom <code>8ch</code> / <code>16ch</code> / <code>live-8ch</code></td><td><code>-ac</code> matches layout</td></tr>\n</tbody></table>\n<p>Always set <strong>sample rate</strong> (<code>-ar</code>, usually <strong>48000</strong>) and <strong>channel count</strong> (<code>-ac</code>) to match the Caspar <strong>channel</strong> video-mode cadence and <strong>channel-layout</strong> in <code>casparcg.config</code>.</p>\n<hr>\n<h2 id=\"1-nvidia-hdmi-displayport-audio\">1. NVIDIA HDMI / DisplayPort audio</h2>\n<h3 id=\"11-verify-kernel-audio-stack\">1.1 Verify kernel audio stack</h3>\n<p>NVIDIA HDMI audio typically uses <strong>Intel HDA</strong> binding to the GPU:</p>\n<pre><code class=\"language-bash\">lsmod | grep snd_hda_intel</code></pre>\n<p>You should see <code>snd_hda_intel</code> (and often <code>snd_hda_codec_hdmi</code>). If the module is missing, fix GPU / driver install first (proprietary <code>nvidia</code> driver vs nouveau affects which codec nodes appear).</p>\n<h3 id=\"12-find-the-alsa-device\">1.2 Find the ALSA device</h3>\n<pre><code class=\"language-bash\">aplay -l | grep -i -E 'nvidia|hdmi|display'</code></pre>\n<p>Note <strong>card</strong> and <strong>device</strong> (e.g. <code>card 1, device 3</code> → <strong><code>hw:1,3</code></strong>). Many GPUs expose <strong>one HDMI port per device number</strong>—use the connector that matches your display cable.</p>\n<h3 id=\"13-test-playback\">1.3 Test playback</h3>\n<pre><code class=\"language-bash\">aplay -D hw:1,3 /usr/share/sounds/alsa/Front_Center.wav</code></pre>\n<p>(Replace path with any stereo WAV; use a multi-channel test file to validate <strong>&gt;2ch</strong> if needed.)</p>\n<h3 id=\"14-optional-alsa-alias-etcasoundconf-or-asoundrc\">1.4 Optional ALSA alias (<code>/etc/asound.conf</code> or <code>~/.asoundrc</code>)</h3>\n<pre><code class=\"language-conf\">pcm.nvidia_hdmi_main {\n  type hw\n  card 1\n  device 3\n}</code></pre>\n<p>Then FFmpeg can use <code>-f alsa nvidia_hdmi_main</code> instead of raw <code>hw:1,3</code>.</p>\n<h3 id=\"15-casparcg-notes\">1.5 CasparCG notes</h3>\n<ul>\n<li><strong>Multi-channel:</strong> HDMI often supports <strong>up to 8ch</strong>; confirm with <code>speaker-test -D hw:N,M -c 8</code> and your display/sink capabilities.</li>\n<li><strong>Multiple displays:</strong> you may have <strong>several</strong> <code>device</code> entries—one per output; pick the active monitor chain.</li>\n<li>Example consumer args (8-channel): <code>-f alsa hw:1,3 -codec:a pcm_s24le -ar 48000 -ac 8</code> (match your <code>channel-layout</code> and codec choices to Caspar’s expectations).</li>\n</ul>\n<h3 id=\"16-hdmi-displayport-into-a-video-switcher-embedded-audio-as-main-output\">1.6 HDMI / DisplayPort into a video switcher (embedded audio as “main” output)</h3>\n<p>Treat the NVIDIA <strong>HDMI or DisplayPort</strong> link as a normal <strong>multi-channel digital audio output</strong>, not only as “sound to a monitor.” Many <strong>video switchers and production routers</strong> accept <strong>HDMI/DP with embedded PCM</strong> on their inputs, <strong>de-embed</strong> that audio internally, and expose it on <strong>Dante</strong>, MADI, analog buses, or other network / facility routing—so the GPU path can be your <strong>primary</strong> playout into house audio, IFB, or a Dante domain, with the switcher acting as <strong>bridge</strong> between Caspar’s embedded stream and the rest of the plant.</p>\n<p>Design checks: match <strong>channel count</strong> and <strong>sample rate</strong> to what the switcher documents for that input; confirm on the switcher that the correct <strong>input port</strong> and <strong>audio mapping</strong> (per-channel vs downmix) match your Caspar <strong>channel-layout</strong>. If the switcher is also the <strong>monitor</strong> path, you still use the same ALSA <code>hw:N,M</code> for Caspar—the difference is <strong>system role</strong> (program vs confidence), not a different Linux device type.</p>\n<hr>\n<h2 id=\"2-35-mm-stereo-onboard-analog\">2. 3.5 mm stereo (onboard analog)</h2>\n<h3 id=\"21-identify-the-device\">2.1 Identify the device</h3>\n<p>In <code>aplay -l</code>, look for <strong>Analog</strong>, <strong>Front</strong>, <strong>Headphones</strong>, or codec names (<strong>ALC892</strong>, <strong>Realtek</strong>, etc.) on the <strong>motherboard audio</strong> card (often <strong>card 0</strong>).</p>\n<p>Example mapping: <code>card 0, device 0</code> → <strong><code>hw:0,0</code></strong>.</p>\n<h3 id=\"22-caspar-configuration\">2.2 Caspar configuration</h3>\n<ul>\n<li>Use channel layout <strong><code>stereo</code></strong> (or default stereo mapping) on that channel.</li>\n<li><strong>FFmpeg consumer:</strong> e.g. <code>-f alsa hw:0,0 -codec:a pcm_s16le -ar 48000 -ac 2</code></li>\n<li><strong>System-audio consumer:</strong> sends to the <strong>default</strong> desktop sink (PipeWire/Pulse); good for quick tests on a workstation, less deterministic on a headless server than explicit <code>hw:</code>.</li>\n</ul>\n<h3 id=\"23-pipewire-default-sink\">2.3 PipeWire / default sink</h3>\n<p>If Caspar uses <strong>Pulse</strong> (<code>-f pulse &lt;sink&gt;</code>) or <code>&lt;system-audio/&gt;</code>, the <strong>default</strong> output may be the 3.5 mm jack when that’s the selected OS output—use <code>qpwgraph</code> or OS sound settings to confirm which physical port is active.</p>\n<hr>\n<h2 id=\"3-usb-audio-interface\">3. USB audio interface</h2>\n<h3 id=\"31-class-compliant-devices\">3.1 Class-compliant devices</h3>\n<p>Most <strong>USB</strong> interfaces appear automatically as an ALSA card:</p>\n<pre><code class=\"language-bash\">aplay -l</code></pre>\n<p>Look for <strong>USB</strong> and the vendor/model. Note <strong><code>hw:N,0</code></strong> (device index is often <code>0</code>).</p>\n<h3 id=\"32-channel-count-and-rate\">3.2 Channel count and rate</h3>\n<ul>\n<li><strong>Match Caspar</strong> channel layout to the interface: e.g. <strong>8 outputs</strong> → layout <strong><code>8ch</code></strong> (or custom <code>live-8ch</code> in generated config) and <strong><code>-ac 8</code></strong> in FFmpeg.</li>\n<li><strong>Sample rate</strong> must match the channel clock (typically <strong>48000</strong> Hz in broadcast Caspar setups). Mismatch causes pitch/speed errors or device refusal.</li>\n</ul>\n<p>Verify channels (example: card 2, device 0):</p>\n<pre><code class=\"language-bash\">speaker-test -D hw:2,0 -c 8 -t wav</code></pre>\n<h3 id=\"33-recommended-multi-channel-gear-linux-friendly\">3.3 Recommended multi-channel gear (Linux-friendly)</h3>\n<table><thead><tr><th>Device class</th><th>Notes</th></tr></thead><tbody>\n<tr><td><strong>RME Digiface USB</strong></td><td>Very high channel count; class-compliant on Linux.</td></tr>\n<tr><td><strong>MOTU UltraLite</strong></td><td>Many channels; verify UAC2 mode if needed.</td></tr>\n<tr><td><strong>Focusrite Scarlett</strong></td><td>Often needs <strong>firmware / class mode</strong> for full channel count—check vendor Linux notes.</td></tr>\n</tbody></table>\n<h3 id=\"34-casparcg-ffmpeg-example-8-outputs\">3.4 CasparCG FFmpeg example (8 outputs)</h3>\n<pre><code class=\"language-xml\">&lt;ffmpeg-consumer&gt;\n  &lt;path&gt;-f alsa hw:2,0&lt;/path&gt;\n  &lt;args&gt;-codec:a pcm_s24le -ar 48000 -ac 8&lt;/args&gt;\n&lt;/ffmpeg-consumer&gt;</code></pre>\n<p>Adjust <code>hw:2,0</code>, codec, and <code>-ac</code> to your card and <code>&lt;channel-layout&gt;</code>.</p>\n<hr>\n<h2 id=\"troubleshooting-all-three\">Troubleshooting (all three)</h2>\n<table><thead><tr><th>Symptom</th><th>What to check</th></tr></thead><tbody>\n<tr><td>No sound</td><td>Wrong <code>hw:N,M</code>; another app holds the device exclusively; mute in <code>alsamixer</code>.</td></tr>\n<tr><td>NVIDIA silent</td><td>Cable on correct HDMI/DP port; monitor audio input selected; try another <code>device</code> index.</td></tr>\n<tr><td>USB only 2 ch</td><td>Interface in <strong>class-1</strong> mode; driver limitation—vendor firmware / UAC2.</td></tr>\n<tr><td>Crackling</td><td>Buffer size; try <code>plughw:</code>; raise PipeWire quantum (see PipeWire doc).</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><a href=\"#/reference--audio--caspar-audio-consumers\" data-wiki-page=\"1\">Caspar audio consumers (system-audio, FFmpeg, NDI, layouts)</a></li>\n<li><a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA device enumeration</a></li>\n<li><a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">PipeWire multi-channel routing</a></li>\n</ul>","reference--audio--jack-audio-routing":"<h1 id=\"jack-audio-routing-linux-alternative-to-pipewire-graph\">JACK audio routing (Linux alternative to PipeWire graph)</h1>\n<p><strong>JACK</strong> (JACK Audio Connection Kit) is a low-latency audio server with a patchable <strong>client graph</strong>. It fits when you want <strong>deterministic</strong> routing between apps, <strong>multiple ALSA devices</strong> via bridges, or <strong>JACK-native</strong> clients/plugins. On many desktops <strong>PipeWire</strong> already covers similar use cases and exposes a <strong>JACK-compatible API</strong>; see <a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">PipeWire multi-channel routing</a> first.</p>\n<p><strong>Context:</strong> WO-06 Audio playout · <strong>T1.3</strong></p>\n<hr>\n<h2 id=\"install\">Install</h2>\n<p>Ubuntu / Debian (example):</p>\n<pre><code class=\"language-bash\">sudo apt update\nsudo apt install jackd2 qjackctl</code></pre>\n<p>Optional:</p>\n<table><thead><tr><th>Package</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>qjackctl</code></td><td>Patchbay, connections, <strong>JACK start/stop</strong> GUI.</td></tr>\n<tr><td><code>carla</code></td><td>Plugin host / advanced routing (optional).</td></tr>\n<tr><td><code>alsa-utils</code></td><td><code>aplay -l</code> for device IDs — see <a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA enumeration</a>.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"start-jack-with-the-alsa-backend-jackd\">Start JACK with the ALSA backend (<code>jackd</code>)</h2>\n<p>Pick <strong>one</strong> ALSA playback device to be the <strong>master</strong> clock for JACK (from <code>aplay -l</code>: card <strong>N</strong>, often device <strong>0</strong>).</p>\n<p>Example: card <strong>1</strong>, 48 kHz, 256 frames/period, 2 periods (adjust for stability vs latency):</p>\n<pre><code class=\"language-bash\">jackd -R -d alsa -d hw:1 -r 48000 -p 256 -n 2</code></pre>\n<table><thead><tr><th>Flag</th><th>Meaning</th></tr></thead><tbody>\n<tr><td><code>-R</code></td><td>Realtime scheduling (often needs <code>limits.conf</code> / <code>audio</code> group).</td></tr>\n<tr><td><code>-d alsa</code></td><td>ALSA backend.</td></tr>\n<tr><td><code>-d hw:1</code></td><td>ALSA <strong>card</strong> index (not <code>hw:1,3</code>—JACK ALSA driver uses <strong>card</strong>; use separate tools for subdevices if needed).</td></tr>\n<tr><td><code>-r 48000</code></td><td>Sample rate (match Caspar / broadcast chain).</td></tr>\n<tr><td><code>-p 256</code></td><td>Period size in frames.</td></tr>\n<tr><td><code>-n 2</code></td><td>Periods in the buffer (latency ∝ <code>p * n</code>).</td></tr>\n</tbody></table>\n<p>Run in a terminal or via <strong>qjackctl → Setup → Parameters → Start</strong>. Stop with Ctrl+C or qjackctl.</p>\n<p><strong>Exclusive access:</strong> while <code>jackd</code> holds <code>hw:N</code>, other apps (including Caspar <strong>FFmpeg <code>-f alsa hw:N</code></strong> to the same device) may fail with “device busy”. Plan <strong>one</strong> owner of the hardware or use bridges below.</p>\n<hr>\n<h2 id=\"multi-device-routing-alsa_in-alsa_out\">Multi-device routing: <code>alsa_in</code> / <code>alsa_out</code></h2>\n<p>When you need <strong>more than one</strong> ALSA card (e.g. USB + onboard), JACK can expose extra devices via:</p>\n<ul>\n<li><strong><code>alsa_in</code></strong> — ALSA capture → JACK inputs (e.g. line in on another card).</li>\n<li><strong><code>alsa_out</code></strong> — JACK outputs → ALSA playback on a <strong>second</strong> device.</li>\n</ul>\n<p>Typical pattern: JACK master on <strong>card A</strong>; route mix through JACK to <strong>card B</strong> with <code>alsa_out</code>:</p>\n<pre><code class=\"language-bash\">alsa_out -d hw:2,0 -j mon_out</code></pre>\n<p>Flags vary by version; use <code>alsa_out --help</code>. Connect <strong>JACK ports</strong> to <code>mon_out</code> in <code>qjackctl</code> <strong>Graph</strong>.</p>\n<p>This is how you achieve <strong>CasparCG → (ALSA or Pulse) → … → JACK → multiple hardware outputs</strong>: Caspar does not speak JACK natively; you either:</p>\n<ol>\n<li>Send Caspar <strong>FFmpeg</strong> to an ALSA device that is <strong>not</strong> the JACK master, then <strong>bridge</strong> into JACK with <code>alsa_in</code>, or</li>\n<li>Use <strong>PipeWire</strong> with Pulse compatibility and patch in <code>qpwgraph</code>, or</li>\n<li>Use a <strong>JACK sink</strong> if your FFmpeg build supports <strong><code>-f jack</code></strong> (uncommon in stock Caspar builds—verify <code>ffmpeg -formats | grep jack</code>).</li>\n</ol>\n<p>For many <strong>CasparCG</strong> deployments, <strong>direct ALSA <code>hw:</code></strong> or <strong>Pulse/PipeWire</strong> sinks are simpler than a full JACK graph. Use JACK when you specifically need its <strong>graph + multi-card bridges</strong> or <strong>JACK-only clients</strong>; otherwise PipeWire/ALSA is often enough.</p>\n<hr>\n<h2 id=\"qjackctl-workflow\"><code>qjackctl</code> workflow</h2>\n<ol>\n<li><strong>Setup:</strong> choose <strong>Interface</strong> (ALSA driver), <strong>Sample rate</strong>, <strong>Frames/period</strong>, <strong>Periods/buffer</strong>.</li>\n<li><strong>Start</strong> JACK.</li>\n<li><strong>Graph</strong> (or <strong>Connect</strong>): wire <strong>outputs</strong> (playback clients) to <strong>inputs</strong> (capture / <code>alsa_out</code> ports).</li>\n<li>Save a <strong>patchbay</strong> preset for repeatable shows.</li>\n</ol>\n<hr>\n<h2 id=\"jackd-vs-pipewire-jack-layer\"><code>jackd</code> vs PipeWire JACK layer</h2>\n<ul>\n<li><strong>PipeWire</strong> can expose <strong>JACK-compatible</strong> sockets so JACK clients connect without a separate <code>jackd</code> (depends on install: <code>pipewire-jack</code>, <code>pw-jack</code>).</li>\n<li>If you already use PipeWire as the session manager, prefer <strong>that</strong> stack unless you have a specific <strong>jackd2</strong> workflow.</li>\n</ul>\n<hr>\n<h2 id=\"casparcg-reminder\">CasparCG (reminder)</h2>\n<ul>\n<li>Caspar <strong>FFmpeg consumers</strong> normally use <strong><code>-f alsa</code></strong> or <strong><code>-f pulse</code></strong>.</li>\n<li>Avoid <strong>double-opening</strong> the same <code>hw:</code> from Caspar and <code>jackd</code>.</li>\n<li>See <a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb\" data-wiki-page=\"1\">Caspar outputs: NVIDIA, stereo, USB</a> and WO-06 <strong>Phase 2</strong> for XML examples.</li>\n</ul>\n<hr>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table><thead><tr><th>Issue</th><th>What to check</th></tr></thead><tbody>\n<tr><td><code>jackd</code> won’t start</td><td>Wrong <code>hw:</code> card; device in use; try larger <code>-p</code>.</td></tr>\n<tr><td>Xruns (red in qjackctl)</td><td>Increase period; close other apps; CPU governor.</td></tr>\n<tr><td>No permission for realtime</td><td>User in <code>audio</code> group; <code>/etc/security/limits.d/</code> <code>rtprio</code> / <code>memlock</code>.</td></tr>\n<tr><td>Second interface silent</td><td><code>alsa_out</code> / graph not wired; wrong <code>-d hw:</code>.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA device enumeration</a></li>\n<li><a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">PipeWire multi-channel routing</a></li>\n<li><a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb\" data-wiki-page=\"1\">Caspar outputs: NVIDIA, stereo, USB</a></li>\n</ul>","reference--audio--network-audio-protocols":"<h1 id=\"network-audio-protocols-linux-casparcg-context\">Network audio protocols (Linux + CasparCG context)</h1>\n<p>Options for moving <strong>audio over the network</strong> or bridging <strong>Dante / AES67</strong> to Linux. Use with local I/O guides: <a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA</a>, <a href=\"#/reference--audio--caspar-audio-consumers\" data-wiki-page=\"1\">Caspar audio consumers</a>, <a href=\"#/reference--audio--pipewire-multichannel-routing\" data-wiki-page=\"1\">PipeWire</a>.</p>\n<p><strong>Context:</strong> WO-06 Audio playout · Phase 3 (T3.1–T3.4)</p>\n<hr>\n<h2 id=\"t31-aes67-and-dante-compatible-setups\">T3.1 — AES67 and Dante-compatible setups</h2>\n<p><strong>Dante Virtual Soundcard does not run on Linux.</strong> On Linux you typically use one of:</p>\n<ol>\n<li><strong>AES67</strong> — Dante devices can expose an <strong>AES67</strong> stream if enabled in <strong>Dante Controller</strong> (usually on a Windows or macOS machine on the same network). Other gear (or Linux with a suitable stack) subscribes to that stream.</li>\n<li><strong>Linux driver</strong> — Some vendors ship <strong>RAVENNA / AES67</strong>-style drivers that expose <strong>ALSA</strong> devices (e.g. Merging’s commercial Linux offerings). Treat them like any other ALSA card in Caspar <strong>FFmpeg</strong> consumers (<code>-f alsa hw:…</code>).</li>\n<li><strong>Hardware USB bridge</strong> — A <strong>USB</strong> interface that speaks <strong>Dante</strong> on the network but shows up on Linux as a <strong>normal multi-channel ALSA</strong> device (e.g. RME Digiface Dante, Audinate AVIO USB). Dante routing and labels stay in <strong>Dante Controller</strong> on another host; Caspar only sees <code>aplay -l</code> and <code>hw:N,M</code>.</li>\n</ol>\n<p><strong>Workflow sketch:</strong> enable AES67 on the Dante side → ensure multicast / PTP / network requirements from the vendor → on Linux either use a <strong>driver</strong> that creates ALSA nodes or a <strong>USB bridge</strong> so Caspar never needs a Windows-only driver.</p>\n<hr>\n<h2 id=\"t32-ndi-audio-extraction\">T3.2 — NDI audio extraction</h2>\n<p>Caspar’s <strong>NDI consumer</strong> sends <strong>video + embedded audio</strong> on the LAN (see <a href=\"#/reference--audio--caspar-audio-consumers\" data-wiki-page=\"1\">caspar-audio-consumers § T2.3</a>).</p>\n<p><strong>Receiving / monitoring elsewhere</strong></p>\n<table><thead><tr><th>Tool</th><th>Notes</th></tr></thead><tbody>\n<tr><td><strong>NDI Studio Monitor</strong></td><td>Common for monitoring; runs on Windows/macOS (check current NDI SDK / vendor docs).</td></tr>\n<tr><td><strong>FFmpeg</strong> with NDI input</td><td>On a machine with a build that includes <code>libndi_newtek</code>, you can decode and send audio to ALSA, e.g.: <code>ffmpeg -f libndi_newtek -i &quot;SOURCE_NAME&quot; -f alsa hw:0</code> (names match what NDI advertises).</td></tr>\n<tr><td><strong>go2rtc</strong></td><td>If you already use go2rtc (e.g. streaming WO), some pipelines can fork NDI → other outputs; keep latency and licensing in mind.</td></tr>\n</tbody></table>\n<p>Exact <strong>FFmpeg</strong> flags and stream names depend on build (<code>ffmpeg -decoders | grep ndi</code>) and your NDI source name as shown in NDI tools.</p>\n<hr>\n<h2 id=\"t33-jack-network-audio-netjack\">T3.3 — JACK network audio (NetJACK)</h2>\n<p><strong>NetJACK</strong> sends <strong>JACK audio streams over a LAN</strong> between machines running JACK.</p>\n<p>Typical pattern:</p>\n<ol>\n<li><strong>Server</strong> (e.g. Caspar host): run <strong><code>jackd</code></strong> with the ALSA backend as usual (<a href=\"#/reference--audio--jack-audio-routing\" data-wiki-page=\"1\">local JACK doc</a>).</li>\n<li><strong>Client</strong> (receiver): run <strong><code>jack_netsource</code></strong> (or the matching NetJACK2 tool for your version) pointing at the server’s IP and NetJACK port.</li>\n</ol>\n<p>You get <strong>multiple channels</strong> (often 32+ depending on configuration) with <strong>low latency</strong> on a solid wired LAN. <strong>Firewall:</strong> allow the TCP/UDP ports your NetJACK build uses.</p>\n<p>Caspar does not output JACK natively; audio reaches JACK via <strong>ALSA bridges</strong> (<code>alsa_in</code> / <code>alsa_out</code>) or other paths described in the JACK doc. NetJACK is for <strong>distributing</strong> JACK streams, not for replacing Caspar’s own consumers.</p>\n<hr>\n<h2 id=\"t34-pipewire-roc-real-time-open-codec\">T3.4 — PipeWire ROC (Real-time Open Codec)</h2>\n<p><strong>ROC</strong> is a network stack for streaming audio (part of the broader PipeWire ecosystem). Typical CLI tools:</p>\n<ul>\n<li><strong><code>roc-send</code></strong> — sender on the machine that has the audio stream (e.g. near Caspar or a loopback).</li>\n<li><strong><code>roc-recv</code></strong> — receiver on the listening machine.</li>\n</ul>\n<p>Use cases: <strong>LAN</strong> streaming between Linux hosts when you want <strong>ROC</strong>’s codec and latency model instead of raw RTP or NDI. Install <strong>roc-toolkit</strong> / distro packages as available; options and port defaults change by version—see <code>roc-send --help</code> / <code>roc-recv --help</code>.</p>\n<p>This is <strong>orthogonal</strong> to Caspar: you would still take audio from Caspar <strong>FFmpeg</strong> / <strong>system-audio</strong> / <strong>ALSA</strong> and feed the ROC sender, unless you patch at the PipeWire graph level.</p>\n<hr>\n<h2 id=\"dante-linux-reminders-from-wo-06\">Dante / Linux reminders (from WO-06)</h2>\n<ul>\n<li><strong>Dante Virtual Soundcard:</strong> not supported on Linux — use <strong>AES67</strong>, <strong>hardware bridge</strong>, or <strong>RAVENNA-class</strong> drivers where applicable.</li>\n<li><strong>SoundGrid (Waves):</strong> control software is not Linux-targeted for typical station workflows.</li>\n<li><strong>Reliable Dante-to-ALSA on Linux:</strong> USB <strong>Dante</strong> interfaces that enumerate as standard ALSA devices are often the least fragile path.</li>\n</ul>\n<hr>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><a href=\"#/reference--audio--caspar-audio-consumers\" data-wiki-page=\"1\">Caspar audio consumers</a> (NDI, FFmpeg)</li>\n<li><a href=\"#/reference--audio--jack-audio-routing\" data-wiki-page=\"1\">JACK audio routing</a> (local graph)</li>\n<li>WO-06 Important Notes</li>\n</ul>","reference--audio--pipewire-multichannel-routing":"<h1 id=\"pipewire-multi-channel-routing-linux\">PipeWire multi-channel routing (Linux)</h1>\n<p><a href=\"https://pipewire.org/\">PipeWire</a> is the default audio server on <strong>Ubuntu 24.04+</strong> (with <strong>WirePlumber</strong> as the session/policy manager). Use it to route <strong>multi-channel</strong> CasparCG output (FFmpeg → Pulse/PipeWire, or system default sink) to HDMI, USB, or other devices with flexible graph routing.</p>\n<p><strong>Companion doc:</strong> <a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA device enumeration</a> · WO-06</p>\n<hr>\n<h2 id=\"packages\">Packages</h2>\n<p>Typical desktop/session install:</p>\n<pre><code class=\"language-bash\">sudo apt update\nsudo apt install pipewire pipewire-pulse wireplumber qpwgraph</code></pre>\n<p>Useful extras:</p>\n<table><thead><tr><th>Package</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><code>qpwgraph</code></td><td>Visual patchbay (nodes ↔ links).</td></tr>\n<tr><td><code>pipewire-audio-client-libraries</code></td><td>Dev headers (optional).</td></tr>\n</tbody></table>\n<p>Verify the <strong>user</strong> session is running PipeWire (common on desktop; headless may differ):</p>\n<pre><code class=\"language-bash\">systemctl --user status pipewire pipewire-pulse wireplumber</code></pre>\n<hr>\n<h2 id=\"sample-rate-and-buffer-size-drop-in-config\">Sample-rate and buffer size (drop-in config)</h2>\n<p>CasparCG channel modes are usually <strong>48 kHz</strong>-aligned. You can set <strong>default clock</strong> and <strong>quantum</strong> (buffer in frames) via a drop-in so apps share a stable time base.</p>\n<p>Create e.g. <code>/etc/pipewire/pipewire.conf.d/99-caspar-clock.conf</code> (path may be <code>~/.config/pipewire/pipewire.conf.d/</code> for <strong>per-user</strong> overrides):</p>\n<pre><code class=\"language-ini\">context.properties = {\n  default.clock.rate = 48000\n  default.clock.quantum = 256\n  default.clock.min-quantum = 64\n}</code></pre>\n<table><thead><tr><th>Property</th><th>Role</th></tr></thead><tbody>\n<tr><td><code>default.clock.rate</code></td><td>Global sample rate (Hz); <strong>48000</strong> matches typical Caspar/FFmpeg consumer args.</td></tr>\n<tr><td><code>default.clock.quantum</code></td><td>Default buffer size in <strong>frames</strong> at the default rate (latency vs stability tradeoff).</td></tr>\n<tr><td><code>default.clock.min-quantum</code></td><td>Lower bound for dynamic quantum (lower = lower latency, higher CPU).</td></tr>\n</tbody></table>\n<p>Restart the session services after editing system-wide files:</p>\n<pre><code class=\"language-bash\">systemctl --user restart wireplumber pipewire pipewire-pulse</code></pre>\n<p><strong>Note:</strong> Exact merge behaviour depends on PipeWire version; if a property is ignored, check <code>journalctl --user -u pipewire</code> for warnings.</p>\n<hr>\n<h2 id=\"virtual-sinks-for-casparcg-routing\">Virtual sinks for CasparCG routing</h2>\n<p>Caspar can send audio to a <strong>named sink</strong> (e.g. FFmpeg <code>-f pulse &lt;sink_name&gt;</code> or system default). Creating a <strong>dedicated sink</strong> lets you:</p>\n<ul>\n<li>Patch <strong>Caspar → mixer → hardware</strong> in <code>qpwgraph</code></li>\n<li>Isolate <strong>PGM vs monitor</strong> buses without renumbering ALSA <code>hw:N,M</code> in XML</li>\n</ul>\n<h3 id=\"pulseaudio-compatibility-pipewire-implements-this\">PulseAudio compatibility (PipeWire implements this)</h3>\n<p>With <strong>pipewire-pulse</strong>, <code>pactl</code> often controls PipeWire’s Pulse-compatible sinks.</p>\n<p><strong>Example:</strong> null sink with a friendly name and multiple channels (adjust <code>channels=</code> to your layout):</p>\n<pre><code class=\"language-bash\">pactl load-module module-null-sink sink_name=caspar_pgm \\\n  sink_properties=device.description=CasparPGM channels=8</code></pre>\n<p>List sinks:</p>\n<pre><code class=\"language-bash\">pactl list short sinks</code></pre>\n<p>Use the sink name in Caspar <strong>FFmpeg</strong> consumer paths that target Pulse, e.g. <code>-f pulse caspar_pgm</code> (see WO-06 Phase 2).</p>\n<p>Unload (note module id from <code>pactl list modules short</code>):</p>\n<pre><code class=\"language-bash\">pactl unload-module &lt;module-id&gt;</code></pre>\n<p>For <strong>persistent</strong> null sinks across reboots, prefer a WirePlumber snippet or a small user systemd unit that runs the <code>pactl load-module</code> line at login—station-specific.</p>\n<hr>\n<h2 id=\"tools\">Tools</h2>\n<h3 id=\"qpwgraph\"><code>qpwgraph</code></h3>\n<ul>\n<li>Launches a <strong>graph</strong> of PipeWire <strong>nodes</strong> (apps, sinks, sources).</li>\n<li><strong>Drag connections</strong> between outputs and inputs to route Caspar (or <code>pw-play</code>) into the right hardware or virtual sink.</li>\n<li>Ideal for validating <strong>multi-channel</strong> paths before baking paths into <code>casparcg.config</code>.</li>\n</ul>\n<h3 id=\"pw-cli-and-pw-dump\"><code>pw-cli</code> and <code>pw-dump</code></h3>\n<ul>\n<li><strong>Inspect</strong> objects, properties, and links:</li>\n</ul>\n<pre><code class=\"language-bash\">  pw-cli ls Node\n  pw-dump | less</code></pre>\n<ul>\n<li>Use to confirm <strong>channel counts</strong> and <strong>port names</strong> on a device node.</li>\n</ul>\n<h3 id=\"pw-top\"><code>pw-top</code></h3>\n<ul>\n<li>Live <strong>CPU</strong> and <strong>quantum</strong> / driver timing view (similar to <code>top</code>).</li>\n<li>Useful when chasing <strong>xruns</strong> or high load while running Caspar + FFmpeg consumers.</li>\n</ul>\n<hr>\n<h2 id=\"casparcg-integration-notes\">CasparCG integration notes</h2>\n<table><thead><tr><th>Path</th><th>Behaviour</th></tr></thead><tbody>\n<tr><td><strong>FFmpeg <code>-f alsa hw:N,M</code></strong></td><td>Talks <strong>ALSA</strong> directly; <strong>bypasses</strong> PipeWire unless ALSA is plumbed to PipeWire.</td></tr>\n<tr><td><strong>FFmpeg <code>-f pulse &lt;sink&gt;</code></strong></td><td>Goes through <strong>PipeWire’s Pulse layer</strong>; virtual sinks and <code>qpwgraph</code> routing apply.</td></tr>\n<tr><td><strong><code>&lt;system-audio/&gt;</code> consumer</strong></td><td>Usually follows <strong>default desktop sink</strong> (often PipeWire).</td></tr>\n</tbody></table>\n<p>For a <strong>single</strong> fixed device, ALSA <code>hw:N,M</code> is often simpler. For <strong>flexible</strong> routing between multiple outputs, <strong>Pulse-compatible sinks</strong> + PipeWire graph are appropriate.</p>\n<hr>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table><thead><tr><th>Issue</th><th>What to check</th></tr></thead><tbody>\n<tr><td>No nodes for Caspar</td><td>Consumer uses ALSA <code>hw:</code> only → no PipeWire node; use <code>pulse</code> or test <code>pw-play</code> into a sink.</td></tr>\n<tr><td>Crackling / dropouts</td><td>Quantum too low vs CPU load; try raising <code>default.clock.quantum</code>; close other heavy clients.</td></tr>\n<tr><td>Wrong rate</td><td>Force 48000 in Caspar channel mode and FFmpeg <code>-ar 48000</code>; match <code>default.clock.rate</code>.</td></tr>\n<tr><td><code>pactl</code> fails</td><td>Ensure <code>pipewire-pulse</code> socket is active; user session vs system PipeWire.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"see-also\">See also</h2>\n<ul>\n<li><a href=\"#/reference--audio--jack-audio-routing\" data-wiki-page=\"1\">JACK audio routing</a> (separate low-latency graph stack)</li>\n<li><a href=\"#/reference--audio--caspar-outputs-nvidia-stereo-usb\" data-wiki-page=\"1\">Caspar outputs: NVIDIA, 3.5 mm stereo, USB</a></li>\n<li><a href=\"#/reference--audio--alsa-device-enumeration\" data-wiki-page=\"1\">ALSA device enumeration</a></li>\n<li>WO-06 Phase 1 — JACK alternative (<strong>T1.3</strong>), NVIDIA (<strong>T1.4</strong>), USB (<strong>T1.5</strong>)</li>\n</ul>","reference--amcp-mapping":"<h1 id=\"amcp-architecture-mapping\">AMCP Architecture Mapping</h1>\n<p>This document maps out the CasparCG TCP AMCP commands, their respective HighAsCG REST endpoints, and the underlying modular JS methods. <em>(Note: As of the <code>casparcg-connection</code> migration, these commands are dispatched either via native typed objects for basic commands, or parsed as custom commands through the adapter, but the surface API remains identical.)</em></p>\n<p>See <a href=\"#/reference--amcp-migration\" data-wiki-page=\"1\">amcp-migration.md</a> for env flags and rollback.</p>\n<h3 id=\"transport-default-library-not-legacy-tcp\">Transport (default: library, not legacy TCP)</h3>\n<table><thead><tr><th>Transport</th><th>When used</th></tr></thead><tbody>\n<tr><td><strong>typed</strong></td><td><code>casparcg-connection</code> method via <code>AmcpClient._invokeTyped()</code></td></tr>\n<tr><td><strong>raw</strong></td><td><code>sendCustom()</code> — complex clips, batch lines, CG ADD with data, DATA STORE escaping, parameterized HELP/INFO/CINF, <code>cgGoto</code>, mixer FILL/LEVELS/CHROMA, engine <code>raw()</code></td></tr>\n</tbody></table>\n<p><strong>Mostly typed today:</strong> basic play/load (simple clips), pause/resume/stop/clear, mixer opacity/brightness/saturation/contrast/keyer/blend/invert/commit/clear, CG play/stop/next/remove/clear/update/invoke, query version/cls/tls/fls/diag/info\\*, data retrieve/list/remove, thumbnails.</p>\n<p><strong>Always raw:</strong> <code>[HTML]</code>/<code>ndi://</code>/STING clips, <code>batchSendChunked</code>, scene-take pipelines, <code>call</code>/<code>swap</code>/<code>add</code>/<code>remove</code>, <code>dataStore</code>, <code>cgGoto</code>, <code>cgAdd</code> with template data payload.</p>\n<h2 id=\"basic-layout\">Basic Layout</h2>\n<table><thead><tr><th>CasparCG AMCP Command</th><th>JS Method (<code>amcp.[sub]</code>)</th><th>REST Endpoint</th></tr></thead><tbody>\n<tr><td><code>LOAD</code></td><td><code>basic.load</code> / <code>.load</code></td><td><code>POST /api/load</code></td></tr>\n<tr><td><code>LOADBG</code></td><td><code>basic.loadbg</code> / <code>.loadbg</code></td><td><code>POST /api/loadbg</code></td></tr>\n<tr><td><code>PLAY</code></td><td><code>basic.play</code> / <code>.play</code></td><td><code>POST /api/play</code></td></tr>\n<tr><td><code>PAUSE</code></td><td><code>basic.pause</code> / <code>.pause</code></td><td><code>POST /api/pause</code></td></tr>\n<tr><td><code>RESUME</code></td><td><code>basic.resume</code> / <code>.resume</code></td><td><code>POST /api/resume</code></td></tr>\n<tr><td><code>STOP</code></td><td><code>basic.stop</code> / <code>.stop</code></td><td><code>POST /api/stop</code></td></tr>\n<tr><td><code>CLEAR</code></td><td><code>basic.clear</code> / <code>.clear</code></td><td><code>POST /api/clear</code></td></tr>\n<tr><td><code>CALL</code></td><td><code>basic.call</code></td><td><code>POST /api/call</code></td></tr>\n<tr><td><code>SWAP</code></td><td><code>basic.swap</code></td><td><code>POST /api/swap</code></td></tr>\n<tr><td><code>ADD</code></td><td><code>basic.add</code></td><td><code>POST /api/add</code></td></tr>\n<tr><td><code>REMOVE</code></td><td><code>basic.remove</code></td><td><code>POST /api/remove</code></td></tr>\n<tr><td><code>PRINT</code></td><td><code>basic.print</code></td><td><code>POST /api/print</code></td></tr>\n<tr><td><code>LOG LEVEL</code></td><td><code>basic.logLevel</code></td><td><code>POST /api/log/level</code></td></tr>\n<tr><td><code>LOG CATEGORY</code></td><td><code>basic.logCategory</code></td><td><code>POST /api/log/category</code></td></tr>\n<tr><td><code>SET</code></td><td><code>basic.set</code></td><td><code>POST /api/set</code></td></tr>\n<tr><td><code>LOCK</code></td><td><code>basic.lock</code></td><td><code>POST /api/lock</code></td></tr>\n<tr><td><code>PING</code></td><td><code>basic.ping</code></td><td><code>POST /api/ping</code></td></tr>\n</tbody></table>\n<h2 id=\"mixer-submodule\">Mixer Submodule</h2>\n<p><em>Note: All Mixer commands accept <code>#duration</code>, <code>#tween</code>, and <code>#defer</code> parameters.</em></p>\n<p><strong><code>MIXER … KEYER</code> (per layer)</strong> toggles Caspar’s <strong>layer</strong> keying / straight-alpha path for that layer (<code>0</code> = off, <code>1</code> = on). It is <strong>not</strong> the same as DeckLink <strong>external/fill keyer</strong> hardware — errors like <code>Failed to enable external keyer</code> in Caspar’s DeckLink consumer logs refer to the <strong>device</strong>, not to this AMCP command. Full-screen HTML CG overlays (e.g. host IP splash) typically need <strong><code>MIXER … FILL</code> + <code>MIXER … OPACITY</code></strong>; <code>KEYER</code> is optional and omitted there to avoid edge-case blanking on some pipelines (see <code>media-ext.js</code> / scene straight-alpha rules for when <code>KEYER 1</code> is appropriate).</p>\n<table><thead><tr><th>CasparCG AMCP Command</th><th>JS Method (<code>amcp.mixer</code>)</th><th>REST Endpoint</th></tr></thead><tbody>\n<tr><td><code>MIXER KEYER</code></td><td><code>mixerKeyer</code></td><td><code>/api/mixer/keyer</code></td></tr>\n<tr><td><code>MIXER CHROMA</code></td><td><code>mixerChroma</code></td><td><code>/api/mixer/chroma</code></td></tr>\n<tr><td><code>MIXER BLEND</code></td><td><code>mixerBlend</code></td><td><code>/api/mixer/blend</code></td></tr>\n<tr><td><code>MIXER INVERT</code></td><td><code>mixerInvert</code></td><td><code>/api/mixer/invert</code></td></tr>\n<tr><td><code>MIXER OPACITY</code></td><td><code>mixerOpacity</code></td><td><code>/api/mixer/opacity</code></td></tr>\n<tr><td><code>MIXER BRIGHTNESS</code></td><td><code>mixerBrightness</code></td><td><code>/api/mixer/brightness</code></td></tr>\n<tr><td><code>MIXER SATURATION</code></td><td><code>mixerSaturation</code></td><td><code>/api/mixer/saturation</code></td></tr>\n<tr><td><code>MIXER CONTRAST</code></td><td><code>mixerContrast</code></td><td><code>/api/mixer/contrast</code></td></tr>\n<tr><td><code>MIXER LEVELS</code></td><td><code>mixerLevels</code></td><td><code>/api/mixer/levels</code></td></tr>\n<tr><td><code>MIXER FILL</code></td><td><code>mixerFill</code></td><td><code>/api/mixer/fill</code></td></tr>\n<tr><td><code>MIXER CLIP</code></td><td><code>mixerClip</code></td><td><code>/api/mixer/clip</code></td></tr>\n<tr><td><code>MIXER ANCHOR</code></td><td><code>mixerAnchor</code></td><td><code>/api/mixer/anchor</code></td></tr>\n<tr><td><code>MIXER CROP</code></td><td><code>mixerCrop</code></td><td><code>/api/mixer/crop</code></td></tr>\n<tr><td><code>MIXER ROTATION</code></td><td><code>mixerRotation</code></td><td><code>/api/mixer/rotation</code></td></tr>\n<tr><td><code>MIXER VOLUME</code></td><td><code>mixerVolume</code></td><td><code>/api/mixer/volume</code></td></tr>\n<tr><td><code>MIXER STRAIGHT_ALPHA_OUTPUT</code></td><td><code>mixerStraightAlphaOutput</code></td><td><code>/api/mixer/straight_alpha</code></td></tr>\n<tr><td><code>MIXER GRID</code></td><td><code>mixerGrid</code></td><td><code>/api/mixer/grid</code></td></tr>\n<tr><td><code>MIXER COMMIT</code></td><td><code>mixerCommit</code></td><td><code>/api/mixer/commit</code></td></tr>\n<tr><td><code>MIXER CLEAR</code></td><td><code>mixerClear</code></td><td><code>/api/mixer/clear</code></td></tr>\n</tbody></table>\n<h2 id=\"cg-submodule\">CG Submodule</h2>\n<table><thead><tr><th>CasparCG AMCP Command</th><th>JS Method (<code>amcp.cg</code>)</th><th>REST Endpoint</th></tr></thead><tbody>\n<tr><td><code>CG ADD</code></td><td><code>cgAdd</code></td><td><code>POST /api/cg/add</code></td></tr>\n<tr><td><code>CG PLAY</code></td><td><code>cgPlay</code></td><td><code>POST /api/cg/play</code></td></tr>\n<tr><td><code>CG STOP</code></td><td><code>cgStop</code></td><td><code>POST /api/cg/stop</code></td></tr>\n<tr><td><code>CG NEXT</code></td><td><code>cgNext</code></td><td><code>POST /api/cg/next</code></td></tr>\n<tr><td><code>CG GOTO</code></td><td><code>cgGoto</code></td><td><code>POST /api/cg/goto</code></td></tr>\n<tr><td><code>CG REMOVE</code></td><td><code>cgRemove</code></td><td><code>POST /api/cg/remove</code></td></tr>\n<tr><td><code>CG CLEAR</code></td><td><code>cgClear</code></td><td><code>POST /api/cg/clear</code></td></tr>\n<tr><td><code>CG UPDATE</code></td><td><code>cgUpdate</code></td><td><code>POST /api/cg/update</code></td></tr>\n<tr><td><code>CG INVOKE</code></td><td><code>cgInvoke</code></td><td><code>POST /api/cg/invoke</code></td></tr>\n<tr><td><code>CG INFO</code></td><td><code>cgInfo</code></td><td><code>POST /api/cg/info</code></td></tr>\n</tbody></table>\n<h2 id=\"data-submodule\">DATA Submodule</h2>\n<table><thead><tr><th>CasparCG AMCP Command</th><th>JS Method (<code>amcp.data</code>)</th><th>REST Endpoint</th></tr></thead><tbody>\n<tr><td><code>DATA STORE</code></td><td><code>dataStore</code></td><td><code>POST /api/data/store</code></td></tr>\n<tr><td><code>DATA RETRIEVE</code></td><td><code>dataRetrieve</code></td><td><code>POST /api/data/retrieve</code></td></tr>\n<tr><td><code>DATA LIST</code></td><td><code>dataList</code></td><td><code>POST /api/data/list</code></td></tr>\n<tr><td><code>DATA REMOVE</code></td><td><code>dataRemove</code></td><td><code>POST /api/data/remove</code></td></tr>\n</tbody></table>\n<h2 id=\"system-queries-submodule\">SYSTEM Queries Submodule</h2>\n<table><thead><tr><th>CasparCG AMCP Command</th><th>JS Method (<code>amcp.query</code>)</th><th>REST Endpoint</th></tr></thead><tbody>\n<tr><td><code>CLS</code></td><td><code>cls</code></td><td>Used internally (<code>/api/media</code>)</td></tr>\n<tr><td><code>TLS</code></td><td><code>tls</code></td><td>Used internally (<code>/api/templates</code>)</td></tr>\n<tr><td><code>FLS</code></td><td><code>fls</code></td><td><code>GET /api/fonts</code></td></tr>\n<tr><td><code>VERSION</code></td><td><code>version</code></td><td><em>Internal property hook</em></td></tr>\n<tr><td><code>INFO</code></td><td><code>info</code></td><td><em>Internal properties</em></td></tr>\n<tr><td><code>INFO SERVER</code></td><td><code>infoServer</code></td><td><code>GET /api/server</code></td></tr>\n<tr><td><code>INFO QUEUES</code></td><td><code>infoQueues</code></td><td><code>GET /api/server/queues</code></td></tr>\n<tr><td><code>INFO THREADS</code></td><td><code>infoThreads</code></td><td><code>GET /api/server/threads</code></td></tr>\n<tr><td><code>GL INFO</code></td><td><code>glInfo</code></td><td><code>GET /api/server/gl</code></td></tr>\n<tr><td><code>GL GC</code></td><td><code>glGc</code></td><td><code>POST /api/gl/gc</code></td></tr>\n<tr><td><code>DIAG</code></td><td><code>diag</code></td><td><code>POST /api/diag</code></td></tr>\n<tr><td><code>HELP</code></td><td><code>help</code></td><td><code>GET /api/help</code> / <code>GET /api/help/:cmd</code></td></tr>\n<tr><td><code>BYE</code></td><td><code>bye</code></td><td><em>Internal shutdown</em></td></tr>\n<tr><td><code>KILL</code></td><td><code>kill</code></td><td><code>POST /api/kill</code></td></tr>\n<tr><td><code>RESTART</code></td><td><code>restart</code></td><td><code>POST /api/restart</code></td></tr>\n</tbody></table>\n<h2 id=\"thumbnail-submodule\">THUMBNAIL Submodule</h2>\n<table><thead><tr><th>CasparCG AMCP Command</th><th>JS Method (<code>amcp.thumb</code>)</th><th>REST Endpoint</th></tr></thead><tbody>\n<tr><td><code>THUMBNAIL LIST</code></td><td><code>thumbnailList</code></td><td><code>GET /api/thumbnails</code></td></tr>\n<tr><td><code>THUMBNAIL RETRIEVE</code></td><td><code>thumbnailRetrieve</code></td><td><code>GET /api/thumbnail/:filename</code></td></tr>\n<tr><td><code>THUMBNAIL GENERATE</code></td><td><code>thumbnailGenerate</code></td><td><code>POST /api/thumbnails/generate</code></td></tr>\n<tr><td><code>THUMBNAIL GENERATE_ALL</code></td><td><code>thumbnailGenerateAll</code></td><td><code>POST /api/thumbnails/generate-all</code></td></tr>\n</tbody></table>\n<h2 id=\"scene-take-post-apiscenetake\">Scene take (<code>POST /api/scene/take</code>)</h2>\n<p>Look takes use <strong><code>runSceneTakeLbg</code></strong>: per layer, <code>LOADBG</code> with the look’s transition (e.g. <code>MIX</code> + frames + tween), then mixer setup, then <code>PLAY</code> with no clip (FG/BG swap). Timeline-only looks use the timeline engine instead.</p>","reference--amcp-clean-look-fade":"<h1 id=\"amcp-clean-look-fade\">AMCP Clean Look Fade</h1>\n<p>This is the working banked look-to-look fade pattern used by HighAsCG for clean program transitions.</p>\n<h2 id=\"goal\">Goal</h2>\n<p>Prepare the incoming look without touching the layer that is currently visible on program, then crossfade both physical banks with the same transition duration.</p>\n<p>For logical look layer <code>10</code>, the two physical program banks are:</p>\n<ul>\n<li>Bank A: <code>10</code></li>\n<li>Bank B: <code>110</code></li>\n</ul>\n<p>If bank B is currently on program, the next look is prepared on bank A.</p>\n<h2 id=\"sequence\">Sequence</h2>\n<p>Example: channel <code>1</code>, current program on <code>1-110</code>, incoming look prepared on <code>1-10</code>, transition <code>75</code> frames.</p>\n<pre><code class=\"language-text\">MIXER 1-10 CLEAR\nLOADBG 1-10 &quot;NEW_CLIP&quot;\nMIXER 1-10 FILL &lt;x&gt; &lt;y&gt; &lt;scaleX&gt; &lt;scaleY&gt; 0\nMIXER 1-10 OPACITY 0 0\nMIXER 1 COMMIT\n\nPLAY 1-10\nMIXER 1-10 OPACITY 0 0\nMIXER 1-10 OPACITY 1 75 linear\nMIXER 1 COMMIT\n\nSTOP 1-110\nMIXER 1-110 CLEAR\nMIXER 1 COMMIT</code></pre>\n<h2 id=\"notes\">Notes</h2>\n<ul>\n<li>Do not send mixer setup to the active program bank. Preparing <code>MIXER FILL</code>, <code>MIXER CLEAR</code>, or similar commands on the currently visible layer will move or reset the on-air look.</li>\n<li>For banked crossfades, <strong>only the top layer</strong> opacity tweens during the fade window (the bottom layer stays at full opacity so the composite never dips to ~50%):</li>\n<li><strong>Incoming on bank B</strong> (<code>110</code> above <code>10</code>): hide incoming, <code>PLAY</code>, fade incoming <code>0→1</code>; outgoing stays at full opacity until teardown.</li>\n<li><strong>Incoming on bank A</strong> (<code>10</code> below <code>110</code>): <code>PLAY</code> incoming at full opacity underneath, fade outgoing <code>1→0</code>; incoming is not pre-hidden or faded in.</li>\n<li><code>LOADBG ... MIX ... AUTO</code> is suitable for native same-layer transitions, but not for this banked crossfade path because the AUTO transition can complete before the prepared bank is visible.</li>\n<li>Teardown of the old bank happens only after the fade window has elapsed.</li>\n</ul>","reference--amcp-migration-qa-checklist":"<h1 id=\"amcp-migration-manual-qa-checklist-t74\">AMCP migration — manual QA checklist (T7.4)</h1>\n<p>Run on <strong>each playout PC</strong> with CasparCG and HighAsCG on the build that includes <code>casparcg-connection</code> (default transport). Record date, machine, and pass/fail.</p>\n<p><strong>Automated preflight (on the machine):</strong></p>\n<pre><code class=\"language-bash\">cd ~/highascg\nnpm run test:highascg:migration:all\nnpm run test:highascg:air-paths    # batchSendChunked, DEFER batch, reconnect (no PGM check)\nHIGHASCG_AMCP_LEGACY_TRANSPORT=1 node --test tools/smoke/smoke-amcp-legacy-transport.test.js</code></pre>\n<h2 id=\"default-transport-casparcg-connection\">Default transport (<code>casparcg-connection</code>)</h2>\n<table><thead><tr><th>#</th><th>Test</th><th>Pass</th><th>Notes</th></tr></thead><tbody>\n<tr><td>1</td><td>Scene take crossfade (LOADBG → PLAY, MIX) smooth on PGM</td><td>☐</td><td></td></tr>\n<tr><td>2</td><td>Global border + CG layer order correct</td><td>☐</td><td></td></tr>\n<tr><td>3</td><td>PIP overlay position + opacity animation</td><td>☐</td><td></td></tr>\n<tr><td>4</td><td>Multiview sources + CALL update</td><td>☐</td><td></td></tr>\n<tr><td>5</td><td>Streaming ADD/REMOVE STREAM</td><td>☐</td><td></td></tr>\n<tr><td>6</td><td>DeckLink PLAY routing</td><td>☐</td><td></td></tr>\n<tr><td>7</td><td>Batch: multiple MIXER DEFER → COMMIT → atomic look</td><td>☐</td><td></td></tr>\n<tr><td>8</td><td>Offline mode: UI/settings without Caspar</td><td>☐</td><td></td></tr>\n<tr><td>9</td><td>Reconnect: restart Caspar, HighAsCG recovers VERSION</td><td>☐</td><td></td></tr>\n</tbody></table>\n<h2 id=\"rollback-highascg_amcp_legacy_transport1\">Rollback (<code>HIGHASCG_AMCP_LEGACY_TRANSPORT=1</code>)</h2>\n<table><thead><tr><th>#</th><th>Test</th><th>Pass</th><th>Notes</th></tr></thead><tbody>\n<tr><td>10</td><td><code>systemctl restart highascg</code> with env in drop-in or unit</td><td>☐</td><td></td></tr>\n<tr><td>11</td><td>Scene take still works on legacy transport</td><td>☐</td><td></td></tr>\n<tr><td>12</td><td><code>npm run test:highascg:legacy</code> passes on host</td><td>☐</td><td></td></tr>\n</tbody></table>\n<h2 id=\"sign-off\">Sign-off</h2>\n<table><thead><tr><th>Role</th><th>Name</th><th>Date</th></tr></thead><tbody>\n<tr><td>Operator / playout</td><td></td><td></td></tr>\n<tr><td>Engineering</td><td></td><td></td></tr>\n</tbody></table>\n<p>When rows 1–12 pass on <strong>both</strong> production PCs, mark WO-52 migration <strong>T7.4</strong> complete in <a href=\"../../work/WO_CASPARCG_CONNECTION_MIGRATION.md\">&lt;code&gt;WO_CASPARCG_CONNECTION_MIGRATION.md&lt;/code&gt;</a>.</p>","reference--amcp-migration":"<h1 id=\"amcp-transport-casparcg-connection-migration\">AMCP transport: <code>casparcg-connection</code> migration</h1>\n<p>HighAsCG uses the <a href=\"https://www.npmjs.com/package/casparcg-connection\">casparcg-connection</a> npm package (v6.x, CommonJS) as the default AMCP transport. The public API (<code>appCtx.amcp.*</code>, REST, WebSocket) is unchanged.</p>\n<h2 id=\"environment-variables\">Environment variables</h2>\n<table><thead><tr><th>Variable</th><th>Default</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><code>HIGHASCG_AMCP_LEGACY_TRANSPORT</code></td><td>unset (<code>0</code>)</td><td>Set to <code>1</code> to use legacy <code>TcpClient</code> + <code>AmcpProtocol</code> instead of the library</td></tr>\n<tr><td><code>HIGHASCG_AMCP_SEND_TIMEOUT_MS</code></td><td><code>15000</code></td><td>Short AMCP reply timeout</td></tr>\n<tr><td><code>HIGHASCG_AMCP_LONG_RESPONSE_MS</code></td><td><code>120000</code></td><td>Long replies (CLS, INFO CONFIG, thumbnails, …)</td></tr>\n<tr><td><code>HIGHASCG_AMCP_CONNECT_SETTLE_MS</code></td><td><code>600</code></td><td>Delay before first VERSION after TCP connect</td></tr>\n<tr><td><code>HIGHASCG_AMCP_HEALTH_MS</code></td><td><code>0</code></td><td>Periodic VERSION health interval (<code>0</code> = off)</td></tr>\n</tbody></table>\n<h2 id=\"architecture\">Architecture</h2>\n<ul>\n<li><strong>Default:</strong> <code>ConnectionManager</code> → <code>CasparCG</code> (library TCP only) → <code>AmcpConnectionAdapter</code> → <code>AmcpClient</code> + <code>AmcpProtocol</code></li>\n<li><strong>Plain AMCP:</strong> The adapter writes normal command lines (<code>MIXER 1-10 …</code>) on the library socket. It does <strong>not</strong> use <code>sendCustom()</code> / <code>REQ &lt;id&gt;</code> (Caspar logs those as <code>400 ERROR</code> on standard builds). Incoming lines are parsed by <code>AmcpProtocol</code>, not the library’s REQ/RES deserializer (<code>_processIncomingData</code> is disabled after connect).</li>\n<li><strong>Batch:</strong> <code>BEGIN…COMMIT</code> payloads use one raw <code>socket.write</code> via <code>adapter.send()</code>; replies are drained from the same socket tap (so <code>202 COMMIT PARTIAL</code> is recognized)</li>\n<li><strong>Rollback:</strong> <code>HIGHASCG_AMCP_LEGACY_TRANSPORT=1</code> restores the pre-migration stack</li>\n</ul>\n<h2 id=\"what-stayed-in-highascg\">What stayed in HighAsCG</h2>\n<table><thead><tr><th>Module</th><th>Reason</th></tr></thead><tbody>\n<tr><td><code>amcp-batch.js</code></td><td>CG-aware pre-commit, chunking, DEFER/COMMIT orchestration</td></tr>\n<tr><td><code>amcp-command-plan.js</code></td><td><code>[HTML]</code>, NDI, STING, route clips</td></tr>\n<tr><td><code>amcp-simulated.js</code></td><td>Offline mode</td></tr>\n<tr><td>Engine <code>raw()</code> / <code>batchSendChunked</code></td><td>Unchanged call sites</td></tr>\n</tbody></table>\n<h2 id=\"typed-vs-raw-commands\">Typed vs raw commands</h2>\n<p>See <a href=\"#/reference--amcp-mapping\" data-wiki-page=\"1\">amcp-mapping.md</a> (Transport column). All production commands go through <code>_send()</code> (plain AMCP). Library typed methods are opt-in only (<code>useLibraryTyped</code> on the adapter, not enabled by default).</p>\n<h2 id=\"batch-transport-t51\">Batch transport (T5.1)</h2>\n<p>The library exposes <code>begin()</code> / <code>commit()</code> / <code>discard()</code>, but HighAsCG keeps <strong><code>amcp-batch.js</code></strong> and sends one raw <code>BEGIN\\r\\n…\\r\\nCOMMIT\\r\\n</code> payload via <code>adapter.send()</code>. Replies are read from the underlying TCP socket so <code>202 COMMIT PARTIAL</code> is not dropped by the library’s <code>RESPONSE_REGEX</code>.</p>\n<h2 id=\"tests\">Tests</h2>\n<pre><code class=\"language-bash\">npm run test:highascg:migration      # Parity + batch + send-after + offline REST (no Caspar)\nnpm run test:highascg:live           # Live Caspar (library transport)\nnpm run test:highascg:legacy         # Live Caspar (HIGHASCG_AMCP_LEGACY_TRANSPORT=1)\nnpm run test:highascg:migration:all  # Full automated gate (one node --test run)\nnpm run test:highascg:air-paths      # T7.4 proxies: chunked batch, DEFER batch, reconnect</code></pre>\n<p>Manual playout QA: <a href=\"#/reference--amcp-migration-qa-checklist\" data-wiki-page=\"1\">amcp-migration-qa-checklist.md</a>.</p>\n<h2 id=\"work-order\">Work order</h2>\n<p>Full task list: <a href=\"../../work/WO_CASPARCG_CONNECTION_MIGRATION.md\">&lt;code&gt;work/WO_CASPARCG_CONNECTION_MIGRATION.md&lt;/code&gt;</a>.</p>","reference--audio_output_architecture":"<h1 id=\"audio-output-architecture-redesign-remaining-issues\">Audio Output Architecture Redesign &amp; Remaining Issues</h1>\n<h2 id=\"what-changed\">What Changed</h2>\n<h3 id=\"1-audio-output-management-fixed\">1. Audio Output Management (🔧 Fixed)</h3>\n<p><strong>Before:</strong> Every PortAudio / ALSA device was auto-generated as a rear panel connector, flooding the panel with ~40 entries for 3 physical cards.</p>\n<p><strong>After:</strong> Audio outputs are <strong>user-managed</strong> — exactly like stream and record outputs:</p>\n<ul>\n<li>Click <strong>+</strong> on the Audio slot in the rear panel to add an audio output</li>\n<li>Click the connector to open the <strong>inspector</strong> with:</li>\n<li>Device picker (dropdown of deduplicated <code>hw:CARD=...</code> devices from <code>aplay -L</code>)</li>\n<li>Manual device name entry (for custom PortAudio strings)</li>\n<li>Channel layout selector (stereo, mono, 5.1, 7.1)</li>\n<li>Save / Remove buttons</li>\n<li>⚠ Warning when build profile is <code>stock</code> (PortAudio requires <code>custom_live</code>)</li>\n</ul>\n<h3 id=\"2-device-deduplication-fixed\">2. Device Deduplication (🔧 Fixed)</h3>\n<p><strong>Before:</strong> <code>aplay -L</code> returned every PCM variant: <code>hw:</code>, <code>plughw:</code>, <code>default:</code>, <code>sysdefault:</code>, <code>dmix:</code>, <code>front:</code>, <code>surround*:</code>, <code>iec958:</code>, <code>hdmi:</code> — all for the same physical card.</p>\n<p><strong>After:</strong> Only <code>hw:CARD=...,DEV=N</code> entries are kept. Different DEV numbers are preserved (= different physical outputs). Result from your server:</p>\n<table><thead><tr><th>Device</th><th>Description</th></tr></thead><tbody>\n<tr><td><code>hw:CARD=HDSPMxa5a694,DEV=0</code></td><td>RME AIO_a5a694, RME AIO</td></tr>\n<tr><td><code>hw:CARD=PCH,DEV=0</code></td><td>HDA Intel PCH, ALC1150 Analog</td></tr>\n<tr><td><code>hw:CARD=PCH,DEV=1</code></td><td>HDA Intel PCH, ALC1150 Digital</td></tr>\n<tr><td><code>hw:CARD=NVidia,DEV=3</code></td><td>HDA NVidia, HDMI 0</td></tr>\n<tr><td><code>hw:CARD=NVidia,DEV=7</code></td><td>HDA NVidia, HDMI 1</td></tr>\n<tr><td><code>hw:CARD=NVidia,DEV=8</code></td><td>HDA NVidia, HDMI 2</td></tr>\n<tr><td><code>hw:CARD=NVidia,DEV=9</code></td><td>HDA NVidia, HDMI 3</td></tr>\n</tbody></table>\n<h3 id=\"3-files-modified\">3. Files Modified</h3>\n<table><thead><tr><th>File</th><th>Change</th></tr></thead><tbody>\n<tr><td><code>src/audio/audio-devices.js</code></td><td>Dedup <code>aplay -L</code> to <code>hw:CARD=</code> entries only</td></tr>\n<tr><td><code>src/config/device-graph-suggest.js</code></td><td>Replaced auto-enum with managed <code>audioOutputs</code> from config</td></tr>\n<tr><td><code>src/config/device-graph-constants.js</code></td><td>Reverted <code>audio_out</code>/<code>audio_in</code> from <code>AUTO_CASPAR_KINDS</code></td></tr>\n<tr><td><code>src/api/settings-post.js</code></td><td>Added <code>audioOutputs</code> persistence</td></tr>\n<tr><td><code>src/api/settings-get.js</code></td><td>Added <code>audioOutputs</code> to GET response</td></tr>\n<tr><td><code>src/api/routes-device-view.js</code></td><td>Added <code>audioOutputs</code> to device view payload</td></tr>\n<tr><td><code>client/components/device-view-inspectors.js</code></td><td>Added <code>renderAudioOutControls</code> inspector</td></tr>\n<tr><td><code>client/components/device-view-caspar-render.js</code></td><td>Slot renders from managed list + <code>+</code> button</td></tr>\n<tr><td><code>client/components/device-view-bands-render.js</code></td><td>Added <code>onAddAudioOutput</code>/<code>onRemoveAudioOutput</code></td></tr>\n<tr><td><code>client/components/device-view.js</code></td><td>Added <code>removeAudioOutputConnector</code> handler</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"still-open-not-addressed-in-this-session\">Still Open — Not Addressed In This Session</h2>\n<h3 id=\"looks-editor-screen-2-compose-preview-missing\">Looks Editor: Screen 2 Compose Preview Missing</h3>\n<p>The compose preview cells are built from <code>settingsState.getSettings()?.tandemTopology?.destinations</code>. The logic in <code>preview-canvas-panel.js:getComposeCellDefs()</code> (lines 34-87) correctly reads destinations and creates PGM/PRV cells per main index.</p>\n<p><strong>Likely cause:</strong> <code>settingsState</code> may not be updated when destinations change in Device View. The settings state is refreshed on tab activation but may miss live changes. The fix would be to either:</p>\n<ul>\n<li>Trigger <code>settingsState.refresh()</code> after destination changes, or</li>\n<li>Read destinations from the <code>stateStore.channelMap</code> instead of <code>settingsState</code></li>\n</ul>\n<h3 id=\"looks-scope-enforcement\">Looks Scope Enforcement</h3>\n<p>The scope selector in <code>scenes-editor-edit.js</code> (lines 33-46) correctly sets <code>scene.mainScope</code>. The take logic in <code>createTakeSceneToProgram</code> (lines 146-152) correctly resolves <code>targetMains</code> from the scope. But if the scope is &quot;Screen 1 only&quot;, the look still visually appears on both screens in the compose preview — the <strong>preview rendering</strong> doesn't filter by scope, only the <strong>take</strong> does.</p>\n<h3 id=\"multiview-add-destination\">Multiview Add Destination</h3>\n<p>The multiview option <strong>IS</strong> in the dropdown (<code>&lt;option value=&quot;multiview&quot;&gt;Multiview&lt;/option&gt;</code> in device-view.js line 48). If it shouldn't appear, it needs to be removed from the dropdown HTML.</p>\n<h3 id=\"portaudio-config-generation\">PortAudio Config Generation</h3>\n<p>The root-level <code>&lt;portaudio&gt;</code> block and per-channel <code>&lt;portaudio/&gt;</code> consumer <strong>DO</strong> generate correctly when <code>caspar_build_profile: 'custom_live'</code> is set. If the generated config on the server is missing PortAudio, ensure:</p>\n<ol>\n<li>Build profile is changed to &quot;Custom Live&quot; in Caspar Host inspector</li>\n<li>Hit &quot;Apply&quot; to regenerate the XML config</li>\n<li>Restart CasparCG</li>\n</ol>","reference--cef-interactive-bridge":"<h1 id=\"cef-interactive-bridge-removed-wo-257\">CEF interactive bridge (removed, WO-257)</h1>\n<p><strong>Status:</strong> Removed. This page is kept as a pointer for anyone following old links or work orders — the feature it describes no longer exists.</p>\n<p>The CEF interactive bridge forwarded operator mouse/keyboard on the multiview (or an interactive screen consumer) to embedded CEF HTML via the Chrome DevTools Protocol (<a href=\"../../work/work-orders/89_WO_CEF_OPERATOR_CONTROL.md\">WO-89</a>, arm/release toggle added in <a href=\"../../work/work-orders/232_WO_MARIO_HTML_PRODUCER.md\">WO-232</a>). It was removed outright in <a href=\"../../work/work-orders/257_WO_REMOVE_CEF_INTERACTIVE.md\">WO-257</a> after repeated production incidents (a <code>warmInFlight</code> crash-loop, <code>zoneTargets</code> connect-rejection, needle-matching poisoning) made the shared-process synthetic-input approach too fragile to keep — see INCIDENT-2026-07-16. WO-255 had already replaced the operator GUI's own CEF layer with a fullscreen Firefox process using native X11 input, which made the interactive bridge's remaining justification just embedded-webpage/template input (e.g. <code>template/mario</code> — see WO-232).</p>\n<h2 id=\"whats-gone\">What's gone</h2>\n<ul>\n<li><code>src/system/cef-interactive-bridge*.js</code>, <code>cef-interactive-cdp.js</code>, <code>cef-interactive-forward.js</code>,</li>\n</ul>\n<p><code>cef-focus-registry.js</code>, <code>cef-interactive-trace.js</code></p>\n<ul>\n<li><code>tools/runtime/cef-interactive-x11.py</code></li>\n<li><code>POST /api/cef/arm-input</code>, <code>POST /api/cef/release-input</code>, all <code>/api/cef-interactive/*</code> routes</li>\n<li>The &quot;Interactive input&quot; arm/release toggle in the scene layer inspector</li>\n</ul>\n<h2 id=\"whats-still-here\">What's still here</h2>\n<ul>\n<li><code>src/system/cef-cdp-client.js</code> — the generic raw CDP client WO-247 introduced to replace</li>\n</ul>\n<p>puppeteer-core. It doesn't depend on anything removed above. WO-248 completed the puppeteer removal: the headless-Chrome thumbnail renderers (<code>src/media/cg-look-thumb-render.js</code>, <code>tools/runtime/generate-lt-thumbnails.js</code>) now run on this client via <code>src/media/headless-chrome-cdp.js</code>, and the <code>puppeteer</code> npm dependency is fully purged from <code>package.json</code>/<code>node_modules</code>.</p>\n<ul>\n<li>Webpage-host content routing (<code>POST /api/host-live/webpage</code>, <code>/api/host-live/operator-fullscreen</code></li>\n</ul>\n<p>— see <a href=\"#/wiki--api--system-settings-hardware#host-live-sources--operator-fullscreen\" data-wiki-page=\"1\">system-settings-hardware API</a>): playing a URL on a host channel and (optionally) routing it fullscreen to the operator display still works. It just no longer arms keyboard/mouse forwarding into the page.</p>\n<ul>\n<li><code>template/mario</code> and any other &quot;interactive&quot; template still play as templates — they just don't</li>\n</ul>\n<p>receive clicks or key presses anymore.</p>","reference--companion-satellite-api":"<h1 id=\"companion-satellite-api-highascg-reference\">Companion Satellite API (HighAsCG reference)</h1>\n<p>HighAsCG uses Bitfocus Companion’s <strong>Satellite API</strong> for <strong>button preview bitmaps</strong> (WO-75). Timeline <strong>press/trigger</strong> stays on the <strong>HTTP Remote Control API</strong> (<a href=\"../../work/work-orders/24_WO_COMPANION_BUTTON_PRESS.md\">WO-24</a>).</p>\n<h2 id=\"official-documentation\">Official documentation</h2>\n<ul>\n<li><strong>Satellite API (canonical):</strong> https://companion.free/for-developers/Satellite-API/</li>\n<li><strong>HTTP Remote Control:</strong> https://companion.free/user-guide/v4.1/remote-control/http-remote-control/</li>\n</ul>\n<h2 id=\"what-highascg-uses\">What HighAsCG uses</h2>\n<table><thead><tr><th>Feature</th><th>Protocol</th><th>Default port</th></tr></thead><tbody>\n<tr><td>Fire button from timeline</td><td>HTTP <code>POST /api/location/{page}/{row}/{column}/press</code></td><td>8000 (Companion web)</td></tr>\n<tr><td>Button preview / page picker</td><td>Satellite <strong>Button Subscriptions</strong> (<code>ADD-SUB</code>, <code>SUB-STATE</code>, <code>REMOVE-SUB</code>)</td><td><strong>16622</strong> TCP</td></tr>\n</tbody></table>\n<p>Requires Companion <strong>~4.3+</strong> with Satellite API <strong>v1.10+</strong> (<code>CAPS SUBSCRIPTIONS=1</code>).</p>\n<h3 id=\"subscribe-example\">Subscribe example</h3>\n<pre><code class=\"language-text\">ADD-SUB SUBID=highascg/1/0/2 LOCATION=1/0/2 BITMAP=72 COLORS=hex TEXT=true TEXT_STYLE=true</code></pre>\n<p>Companion responds with <code>SUB-STATE</code> including base64 <strong>8-bit RGB</strong> <code>BITMAP</code> at the requested size.</p>\n<h2 id=\"implementation-map\">Implementation map</h2>\n<table><thead><tr><th>Module</th><th>Path</th></tr></thead><tbody>\n<tr><td>TCP client + subscriptions</td><td><code>src/companion/satellite-preview-client.js</code></td></tr>\n<tr><td>Line protocol parse/format</td><td><code>src/companion/satellite-protocol.js</code></td></tr>\n<tr><td>JPEG cache</td><td><code>src/companion/button-preview-cache.js</code></td></tr>\n<tr><td>Config helper</td><td><code>src/companion/companion-config.js</code></td></tr>\n<tr><td>HTTP routes</td><td><code>src/api/routes-companion-preview.js</code></td></tr>\n</tbody></table>\n<h2 id=\"enable-satellite-in-companion\">Enable Satellite in Companion</h2>\n<p>In Companion <strong>Settings</strong>:</p>\n<ol>\n<li><strong>Satellite server</strong> — enable TCP (default port <strong>16622</strong>). This alone is not enough for HighAsCG previews.</li>\n<li><strong>Button Subscriptions API</strong> — must be <strong>enabled</strong>. Companion advertises this as <code>CAPS SUBSCRIPTIONS=1</code>. If disabled, Companion returns <code>SUBSCRIPTIONS=0</code> and <code>ADD-SUB ERROR MESSAGE=&quot;Subscriptions not enabled&quot;</code>.</li>\n</ol>\n<p>HighAsCG checks <code>/api/companion/button-preview/status</code> — <code>previewAvailable: false</code> with <code>reason: subscriptions_disabled</code> means step 2 is missing.</p>\n<p>Firewall must allow HighAsCG → Companion on the Satellite TCP port (localhost is typical).</p>\n<p><strong>Note:</strong> HTTP press (timeline trigger) does not need Button Subscriptions — only preview bitmaps and the page picker do.</p>","reference--GPU_SCREEN_CONSUMER_AND_XRANDR":"<h1 id=\"gpu-output-screen-consumers-and-x11-xrandr-intended-interaction\">GPU output, screen consumers, and X11 (xrandr) — intended interaction</h1>\n<p>This document describes how <strong>screen destinations</strong>, <strong>CasparCG channel / screen consumers</strong>, <strong>Device View cabling</strong>, and <strong>OS-level display layout (xrandr)</strong> are meant to work together in HighAsCG. It is normative for product behaviour; implementation may lag until the linked work order is complete.</p>\n<hr>\n<h2 id=\"1-roles-of-each-layer\">1. Roles of each layer</h2>\n<h3 id=\"11-screen-destinations-tandem-routing-config\">1.1 Screen destinations (tandem / routing config)</h3>\n<ul>\n<li><strong>Screen destinations</strong> define logical program outputs: which <strong>CasparCG channels</strong> exist, their <strong>video mode</strong> (e.g. <code>720p50</code>, <code>1080p5000</code>, or custom width × height × fps), and <strong>layout metadata</strong> such as <strong>position</strong> on the virtual canvas (e.g. <code>0,0</code> for the first main screen, then horizontal tiling for additional mains).</li>\n<li>Persisted settings drive <strong>Caspar config generation</strong> (<code>&lt;screen&gt;</code> consumers, channel map, etc.) so the <strong>Caspar “screen consumer” window</strong> for a bound channel matches that destination’s mode and placement <strong>inside Caspar’s coordinate system</strong>.</li>\n</ul>\n<h3 id=\"12-device-graph-channel-gpu-connector\">1.2 Device graph: channel → GPU connector</h3>\n<ul>\n<li>The user <strong>cables</strong> a <strong>channel output</strong> (or logical screen sink) to a <strong><code>gpu_out</code></strong> connector on the rear panel.</li>\n<li>That edge means: <em>this physical output (HDMI/DP via X)</em> is <strong>bound</strong> to <strong>display that screen’s consumer</strong>* — i.e. the Caspar window for the channel tied to that destination should appear on that connector’s monitor.</li>\n<li>Binding must resolve which <strong>main screen index</strong> (<code>mainScreenIndex</code> / Caspar screen index) the GPU output carries, so server-side layout code can map <strong>one logical screen → one <code>xrandr</code> output</strong> (<code>screen_N_system_id</code>, e.g. <code>HDMI-0</code>).</li>\n</ul>\n<h3 id=\"13-default-behaviour-override-off\">1.3 Default behaviour (override <strong>off</strong>)</h3>\n<p>When <strong>Override</strong> is <strong>not</strong> enabled for that screen:</p>\n<ol>\n<li><strong>xrandr <code>--mode</code></strong> (pixel resolution) should follow the <strong>bound screen destination’s</strong> effective format: standard mode tokens map to WxH (e.g. <code>720p50</code> → <code>1280x720</code>), custom modes use destination width × height; refresh where applicable should align with destination fps / mode naming.</li>\n<li><strong>xrandr <code>--pos</code></strong> for that head should follow the <strong>channel / destination position</strong> used for that screen in the tandem layout (e.g. first screen at <code>0x0</code>, next screen to the right of the previous head’s width so physical desktop matches logical canvas tiling).</li>\n<li>Applying layout (<strong>Apply GPU</strong> / <code>POST /api/settings/apply-os</code>) should <strong>recompute</strong> the full horizontal plan so <strong>all</strong> bound GPU heads stay consistent after any one destination or cable change.</li>\n</ol>\n<h3 id=\"14-override-behaviour-override-on\">1.4 Override behaviour (override <strong>on</strong>)</h3>\n<p>When the physical monitor <strong>cannot</strong> or <strong>should not</strong> match the channel’s native format (operator wants 1080p on a 720p channel path, lab test, EDID mismatch, etc.):</p>\n<ol>\n<li>The user enables <strong>Override</strong> for that GPU / screen row in Device View.</li>\n<li>They choose <strong>resolution</strong> from the EDID-backed dropdown and/or <strong>custom</strong> width × height × fps (same controls as today), then <strong>Apply GPU</strong> (or the combined save + apply path).</li>\n<li><strong>xrandr</strong> must use that <strong>chosen</strong> mode and rate, <strong>not</strong> the destination’s pixel mode.</li>\n<li><strong>Positions of subsequent heads</strong> must still be recalculated so the X desktop tiling matches the <strong>actual</strong> widths applied (override may change width; following screens’ <code>--pos</code> must shift).</li>\n</ol>\n<h3 id=\"15-caspar-vs-os\">1.5 Caspar vs OS</h3>\n<ul>\n<li><strong>Caspar</strong> continues to render the <strong>screen consumer</strong> at the <strong>destination’s</strong> mode (unless the operator separately changes Caspar screen settings). Override affects <strong>OS framebuffer layout</strong> seen by the window manager and the monitor — it does not by itself rescale Caspar’s internal buffer unless separate consumer/stretch settings are used.</li>\n<li>Document this distinction in UI copy where confusion is likely.</li>\n</ul>\n<h3 id=\"16-v-sync-nvidia-screen-consumers\">1.6 V-sync (NVIDIA screen consumers)</h3>\n<p>For <strong>good vsync</strong> on program <strong>screen</strong> outputs with an <strong>NVIDIA</strong> GPU:</p>\n<ol>\n<li><strong>NVIDIA Settings:</strong> <strong>Sync to VBlank</strong> → <strong>Off</strong> (driver-level; HighAsCG sets <code>__GL_SYNC_TO_VBLANK=0</code> and <code>highascg-nvidia-x-apply.sh</code> on install/ISO).</li>\n<li><strong>Caspar screen consumer:</strong> <strong>V-sync</strong> → <strong>On</strong> (<code>screen_N_vsync: true</code> / <code>&lt;vsync&gt;true&lt;/vsync&gt;</code> in generated config).</li>\n</ol>\n<p>See <strong><a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">screen-consumer-vsync-nvidia.md</a></strong> for rationale, verification, and operator steps.</p>\n<hr>\n<h2 id=\"2-data-flow-summary\">2. Data flow summary</h2>\n<pre><code>Screen destinations  ──►  Caspar config (channels, screen consumers, positions)\n        │\n        │  (default) same WxH + tiling hint\n        ▼\nDevice graph edge  ──►  screen index N  ──►  xrandr: output=sysId, --mode, --pos\n        │\n        │  (override on)\n        ▼\nInspector Override + dropdown/custom  ──►  xrandr --mode / --rate from user choice</code></pre>\n<hr>\n<h2 id=\"3-related-code-current-anchors\">3. Related code (current anchors)</h2>\n<ul>\n<li><strong>Implementation method (screen index, apply-os, override):</strong> <a href=\"#/reference--xrandr-gpu-screen-mapping\" data-wiki-page=\"1\">xrandr-gpu-screen-mapping.md</a>.</li>\n<li>Layout planning: <code>src/utils/os-layout-calculator.js</code>, <code>src/utils/os-config.js</code> (<code>applyX11Layout</code>).</li>\n<li>Apply pipeline: <code>src/api/settings-os.js</code> (<code>POST /api/settings/apply-os</code>).</li>\n<li>UI: <code>client/components/device-view-inspector-gpu.js</code>, <code>device-view-bands-render.js</code>, <code>device-view-caspar-render.js</code>.</li>\n<li>Destination definitions: <code>src/config/screen-destinations.js</code> and persisted <code>screenDestinations</code> in config.</li>\n</ul>\n<hr>\n<h2 id=\"4-glossary\">4. Glossary</h2>\n<table><thead><tr><th>Term</th><th>Meaning</th></tr></thead><tbody>\n<tr><td>Screen destination</td><td>Logical output definition (mode, position on canvas, main index).</td></tr>\n<tr><td>GPU connector (<code>gpu_out</code>)</td><td>Physical video output; bound to a screen via graph edge.</td></tr>\n<tr><td>Override</td><td>User flag: OS xrandr mode/rate come from inspector, not from destination topology.</td></tr>\n</tbody></table>\n<hr>\n<p><em>Last updated: 2026-05-15 — authored for HighAsCG Device View / OS layout alignment.</em></p>","reference--SUDO_UBUNTU_SETUP":"<h1 id=\"highascg-sudo-no-password-setup-on-ubuntu\">HighAsCG sudo (no-password) setup on Ubuntu</h1>\n<p>This guide configures <strong>only the minimum commands</strong> that HighAsCG may run as root without a password when the app runs as <code>casparcg:casparcg</code>.</p>\n<p>Use this for the web UI &quot;Nuclear&quot; actions:</p>\n<ul>\n<li>restart window manager (<code>nodm</code>)</li>\n<li>reboot host</li>\n</ul>\n<h2 id=\"1-verify-service-user\">1) Verify service user</h2>\n<pre><code class=\"language-bash\">ps -o user= -p &quot;$(pgrep -f highascg | head -n1)&quot;</code></pre>\n<p>Expected user: <code>casparcg</code></p>\n<h2 id=\"2-create-a-dedicated-sudoers-file\">2) Create a dedicated sudoers file</h2>\n<p>Use <code>visudo</code> (never edit sudoers with a regular editor):</p>\n<pre><code class=\"language-bash\">sudo visudo -f /etc/sudoers.d/highascg</code></pre>\n<p>Add:</p>\n<pre><code class=\"language-sudoers\"># HighAsCG privileged actions (strict allowlist)\ncasparcg ALL=(root) NOPASSWD: /bin/systemctl restart nodm, /usr/bin/systemctl restart nodm, /sbin/reboot, /usr/sbin/reboot, /bin/systemctl reboot, /usr/bin/systemctl reboot</code></pre>\n<p>Save and exit.</p>\n<h2 id=\"3-set-correct-permissions\">3) Set correct permissions</h2>\n<pre><code class=\"language-bash\">sudo chown root:root /etc/sudoers.d/highascg\nsudo chmod 0440 /etc/sudoers.d/highascg</code></pre>\n<h2 id=\"4-test-as-casparcg\">4) Test as <code>casparcg</code></h2>\n<pre><code class=\"language-bash\">sudo -u casparcg sudo -n /bin/systemctl restart nodm\nsudo -u casparcg sudo -n /sbin/reboot</code></pre>\n<p>Notes:</p>\n<ul>\n<li><code>-n</code> means non-interactive (fail immediately instead of prompting for password).</li>\n<li>Reboot command will reboot immediately if permission is correct.</li>\n</ul>\n<h2 id=\"5-highascg-behavior\">5) HighAsCG behavior</h2>\n<p>HighAsCG calls sudo non-interactively (<code>sudo -n</code>). If sudoers is missing or too strict, API calls fail with permission errors.</p>\n<h2 id=\"security-recommendations\">Security recommendations</h2>\n<ul>\n<li>Keep this list <strong>narrow</strong> (only exact command paths).</li>\n<li>Do <strong>not</strong> allow broad patterns like <code>systemctl *</code>.</li>\n<li>Keep the file in <code>/etc/sudoers.d/</code> with mode <code>0440</code>.</li>\n<li>Review this file after OS updates (binary paths can differ by distro).</li>\n</ul>","reference--hot-backup-replication":"<h1 id=\"hot-backup-replication-wo-54-wo-61\">Hot backup replication (WO-54 / WO-61)</h1>\n<p>Two playout machines run as a <strong>leader/follower</strong> pair for hot backup.</p>\n<h2 id=\"operator-workflow-device-view\">Operator workflow (Device View)</h2>\n<h3 id=\"leader\">Leader</h3>\n<ol>\n<li>Device View → select <strong>Server</strong> (rear panel band).</li>\n<li><strong>Hot backup</strong> → mode <strong>Leader</strong> → <strong>Become leader</strong>.</li>\n<li>This sets <code>leaderAvailable</code> — followers can discover this box on the LAN.</li>\n</ol>\n<h3 id=\"follower\">Follower</h3>\n<ol>\n<li>Device View → <strong>Server</strong> on the backup box.</li>\n<li>Mode <strong>Follower</strong> → <strong>Scan for leaders</strong> → pick from dropdown.</li>\n<li><strong>Connect to leader</strong> — syncs running project, timelines, and <strong>media clips used in looks</strong> (not the whole library).</li>\n</ol>\n<p>When paired, the inspector shows: <em>Screen destinations sync from the leader; wiring and Caspar consumers stay local on this box.</em></p>\n<p><strong>Caspar config on the follower</strong> is generated from <strong>this box’s</strong> Device View wiring (cables, GPU/DeckLink layout) plus <strong>leader screen destinations</strong> (channel ids/modes). Leader sync does not copy leader cables or consumer topology.</p>\n<ul>\n<li>Local machine profile (<code>config/replication-local-machine.json</code>) stores <strong>wiring and routing slices only</strong> — not screen destinations.</li>\n<li>After leader show sync, the follower <strong>re-applies</strong> local wiring and <strong>leader destinations</strong>, then <strong>regenerates</strong> <code>casparcg.config</code>.</li>\n<li>Manual: <strong>Server → Hot backup → Regenerate Caspar from Device View</strong> (or <code>POST /api/replication/reload-local-machine</code>).</li>\n</ul>\n<p>If the backup was seeded via full mirror rsync, it may still have the leader’s <code>device_graph.json</code> on disk until you <strong>edit Device View on the backup once</strong> (wire only what exists on that box), then regenerate.</p>\n<h3 id=\"disconnect\">Disconnect</h3>\n<ul>\n<li><strong>Disconnect (standalone)</strong> on either box, or automatic after ~5s lost peer link.</li>\n<li><strong>Both machines keep playing locally</strong> — no auto-promote, no stop.</li>\n</ul>\n<h2 id=\"data-tiers-smart-config-sync\">Data tiers (smart config sync)</h2>\n<p>Leader pushes <strong>show content</strong> and <strong>screen destination definitions</strong>. The follower <strong>keeps its own wiring</strong> (rear panel, cables, GPU/DeckLink mapping, Caspar consumer topology).</p>\n<table><thead><tr><th>Tier</th><th>Examples</th><th>Leader → follower?</th><th>Mechanism</th></tr></thead><tbody>\n<tr><td><strong>Show content</strong></td><td>Looks/scenes, timelines, referenced media, templates, <strong>screen destinations</strong> (output ids, modes, video modes), logical routing intent (<code>audioRouting</code>, stream/record <strong>definitions</strong>, DMX, Companion maps), <strong><code>project.hotBackup</code></strong> (paired peer hardware id + hostname)</td><td><strong>Yes</strong></td><td>HTTP project/timeline push + pull; optional Syncthing for media</td></tr>\n<tr><td><strong>Machine profile</strong></td><td>Device View <strong>device graph</strong> (cables, rear panel), <code>osDisplay</code> / <code>screen_N_system_id</code>, GPU layout, DeckLink numbers, <code>casparServer</code> host/port</td><td><strong>Never</strong></td><td><code>config-classify.js</code> strip on push, merge preserves local on receive</td></tr>\n<tr><td><strong>Live playout</strong></td><td>Active scene, timeline position, mixer</td><td><strong>Yes</strong></td><td>WS <code>/api/replication/ws</code> + CT-SS scheduled apply on <strong>follower's</strong> channel map</td></tr>\n</tbody></table>\n<p>Implementation: <code>src/config/config-classify.js</code> — <code>stripDeviceLocalFromProject</code>, <code>mergeSharedProjectIntoLocal</code>, <code>splitConfigForReplication</code>.</p>\n<h3 id=\"project-pair-metadata-projecthotbackup-wo-78\">Project pair metadata (<code>project.hotBackup</code>, WO-78)</h3>\n<p>When paired, the <strong>active project</strong> stores show-tier pair metadata (replicated leader → follower):</p>\n<pre><code class=\"language-json\">{\n  &quot;hotBackup&quot;: {\n    &quot;pairId&quot;: &quot;uuid&quot;,\n    &quot;role&quot;: &quot;leader&quot;,\n    &quot;self&quot;: { &quot;hardwareId&quot;: &quot;1234&quot;, &quot;hostname&quot;: &quot;highascg1234&quot;, &quot;host&quot;: &quot;10.0.0.5&quot; },\n    &quot;peer&quot;: { &quot;hardwareId&quot;: &quot;7579&quot;, &quot;hostname&quot;: &quot;highascg7579&quot;, &quot;host&quot;: &quot;10.0.0.12&quot; },\n    &quot;pairedAt&quot;: &quot;2026-07-01T12:00:00.000Z&quot;\n  }\n}</code></pre>\n<ul>\n<li>Written on the <strong>leader</strong> when a follower registers (<code>applyLeaderHotBackupFromRegister</code>), then pushed with the project slice.</li>\n<li>Cleared locally when either box returns to <strong>standalone</strong> (manual disconnect or peer lost).</li>\n<li>UI: header badge shows <strong>Paired with highascg####</strong>; Device View → Hot backup lists peer hostname + hardware id.</li>\n<li>Follower UI resolves the paired box via <code>hotBackupPeerBoxForViewer</code> (stored metadata is leader-canonical).</li>\n</ul>\n<p>Hardware ids come from the primary Ethernet MAC (<code>highascg####</code> hostnames). Re-pair after upgrading to populate metadata on existing pairs.</p>\n<p><strong>Hostname apply:</strong> <code>highascg-hardware-hostname.service</code> runs at boot (before <code>highascg.service</code>) and migrates clone ISO names (<code>highascg-nvidia-*</code>) → <code>highascg####</code>. Bridge start also calls <code>ensureHardwareHostname()</code> as a fallback. Install on existing hosts: <code>sudo bash scripts/setup/16-hardware-hostname-boot.sh</code>; apply once: <code>sudo systemctl start highascg-hardware-hostname.service</code>.</p>\n<h3 id=\"wo-78-qa\">WO-78 QA</h3>\n<table><thead><tr><th>Scope</th><th>Command</th></tr></thead><tbody>\n<tr><td>Single box (smoke + identity)</td><td><code>bash ~/highascg/tools/runtime/replication-pair-qa.sh</code></td></tr>\n<tr><td>Register rejection probe</td><td><code>bash ~/highascg/tools/runtime/replication-pair-qa.sh --register-reject-test</code></td></tr>\n<tr><td>Post-pair two-box</td><td><code>REPL_QA_PEER=&lt;other-box-ip&gt; bash ~/highascg/tools/runtime/replication-pair-qa.sh</code></td></tr>\n<tr><td>Stick boot module</td><td>included as test <code>11</code> in <code>tools/startup/stick-boot-test/run-stick-boot-tests.sh</code></td></tr>\n</tbody></table>\n<p>Install rsync-only SSH wrapper on <strong>both</strong> boxes before pairing: <code>sudo bash scripts/replication/install-replication-ssh-wrapper.sh</code>.</p>\n<h3 id=\"audited-replication-paths-wo-61-t03\">Audited replication paths (WO-61 T0.3)</h3>\n<table><thead><tr><th>Path</th><th>Machine profile safe?</th></tr></thead><tbody>\n<tr><td><code>pushProjectToPeer</code></td><td>Yes — <code>stripDeviceLocalFromProject</code> before POST</td></tr>\n<tr><td><code>receiveProjectFromPeer</code></td><td>Yes — strip again on receive + <code>mergeSharedProjectIntoLocal</code></td></tr>\n<tr><td><code>GET /api/replication/export/project</code></td><td>Yes — strip on export</td></tr>\n<tr><td><code>reconcileFromLeader</code></td><td>Yes — uses export + receive (above)</td></tr>\n<tr><td><code>replicate-timelines</code></td><td>N/A — timelines only, no device graph</td></tr>\n<tr><td>Replication config saves (<code>connect-pair</code>, <code>promote</code>)</td><td>Only touches <code>replication</code> block</td></tr>\n<tr><td>Full rsync mirror (<code>push-backup-box.sh</code>)</td><td><strong>Raw clone</strong> — not for paired hot backup; use show-slice presets (WO-61)</td></tr>\n</tbody></table>\n<p>Peer rsync of <code>config/device_graph.json</code> must <strong>not</strong> be used on a paired follower without explicit operator opt-in. <code>screen_destinations.json</code> is replicated via project sync when paired.</p>\n<h2 id=\"project-media-sync-rsync-default\">Project media sync (rsync, default)</h2>\n<p>On connect and when the leader pushes project updates, HighAsCG syncs <strong>project-referenced media</strong> over <strong>rsync/SSH</strong> — not the whole <code>media/</code> tree:</p>\n<ol>\n<li><strong><code>media/projects/&lt;active-slug&gt;/</code></strong> — project-scoped folder (WO-62)</li>\n<li>Any <strong>referenced clips</strong> still in flat <code>media/</code> (legacy paths)</li>\n<li>Referenced <strong><code>template/</code></strong> HTML files</li>\n</ol>\n<table><thead><tr><th>Role</th><th>Direction</th><th>When</th></tr></thead><tbody>\n<tr><td>Leader</td><td>push → backup</td><td>Explicit push (media sync button / API) or connect flow</td></tr>\n<tr><td>Follower</td><td>pull ← leader</td><td>Explicit pull (media sync button / API) or connect flow</td></tr>\n</tbody></table>\n<p>Media is <strong>not</strong> synced automatically on project saves — spreading media is a deliberate operator action based on the project media folder (<code>media/projects/&lt;slug&gt;/</code>).</p>\n<p>Env (optional): <code>HIGHASCG_REPL_RSYNC_USER</code> (default <code>casparcg</code>), <code>HIGHASCG_REPL_RSYNC_REMOTE_ROOT</code> (default repo root), <code>HIGHASCG_REPL_RSYNC_SSH_OPTS</code>, <code>HIGHASCG_REPL_RSYNC_TIMEOUT_MS</code>.</p>\n<p>Manual: <code>POST /api/replication/sync-project-media</code> with optional <code>{ &quot;direction&quot;: &quot;push&quot;|&quot;pull&quot;|&quot;auto&quot; }</code>.</p>\n<p><strong>Requires</strong> passwordless SSH between boxes (same as <code>scripts/deploy/push-backup-box.sh</code>).</p>\n<blockquote><p><strong>Removed (2026-07):</strong> the legacy Syncthing <code>media/.replication-active/</code> staging-folder workflow. It hardlinked/copied every referenced clip into a hidden folder at the media root on each project save; those copies entered Caspar's CLS catalog and clip resolution could rewrite PLAY lines to the staged copies. Media now spreads only from <code>media/projects/&lt;slug&gt;/</code> via explicit rsync push/pull.</p></blockquote>\n<p>Bulk file sync between boxes can also use <strong>full rsync mirror</strong> (WO-61 / <code>push-backup-box.sh DEPLOY_MODE=mirror</code>).</p>\n<h2 id=\"project-files-autosave-and-replication-wo-76\">Project files, autosave, and replication (WO-76)</h2>\n<p>Each box keeps its own on-disk project tree under <code>~/highascg/projects/</code>:</p>\n<table><thead><tr><th>File</th><th>Scope</th><th>Replicated leader → follower?</th></tr></thead><tbody>\n<tr><td><code>projects/&lt;slug&gt;.json</code></td><td>Named save (explicit Save)</td><td><strong>Yes</strong> — HTTP project push sends <strong>main file at save time</strong> (<code>stripDeviceLocalFromProject</code>)</td></tr>\n<tr><td><code>projects/_autosave/&lt;slug&gt;.json</code></td><td>Debounced draft for <strong>that slug only</strong></td><td><strong>No</strong> — local per box; not part of replication POST body</td></tr>\n<tr><td>Active slug (<code>.highascg-state.json</code>)</td><td>Which show is live</td><td>Follower sets active slug when receiving leader project</td></tr>\n</tbody></table>\n<p><strong>Boot / USB / bridge (exFAT):</strong></p>\n<ul>\n<li><code>exfat-sync --boot</code> can pull <code>projects/</code> <strong>including <code>_autosave/</code></strong> into the working directory on <strong>non-leader</strong> boxes (<code>shouldAllowExfatPullShowData</code>).</li>\n<li><strong>Leader</strong> with <code>leaderAvailable</code> / <code>role: leader</code> <strong>blocks</strong> stick/bridge from overwriting the active show.</li>\n<li>Autosave pull (<code>pullAutosaveSlugFromVolumesIfNewer</code>) runs only for the <strong>requested slug</strong> — <code>_autosave/&lt;slug&gt;.json</code> never bleeds across slugs.</li>\n</ul>\n<p><strong>Operator expectations:</strong></p>\n<table><thead><tr><th>Action</th><th>Leader</th><th>Follower</th></tr></thead><tbody>\n<tr><td>Edit looks → autosave</td><td>Local <code>_autosave/&lt;active&gt;.json</code> + optional USB push</td><td>Own local autosave only</td></tr>\n<tr><td>Explicit Save</td><td>Main JSON pushed to follower on save hook</td><td>Receives show slice; <strong>keeps local Device View / GPU map</strong></td></tr>\n<tr><td>Load another project (UI)</td><td>Main file only (no autosave merge)</td><td>Same — not driven by replication</td></tr>\n<tr><td>Server restart (active slug)</td><td>Merges main + autosave for <strong>active slug only</strong></td><td>Same</td></tr>\n</tbody></table>\n<p>See also: <a href=\"#/wiki--api--project\" data-wiki-page=\"1\">wiki project API</a> — <code>POST /api/project/load</code> autosave merge rules.</p>\n<h2 id=\"ct-ss-time-aligned-mirror\">CT-SS / time-aligned mirror</h2>\n<p>Live intent packets include <code>applyAtLeaderTimeMs</code>. The follower estimates clock offset from ping (<code>serverTimeMs</code>) and applies takes at the aligned wall time — same approach as <a href=\"../../CT-SS-master/reade.md\">CT-SS SyncPlay</a>.</p>\n<p>Config: <code>scheduledApply: true</code>, <code>syncClock: ct-ss</code>, <code>scheduledApplyLeadMs: 1500</code> (tune on hardware).</p>\n<blockquote><p><strong>Note (2026-06-27):</strong> Semantic live-state mirror (<code>mirror-apply</code> re-take) is <strong>legacy</strong> for air sync. <strong>Shipped:</strong> <strong><a href=\"../work/work-orders/64_WO_HOT_BACKUP_AMCP_FANOUT.md\">WO-64 AMCP fan-out</a></strong>. <strong>Next:</strong> playhead sync + failover — <strong><a href=\"../work/work-orders/65_WO_HOT_BACKUP_ROBUSTNESS_FAILOVER_PLAYHEAD_SYNC.md\">WO-65</a></strong>.</p></blockquote>\n<h2 id=\"api\">API</h2>\n<table><thead><tr><th>Endpoint</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><code>POST /api/replication/become-leader</code></td><td>Advertise as available leader</td></tr>\n<tr><td><code>POST /api/replication/stop-leader</code></td><td>Stop advertising</td></tr>\n<tr><td><code>GET /api/replication/leaders</code></td><td>LAN scan for available leaders</td></tr>\n<tr><td><code>POST /api/replication/connect</code></td><td>Follower joins a leader</td></tr>\n<tr><td><code>POST /api/replication/disconnect</code></td><td>Return to standalone</td></tr>\n<tr><td><code>GET /api/replication/status</code></td><td>Role, peer, media %, lag, clock offset, <code>companion</code> control-plane, <code>projectHotBackup</code></td></tr>\n<tr><td><code>GET /api/companion/control-status</code></td><td>Lightweight Companion routing hint (<code>acceptsCompanionControl</code>, <code>suggestedCompanionTarget</code>)</td></tr>\n</tbody></table>\n<h3 id=\"companion-hot-backup-wo-70\">Companion hot backup (WO-70)</h3>\n<p>When Companion is configured with <strong>main</strong> and <strong>backup</strong> box hosts, poll:</p>\n<pre><code class=\"language-http\">GET /api/companion/control-status</code></pre>\n<table><thead><tr><th>Field</th><th>Use</th></tr></thead><tbody>\n<tr><td><code>acceptsCompanionControl</code></td><td>Send button actions to this box when <code>true</code></td></tr>\n<tr><td><code>suggestedCompanionTarget</code></td><td><code>self</code> — keep this connection; <code>peer</code> — prefer the paired leader IP; <code>none</code> — degraded</td></tr>\n<tr><td><code>controlPlaneReason</code></td><td><code>standalone</code>, <code>leader_air</code>, <code>follower_standby</code>, <code>follower_promoted_backup</code>, <code>degraded_no_caspar</code>, <code>not_paired</code></td></tr>\n<tr><td><code>warnings</code></td><td>e.g. <code>channel_parity_mismatch</code> — does not block control</td></tr>\n</tbody></table>\n<p>Same payload is nested under <code>companion</code> on <code>GET /api/replication/status</code>.</p>\n<h2 id=\"tests\">Tests</h2>\n<pre><code class=\"language-bash\">npm run test:replication</code></pre>\n<p>See also: <a href=\"../../work/work-orders/61_WO_RSYNC_PEER_SYNC_AND_NETWORK_SETTINGS.md\">61_WO_RSYNC_PEER_SYNC_AND_NETWORK_SETTINGS.md</a> for rsync peer sync and Tailscale/Syncthing settings UI.</p>","reference--amcp-pgm-look-take-pipeline":"<h1 id=\"pgm-look-take-amcp-pipeline\">PGM look take — AMCP pipeline</h1>\n<p><code>POST /api/scene/take</code> runs <code>runSceneTakeLbg</code> on the Caspar program channel (and optionally the paired preview channel). This document is the <strong>command order</strong> for a standard crossfade take to PGM.</p>\n<h2 id=\"routing-channel-1-pgm-channel-2-prv\">Routing (channel 1 PGM, channel 2 PRV)</h2>\n<table><thead><tr><th>Step</th><th>HTTP / API</th><th>Caspar channel</th></tr></thead><tbody>\n<tr><td>Send look to <strong>preview</strong></td><td><code>POST /api/scene/take</code> with <code>&quot;target&quot;: &quot;preview&quot;</code> (or <code>&quot;bus&quot;: &quot;prv&quot;</code>) and <code>channel</code> = PGM (1)</td><td>Incoming look built on <strong>PRV</strong> (2)</td></tr>\n<tr><td>Send look to <strong>program</strong></td><td><code>POST /api/scene/take</code> with <code>channel</code> = PGM (1), no <code>target</code></td><td>Incoming on <strong>PGM</strong> (1); previous PGM copied to <strong>PRV</strong> (2)</td></tr>\n</tbody></table>\n<p>Preview-only takes use <code>skipLayerVisualEquality: true</code> so layers always load even when live JSON already matches.</p>\n<h2 id=\"physical-layer-banks-pgm\">Physical layer banks (PGM)</h2>\n<p>Logical look layer <code>N</code> uses two physical layers on the <strong>same</strong> Caspar channel:</p>\n<table><thead><tr><th>Bank</th><th>Physical layer</th><th>When</th></tr></thead><tbody>\n<tr><td>A</td><td><code>N</code> (e.g. <code>10</code>)</td><td>Off-air or on-air depending on <code>programLayerBankByChannel</code></td></tr>\n<tr><td>B</td><td><code>N + 100</code> (e.g. <code>110</code>)</td><td>Opposite bank</td></tr>\n</tbody></table>\n<p><strong>Rule:</strong> never <code>MIXER FILL</code> / <code>MIXER CLEAR</code> on the <strong>active</strong> (on-air) bank while preparing a new look. Incoming media is prepared on the <strong>inactive</strong> bank, then opacity crossfade swaps visibility.</p>\n<p>See also <a href=\"#/reference--amcp-clean-look-fade\" data-wiki-page=\"1\">amcp-clean-look-fade.md</a>.</p>\n<h2 id=\"phase-0-planning-no-amcp\">Phase 0 — Planning (no AMCP)</h2>\n<ol>\n<li><code>diffScenes(currentScene, incomingScene)</code> → exit / update / enter.</li>\n<li><code>buildTakeJobs</code> → per-layer <code>LOADBG</code> + mixer lines + <code>PLAY</code> plan.</li>\n<li>Orphan layers still on Caspar but not in the incoming look are added to the exit list.</li>\n<li><code>shouldRunBankCrossfade</code> = transition duration &amp;gt; 0, previous look non-empty, not <code>MERGE/+Animate</code>.</li>\n</ol>\n<h2 id=\"phase-1-exit-fade-optional\">Phase 1 — Exit fade (optional)</h2>\n<p>When there are exiting media layers, transition duration &amp;gt; 0, and <strong>no</strong> bank crossfade:</p>\n<pre><code class=\"language-text\">MIXER 1-&lt;activeBank&gt; OPACITY 0 &lt;frames&gt; &lt;tween&gt;\nMIXER 1 COMMIT</code></pre>\n<h2 id=\"phase-2-stale-off-air-bank-cleanup\">Phase 2 — Stale off-air bank cleanup</h2>\n<p>On the <strong>inactive</strong> bank only, <code>STOP</code> + <code>MIXER CLEAR</code> any look-stack layer not in the incoming look (prevents leftovers after a prior swap).</p>\n<h2 id=\"phase-3-global-border-optional\">Phase 3 — Global border (optional)</h2>\n<p>CG <code>ADD</code> / <code>UPDATE</code> on layer <code>998</code> (or <code>996</code>), sometimes at opacity <code>0</code> before a linked fade.</p>\n<h2 id=\"phase-4-per-incoming-layer-inactive-bank\">Phase 4 — Per incoming layer (inactive bank)</h2>\n<p>For each layer in the incoming look (example: logical <code>10</code> → physical <code>110</code> when bank B is inactive):</p>\n<pre><code class=\"language-text\">MIXER 1-110 CLEAR\nLOADBG 1-110 &quot;clip.mov&quot;\nMIXER 1-110 OPACITY 0 0          # only when incoming is the top layer (bank B)\nMIXER 1-110 FILL &lt;x&gt; &lt;y&gt; &lt;scaleX&gt; &lt;scaleY&gt; 0\nMIXER 1-110 ROTATION &lt;deg&gt; 0\n… other DEFER mixer lines (volume, keyer, effects) …</code></pre>\n<p>When incoming is on <strong>bank A</strong> (under outgoing bank B), prep uses <code>MIXER 1-10 OPACITY 1 0</code> instead of hiding — incoming must be full opacity under the top layer.</p>\n<p>Then PIP overlay CG lines if configured.</p>\n<p><strong>Important:</strong> When incoming is on top, <code>OPACITY 0 0</code> is sent <strong>before</strong> <code>FILL</code> so geometry changes are not visible on program during prep.</p>\n<h2 id=\"phase-5-bank-crossfade-play-when-shouldrunbankcrossfade\">Phase 5 — Bank crossfade + PLAY (when <code>shouldRunBankCrossfade</code>)</h2>\n<p>After a short preroll (~80–180 ms when incoming is pre-hidden; ~80 ms when incoming is underneath).</p>\n<p><strong>Case A — incoming above outgoing</strong> (on-air <code>1-10</code>, incoming <code>1-110</code>):</p>\n<pre><code class=\"language-text\">MIXER 1 COMMIT\nPLAY 1-110\nMIXER 1-110 OPACITY 0 0\nMIXER 1-110 OPACITY 1 &lt;frames&gt; &lt;tween&gt;\nMIXER 1 COMMIT</code></pre>\n<p>Outgoing <code>1-10</code> stays at full opacity; teardown clears it after the fade window.</p>\n<p><strong>Case B — outgoing above incoming</strong> (on-air <code>1-110</code>, incoming <code>1-10</code>):</p>\n<pre><code class=\"language-text\">MIXER 1 COMMIT\nPLAY 1-10\nMIXER 1-10 OPACITY 1 0\nMIXER 1-110 OPACITY 0 &lt;frames&gt; &lt;tween&gt;\nMIXER 1 COMMIT</code></pre>\n<p>Incoming <code>1-10</code> is already at full opacity under <code>1-110</code>; only the top layer tweens.</p>\n<h2 id=\"phase-5b-cut-no-crossfade\">Phase 5b — Cut / no crossfade</h2>\n<pre><code class=\"language-text\">MIXER 1 COMMIT\nPLAY 1-110\nMIXER 1 COMMIT</code></pre>\n<h2 id=\"phase-6-teardown-after-fade-time\">Phase 6 — Teardown (after fade time)</h2>\n<pre><code class=\"language-text\">STOP 1-10\nMIXER 1-10 CLEAR\nMIXER 1 COMMIT</code></pre>\n<p>Bank pointer flips: inactive becomes active for the next take.</p>\n<h2 id=\"animate-path-same-on-air-physical-layer\"><code>+ Animate</code> path (same on-air physical layer)</h2>\n<p>No bank swap. Incoming uses <strong><code>phys(N, activeBank)</code></strong> — the layer currently on program (e.g. <code>1-110</code> when bank B is on air).</p>\n<p><strong>Phase A — Prepare</strong></p>\n<pre><code class=\"language-text\">LOADBG 1-110 &quot;example2.mov&quot; LOOP …\nMIXER 1-110 FILL &lt;x&gt; &lt;y&gt; &lt;sx&gt; &lt;sy&gt; 75 linear\nMIXER 1-110 ROTATION … 0\n… KEYER / VOLUME / effects (DEFER) …</code></pre>\n<p>No <code>LOADBG MIX</code>. No <code>MIXER OPACITY &lt;dur&gt;</code> on the on-air layer (Caspar <code>PLAY … MIX</code> owns the dissolve).</p>\n<p><strong>Phase B — COMMIT sandwich</strong></p>\n<pre><code class=\"language-text\">MIXER 1 COMMIT\nPLAY 1-110 &quot;example2.mov&quot; MIX 75 linear\nMIXER 1 COMMIT</code></pre>\n<p><code>FILL</code> duration/tween matches <code>PLAY</code> transition frames/tween.</p>\n<p><strong>Phase C — Teardown</strong></p>\n<p>After <code>fadeMs</code>: clear <strong>inactive</strong> bank ghost for updated layers; <code>STOP/CLEAR</code> true exits on both banks. <code>programLayerBank</code> unchanged. Then PGM→PRV exchange (<code>routes-scene.js</code>).</p>\n<h2 id=\"pgm-prv-dual-channel-summary\">PGM + PRV dual-channel summary</h2>\n<ol>\n<li><strong>Preview</strong> (<code>target: &quot;preview&quot;</code>): phases 0–6 on <strong>PRV</strong> only (uses <code>forceCut</code> from the request unless you need a faded preview).</li>\n<li><strong>Program</strong> (default): <strong>hard-cut</strong> incoming onto <strong>PRV</strong> first (<code>forceCut: true</code> always), then run the look transition on <strong>PGM</strong> only. Set <code>stageOnPreview: false</code> to skip PRV staging. After PGM take, <strong>previous</strong> PGM is copied onto <strong>PRV</strong> (hard cut).</li>\n</ol>\n<h2 id=\"what-causes-visible-fill-changes-avoid\">What causes visible FILL changes (avoid)</h2>\n<ul>\n<li><code>MIXER FILL</code> on the <strong>active</strong> bank during prep.</li>\n<li>Animated <code>MIXER FILL</code> with a non-zero duration on an on-air layer (<code>MERGE</code> path).</li>\n<li><code>FILL</code> before <code>OPACITY 0</code> on the incoming (off-air) layer.</li>\n</ul>\n<p>Current code applies immediate <code>FILL</code> (tail <code>0</code>). The inactive bank is hidden with <code>OPACITY 0 0</code> only when it is the <strong>top</strong> layer (bank B); when incoming is bank A, it is set to full opacity before <code>PLAY</code>.</p>","reference--pip-overlay-outside-border":"<h1 id=\"pip-overlay-outside-border-canonical-model\">PIP overlay — outside border (canonical model)</h1>\n<p><strong>Status:</strong> Canonical reference (2026-06-29). Do not regress this without updating this doc and <code>tools/smoke/smoke-pip-overlay-placement.test.js</code>.</p>\n<p>Outside borders, glows, and edge strips on scene-layer PIP overlays use a <strong>two-layer coordinate model</strong>: an expanded Caspar CG canvas with a normalized <strong>content hole</strong>. This is <strong>not</strong> the same as the full-frame <strong>global border</strong> (which always forces <code>side: 'inside'</code>).</p>\n<hr>\n<h2 id=\"the-rule\">The rule</h2>\n<blockquote><p><strong>Outside effects require the overlay CG layer to be physically larger than the video layer, with the template drawing on the content edge and the effect spilling into the margin.</strong></p></blockquote>\n<p>If the CG <code>MIXER FILL</code> matches the video rect exactly, an “outside” stroke is clipped by the layer bounds and looks like an <strong>inside</strong> border. That is a placement bug, not a template tweak.</p>\n<hr>\n<h2 id=\"placement-server-client-must-match\">Placement (server + client must match)</h2>\n<p>Implementation: <code>src/engine/pip-overlay-utils.js</code>, mirrored in <code>client/lib/pip-overlay-amcp.js</code>.</p>\n<table><thead><tr><th>Piece</th><th>Inside (<code>side: 'inside'</code>)</th><th>Outside (<code>side: 'outside'</code>)</th></tr></thead><tbody>\n<tr><td><code>MIXER FILL</code></td><td>Same as content layer</td><td><code>expandFillOutward(contentFill, totalOutsetPx, chW, chH)</code></td></tr>\n<tr><td><code>inner</code> in CG JSON</td><td><code>{ l:0, t:0, w:1, h:1 }</code></td><td><code>innerRectInOverlayNorm(contentFill, overlayFill)</code> — content hole inset in the expanded canvas</td></tr>\n<tr><td>Stack banding</td><td>N/A</td><td><code>computeOutsideStackBands</code> / <code>ringInnerPx</code> / <code>ringOuterPx</code> / <code>totalOutsetPx</code></td></tr>\n</tbody></table>\n<p><strong><code>totalOutsetPx</code></strong> = sum of <code>outsetPxForPipOverlay()</code> for every <strong>outside</strong> overlay in the stack (index 0 = furthest out, higher indices closer to content).</p>\n<p><strong><code>params.side</code> in CG JSON</strong> must match placement: <code>buildPipOverlayCgPayload()</code> forces <code>side</code> via <code>effectivePipOverlaySide()</code>. Defaults are <code>outside</code> for <code>border</code> and <code>edge_strip</code>; do not let registry defaults disagree between server and client.</p>\n<hr>\n<h2 id=\"amcp-order\">AMCP order</h2>\n<p>Every ADD and UPDATE must include:</p>\n<ol>\n<li><code>CG … UPDATE</code> with <code>inner</code>, <code>side</code>, and ring fields</li>\n<li><code>MIXER &lt;ch&gt;-&lt;overlayLayer&gt; FILL …</code> using the <strong>expanded</strong> fill (when outside)</li>\n<li><code>MIXER &lt;channel&gt; COMMIT</code> so DEFER lines apply (<code>sendPipOverlayLinesSerial</code>)</li>\n</ol>\n<p>Inside↔outside toggles require <strong>CG re-ADD</strong> (not UPDATE-only) so CEF reloads template behaviour. Live inspector keys stack shape by <code>type:side</code> for this reason.</p>\n<hr>\n<h2 id=\"template-rendering-pip_borderhtml-pip_glowhtml\">Template rendering (<code>pip_border.html</code>, <code>pip_glow.html</code>)</h2>\n<p>Do <strong>not</strong> use CSS <code>border</code>, <code>outline</code>, or SVG stroke centered on a content-sized box for outside mode — Caspar CEF often clips or paints inward.</p>\n<p><strong>Correct pattern</strong> (same as <code>pip_glow</code>):</p>\n<ol>\n<li><code>#root</code> fills the <strong>expanded</strong> CG viewport (<code>inset: 0</code>, <code>overflow: visible</code>).</li>\n<li><code>.pip-frame</code> is positioned at <code>inner</code> (<code>left/top/width/height</code> as % of root).</li>\n<li><strong>Outside:</strong> <code>box-shadow: 0 0 0 &lt;width&gt;px &lt;color&gt;</code> on <code>.pip-frame</code> — ring grows <strong>outward</strong> into the margin.</li>\n<li><strong>Inside:</strong> <code>box-shadow: inset 0 0 0 &lt;width&gt;px &lt;color&gt;</code>.</li>\n</ol>\n<p><code>pip_edge_strip.html</code> uses SVG on the content boundary in full-canvas coordinates for outside mode (animated dash). <code>pip_router.html</code> is legacy; multi-overlay stacks use <strong>one template per slot</strong> (<code>buildPipOverlayAmcpLinesAll</code>), not a single router layer.</p>\n<hr>\n<h2 id=\"anti-patterns-do-not-reintroduce\">Anti-patterns (do not reintroduce)</h2>\n<table><thead><tr><th>Wrong</th><th>Why it breaks</th></tr></thead><tbody>\n<tr><td>Content-sized <code>MIXER FILL</code> + <code>side: 'outside'</code></td><td>Outside half of stroke clipped → looks inside</td></tr>\n<tr><td><code>innerRectPipLocalFromOutset()</code> paired with expanded fill</td><td>Shrinks frame below content; border draws too small</td></tr>\n<tr><td>Placement <code>outside</code> but CG JSON <code>side: 'inside'</code></td><td>Template draws inset inside the content hole</td></tr>\n<tr><td><code>pip_router</code> for multi-overlay stacks</td><td>Collapsed effects; outside border regressed</td></tr>\n<tr><td>CSS <code>outline</code> / SVG stroke only, no expanded fill</td><td>Unreliable in CEF; margin required</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"global-border-contrast\">Global border contrast</h2>\n<p><code>src/engine/global-border.js</code> forces <code>side: 'inside'</code> because the global border spans the full channel (<code>inner = full canvas</code>). Outside mode there pushes past the HTML body and clips. <strong>PIP per-layer overlays</strong> are the only place outside borders are valid.</p>\n<hr>\n<h2 id=\"verification\">Verification</h2>\n<pre><code class=\"language-bash\">node --test tools/smoke/smoke-pip-overlay-placement.test.js</code></pre>\n<p>After template changes: restart <code>highascg</code>, hard-refresh UI, <strong>remove and re-add</strong> the overlay on air so Caspar reloads HTML.</p>\n<p><strong>See also:</strong> <a href=\"../../work/work-orders/25_WO_PIP_OVERLAY_EFFECTS.md\">25_WO_PIP_OVERLAY_EFFECTS.md</a></p>","reference--PIXELHUE_API":"<h1 id=\"pixelhue-unico-http-websocket-api-companion-pixelflow-openapi-v201\">PixelHue / Unico HTTP / WebSocket API (Companion + PixelFlow OpenAPI V2.0.1)</h1>\n<p>This document combines:</p>\n<ol>\n<li><strong>Authoritative path list</strong> — <strong>PixelFlow OpenAPI Manual V2.0.1</strong> (NovaStar), PDF at <a href=\"../reference/vendor/PixelFlow-OpenAPI%20Manual-V2.0.1.pdf\">&lt;code&gt;vendor/PixelFlow-OpenAPI Manual-V2.0.1.pdf&lt;/code&gt;</a> — supported devices: <strong>P10, P20, Q8, P80</strong> (per release notes; Companion also uses a <strong>PF</strong> model profile on virtual devices).</li>\n<li><strong>Request bodies and working usage</strong> — <strong>PixelHue</strong> Companion module in this repo: <code>companion-module-pixelhue-switcher-main 2/</code> (<code>src/services/ApiClient.ts</code>, <code>src/config/devices/*.ts</code>).</li>\n</ol>\n<blockquote><p><strong>Caveats</strong>   - <strong>Base URL</strong> in the manual: <code>http://{ip}:8088/unico</code> + <em>function path</em> (example port); on real gear the <strong>HTTP API port</strong> comes from discovery (<code>device-list</code> → <code>protocols</code> with <code>linkType: 'http'</code>), not necessarily <code>8088</code>.   - <strong>Unico</strong> control plane uses <strong>port 19998</strong> (HTTPS) for discovery and WebSocket. <strong>REST</strong> for mixing uses <code>http://{host}:{apiPort}</code>.   - Bodies in this doc reflect the <strong>TypeScript</strong> module; the PDF has fuller field tables. Use the PDF for edge-case parameters.   - Use <strong>self-signed</strong> TLS on discovery as the module does: <code>rejectUnauthorized: false</code> on HTTPS.</p></blockquote>\n<hr>\n<h2 id=\"1-transports-and-ports\">1. Transports and ports</h2>\n<table><thead><tr><th>Channel</th><th>Base URL</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><strong>Discovery</strong> (Companion)</td><td><code>https://{host}:19998</code></td><td><code>GET /unico/v1/ucenter/device-list</code> — list devices (SN, model, <code>protocols</code> with ports). <em>Not</em> listed as a <code>Path</code> line in the PixelFlow PDF; it is the Unico/ucenter discovery the module uses.</td></tr>\n<tr><td><strong>Node open detail</strong> (HTTP API)</td><td><code>http://{host}:{apiPort}</code></td><td><code>GET /unico/v1/node/open-detail</code> — <strong>sn</strong> + <strong>startTime</strong> for JWT.</td></tr>\n<tr><td><strong>REST (mixing)</strong></td><td><code>http://{host}:{apiPort}</code></td><td><code>http://{host}:{apiPort}/unico/v1/...</code> with <code>Authorization: {token}</code>.</td></tr>\n<tr><td><strong>WebSocket</strong> (Companion)</td><td><code>wss://{host}:19998/unico/v1/ucenter/ws?client-type=8</code></td><td>TLV-framed events; <code>Authorization: {token}</code>.</td></tr>\n</tbody></table>\n<p><code>HttpClient</code> in the module sends paths <strong>not</strong> starting with <code>/ucenter</code> to <code>http://{host}:{apiPort}</code>; paths like <code>/ucenter/...</code> go to <code>https://{host}:{unicoPort}</code> (see <code>ApiClient</code> / <code>HttpClient</code>).</p>\n<hr>\n<h2 id=\"2-authentication\">2. Authentication</h2>\n<p>The <strong>PixelFlow manual</strong> documents two ways to obtain a token; the <strong>Companion module uses only the second</strong>.</p>\n<h3 id=\"21-user-login-pdf-11-not-used-by-the-companion\">2.1 User login (PDF §1.1) — <em>not</em> used by the Companion</h3>\n<table><thead><tr><th>Method</th><th>Path</th></tr></thead><tbody>\n<tr><td>POST</td><td><code>/unico/v1/system/auth/login</code></td></tr>\n</tbody></table>\n<ul>\n<li>Body: <code>username</code>, <code>password</code> (JSON).</li>\n<li>Returns a <code>data.token</code> suitable for <code>Authorization: {token}</code> on subsequent calls.</li>\n</ul>\n<h3 id=\"22-open-detail-hs256-jwt-pdf-12-companion-used-by-the-module\">2.2 Open detail + HS256 JWT (PDF §1.2 + Companion) — <em>used by the module</em></h3>\n<ol>\n<li><code>GET /unico/v1/node/open-detail</code> on the <strong>device HTTP</strong> port (optional <code>nodeId</code> query in the manual).</li>\n<li>Read <code>data.sn</code> and <code>data.startTime</code> (manual: startup time, ms, used as shared secret in Companion).</li>\n<li>Build a <strong>JWT</strong> (HS256, <strong>no <code>iat</code></strong>) and send as <code>Authorization: {token}</code> on authed requests and on WSS (Companion).</li>\n</ol>\n<pre><code class=\"language-ts\">// companion: src/utils/utils.ts\ntoken = jwt.sign({ SN: serialNumber }, startTime, { algorithm: 'HS256', noTimestamp: true })</code></pre>\n<ol>\n<li><code>GET /unico/v1/node/detail</code> (PDF §2.1) — full node information with <code>Authorization</code>; Companion does not call this in <code>ApiClient</code> setup, but it is the official “node detail” read when you need working mode, etc.</li>\n</ol>\n<hr>\n<h2 id=\"3-pixelflow-v201-official-path-index-from-the-manual\">3. PixelFlow V2.0.1 — official <code>Path</code> index (from the manual)</h2>\n<p>All paths are prefixed with <strong><code>/unico</code></strong>; the relative part in the PDF is <code>/v1/...</code>, so the full path is <strong><code>/unico/v1/...</code></strong>.</p>\n<table><thead><tr><th>Method</th><th>Path (full)</th><th>Notes</th></tr></thead><tbody>\n<tr><td>POST</td><td><code>/unico/v1/system/auth/login</code></td><td>See §2.1.</td></tr>\n<tr><td>GET</td><td><code>/unico/v1/node/open-detail</code></td><td>Public node info; feeds JWT in Companion.</td></tr>\n<tr><td>GET</td><td><code>/unico/v1/node/detail</code></td><td>Authed node information.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/system/restore-factory</code></td><td>Factory reset.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/screen/global/switch-effect</code></td><td>Global transition effect.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/screen/global/swap</code></td><td>Global swap.</td></tr>\n<tr><td>GET</td><td><code>/unico/v1/node/state-info</code></td><td>Monitoring / state.</td></tr>\n<tr><td>GET</td><td><code>/unico/v1/interface/list-detail</code></td><td>Connectors / inputs.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/node/interface-location</code></td><td>Interface placement.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/interface/image-quality</code></td><td>Image quality.</td></tr>\n<tr><td><strong>Screen API v1 (legacy, PDF §4.1)</strong></td><td></td><td><em>Global</em> selected-screen behaviour.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/screen/selected/ftb</code></td><td>Global FTB (<code>ftb.enable</code>, <code>ftb.time</code>).</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/screen/selected/freeze</code></td><td>Global freeze (<code>freeze</code> 0/1).</td></tr>\n<tr><td><strong>Screen API (shared + v2 per-screen, PDF §4.1.3+ / §4.2)</strong></td><td></td><td></td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/screen/take</code></td><td>Take (PVW→PGM / PGM→PVW per <code>direction</code>).</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/screen/cut</code></td><td>Cut.</td></tr>\n<tr><td>GET</td><td><code>/unico/v1/screen/list-detail</code></td><td>Screen list.</td></tr>\n<tr><td><strong>Screen API v2 (PDF §4.2 )</strong></td><td></td><td><em>Per-screen</em> arrays (Companion uses this style).</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/screen/freeze</code></td><td>Per-screen freeze.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/screen/ftb</code></td><td>Per-screen FTB.</td></tr>\n<tr><td><strong>Layer (PDF §5)</strong></td><td></td><td></td></tr>\n<tr><td>GET</td><td><code>/unico/v1/layers/list-detail</code></td><td>Layers; supports query <code>limit</code>, <code>page</code>, <code>layerId</code>, etc.</td></tr>\n<tr><td>GET</td><td><code>/unico/v1/layers/template</code></td><td>Layer <em>templates</em> (<code>moduleType</code>, <code>templateId</code> query).</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/layers/template/select</code></td><td>Apply template selection.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/layers/source</code></td><td>Set layer source.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/layers/select</code></td><td><strong>Layer select</strong> — array of objects with <code>layerId</code> and <code>selected</code> (0/1).</td></tr>\n<tr><td><strong>Preset (PDF §6; v1 vs v2 chapters)</strong></td><td></td><td></td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/preset/general</code></td><td>Edit preset (v1-style).</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/preset/create-assign</code></td><td>Create / assign.</td></tr>\n<tr><td>PUT</td><td><code>/unico/v1/preset/play</code></td><td>Play.</td></tr>\n<tr><td>GET</td><td><code>/unico/v1/preset</code></td><td>List / detail (Companion: list for show presets).</td></tr>\n<tr><td>POST</td><td><code>/unico/v1/preset/create</code></td><td>Create preset.</td></tr>\n<tr><td>POST</td><td><code>/unico/v1/preset/apply</code></td><td><strong>Load</strong> preset to region (used by Companion).</td></tr>\n<tr><td>POST</td><td><code>/unico/v1/preset/update-name</code></td><td>Rename.</td></tr>\n<tr><td>POST</td><td><code>/unico/v1/preset/delete</code></td><td>Delete.</td></tr>\n<tr><td><strong>Gallery (PDF §7)</strong></td><td></td><td></td></tr>\n<tr><td>GET</td><td><code>/unico/v1/picture/list</code></td><td>Image gallery list.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"4-companion-vs-manual-renames-aliases-and-extra-endpoints\">4. Companion vs manual — renames, aliases, and extra endpoints</h2>\n<table><thead><tr><th>Topic</th><th>PixelFlow V2.0.1 (manual)</th><th>PixelHue Companion (<code>PF.ts</code> and siblings)</th></tr></thead><tbody>\n<tr><td><strong>Layer selection</strong></td><td><code>PUT /unico/v1/layers/select</code> (§5.5)</td><td><code>PUT /unico/v1/screen/select</code> — <strong>same</strong> <code>{ layerId, selected }</code> pattern; all models use <code>screen/select</code>. If a new client follows the manual strictly, use <code>layers/select</code> first; fall back to <code>screen/select</code> if the device was tuned for the Companion path.</td></tr>\n<tr><td><strong>Layer “style” / looks</strong></td><td><code>GET /unico/v1/layers/template</code>, <code>PUT /unico/v1/layers/template/select</code></td><td><code>GET /unico/v1/layers/layer-preset/list-detail</code>, <code>PUT /unico/v1/layers/layer-preset/apply</code> — <strong>different path names</strong>; same product idea (per-layer look). Use what your firmware exposes.</td></tr>\n<tr><td><strong>Layer geometry / PIP</strong></td><td><em>Not</em> in the PDF’s <code>Path</code> list</td><td><code>PUT /unico/v1/layers/window</code></td></tr>\n<tr><td><strong>Z-order</strong></td><td><em>Not</em> in the PDF’s <code>Path</code> list</td><td><code>PUT /unico/v1/layers/zorder</code></td></tr>\n<tr><td><strong>UMD / metadata</strong></td><td><em>Not</em> in the PDF’s <code>Path</code> list</td><td><code>PUT /unico/v1/layers/umd</code></td></tr>\n<tr><td><strong>Source backup</strong></td><td><em>Not</em> in the PDF’s <code>Path</code> list</td><td><code>GET</code> / <code>PUT</code> <code>/unico/v1/system/ctrl/source-backup</code> (key <code>crtl</code> in <code>MachineConfig</code> is a typo in TS).</td></tr>\n</tbody></table>\n<p>These <strong>extra</strong> <code>layers/<em></code> and <code>system/ctrl/</em></code> routes are used by the shipping module; treat them as <strong>PixelHue firmware extensions</strong> on top of the published PixelFlow list until NovaStar documents them in a newer manual.</p>\n<h3 id=\"example-endpoint-map-model-pf-module-paths\">Example endpoint map (model <strong>PF</strong>) — module paths</h3>\n<table><thead><tr><th>Area</th><th>Method</th><th>Path</th><th>Role</th></tr></thead><tbody>\n<tr><td><strong>Screens</strong></td><td>GET</td><td><code>/unico/v1/screen/list-detail</code></td><td>List screens.</td></tr>\n<tr><td></td><td>PUT</td><td><code>/unico/v1/screen/take</code> / <code>…/cut</code> / <code>…/ftb</code> / <code>…/freeze</code></td><td>v2 per-screen style bodies.</td></tr>\n<tr><td></td><td>PUT</td><td><code>/unico/v1/screen/select</code></td><td>Layer select (see alias table).</td></tr>\n<tr><td><strong>Show presets</strong></td><td>GET</td><td><code>/unico/v1/preset</code></td><td>List presets.</td></tr>\n<tr><td></td><td>POST</td><td><code>/unico/v1/preset/apply</code></td><td>Load preset.</td></tr>\n<tr><td><strong>Layers</strong></td><td>GET</td><td><code>/unico/v1/layers/list-detail</code></td><td>All layers.</td></tr>\n<tr><td></td><td>PUT</td><td><code>…/source</code>, <code>…/window</code>, <code>…/zorder</code>, <code>…/umd</code></td><td>See §4.</td></tr>\n<tr><td></td><td>GET / PUT</td><td><code>…/layer-preset/list-detail</code>, <code>…/layer-preset/apply</code></td><td>PH layer style.</td></tr>\n<tr><td><strong>Inputs</strong></td><td>GET</td><td><code>/unico/v1/interface/list-detail</code></td><td>Interfaces.</td></tr>\n<tr><td><strong>System</strong></td><td>GET / PUT</td><td><code>/unico/v1/system/ctrl/source-backup</code></td><td>Backup.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"5-preset-load-target-region-loadin\">5. Preset load target region (<code>LoadIn</code>)</h2>\n<p>From <code>src/interfaces/Preset.ts</code>:</p>\n<table><thead><tr><th>Constant</th><th>Value</th><th>Meaning (module comments)</th></tr></thead><tbody>\n<tr><td><code>LoadIn.preview</code></td><td><strong>4</strong></td><td><strong>Preview (PVW)</strong>.</td></tr>\n<tr><td><code>LoadIn.program</code></td><td><strong>2</strong></td><td><strong>Program (PGM)</strong>.</td></tr>\n</tbody></table>\n<p><strong><code>POST</code> <code>/unico/v1/preset/apply</code></strong> (example from <code>ApiClient.loadPreset</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;auxiliary&quot;: {\n    &quot;keyFrame&quot;: { &quot;enable&quot;: 1 },\n    &quot;switchEffect&quot;: { &quot;type&quot;: 1, &quot;time&quot;: 500 },\n    &quot;swapEnable&quot;: 1,\n    &quot;effect&quot;: { &quot;enable&quot;: 1 }\n  },\n  &quot;serial&quot;: 1,\n  &quot;targetRegion&quot;: 4,\n  &quot;presetId&quot;: &quot;&lt;preset-guid&gt;&quot;\n}</code></pre>\n<ul>\n<li><code>targetRegion</code>: <code>4</code> = preview; <code>2</code> = program (per <code>LoadIn</code>).</li>\n</ul>\n<hr>\n<h2 id=\"6-take-preview-program\">6. Take (preview → program)</h2>\n<p><strong><code>PUT</code> <code>/unico/v1/screen/take</code></strong></p>\n<p>Body: <strong>array, one object per screen</strong> (<code>ApiClient.take</code>). The <strong>manual</strong> documents <code>screenId</code> / <code>screenName</code>, <code>effectSelect</code> (0 = default, 1 = custom effect), <code>direction</code> (0 = PVW→PGM, 1 = PVW from PGM), <code>switchEffect</code> (<code>type</code> 0 = CUT, 1 = FADE; <code>time</code> in ms), and <code>swapEnable</code> where applicable.</p>\n<pre><code class=\"language-json\">[\n  {\n    &quot;direction&quot;: 0,\n    &quot;effectSelect&quot;: 0,\n    &quot;screenGuid&quot;: &quot;&lt;guid&gt;&quot;,\n    &quot;screenId&quot;: 1,\n    &quot;screenName&quot;: &quot;Screen A&quot;,\n    &quot;swapEnable&quot;: 1,\n    &quot;switchEffect&quot;: { &quot;type&quot;: 1, &quot;time&quot;: 500 }\n  }\n]</code></pre>\n<p><strong><code>PUT</code> <code>/unico/v1/screen/cut</code></strong>: array with <code>direction</code>, <code>screenId</code>, <code>swapEnable</code> (Companion).</p>\n<hr>\n<h2 id=\"7-layer-operations-companion-manual-alignment\">7. Layer operations (Companion + manual alignment)</h2>\n<ul>\n<li><strong>Select layer (official name)</strong> — <code>PUT</code> <code>/unico/v1/layers/select</code> (manual §5.5). <strong>Companion</strong> calls <code>PUT</code> <code>/unico/v1/screen/select</code> with the same selection semantics.</li>\n<li><strong>Z-order</strong> — <code>PUT</code> <code>/unico/v1/layers/zorder</code>: <code>[{ &quot;layerId&quot;, &quot;zorder&quot;: { &quot;type&quot;: 1, &quot;para&quot;: &lt;to&gt; } }]</code> (module; not a named <code>Path</code> in V2.0.1).</li>\n<li><strong>Layer bounds</strong> — <code>PUT</code> <code>/unico/v1/layers/window</code>.</li>\n<li><strong>Set input</strong> — <code>PUT</code> <code>/unico/v1/layers/source</code>.</li>\n<li><strong>Layer style</strong> — <code>PUT</code> <code>/unico/v1/layers/layer-preset/apply</code> in the module; manual lists <strong>template</strong> APIs under <code>GET/PUT</code> <code>/unico/v1/layers/template</code> (+ <code>…/template/select</code>).</li>\n</ul>\n<blockquote><p><strong>Name collision:</strong> “Layer preset” on PixelHue is a <strong>per-layer style template</strong>, not HighAsCG’s <strong>look preset</strong> (whole scene). In the UI, call them <strong>PH layer style</strong> vs <strong>look preset</strong>.</p></blockquote>\n<hr>\n<h2 id=\"8-websocket-event-stream\">8. WebSocket (event stream)</h2>\n<ul>\n<li><strong>URL:</strong> <code>wss://{host}:19998/unico/v1/ucenter/ws?client-type=8</code></li>\n<li><strong>Header:</strong> <code>Authorization: {token}</code></li>\n<li><strong>Payload:</strong> <strong>TLV</strong> binary: JSON header in first TLV, then tag + JSON body (<code>WebSocketClient.parseTLVBuffer</code>).</li>\n<li><strong>Dispatch:</strong> <code>webSocketHandlers[tag]</code> in <code>WebSocketHandling</code> (<code>src/services/WebSocketHandling.ts</code>).</li>\n</ul>\n<p>HighAsCG can start with <strong>HTTP polling</strong>; WS is for low-latency mirroring and Companion feedbacks.</p>\n<hr>\n<h2 id=\"9-reference-files\">9. Reference files</h2>\n<table><thead><tr><th>File</th><th>Content</th></tr></thead><tbody>\n<tr><td><a href=\"../reference/vendor/PixelFlow-OpenAPI%20Manual-V2.0.1.pdf\">&lt;code&gt;vendor/PixelFlow-OpenAPI Manual-V2.0.1.pdf&lt;/code&gt;</a></td><td>Official paths, methods, and field tables.</td></tr>\n<tr><td><code>src/services/ApiClient.ts</code></td><td>HTTP methods and bodies.</td></tr>\n<tr><td><code>src/services/HttpClient.ts</code></td><td>HTTP vs HTTPS base selection.</td></tr>\n<tr><td><code>src/services/WebSocketClient.ts</code></td><td>WSS URL, TLV parsing.</td></tr>\n<tr><td><code>src/config/devices/PF.ts</code> (etc.)</td><td>Per-model path constants.</td></tr>\n<tr><td><code>src/interfaces/*.ts</code></td><td>DTOs.</td></tr>\n<tr><td><code>PRESET-AUTO-TAKE/api-reference.md</code></td><td>Preset-apply + take.</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"10-security-note-for-highascg\">10. Security note for HighAsCG</h2>\n<ul>\n<li>Prefer a <strong>server-side</strong> proxy: Node holds <strong>host, ports, and token</strong>; the browser only calls <strong><code>/api/pixelhue/...</code></strong>.</li>\n<li><strong>Never</strong> embed <code>open-detail</code> material or long-lived tokens in the static web bundle.</li>\n<li>Keep control traffic on <strong>LAN</strong> or VPN; the module disables TLS verification for self-signed ucenter on discovery.</li>\n</ul>\n<p>This document is the technical baseline for HighAsCG PixelHue / PixelFlow integration.</p>","reference--polling-vs-osc":"<h1 id=\"polling-vs-osc-in-highascg\">Polling vs OSC in HighAsCG</h1>\n<p>CasparCG exposes rich state in two ways: <strong>AMCP</strong> (TCP, request/response) and <strong>OSC</strong> (UDP, push). HighAsCG uses OSC for real-time mixer, layer, playback, profiler, and output data where implemented, and keeps AMCP for commands and for <strong>fallback</strong> when OSC is off or incomplete.</p>\n<p><strong>Related:</strong> <a href=\"#/osc-integration\" data-wiki-page=\"1\">osc-integration.md</a></p>\n<hr>\n<h2 id=\"what-osc-replaces-conceptually\">What OSC replaces (conceptually)</h2>\n<table><thead><tr><th>AMCP / legacy approach</th><th>OSC source</th><th>Benefit</th></tr></thead><tbody>\n<tr><td><code>periodic-sync</code> → <code>INFO</code> / layer polling for status</td><td><code>/channel/N/stage/layer/L/type</code>, <code>paused</code>, <code>time</code>, <code>frame</code>, …</td><td>Push updates at frame rate instead of every N seconds</td></tr>\n<tr><td><code>playback-tracker</code> matrix from command hooks only</td><td><code>file/time</code>, <code>file/frame</code> on layers</td><td>Authoritative playback from server; works for external plays</td></tr>\n<tr><td><code>INFO</code> for clip progress</td><td><code>file/time</code> (elapsed/total)</td><td>No TCP round-trip for progress</td></tr>\n<tr><td>Companion variables from polled state</td><td>OSC-driven snapshot → variables</td><td>Finer-grained updates</td></tr>\n<tr><td>Channel format from <code>INFO CONFIG</code> (where replaced)</td><td><code>/channel/N/format</code></td><td>Format changes without full config poll</td></tr>\n<tr><td><em>(No AMCP equivalent for live VU)</em></td><td><code>/channel/N/mixer/audio/M/dBFS</code></td><td><strong>New</strong> live mixer levels</td></tr>\n<tr><td>Consumer health from <code>INFO</code> (where replaced)</td><td><code>output/port/P/type</code>, <code>frame</code></td><td>Push updates for outputs</td></tr>\n</tbody></table>\n<p>Exact behavior depends on build and config: some AMCP paths remain for <strong>reconciliation</strong>, <strong>startup</strong>, or when OSC is disabled.</p>\n<hr>\n<h2 id=\"performance-comparison-typical\">Performance comparison (typical)</h2>\n<table><thead><tr><th>Aspect</th><th>AMCP polling</th><th>OSC push</th></tr></thead><tbody>\n<tr><td>Transport</td><td>TCP, synchronous commands per query</td><td>UDP, fire-and-forget</td></tr>\n<tr><td>Cadence</td><td>User-defined interval (e.g. 3–30 s for full sync; OSC-aware paths may use longer intervals)</td><td>Frame rate for active addresses (often <strong>25–60+ messages/sec</strong> per hot channel)</td></tr>\n<tr><td>Layer/channel cost</td><td>One or more <code>INFO</code>/<code>INFO channel-layer</code> per item per tick</td><td>Many small OSC messages; no per-query round trip</td></tr>\n<tr><td>Suitability</td><td>Config, one-shot queries, fallback</td><td>Live meters, playback, layer state</td></tr>\n</tbody></table>\n<p><strong>Rule of thumb:</strong> polling is appropriate for <strong>occasional</strong> consistency checks; OSC is appropriate for <strong>continuous</strong> UI and automation that must track <strong>live</strong> state.</p>\n<hr>\n<h2 id=\"periodic-sync-and-osc\">Periodic sync and OSC</h2>\n<p>When the <strong>OSC UDP listener is running</strong> (normal production setup), HighAsCG does <strong>not</strong> run a periodic AMCP poll loop. Caspar pushes layer/mixer/playback state; HighAsCG listens — there is nothing to “sync” on a timer.</p>\n<table><thead><tr><th>Data</th><th>How it updates</th></tr></thead><tbody>\n<tr><td>Layers, playback, meters</td><td>OSC push from Caspar</td></tr>\n<tr><td>Media/template catalog (CLS/TLS)</td><td>Once on Caspar connect + manual refresh / upload / ingest</td></tr>\n<tr><td>Channel INFO (AMCP fallback)</td><td><strong><code>periodic_sync_interval_sec</code></strong> interval only when OSC is <strong>off</strong> (<code>--no-osc</code> or <code>osc.enabled: false</code>)</td></tr>\n</tbody></table>\n<p><code>periodic_sync_interval_sec_osc</code> is <strong>ignored</strong> when OSC is on (legacy setting; do not use for CLS).</p>\n<p>Tune AMCP-only polling via <code>periodic_sync_interval_sec</code> / <code>HIGHASCG_PERIODIC_SYNC_SEC</code> (only applies without OSC).</p>\n<h3 id=\"info-1-or-info-on-your-program-channel-every-2-seconds\"><code>INFO 1</code> (or <code>INFO</code> on your program channel) every ~2 seconds</h3>\n<p>This is <strong>not</strong> the main periodic sync interval. <strong>By default (OSC production)</strong> this supplement is <strong>off</strong> — no periodic <code>INFO 1</code> spam. If you enable it (<code>startOscPlaybackInfoSupplement</code> in <code>src/utils/periodic-sync.js</code>), HighAsCG sends <strong><code>INFO</code> + program channel</strong> on a fixed schedule for edge cases where OSC omits <code>file/time</code>.</p>\n<p><strong>Why opt-in:</strong> most installs get full playback data from OSC; AMCP <code>INFO</code> every ~2s was noisy in Caspar logs.</p>\n<p><strong>Tune or enable</strong></p>\n<table><thead><tr><th>Goal</th><th>How</th></tr></thead><tbody>\n<tr><td>OSC only (default)</td><td>Leave <code>osc_info_supplement_ms</code> unset / <strong><code>null</code></strong> / <strong><code>0</code></strong> (or omit <code>HIGHASCG_OSC_INFO_MS</code>)</td></tr>\n<tr><td>Enable supplement</td><td>Set <code>osc_info_supplement_ms</code> ≥ <strong>500</strong> in <code>highascg.config.json</code> or env <code>HIGHASCG_OSC_INFO_MS</code></td></tr>\n</tbody></table>\n<p>Minimum when enabled: <strong>500</strong> ms.</p>\n<hr>\n<h2 id=\"fallback-when-osc-is-unavailable\">Fallback when OSC is unavailable</h2>\n<ol>\n<li><strong>Listener disabled</strong> (<code>--no-osc</code> only): no UDP bind; <code>oscState</code> is absent; REST <code>/api/osc/*</code> returns disabled messaging; WebSocket does not emit live OSC payloads.</li>\n<li><strong>Caspar not sending</strong> (wrong IP/port, firewall, Caspar config): listener runs but <strong>no</strong> or <strong>stale</strong> data; UI components should treat missing data as empty; <strong>periodic-sync</strong> and <strong>AMCP</strong> paths remain for reconciliation.</li>\n<li><strong>Partial data</strong>: e.g. non-FFmpeg layer — <code>file/time</code> may be absent; UI falls back to neutral/muted display (see individual components).</li>\n</ol>\n<hr>\n<h2 id=\"when-to-prefer-amcp\">When to prefer AMCP</h2>\n<ul>\n<li><strong>Commands</strong>: <code>PLAY</code>, <code>LOAD</code>, <code>CG</code>, mixer routes, etc. — always AMCP (or HTTP batching to AMCP).</li>\n<li><strong>One-shot</strong> diagnostics: sometimes a single <code>INFO</code> or <code>VERSION</code> is simpler than inferring from OSC.</li>\n<li><strong>No UDP path</strong>: networks that block UDP may require polling-only operation (not ideal for VU).</li>\n</ul>\n<hr>\n<h2 id=\"configuration-pointers\">Configuration pointers</h2>\n<ul>\n<li>OSC listener: <code>config/osc</code> + env in <a href=\"#/osc-integration#highascg-configuration\" data-wiki-page=\"1\">osc-integration.md</a>.</li>\n<li>Polling intervals: app settings + <code>HIGHASCG_PERIODIC_SYNC_SEC</code> / <code>HIGHASCG_PERIODIC_SYNC_OSC_SEC</code>.</li>\n<li>OSC + AMCP <code>INFO</code> supplement: <code>osc_info_supplement_ms</code> / <code>HIGHASCG_OSC_INFO_MS</code> (see section above).</li>\n</ul>","reference--private-volume-sync":"<h1 id=\"private-volume-sync-machine-secrets-on-usb-bridge\">Private volume sync (machine secrets on USB / bridge)</h1>\n<p>Per-machine folder for <strong>Tailscale</strong>, <strong>Syncthing</strong>, and <strong>replication pairing</strong> — kept off the public <code>configs/</code> and <code>projects/</code> sync paths.</p>\n<h2 id=\"layout\">Layout</h2>\n<table><thead><tr><th>Location</th><th>Path</th></tr></thead><tbody>\n<tr><td>Host</td><td><code>~/highascg/.private/&lt;machine-id&gt;/</code></td></tr>\n<tr><td>USB stick</td><td><code>/home/casparcg/exfat/.private/&lt;machine-id&gt;/</code></td></tr>\n<tr><td>Bridge disk</td><td><code>/home/casparcg/bridge/.private/&lt;machine-id&gt;/</code></td></tr>\n</tbody></table>\n<p><code>&lt;machine-id&gt;</code> resolves from <code>replication.selfId</code>, then <code>general.machineId</code>, then hostname.</p>\n<h3 id=\"contents-exported-on-sync\">Contents (exported on sync)</h3>\n<pre><code>.private/&lt;machine-id&gt;/\n  README.txt\n  syncthing/device-id.txt\n  syncthing/folders.json\n  tailscale/status.json\n  replication/pairing.json</code></pre>\n<p>The stick <code>pairing.json</code> omits <code>peer.token</code>. Tokens stay in <code>config/replication.json</code> on each host.</p>\n<h2 id=\"boot-sync\">Boot / sync</h2>\n<p>Runs after exFAT boot sync (<code>highascg-exfat-sync.service</code>). Manual:</p>\n<pre><code class=\"language-bash\">node tools/runtime/exfat-sync-cli.js --private</code></pre>\n<h2 id=\"api\">API</h2>\n<ul>\n<li><code>GET /api/system/private-sync</code></li>\n<li><code>POST /api/system/private-sync/run</code> with <code>confirm: PRIVATE_SYNC</code></li>\n</ul>\n<h2 id=\"setup\">Setup</h2>\n<pre><code class=\"language-bash\">sudo bash scripts/setup/14-private-volume-bootstrap.sh</code></pre>\n<p>See also <a href=\"#/reference--hot-backup-replication\" data-wiki-page=\"1\">hot-backup-replication.md</a>.</p>","reference--README":"<h1 id=\"reference-documentation\">Reference documentation</h1>\n<p>Technical deep dives for integrators and developers. Operators should start at <a href=\"#/README\" data-wiki-page=\"1\">../README.md</a>.</p>\n<table><thead><tr><th>Area</th><th>Files</th></tr></thead><tbody>\n<tr><td>Protocols</td><td><a href=\"#/reference--amcp-mapping\" data-wiki-page=\"1\">amcp-mapping.md</a>, <a href=\"#/reference--amcp-migration\" data-wiki-page=\"1\">amcp-migration.md</a>, <a href=\"#/reference--amcp-migration-qa-checklist\" data-wiki-page=\"1\">amcp-migration-qa-checklist.md</a>, <a href=\"#/reference--polling-vs-osc\" data-wiki-page=\"1\">polling-vs-osc.md</a>, <a href=\"#/reference--PIXELHUE_API\" data-wiki-page=\"1\">PIXELHUE_API.md</a></td></tr>\n<tr><td>PIP overlays</td><td><a href=\"#/reference--pip-overlay-outside-border\" data-wiki-page=\"1\">pip-overlay-outside-border.md</a> — outside border / glow placement (do not regress)</td></tr>\n<tr><td>GPU / displays</td><td><a href=\"#/reference--GPU_SCREEN_CONSUMER_AND_XRANDR\" data-wiki-page=\"1\">GPU_SCREEN_CONSUMER_AND_XRANDR.md</a>, <a href=\"#/reference--xrandr-gpu-screen-mapping\" data-wiki-page=\"1\">xrandr-gpu-screen-mapping.md</a>, <a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">screen-consumer-vsync-nvidia.md</a></td></tr>\n<tr><td>Operator CEF bridge (removed)</td><td><a href=\"#/reference--cef-interactive-bridge\" data-wiki-page=\"1\">cef-interactive-bridge.md</a> — WO-257 removed the input-forwarding bridge; webpage-host routing stays</td></tr>\n<tr><td>Audio (detailed)</td><td><a href=\"../reference/audio\">audio/</a> — ALSA, PipeWire, JACK, Caspar consumers</td></tr>\n<tr><td>Vendor PDFs</td><td><a href=\"../reference/vendor\">vendor/</a> — PixelFlow / switcher manuals</td></tr>\n</tbody></table>\n<p>Operator audio entry point: <a href=\"#/guides--audio--audio-setup-guide\" data-wiki-page=\"1\">../guides/audio/audio-setup-guide.md</a>.</p>","reference--config_from_server":"<h1 id=\"sample-casparcgconfig-from-production-custom-resolution\">Sample <code>casparcg.config</code> from production (custom resolution)</h1>\n<p><strong>NVIDIA screen consumer vsync:</strong> use <strong>Sync to VBlank off</strong> in NVIDIA Settings and <strong><code>&lt;vsync&gt;true&lt;/vsync&gt;</code></strong> on the Caspar screen consumer. See <a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">screen-consumer-vsync-nvidia.md</a>.</p>\n<p><strong>Custom mode <code>3072x1728</code> @ 60 fps:</strong> <code>&lt;time-scale&gt;60000&lt;/time-scale&gt;</code> = <code>fps × 1000</code>; <code>&lt;duration&gt;1000&lt;/duration&gt;</code> is always 1000; <code>&lt;cadence&gt;800&lt;/cadence&gt;</code> = <code>48000 / fps</code>. At 50 fps: <code>time-scale</code> 50000, <code>cadence</code> 960, <code>duration</code> still 1000.</p>\n<h2 id=\"decklink-inputs-one-virtual-screen-many-layers\">Decklink inputs: one virtual screen, many layers</h2>\n<p>All Decklink <strong>inputs</strong> should share <strong>a single channel</strong> (one resolution / one “virtual screen”), not one channel per input. Each physical input is brought in as a <strong>Decklink producer on its own layer</strong> on that channel (e.g. layer 1 = SDI 1, layer 2 = SDI 2, …). The mixer then treats them like any other layers: they can be positioned, keyed, and <strong>routed</strong> elsewhere—other channels (e.g. program/preview), NDI consumers, multiview cells, or downstream processing—without duplicating full-screen channels per input.</p>\n<ul>\n<li><strong>Why one channel:</strong> One <code>video-mode</code> and one raster; predictable memory and timing; routing and composition stay explicit (layers), not implicit (parallel full-frame channels).</li>\n<li><strong>Global <code>&lt;decklink/&gt;</code>:</strong> In the sample below, the empty block enables Decklink support with defaults; per-input <strong>device</strong> and mode are chosen when loading the <strong>producer</strong> on each layer (AMCP / playout), not by multiplying <code>&lt;channel&gt;</code> entries for each SDI line.</li>\n</ul>\n<hr>\n<pre><code class=\"language-text\">casparcg@serwer:/tmp$ cat /opt/casparcg/media/casparcg.config.ftd\n&lt;configuration&gt;\n    &lt;paths&gt;\n        &lt;media-path&gt;media/&lt;/media-path&gt;\n        &lt;log-path disable=&quot;false&quot;&gt;log/&lt;/log-path&gt;\n        &lt;data-path&gt;media/&lt;/data-path&gt;\n        &lt;template-path&gt;template/&lt;/template-path&gt;\n    &lt;/paths&gt;\n    &lt;lock-clear-phrase&gt;secret&lt;/lock-clear-phrase&gt;\n    &lt;channels&gt;\n        &lt;channel&gt;\n            &lt;video-mode&gt;3072x1728&lt;/video-mode&gt;\n            &lt;consumers&gt;\n                &lt;screen&gt;\n                    &lt;device&gt;1&lt;/device&gt;\n                    &lt;x&gt;0&lt;/x&gt;&lt;y&gt;0&lt;/y&gt;\n                    &lt;width&gt;3072&lt;/width&gt;&lt;height&gt;1728&lt;/height&gt;\n                    &lt;stretch&gt;none&lt;/stretch&gt;\n                    &lt;windowed&gt;true&lt;/windowed&gt;\n                    &lt;vsync&gt;false&lt;/vsync&gt;\n                    &lt;always-on-top&gt;true&lt;/always-on-top&gt;\n                    &lt;borderless&gt;true&lt;/borderless&gt;\n                &lt;/screen&gt;\n            &lt;/consumers&gt;\n        &lt;/channel&gt;\n        &lt;channel&gt;\n            &lt;video-mode&gt;3072x1728&lt;/video-mode&gt;\n            &lt;consumers/&gt;\n        &lt;/channel&gt;\n        &lt;channel&gt;\n            &lt;video-mode&gt;1080p6000&lt;/video-mode&gt;\n            &lt;consumers&gt;\n                &lt;screen&gt;\n                    &lt;device&gt;2&lt;/device&gt;\n                    &lt;x&gt;3840&lt;/x&gt;&lt;y&gt;0&lt;/y&gt;\n                    &lt;width&gt;1920&lt;/width&gt;&lt;height&gt;1080&lt;/height&gt;\n                    &lt;stretch&gt;none&lt;/stretch&gt;\n                    &lt;windowed&gt;true&lt;/windowed&gt;\n                    &lt;vsync&gt;true&lt;/vsync&gt;\n                    &lt;borderless&gt;false&lt;/borderless&gt;\n                &lt;/screen&gt;\n            &lt;/consumers&gt;\n        &lt;/channel&gt;\n    &lt;/channels&gt;\n    &lt;video-modes&gt;\n        &lt;video-mode&gt;\n            &lt;id&gt;3072x1728&lt;/id&gt;\n            &lt;width&gt;3072&lt;/width&gt;\n            &lt;height&gt;1728&lt;/height&gt;\n            &lt;time-scale&gt;60000&lt;/time-scale&gt;\n            &lt;duration&gt;1000&lt;/duration&gt;\n            &lt;cadence&gt;800&lt;/cadence&gt;\n        &lt;/video-mode&gt;\n    &lt;/video-modes&gt;\n    &lt;controllers&gt;&lt;tcp&gt;&lt;port&gt;5250&lt;/port&gt;&lt;protocol&gt;AMCP&lt;/protocol&gt;&lt;/tcp&gt;\n&lt;/controllers&gt;   \n &lt;osc&gt;\n  &lt;default-port&gt;6250&lt;/default-port&gt;\n  &lt;disable-send-to-amcp-clients&gt;false&lt;/disable-send-to-amcp-clients&gt;\n  &lt;predefined-clients&gt;\n    &lt;predefined-client&gt;\n      &lt;address&gt;127.0.0.1&lt;/address&gt;\n      &lt;port&gt;6251&lt;/port&gt;\n    &lt;/predefined-client&gt;\n  &lt;/predefined-clients&gt;\n&lt;/osc&gt;\n    &lt;amcp&gt;&lt;media-server&gt;&lt;host&gt;localhost&lt;/host&gt;&lt;port&gt;8000&lt;/port&gt;&lt;/media-server&gt;&lt;/amcp&gt;\n    &lt;ndi&gt;&lt;auto-load&gt;false&lt;/auto-load&gt;&lt;/ndi&gt;\n    &lt;decklink/&gt;\n    &lt;html&gt;&lt;enable-gpu&gt;true&lt;/enable-gpu&gt;&lt;/html&gt;\n&lt;/configuration&gt;</code></pre>","reference--screen-consumer-vsync-nvidia":"<h1 id=\"screen-consumer-vsync-nvidia\">Screen consumer vsync (NVIDIA)</h1>\n<p>Normative setup for <strong>smooth, tear-free</strong> output on CasparCG <strong>screen</strong> consumers when the playout GPU is <strong>NVIDIA</strong>.</p>\n<h2 id=\"required-combination\">Required combination</h2>\n<table><thead><tr><th>Layer</th><th>Setting</th><th>Value</th></tr></thead><tbody>\n<tr><td><strong>NVIDIA</strong> (<code>nvidia-settings</code>)</td><td><strong>Sync to VBlank</strong> (<code>SyncToVBlank</code> on GPU/screen)</td><td><strong>Off</strong></td></tr>\n<tr><td><strong>NVIDIA</strong> (<code>nvidia-settings</code> → X Server Display Configuration → Advanced, per output)</td><td><strong>Force composition pipeline</strong></td><td><strong>On</strong> (all connected screens)</td></tr>\n<tr><td><strong>Caspar screen consumer</strong> (<code>&lt;screen&gt;</code> / HighAsCG <code>screen_N_vsync</code>)</td><td><strong>V-sync</strong></td><td><strong>On</strong> (<code>true</code>)</td></tr>\n</tbody></table>\n<p>Do <strong>all three</strong>. Using only one side often causes tearing, stutter, or frame pacing that looks like “bad vsync.”</p>\n<h2 id=\"why\">Why</h2>\n<ul>\n<li><strong>Driver sync-to-vblank</strong> forces OpenGL swaps to the display refresh at the <strong>driver</strong> level for all GL clients on that GPU.</li>\n<li>Caspar’s <strong>screen consumer</strong> has its own <strong>vsync</strong> when presenting the playout window.</li>\n<li>With NVIDIA <strong>Sync to VBlank on</strong>, the driver and the consumer can <strong>double-sync</strong> or fight each other → uneven frame times.</li>\n<li>With NVIDIA <strong>Sync to VBlank off</strong>, <strong>Force Composition Pipeline on</strong> (all outputs), and Caspar <strong>vsync on</strong>, presentation is gated correctly for playout — verified on multi-head DP/HDMI + DeckLink setups.</li>\n</ul>\n<h2 id=\"how-to-apply\">How to apply</h2>\n<h3 id=\"nvidia-os-x-session\">NVIDIA (OS / X session)</h3>\n<ol>\n<li>Open <strong><code>nvidia-settings</code></strong> (HighAsCG: <strong>Settings → System → Open NVIDIA Settings</strong>, or <code>DISPLAY=:0 nvidia-settings</code>).</li>\n<li><strong>X Server Display Configuration</strong> → <strong>Advanced</strong> (per output): enable <strong>Force composition pipeline</strong> on <strong>every</strong> connected screen (not “Force full composition pipeline”).</li>\n<li>Under the GPU (and screen section if shown), set <strong>Sync to VBlank</strong> to <strong>disabled / off</strong>.</li>\n<li>Confirm with:</li>\n</ol>\n<pre><code class=\"language-bash\">   nvidia-settings -q &quot;[gpu:0]/SyncToVBlank&quot;\n   nvidia-settings -q CurrentMetaMode -t | grep ForceCompositionPipeline</code></pre>\n<p>(<code>SyncToVBlank</code> <strong>0</strong> = off; each MetaMode block should include <code>ForceCompositionPipeline=On</code>)</p>\n<p>HighAsCG production install also sets:</p>\n<ul>\n<li>Environment: <strong><code>__GL_SYNC_TO_VBLANK=0</code></strong> in the X session / autostart chain.</li>\n<li>Script: <strong><code>highascg-nvidia-x-apply.sh</code></strong> (PowerMizer max, <code>SyncToVBlank=0</code>, <strong>Force Composition Pipeline on all outputs</strong>), installed by <code>scripts/setup/09-openbox-autostart.sh</code> from <code>tools/runtime/highascg-nvidia-x-apply.sh</code>. Node bridge resolves the same script via <code>src/utils/nvidia-display-policy.js</code> after every layout apply and before opening <strong>nvidia-settings</strong> from Settings.</li>\n</ul>\n<p>Re-run after layout apply, <strong>nodm restart</strong>, driver upgrades, or if tearing returns:</p>\n<p><strong>Important:</strong> <code>xrandr</code> / layout apply resets NVIDIA <strong>CurrentMetaMode</strong> and clears per-output <strong>Force Composition Pipeline</strong> in MetaMode. HighAsCG runs <code>highascg-nvidia-x-apply.sh</code> <strong>after</strong> every <code>apply-layout.sh</code> (openbox autostart + apply-layout tail), with retries at 6s and 18s.</p>\n<p><strong>Persistent defaults (no polling watchdog):</strong></p>\n<ol>\n<li><strong>Xorg</strong> — <code>scripts/setup/09-openbox-autostart.sh</code> installs <code>/etc/X11/xorg.conf.d/99-highascg-force-composition.conf</code> with <code>Option &quot;ForceCompositionPipeline&quot; &quot;On&quot;</code> on the NVIDIA device (driver default at session start).</li>\n<li><strong>nvidia-settings rc</strong> — after each apply, <code>highascg-nvidia-x-apply.sh</code> runs <code>nvidia-settings --save</code> so <code>~/.nvidia-settings-rc</code> includes the patched <code>CurrentMetaMode</code>.</li>\n<li><strong>Runtime</strong> — any <code>xrandr</code> layout change must still be followed by <code>highascg-nvidia-x-apply.sh</code> (wired into <code>apply-layout.sh</code> and full config apply).</li>\n</ol>\n<p>Permanent on a production box:</p>\n<ol>\n<li>Install script + openbox chain: <code>sudo bash scripts/setup/09-openbox-autostart.sh</code> (from the HighAsCG repo).</li>\n<li>Ensure <code>~/.config/highascg/apply-layout.sh</code> exists (run <strong>Apply</strong> once from the UI, or <code>POST /api/caspar-config/apply</code>).</li>\n<li>After manual <code>sudo systemctl restart nodm</code>, composition pipeline is re-applied automatically (layout script + delayed retries).</li>\n</ol>\n<p>Manual check / one-shot fix:</p>\n<pre><code class=\"language-bash\">DISPLAY=:0 XAUTHORITY=/home/casparcg/.Xauthority /usr/local/bin/highascg-nvidia-x-apply.sh</code></pre>\n<h3 id=\"caspar-screen-consumer\">Caspar (screen consumer)</h3>\n<p>In generated <strong><code>casparcg.config</code></strong>:</p>\n<pre><code class=\"language-xml\">&lt;screen&gt;\n    …\n    &lt;vsync&gt;true&lt;/vsync&gt;\n&lt;/screen&gt;</code></pre>\n<p>In HighAsCG settings (<code>casparServer</code> / Device View), keep <strong><code>screen_N_vsync</code>: true</strong> (defaults in <code>src/config/defaults-caspar-server.js</code>). <strong>Write &amp; restart</strong> Caspar after changing consumer flags.</p>\n<p><strong>Preview</strong> outputs may use a separate policy (<code>preview_screen_consumer</code>); program <strong>PGM/PRV screen consumers</strong> on NVIDIA should follow the table above.</p>\n<h2 id=\"verification\">Verification</h2>\n<ol>\n<li>NVIDIA: Sync to VBlank <strong>off</strong>; CurrentMetaMode shows <strong>ForceCompositionPipeline=On</strong> on each output.</li>\n<li>Caspar config: <code>&lt;vsync&gt;true&lt;/vsync&gt;</code> on the program screen consumer(s).</li>\n<li>Observe output: no torn frames at stable refresh; frame drops show in Caspar log rather than horizontal tears.</li>\n</ol>\n<h2 id=\"related-docs\">Related docs</h2>\n<ul>\n<li><a href=\"#/reference--GPU_SCREEN_CONSUMER_AND_XRANDR\" data-wiki-page=\"1\">GPU_SCREEN_CONSUMER_AND_XRANDR.md</a> — destinations, xrandr, consumer geometry</li>\n<li><a href=\"#/reference--xrandr-gpu-screen-mapping\" data-wiki-page=\"1\">xrandr-gpu-screen-mapping.md</a> — Apply GPU / OS layout</li>\n<li><a href=\"#/caspar_config_explained\" data-wiki-page=\"1\">caspar_config_explained.md</a> — <code>&lt;screen&gt;</code> consumer XML</li>\n<li><a href=\"#/ISO_CONTENTS\" data-wiki-page=\"1\">ISO_CONTENTS.md</a> — baked NVIDIA X session defaults on live ISO</li>\n</ul>\n<p><em>Last updated: 2026-06-24</em></p>","reference--sudo_and_audio_setup":"<h1 id=\"sudo-setup-for-casparcg-highascg\">Sudo Setup for CasparCG / HighAsCG</h1>\n<p><strong>Avoid <code>NOPASSWD: ALL</code> for <code>casparcg</code>.</strong> Use narrow <strong><code>/etc/sudoers.d/</code></strong> fragments (see <strong><code>docs/HIGHASCG_PASSWORDLESS_SUDO.md</code></strong>): media mount helper, optional nuclear actions, etc.</p>\n<p><strong>ALSA / <code>/etc/asound.conf</code>:</strong> HighAsCG <strong>does not install</strong> passwordless <code>tee</code> → <code>/etc/asound.conf</code> by default. <strong>PortAudio + device names</strong> and per-user <strong><code>~/.asoundrc</code></strong> cover normal playout without sudo. Enable system-wide ALSA only if you truly need it: <strong><code>HIGHASCG_INSTALL_ASOUND_SUDOERS=1</code></strong> during <strong><code>scripts/install.sh</code></strong> (see <strong><code>install-phase3.sh</code></strong>).</p>\n<p>Historical note: older docs sometimes suggested broad <code>tee</code> or <code>ALL</code> rules; prefer the inventory doc above.</p>\n<hr>\n<h2 id=\"legacy-overly-broad-not-recommended\">Legacy (overly broad — not recommended)</h2>\n<p>The following is <strong>not</strong> the reference layout; kept for context only.</p>\n<h3 id=\"1-open-the-sudoers-file\">1. Open the Sudoers File</h3>\n<p>Run the following command as a root-level user:</p>\n<pre><code class=\"language-bash\">sudo visudo</code></pre>\n<h3 id=\"2-add-permission-for-the-casparcg-user\">2. Add Permission for the <code>casparcg</code> User</h3>\n<p>Append the following line to the end of the file:</p>\n<pre><code class=\"language-sudoers\">casparcg ALL=(ALL) NOPASSWD: ALL</code></pre>\n<blockquote><p>[!NOTE] If you prefer to limit the scope for better security, you can specify only the necessary commands (like <code>tee</code> for config writing): <code>casparcg ALL=(ALL) NOPASSWD: /usr/bin/tee, /bin/tee</code></p></blockquote>\n<h3 id=\"3-verify\">3. Verify</h3>\n<p>Switch to the <code>casparcg</code> user and test a sudo command:</p>\n<pre><code class=\"language-bash\">sudo -u casparcg sudo -n true &amp;&amp; echo &quot;Success: Passwordless sudo is active&quot;</code></pre>\n<hr>\n<h2 id=\"audio-configuration-casparcg-enhanced\">Audio Configuration (CasparCG Enhanced)</h2>\n<p>With PortAudio, you should typically use the <strong>custom_live</strong> profile in HighAsCG. This profile enables:</p>\n<ul>\n<li><code>&lt;portaudio&gt;</code> consumers inside <code>&lt;channels&gt;</code>.</li>\n<li>Direct hardware device selection for DP/HDMI outputs.</li>\n<li>Low-latency monitoring.</li>\n</ul>\n<h3 id=\"enabling-portaudio-outputs-on-gpu\">Enabling PortAudio Outputs on GPU</h3>\n<ol>\n<li>In the <strong>Device View</strong>, click on a <strong>GPU (DP/HDMI)</strong> connector.</li>\n<li>In the inspector, ensure <strong>PortAudio</strong> is enabled.</li>\n<li>Select your hardware device from the list (e.g., <code>NVidia HDMI 0</code>).</li>\n<li>Set the <strong>Channel Layout</strong> to <code>8ch</code> for surround/multi-channel support.</li>\n</ol>","reference--tailscale-integration":"<h1 id=\"tailscale-integration-wo-91\">Tailscale integration (WO-91)</h1>\n<p>HighAsCG controls Tailscale from <strong>Settings → Tailscale</strong> and exposes HTTP APIs for status, login, and operator-monitor browser assist.</p>\n<h2 id=\"config\">Config</h2>\n<p>File: <code>config/tailscale.json</code></p>\n<table><thead><tr><th>Key</th><th>Default</th><th>Meaning</th></tr></thead><tbody>\n<tr><td><code>enabled</code></td><td><code>true</code></td><td>Start/stop <code>tailscaled</code> via systemd</td></tr>\n<tr><td><code>autoLoginOnBoot</code></td><td><code>false</code></td><td>On <code>highascg</code> start, call login when node needs auth</td></tr>\n<tr><td><code>hostname</code></td><td><code>&quot;&quot;</code></td><td>Optional <code>--hostname</code> for <code>tailscale up</code></td></tr>\n<tr><td><code>acceptRoutes</code></td><td><code>false</code></td><td>Pass <code>--accept-routes</code> on login</td></tr>\n<tr><td><code>operatorLoginAssist</code></td><td><code>true</code></td><td>Spawn Firefox on <code>DISPLAY=:0</code> with auth URL when logging in</td></tr>\n</tbody></table>\n<h2 id=\"api\">API</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Notes</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/network/tailscale/status</code></td><td>Full status + config</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/enable</code></td><td><code>{ enabled: boolean }</code></td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/login</code></td><td>Start interactive login</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/login-operator-ui</code></td><td>Login + open auth URL on operator monitor</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/prefs</code></td><td>Save config keys above</td></tr>\n<tr><td>POST</td><td><code>/api/network/tailscale/logout</code></td><td><code>tailscale logout</code></td></tr>\n</tbody></table>\n<p><code>GET /api/system/setup</code> includes a compact <code>tailscale</code> block from the same service.</p>\n<p>Privileged POST routes use the <strong>nuclear password</strong> gate when <code>ui.nuclearRequirePassword</code> is enabled.</p>\n<h2 id=\"operator-monitor-login\">Operator monitor login</h2>\n<ol>\n<li>Settings → <strong>Log in</strong> or <strong>Open on operator monitor</strong></li>\n<li>Server calls Tailscale Local API <code>login-interactive</code> or <code>sudo tailscale up</code></li>\n<li>Auth URL must match <code>https://login.tailscale.com/…</code> (server-generated only)</li>\n<li>Firefox launches via <code>highascg-launch-operator-firefox.sh &lt;url&gt;</code> on <code>:0</code></li>\n</ol>\n<p>Requires nodm/X session on the operator head (WO-87 pointer confine compatible).</p>\n<h2 id=\"sudo-passwordless\">Sudo (passwordless)</h2>\n<p>The <code>casparcg</code> user needs non-interactive sudo for:</p>\n<ul>\n<li><code>systemctl start|stop|enable --now tailscaled</code></li>\n<li><code>systemctl start|stop snap.tailscale.tailscaled.service</code></li>\n<li><code>tailscale up</code>, <code>tailscale logout</code> (deb or snap path)</li>\n</ul>\n<p>See <code>docs/HIGHASCG_PASSWORDLESS_SUDO.md</code>.</p>\n<h2 id=\"boot-hook\">Boot hook</h2>\n<p>~4s after <code>highascg</code> starts, <code>maybeAutoLoginOnBoot()</code> runs when <code>autoLoginOnBoot</code> is true.</p>\n<h2 id=\"related\">Related</h2>\n<ul>\n<li><a href=\"../../work/work-orders/91_WO_TAILSCALE_SETTINGS_AND_OPERATOR_UI.md\">WO-91</a></li>\n<li><a href=\"../../work/work-orders/61_WO_RSYNC_PEER_SYNC_AND_NETWORK_SETTINGS.md\">WO-61</a> — broader network sync (Syncthing/rsync)</li>\n</ul>","reference--web-browser-and-server-logs":"<p>[2026-04-05 09:07:18.294] [info]    async_event_server[:5250] Client 127.0.0.1 disconnected (0 connections). [2026-04-05 09:07:18.510] [info]    async_event_server[:5250] Accepted connection from 127.0.0.1 (1 connections). [2026-04-05 09:07:18.521] [info]    Received message from 127.0.0.1: VERSION\\r\\n [2026-04-05 09:07:18.521] [info]    Sent message to 127.0.0.1:201 VERSION OK\\r\\n2.5.0 N/A Stable\\r\\n [2026-04-05 09:07:21.022] [info]    Received message from 127.0.0.1: REMOVE 1 STREAM udp://127.0.0.1:10001\\r\\n [2026-04-05 09:07:21.023] [info]    ffmpeg[udp://127.0.0.1:10001] Uninitialized. [2026-04-05 09:07:21.023] [info]    Sent message to 127.0.0.1:202 REMOVE OK\\r\\n [2026-04-05 09:07:21.026] [info]    [ffmpeg] [aac @ 0x712a965fef00] Qavg: 65536.000 [2026-04-05 09:07:21.026] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] frame I:311   Avg QP:25.36  size: 59541 [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] frame P:15213 Avg QP:28.19  size:  9155 [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] mb I  I16..4: 100.0%  0.0%  0.0% [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] mb P  I16..4:  0.2%  0.0%  0.0%  P16..4: 18.9%  0.0%  0.0%  0.0%  0.0%    skip:80.8% [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] final ratefactor: 30.44 [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] coded y,uvDC,uvAC intra: 55.6% 57.7% 46.1% inter: 12.1% 8.2% 5.6% [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] i16 v,h,dc,p: 49% 25% 23%  2% [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] i8c dc,h,v,p: 47% 28% 24%  2% [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.028] [info]    [ffmpeg] [libx264 @ 0x712a951c9200] kb/s:2032.88 [2026-04-05 09:07:21.028] [info] [2026-04-05 09:07:21.032] [info]    ffmpeg[udp://127.0.0.1:10001] Uninitialized. [2026-04-05 09:07:21.174] [info]    Received message from 127.0.0.1: ADD 1 STREAM udp://127.0.0.1:10001 -filter:v scale=960:540,format=yuv420p,fps=25 -codec:v libx264 -preset:v ultrafast -tune:v zerolatency -b:v 2000k -g:v 50 -x264-params:v min-keyint=25:scenecut=0:repeat-headers=1 -filter:a aformat=channel_layouts=stereo,aresample=48000 -codec:a aac -b:a 128k -format mpegts\\r\\n [2026-04-05 09:07:21.175] [info]    ffmpeg[udp://127.0.0.1:10001] Initialized. [2026-04-05 09:07:21.175] [info]    Sent message to 127.0.0.1:202 ADD OK\\r\\n [2026-04-05 09:07:21.208] [info]    [ffmpeg] [libx264 @ 0x7129f51c9500] using SAR=1/1 [2026-04-05 09:07:21.208] [info] [2026-04-05 09:07:21.210] [info]    [ffmpeg] [libx264 @ 0x7129f51c9500] using cpu capabilities: MMX2 SSE2Fast SSSE3 SSE4.2 AVX FMA3 BMI2 AVX2 [2026-04-05 09:07:21.210] [info] [2026-04-05 09:07:21.212] [info]    [ffmpeg] [libx264 @ 0x7129f51c9500] profile Constrained Baseline, level 3.1, 4:2:0, 8-bit [2026-04-05 09:07:21.212] [info] [2026-04-05 09:07:21.276] [info]    Received message from 127.0.0.1: REMOVE 2 STREAM udp://127.0.0.1:10002\\r\\n [2026-04-05 09:07:21.277] [info]    ffmpeg[udp://127.0.0.1:10002] Uninitialized. [2026-04-05 09:07:21.277] [info]    Sent message to 127.0.0.1:202 REMOVE OK\\r\\n [2026-04-05 09:07:21.291] [info]    [ffmpeg] [aac @ 0x712a9e5ff080] Qavg: 65536.000 [2026-04-05 09:07:21.291] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] frame I:311   Avg QP:26.64  size: 59918 [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] frame P:15213 Avg QP:29.64  size:  9078 [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] mb I  I16..4: 100.0%  0.0%  0.0% [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] mb P  I16..4:  0.3%  0.0%  0.0%  P16..4: 20.1%  0.0%  0.0%  0.0%  0.0%    skip:79.7% [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] final ratefactor: 31.02 [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] coded y,uvDC,uvAC intra: 57.5% 59.9% 47.9% inter: 13.0% 8.7% 5.9% [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] i16 v,h,dc,p: 48% 26% 23%  2% [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] i8c dc,h,v,p: 49% 24% 24%  2% [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.292] [info]    [ffmpeg] [libx264 @ 0x712a9d1c9bc0] kb/s:2019.27 [2026-04-05 09:07:21.292] [info] [2026-04-05 09:07:21.294] [info]    ffmpeg[udp://127.0.0.1:10002] Uninitialized. [2026-04-05 09:07:21.429] [info]    Received message from 127.0.0.1: ADD 2 STREAM udp://127.0.0.1:10002 -filter:v scale=960:540,format=yuv420p,fps=25 -codec:v libx264 -preset:v ultrafast -tune:v zerolatency -b:v 2000k -g:v 50 -x264-params:v min-keyint=25:scenecut=0:repeat-headers=1 -filter:a aformat=channel_layouts=stereo,aresample=48000 -codec:a aac -b:a 128k -format mpegts\\r\\n [2026-04-05 09:07:21.429] [info]    ffmpeg[udp://127.0.0.1:10002] Initialized. [2026-04-05 09:07:21.429] [info]    Sent message to 127.0.0.1:202 ADD OK\\r\\n [2026-04-05 09:07:21.446] [info]    [ffmpeg] [libx264 @ 0x712ba159c840] using SAR=1/1 [2026-04-05 09:07:21.446] [info] [2026-04-05 09:07:21.450] [info]    [ffmpeg] [libx264 @ 0x712ba159c840] using cpu capabilities: MMX2 SSE2Fast SSSE3 SSE4.2 AVX FMA3 BMI2 AVX2 [2026-04-05 09:07:21.450] [info] [2026-04-05 09:07:21.451] [info]    [ffmpeg] [libx264 @ 0x712ba159c840] profile Constrained Baseline, level 3.1, 4:2:0, 8-bit [2026-04-05 09:07:21.451] [info] [2026-04-05 09:07:21.531] [info]    Received message from 127.0.0.1: REMOVE 3 STREAM udp://127.0.0.1:10005\\r\\n [2026-04-05 09:07:21.531] [info]    ffmpeg[udp://127.0.0.1:10005] Uninitialized. [2026-04-05 09:07:21.531] [info]    Sent message to 127.0.0.1:202 REMOVE OK\\r\\n [2026-04-05 09:07:21.541] [info]    [ffmpeg] [aac @ 0x712af1dfed40] Qavg: 65536.000 [2026-04-05 09:07:21.541] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] frame I:311   Avg QP:15.14  size: 63495 [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] frame P:15213 Avg QP:17.75  size:  9463 [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] mb I  I16..4: 100.0%  0.0%  0.0% [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] mb P  I16..4:  0.0%  0.0%  0.0%  P16..4:  8.6%  0.0%  0.0%  0.0%  0.0%    skip:91.4% [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] final ratefactor: 24.43 [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] coded y,uvDC,uvAC intra: 26.6% 27.1% 26.4% inter: 6.6% 5.1% 4.3% [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] i16 v,h,dc,p: 61% 16% 21%  1% [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] i8c dc,h,v,p: 71% 17% 11%  1% [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.542] [info]    [ffmpeg] [libx264 @ 0x712af094f300] kb/s:2109.03 [2026-04-05 09:07:21.542] [info] [2026-04-05 09:07:21.543] [info]    ffmpeg[udp://127.0.0.1:10005] Uninitialized. [2026-04-05 09:07:21.684] [info]    Received message from 127.0.0.1: ADD 3 STREAM udp://127.0.0.1:10005 -filter:v scale=960:540,format=yuv420p,fps=25 -codec:v libx264 -preset:v ultrafast -tune:v zerolatency -b:v 2000k -g:v 50 -x264-params:v min-keyint=25:scenecut=0:repeat-headers=1 -filter:a aformat=channel_layouts=stereo,aresample=48000 -codec:a aac -b:a 128k -format mpegts\\r\\n [2026-04-05 09:07:21.685] [info]    ffmpeg[udp://127.0.0.1:10005] Initialized. [2026-04-05 09:07:21.685] [info]    Sent message to 127.0.0.1:202 ADD OK\\r\\n [2026-04-05 09:07:21.698] [info]    [ffmpeg] [libx264 @ 0x712ae9d16dc0] using SAR=1/1 [2026-04-05 09:07:21.698] [info] [2026-04-05 09:07:21.702] [info]    [ffmpeg] [libx264 @ 0x712ae9d16dc0] using cpu capabilities: MMX2 SSE2Fast SSSE3 SSE4.2 AVX FMA3 BMI2 AVX2 [2026-04-05 09:07:21.702] [info] [2026-04-05 09:07:21.703] [info]    [ffmpeg] [libx264 @ 0x712ae9d16dc0] profile Constrained Baseline, level 3.1, 4:2:0, 8-bit [2026-04-05 09:07:21.703] [info] [2026-04-05 09:07:24.288] [info]    Received message from 127.0.0.1: INFO 1\\r\\n [2026-04-05 09:07:24.288] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:07:24.289] [info]    Received message from 127.0.0.1: INFO 2\\r\\n [2026-04-05 09:07:24.296] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:07:24.298] [info]    Received message from 127.0.0.1: INFO 3\\r\\n [2026-04-05 09:07:24.299] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:07:32.617] [info]    Received message from 127.0.0.1: THUMBNAIL RETRIEVE LED-GRID-3584X1408\\r\\n [2026-04-05 09:07:32.622] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:07:32.623] [info]    Received message from 127.0.0.1: THUMBNAIL RETRIEVE LED-GRID-12288X1280\\r\\n [2026-04-05 09:07:32.632] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:07:43.317] [info]    Received message from 127.0.0.1: CLS\\r\\n [2026-04-05 09:07:43.322] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:07:43.324] [info]    Received message from 127.0.0.1: TLS\\r\\n [2026-04-05 09:07:43.326] [info]    Sent message to 127.0.0.1:200 TLS OK\\r\\nMULTIVIEW_OVERLAY\\r\\n\\r\\n [2026-04-05 09:07:47.272] [info]    Received message from 127.0.0.1: THUMBNAIL RETRIEVE PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210\\r\\n [2026-04-05 09:07:47.274] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:07:47.641] [info]    Received message from 127.0.0.1: PLAY 2-10 PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210\\r\\n [2026-04-05 09:07:47.644] [info]    Sent message to 127.0.0.1:202 PLAY OK\\r\\n [2026-04-05 09:07:47.650] [info]    Received message from 127.0.0.1: MIXER 2-10 ANCHOR 0 0\\r\\n [2026-04-05 09:07:47.650] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:47.651] [info]    Received message from 127.0.0.1: MIXER 2-10 FILL 0 0 1 1 0\\r\\n [2026-04-05 09:07:47.651] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:47.652] [info]    Received message from 127.0.0.1: MIXER 2-10 ROTATION 0 0\\r\\n [2026-04-05 09:07:47.653] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:47.653] [info]    Received message from 127.0.0.1: MIXER 2-10 OPACITY 1 0\\r\\n [2026-04-05 09:07:47.654] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:47.654] [info]    Received message from 127.0.0.1: MIXER 2-10 KEYER 0\\r\\n [2026-04-05 09:07:47.655] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:47.655] [info]    Received message from 127.0.0.1: MIXER 2 COMMIT\\r\\n [2026-04-05 09:07:47.655] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:47.678] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x712a0820c8c0] No channel layout for input 1 [2026-04-05 09:07:47.678] [warning] [2026-04-05 09:07:47.679] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x712a0820c8c0] Input channel layouts overlap: output layout will be determined by the number of distinct input channels [2026-04-05 09:07:47.679] [warning] [2026-04-05 09:07:47.739] [info]    image_producer[media/led-grid-3584x1408.png] Destroyed. [2026-04-05 09:07:47.739] [warning] ffmpeg[PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210|0.0000/2850.4000] Latency: 6 [2026-04-05 09:07:53.442] [info]    Received message from 127.0.0.1: PLAY 2-10 PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210\\r\\n [2026-04-05 09:07:53.444] [info]    Sent message to 127.0.0.1:202 PLAY OK\\r\\n [2026-04-05 09:07:53.444] [info]    Received message from 127.0.0.1: MIXER 2-10 ANCHOR 0 0\\r\\n [2026-04-05 09:07:53.444] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.444] [info]    Received message from 127.0.0.1: MIXER 2-10 FILL 0 0 1 1 0\\r\\n [2026-04-05 09:07:53.445] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.447] [info]    Received message from 127.0.0.1: MIXER 2-10 ROTATION 0 0\\r\\n [2026-04-05 09:07:53.448] [info]    Received message from 127.0.0.1: MIXER 2-10 OPACITY 1 0\\r\\n [2026-04-05 09:07:53.447] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.448] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.448] [info]    Received message from 127.0.0.1: MIXER 2-10 KEYER 0\\r\\n [2026-04-05 09:07:53.449] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.449] [info]    Received message from 127.0.0.1: MIXER 2 COMMIT\\r\\n [2026-04-05 09:07:53.450] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.462] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x7128ac20bac0] No channel layout for input 1 [2026-04-05 09:07:53.462] [warning] [2026-04-05 09:07:53.462] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x7128ac20bac0] Input channel layouts overlap: output layout will be determined by the number of distinct input channels [2026-04-05 09:07:53.462] [warning] [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: BEGIN\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: STOP 1-110\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: MIXER 1-110 CLEAR\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: PLAY 1-110 PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: MIXER 1-110 ANCHOR 0 0\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: MIXER 1-110 FILL 0 0 1 1 0\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: MIXER 1-110 ROTATION 0 0\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: MIXER 1-110 OPACITY 1 0\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: MIXER 1-110 KEYER 0\\r\\n [2026-04-05 09:07:53.475] [info]    Received message from 127.0.0.1: COMMIT\\r\\n [2026-04-05 09:07:53.475] [warning] Executing batch: BATCH(8 commands) [2026-04-05 09:07:53.475] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.475] [info]    Sent message to 127.0.0.1:202 STOP OK\\r\\n [2026-04-05 09:07:53.478] [info]    Sent message to 127.0.0.1:202 PLAY OK\\r\\n [2026-04-05 09:07:53.478] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.478] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.478] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.479] [info]    image_producer[media/led-grid-3584x1408.png] Destroyed. [2026-04-05 09:07:53.479] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.479] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.480] [info]    Sent message to 127.0.0.1:202 COMMIT OK\\r\\n [2026-04-05 09:07:53.515] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x71287020b700] No channel layout for input 1 [2026-04-05 09:07:53.515] [warning] [2026-04-05 09:07:53.515] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x71287020b700] Input channel layouts overlap: output layout will be determined by the number of distinct input channels [2026-04-05 09:07:53.515] [warning] [2026-04-05 09:07:53.519] [info]    Received message from 127.0.0.1: MIXER 1 COMMIT\\r\\n [2026-04-05 09:07:53.519] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.539] [warning] ffmpeg[PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210|0.0000/2850.4000] Latency: 6 [2026-04-05 09:07:53.539] [info]    ffmpeg[media/PiekloKobiet_S01E01_Master_178_R709_PL-XX_20DFX_HD_25fps_HAP_20260210.mov|346/171024] Destroyed. [2026-04-05 09:07:53.573] [warning] ffmpeg[PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210|0.0000/2850.4000] Latency: 10 [2026-04-05 09:07:53.894] [info]    Received message from 127.0.0.1: PLAY 2-10 PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210\\r\\n [2026-04-05 09:07:53.901] [info]    Sent message to 127.0.0.1:202 PLAY OK\\r\\n [2026-04-05 09:07:53.902] [info]    Received message from 127.0.0.1: MIXER 2-10 ANCHOR 0 0\\r\\n [2026-04-05 09:07:53.905] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.905] [info]    Received message from 127.0.0.1: MIXER 2-10 FILL 0 0 1 1 0\\r\\n [2026-04-05 09:07:53.906] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.909] [info]    Received message from 127.0.0.1: MIXER 2-10 ROTATION 0 0\\r\\n [2026-04-05 09:07:53.909] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.910] [info]    Received message from 127.0.0.1: MIXER 2-10 OPACITY 1 0\\r\\n [2026-04-05 09:07:53.910] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.912] [info]    Received message from 127.0.0.1: MIXER 2-10 KEYER 0\\r\\n [2026-04-05 09:07:53.912] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.915] [info]    Received message from 127.0.0.1: MIXER 2 COMMIT\\r\\n [2026-04-05 09:07:53.916] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:07:53.940] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x712a0c20bac0] No channel layout for input 1 [2026-04-05 09:07:53.940] [warning] [2026-04-05 09:07:53.941] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x712a0c20bac0] Input channel layouts overlap: output layout will be determined by the number of distinct input channels [2026-04-05 09:07:53.941] [warning] [2026-04-05 09:07:54.006] [info]    ffmpeg[media/PiekloKobiet_S01E01_Master_178_R709_PL-XX_20DFX_HD_25fps_HAP_20260210.mov|26/171024] Destroyed. [2026-04-05 09:07:54.006] [warning] ffmpeg[PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210|0.0000/2850.4000] Latency: 7 [2026-04-05 09:07:56.472] [info]    Received message from 127.0.0.1: THUMBNAIL RETRIEVE LED-GRID-12288X1280\\r\\n [2026-04-05 09:07:56.481] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:07:56.493] [info]    Received message from 127.0.0.1: THUMBNAIL RETRIEVE LED-GRID-3584X1408\\r\\n [2026-04-05 09:07:56.509] [info]    Sent more than 512 bytes to 127.0.0.1 [2026-04-05 09:08:03.434] [info]    Received message from 127.0.0.1: PLAY 2-10 LED-GRID-3584X1408\\r\\n [2026-04-05 09:08:03.503] [info]    image_producer[media/led-grid-3584x1408.png] Initialized [2026-04-05 09:08:03.503] [info]    Sent message to 127.0.0.1:202 PLAY OK\\r\\n [2026-04-05 09:08:03.503] [info]    Received message from 127.0.0.1: MIXER 2-10 ANCHOR 0 0\\r\\n [2026-04-05 09:08:03.503] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.503] [info]    Received message from 127.0.0.1: MIXER 2-10 FILL -0.11675047596198496 0.2216948438359934 1 1 0\\r\\n [2026-04-05 09:08:03.504] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.504] [info]    Received message from 127.0.0.1: MIXER 2-10 ROTATION 0 0\\r\\n [2026-04-05 09:08:03.504] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.504] [info]    Received message from 127.0.0.1: MIXER 2-10 OPACITY 1 0\\r\\n [2026-04-05 09:08:03.504] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.504] [info]    Received message from 127.0.0.1: MIXER 2-10 KEYER 0\\r\\n [2026-04-05 09:08:03.504] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.504] [info]    Received message from 127.0.0.1: MIXER 2 COMMIT\\r\\n [2026-04-05 09:08:03.504] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.505] [info]    ffmpeg[media/PiekloKobiet_S01E01_Master_178_R709_PL-XX_20DFX_HD_25fps_HAP_20260210.mov|569/171024] Destroyed. [2026-04-05 09:08:03.532] [info]    Received message from 127.0.0.1: PLAY 2-11 0-POZYTYWNI_CZYSTY_1_1 LOOP\\r\\n [2026-04-05 09:08:03.533] [info]    Sent message to 127.0.0.1:202 PLAY OK\\r\\n [2026-04-05 09:08:03.533] [info]    Received message from 127.0.0.1: MIXER 2-11 ANCHOR 0 0\\r\\n [2026-04-05 09:08:03.533] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.533] [info]    Received message from 127.0.0.1: MIXER 2-11 FILL 0.13420594654400553 0.2547519373278221 0.6996604415248732 0.6996604415248732 0\\r\\n [2026-04-05 09:08:03.533] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.533] [info]    Received message from 127.0.0.1: MIXER 2-11 ROTATION 0 0\\r\\n [2026-04-05 09:08:03.533] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.533] [info]    Received message from 127.0.0.1: MIXER 2-11 OPACITY 1 0\\r\\n [2026-04-05 09:08:03.533] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.533] [info]    Received message from 127.0.0.1: MIXER 2-11 KEYER 0\\r\\n [2026-04-05 09:08:03.533] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.533] [info]    Received message from 127.0.0.1: MIXER 2 COMMIT\\r\\n [2026-04-05 09:08:03.533] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:03.573] [warning] ffmpeg[0-POZYTYWNI_CZYSTY_1_1|0.0000/15.0333] Latency: 2 [2026-04-05 09:08:03.589] [info]    ffmpeg[media/0-Pozytywni_Czysty_1_1.mov|231/902] Destroyed. [2026-04-05 09:08:06.215] [info]    Received message from 127.0.0.1: STOP 2-11\\r\\n [2026-04-05 09:08:06.216] [info]    Sent message to 127.0.0.1:202 STOP OK\\r\\n [2026-04-05 09:08:06.216] [info]    ffmpeg[media/0-Pozytywni_Czysty_1_1.mov|158/902] Destroyed. [2026-04-05 09:08:06.217] [info]    Received message from 127.0.0.1: MIXER 2-11 CLEAR\\r\\n [2026-04-05 09:08:06.218] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:06.243] [info]    Received message from 127.0.0.1: PLAY 2-10 PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210\\r\\n [2026-04-05 09:08:06.246] [info]    Sent message to 127.0.0.1:202 PLAY OK\\r\\n [2026-04-05 09:08:06.247] [info]    Received message from 127.0.0.1: MIXER 2-10 ANCHOR 0 0\\r\\n [2026-04-05 09:08:06.247] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:06.248] [info]    Received message from 127.0.0.1: MIXER 2-10 FILL 0 0 1 1 0\\r\\n [2026-04-05 09:08:06.248] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:06.248] [info]    Received message from 127.0.0.1: MIXER 2-10 ROTATION 0 0\\r\\n [2026-04-05 09:08:06.248] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:06.248] [info]    Received message from 127.0.0.1: MIXER 2-10 OPACITY 1 0\\r\\n [2026-04-05 09:08:06.249] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:06.250] [info]    Received message from 127.0.0.1: MIXER 2-10 KEYER 0\\r\\n [2026-04-05 09:08:06.250] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:06.250] [info]    Received message from 127.0.0.1: MIXER 2 COMMIT\\r\\n [2026-04-05 09:08:06.251] [info]    Sent message to 127.0.0.1:202 MIXER OK\\r\\n [2026-04-05 09:08:06.266] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x7127cc20b840] No channel layout for input 1 [2026-04-05 09:08:06.266] [warning] [2026-04-05 09:08:06.266] [warning] [ffmpeg] [Parsed_amerge_0 @ 0x7127cc20b840] Input channel layouts overlap: output layout will be determined by the number of distinct input channels [2026-04-05 09:08:06.266] [warning] [2026-04-05 09:08:06.339] [warning] ffmpeg[PIEKLOKOBIET_S01E01_MASTER_178_R709_PL-XX_20DFX_HD_25FPS_HAP_20260210|0.0000/2850.4000] Latency: 6 [2026-04-05 09:08:06.339] [info]    image_producer[media/led-grid-3584x1408.png] Destroyed. casparcg@serwer:~$</p>","reference--xrandr-gpu-screen-mapping":"<h1 id=\"xrandr-gpu-screen-mapping-device-view\">xrandr GPU ↔ screen mapping (Device View)</h1>\n<p>This document is the <strong>implementation-facing</strong> companion to the product narrative in <a href=\"#/reference--GPU_SCREEN_CONSUMER_AND_XRANDR\" data-wiki-page=\"1\">GPU_SCREEN_CONSUMER_AND_XRANDR.md</a>. It describes how HighAsCG picks <strong>screen index <code>N</code></strong>, *<em><code>screen_N_</em></code> settings<strong>, and the </strong>xrandr plan<strong> so </strong>Apply GPU** / <code>POST /api/settings/apply-os</code> does what operators expect.</p>\n<hr>\n<h2 id=\"1-screen-index-n-must-be-consistent-everywhere\">1. Screen index <code>N</code> (must be consistent everywhere)</h2>\n<p><code>N</code> is the <strong>1-based Caspar main screen index</strong> (<code>screen_1_<em></code> … <code>screen_4_</em></code>). It must match between:</p>\n<ul>\n<li><code>calculateLayoutPositions</code> (<code>src/utils/os-layout-calculator.js</code>) for graph-bound <code>gpu_out</code>, and</li>\n<li>Device View GPU inspector (<code>client/components/device-view-inspector-gpu.js</code>), which reads/writes <code>screen_${N}_*</code>.</li>\n</ul>\n<p><strong>Resolution order (same as the layout calculator):</strong></p>\n<ol>\n<li><strong><code>gpu_out.caspar.outputBinding</code></strong> — if <code>type === 'screen'</code>, use <strong><code>outputBinding.index</code></strong> (already 1-based).</li>\n<li>Else <strong>incoming graph edge</strong> <code>dst_in_&lt;destinationId&gt;</code> → <code>gpu_out.sinkId</code> — use that destination’s <strong><code>mainScreenIndex + 1</code></strong> (PGM/PRV routable destinations only; not stream/multiview).</li>\n<li>Else <strong><code>gpu_out.caspar.mainIndex</code></strong> (0-based) → <code>N = mainIndex + 1</code>.</li>\n<li>Else <strong>fallback</strong>: order of <code>gpu_out</code> in the suggested connector list (legacy only).</li>\n</ol>\n<p>If the UI used a different rule (e.g. suggested list order), operators could set *<em><code>screen_2_</em></code><strong> while the head was really </strong>screen 1** — override and OS mode would never apply to the head that xrandr moves.</p>\n<hr>\n<h2 id=\"2-default-vs-override-what-drives---mode\">2. Default vs override (what drives <code>--mode</code>)</h2>\n<table><thead><tr><th>Override (<code>screen_N_force_os_resolution</code>)</th><th>Source of xrandr <code>--mode</code> / width</th></tr></thead><tbody>\n<tr><td><strong>Off</strong></td><td>Bound <strong>destination <code>videoMode</code></strong> (and custom WxH×fps) wins over a “stale” explicit <code>screen_N_os_mode</code> when they disagree — see <code>explicitPixelOsMode</code> branch in <code>os-layout-calculator.js</code>.</td></tr>\n<tr><td><strong>On</strong></td><td><strong>Caspar video mode</strong> for that screen (<code>screen_N_mode</code> in <code>casparServer</code>, or connector default) maps through <code>mapCasparModeToXrandrRes</code>. <strong>Additionally</strong>, if <code>screen_N_os_mode</code> is already an explicit pixel string (<code>1920x1080</code>), that value is used as <strong><code>modeForXrandr</code></strong> (operator / UI set the OS line explicitly).</td></tr>\n</tbody></table>\n<p>Refresh / rate: <code>screen_N_os_rate</code> when set; else inferred from the Caspar mode id where applicable (<code>inferRefreshHzFromCasparMode</code>).</p>\n<hr>\n<h2 id=\"3-post-apisettingsapply-os-contract\">3. <code>POST /api/settings/apply-os</code> contract</h2>\n<p>Handler: <code>src/api/settings-os.js</code> → <code>handleOsPost</code>.</p>\n<ol>\n<li><strong><code>mergeSystemDisplaySettings(ctx, s)</code></strong> — merges <strong>root</strong> keys in <code>SYSTEM_DISPLAY_KEYS</code> from the JSON body <code>s</code> into <strong><code>ctx.config</code></strong> (<code>screen_N_system_id</code>, <code>screen_N_os_mode</code>, <code>screen_N_os_rate</code>, <code>screen_N_force_os_resolution</code>, timing source, etc.).</li>\n</ol>\n<ol>\n<li><strong><code>s.casparServer</code></strong> — if present, merged into <strong><code>ctx.config.casparServer</code></strong> (so <strong><code>screen_N_mode</code></strong>, custom WxH, override mirror in <code>casparServer</code>, etc. exist before layout). <code>mergeSystemDisplaySettings</code> alone does <strong>not</strong> touch <code>casparServer</code>.</li>\n</ol>\n<ol>\n<li><strong>Persistence</strong> — when writing modular config, <strong><code>SYSTEM_DISPLAY_KEYS</code> are copied from <code>ctx.config</code> (<code>cfg</code>) into <code>newConfig</code> not only when a key appears in <code>s</code>, but also when <code>cfg[k] !== cur[k]</code></strong> (<code>cur</code> = previous <code>configManager.get()</code>). Otherwise a flag set only in memory (or omitted from a minimal client body) could be <strong>lost on save</strong>, and the next reload would revert to destination-driven <strong>720p</strong> while logs still showed <strong>1920x1080</strong> for <code>os_mode</code> on the in-memory merge.</li>\n</ol>\n<ol>\n<li><strong><code>calculateLayoutPositions(ctx.config)</code></strong> then <strong><code>applyX11Layout</code></strong> — logs <code>[settings-os] plan id=…</code> lines; xrandr runs with resolved modes (including EDID fallback / CVT create when enabled).</li>\n</ol>\n<p>Device View <strong>Apply resolution</strong> sends <strong><code>apply-os</code></strong> a body that includes both <strong>root OS keys</strong> and a <strong><code>casparServer</code> slice</strong> for the active screen so steps 1–2 are satisfied in one request.</p>\n<hr>\n<h2 id=\"4-ui-server-keys-gpu-inspector\">4. UI ↔ server keys (GPU inspector)</h2>\n<ul>\n<li><strong>Override checkbox</strong> → root <strong><code>screen_N_force_os_resolution</code></strong> and <strong><code>casparServer.screen_N_force_os_resolution</code></strong> (kept in sync on save/apply).</li>\n<li><strong>Video mode</strong> (Caspar) → <strong><code>casparServer.screen_N_mode</code></strong> (+ custom width/height/fps keys when <code>custom</code>).</li>\n<li><strong>With override on</strong>, OS apply uses <strong>Caspar video mode</strong> mapped to <strong><code>screen_N_os_mode</code> / <code>screen_N_os_rate</code></strong> (see <code>casparVideoModeToOsModeAndRate</code> in <code>client/components/device-view-destinations-inspector.js</code>) so xrandr is not accidentally driven only by the EDID dropdown (which still reflects the <strong>current</strong> link).</li>\n</ul>\n<hr>\n<h2 id=\"5-code-map\">5. Code map</h2>\n<table><thead><tr><th>Concern</th><th>Location</th></tr></thead><tbody>\n<tr><td>Layout / <code>modeForXrandr</code>, override, topology</td><td><code>src/utils/os-layout-calculator.js</code></td></tr>\n<tr><td>xrandr execution, mode availability, CVT</td><td><code>src/utils/os-config.js</code>, <code>src/utils/xrandr-custom-mode.js</code></td></tr>\n<tr><td>Cold-boot custom mode script</td><td><code>src/utils/xrandr-persist-script.js</code> → <code>~/.config/highascg/apply-layout.sh</code></td></tr>\n<tr><td>apply-os merge + save</td><td><code>src/api/settings-os.js</code></td></tr>\n<tr><td>Full settings merge</td><td><code>src/api/settings-post.js</code> (<code>mergeSystemDisplaySettings</code>)</td></tr>\n<tr><td>GPU inspector screen index + apply payload</td><td><code>client/components/device-view-inspector-gpu.js</code></td></tr>\n<tr><td>Caspar mode ↔ OS WxH helper (web)</td><td><code>client/components/device-view-destinations-inspector.js</code> (<code>CASPAR_VIDEO_MODE_SPECS</code>, <code>casparVideoModeToOsModeAndRate</code>)</td></tr>\n<tr><td>Regression tests</td><td><code>npm run test:os-layout-w40</code> → <code>tools/smoke-os-layout-w40.js</code></td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"6-operator-checklist\">6. Operator checklist</h2>\n<ol>\n<li>Cable <strong>destination</strong> → <strong><code>gpu_out</code></strong>; confirm <strong>screen <code>N</code></strong> in the inspector title matches the destination <strong>main index</strong> you intend.</li>\n<li><strong>Override off</strong> — xrandr follows <strong>destination video mode</strong>; EDID row is a hint / alternate OS apply path (“Use detected display mode”).</li>\n<li><strong>Override on</strong> — set <strong>Video mode</strong> to <strong>Custom</strong> (or a standard mode); <strong>Apply resolution</strong>; confirm logs show <strong><code>plan id=&lt;connector&gt; mode=&lt;WxH&gt;</code></strong> matching your choice, not the old link resolution.</li>\n<li><strong>OS output</strong> dropdown: pick a mode from <strong>EDID</strong> (uses that xrandr token as-is) or <strong>Custom</strong> (registers RandR mode at WxH×Hz via CVT — e.g. 1080p50 when EDID only lists 60 Hz). Stored as <code>screen_N_os_mode_source</code>.</li>\n<li><strong>NVIDIA + screen consumer:</strong> <strong>Sync to VBlank off</strong> in <code>nvidia-settings</code>, <strong>vsync on</strong> on the Caspar screen consumer — see <strong><a href=\"#/reference--screen-consumer-vsync-nvidia\" data-wiki-page=\"1\">screen-consumer-vsync-nvidia.md</a></strong>.</li>\n</ol>\n<hr>\n<h2 id=\"7-custom-modes-and-cold-boot-wo-80\">7. Custom modes and cold boot (WO-80)</h2>\n<p>When the planned WxH is <strong>not</strong> already listed for the output (<code>xrandr --query</code>), Apply GPU automatically:</p>\n<ol>\n<li>Runs <strong><code>cvt</code> / <code>gtf</code></strong> → <strong><code>xrandr --newmode</code></strong> → <strong><code>--addmode</code></strong> (live), then <strong><code>--output … --mode</code></strong>.</li>\n<li>Persists the same sequence into <strong><code>~/.config/highascg/apply-layout.sh</code></strong> (and <code>/etc/highascg/apply-layout.sh</code> when sudo allows): all <strong><code>--newmode</code></strong> lines first (one per unique mode name), then <strong><code>--addmode</code></strong> per output, then the combined layout command, then primary/mouse/NVIDIA policy lines.</li>\n</ol>\n<p><strong>EDID / System resolution dropdown</strong> — reuses the selected xrandr mode token (<code>screen_N_os_mode_source=edid</code>); no <code>newmode</code> step.</p>\n<p><strong>Custom</strong> in the same dropdown — registers RandR mode at operator WxH×Hz (<code>screen_N_os_mode_source=custom</code>), even when EDID lists the same pixel size at a different refresh.</p>\n<p>Opt-out only: <strong><code>os_xrandr_create_missing_modes: false</code></strong> disables custom registration (nearest-EDID fallback).</p>\n<p>Boot script lines use <code>2&gt;/dev/null || true</code> so re-login is idempotent when modes already exist.</p>\n<p>Last apply metadata: <strong><code>data/runtime/xrandr-custom-modes-last-apply.json</code></strong> (support bundle field <code>plannedCustomModes</code>).</p>\n<p>Regression: <code>npm run test:xrandr-persist-script</code>, <code>npm run test:xrandr-custom-mode</code>.</p>\n<hr>\n<p><em>Last updated: 2026-06-29 — WO-80 cold-boot custom mode persistence.</em></p>","decoupled_architecture_list":"<h1 id=\"highascg-decoupled-service-architecture\">HighAsCG Decoupled Service Architecture</h1>\n<blockquote><p><strong>Production (WO-52):</strong> API and UI both run on the <strong>playout machine</strong> at <strong><code>http://&lt;host&gt;:4200/</code></strong> (<code>dist-web/</code> + <code>/api/*</code>). The diagram below is the <strong>logical</strong> split (browser vs Node bridge), not separate physical hosts.  <strong>Electron launcher</strong> (<a href=\"https://github.com/mko1989/highascg-client\">highascg-client</a>): optional packaging extract from <code>client/tools/electron-launcher/</code> — <strong>simulator</strong>, <strong>multiserver</strong>, <strong>modules</strong>; opens the system browser to <code>:4200</code>. <strong>Canonical UI:</strong> in-repo <strong><code>client/</code></strong> → <code>npm run build:client</code> → <code>dist-web/</code>.</p></blockquote>\n<p>This document defines the strict boundary of responsibilities between the <strong>Node bridge (server)</strong> and the <strong>browser SPA (UI)</strong>.</p>\n<pre><code class=\"language-mermaid\">graph TD\n    subgraph Browser Client (Frontend SPA)\n        UI[Interactive UI &amp; Inspectors]\n        VS[Vite Dev Server &amp; Proxy]\n        WS_C[Client WebSocket Manager]\n        Previs[Three.js 3D Previs]\n    end\n\n    subgraph Headless Server (Backend Engine)\n        API[Express REST API]\n        WS_S[WebSocket Telemetry Hub]\n        AMCP[CasparCG AMCP Driver]\n        DMX[ArtNet DMX Controller]\n        DB[(State &amp; Presets JSON DB)]\n    end\n\n    UI --&gt;|Relative Requests| VS\n    VS --&gt;|Proxy Pass /api| API\n    VS --&gt;|Proxy Pass /ws| WS_S\n    WS_C &lt;--&gt;|Bi-directional State Sync| WS_S\n    API &lt;--&gt; DB\n    API --&gt; AMCP\n    AMCP &lt;--&gt;|TCP Playout Control| CasparCG[CasparCG Server]</code></pre>\n<hr>\n<h2 id=\"1-headless-backend-server-service-engine\">1. Headless Backend Server (Service Engine)</h2>\n<p>The Node bridge runs as a system service on playout. It owns state persistence, hardware interfaces, and playout scheduling. The browser UI (<code>dist-web/</code>) is served from the same process on production hosts.</p>\n<h3 id=\"responsibilities\">Responsibilities</h3>\n<ul>\n<li><strong>HTTP / REST API Engine</strong>: Handles saving configurations, uploading previs GLTF models, backing up snapshot states, and processing operational logs.</li>\n<li><strong>WebSocket Synchronization Hub</strong>: Coordinates bidirectional telemetry updates (<code>state_delta</code> updates, active playback metrics, and DMX level packets).</li>\n<li><strong>CasparCG Connection (AMCP Driver)</strong>: Manages direct socket channels to CasparCG servers. Automatically performs reconnects, parses clip listings, query configurations, and dispatches real-time commands.</li>\n<li><strong>DMX &amp; ArtNet Driver</strong>: Maps physical LED mapping dimensions, binds UDP sockets, and emits real-time lighting control packets.</li>\n<li><strong>Scene Engine &amp; Take Compositor</strong>: Performs mathematical layout evaluations, groups devices into active configurations, and schedules multi-step take playout jobs.</li>\n<li><strong>Config &amp; Preset Database</strong>: Reads and persists settings inside local JSON files (e.g., <code>highascg.config.json</code> and active scene states).</li>\n</ul>\n<hr>\n<h2 id=\"2-decoupled-frontend-client-spa\">2. Decoupled Frontend Client (SPA)</h2>\n<p>The frontend is a lightweight static bundle (HTML, CSS, JS) built with Vite. <strong>On production playout</strong> it is served by the same Node process as the API (<code>dist-web/</code> on <code>:4200</code>). Dev builds may use a separate Vite dev server that proxies to the API.</p>\n<h3 id=\"responsibilities\">Responsibilities</h3>\n<ul>\n<li><strong>Interactive UI (Vanilla JS Components)</strong>: Renders the Devices panel, Sources inspector, Audio Mixer, and Timeline grid.</li>\n<li><strong>State Store (<code>state-store.js</code>)</strong>: Tracks client-side caches of loaded media, active streams, and DMX universes.</li>\n<li><strong>WebSocket Interface (<code>ws-client.js</code>)</strong>: Subscribes to real-time events and handles instant state synchronization updates.</li>\n<li><strong>Lazy-Loaded Modules</strong>:</li>\n<li><strong>Three.js Previs Viewport</strong>: Renders 3D layouts, camera orbits, and mock video textures.</li>\n<li><strong>GrapesJS Layout builder</strong>: Renders HTML/CSS visual composition editors.</li>\n<li><strong>WebRTC Streaming Client</strong>: Binds incoming low-latency video preview streams.</li>\n<li><strong>Style System (CSS)</strong>: Defines harmonious layout tokens, glassmorphism headers, dark mode variants, and responsive layout grids.</li>\n</ul>\n<hr>\n<h2 id=\"3-the-shared-boundary-api-ws-contracts\">3. The Shared Boundary (API &amp; WS Contracts)</h2>\n<p>The frontend and backend interact <strong>only</strong> through a strictly defined interface.</p>\n<table><thead><tr><th>Category</th><th>Endpoint / Event</th><th>Payload Structure</th><th>Description</th></tr></thead><tbody>\n<tr><td><strong>REST API</strong></td><td><code>GET /api/media</code></td><td>Array of <code>MediaItem</code> objects</td><td>Fetch cached files from the server's media path.</td></tr>\n<tr><td><strong>REST API</strong></td><td><code>POST /api/config/save</code></td><td>Full <code>Config</code> JSON model</td><td>Overwrite the server's active hardware mapping.</td></tr>\n<tr><td><strong>WebSocket</strong></td><td>Send <code>{ type: &quot;amcp&quot;, cmd: &quot;...&quot; }</code></td><td><code>{ type: &quot;amcp_result&quot;, data: &quot;...&quot; }</code></td><td>Execute live raw AMCP commands directly.</td></tr>\n<tr><td><strong>WebSocket</strong></td><td>Recv <code>state_delta</code></td><td>HSL or diff delta object</td><td>Real-time synchronization of screen layouts.</td></tr>\n</tbody></table>","ARTNET_PIXEL_MAPPING":"<h1 id=\"art-net-pixel-mapping-capability-landscape-wo-241-updated-wo-242\">Art-Net &amp; pixel mapping — capability landscape (WO-241, updated WO-242)</h1>\n<p><strong>Status:</strong> Canonical reference (2026-07) | <strong>Binary:</strong> <code>bin/casparcg</code> highascg-build-v1, deployed 2026-07-15 — see <a href=\"#/CASPARCG_BUILD_PROVENANCE\" data-wiki-page=\"1\">CASPARCG_BUILD_PROVENANCE.md</a> <strong>Walkthroughs:</strong> <a href=\"#/WALKTHROUGH_ARTNET_LED_WALL\" data-wiki-page=\"1\">WALKTHROUGH_ARTNET_LED_WALL.md</a> (native engines, primary flow) · <a href=\"#/WALKTHROUGH_PIXELMAP_FIXTURES\" data-wiki-page=\"1\">WALKTHROUGH_PIXELMAP_FIXTURES.md</a> (live JS pipeline, [DEPRECATED — legacyJsPixelmap flag])</p>\n<p>This rig has <strong>three independent engines</strong> that turn live video into DMX-over-network (Art-Net / sACN). They coexist; pick per production. Everything below is read from the code actually deployed — source cites are <code>file:line</code> into the built Caspar tree (<code>/home/casparcg/caspar-build/src-tree/</code>) and this repo.</p>\n<hr>\n<h2 id=\"pixel-map-screen-native-primary-flow-wo-242\">Pixel-map screen (native) — primary flow (WO-242)</h2>\n<p><strong>Recommended default as of WO-242.</strong> Device View can now create a dedicated <code>pixelmap</code> screen destination end-to-end: a Caspar channel (custom raster-exact video-mode) plus a generated native <code>&lt;artnet&gt;</code> consumer, wired up automatically — no hand-editing <code>config/casparcg.config</code> and no risk of the block being wiped by the next regen (that gap, flagged in WO-241 below, is closed).</p>\n<ol>\n<li><strong>Device View → rear panel → destination type &quot;Pixel Map&quot;.</strong> Set label, fixture columns/rows,</li>\n</ol>\n<p>controller IP, start universe, start DMX address, fixture type (RGB/RGBW), DMX refresh rate, and (optionally) a custom raster width/height/fps — same panel as any other screen destination (<code>client/components/device-view-destinations-inspector-form.js</code>).</p>\n<ol>\n<li><strong>Apply Caspar config (restart)</strong> — same WO-236 restart-dirty affordance as every other</li>\n</ol>\n<p>config-affecting Device View change; any pixel-map field edit turns the button orange.</p>\n<ol>\n<li>The generator emits the channel + <code>&lt;artnet&gt;</code> consumer block automatically on every regen —</li>\n</ol>\n<p>see <a href=\"#/WALKTHROUGH_ARTNET_LED_WALL\" data-wiki-page=\"1\">WALKTHROUGH_ARTNET_LED_WALL.md</a> for the exact schema and a worked 8×4-wall example, and <code>src/config/config-generator-consumer-attach.js</code> (<code>buildPixelmapChannel</code>) for the implementation.</p>\n<ol>\n<li><strong>Looks integration for free:</strong> the pixel-map channel registers as a <strong>PGM-only</strong> screen (WO-160b</li>\n</ol>\n<p>path — same mechanism as any &quot;PGM only&quot; destination), so it appears in decks/screen selectors, respects custom screen labels (WO-222), and takes onto it run the pgm-only path automatically. No preview bus is allocated for it.</p>\n<ol>\n<li><strong>What you lose vs. the legacy JS pipeline:</strong> Art-Net only (no sACN), and every fixture-array</li>\n</ol>\n<p>change needs a config regen + Caspar restart (no live remapping mid-show). If you need either of those, re-enable the legacy engine below via <code>settings.ui.legacyJsPixelmap</code> instead.</p>\n<hr>\n<h2 id=\"the-three-engines\">The three engines</h2>\n<h3 id=\"1-highascg-js-sampling-pipeline-live-editable-deprecated-legacyjspixelmap-flag\">1. HighAsCG JS sampling pipeline — [LIVE-EDITABLE] · [DEPRECATED — legacyJsPixelmap flag]</h3>\n<p><strong>WO-242:</strong> this engine's fixture inspector (in the &quot;Pixel Map&quot; tab) is now hidden by default — the code is untouched and still fully functional, but new fixtures can't be created from the UI unless <code>settings.ui.legacyJsPixelmap</code> is set to <code>true</code> (default <code>false</code>/absent). (Note: mapping nodes — the &quot;+ Add mapping node&quot; button in Device View — are NOT part of this pipeline; they are the canvas-splitting output feature and remain fully active.) Prefer the native &quot;Pixel-map screen&quot; flow above unless you specifically need sACN output or restart-free live remapping (see the walkthrough for the honest trade-off).</p>\n<p>The WO-179 pipeline inside the Node server. Caspar streams a downscaled copy of a program channel back to Node (<code>ADD &lt;ch&gt; STREAM</code> MPEG-TS + local ffmpeg, or a FILE consumer on dedicated slot 97 — <code>src/sampling/dmx-sampling-ingress.js:17</code>), a worker thread samples per-fixture regions, and <code>src/sampling/dmx-output.js</code> transmits <strong>Art-Net (npm dmxnet) or sACN (npm sacn) per fixture</strong>. Fixtures are edited in the UI and applied <strong>without touching Caspar's config or restarting anything</strong> (<code>index.js:316</code> re-runs <code>SamplingManager.updateConfig()</code> on every settings save).</p>\n<ul>\n<li>Fixture model: sample rect + rotation + mirror H/V + grid cols×rows + colorOrder (rgb/grb/…/rgbw/rgbwa) + gamma + brightness + protocol + universe + start channel + destination IP (<code>client/lib/dmx-state.js:69-99</code>).</li>\n<li>Sampling: per grid cell, either single center pixel or region average capped at ~64 samples/cell (<code>src/sampling/fixture-transform.js:50-55</code>).</li>\n<li>Rates: default 25 fps, operator-configurable (<code>src/sampling/dmx-sampling.js:29,145</code>); ingress downscale 10–50 % of PGM (<code>dmx-sampling.js:79-82</code>).</li>\n<li>Also in this family: Art-Net <strong>input</strong> listener for the global border (default <strong>OFF</strong>, <code>src/artnet/artnet-slot-config.js:10-16</code>) and an sACN input receiver (<code>src/artnet/sacn-receiver.js</code>).</li>\n</ul>\n<h3 id=\"2-native-artnet-consumer-improved-artnet-pr-1752-configcaspar-restart\">2. Native <code>&lt;artnet&gt;</code> consumer (improved-artnet, PR #1752) — [CONFIG+CASPAR-RESTART]</h3>\n<p>Compiled into the deployed binary (provenance row 1). A real Caspar <code>frame_consumer</code> attached to a channel in <code>casparcg.config</code>; registered <strong>preconfigured-only</strong> (<code>modules/artnet/artnet.cpp:36</code>) — there is no AMCP <code>ADD ... ARTNET</code>, so every fixture change means config edit + Caspar restart.</p>\n<ul>\n<li>Per-fixture sample <strong>regions on the channel raster</strong> with center/edge positioning, rotation (bilinear 4× supersampled resample, <code>artnet_consumer.cpp:63,371-410</code>), mirror-x/y, per-fixture host/port/universe.</li>\n<li><strong>True area averaging via libswscale</strong> (<code>SWS_AREA</code>, <code>artnet_consumer.cpp:512-525</code>): each fixture group is downscaled to exactly cols×rows pixels — every source pixel contributes, no stride caps. This is the best-quality averaging on the rig.</li>\n<li>Fixture types DIMMER/RGB/RGBW, per-LED flux compensation, brightness, multi-universe auto-spill past 512 channels (<code>artnet_consumer.cpp:318-333</code>).</li>\n<li>Own send thread at <code>refresh-rate</code> Hz (default 10, <code>artnet_consumer.cpp:67,140-218</code>) — DMX rate is decoupled from video rate.</li>\n<li>8-bit channels only (<code>artnet_consumer.cpp:745-746</code>).</li>\n</ul>\n<h3 id=\"3-native-pixel-consumer-pr-1751-configcaspar-restart\">3. Native <code>&lt;pixel&gt;</code> consumer (PR #1751) — [CONFIG+CASPAR-RESTART]</h3>\n<p>Also compiled in (provenance row 9). The opposite philosophy: <strong>no regions, no averaging — the whole channel raster is the pixel map.</strong> Every frame, every pixel of the video mode is converted (luma / rgb / rgbw / rgbx) and packed sequentially into consecutive Art-Net universes (<code>modules/pixel/consumer/pixel_consumer.cpp:83-116</code>, <code>artdmx_sink.h:76-109</code>). Grid resolution = channel video-mode resolution, so it is used with a small <strong>custom video mode</strong> matching the LED grid (e.g. 96×54). Per-channel color coefficients + gamma (<code>pixel_consumer.cpp:176-187</code>). Registered preconfigured-only as <code>pixel</code> (<code>modules/pixel/pixel.cpp:29-31</code>); Art-Net is the only protocol it accepts today (<code>pixel_consumer.cpp:148-150</code>).</p>\n<hr>\n<h2 id=\"comparison\">Comparison</h2>\n<table><thead><tr><th></th><th>JS pipeline (WO-179)</th><th>Native <code>&lt;artnet&gt;</code> (improved-artnet)</th><th>Native <code>&lt;pixel&gt;</code> (#1751)</th></tr></thead><tbody>\n<tr><td><strong>Badge</strong></td><td>[LIVE-EDITABLE]</td><td>[CONFIG+CASPAR-RESTART]</td><td>[CONFIG+CASPAR-RESTART]</td></tr>\n<tr><td><strong>Latency path</strong></td><td>Caspar encode → UDP → ffmpeg decode → JS worker → dmxnet/sacn (extra encode/decode hop; runs at its own fps, default 25)</td><td>In-process: consumer holds the last frame, sends on its own thread at <code>refresh-rate</code> Hz (default 10, raise in config)</td><td>In-process, sends <strong>every frame</strong> at channel rate (50 fps on this rig)</td></tr>\n<tr><td><strong>Live editability</strong></td><td>Full — fixtures, regions, rates re-apply on settings save, no Caspar restart</td><td>None — config regen/edit + Caspar restart per change</td><td>None — same, plus video-mode changes for grid changes</td></tr>\n<tr><td><strong>Averaging quality</strong></td><td>Box average capped ~64 samples/cell (stride skips pixels on big regions), or single center pixel (<code>fixture-transform.js:50-55</code>)</td><td><strong>Best</strong>: libswscale <code>SWS_AREA</code> full box filter, every pixel weighted (<code>artnet_consumer.cpp:518</code>); rotation via 4× supersampled bilinear</td><td>None needed — 1 source pixel = 1 LED (compose/scale content on the channel instead)</td></tr>\n<tr><td><strong>Protocols out</strong></td><td>Art-Net <strong>and sACN</strong>, per fixture</td><td>Art-Net only</td><td>Art-Net only</td></tr>\n<tr><td><strong>Universe range</strong></td><td>Art-Net clamped to universes <strong>0–15</strong> by dmxnet (<code>dmx-output.js:30</code>); sACN unrestricted</td><td>0–32767, auto-spill across universes</td><td>0–32767, sequential spill</td></tr>\n<tr><td><strong>Fixture types</strong></td><td>Any colorOrder string incl. amber (rgbwa)</td><td>DIMMER / RGB / RGBW (+<code>fixture-channels</code> padding)</td><td>luma / rgb / rgbw / rgbx</td></tr>\n<tr><td><strong>Config surface</strong></td><td>UI (Pixel Map tab + Inspector), persisted in <code>settings.dmx</code></td><td><code>&lt;artnet&gt;</code> block in a channel's <code>&lt;consumers&gt;</code> — full schema in the LED-wall walkthrough</td><td><code>&lt;pixel&gt;</code> block + custom video mode</td></tr>\n<tr><td><strong>Restart coupling</strong></td><td>None</td><td>Caspar restart (Device View → Apply Caspar config)</td><td>Caspar restart</td></tr>\n<tr><td><strong>CPU cost</strong></td><td>Highest (ffmpeg decode + JS loops per frame)</td><td>Low (SIMD libswscale, one small scale per fixture group)</td><td>Lowest per pixel, but sends W×H×bytes channels per frame — keep the grid small</td></tr>\n<tr><td><strong>Monitoring</strong></td><td><code>[DMX]</code> lines in app log, live color preview over WS (<code>dmx-sampling.js:363-369</code>)</td><td>OSC/monitor state <code>artnet/…</code> + log warnings (<code>artnet_consumer.cpp:242-254,453-505</code>)</td><td>Log errors on send failure only (<code>pixel_consumer.cpp:104-112</code>)</td></tr>\n</tbody></table>\n<p><strong>Rule of thumb:</strong> mapping edited during the show → JS pipeline. Fixed rig, best color fidelity / lowest CPU → native <code>&lt;artnet&gt;</code>. Raw LED processor or wall that wants a full raster → native <code>&lt;pixel&gt;</code>.</p>\n<hr>\n<h2 id=\"what-can-we-produce-with-this\">What can we produce with this</h2>\n<p>Concrete production ideas, each pointing at the right engine.</p>\n<ol>\n<li><strong>LED wall behind the stage, fed by a dedicated Caspar channel</strong> — add a channel that carries wall content (loops, generative looks, a routed PGM copy), attach a native <code>&lt;artnet&gt;</code> consumer with one <code>fixture-count=&quot;WxH&quot;</code> group per panel section. Area-averaged, restart-once, then rock solid all show. → <a href=\"#/WALKTHROUGH_ARTNET_LED_WALL\" data-wiki-page=\"1\">WALKTHROUGH_ARTNET_LED_WALL.md</a>.</li>\n<li><strong>Pixel-tape stage outlines sampled from PGM edges</strong> — thin fixtures (e.g. 128×1) whose sample regions hug the top/side edges of the program frame, so the tape &quot;continues&quot; the picture beyond the screen. Live-editable placement matters here (you re-aim regions after the set changes) → JS pipeline, <code>sampleMode: 'average'</code>. → <a href=\"#/WALKTHROUGH_PIXELMAP_FIXTURES\" data-wiki-page=\"1\">WALKTHROUGH_PIXELMAP_FIXTURES.md</a>.</li>\n<li><strong>Matrix / blinder walls driven by a looks layer</strong> — play stingers/looks on a dedicated layer of a small channel and let the native <code>&lt;artnet&gt;</code> consumer's DIMMER type drive blinder cells (luma-weighted 0.279/0.547/0.106, <code>artnet_consumer.cpp:429</code>); RGBW matrix cells get proper white extraction (<code>min(r,g,b)</code>, <code>artnet_consumer.cpp:436-443</code>).</li>\n<li><strong>House-light color washes via sACN from PGM's dominant colors</strong> — one or two big-region fixtures averaging the whole frame (or its left/right halves), protocol <code>sacn</code>, pointed at the house console's input universes. Only the JS pipeline speaks sACN, and it multicasts natively (<code>dmx-output.js:72-91</code>).</li>\n<li><strong>Floor grids / dance-floor pixels</strong> — mid-size grid (e.g. 24×16): either engine. Choose native <code>&lt;artnet&gt;</code> when the floor is rigged once per venue; choose JS when the floor mapping is re-blocked between acts.</li>\n<li><strong>Low-res audience surfaces (wristband-style / seat pixels)</strong> — hundreds of coarse cells fed the mood of the show: <code>&lt;pixel&gt;</code> consumer on a tiny custom video mode (the raster IS the seat map), content authored as ordinary Caspar media on that channel. Sequential universe packing does the addressing for you.</li>\n<li><strong>External pixel processors (Madrix-style, Resolume, ArtNet-in media servers)</strong> — hand them a clean raster: <code>&lt;pixel&gt;</code> consumer, type <code>rgb</code>, one universe boundary every 170 pixels; the processor re-maps downstream.</li>\n</ol>\n<hr>\n<h2 id=\"current-limits-planned-work\">Current limits &amp; planned work</h2>\n<ul>\n<li><strong>[PLANNED] WO-228 hybrid sws sampling</strong> — replace the JS worker's stride-capped averaging with a Caspar-side ffmpeg <code>scale=cols:rows:flags=area</code> consumer feeding the existing live-editable output path (drafted in <code>work/work-orders/225_WO_ARTNET_INVESTIGATION.md</code>). Not implemented; today the UDP ingress already downscales with <code>flags=area</code> (<code>dmx-sampling-ingress.js:152</code>) but the per-cell average is still JS-side.</li>\n<li><strong>[PLANNED] WO-180 GDTF fixture import/export</strong> — industry fixture exchange (<code>.gdtf</code>) into/out of the pixel-map fixture model. Research-stage only (<code>work/work-orders/180_WO_GDTF_FIXTURE_IMPORT_EXPORT.md</code>).</li>\n<li><strong>[DONE — WO-242]</strong> The config generator now emits <code>&lt;artnet&gt;</code> consumer blocks automatically for</li>\n</ul>\n<p><code>pixelmap</code> screen destinations (<code>src/config/config-generator-consumer-attach.js</code> <code>buildPixelmapChannel</code>) — those survive every regen. This does <strong>not</strong> cover the manual single-fixture <code>&lt;artnet&gt;</code> edit from the LED-wall walkthrough's step 4, nor the <code>&lt;pixel&gt;</code> consumer: both are still hand-edited config and are <strong>lost on the next Device View regen</strong> — re-add after every &quot;Apply Caspar config&quot; if you use either outside a <code>pixelmap</code> destination. Generator support for <code>&lt;pixel&gt;</code> remains undrafted follow-up work.</p>\n<ul>\n<li>JS Art-Net output is clamped to universes 0–15 (<code>dmx-output.js:30</code>); use sACN or the native consumers above that.</li>\n<li>All three engines are 8-bit (<code>artnet_consumer.cpp:745</code>, <code>pixel_consumer.cpp:140</code>).</li>\n</ul>","CASPARCG_BUILD_PROVENANCE":"<h1 id=\"casparcg-highascg-build-v1-provenance-manifest\">CasparCG highascg-build-v1 — provenance manifest</h1>\n<p>Every component of the final binary, mapped to its source. Keep current through assembly/build. (Exact commit hashes get filled by the assembly step — see SOURCES.txt in the build kit for the raw list.)</p>\n<table><thead><tr><th>#</th><th>Component</th><th>Source</th><th>What it provides</th><th>Why we carry it</th></tr></thead><tbody>\n<tr><td>1</td><td><strong>Base tree</strong></td><td><code>firecraftgaming/caspar-server</code> branch <code>improved-artnet</code> = upstream <strong>PR #1752</strong> (tracks recent CasparCG/server master)</td><td>Core server close to current upstream; <strong>improved Art-Net consumer</strong>: per-fixture region config with native <code>sws_scale</code> box-averaging (fixes LED-wall rasterization)</td><td>Newest stable core + the artnet quality fix; base for everything below</td></tr>\n<tr><td>2</td><td><strong>PortAudio/ASIO/JACK consumer</strong></td><td><code>gmeisel01/CasparCG_Enhanced</code> differentiator commits</td><td>Low-latency system audio output paths</td><td><strong>Owner-critical audio</strong> (the reason Enhanced was built)</td></tr>\n<tr><td>3</td><td><strong>OAL video-scheduled audio</strong></td><td>CasparCG_Enhanced</td><td>OpenAL audio consumer scheduled against video clock</td><td>Enhanced audio stack; A/V sync behavior the rig relies on</td></tr>\n<tr><td>4</td><td><strong>Screen consumer GL/aspect fixes</strong></td><td>CasparCG_Enhanced</td><td>Fixes GL scaling/aspect on screen consumers</td><td>Live PGM screen consumers on this rig</td></tr>\n<tr><td>5</td><td><strong>DeckLink teardown fix</strong></td><td>upstream <strong>PR #1763</strong></td><td>Use-after-free on DeckLink consumer teardown</td><td>Crash-class fix; DeckLink is the primary output</td></tr>\n<tr><td>6</td><td><strong>CG payload escaping</strong></td><td>upstream <strong>PR #1761</strong></td><td>Correct escaping of CG UPDATE payloads</td><td>We drive countdown/timer CG UPDATE constantly</td></tr>\n<tr><td>7</td><td><strong>NDI memory-leak fix</strong></td><td>upstream <strong>PR #1691</strong> (rebased during assembly)</td><td>Bounded queue + clean shutdown in NDI consumer</td><td>NDI in production; leak kills long shows</td></tr>\n<tr><td>8</td><td><strong>STREAM auto-reconnect</strong></td><td>upstream <strong>PR #1727</strong></td><td>ffmpeg STREAM consumer reconnects after network drops</td><td>Owner streams; deployment note: keep STREAM off the primary DeckLink channel (disconnect thread-join latency)</td></tr>\n<tr><td>9</td><td><strong>Pixel consumer</strong></td><td>upstream <strong>PR #1751</strong> — OWNER PICK</td><td>Generic full-frame pixel raster output over Art-Net (grid = video-mode resolution, config-file)</td><td>Media-server general tool: raw grid walls / external pixel processors; complements (not replaces) #1 for fixture mapping</td></tr>\n<tr><td>10</td><td><strong>MIXER BLUR/SHARPEN/GRAIN</strong></td><td>upstream <strong>PR #1762</strong></td><td>New mixer effects, off-by-default</td><td>Live-events look tools; zero cost when unused; applied LAST (touches mixer core)</td></tr>\n</tbody></table>\n<p><strong>Deliberately NOT included</strong> (revisit later): #1758/#1765/#1766 color-grading series (all patch the same 6 files — manual-merge project, deferred); #1755/#1756 crash-hardening drafts (unreviewed, need owner soak); #1670 (stale); #1737/#1731 (large, orthogonal).</p>\n<p><strong>CEF (PINNED, owner-mandated):</strong> the EXACT owner build from https://github.com/mko1989/highascg/releases/tag/v.142 (<code>cef_build_minimal_no_root</code>), sha256 <code>2a03fa6b9da0e374fb318903d9502e098ac91ba2ec5be83e96ae076b5aa63cc9</code> — pinned by URL+hash in Bootstrap_Linux.cmake (branch commit 253c16c). The upstream mirror ships a DIFFERENT binary under the same filename; the hash pin makes any substitution fail the build. The tarball itself is included in the build kit for offline builds. <strong>Assembly hashes:</strong> base <code>6b065c8</code> (improved-artnet), final HEAD <code>253c16c</code> (34 source commits + CEF pin); full per-commit provenance in the kit's SOURCES.txt. <strong>Build/deploy:</strong> kit at ~/Downloads/caspar-build-kit/ (docker + bare-metal variants); binary lands as bin/casparcg.new; owner swaps in a maintenance window; previous binary kept as casparcg.prev.</p>\n<hr>\n<h2 id=\"build-record-2026-07-15-built-on-the-highascg-dev-box\">Build record (2026-07-15, built on the highascg dev box)</h2>\n<ul>\n<li>Assembled branch <code>highascg-build-v1</code>, final source HEAD <code>b96e58d</code> (incl. the CEF pin commit <code>253c16c</code> and the C++20 lambda-capture fix for Enhanced's oal/portaudio consumers).</li>\n<li>Toolchain: system cmake 3.28.3 + ninja 1.11.1, gcc, Release, <code>-j28</code>; <code>USE_SYSTEM_CEF=OFF</code>; CEF pre-seeded from the pinned tarball (sha256 <code>2a03fa6b…</code>), which is byte-identical to the deployed <code>highascg/lib/libcef.so</code> — the runtime lib tree required no changes.</li>\n<li>Output: <code>bin/casparcg</code> (9.3 MB, reports <code>2.6.0 Dev Revision: 253c16c</code>), deployed 2026-07-15 (previous binary kept as <code>bin/casparcg.prev</code>).</li>\n<li>Rebuild recipe + full per-commit provenance: <code>~/Downloads/caspar-build-kit/</code> (<code>SOURCES.txt</code>, <code>build-baremetal.sh</code>, <code>build-docker.sh</code>, unshallowed git bundle, the exact CEF tarball) and <code>work/work-orders/234_WO_CASPAR_FINAL_BUILD.md</code>.</li>\n<li>Known post-swap integration work: WO-235 (2.6-dev OSC/INFO schema drift vs highascg parsers).</li>\n</ul>","STICK_QUICK_START":"<h1 id=\"highascg-playout-stick-quick-start\">HighAsCG playout stick — quick start</h1>\n<p>Prepare a <strong>bootable HighAsCG USB stick</strong> on <strong>Windows</strong> or <strong>macOS</strong>: flash the live ISO, add the operator <strong>exFAT</strong> partition, copy the starter folder layout, then boot the playout machine from the stick.</p>\n<p><strong>Time:</strong> ~20 minutes · <strong>USB size:</strong> 32 GiB recommended (minimum: ISO size + ~2 GiB free for exFAT)</p>\n<hr>\n<h2 id=\"what-you-end-up-with\">What you end up with</h2>\n<table><thead><tr><th>Layer</th><th>Label</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><strong>Boot image</strong> (written by Etcher)</td><td><code>highascg</code></td><td>Live Linux + CasparCG — <strong>do not delete or reformat</strong></td></tr>\n<tr><td><strong>Operator data</strong> (you create this)</td><td><strong><code>HIGHASCGEXF</code></strong></td><td>Configs, media, server updates — must be <strong>exactly</strong> this label (11 characters)</td></tr>\n</tbody></table>\n<p>On boot, the playout machine mounts <strong><code>HIGHASCGEXF</code></strong> at <code>/home/casparcg/exfat</code>, applies any server drop in <code>drop-update/</code>, syncs <code>configs/</code>, and starts the operator UI at <strong><code>http://&lt;playout-ip&gt;/</code></strong> (nginx proxies port 80 → <code>:4200</code>; direct URL <strong><code>http://&lt;playout-ip&gt;:4200/</code></strong> also works).</p>\n<hr>\n<h2 id=\"1-download-the-latest-iso\">1. Download the latest ISO</h2>\n<p>Always use the <strong>current</strong> build from the download site — filenames change with each release.</p>\n<table><thead><tr><th>Method</th><th>Link / command</th></tr></thead><tbody>\n<tr><td><strong>Browser (recommended)</strong></td><td><a href=\"https://highascg.dpdns.org/\">https://highascg.dpdns.org/</a> — click <strong>DOWNLOAD BOOTABLE ISO</strong></td></tr>\n<tr><td><strong>Direct URL (auto-latest)</strong></td><td>Read <code>download_url</code> from <a href=\"https://highascg.dpdns.org/release.json\">release.json</a></td></tr>\n</tbody></table>\n<p><strong>Resume a large download (Linux / macOS Terminal):</strong></p>\n<pre><code class=\"language-bash\">ISO_URL=$(python3 -c &quot;import json,urllib.request; print(json.load(urllib.request.urlopen('https://highascg.dpdns.org/release.json'))['download_url'])&quot;)\nwget -c &quot;$ISO_URL&quot; -O highascg-latest.iso\necho &quot;Saved: highascg-latest.iso&quot;</code></pre>\n<p><strong>Windows PowerShell:</strong></p>\n<pre><code class=\"language-powershell\">$release = Invoke-RestMethod https://highascg.dpdns.org/release.json\nInvoke-WebRequest -Uri $release.download_url -OutFile highascg-latest.iso</code></pre>\n<p>Example filename at time of writing: <code>highascg-nvidia-595_amd64_2026-06-08_0812.iso</code> (~2.7 GiB). Yours may differ — trust <strong><code>release.json</code></strong>, not a fixed name.</p>\n<hr>\n<h2 id=\"2-flash-the-stick-with-balena-etcher\">2. Flash the stick with Balena Etcher</h2>\n<p>Install <a href=\"https://etcher.balena.io/\">Balena Etcher</a> if needed.</p>\n<h3 id=\"step-1-load-the-iso\">Step 1 — Load the ISO</h3>\n<p>Open Etcher → <strong>Flash from file</strong> → select your <code>highascg-*.iso</code>.</p>\n<p>!<a href=\"../guides/stick/images/flash_step1_load_iso.png\">Loading ISO in Etcher</a></p>\n<h3 id=\"step-2-select-the-usb-drive\">Step 2 — Select the USB drive</h3>\n<p>Click <strong>Select target</strong> → choose your USB stick. <strong>Check capacity and model</strong> — Etcher will erase the whole device.</p>\n<p>!<a href=\"../guides/stick/images/flash_step2_select_usb.png\">Target USB selection in Etcher</a></p>\n<h3 id=\"step-3-flash-and-verify\">Step 3 — Flash and verify</h3>\n<p>Click <strong>Flash!</strong> → approve the admin prompt → wait for verification (<strong>Flash Complete!</strong>).</p>\n<p>!<a href=\"../guides/stick/images/flash_step3_verify_success.png\">Flash complete in Etcher</a></p>\n<blockquote><p><strong>Important:</strong> When Windows or macOS asks to format new volumes after flashing, click <strong>Cancel</strong> / <strong>Don't format</strong>. You still need to create the exFAT partition in the next step.</p></blockquote>\n<hr>\n<h2 id=\"3-create-the-exfat-partition-highascgexf\">3. Create the exFAT partition (<code>HIGHASCGEXF</code>)</h2>\n<p>Use <strong>only the unallocated space at the end</strong> of the USB disk. Never delete or reformat the small boot partitions Etcher created.</p>\n<h3 id=\"windows-disk-management\">Windows — Disk Management</h3>\n<ol>\n<li><code>Win + X</code> → <strong>Disk Management</strong></li>\n<li>In the <strong>lower</strong> pane, select your <strong>Removable</strong> USB disk (verify size — <strong>not</strong> internal Disk 0)</li>\n<li>Right-click the <strong>black unallocated</strong> bar at the <strong>right end</strong> → <strong>New Simple Volume…</strong></li>\n<li>Use all available space → file system <strong>exFAT</strong> → volume label <strong><code>HIGHASCGEXF</code></strong> (exact spelling, no spaces)</li>\n<li>Finish and note the drive letter (e.g. <code>E:</code>)</li>\n</ol>\n<p><strong>Optional — <code>diskpart</code> (run as Administrator)</strong> — replace <code>2</code> with your USB disk number from Disk Management:</p>\n<pre><code class=\"language-text\">diskpart\nlist disk\nselect disk 2\ncreate partition primary\nformat fs=exfat label=HIGHASCGEXF quick\nassign\nexit</code></pre>\n<h3 id=\"macos-terminal\">macOS — Terminal</h3>\n<p>Replace <strong><code>disk2</code></strong> with your USB whole-disk id from step 1. <strong>Never use <code>disk0</code> or <code>disk1</code></strong> (internal disks).</p>\n<p><strong>1. List external USB disks (read-only):</strong></p>\n<pre><code class=\"language-bash\">diskutil list external physical</code></pre>\n<p><strong>2. Inspect the stick (read-only):</strong></p>\n<pre><code class=\"language-bash\">diskutil list disk2</code></pre>\n<p><strong>3. Unmount before editing the partition table:</strong></p>\n<pre><code class=\"language-bash\">diskutil unmountDisk disk2</code></pre>\n<p><strong>4. Add an exFAT slice with <code>fdisk</code> (interactive):</strong></p>\n<pre><code class=\"language-bash\">sudo fdisk -e /dev/rdisk2</code></pre>\n<p>In <code>fdisk</code>, type one line at a time (do not paste the whole block):</p>\n<ol>\n<li><code>print</code> — note the last free MBR slot (often slot 4)</li>\n<li><code>edit 4</code> — use your free slot number</li>\n<li><code>07</code> — exFAT/NTFS family type</li>\n<li>Choose <strong>sector offsets</strong> when asked (not CHS)</li>\n<li>Start sector: <strong><code>12582912</code></strong> (6 GiB offset — safe for current ~5 GiB ISO builds)</li>\n<li>End sector: total sectors from <code>diskutil info disk2</code> <strong>minus 1</strong></li>\n<li><code>print</code> — confirm the boot ISO slice is unchanged</li>\n<li><code>write</code> → confirm → <code>exit</code></li>\n</ol>\n<p><strong>5. Format the new slice only</strong> — replace <code>disk2s4</code> with your new slice from <code>diskutil list disk2</code>:</p>\n<pre><code class=\"language-bash\">sudo newfs_exfat -v HIGHASCGEXF /dev/rdisk2s4\ndiskutil mount disk2s4</code></pre>\n<p>If the volume mounts as <code>HIGHASCGEXF 1</code>, rename it:</p>\n<pre><code class=\"language-bash\">diskutil rename &quot;HIGHASCGEXF 1&quot; HIGHASCGEXF</code></pre>\n<hr>\n<h2 id=\"4-copy-the-starter-folder-layout\">4. Copy the starter folder layout</h2>\n<p>Download <strong><a href=\"../guides/stick/HIGHASCGEXF-starter-layout.zip\">HIGHASCGEXF-starter-layout.zip</a></strong> from this repo (or build it with <code>npm run exfat:starter-zip</code>).</p>\n<p>Unzip <strong>directly onto the exFAT volume root</strong> — not inside an extra folder.</p>\n<p><strong>Expected top-level folders after unzip:</strong></p>\n<pre><code>HIGHASCGEXF/\n  configs/              ← factory settings + starter show\n  drop-config/\n  drop-update/          ← put highascg-server_*.tar.gz here later\n  drop-update/applied/\n  media/\n  projects/\n  snapshots/rear-panels/\n  templates/\n  decklink/               ← optional DeckLink vendor debs (see below)\n  README.txt</code></pre>\n<p><strong>Windows (PowerShell)</strong> — adjust <code>E:</code> to your drive letter:</p>\n<pre><code class=\"language-powershell\">Expand-Archive -Path HIGHASCGEXF-starter-layout.zip -DestinationPath E:\\ -Force</code></pre>\n<p><strong>macOS / Linux:</strong></p>\n<pre><code class=\"language-bash\">unzip -o HIGHASCGEXF-starter-layout.zip -d /Volumes/HIGHASCGEXF</code></pre>\n<p>Safely eject the stick when copying finishes.</p>\n<blockquote><p><strong>Optional later:</strong> extract a <a href=\"https://github.com/mko1989/highascg/releases\">GitHub server release</a> (<code>highascg-server_*.tar.gz</code>) into <code>drop-update/</code> so <code>drop-update/package.json</code> exists. The starter zip alone is enough for a first boot test with the embedded ISO server tree.</p></blockquote>\n<h3 id=\"decklink-drivers-optional-operator-supplied\">DeckLink drivers (optional — operator-supplied)</h3>\n<p>The public ISO does <strong>not</strong> ship Blackmagic Desktop Video. If the playout machine has a DeckLink card, copy vendor packages into <strong><code>decklink/</code></strong> on the stick (create the folder if your starter zip predates it):</p>\n<pre><code>HIGHASCGEXF/decklink/\n  desktopvideo_&lt;version&gt;_amd64.deb          ← required for Caspar decklink I/O\n  desktopvideo-gui_&lt;version&gt;_amd64.deb      ← optional (Setup / Updater GUI)</code></pre>\n<p>Download <a href=\"https://www.blackmagicdesign.com/support/family/capture-and-playback\">Desktop Video for Linux</a>, extract the tarball, and copy from <code>deb/x86_64/</code>.</p>\n<table><thead><tr><th>Package</th><th>Needed for playout?</th><th>Purpose</th></tr></thead><tbody>\n<tr><td><strong><code>desktopvideo</code></strong></td><td><strong>Yes</strong> (when using DeckLink)</td><td>Kernel driver, <code>libDeckLinkAPI.so</code>, firmware / CLI update tools</td></tr>\n<tr><td><strong><code>desktopvideo-gui</code></strong></td><td><strong>No</strong></td><td>Blackmagic Desktop Video <strong>Setup</strong> and <strong>Updater</strong> GUI only</td></tr>\n</tbody></table>\n<p><strong><code>desktopvideo</code> alone is enough</strong> when the card is already on appropriate firmware and Caspar channel settings in <code>configs/</code> are sufficient. Without <code>desktopvideo-gui</code>, Settings → <strong>Desktop Video Setup</strong> / <strong>Updater</strong> report <em>GUI not installed</em> — playout is unaffected.</p>\n<p>On boot, HighAsCG installs from <code>decklink/</code> when needed (<code>highascg-decklink-install.service</code>). See <code>decklink/README.txt</code> on the volume.</p>\n<hr>\n<h2 id=\"5-boot-from-the-stick\">5. Boot from the stick</h2>\n<ol>\n<li>Insert the USB into the <strong>playout machine</strong></li>\n<li>Power on and open <strong>UEFI/BIOS setup</strong> (common keys: <code>F2</code>, <code>F12</code>, <code>Del</code>, <code>Esc</code> at the manufacturer logo)</li>\n<li><strong>Disable Secure Boot</strong> — NVIDIA and DeckLink drivers on the live image are not signed for Secure Boot</li>\n<li>Set <strong>USB</strong> as the first boot device, <strong>or</strong> use the one-time boot menu (<code>F12</code> on many boards) and pick the USB stick</li>\n<li>Save and exit — the machine should show the <strong>HighAsCG GRUB</strong> menu</li>\n<li>Choose the default <strong>Live</strong> entry</li>\n</ol>\n<p>After boot:</p>\n<ul>\n<li>Operator UI: <strong><code>http://127.0.0.1/</code></strong> or <strong><code>http://&lt;playout-ip&gt;/</code></strong> from another machine on the LAN (port 80 → <code>:4200</code>); <strong><code>http://&lt;playout-ip&gt;:4200/</code></strong> works directly too</li>\n<li>Confirm the stick mounted: Settings → exFAT sync, or on the machine run <code>lsblk -f</code> and look for <strong><code>HIGHASCGEXF</code></strong></li>\n</ul>\n<hr>\n<h2 id=\"checklist\">Checklist</h2>\n<table><thead><tr><th>Step</th><th>Done when</th></tr></thead><tbody>\n<tr><td>ISO downloaded</td><td>File opens in Etcher; size matches <a href=\"https://highascg.dpdns.org/release.json\">release.json</a></td></tr>\n<tr><td>Etcher flash</td><td><strong>Flash Complete!</strong> banner</td></tr>\n<tr><td>exFAT created</td><td>Explorer/Finder shows <strong><code>HIGHASCGEXF</code></strong> with <strong>gigabytes</strong> free (not ~15 MiB)</td></tr>\n<tr><td>Starter zip copied</td><td><code>configs/</code>, <code>drop-update/</code>, <code>media/</code>, … at volume root</td></tr>\n<tr><td>BIOS</td><td>Secure Boot <strong>off</strong>, USB boot <strong>on</strong></td></tr>\n<tr><td>Live boot</td><td>GRUB menu → desktop or headless playout starts; <code>:4200</code> responds</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table><thead><tr><th>Problem</th><th>Fix</th></tr></thead><tbody>\n<tr><td>No unallocated space after Etcher</td><td>Use a <strong>larger</strong> USB (32 GiB recommended)</td></tr>\n<tr><td>Windows only shows a tiny partition</td><td>Do not format the ISO slice — partition only the <strong>trailing free space</strong></td></tr>\n<tr><td>macOS <strong>Partition</strong> greyed out</td><td>Use the Terminal / <code>fdisk</code> steps above</td></tr>\n<tr><td>Stick boots but no exFAT sync</td><td>Volume label must be exactly <strong><code>HIGHASCGEXF</code></strong></td></tr>\n<tr><td>Secure Boot error / no NVIDIA</td><td>Disable Secure Boot in BIOS</td></tr>\n</tbody></table>\n<hr>\n<h2 id=\"more-detail\">More detail</h2>\n<table><thead><tr><th>Topic</th><th>Document</th></tr></thead><tbody>\n<tr><td>exFAT vs ISO contents</td><td><a href=\"#/WO47_ISO_VS_EXFAT\" data-wiki-page=\"1\">WO47_ISO_VS_EXFAT.md</a></td></tr>\n<tr><td>Bridge disk + USB roles</td><td><a href=\"#/BRIDGE_DISK_AND_USB_EXFAT\" data-wiki-page=\"1\">BRIDGE_DISK_AND_USB_EXFAT.md</a></td></tr>\n<tr><td>Server drops on the stick</td><td><a href=\"#/EXFAT_SERVER_UPDATE\" data-wiki-page=\"1\">EXFAT_SERVER_UPDATE.md</a></td></tr>\n<tr><td>GUI prep kit (optional)</td><td><a href=\"https://github.com/mko1989/highascg-client\">Electron launcher</a> — same flash/partition screenshots</td></tr>\n</tbody></table>","SECURITY":"<h1 id=\"highascg-security-model\">HighAsCG security model</h1>\n<h2 id=\"overview\">Overview</h2>\n<p>HighAsCG exposes an HTTP API and operator WebSocket on the playout server. By default <strong>authentication is not enforced</strong> so existing deployments keep working. When enforcement is enabled, every <code>/api/*</code> route (except the auth bootstrap endpoints) and the operator WebSocket require a valid API token.</p>\n<h2 id=\"enabling-api-authentication\">Enabling API authentication</h2>\n<p>Set either:</p>\n<ul>\n<li>Environment: <code>HIGHASCG_ENFORCE_AUTH=1</code></li>\n<li>Config: <code>security.enforceAuth: true</code> in <code>highascg.config.json</code> or modular <code>config/general.json</code></li>\n</ul>\n<p>On first boot with enforcement enabled, the server generates a random token in <code>.private/api-token</code> (mode <code>0600</code>) and logs a warning with recovery instructions.</p>\n<p>Print the token on the box:</p>\n<pre><code class=\"language-bash\">bash tools/runtime/print-api-token.sh</code></pre>\n<h2 id=\"operator-login-browser-ui\">Operator login (browser UI)</h2>\n<ol>\n<li>Open the UI on the same host/port as the server (same-origin).</li>\n<li>If auth is enforced and you are not logged in, a sign-in overlay appears.</li>\n<li>Paste the API token; the server sets an <code>HttpOnly; SameSite=Strict</code> session cookie.</li>\n<li>Subsequent API calls and WebSocket upgrades use the cookie automatically.</li>\n</ol>\n<p>For scripts and automation, send <code>Authorization: Bearer &lt;token&gt;</code> on HTTP requests. WebSocket upgrades accept the same token via <code>?token=</code> query param.</p>\n<h2 id=\"network-exposure\">Network exposure</h2>\n<table><thead><tr><th>Setting</th><th>Effect</th></tr></thead><tbody>\n<tr><td><code>security.enforceAuth: false</code> (default)</td><td>Legacy behaviour: binds <code>0.0.0.0</code> unless overridden</td></tr>\n<tr><td><code>security.enforceAuth: true</code> + <code>exposeToNetwork: false</code></td><td>Binds <code>127.0.0.1</code> only</td></tr>\n<tr><td><code>security.enforceAuth: true</code> + <code>exposeToNetwork: true</code></td><td>LAN bind allowed when a token is configured</td></tr>\n</tbody></table>\n<p>Refusing to bind <code>0.0.0.0</code> without a token prevents accidental open control planes.</p>\n<h2 id=\"cors-and-cross-origin-access\">CORS and cross-origin access</h2>\n<p>With auth enforcement, CORS no longer uses <code>Access-Control-Allow-Origin: *</code>. Allowed origins are:</p>\n<ul>\n<li>Same host as the request (<code>Origin</code> matches <code>Host</code>)</li>\n<li>Extra origins in <code>HIGHASCG_CORS_ORIGINS</code> (comma-separated)</li>\n</ul>\n<p>Cross-site browser pages cannot drive the API when cookies are <code>SameSite=Strict</code>.</p>\n<h2 id=\"secrets-in-api-responses\">Secrets in API responses</h2>\n<p><code>GET /api/settings</code> runs sensitive fields through redaction (<code>nuclearPassword</code>, <code>apiToken</code>, keys matching <code><em>password</em></code> / <code><em>token</em></code> / <code><em>secret</em></code>).</p>\n<h2 id=\"tls-reverse-proxy\">TLS / reverse proxy</h2>\n<p>HighAsCG does not terminate TLS in-process. Put nginx, Caddy, or Tailscale Serve in front for HTTPS. Set <code>Secure</code> on session cookies when the proxy sends <code>X-Forwarded-Proto: https</code>.</p>\n<h2 id=\"recovery\">Recovery</h2>\n<p>If locked out:</p>\n<ol>\n<li>SSH to the playout box.</li>\n<li>Run <code>bash tools/runtime/print-api-token.sh</code>.</li>\n<li>Log in locally or pass <code>Authorization: Bearer …</code> to API clients.</li>\n</ol>\n<p>To disable enforcement temporarily: unset <code>HIGHASCG_ENFORCE_AUTH</code> and set <code>security.enforceAuth: false</code>, then restart.</p>\n<h2 id=\"production-iso-eggs-sticks\">Production ISO / eggs sticks</h2>\n<p><code>prepare-eggs-clone-with-exfat.sh</code> installs <code>highascg.service.d/25-api-auth.conf</code> (<code>HIGHASCG_ENFORCE_AUTH=1</code>) unless <code>HIGHASCG_EGG_ENFORCE_AUTH=0</code>. Factory modular config from <code>write-iso-default-config.js</code> sets <code>security.enforceAuth: true</code> and <code>exposeToNetwork: true</code>.</p>\n<p>Each stick generates a <strong>unique</strong> token on first <code>highascg</code> start (<code>ensureApiToken</code>). Operators recover it with <code>print-api-token.sh</code> on the box.</p>\n<h2 id=\"dependency-audit-policy\">Dependency audit policy</h2>\n<p>CI runs <code>node tools/ci/npm-audit-ci.js</code>, which fails on <strong>high</strong> or <strong>critical</strong> advisories in production dependencies (<code>npm audit --omit=dev</code>).</p>\n<table><thead><tr><th>Package</th><th>Notes</th></tr></thead><tbody>\n<tr><td><code>ws</code></td><td>Direct + transitive (via <code>osc</code>) pinned to <strong>8.21.0+</strong> via <code>package.json</code> <code>overrides</code>.</td></tr>\n<tr><td><code>puppeteer</code></td><td><strong>Removed</strong> (WO-247/WO-248). CEF control and headless-Chrome thumbnail rendering now use the in-repo raw CDP client (<code>src/system/cef-cdp-client.js</code>, <code>src/media/headless-chrome-cdp.js</code>) over <code>ws</code>. The Chrome binary is resolved from <code>HIGHASCG_CHROME_BIN</code>, the cached Chrome-for-Testing under <code>~/.cache/puppeteer/chrome/</code>, or a system <code>chromium</code>/<code>google-chrome</code>.</td></tr>\n<tr><td><code>exceljs</code></td><td>Optional — lower-third roster <code>.xlsx</code> import only. Parses with a <strong>2 MB</strong> size cap and <strong>15 s</strong> timeout; row objects use null prototypes and unsafe keys (<code>__proto__</code>, etc.) are stripped.</td></tr>\n<tr><td>Legacy <code>.xls</code></td><td>Not supported — operators should save as <code>.xlsx</code> or CSV.</td></tr>\n</tbody></table>\n<p>Run locally: <code>node tools/ci/npm-audit-ci.js</code></p>\n<h2 id=\"related-work\">Related work</h2>\n<ul>\n<li>WO-96: API/WS authentication (this document)</li>\n<li>WO-97: Zip-Slip-safe ingest extraction (<code>src/utils/safe-unzip.js</code>)</li>\n<li>WO-105: Dependency audit policy and roster import hardening (this section)</li>\n</ul>","wiki--integration--virtual-camera-output":"<h1 id=\"virtual-camera-output-v4l2loopback\">Virtual camera output (v4l2loopback)</h1>\n<p>Feed Caspar program output to <strong>Zoom</strong>, <strong>OBS</strong>, or other apps as a <strong>V4L2 webcam</strong> plus an optional <strong>ALSA virtual mic</strong>. HighAsCG uses the same safe JPEG buffer pattern as compose preview — Caspar writes a single overwriting JPEG; an ffmpeg relay pushes frames to <strong>v4l2loopback</strong>.</p>\n<p><strong>Default:</strong> off until the operator starts from Device View or the API.</p>\n<h2 id=\"requirements\">Requirements</h2>\n<ul>\n<li><strong><code>v4l2loopback</code></strong> kernel module (video device, typically <code>/dev/video10</code>)</li>\n<li><strong><code>snd-aloop</code></strong> when audio is enabled (ALSA loopback card <code>HighAsCG_VCam</code>)</li>\n<li><strong>FFmpeg</strong> on the playout host</li>\n<li>Caspar <strong>AMCP connected</strong></li>\n</ul>\n<p>No PulseAudio / PipeWire — audio uses <strong>ALSA loopback + PortAudio</strong> capture only.</p>\n<h2 id=\"quick-setup\">Quick setup</h2>\n<ol>\n<li><strong>Device View → rear panel → Virtual cam</strong> — open inspector.</li>\n<li>Set <strong>channel</strong>, resolution, fps; enable <strong>Include audio</strong> if needed.</li>\n<li><strong>Save settings</strong> → <strong>Start virtual cam</strong>.</li>\n<li>In Zoom/OBS:</li>\n</ol>\n<ul>\n<li>Camera: <strong>CasparCG Out</strong> (or label from config)</li>\n<li>Mic (optional): <strong>HighAsCG_VCam</strong> / loopback capture (<code>hw:HighAsCG_VCam,1,0</code>)</li>\n</ul>\n<p>Or cable a <strong>destination</strong> (PGM) to the <strong>Virtual cam</strong> port on the rear panel, <strong>Apply Caspar config</strong> — that sets <code>virtualCamera.channel</code> from the cabled source.</p>\n<h2 id=\"kernel-modules\">Kernel modules</h2>\n<p>Modules load automatically on <strong>Start</strong> when passwordless sudo is configured:</p>\n<pre><code class=\"language-bash\">sudo bash scripts/setup/12-passwordless-sudo.sh</code></pre>\n<p>This installs <code>/usr/local/lib/highascg/highascg-vcam-modules-up.sh</code> and a <code>NOPASSWD</code> rule. HighAsCG writes <code>/run/highascg/vcam-modules.conf</code> then runs the wrapper via <code>sudo -n</code>.</p>\n<p>Manual one-time per boot (if sudo wrapper not installed):</p>\n<pre><code class=\"language-bash\">sudo modprobe v4l2loopback devices=1 video_nr=10 card_label=&quot;CasparCG Out&quot;\nsudo modprobe snd-aloop enable=1 index=20 id=HighAsCG_VCam pcm=2</code></pre>\n<h2 id=\"architecture\">Architecture</h2>\n<pre><code>Caspar chN ──consumer 710──► media/highascg_vcam/chN.jpg  (mjpeg -update 1)\n                                    │\n                                    ▼\n                          ffmpeg relay (HighAsCG subprocess)\n                                    │\n                                    ▼\n                               /dev/video10  ──► Zoom / OBS\n\nCaspar chN ──consumer 711──► ALSA plughw:HighAsCG_VCam,0,0  (playback)\n                                    │\n                                    ▼\n                          hw:HighAsCG_VCam,1,0  ──► Zoom mic</code></pre>\n<table><thead><tr><th>Piece</th><th>Role</th></tr></thead><tbody>\n<tr><td>Consumer <strong>710</strong></td><td>Caspar FILE consumer → overwriting JPEG</td></tr>\n<tr><td>Consumer <strong>711</strong></td><td>Caspar ALSA consumer → loopback playback</td></tr>\n<tr><td>ffmpeg relay</td><td>Reads JPEG → v4l2loopback device</td></tr>\n<tr><td><code>virtualCamera.enabled</code></td><td>Runtime gate — default <strong>false</strong></td></tr>\n</tbody></table>\n<h2 id=\"config-generaljson-virtualcamera\">Config (<code>general.json</code> → <code>virtualCamera</code>)</h2>\n<table><thead><tr><th>Key</th><th>Default</th><th>Notes</th></tr></thead><tbody>\n<tr><td><code>enabled</code></td><td><code>false</code></td><td>Runtime; cleared on Stop</td></tr>\n<tr><td><code>channel</code></td><td><code>1</code></td><td>Caspar source channel</td></tr>\n<tr><td><code>device</code></td><td><code>/dev/video10</code></td><td>v4l2loopback node</td></tr>\n<tr><td><code>width</code> / <code>height</code></td><td><code>1920</code> / <code>1080</code></td><td>Full program resolution</td></tr>\n<tr><td><code>fps</code></td><td><code>50</code></td><td>Relay frame rate</td></tr>\n<tr><td><code>audioEnabled</code></td><td><code>true</code></td><td>ALSA loopback mic</td></tr>\n<tr><td><code>alsaLoopbackCardId</code></td><td><code>HighAsCG_VCam</code></td><td>Card id for <code>snd-aloop</code></td></tr>\n<tr><td><code>showInDeviceView</code></td><td><code>true</code></td><td>Rear panel connector</td></tr>\n</tbody></table>\n<h2 id=\"api\">API</h2>\n<table><thead><tr><th>Method</th><th>Path</th><th>Purpose</th></tr></thead><tbody>\n<tr><td>GET</td><td><code>/api/virtual-camera</code></td><td>Config + runtime status</td></tr>\n<tr><td>GET</td><td><code>/api/virtual-camera/status</code></td><td>Alias</td></tr>\n<tr><td>POST</td><td><code>/api/virtual-camera/config</code></td><td>Persist settings</td></tr>\n<tr><td>POST</td><td><code>/api/virtual-camera/start</code></td><td>Start bridge (loads modules if needed)</td></tr>\n<tr><td>POST</td><td><code>/api/virtual-camera/stop</code></td><td>Stop bridge</td></tr>\n</tbody></table>\n<p>Status also on <code>GET /api/state</code> → <code>virtualCameraStatus</code> and Device View <code>live.virtualCamera</code>.</p>\n<pre><code class=\"language-bash\">curl -s http://127.0.0.1:4200/api/virtual-camera/status | python3 -m json.tool\ncurl -s -X POST http://127.0.0.1:4200/api/virtual-camera/start \\\n  -H 'Content-Type: application/json' -d '{&quot;channel&quot;:1}' | python3 -m json.tool</code></pre>\n<p><strong>HTTP errors:</strong> <code>503</code> if Caspar disconnected; <code>502</code> if relay fails after start.</p>\n<h2 id=\"troubleshooting\">Troubleshooting</h2>\n<table><thead><tr><th>Symptom</th><th>Check</th></tr></thead><tbody>\n<tr><td><code>/dev/video10</code> missing</td><td>Run sudo setup script; <code>ls -l /dev/video10</code></td></tr>\n<tr><td>Start returns module error</td><td><code>sudo -n /usr/local/lib/highascg/highascg-vcam-modules-up.sh</code> after conf exists</td></tr>\n<tr><td>Video black in Zoom</td><td>Bridge running? <code>ffplay -f v4l2 -video_size 1920x1080 /dev/video10</code></td></tr>\n<tr><td>No audio in Zoom</td><td><code>snd-aloop</code> loaded? `aplay -l \\</td><td>grep HighAsCG_VCam`; pick loopback <strong>capture</strong> in app</td></tr>\n<tr><td>Wrong channel</td><td>Inspector channel vs cabled destination — Apply after cabling</td></tr>\n<tr><td>Disk fill</td><td>Bridge is opt-in; no unix-socket sink</td></tr>\n</tbody></table>\n<h2 id=\"related\">Related</h2>\n<ul>\n<li>Work order: <code>work/work-orders/137_WO_VIRTUAL_CAMERA_OUTPUT_DEVICE_VIEW.md</code></li>\n<li>USB <strong>input</strong> (exclude loopback): <a href=\"#/wiki--integration--usb-v4l2-input\" data-wiki-page=\"1\">usb-v4l2-input.md</a></li>\n<li>Passwordless sudo: <a href=\"#/HIGHASCG_PASSWORDLESS_SUDO\" data-wiki-page=\"1\">HIGHASCG_PASSWORDLESS_SUDO.md</a></li>\n</ul>","WALKTHROUGH_ARTNET_LED_WALL":"<h1 id=\"walkthrough-led-wall-on-the-native-art-net-consumer\">Walkthrough — LED wall on the native Art-Net consumer</h1>\n<p><strong>Badge:</strong> [CONFIG+CASPAR-RESTART] — every step in this doc that changes fixtures requires a Caspar restart. <strong>Engine:</strong> improved-artnet consumer compiled into <code>bin/casparcg</code> (highascg-build-v1, 2026-07-15). All schema below is read from the deployed source: <code>/home/casparcg/caspar-build/src-tree/src/modules/artnet/</code> — cites are <code>artnet_consumer.cpp:&lt;line&gt;</code> into <code>consumer/artnet_consumer.cpp</code> unless noted. <strong>Landscape / engine choice:</strong> <a href=\"#/ARTNET_PIXEL_MAPPING\" data-wiki-page=\"1\">ARTNET_PIXEL_MAPPING.md</a></p>\n<p>Scenario: an <strong>8×4-panel LED wall</strong> behind the stage, each panel driven as one RGB pixel-block, wall controller at <code>192.168.1.50</code>, Art-Net universe 0.</p>\n<p><strong>WO-242 update:</strong> for exactly this scenario — one fixture group sampling the <em>whole</em> channel raster, row-major grid, RGB/RGBW — Device View can now generate this whole config for you: create a <code>pixelmap</code> screen destination (rear panel → destination type &quot;Pixel Map&quot;) with the same columns/rows/IP/universe/address/color-order/refresh-rate values, then <strong>Apply Caspar config (restart)</strong>. See docs/ARTNET_PIXEL_MAPPING.md's &quot;Pixel-map screen (native) — primary flow&quot; section. The manual walk below is still the right tool for anything the generated single-fixture-group model doesn't cover — multiple <code>&lt;fixture&gt;</code> groups per channel, partial/off-center sample regions, rotation, or mirror-x/y — and it's how <code>buildPixelmapChannel</code> (<code>src/config/config-generator-consumer-attach.js</code>) itself was derived, so the schema table in step 2 is still the source of truth either way.</p>\n<hr>\n<h2 id=\"1-dedicate-a-caspar-channel-to-wall-content\">1. Dedicate a Caspar channel to wall content</h2>\n<p>Give the wall its own channel so wall looks/loops don't fight PGM. The consumer samples whatever the channel renders — media, HTML templates, a routed copy of PGM. A small video mode is fine (the consumer rescales per fixture region anyway); 8-bit only — the consumer refuses to attach to deep-color channels (&quot;Artnet consumer only supports 8-bit color depth.&quot;, <code>artnet_consumer.cpp:745-746</code>).</p>\n<p>On this rig channels come from the Device View generator (<code>config/casparcg.config</code> is generated). As of WO-242 the generator <em>can</em> emit a single-fixture-group <code>&lt;artnet&gt;</code> block automatically for a <code>pixelmap</code> screen destination (see the update note above) — this manual walk instead hand-edits an <code>&lt;artnet&gt;</code> block onto an arbitrary existing channel (step 4), which is still how you'd add a second/partial fixture group, rotation, or mirroring the generated model doesn't expose.</p>\n<h2 id=\"2-the-real-artnet-consumer-schema\">2. The real <code>&lt;artnet&gt;</code> consumer schema</h2>\n<p>Parsed by <code>create_preconfigured_consumer()</code> + <code>get_fixtures_ptree()</code> (<code>artnet_consumer.cpp:737-756, 566-735</code>). Everything the parser reads, with rules:</p>\n<table><thead><tr><th>Element</th><th>Required</th><th>Range / format</th><th>Default</th><th>Source</th></tr></thead><tbody>\n<tr><td><code>&lt;refresh-rate&gt;</code></td><td>no</td><td>int ≥ 1 (DMX sends/sec)</td><td>10</td><td>:748-751, :67</td></tr>\n<tr><td><code>&lt;fixtures&gt;</code> → <code>&lt;fixture&gt;</code>…</td><td>yes</td><td>one <code>&lt;fixture&gt;</code> per group</td><td>—</td><td>:572-573</td></tr>\n<tr><td><code>fixture/start-address</code></td><td><strong>yes</strong></td><td>1–512 (1-based DMX)</td><td>—</td><td>:577-583</td></tr>\n<tr><td><code>fixture/host</code></td><td><strong>yes</strong></td><td>valid IP literal (not hostname)</td><td>—</td><td>:585-593</td></tr>\n<tr><td><code>fixture/port</code></td><td><strong>yes</strong></td><td>1–65535 (Art-Net = 6454)</td><td>—</td><td>:595-600</td></tr>\n<tr><td><code>fixture/universe</code></td><td><strong>yes</strong></td><td>0–32767</td><td>—</td><td>:602-607</td></tr>\n<tr><td><code>fixture/fixture-count</code></td><td><strong>yes</strong></td><td><code>&quot;N&quot;</code> (1×N chain) or <code>&quot;WxH&quot;</code> grid</td><td>—</td><td>:609-631</td></tr>\n<tr><td><code>fixture/type</code></td><td><strong>yes</strong></td><td><code>DIMMER</code> \\</td><td><code>RGB</code> \\</td><td><code>RGBW</code> (case-insens.)</td><td>—</td><td>:633-645</td></tr>\n<tr><td><code>fixture/fixture-channels</code></td><td>no</td><td>≥ channels of type (1/3/4), ≤ 512 — pad for e.g. 4-ch RGB+strobe cells</td><td>= type size</td><td>:647-657</td></tr>\n<tr><td><code>fixture/flux</code> → <code>&lt;r&gt;&lt;g&gt;&lt;b&gt;&lt;w&gt;</code></td><td>no</td><td>floats &gt; 0; per-LED output compensation (value divides the channel)</td><td>1.0 each</td><td>:659-667, fixture_calculation.h:45-53</td></tr>\n<tr><td><code>fixture/brightness</code></td><td>no</td><td>0.0–1.0 master multiplier</td><td>1.0</td><td>:669-671</td></tr>\n<tr><td><code>fixture/rotation</code></td><td>no</td><td>degrees, clockwise about region center</td><td>0</td><td>:673-674</td></tr>\n<tr><td><code>fixture/mirror-x</code>, <code>mirror-y</code></td><td>no</td><td>bool — applied after rotation, in grid space</td><td>false</td><td>:676-677, :202-208</td></tr>\n<tr><td>geometry (pixels on the channel raster)</td><td>yes*</td><td><code>&lt;width&gt;/&lt;height&gt;</code> + one of <code>&lt;x&gt;/&lt;y&gt;</code> (center) or <code>&lt;left&gt;/&lt;top&gt;</code> or <code>&lt;right&gt;/&lt;bottom&gt;</code>; or omit size and give both <code>&lt;left&gt;+&lt;right&gt;</code> / <code>&lt;top&gt;+&lt;bottom&gt;</code> to derive it. Priority center → left/top → right/bottom; conflicting specifiers log a warning</td><td>0</td><td>:679-729</td></tr>\n</tbody></table>\n<p>*A missing/empty box is skipped with a warning at startup (:484-485); a box poking outside the frame is clamped with a warning (:453-470).</p>\n<p>Sampling internals worth knowing: each fixture group is rescaled to exactly cols×rows pixels with libswscale <code>SWS_AREA</code> (true box average, every pixel counts, :512-525); rotated groups first go through a 4×-supersampled bilinear resample (:63, :371-410). <code>DIMMER</code> outputs luma <code>0.279R+0.547G+0.106B</code> (:429); <code>RGBW</code> extracts white as <code>min(r,g,b)</code> (:436-443).</p>\n<p><strong>Universe math / auto-spill:</strong> sub-fixtures are addressed sequentially from <code>start-address</code>; when the next one would cross channel 512 it spills to <code>universe+1</code> at channel 1, same host/port (:318-333). So with <code>fixture-channels</code> = 3: <code>floor(512/3)</code> = <strong>170 fixtures per universe</strong>. An 8×4 wall (32 × 3 = 96 ch) fits in one universe; a 48×27 pixel grid (1296 px) needs <code>ceil(1296/170)</code> = <strong>8 universes</strong> (0–7) automatically.</p>\n<h2 id=\"3-config-for-the-84-wall\">3. Config for the 8×4 wall</h2>\n<p>Inside the wall channel's <code>&lt;consumers&gt;</code> in <code>config/casparcg.config</code>:</p>\n<pre><code class=\"language-xml\">&lt;channel&gt;\n    &lt;video-mode&gt;1080p5000&lt;/video-mode&gt;\n    &lt;consumers&gt;\n        &lt;!-- ... existing consumers for this channel ... --&gt;\n        &lt;artnet&gt;\n            &lt;refresh-rate&gt;30&lt;/refresh-rate&gt;\n            &lt;fixtures&gt;\n                &lt;fixture&gt;\n                    &lt;type&gt;RGB&lt;/type&gt;\n                    &lt;start-address&gt;1&lt;/start-address&gt;\n                    &lt;fixture-count&gt;8x4&lt;/fixture-count&gt;\n                    &lt;fixture-channels&gt;3&lt;/fixture-channels&gt;\n                    &lt;host&gt;192.168.1.50&lt;/host&gt;\n                    &lt;port&gt;6454&lt;/port&gt;\n                    &lt;universe&gt;0&lt;/universe&gt;\n                    &lt;!-- sample the full 1920x1080 raster --&gt;\n                    &lt;x&gt;960&lt;/x&gt;\n                    &lt;y&gt;540&lt;/y&gt;\n                    &lt;width&gt;1920&lt;/width&gt;\n                    &lt;height&gt;1080&lt;/height&gt;\n                    &lt;brightness&gt;1.0&lt;/brightness&gt;\n                &lt;/fixture&gt;\n            &lt;/fixtures&gt;\n        &lt;/artnet&gt;\n    &lt;/consumers&gt;\n&lt;/channel&gt;</code></pre>\n<p>That one <code>&lt;fixture&gt;</code> element is the whole wall: an 8-col × 4-row grid, row-major, panel (0,0) = channels 1-3 of universe 0, panel (7,3) = channels 94-96. Wall wired differently? Flip with <code>&lt;mirror-x&gt;true&lt;/mirror-x&gt;</code> / <code>&lt;mirror-y&gt;</code>, or split into multiple <code>&lt;fixture&gt;</code> groups (one per daisy-chain) each with its own region + start address — per-fixture <code>host</code>/<code>universe</code> means different chains can even go to different controllers.</p>\n<p>Add <code>&lt;flux&gt;</code> if the wall's red LEDs run visibly hot: e.g. <code>&lt;flux&gt;&lt;r&gt;1.2&lt;/r&gt;&lt;/flux&gt;</code> divides red by 1.2 (fixture_calculation.h:45-53).</p>\n<h2 id=\"4-applying-on-this-rig\">4. Applying on this rig</h2>\n<p>The generator will overwrite hand edits, so use the one-shot editor flow:</p>\n<ol>\n<li>Device View → toolbar pencil icon (<strong>&quot;View or edit generated Caspar config (advanced)&quot;</strong>, <code>client/components/device-view-toolbar.js:26-31</code>).</li>\n<li><strong>✏️ Edit</strong> → paste the <code>&lt;artnet&gt;</code> block into the wall channel's <code>&lt;consumers&gt;</code> → <strong>Apply &amp; restart</strong> (<code>client/components/caspar-config-modal.js:28-40</code>). This writes the config and restarts Caspar — brief output interruption on all channels, do it in a maintenance window.</li>\n<li>Alternatively edit <code>config/casparcg.config</code> on disk and use Settings → <strong>Restart CasparCG</strong> (<code>client/components/settings-modal-templates.js:317</code>), the same WO-236-style restart affordance (Device View's <strong>Apply Caspar config (restart)</strong> button turns orange when config-affecting settings are dirty, <code>client/components/device-view-selection.js:30-31</code>).</li>\n</ol>\n<p><strong>Caveat (repeat it to yourself):</strong> any later Device View regen/apply regenerates the file <strong>without</strong> the <code>&lt;artnet&gt;</code> block — re-add it afterwards. Generator support is [PLANNED] (drafted WO-230, see WO-233's retarget note).</p>\n<h2 id=\"5-verification\">5. Verification</h2>\n<ul>\n<li>Startup: the consumer registers as <code>artnet</code> (module init, <code>modules/artnet/artnet.cpp:36</code>) and appears in channel INFO/OSC state with <code>artnet/fixtures</code>, <code>artnet/computed-fixtures</code>, <code>artnet/output-universes</code>, <code>artnet/refresh-rate</code> (<code>artnet_consumer.cpp:242-254</code>).</li>\n<li>Misconfig warnings in the Caspar log (exact strings from source):</li>\n<li><code>artnet: fixture box extends outside the frame; clamping to the nearest point inside it.</code> (:463-464)</li>\n<li><code>artnet: fixture group has no sub-fixtures; skipping.</code> (:475)</li>\n<li><code>artnet: fixture box is empty or outside the channel; skipping.</code> (:485)</li>\n<li><code>artnet: fixture box is smaller than a pixel; skipping.</code> (:504)</li>\n<li><code>artnet: fixture has conflicting horizontal specifiers. …</code> (:700-703, same for vertical :706-709)</li>\n<li>Bad config aborts consumer creation with explicit user errors, e.g. <code>Fixture &lt;start-address&gt; must be between 1 and 512</code> (:581), <code>Fixture &lt;host&gt; must be a valid IP address</code> (:592-593), <code>Fixture count must be 'N' or 'WxH'</code> (:624), <code>Refresh rate must be at least 1</code> (:751).</li>\n<li>Per-packet trace (only at trace log level): <code>Sent DMX data to Artnet, universe: N</code> (:560).</li>\n<li>On the wall: play bars/a grid ident on the wall channel; each panel should show the area-average of its region. Full 512-channel ArtDMX frames go out every <code>1000/refresh-rate</code> ms per universe (:140-218, :533-563) even when video is static — sniff with Wireshark (<code>udp.port == 6454</code>) if the controller shows nothing.</li>\n</ul>\n<hr>\n<h2 id=\"variant-pixel-consumer-raw-full-frame-grid\">Variant — <code>&lt;pixel&gt;</code> consumer (raw full-frame grid)</h2>\n<p><strong>When to prefer it:</strong> the wall/processor wants a raw pixel raster (1 video pixel = 1 LED) and you'd rather author content at the LED resolution than define regions — LED processors, Madrix-style software, seat/audience surfaces. No regions, no averaging, no per-fixture placement.</p>\n<p>Source: <code>/home/casparcg/caspar-build/src-tree/src/modules/pixel/</code> — registered preconfigured-only as <code>pixel</code> (<code>pixel.cpp:29-31</code>), config parsed in <code>consumer/pixel_consumer.cpp:134-190</code>:</p>\n<table><thead><tr><th>Element</th><th>Required</th><th>Range</th><th>Default</th><th>Source</th></tr></thead><tbody>\n<tr><td><code>&lt;protocol&gt;</code></td><td><strong>yes</strong></td><td><code>artnet</code> (only value accepted)</td><td>—</td><td>:148-150</td></tr>\n<tr><td><code>&lt;host&gt;</code></td><td>no</td><td>IP</td><td>127.0.0.1</td><td>:152</td></tr>\n<tr><td><code>&lt;port&gt;</code></td><td>no</td><td>uint16</td><td>6454</td><td>:153</td></tr>\n<tr><td><code>&lt;universe&gt;</code></td><td>no</td><td>0–32767</td><td>0</td><td>:155-157</td></tr>\n<tr><td><code>&lt;start-address&gt;</code></td><td>no</td><td>1–512</td><td>1</td><td>:159-161</td></tr>\n<tr><td><code>&lt;type&gt;</code></td><td><strong>yes</strong></td><td><code>luma</code> \\</td><td><code>rgb</code> \\</td><td><code>rgbw</code> \\</td><td><code>rgbx</code></td><td>—</td><td>:166-174</td></tr>\n<tr><td><code>&lt;coef&gt;</code> → <code>&lt;r&gt;&lt;g&gt;&lt;b&gt;&lt;w&gt;</code></td><td>no</td><td>&gt; 0, normalized to max(r,g,b)</td><td>1.0</td><td>:176-182, color_grader.h:46-53</td></tr>\n<tr><td><code>&lt;gamma&gt;</code></td><td>no</td><td>0.1–10</td><td>1.0</td><td>:179, :184-185</td></tr>\n</tbody></table>\n<p>Behavior: <strong>every frame</strong> (no refresh-rate), every pixel row-major → 1/3/4 bytes per pixel (<code>pixel_consumer.cpp:96-102</code>), gamma LUT then coefficients (color_grader.h:56, <code>rgbw</code> white = <code>min(r,g,b)</code> :72-80, <code>rgbx</code> pads a zero byte :82-86), packed into consecutive 512-channel ArtDMX universes auto-incrementing from <code>&lt;universe&gt;</code> (<code>artdmx_sink.h:97-108</code>). 8-bit channels only (:140-141).</p>\n<p><strong>The grid is the video mode</strong> — so give the wall channel a custom mode matching the LED grid. Example: 96×54 wall, RGB = 15,552 channels = 31 universes starting at universe 0:</p>\n<pre><code class=\"language-xml\">&lt;channel&gt;\n    &lt;video-mode&gt;96x54p50&lt;/video-mode&gt;   &lt;!-- custom mode; generator supports custom modes\n                                             (src/config/config-generator-custom-modes.js) --&gt;\n    &lt;consumers&gt;\n        &lt;pixel&gt;\n            &lt;protocol&gt;artnet&lt;/protocol&gt;\n            &lt;host&gt;192.168.1.50&lt;/host&gt;\n            &lt;port&gt;6454&lt;/port&gt;\n            &lt;universe&gt;0&lt;/universe&gt;\n            &lt;start-address&gt;1&lt;/start-address&gt;\n            &lt;type&gt;rgb&lt;/type&gt;\n            &lt;gamma&gt;2.2&lt;/gamma&gt;\n        &lt;/pixel&gt;\n    &lt;/consumers&gt;\n&lt;/channel&gt;</code></pre>\n<p>Sizing rule: <code>universes = ceil(W × H × bytes_per_pixel / 512)</code>. Keep it sane — this sends at full channel rate; 50 fps × 31 universes is ~1,550 packets/s, fine on a dedicated lighting VLAN, silly over WiFi. Do <strong>not</strong> point it at a 1080p mode (12,150 universes/frame).</p>\n<p>Verification: on send failure it logs one error with the consumer tag <code>pixel[&lt;ch&gt;|&lt;mode&gt;]</code> and later <code>: Connection restored</code> when the socket recovers (<code>pixel_consumer.cpp:104-112,118-121</code>). Config errors abort startup: <code>Unsupported or unspecified protocol.</code> (:150), <code>Unsupported or unspecified pixel type.</code> (:174), <code>Invalid universe number.</code> / <code>Invalid start address.</code> / <code>Invalid gamma value.</code> (:157,161,185).</p>\n<p>Applying = same restart flow as step 4, same regen caveat.</p>","WALKTHROUGH_PIXELMAP_FIXTURES":"<h1 id=\"walkthrough-live-editable-pixel-map-fixtures-highascg-js-pipeline\">Walkthrough — live-editable pixel-map fixtures (HighAsCG JS pipeline)</h1>\n<p><strong>Badge:</strong> [DEPRECATED — legacyJsPixelmap flag] · [LIVE-EDITABLE] — everything here re-applies on save with <strong>no Caspar restart</strong> and no config regen. <strong>Engine:</strong> WO-179 sampling pipeline (<code>src/sampling/</code>, <code>src/artnet/</code>, <code>client/lib/dmx-state.js</code>). Cites are <code>file:line</code> in this repo. <strong>Landscape / engine choice:</strong> <a href=\"#/ARTNET_PIXEL_MAPPING\" data-wiki-page=\"1\">ARTNET_PIXEL_MAPPING.md</a></p>\n<p><strong>WO-242:</strong> this whole engine is deprecated in favor of native <code>pixelmap</code> screen destinations (dedicated Caspar channel + native <code>&lt;artnet&gt;</code> consumer — see <a href=\"#/WALKTHROUGH_ARTNET_LED_WALL\" data-wiki-page=\"1\">WALKTHROUGH_ARTNET_LED_WALL.md</a> and the &quot;Pixel-map screen (native) — primary flow&quot; section of <a href=\"#/ARTNET_PIXEL_MAPPING\" data-wiki-page=\"1\">ARTNET_PIXEL_MAPPING.md</a>). The code below still works exactly as documented, but its fixture-creation UI is now hidden unless <code>settings.ui.legacyJsPixelmap</code> is set to <code>true</code>. (Note: the Device View &quot;mapping nodes&quot; band — &quot;+ Add mapping node&quot; button — is a separate canvas-splitting feature and remains fully active; see the Pixel Map tab inspector and ARTNET_PIXEL_MAPPING.md for clarity.) Only this engine gives you <strong>sACN output</strong> and <strong>restart-free live remapping</strong> — the native path is Art-Net only and requires a config regen + Caspar restart per change. Re-enable the flag if you specifically need those two properties; otherwise use the native flow.</p>\n<p>Use this engine when the mapping changes during the show: pixel tape you re-aim after a set change, house-light sACN washes, anything you want to nudge from FOH mid-act.</p>\n<hr>\n<h2 id=\"1-how-it-runs-so-you-know-what-youre-operating\">1. How it runs (so you know what you're operating)</h2>\n<ol>\n<li>When <code>settings.dmx.enabled</code> is true and fixtures exist, the server attaches a sampling tap per source program channel: default is Caspar <code>ADD &lt;ch&gt; STREAM udp://…</code> (MPEG-TS) decoded by a local ffmpeg with <code>scale=…:flags=area</code> (<code>src/sampling/dmx-sampling-ingress.js:137-160</code>); fallback/opt-in FILE mode uses a raw-RGB FIFO on dedicated consumer slot <strong>97</strong> (<code>dmx-sampling-ingress.js:17,217-250</code>) — both are dynamic AMCP consumers, not config-file consumers.</li>\n<li>The tap is downscaled to 10–50 % of PGM, sized from the densest fixture grid ×2 safety margin (<code>src/sampling/dmx-sampling.js:60-86</code>).</li>\n<li>A worker thread samples each fixture's grid cells (center pixel or region average), applies brightness → gamma LUT → colorOrder (<code>src/sampling/sampling-worker.js:60-172</code>).</li>\n<li><code>DmxOutput.send()</code> transmits per fixture: Art-Net via dmxnet or sACN via the sacn package (<code>src/sampling/dmx-output.js:26-91</code>).</li>\n<li>Every settings save re-runs <code>SamplingManager.updateConfig()</code> live (<code>index.js:316</code>); it only restarts its own taps when the channel plan actually changed (<code>dmx-sampling.js:120-161</code>).</li>\n</ol>\n<h2 id=\"2-the-fixture-model-what-you-are-editing\">2. The fixture model (what you are editing)</h2>\n<p>Stored in <code>settings.dmx.fixtures</code>; created with these defaults (<code>client/lib/dmx-state.js:69-99</code>):</p>\n<pre><code class=\"language-json\">{\n  &quot;id&quot;: &quot;fixture_xyz&quot;,\n  &quot;sample&quot;: { &quot;x&quot;: 0, &quot;y&quot;: 0, &quot;w&quot;: 200, &quot;h&quot;: 200 },\n  &quot;rotation&quot;: 0,\n  &quot;mirrorH&quot;: false,\n  &quot;mirrorV&quot;: false,\n  &quot;sampleMode&quot;: &quot;average&quot;,\n  &quot;sourceChannel&quot;: 1,\n  &quot;grid&quot;: { &quot;cols&quot;: 1, &quot;rows&quot;: 1 },\n  &quot;colorOrder&quot;: &quot;rgb&quot;,\n  &quot;universe&quot;: 1,\n  &quot;startChannel&quot;: 1,\n  &quot;protocol&quot;: &quot;artnet&quot;,\n  &quot;destination&quot;: &quot;127.0.0.1&quot;,\n  &quot;gamma&quot;: 2.2,\n  &quot;brightness&quot;: 1.0\n}</code></pre>\n<ul>\n<li><code>sample</code> is in <strong>program-channel pixels</strong> of <code>sourceChannel</code> (canvas syncs to the live PGM resolution, <code>dmx-state.js:144-152</code>).</li>\n<li><code>grid</code> splits the sample rect into cols×rows cells → one DMX color per cell, row-major.</li>\n<li><code>colorOrder</code> is a free string: <code>rgb</code>, <code>grb</code>, <code>bgr</code>… map raw channels; any order containing <code>w</code>/<code>a</code> switches to white-extraction (<code>w = min(r,g,b)</code>, amber = <code>min(r,g)×0.5</code>) — e.g. <code>rgbw</code>, <code>rgbwa</code> (<code>src/sampling/sampling-worker.js:30-58</code>).</li>\n<li><code>protocol</code>: <code>artnet</code> or <code>sacn</code>. sACN multicasts when <code>destination</code> is empty (<code>dmx-output.js:77-80</code>); Art-Net unicasts to <code>destination</code> or broadcasts to 255.255.255.255 (<code>dmx-output.js:51-57</code>).</li>\n</ul>\n<h2 id=\"3-ui-steps-as-the-code-has-them-today\">3. UI steps (as the code has them today)</h2>\n<ol>\n<li><strong>Enable the engine:</strong> <code>settings.dmx.enabled</code> + <code>fps</code> (+ <code>debugLogDmx</code>). The settings modal currently passes the <code>dmx</code> object through untouched (<code>client/components/settings-modal-logic.js:108</code>) — there is no dedicated enable checkbox in the modal yet; flip it via the settings API (<code>POST /api/settings</code> with <code>dmx.enabled: true</code>) or devtools <code>dmxState.setEnabled(true)</code>. Honest gap, see §6.</li>\n<li><strong>Pixel Map tab:</strong> the workspace has a <code>pixelmap</code> tab (<code>client/index.html:91</code>, activation wiring <code>client/app.js:124-127</code>). While it is active, the main <strong>Inspector</strong> panel hosts the fixture properties editor (<code>client/components/inspector-panel.js:47-52,74-79</code>).</li>\n<li><strong>Fixture properties</strong> (<code>client/components/fixture-inspector.js:33-71</code>): Name, <strong>Universe</strong>, <strong>Start Ch</strong>, <strong>Protocol</strong> (Art-Net / sACN dropdown), <strong>Destination</strong>, <strong>Color order</strong>, <strong>Source channel</strong>, <strong>Grid cols/rows</strong>, <strong>Brightness</strong>, <strong>Rotation (°)</strong>, <strong>Mirror H / Mirror V</strong> checkboxes, <strong>Sample mode</strong> (<code>Single pixel (center)</code> / <code>Region average</code>), <strong>Delete fixture</strong>. Every field saves debounced (450 ms, <code>dmx-state.js:22,154-170</code>) and the server re-applies live.</li>\n<li><strong>Creating a fixture — current gap:</strong> <code>dmxState.addFixture()</code> exists (<code>client/lib/dmx-state.js:69</code>) but no button in the current build calls it, and the &quot;DMX Fixtures&quot; template section of the mapping browser (<code>client/components/pixel-mapping-browser.js</code>) is not mounted. Today fixtures are created by writing <code>settings.dmx.fixtures</code> (settings API) or <code>dmxState.addFixture({...})</code> from the browser console; after that, all editing is first-class UI via the Inspector.</li>\n<li><strong>Don't confuse it with the Mapping Preview overlay</strong> (<code>client/components/pixel-map-editor.js</code>): that editor (toolbar &quot;Mapping Preview&quot; / &quot;Delete Slice&quot; / &quot;Close Preview&quot;) belongs to Device View <strong>pixel-mapping nodes</strong> — video slicing onto DeckLink/GPU outputs (<code>src/config/pixel-mapping-config.js</code>) — not DMX fixtures.</li>\n<li><strong>Live feedback:</strong> sampled colors stream to the UI over WebSocket <code>dmx:colors</code> (<code>src/sampling/dmx-sampling.js:363-369</code>, <code>client/lib/app-ws-handlers.js:122</code>). For on-console verification enable <code>debugLogDmx</code>: once per second the app log prints <code>[DMX] debug: &lt;id&gt; artnet→&lt;dest&gt; u&lt;uni&gt;@&lt;start&gt;: [ch,ch,…]</code> (<code>dmx-sampling.js:341-361</code>).</li>\n</ol>\n<h2 id=\"4-live-re-mapping-mid-show\">4. Live re-mapping mid-show</h2>\n<p>This is the whole point: change <code>sample</code> x/y/w/h, rotation, mirror, grid, universe, even protocol — the save debounce fires, the server updates config, and DMX follows within ~a second. No Caspar restart; the tap itself only re-arms when the source-channel plan changes (different channel, denser grid), which costs a sub-second <code>REMOVE</code>/<code>ADD</code> of the sampling consumer only — program output is untouched.</p>\n<p>Watch the app log for the pipeline's own lines:</p>\n<ul>\n<li><code>[DMX] Ingress MPEG-TS UDP (Caspar STREAM + local ffmpeg) @ 25 fps — channels: 1</code> (<code>dmx-sampling.js:167-172</code>)</li>\n<li><code>[DMX] Channel 1: 1920x1080 PGM @ 10% → 192x108 buffer</code> (<code>dmx-sampling.js:184-187</code>)</li>\n<li><code>[DMX] Channel 1: receiving pixel data (N bytes first chunk)</code> (<code>dmx-sampling.js:233-238</code>)</li>\n<li><code>[DMX] Channel 1: no pixel data after 5s (…)</code> — the 5-second ingress watchdog (<code>dmx-sampling-ingress.js:76-88</code>)</li>\n</ul>\n<h2 id=\"5-inputs-the-other-direction\">5. Inputs (the other direction)</h2>\n<ul>\n<li>Art-Net <strong>input</strong> listener for the global border effect: <strong>default OFF</strong> since WO-179 (<code>src/artnet/artnet-slot-config.js:10-16</code>, <code>src/artnet/artnet-receiver.js:60</code>); enable per border slot in its inspector. Port 6454.</li>\n<li><strong>sACN input</strong> alternative on port 5568 (<code>src/artnet/sacn-receiver.js</code>), selected by the slot's <code>lightingProtocol</code> (<code>artnet-slot-config.js:18-23</code>).</li>\n</ul>\n<h2 id=\"6-honest-limits-numbers-from-code\">6. Honest limits (numbers from code)</h2>\n<ul>\n<li><strong>Averaging stride:</strong> <code>Region average</code> caps at ~64 samples per cell — stride = <code>ceil(cellDim/8)</code> per axis (<code>src/sampling/fixture-transform.js:50-55</code>), on a buffer that is already only 10–50 % of PGM. Fine for washes and tape; a 1-cell fixture covering the whole frame is averaging ≤64 of ~20k buffer pixels. The native <code>&lt;artnet&gt;</code> consumer weighs <strong>every</strong> pixel (see the LED-wall walkthrough) — prefer it where color fidelity is critical and the rig is fixed.</li>\n<li><strong>Rates:</strong> default 25 fps, configurable via <code>settings.dmx.fps</code> (<code>dmx-sampling.js:29,145</code>); the extra encode→UDP→decode hop adds latency the native consumers don't have.</li>\n<li><strong>Art-Net universes 0–15 only</strong> in this path (dmxnet clamp, <code>src/sampling/dmx-output.js:30</code>). Use sACN for higher universes.</li>\n<li><strong>No fixture-creation button yet</strong> (§3.4) and enable/fps have no settings-modal controls yet (§3.1).</li>\n<li><code>rotation</code> + <code>Region average</code> averages the rotated cell's <strong>bounding box</strong>, not the exact rotated polygon (<code>sampling-worker.js:93-126</code>) — slight bleed on 45° tape runs.</li>\n</ul>\n<h2 id=\"7-not-yet-planned-dont-look-for-it-in-the-ui\">7. NOT YET — planned, don't look for it in the UI</h2>\n<ul>\n<li><strong>[PLANNED] WO-228 hybrid sws sampling:</strong> move averaging into a Caspar-side ffmpeg consumer (<code>scale=cols:rows:flags=area</code>) feeding this same live-editable output path — kills the stride cap and most of the CPU cost while staying restart-free. Draft in <code>work/work-orders/225_WO_ARTNET_INVESTIGATION.md</code> (WO-228 section); status per WO-233: still the recommended interim architecture, not implemented.</li>\n<li><strong>[PLANNED] WO-180 GDTF import/export:</strong> create fixtures from vendor <code>.gdtf</code> files and export ours for consoles (<code>work/work-orders/180_WO_GDTF_FIXTURE_IMPORT_EXPORT.md</code>). Research stage, nothing shipped.</li>\n</ul>"}};
