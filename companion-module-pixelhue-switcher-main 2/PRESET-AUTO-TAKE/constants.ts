/**
 * Companion "Load Preset" dropdown value for Program (Auto-Take).
 * Must match the choice id in src/actions.ts → loadPreset → Load in dropdown.
 */
export const LOAD_IN_PROGRAM_AUTO = -1 as const
