node_modules/
__pycache__/
*.pyc
.DS_Store
*.log
npm-debug.log*
.env
.env.local
.env.deploy
.module-state.json
.module-state.json.tmp
.highascg-state.json
.highascg-state.json.tmp
.private/
autosave.json
projects/
highascg.config.json

# FFmpeg waveform JSON cache (GET /api/local-media/.../waveform)
data/waveforms/

# Local reference clones (nested git); fetch upstream if needed
.reference/


# Operator UI dev env (local only)
.env.development

# Vite production bundle (built from client/ via npm run build:client)
dist-web/
dist/

# Local/runtime-heavy folders (never commit)
CasparCG_Enhanced-main/
cef-cache/
bin/
/media/
/lib/
_media/
data/
!data/nvidia-driver-guide.json

# CasparCG daily logs (runtime; can grow to hundreds of MB)
log/

# Live border state polled by pip_* templates (written by Art-Net / inspector)
template/global-border-live-*.json
template/global-border-live-*.json.tmp

# Client integration notes (local only — not on GitHub)
for_client/


refs/

# User/system local artifacts
core
health.json
libndi.so.6
casparcg.config
config/casparcg.config

# Local live-USB flash settings (copy from flash-iso.conf.example)
tools/live-usb/flash-iso.conf
tools/eggs/live-usb/flash-iso.conf

# User-specific runtime configs (keep defaults/examples in source, ignore live overrides)
config/*.json
!config/*.example.json
!config/default*.json
!config/exfat-sync.json

# Local-only trees (never on GitHub)
audio_testing/
From_client/
/deprecated/
!scripts/deprecated/
logs_lap/
CT-SS-master/

# Syncthing conflict copies (never commit)
*.sync-conflict-*

# Local Caspar config backups / duplicates
config/casparcg.config.bak.*
config/*copy.config

# Runtime scratch / local timelines (like projects/)
.w
work/logs/
work/multi-logs/
timelines/