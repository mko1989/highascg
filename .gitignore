node_modules/
__pycache__/
*.pyc
.DS_Store
*.log
npm-debug.log*
.env
.env.local
.env.deploy
.module-state.json
.module-state.json.tmp
.highascg-state.json
.highascg-state.json.tmp
BUILD_STAMP
.highascg-build-stamp
.private/
autosave.json
projects/
highascg.config.json

# FFmpeg waveform JSON cache (GET /api/local-media/.../waveform)
data/waveforms/

# Local reference clones (nested git); fetch upstream if needed
.reference/


# Operator UI dev env (local only)
.env.development

# Vite production bundle (built from client/ via npm run build:client)
dist-web/
dist/
dist-map/

# Generated map data
client/assets/map-data.json
dist-web/assets/map-data.json

# Local/runtime-heavy folders (never commit)
CasparCG_Enhanced-main/
cef-cache/
bin/
/media/
/lib/
_media/
data/*
!data/nvidia-driver-guide.json
!data/default-empty-project.json
!data/known-gpus.json

# CasparCG daily logs (runtime; can grow to hundreds of MB)
log/

# Live border state polled by pip_* templates (written by Art-Net / inspector)
template/global-border-live-*.json
template/global-border-live-*.json.tmp

# Client integration notes (local only — not on GitHub)
for_client/


refs/

# User/system local artifacts
core
health.json
libndi.so.6
casparcg.config
config/casparcg.config

# Local live-USB flash settings (copy from flash-iso.conf.example)
tools/live-usb/flash-iso.conf
tools/eggs/live-usb/flash-iso.conf

# Runtime configs: factory defaults tracked in git (regenerate: npm run config:write-defaults)
config/*.json
!config/general.json
!config/caspar.json
!config/server.json
!config/osc.json
!config/ui.json
!config/editor_defaults.json
!config/audio_routing.json
!config/dmx.json
!config/rtmp.json
!config/usb_ingest.json
!config/project_scoped_media.json
!config/streaming_channel.json
!config/record_outputs.json
!config/audio_outputs.json
!config/stream_outputs.json
!config/caspar_server.json
!config/screen_destinations.json
!config/device_graph.json
!config/companion.json
!config/plugins.json
!config/replication.json
!config/security.json
!config/exfat-sync.json
# Machine-local config (never commit)
config/hardware-identity.json
config/replication-local-identity.json
config/tailscale.json
config/.highascg-state.json

# Local-only trees (never on GitHub)
audio_testing/
From_client/
/deprecated/
!scripts/deprecated/
logs_lap/
CT-SS-master/

# Syncthing conflict copies (never commit)
*.sync-conflict-*

# Local Caspar config backups / duplicates
config/casparcg.config.bak.*
config/*copy.config

# Runtime scratch / local timelines (like projects/)
.w
work/logs/
work/multi-logs/
timelines/
.operator-firefox-profile/
config/*.tmp

# WO-319: self-signed TLS proxy cert/key — never commit
config/certs/

# WO-427: GUI-uploaded DeckLink driver packages (250MB+, user-supplied, never committed)
/vendor/

# WO-498: the nginx :80 proxy was removed. config/ is restored from the stick/bridge by
# exfat-sync, which resurrects this file on disk — ignoring it stops that stray being re-committed.
config/nginx/
