# Open issues (operator / production)

> **06.08.26 — "close all open WOs" sweep (WO-445..451 session):** 58 stale rows corrected to
> their WO status lines; WO-176/190/215/221/247/262/390/403/404 closed (see each WO's closure
> note). Everything still open is an owner tick/action/decision consolidated in
> **[checklist06.08.26_close_all_wos.md](./checklist06.08.26_close_all_wos.md)** — ticking that
> file closes the queue.

Lightweight index of **unresolved** work. Close items here when verified on hardware.

**Status legend:** 🟡 Implemented — code landed, needs highascg service restart + browser reload, then operator/hardware sign-off. 🔴 Open — work remaining. ✅ Done — verified.

> **Batch note (2026-07-13/14):** WO-155 through WO-203 were implemented by agents on this box. **Server-side changes are NOT active until the `highascg` service restarts**; `dist-web/` is rebuilt, so a browser reload picks up client fixes. Owner re-reports of screen-2 takes / timer-at-00 / devices-tab were the un-restarted fixes. Exception: the media scanner (WO-162) was fixed live and is running.

| ID | Summary | Tracker | Status |
|----|---------|---------|--------|
| LOOKS-RESTART | Clip restarted on every looks-editor param edit; PRV mirror; stale PRV thumbnail | [WO-155](./work-orders/155_WO_LOOKS_EDITOR_CLIP_RESTART_AND_PRV_MIRROR.md) | 🟡 Implemented |
| ROUTE-SELFLOOP | Self-route guard (all playout paths); multiview auto re-apply + refresh button | [WO-156](./work-orders/156_WO_ROUTE_SELF_LOOP_GUARD_AND_MULTIVIEW_RESTART_REAPPLY.md) | 🟡 Implemented |
| AUDIO-STRIP-ROUTING | Screens row above stereo-pair on media strips (UI done); cross-screen fan-out model | [WO-157](./work-orders/157_WO_AUDIO_MIXER_SCREEN_THEN_PAIR_ROUTING.md) | 🟡 UI done / 🔴 fan-out owner-gated |
| CROP-UX | Crop visible in editor, px values, visual handles, crop-aware PIP borders | [WO-158](./work-orders/158_WO_LOOKS_CROP_VISUAL_HANDLES_PIXELS_AND_BORDER_AWARENESS.md) | 🟡 Implemented |
| CH3-BLACK-PREVIEW | Stale jpeg truncation, blocklist WS bootstrap + reconnect reset, badge states | [WO-159](./work-orders/159_WO_CH3_BLACK_COMPOSE_PREVIEW_STALE_JPEG_AND_BLOCKLIST_UX.md) | 🟡 Implemented |
| BANK-LAYERS | Consecutive layers 10+/110+/210+, PIP band 260-979, migration; PGM-only on real bank crossfades | [WO-160](./work-orders/160_WO_BANK_LAYER_SCHEME_90_LAYERS_AND_PGM_ONLY_BANKS.md) | 🟡 Implemented |
| CONFIG-LIFECYCLE | Atomic XML writes, save mutex, configVersion; stale-file/backup deletions | [WO-161](./work-orders/161_WO_CONFIG_LIFECYCLE_ATOMIC_WRITES_STALE_FILES_BACKUPS.md) | 🟡 Implemented / 🔴 deletions owner-gated |
| SCANNER-CONFIG | Media scanner revived live (was crash-looping since Jun 28); eggs guard + seeding | [WO-162](./work-orders/162_WO_SCANNER_CONFIG_RESTORE_AND_EGGS_RESET_GUARD.md) | ✅ Scanner live / 🟡 eggs guard needs next produce |
| MAP-REMOVAL | Project map removed from main service (GitHub Pages only) | [WO-163](./work-orders/163_WO_REMOVE_PROJECT_MAP_FROM_MAIN_SERVICE.md) | ✅ Done |
| INFO-POLL | Live-audio watchdog INFO 6-10 every 15 s → probe-on-suspicion only | [WO-164](./work-orders/164_WO_LIVE_AUDIO_WATCHDOG_QUIET_INFO_PROBE.md) | 🟡 Implemented |
| EYES+MODALS | Per-process CPU/RSS in status-eye hover; settings/logs modals undimmed | [WO-165](./work-orders/165_WO_STATUS_EYES_PROCESS_USAGE_AND_MODAL_BACKDROP.md) | 🟡 Implemented |
| LIVE-AUDIO-SWAP | Device change from the layer inspector, no Caspar restart | [WO-166](./work-orders/166_WO_LIVE_AUDIO_DEVICE_SWAP_IN_INSPECTOR.md) | ✅ Done (06.08 status sweep — per WO status line) |
| TEMPLATE-REFRESH | Refresh re-fetches template catalog client-side | [WO-167](./work-orders/167_WO_TEMPLATE_REFRESH_CLIENT_FETCH.md) | ✅ Done (06.08 status sweep — per WO status line) |
| EGGS-EXCLUDES | `.private` identity leak excluded; trash/backup purge; defaults manifest. **Rotate identities on boxes cloned from pre-2026-07-13 ISOs.** | [WO-168](./work-orders/168_WO_EGGS_EXCLUDES_CLEANUP_AND_FACTORY_DEFAULTS.md) | 🟡 Implemented (verify on next produce) |
| COUNTDOWN | Countdown/timer CG template + inspector + stateless API; multi-instance | [WO-169](./work-orders/169_WO_COUNTDOWN_TIMER_TEMPLATE.md) | 🟡 Implemented |
| COMPANION-PARITY | Streaming/record + multiview + timeline + countdown actions in the companion module (v1.0.2) | [WO-170](./work-orders/170_WO_COMPANION_MODULE_API_PARITY.md) | ✅ Done (06.08 status sweep — per WO status line) |
| MATH-INPUTS | Math expressions in all number inputs (44 fields + WO-171-B sweep) | [WO-171](./work-orders/171_WO_MATH_IN_ALL_NUMBER_INPUTS.md) | 🟡 Implemented |
| STREAM-RECORD | Device-view→stream source sync (missing export), record audio filter, layout-aware downmix, flag policy | [WO-172](./work-orders/172_WO_STREAMING_RECORD_SOURCE_SYNC_FLAGS_AUDIO.md) | 🟡 Implemented |
| TIMELINE-BATCH | Schedule-style batched AMCP (all 3 phases; >99% volume reduction on fades) | [WO-173](./work-orders/173_WO_TIMELINE_SCHEDULE_BATCHED_AMCP.md) | 🟡 Implemented |
| ROUTE-AUDIO-PICK | Choose source audio channels on route layers | [WO-174](./work-orders/174_WO_ROUTE_SOURCE_AUDIO_CHANNEL_PICK.md) | 🟡 Implemented |
| FTB-CLEAR | FTB fades 0.5 s at project fps then clears; Unblock button removed | [WO-175](./work-orders/175_WO_FTB_FADE_THEN_CLEAR_REMOVE_UNBLOCK.md) | 🟡 Implemented |
| PRV-RECHECK | PRV re-test after restart (superseded largely by WO-199 root cause) | [WO-176](./work-orders/176_WO_PRV_CURRENT_LOOK_VERIFY_AFTER_RESTART.md) | ✅ Done 06.08 — closed as duplicate-of-155/159 (A176.1: dozens of restarts since, no recurrence) |
| BORDER-COLOR | Border color stomped by mixer_update WS echo → whitelist + recent-edit guard | [WO-177](./work-orders/177_WO_BORDER_COLOR_STOMPED_BY_MIXER_UPDATE_ECHO.md) | 🟡 Implemented |
| MINI-SLIDERS | Slim sliders under 54 bounded number inputs | [WO-178](./work-orders/178_WO_MINI_SLIDERS_UNDER_NUMBER_INPUTS.md) | ✅ Done (06.08 status sweep — per WO status line) |
| LIGHTING-IO | Art-Net listener default OFF; sACN input; region-averaged sampling; mirror H/V | [WO-179](./work-orders/179_WO_ARTNET_SACN_DEFAULTS_AND_PIXELMAP_SAMPLING.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| GDTF | GDTF fixture import/export | [WO-180](./work-orders/180_WO_GDTF_FIXTURE_IMPORT_EXPORT.md) | 🔴 Open (after WO-179 QA) |
| PGM-ONLY-500 | Take 500 (latent missing import) + exit-edit 400 on PGM-only mains | [WO-181](./work-orders/181_WO_PGM_ONLY_TAKE_500_AND_EXIT_EDIT_400.md) | 🟡 Implemented (needs restart) |
| EYES-CPU | 1400% CPU display → normalized machine share | [WO-182](./work-orders/182_WO_EYES_CPU_NORMALIZATION.md) | 🟡 Implemented |
| MIXER-TIMELINE | Timeline clip strips in the mixer after timeline takes | [WO-183](./work-orders/183_WO_AUDIO_MIXER_TIMELINE_TAKE_INPUTS.md) | ✅ Done (06.08 status sweep — per WO status line) |
| THUMB-SPAM | Media-thumb corrupt/truncated spam → atomic writes, serve guards, retry-once | [WO-184](./work-orders/184_WO_IMAGE_CORRUPT_TRUNCATED_THUMB_SPAM.md) | 🟡 Implemented |
| GLOBAL-PLAY-MAIN | Global play in editor targets the edited look's main | [WO-185](./work-orders/185_WO_GLOBAL_PLAY_WRONG_MAIN_IN_EDITOR.md) | 🟡 Implemented |
| TIMER-PANEL | Collapsible timer control panel (selector, HH/MM/SS, center default) | [WO-186](./work-orders/186_WO_TIMER_CONTROL_PANEL.md) | ✅ Done (06.08 status sweep — per WO status line) |
| TEMPLATE-THUMBS | Rendered "shown-state" template snapshots in the looks editor | [WO-187](./work-orders/187_WO_TEMPLATE_THUMBNAILS_IN_LOOKS_EDITOR.md) | ✅ Done (06.08 status sweep — per WO status line) |
| UPDATE-GAPS | dist-web in drops; DeckLink install API/UI + tar.gz; vendor seeding | [WO-188](./work-orders/188_WO_UPDATE_SYSTEM_GAPS_DISTWEB_DECKLINK.md) | 🟡 Implemented |
| HW-DISPLAY | Settings → System hardware summary | [WO-189](./work-orders/189_WO_SETTINGS_SYSTEM_HARDWARE_DISPLAY.md) | ✅ Done (06.08 status sweep — per WO status line) |
| MV-CROP | Multiview crop mismatch — apply lock + debug endpoint shipped; **owner repro capture needed** (WO §5 procedure) | [WO-190](./work-orders/190_WO_MULTIVIEW_TOP_CROP_MISMATCH.md) | ✅ Closed 06.08 — not reproduced since 15.07; re-open with live capture |
| MV-TIMERS | Per-layer L-rows with timers/progress on the multiview overlay | [WO-191](./work-orders/191_WO_MULTIVIEW_PER_LAYER_TIMERS.md) | 🟡 Implemented |
| TIMER-PANEL-FIX | Live ticking display, duration persistence, presets | [WO-192](./work-orders/192_WO_TIMER_PANEL_LIVE_DISPLAY_PERSIST_PRESETS.md) | 🟡 Implemented |
| SYSTEM-TIME | System time view/NTP/manual set (sudoers via next installer run) | [WO-193](./work-orders/193_WO_SYSTEM_TIME_SETTING.md) | ✅ Done (06.08 status sweep — per WO status line) |
| HOSTNAME-USB | Hostname-from-MAC fail-open logging; self-corrects to highascg7579 next boot | [WO-194](./work-orders/194_WO_HOSTNAME_FROM_MAC_USB_BOOT_FIX.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| MV-OVERLAY-REFINE | Pip rows hidden, L##+filename labels, no top block, PRV mapping fix, instant stale removal | [WO-195](./work-orders/195_WO_MULTIVIEW_OVERLAY_REFINEMENTS.md) | 🟡 Implemented |
| COUNTDOWN-LIFECYCLE | CG cleared on look exit; continuity across same-timer transitions; project-wide panel list | [WO-196](./work-orders/196_WO_COUNTDOWN_LIFECYCLE_TEARDOWN_CONTINUITY_PANEL.md) | 🟡 Implemented |
| LOGS-TABS | Logs/Shortcuts as full tabs in the connection-eye modal | [WO-197](./work-orders/197_WO_LOGS_MODAL_TABS.md) | ✅ Done (06.08 status sweep — per WO status line) |
| COMPOSE-404-LATENCY | Truncation race closed; WO-155 settle-nudge reverted (latency); client retry | [WO-198](./work-orders/198_WO_COMPOSE_PREVIEW_404_RACE_AND_LATENCY.md) | 🟡 Implemented |
| PRV-BANK-LEAK | PRV received bank-B layers → PRV pinned to logical everywhere + orphan sweep | [WO-199](./work-orders/199_WO_PRV_BANK_MAPPING_LEAK.md) | 🟡 Implemented |
| CSP-MATH | CSP-safe math parser (new Function was blocked; feature silently dead) | [WO-200](./work-orders/200_WO_CSP_SAFE_MATH_EVALUATOR.md) | ✅ Done (06.08 status sweep — per WO status line) |
| MV-DEADLOCK+FPS | Apply chain un-poisoned; compose consumer honors composePreview.fps (-r) | [WO-201](./work-orders/201_WO_MV_APPLY_DEADLOCK_AND_COMPOSE_FPS.md) | 🟡 Implemented |
| DEVICES-TAB-PERF | Progressive render + caches + ETag/304 (skeleton regression hotfixed; 304 header fix) | [WO-202](./work-orders/202_WO_DEVICES_TAB_RENDER_PERFORMANCE.md) | 🟡 Implemented |
| MV-TIMER-SIZE | Timer size % (50-300) + top-running-layer highlight, both templates | [WO-203](./work-orders/203_WO_MULTIVIEW_TIMER_SIZE_SETTING.md) | 🟡 Implemented |
| MV-TIMER-POLISH2 | Full-width timer rows, doubled base sizes, ellipsis labels (both templates) | [WO-204](./work-orders/204_WO_MV_TIMER_POLISH_ROUND2.md) | 🟡 Implemented |
| TIMER-PANEL-MIRROR | Panel mirrors the actual on-air timer via server command registry; duration persists via scene patch | [WO-205](./work-orders/205_WO_TIMER_PANEL_TRUE_MIRROR_AND_PERSIST.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| MV-EDITOR-REFRESH | Auto-apply on edit (debounced 800 ms, toggleable); real channel resolutions shown | [WO-206](./work-orders/206_WO_MV_EDITOR_REFRESH_AND_RESOLUTION_DISPLAY.md) | 🟡 Implemented |
| CG-ORPHANS | Tracked CG adds + startup/reconnect sweep (hosts 700-789) + panel "Take off air" button (`/api/countdown/off`) | [WO-207](./work-orders/207_WO_TEMPLATE_CG_ORPHAN_LIFECYCLE.md) | 🟡 Implemented |
| SUBTIMERS | Using a timer creates a named reusable subtimer under the countdown template; canonical layer per screen keeps WO-196 continuity — **drag-into-looks UI removed by WO-210** (model retained) | [WO-208](./work-orders/208_WO_SUBTIMERS_IN_TEMPLATES.md) | ✅ Done (06.08 status sweep — per WO status line) |
| PRV-BANKLESS | PRV exchange staged content on bank-B (110/199) while edits hit logical layers — WO-199's pin was backwards; bankless take mode + B-band sweep + startup pointer normalization | [WO-209](./work-orders/209_WO_PRV_BANKLESS_TAKE_MODE.md) | 🟡 Implemented |
| SCREEN-TIMERS | Timer redesign: panel-owned screen timers on band 980-989, opacity on/off (never destroyed), persisted registry restored on startup, per-look enabled state applied at take | [WO-210](./work-orders/210_WO_SCREEN_TIMERS_PANEL_OWNED.md) | 🟡 Implemented |
| PLAYLIST-STALL | Multi-item playlist stuck on item 1 (LOOP leak) + stall watchdog for video-shorter-than-container media | [WO-211](./work-orders/211_WO_PLAYLIST_FIRST_ITEM_LOOP_STUCK.md) | 🟡 Implemented |
| MV-DOCK+PLAYLIST-LABEL | Editor preview dock full-width (matches output); MV timer labels show playlist `current -> next` (both templates) | [WO-212](./work-orders/212_WO_MV_EDITOR_DOCK_WIDTH_AND_PLAYLIST_LABELS.md) | ✅ Done (06.08 status sweep — per WO status line) |
| PRV-BORDER-STALE | Client preview cache invalidated when server rewrites PRV (borders no longer stale after takes) | [WO-213](./work-orders/213_WO_PRV_BORDER_STALE_AFTER_EXCHANGE.md) | ✅ Done (06.08 status sweep — per WO status line) |
| TIMELINE-MIXER | Timeline strips never rendered (nonexistent programCh() call) — fixed; live route-picker endpoint = follow-up | [WO-214](./work-orders/214_WO_TIMELINE_AUDIO_MIXER_STRIPS_DEAD.md) | 🟡 Implemented |
| MV-CELL-SCALING | Cell fill math measured aspect-true (ch4 IS 1080p) — needs owner screenshot of the wrong-looking cell | [WO-215](./work-orders/215_WO_MV_CELL_SCALING_WITH_TIMERS_DOCK.md) | ✅ Closed 06.08 — not reproduced; owner repro never arrived; re-open with fresh capture |
| COMPANION-WOL | Companion module v1.0.3: stored WoL target (mac/hostname/ip) + wake actions from the Companion host | [WO-216](./work-orders/216_WO_COMPANION_WAKE_ON_LAN.md) | 🟡 Implemented (module tgz packed) |
| PGM-ONLY-SELF-BLANK | Merge take faded the layer it just played into (sticky opacity 0; live outage 15:50, relieved 16:0x) — fade guard + unconditional opacity reset | [WO-217](./work-orders/217_WO_PGM_ONLY_SELF_BLANKING_ORPHAN_FADE.md) | 🟡 Implemented |
| BANK-DRIFT | Producer and MIXER state (CROP) split across banks via skipped visually-equal layers — SWAP TRANSFORMS follows the flip + vacated-layer hygiene (answers the MV crop mystery; supersedes WO-190 H1) | [WO-218](./work-orders/218_WO_BANK_DRIFT_SKIPPED_LAYERS_MIXER_STATE.md) | 🟡 Implemented |
| TIMER-PANEL-V2 | Panel was unstyled (CSS never written for the WO-210 markup) + no settings editor + unwired channel map; hard 980-989 band guard (timer paths can never touch content layers) | [WO-219](./work-orders/219_WO_TIMER_PANEL_V2_POLISH_AND_BAND_GUARD.md) | ✅ Done (06.08 status sweep — per WO status line) |
| PIP-SHADOW | Drop shadow/glow overlays never painted (CEF skips box-shadow-only transparent elements) — compositing-forcing background added to both templates | [WO-220](./work-orders/220_WO_PIP_SHADOW_NOT_RENDERING.md) | 🟡 Implemented (owner visual check) |
| REVIEW-2026-07-15 | Full review wave over WO-155..220 (4 sonnet reviewers): 10 real defects found and ALL FIXED same day (broken timer CG lines, cmd band-guard gap, system-time _ctx crash, bank-pointer disk desync + persistence test isolation, MV mid-drag apply, WO-213 invalidation hole, pause-freeze, all-screens mixer, forceCut passthrough, test hygiene) | [reviews/2026-07-15-*](./reviews/) | ✅ Fixed |
| SCREEN-LABELS | Canonical screenLabels store + /api/screens/label + helper across looks selector/MV/panels/settings UI | [WO-222](./work-orders/222_WO_SCREEN_LABELS_EVERYWHERE.md) | 🟡 Implemented |
| MV-ROUTE-LABELS | MV rows show PGM1/PRV1/screen label instead of "L10 1" (both templates, parity-proven) | [WO-223](./work-orders/223_WO_MV_ROUTE_ROW_FRIENDLY_LABELS.md) | 🟡 Implemented |
| PLAYLIST-MANUAL | "Doesn't loop" = layers were in MANUAL advance (by design); Next ▶ button + /api/playlist/next + manual items hold via LOOP; auto-wrap sim-proven | [WO-224](./work-orders/224_WO_PLAYLIST_LOOP_WRAP_AND_MANUAL_NEXT.md) | 🟡 Implemented |
| FILE-SPLITS | 500-LOC limit: Phase A done (7 client JS files split, mixin pattern); Phases B-E pending (13 files: CSS/server/templates/tools) | [WO-221](./work-orders/221_WO_SPLIT_OVERSIZED_FILES.md) | ✅ Done 06.08 — superseded by WO-114/118/122 splits + CI gate (0 files >500 today) |
| ARTNET-REDESIGN | Investigation DONE: native artnet_consumer confirmed in bin/casparcg (config-only, no dynamic ADD); no local source to rebuild; recommended hybrid = JS output + sws_scale downscaling consumer. Draft WO-228..231 appended | [WO-225](./work-orders/225_WO_ARTNET_INVESTIGATION.md) | 🟡 Awaiting owner arch pick (hybrid vs native) |
| TIMER-OVERLAY-UX | Drop-to-assign (no look), ⏱ icon by screen label/FTB, timer inspector modal (settings+size+position+Fade In/Out via fadeFrames), compact transport next to audio | [WO-226](./work-orders/226_WO_TIMER_SCREEN_OVERLAY_UX.md) | 🟡 Implemented |
| MIXER-8CH | Compact mixer dense mode: >4 strips narrow to 80px + overflow-x; 2-4ch pixel-identical | [WO-227](./work-orders/227_WO_COMPACT_MIXER_8CH_OVERFLOW.md) | 🟡 Implemented |
| COUNTDOWN-FREEZE | Inspector auto-create recursion froze the app — debounced persist + re-entrancy guard (hotfix 2125561) | [WO-208](./work-orders/208_WO_SUBTIMERS_IN_TEMPLATES.md) | ✅ Done (06.08 status sweep — per WO status line) |
| MARIO | Mario as transparent-bg HTML producer via CEF bridge (vendored game) | [WO-232](./work-orders/232_WO_MARIO_HTML_PRODUCER.md) | 🔵 In progress |
| PIXELMAP-NATIVE | Owner picked native-first: `pixelmap` screen destination → dedicated channel + generated `<artnet>` consumer (universe auto-spill), PGM-only takes, device-view UI; JS pixelmap hidden behind `legacyJsPixelmap` (default off — sACN + live remap lost); supersedes hybrid draft WO-228 | [WO-242](./work-orders/242_WO_NATIVE_PIXELMAP_SCREENS.md) | 🟡 Implemented (owner A242.1: create dest → apply config → caspar restart → take) |
| GUI-TRANSPARENT-PREVIEWS | NEW_IDEA (todos15 §NEW_IDEA): operator GUI fullscreen with compose-preview rects transparent over a Caspar screen consumer — Option A: Firefox+picom compositing; Option B: GUI as interactive CEF layer inside a Caspar channel (native input injection in our build, no puppeteer — html producer has no SendMouse/KeyEvent today). Feasibility research running | (WO-243 pending) | 🔵 Investigating |
| CONFIG-APPLY-CLOBBER | Full apply at 2026-07-15 15:42 flushed an earlier in-memory wipe: settings-post rebuilt streamingChannel coercing absent fields to '' (likely vector: device-view start-stream settings-snapshot round-trip). Fixed by WO-244: preserve-on-empty + clearCredentials escape hatch, streamKey never sent to clients (hasStreamKey), RTMP editor in settings modal, smoke in gate. Follow-ups: streamOutputs[] keys still sent unmasked (needs server-side key resolution in start action); generic stale-tab boolean clobber → config revision check (candidate WO-245). Owner: restore-vs-rotate the YouTube key (it's in git history — rotate recommended); config/*.json still dirty | [WO-244](./work-orders/244_WO_STREAM_SECRET_PRESERVE_ON_SAVE.md) | 🟡 Implemented (owner A244.1) |

| OPERATOR-MON-AUTO | Auto-select operator monitor: one connected display → auto ('auto-single'), multiple → explicit screen_N flag or unresolved ('none'), detection failure → flag-only fallback; snapshot exposes {port, mode}. Orchestrator caught+fixed haiku off-by-one (slotOrder 0-based vs 1-based flags), live-verified both scenarios | [WO-246](./work-orders/246_WO_OPERATOR_MONITOR_AUTO_SELECT.md) | 🟡 Implemented (owner A246.1 unplug test; needs service restart) |

| CEF-NO-PUPPETEER | Raw CDP client (ws) replaces puppeteer for all CEF interactive input — same exports, consumers untouched; mock-CDP smoke (12 tests) in gate; puppeteer out of package.json (node_modules kept for rollback). A247.1 live proof (mario keys/mouse) after service restart → then WO-248 removes the whole branch (thumbnail renderers to raw CDP over cached Chrome binary, npm uninstall, ~650MB cache trim) | [WO-247](./work-orders/247_WO_RAW_CDP_NO_PUPPETEER.md) / [WO-248](./work-orders/248_WO_PUPPETEER_FULL_REMOVAL.md) | ✅ DEPRECATED 27.07 — mooted by WO-257; CDP client survives for WO-248 thumbnails |

| OPERATOR-GUI-CEF | Owner architecture: multiview untouched; new `operator_gui` destination → dedicated channel with route layers mirroring compose-preview rects (MIXER FILL) under a CEF web-UI layer (`?cefOperator=1` renders transparent holes + reports rects); screen consumer on operator monitor; interactive zone for raw-CDP input. Client mode hard-gated (inert without query param) | [WO-243](./work-orders/243_WO_OPERATOR_GUI_CEF_CHANNEL.md) | 🟡 Implemented (owner A243.1: create dest → apply config → caspar restart → arm input) |
| AUDIO-PAIR-PGM2 | 8ch→stereo: mixer rows now show the real source pair (was always "1+2" — the reporting lie behind "I hear 3&4"); streaming/record bus gets a Source-audio-pair dropdown threaded into the ffmpeg pan= (out-of-range → warn+fallback, never a dead encode) | [WO-249](./work-orders/249_WO_AUDIO_PAIR_SELECT_PGM2.md) | 🟡 Implemented (owner A249.1) |
| TIMER-GIBBERISH+MV-BARS | Timer picked highest layer incl. stale opposite-bank (110+) for up to 10s after takes → bank-aware+stale-aware pick + timer/border band exclusion; MV master template clipped bars out of the row budget → bar inside row | [WO-250](./work-orders/250_WO_TIMER_BANK_AWARE_AND_MV_BARS.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| PLAYLIST-NEVER-WIRED | `handlePlaylistOscUpdate` (advance/wrap/stall-watchdog) existed since WO-211 but was NEVER subscribed — playlists played 2 items and stopped; git-grep-proven at 80a2df5 and d8e4509. Now dispatched from osc-lifecycle (4Hz throttle) | [WO-251](./work-orders/251_WO_PLAYLIST_OSC_HANDLER_NEVER_WIRED.md) | 🟡 Implemented (owner A251.1) |
| OSC-DURATION-GAP | 2.6-dev binary omits duration+frameTotal on OSC (live-proven) → MV bars dark, watchdog inert. AMCP INFO supplement now injects durations into oscState (fill-only-when-absent, bank-B aware), default 2000ms poll | [WO-252](./work-orders/252_WO_INFO_DURATION_SUPPLEMENT_OSCSTATE.md) | 🟡 Implemented (owner A252.1) |
| MAPPING-NODE-MISDEPRECATED | WO-242's agent gated the mapping-node band (canvas → multi-output splits) believing it was the JS Art-Net pipeline; owner corrected. Un-gated + display renamed "Mapping nodes" + docs re-scoped (orchestrator also fixed agent-mangled curly quotes in the band HTML) | [WO-253](./work-orders/253_WO_MAPPING_NODE_UNDEPRECATE.md) | ✅ Done — landed 6e3c24a; eyeball tick on checklist06.08.26 |

| INCIDENT-2026-07-16 | Boot crash-loop (~7s cycle, 502 storm, phantom "missing assets"): bare `warmInFlight` ReferenceError in cef-interactive-bridge-events.js:122 — latent until WO-243's auto-arm started the bridge every boot. Hotfix self-healed via systemd restart (stable 10:13:30). Same batch: project-store⇄volume-sync require cycle (journal flood), WO-252 INFO poll self-throttle (in-flight guard + 30s timeout backoff — a booting operator-GUI CEF starved AMCP, 120s INFO timeouts wedged the queue), loop-modulo elapsed (looping producers' monotonic clock made GUI timers jump "05↔7686"), cefOperator OSC ingest coalesced to 4Hz | c162c98 / acaccb4 / 219952c | ✅ Fixed (owner: recheck icons after reload) |

| OPERATOR-GUI-V2 | Owner pivot ("shaper and firefox"): CEF-in-Caspar retired. Firefox-ESR kiosk (native input, own process) + always-on-top Caspar consumer shaped via X SHAPE to preview rects with empty input region (clicks pass through video). Surfaces: compose/looks, timeline, mv-edit; modal/drag suppression; Launch/raise routes + inspector button. PoC + window-tree live-verified (depth-2 WM frames → recursive match, shapes on toplevel+client, exact channel-title match). Supersedes OPERATOR-GUI-CEF row; mario keeps the interactive bridge | [WO-255](./work-orders/255_WO_SHAPED_OVERLAY_FIREFOX_OPERATOR_GUI.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |

| OPERATOR-TILES | Compose preview in operator mode = free-tile canvas (MV-style movable/resizable windows, label header + bank-aware progress footer, snap+persistence, defaults mirror PRV|PGM pairs); server untouched — tiles feed the existing layout→shape pipeline | [WO-256](./work-orders/256_WO_OPERATOR_COMPOSE_CANVAS_TILES.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| CEF-INTERACTIVE-REMOVED | Owner: "cef interactive needs to go" — 21 files deleted (bridge/focus/forward/routes/X11 capture/arm UI), callers stripped, webpage-host keeps routing minus input, cef-cdp-client.js stays for WO-248 (now UNBLOCKED); mario loses controls (accepted). Double dangling-require sweeps + runtime loads | [WO-257](./work-orders/257_WO_REMOVE_CEF_INTERACTIVE.md) | ✅ Done 06.08 — CEF interactive removed, clean boots since; mario-plays tick on checklist06.08.26 |
| TIMER-INFO-LEAPFROG | "Bogus running time every 0.5s": client merge let 2s-stale INFO timeSec override fresh OSC elapsed + unconditional remaining override, re-anchoring the clock per flip; INFO is now a gap-filler only (mirrors server guard) | c8d36ba | 🟡 Implemented (owner reload check) |
| TAKE-TWO-PHASE | Transition stagger root-caused (every command awaited a Caspar round-trip) → Phase A (LOADBG+mixers+CG) and Phase B (non-route PLAYs+crossfade) each one BEGIN..COMMIT wire write; 20 round-trips → 2 batches on a 3-layer crossfade; `take_two_phase_batch:false` = byte-identical legacy (verbatim branch + smoke-proven); routes stay staggered. T259.2 stopped legitimately: scene-take-pgm-only.js is DEAD since WO-160b — deletion candidate | [WO-259](./work-orders/259_WO_TWO_PHASE_BATCHED_TAKE.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| BROWSER-SOURCE-V2 | Real Firefox in the desktop dead zone (probe-decided over Xvfb: 1920x648 free area already exists; no-fit fails honestly), x11grab→udp→Caspar relay (input-bridge precedent), grab-follows-window Interact; full client UI (add form 1152x648@25, rows, inspector Interact/Return). CEF = templates only. Zoom-ready pattern. Size cap 1920x648 on current layout | [WO-258](./work-orders/258_WO_BROWSER_SOURCE_VIRTUAL_DISPLAY.md) / [WO-260](./work-orders/260_WO_BROWSER_SOURCE_CLIENT_UI.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |

| OPERATOR-OVERLAY-INVERTED | Operator GUI video: shape FIREFOX (holes over the plain Caspar consumer below), not the consumer on top — video-on-top vanished the instant the GUI was clicked (WM raised focused Firefox). Firefox matched by WM_CLASS on the monitor rect (PID-independent, re-finds manual restarts). Also fixed the stranded-stdin bug (buffered readline vs select → os.read drain) that left the helper shaped-empty. always-on-top→false | [WO-262](./work-orders/262_WO_SHAPE_FEED_STDIN_DIAGNOSIS.md) / [WO-263](./work-orders/263_WO_SHAPE_FIREFOX_NOT_CASPAR.md) | ✅ Done 06.08 — superseded by WO-263 pivot (shipped w/ 264/269); GUI in daily use |

| PUPPETEER-PURGED | Thumbnail renderers migrated off puppeteer onto a headless-chrome-cdp helper (raw CDP over the cached Chrome binary; alpha/omitBackground preserved, render-tested); `npm uninstall puppeteer` removed 24 pkgs (~node_modules 246→223). NOTE: running server needs a restart before thumbnails render again (in-memory old code still lazily requires the now-gone puppeteer). ~/.cache/puppeteer linux-149 dir (~379MB) is owner-deletable | [WO-248](./work-orders/248_WO_PUPPETEER_FULL_REMOVAL.md) | 🟡 Implemented (owner A248.1 + restart) |

| STREAM-CREDS-PROJECT | Owner: "youtube key saved in project and in it only". RTMP url+key now resolve from the active project (config=migration fallback); one-shot migration moves+blanks all config copies (streamingChannel/streamOutputs/deviceGraph connector); server-side resolution at stream-start (client stops sending the key); no raw key in any client-bound payload (masked everywhere, independently verified). projects/ gitignored. Closes WO-244 per-output leak | [WO-261](./work-orders/261_WO_PROJECT_SCOPED_STREAM_CREDENTIALS.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| TIMER-JITTER | Progress bar jumped even with sane numbers: the extrapolation clock re-anchored to the noisy OSC sample every 50ms tick, fighting the smooth 60fps clock. Now absorbs samples within 0.5s of the extrapolated position; re-seeds only on seek/loop/large drift. Follows the frames-as-seconds fix | 6b61f0c | 🟡 Implemented (owner reload) |

| TIMER-7000-SECONDS | Root cause found (NOT frames): looping producers send OSC file/time duration=0, which passed the sanity check and clobbered the WO-252 INFO-injected real duration every 50ms tick → loop-modulo had nothing to wrap → raw monotonic elapsed (462289s) shown. Fix: only a POSITIVE duration is stored (0=absent, INFO survives); monotonic elapsed >12h with no duration is nulled. Live-verified ch1/ch3 → 4.2s not 128h. SERVER change (needs restart) | c36c3f8 | 🟡 Implemented (owner restart) |

| OSC-FLOAT-ENDIAN | **The actual root cause of the whole timer family** ("still jumpy timers", 05↔7686, WO-235 garbage floats, WO-252 "missing" durations, frozen non-audio settings variables): the 2.6-dev binary writes OSC **float32 args little-endian** (spec is BE; its ints ARE correct BE — which is why audio meters, sourced from int volume messages, kept moving while every float-sourced value was garbage). Raw-bytes proof: `file/time` `52b87e40 ae47a140` → BE 3.9e11/-4.5e-11, LE **3.98/5.04** advancing exactly one frame per tick. Fix: in-place float byte-swap on the raw datagram before osc.js parses (port "raw" event), byte order auto-detected+latched via `.../fps` canaries (spec-compliant binary passes through untouched; `osc.floatByteOrder`/`OSC_FLOAT_BYTE_ORDER` override). Live-verified: latched 'le' at boot, elapsed monotonic, clean loop wrap 14.8→0.24 on ch1+ch3. All prior sanity/modulo guards kept as belt-and-suspenders. 5-test smoke in gate. UPSTREAM: fix the binary's OSC writer eventually | smoke-osc-float-endian.test.js | 🟡 Implemented (owner: confirm timers+variables in GUI) |
| OPERATOR-BELOW-LOCK | Two rounds. (1) 4c3c83c landed find_caspar_consumer/BELOW-atom/channel plumbing but apply_holes never CALLED any of it (channel dropped; ABOVE sent to the WM frame Openbox ignores) → fixed: ABOVE on the Firefox CLIENT, consumer stripped of ABOVE + pinned BELOW. (2) Owner re-report "can still click caspar and raise it" exposed the REAL flaw: X SHAPE intersects input with bounding, so 7dade2f's 'input=full makes holes clickable-through-to-firefox' is IMPOSSIBLE — a hole click always reaches the consumer below, and Openbox executes the focused client's restack over FULLSCREEN+ABOVE (synthetic-click proven: BELOW stayed set while stacked on top). Fix: consumer made **input-dead** (empty INPUT shape, frame+client — hole clicks land on the inert desktop, no focus/raise possible) + 2s stacking watchdog (_NET_ACTIVE_WINDOW re-raises Firefox if anything still inverts) + input restored on helper exit. Live-verified: synthetic clicks in both holes no longer flip stacking; watchdog self-healed a pre-existing inversion at startup. Known quirk: clicking the video drops keyboard focus to root until the GUI is clicked again | tools/runtime/operator-shape-overlay.py | 🟡 Implemented (owner: click video + drag windows, caspar must stay under) |

| WO-340 | Shader modal: auto-detect tweakable GLSL variables (#define floats, const vec3 colors, @slider/@color annotations) → color pickers + sliders that rewrite the source literals; v1 zero runtime changes, v2 (follow-up) true live uniforms via CG UPDATE | [WO-340](./work-orders/340_WO_SHADER_PARAMS_UI.md) | 🟡 Implemented v1 (3256730 — owner: open shader modal, tweak) |

| WO-341 | Client sync redesign: server state is the single truth, clients converge (never drop), ONLY user interaction writes — echo-loop kill list, scene.deck ingestion, seq-gap detection, autosave rev-race | [WO-341](./work-orders/341_WO_CLIENT_SYNC_SERVER_TRUTH.md) | 🟡 Kill list 1-7 done 27.07; item 8 = two-client observation drill (owner QA) |

| WO-342 | Deck header empty-space click clears that screen PRV | [WO-342](./work-orders/342_WO_DECK_HEADER_CLICK_CLEARS_PRV.md) | ✅ Implemented (status was stale; handler live at scene-list-column.js:64, dead duplicate removed 9d2f6dd — owner eyeball owed) |
| WO-343 | Look editor shows REAL PRV video on the screen consumer while editing (WO-339 v2; X SHAPE input contract applies). Status row was STALE — implemented `9f0e2d8` + design-2 `ba8970f`; verified present in the tree 28.07 and given its first test (9-test behavioural smoke over the suppression state machine that v1's revert turned on) | [WO-343](./work-orders/343_WO_LOOK_EDITOR_REAL_PRV_VIDEO.md) | 🟡 Implemented — owner QA on the glass (drag layers with PRV watch on) |
| WO-344 | Shader look thumbnails. Status was STALE (crop shipped 465d071; fill compositing already existed). Running it found worse: **some shader looks produced NO thumb at all** — the CDP client's hard 5s per-command timeout vs a fullscreen raymarcher on SwiftShader (`sh-audio` failed every time, now renders in 10.6s at 960×540/25s budget). Also fixed: cache key ignored the template FILE, so a re-saved shader kept its old picture. Synthetic thumb-audio implemented but NOT proven (WO §5 records why) | [WO-344](./work-orders/344_WO_SHADER_LOOK_THUMBNAILS.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-AUDIT | Last-week WO audit + compiled todo list | [todos26.07.26_wo_audit.md](./todos26.07.26_wo_audit.md) | ✅ 2026-07-26 |

| WO-345 | Shader Live workflow: shades-bunny toggles live-shader editor; params ride the RUNNING producer via CG UPDATE hot-recompile | [WO-345](./work-orders/345_WO_SHADER_LIVE_WORKFLOW.md) | ✅ Implemented 27.07 (status was stale; WO's own line: hot-recompile live-verified. Built out by WO-348/355/356/357, all owner-signed 28.07) |

| WO-346 | Compose toolbar: Reset replaces PRT PGM; per-tile PRT capture | [WO-346](./work-orders/346_WO_COMPOSE_TOOLBAR_RESET_AND_TILE_PRT.md) | ✅ Done 27.07 |
| WO-347 | Playlists panel: all project playlists + set start item pre-playout | [WO-347](./work-orders/347_WO_PLAYLISTS_PANEL_PROJECT_WIDE.md) | ✅ Done 27.07 |
| WO-348 | Shader Live: per-param revert, reset-all, 2-column layout | [WO-348](./work-orders/348_WO_SHADER_LIVE_PARAM_RESET_LAYOUT.md) | ✅ Done 27.07 |
| WO-349 | CI: verify job green again + real checks (500-line gate, client build) | [WO-349](./work-orders/349_WO_ci_green_and_real_checks.md) | ✅ Done 27.07 |
| WO-350 | UI polish (bar heights, transition group right) + timeless secs in Playlists compact | [WO-350](./work-orders/350_WO_ui_polish_and_playlist_timeless_compact.md) | ✅ Done 27.07 |
| WO-351 | Monitor picker: black/white prompt + pick wires the devices-tab cable | [WO-351](./work-orders/351_WO_monitor_picker_calm_colors_and_graph_cable.md) | ✅ Done 27.07 |
| WO-352 | Taskbar: system-icon circle chips + raise/recall bug (3 root causes) | [WO-352](./work-orders/352_WO_taskbar_icons_and_raise_fix.md) | ✅ Done 27.07 |
| WO-353 | Playlist item rows minimal + all trashcans → ✕ | [WO-353](./work-orders/353_WO_playlist_rows_minimal_trashcans_x.md) | ✅ Done 27.07 |
| WO-354 | Channel-scoped playlist state; shader MIX hops + thumb eligibility; 2 UI fixes | [WO-354](./work-orders/354_WO_playlist_channel_state_shader_mix_thumbs_ui.md) | ✅ Done 27.07 |
| WO-355 | Playlists PGM-only + stop on take-out; Shader Live expand (labels, blink, clip); clock | [WO-355](./work-orders/355_WO_playlists_pgm_only_shader_editor_expand_clock.md) | ✅ Done 27.07 |
| WO-356 | Shader Live v3: rect groups, wheel steps, child saves, audition (shaders mode) | [WO-356](./work-orders/356_WO_shader_live_v3_children_audition_wheel.md) | ✅ Done 27.07 |
| WO-357 | Shader Live: ▶ take fixed + PGM layer stack (exchange L10 / stack 11-20, MIX) | [WO-357](./work-orders/357_WO_shader_live_take_and_pgm_stack.md) | ✅ Done 27.07 |
| WO-358 | Global border: area rect + glow secondary color cycle + pulse to 0 | [WO-358](./work-orders/358_WO_global_border_area_color2_pulse0.md) | ✅ Done 27.07 |
| WO-359 | Route takes rock solid — cut-take sweep cleared visually-equal sources | [WO-359](./work-orders/359_WO_route_take_consistency.md) | ✅ Done 27.07 (6-cycle live drill) |
| WO-360 | Playout status surfacing: missing media / failed inputs visibly reported | [WO-360](./work-orders/360_WO_playout_status_surfacing.md) | ✅ Done 28.07 (probe-verified; route-to-empty deferred) |
| WO-361 | Companion module dev-mode workflow | [WO-361](./work-orders/361_WO_companion_module_dev_mode.md) | ✅ Done 28.07 (dev-mode.sh; --extra-module-path was already configured) |
| WO-362 | Routed looks (route://1-10) reliability + shader CG ADD pre-fade reorder | [WO-362](./work-orders/362_WO_route_layer10_look_reliability.md) | ✅ Done 28.07 (12/12 pixel-probed ROUTES-ALIVE; owner on-glass confirm pending) |
| WO-363 | Factory reset defaulted compose preview to JPEG stream | [WO-363](./work-orders/363_WO_factory_reset_compose_preview_default.md) | ✅ Done 28.07 (default → canvas; live reset left to owner) |
| WO-364 | PRV bus as first-class output (GPU head, matrix split, outside routing) | [WO-364](./work-orders/364_WO_prv_physical_output_gpu.md) | ✅ Done 28.07 (71aa5a1; cable staged to DP-2 — owner presses Apply) |
| WO-365 | Matrix ghost 3rd row fixed (bare-id `consumedIds` vs the `id#half` dedupe key); cables now anchor per half (`data-connector-anchor`, position map keyed by anchor); one shared outputLayer parser; **bonus defect found**: re-grabbing a PRV cable dropped its `{outputLayer:2}` note and demoted it to a second PGM feed — now carried across the move and the rollback | [WO-365](./work-orders/365_WO_matrix_prv_duplicate_row_and_cable_anchor.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-366 | todos21.07.26 top block never triaged — 7 owner items with zero WO/commit coverage (record bus records wrong channel; live-audio channel resolution; numlock; timeline drops/inspector/latency; timeline compose label bar) | [WO-366](./work-orders/366_WO_todos21_untriaged_backlog.md) | 🟡 Owner answered 4 of 7 on 04.08 (checklist04.08): #2 record-bus, #4 timeline drag-drop, #5 clip inspector, #7 label bar — all "fine now", closed. Still unanswered: #1 live-audio cheapest resolution, #3 NumLock reset, #6 timeline edit speed on caspar output |
| WO-367 | Three gates against "written but never called": eslint `--max-warnings 224` (re-measured, not the quoted 225), `tools/ci/check-unwired-exports.js` as a CI step (697-orphan baseline, new ones fail), and an init-call smoke reproducing the `6e53abe` shape. Each proven to fail on its own regression | [WO-367](./work-orders/367_WO_lint_ratchet_and_unwired_code_gate.md) | ✅ Done 28.07 (suite 1597/0; cap 224 + baseline 697 recorded, shrink-only) |
| WO-368 | template/shaders is box-owned in .stignore but still git-tracked: permanent dirty tree, deleted shaders resurrectable by any checkout, 9 untracked shaders exist on one machine only (checklist27 item 16) | [WO-368](./work-orders/368_WO_shader_store_git_ownership.md) | ✅ Done 04.08 per WO (option B implemented at current disk state) |
| WO-369 | Checklist27 UI notes: "Back to GUI" gated off wherever auto-park runs — **not deleted**, because auto-park is behind `operatorTools.multiHelperTaskbar` and the flag-off WO-283 path still needs it (invisible on this box); wall clock 12→17px, weight 600, 6px to the eyes, placement untouched | [WO-369](./work-orders/369_WO_checklist27_ui_notes_batch.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-370 | Playlist rows show the timeless default on media clips — inspector row now gates on isTimelessItem (input for timeless, real clip length as static text for media); new `media-duration.js` index with an explicit tie-break over the contradictory CINF/ffprobe duplicate rows; `duration: 5` no longer stamped on movies at creation | [WO-370](./work-orders/370_WO_playlist_rows_real_media_durations.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-371 | "In PRV the playlist stops after the first item" — item 23 contradicted item 27; WO-355 made playlists PGM-only after WO-354's acceptance was written. **Owner decided 28.07: option B** — preview plays every item AND loops; WO-355's stop-on-take-out stays; item 27 must be reworded | [WO-371](./work-orders/371_WO_prv_playlist_preview_playback.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-372 | Companion showed no dev module to choose — dev + installed both declared `highpass-highascg@1.0.4`. `package:dev` now stamps `<next patch>-dev.<utc>` + `isPrerelease` into the PACKAGED manifest only (version forms pre-validated against Companion's own validator); `dev-mode.sh` repointed to the real paths and exits 1 on a version collision. WO-361 status corrected to CORRECTED | [WO-372](./work-orders/372_WO_companion_dev_module_not_selectable.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| WO-373 | Record bus recorded the wrong channel. §1c settled from code: a Caspar output is SINGLE-cable (`addEdgeToGraph` → `sink_already_connected`) — but `validateDeviceGraph` never enforced it and **matrix view wrote the whole graph through settings**, stacking a second layer-1 edge that the old `sort(...)[0]` then resolved to the OLDER cable. Fixed at both ends: shared `pickOutputEdgeWinner` (PGM>PRV, then most-recently-cabled, warns loudly) across streams/records/virtual-cam, matrix crosspoints now replace, and record/stream ports show the bus they will capture | [WO-373](./work-orders/373_WO_record_bus_wrong_source_channel.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-374 | "alpha doesnt work on shaders" — the transparent page and alpha-claimed WebGL2 context were already right; every alpha-enabled shader ends `fragColor = vec4(col, 1.0)`, so there was nothing to see through. Image pass is now wrapped when the Alpha flag is on: author's `mainImage` renamed + called, near-black keyed out, premultiplied; an authored alpha still wins; skipped unless exactly one `mainImage` declaration | [WO-374](./work-orders/374_WO_shader_alpha_keying.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-375 | "audio reactive" ticked bound nothing — the texture is only created when a pass BINDS 'audio', so 4 shaders (sh-balatro/console/test/wavy) wore the ♪ badge and could not react. Now auto-bound to the first free image channel in the store (every save) AND at load in player.js, so already-exported shaders work with no re-export and no rewrite of the single-copy store | [WO-375](./work-orders/375_WO_shader_audio_channel_autobind.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-376 | Shader `camera` channel — owner chose **A + an opt-in tick**: `camera` in the same per-pass dropdown as `audio`, fed by the virtual camera, and only when "Send to shaders as camera" is ticked in its inspector. Two gates before any device opens; black 1×1 texture up front so a camera shader renders fine with no device; `iChannelResolution` corrected on first frame; camera code split into a lazily-loaded `player-camera.js` (player.js hit the 500-line limit — old exports pick it up with no re-export) | [WO-376](./work-orders/376_WO_shader_camera_channel.md) | 🟡 Implemented (fail-soft render-probed: left [0,0,0] / right [0,64,128]) — **needs restart**; live camera image + CEF video permission unverified |
| WO-377 | Host-channel cable ignored: `dst_in_host_decklink_input_4 → vcam_1` existed in the graph but host-channel destinations are VIRTUAL (never in `screenDestinations`), so `collectDestinationOutputEdges` dropped the edge and the virtual camera kept its stored channel 1. Now resolved from the connector's own `caspar.hostChannel`; string-based stream/record sources guarded against unnameable sources | [WO-377](./work-orders/377_WO_host_channel_cable_ignored_by_output_mapping.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-378 | Owner: "host channels must be able to feed any and all outputs" + "program should be only a label/id of a pgm screen, the rest in channels terminology". New `src/config/output-source-name.js` is the one vocabulary: `channel_<N>` = a Caspar channel (unbounded by screen count), `program_<N>`/`preview_<N>` = screen buses. Three duplicate resolvers (two silently falling back to channel 1) now delegate to it; host-channel edges are named `channel_<N>` so they feed record/stream/vcam alike. Dead client `hostChannelVideoSourceToken` (never called, would have resolved to 1) removed | [WO-378](./work-orders/378_WO_channel_source_vocabulary.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-379 | "sh-mirrors fails to load with 404" was systemic: a shader lives in TWO files (config JSON + exported template) and they had diverged BOTH ways — 7 configs with no template (404 on play), 2 templates with no config (404 in the library). `reconcileShaderStore()` repairs both directions on read and never deletes (missing HTML re-exported; missing JSON recovered from the config embedded in the HTML). Also: PGM stack was append-only — `clear: true` action + ✕ on occupied rows (fade, then CLEAR) | [WO-379](./work-orders/379_WO_shader_store_reconcile_and_stack_clear.md) | ✅ Done (06.08 status sweep — per WO status line) |
| WO-380 | Paste a Shadertoy share link → the code layers fill in. Cloudflare blocks every unauthenticated route (plain 403, unkeyed api/v1, the site's own POST endpoint, AND headless Chrome — all measured), so the build targets the sanctioned free API key. Pure halves proven: link→id, and renderpass→our config wired by buffer OUTPUT id, with music→audio and webcam→camera; unrepresentable channels reported in `skipped[]`, never mis-wired. Import returns a config for review — it never saves over the open shader | [WO-380](./work-orders/380_WO_shadertoy_link_import.md) | 🟡 Implemented — **owner: add a free Shadertoy API key**; the keyed fetch itself is unverified |
| WO-381 | "host channels stuck on planned" + "it first was created as ch4 which was already occupied by operator gui": `usedCasparChannels` never counted the Operator GUI's channel (proven: `suggestNextHostChannel` → **4** on this box's own config, now 9) and `generatedChannelOrder` left a hole where ch 4 is. The "(planned)" badge compared `buildChannelMap(ctx)` with **itself** (settings vs state) and defaulted to `true` with no live map — it now means "the running Caspar has no such channel" (INFO CONFIG via `configComparison`), so it clears itself after Apply + restart. Timers dock is a live controller only: standing time input per row (fans out to every assigned screen), eye fades in/out (`fadeFrames: 25` — it was posting the cut form), create + "Add to screen" + per-chip remove gone, and the duplicate compact timer strip below it (WO-226 T226.5, in the audio mixer panel) removed | [WO-381](./work-orders/381_WO_operator_gui_channel_reserved_planned_badge_timer_dock.md) | 🟡 Implemented 29.07 (live: order now `4:operator_gui`, ch 4-8 not planned / ch 9 planned; suite 1678/0/2) — owner eyeball on the dock |

| WO-387 | Owner: "launch other apps from the operator gui open window. terminal, zoom. but i dont want it hard coded list but rather whats installed/available." The `.desktop` spec already carries the three tables WO-283/317 maintained by hand — `Exec=` (what to run), **`StartupWMClass=` (which window, the field the helper watchdog lives on)**, `Icon=`. New catalog (pure parse + cached scan) drives all three; five curated tools stay pinned, everything installed sits under a collapsed "Show all apps". API takes an app ID (`app:<desktop-id>`) and re-reads the catalog — never a command line. Console tools are wrapped in the box's terminal under a private WM_CLASS. Also fixed a WO-317 hole this exposed: the taskbar delegated launches to WO-283's SINGLE session, so the box was capped at one open window (owner: "i cant open anything else") | [WO-387](./work-orders/387_WO_OPERATOR_INSTALLED_APP_LAUNCHER.md) | ✅ Done (06.08 status sweep — per WO status line) |

| WO-388 | Owner: "i need the crop to be included in the size of the layer … when i want the layer to be adjusted to left, it adjusts including the crop." **WO-238 read this requirement INVERTED** ("doesn't count X in" = X is missing, not X must be excluded) and closed it "code correct", locking in the wrong behaviour behind a test that cannot fail. Align now seats the **visible (cropped)** rect on the canvas edge/centre via one shared `alignFillForCrop` (ESM + CJS mirror, same parity rule as the rest of the crop math); identity crop reduces algebraically to the old formula so uncropped layers are bit-identical (guarded against the pre-change formula as oracle). contentFit, the compose crop-drag handles and the W/H inputs deliberately unchanged — reasons in the WO | [WO-388](./work-orders/388_WO_CROP_COUNTS_INTO_LAYER_SIZE_FOR_ALIGN.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |

| WO-389 | Owner: "the open window drop down 'hides' under the compose preview windows." Not z-index (it already had 9000) — the compose tiles are **X SHAPE holes** cut out of the Firefox window (WO-263), so pixels painted over them do not exist. The existing occluder suppression missed it twice: the menu is not a `.modal-overlay`, and it opens by toggling `style.display` on a node that never leaves the DOM, which a `childList` MutationObserver cannot see. Added a keyed `setOccluderOpen()` registry (rejected widening the observer to attribute mutations — it would fire on every render app-wide for one dropdown) and wired it through the menu's single `closeMenu()` choke point so the latch cannot stick and black out the tiles | [WO-389](./work-orders/389_WO_OPEN_WINDOW_DROPDOWN_UNDER_VIDEO_HOLES.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |

| WO-390 | Owner: "the crop didnt apply on the actual caspar output." **Caspar exonerated with live before/after PRINT frames**: the 2.6-dev binary renders CROP on an image producer AND on a route producer (the owner's layer is `route://6-1`), immediate and `DEFER`+`COMMIT` both. **The take path is exonerated too** — the real `buildTakeJobs` run offline against the owner's saved look emits `MIXER 1-11 CROP 0.208… 0 0.791… 1 0 DEFER` on both a cold take and a crop-added re-take. The one reachable no-CROP case is a re-take where live state ALREADY records the crop but Caspar does not (probe: `jobs: 0`) — most likely a Caspar restart outliving the persisted live snapshot (cf. WO-176) or WO-218 bank drift. Also found: the nudge path emits crop as the only line WITHOUT `DEFER` (one-frame tear on crop drags) | [WO-390](./work-orders/390_WO_CROP_NOT_APPLYING_ON_CASPAR_OUTPUT.md) | ✅ Done 06.08 — evidence window expired, §3.1 mitigated by live-scene-reconcile, §4 nudge-crop DEFER fixed |

| WO-391 | Owner: "mouse on the machine is lagging at times … maybe there is some leftover process that gets the mouse position." Right mechanism, but it is a shipped feature, not leftover: `confine-pointer-barriers.py`'s `warp_watchdog` polls `XQueryPointer` every **50 ms** and `XWarpPointer`s the cursor back whenever it leaves the operator monitor. Live: fenced to `DP-1 +1920+0` while `DP-5 +0+0` exists — so every move onto the second monitor is fought 20×/s. `operatorTools.pointerConfine` is `'auto'` with no override, and WO-308 added `'off'` for exactly this. Ruled out: swap (0 B used), the 116k/s context switches (~104k of them are casparcg's own 8×50fps threads), the 30 s `PRINT 8`. Separately reaped-worthy: a **headless Firefox from a dead Claude session, 19.5 h / 826 MB / CDP :9223** | [WO-391](./work-orders/391_WO_MOUSE_LAG_POINTER_CONFINE_WARP_WATCHDOG.md) | ✅ Done 30.07 per WO — poll-loop fixes shipped (WO-391b/c); the one-off lag stays unexplained, re-open on recurrence |

| WO-392 | todos30.07 §"gui refreshing live-input thumbnails periodically": the 30 s TTL-window refresh (client bust rotation + deck rollover timer + server stale-recapture PRINT) removed end-to-end — live-input thumbs are now capture-once, stable URLs, server `no-cache`+ETag/304; explicit ↻/upload and bus-invalidation refresh paths intact. WO-331's guards repointed | [WO-392](./work-orders/392_WO_LIVE_THUMB_NO_PERIODIC_REFRESH.md) | ✅ Done 30.07 (suite 1734/0/2; live probes: no PRINT on cached GET, 304 on If-None-Match, force-capture OK) — owner: watch a live tile >30 s, no blink |

| WO-393 | todos30.07: 0 stream/record outputs were impossible — settings GET + device-graph suggester re-seeded phantom `str_1`/`rec_1` on EMPTY arrays. Now `[]` round-trips (operator choice); only an absent key seeds a default. Adjacent unfixed: add-output id collision after sparse removals | [WO-393](./work-orders/393_WO_ALLOW_ZERO_STREAM_RECORD_OUTPUTS.md) | ✅ Done 30.07 (suite 1737/0/2; live: GET returns `[]` on this box's real config) — owner: remove-last / add-from-zero in Devices view |

| WO-394 | todos30.07: Companion is a HighAsCG module now — direct TCP/AMCP client deleted (play/stop/clear gone; raw AMCP → `POST /api/amcp/raw`), AMCP port + bridge toggle out of config, "+ CasparCG" framing dropped, `caspar_connected` reads the app's own link. Module commits 95080f1 (WO-372-era residue as found) + 501763d | [WO-394](./work-orders/394_WO_COMPANION_HIGHASCG_ONLY_MODULE.md) | 🟡 Implemented (module tests 40/0) — **owner A394.1: restart Companion, pick dev module, re-save config** |
| WO-395 | todos30.07: stream/record start/stop for the CONFIGURED outputs — server status now carries a safe `outputs` catalog (live-verified: `rec_1`, empty stream list); module actions are catalog dropdowns (no placeholder URL/key fields), per-output feedbacks, Stream & Record presets per output. Also fixed: feedbacks were FROZEN while WS was up (`streaming_channel` broadcast unhandled) and record_state never left idle | [WO-395](./work-orders/395_WO_COMPANION_CONFIGURED_OUTPUT_ACTIONS.md) | 🟡 Implemented (suite 1740/0/2; module 40/0) — **owner A395.1: desk QA presets after Companion restart** |

| WO-400 | run.sh/supervisor-lib REVIEW (owner: "i fear there maybe a lot of unneeded code" — confirmed): 6 dead functions (~40 % of the lib) incl. the ENTIRE crash-loop backoff engine — so the designed give-up→inhibit-file path NEVER fires and a permanently crashing Caspar loops forever under systemd Restart=on-failure; two damping engines, two stop routines, double grace sleeps, drifted hard-fail code lists. Cleanup plan staged (quiet window, together with WO-398) | [WO-400](./work-orders/400_WO_RUNSH_SUPERVISOR_REVIEW.md) | ✅ Done 30.07 (owner "do this now"): dead symbols deleted, run.sh calls the lib backoff — sandbox rehearsal proved the inhibit file lands at give-up; deployed, Caspar recovered (8 ch); suite 1757/0/2 |
| WO-398 | run.sh hang-detector forks `ss`+`grep`+`sleep` EVERY second forever (~260k forks/day) in the healthy state, for a 90 s hang budget. Fix proposed: 10 s poll when healthy, 1 s only while counting a stall. No X contact — hygiene, not the lag | [WO-398](./work-orders/398_WO_RUNSH_1HZ_FORK_LOOP.md) | ✅ Done 30.07 with WO-400: healthy poll 10 s — live fork capture 5/32 s, was ~96 |
| WO-399 | Virtual-camera jpg relay: ffmpeg re-decodes ONE compose-preview JPEG at 50 fps → 39 % CPU constant (`v4l2-bridge-relay.js:77`). Fix proposed: jpg-branch fps 50→10 (stream mode untouched), verify /dev/video10 consumers | [WO-399](./work-orders/399_WO_VCAM_FFMPEG_STILL_50FPS_BURN.md) | ✅ Done 30.07 per the WO status line (stream mode default, 25 fps verified on /dev/video10, suite 1750/0/2) — queue row was stale, corrected 04.08 |
| WO-397 | Mouse lag ROOT-CAUSED (measured, causally proven): the 8 s pointer-confine barrier watchdog recomputes the full layout each tick → `xrandr --verbose` + `--query` spawns → **two ~180 ms X-server freezes every 8.000 s** (probe: 47 spikes/3 min at fixed phase; manual xrandr reproduces on demand). WO-391c's 3 s xrandr cache TTL < the 8 s caller → guaranteed miss. **FIXED same day** (owner: "do the 397"): steadyTick fast path (no layout recompute on the 8 s tick), xrandr TTL 3 s → 60 s, nvidia-smi memoized. Probe re-run: **spikes 47 → 6 per 3 min, p99 2.56 → 0.34 ms**; residual = one pair per ~65 s from the OS-Config layout check (env-tunable, WO-397b candidate) | [WO-397](./work-orders/397_WO_MOUSE_LAG_8S_XRANDR_FREEZE.md) | ✅ Done 30.07 (suite 1750/0/2; probe before/after recorded) — owner: feel-check |
| WO-401 | Machine performance research pass (owner ask 30.07): 15 ranked findings with live measurements — top: OSC debug sample-list scan ~15 % of node CPU (`osc-listener.js:26`), 4 deep clones + 2 stringifies of OSC state per 50 ms tick, WS full-state 323 KB/s/client with the delta path implemented but OFF (`wsDeltaBroadcast`), sync `writeFileSync` before EVERY AMCP send (take latency), v4l2 relay's redundant `-vf scale` at 22 % CPU. Also: todos item 6 root cause = Sources panel rebuilds `<img>` via innerHTML every state tick (WO-392 only fixed the URL bust); item 8 was already fixed by WO-396. F1–F5 source-verified | [WO-401](./work-orders/401_WO_MACHINE_PERFORMANCE_RESEARCH_PASS.md) | 🟡 ALL actionable findings implemented 30.07 eve: F1+F2+F4+F6+F7+F8+F9 server-side; F12 + thumbnails item 6 client (source-only, dist-web NOT rebuilt mid-show); **F3-revised** value-aware dirty marking + `HIGHASCG_OSC_WS_DELTA=1` (delta WS now pays — idle channels drop out of the 20 Hz stream); **F5-revised** vcam fps derives from source rate halved with 25 floor (owner spec; box: 50→25). Suite 1768/0 — **post-show deploy: build:client → service restart → kiosk F5**; QA list + delta rollback in WO §3; deferred: F10/F11/F13/F14/F15 (reasons in WO §2) |
| WO-402 | mp4 upload fails "expected JSON, got HTML": nginx :80 proxy `client_max_body_size 512m` 413s any file > 512 MB with an HTML page before node sees it (access.log smoking gun 01.08). Repo config fixed: `client_max_body_size 0` + `proxy_request_buffering off` (node owns the 200 GB cap). Live nginx reload = owner one-liner in WO (graceful, show-safe); interim: use `http://192.168.1.253:4200/` direct | [WO-402](./work-orders/402_WO_NGINX_413_UPLOAD_LIMIT.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| WO-403 | issues01.08 item 1: Shader Live editor vs shader playlists — scene.live keeps the AUTHORED playlist source across engine hops, so the editor listed only the first shader and its 403→CG ADD re-host replayed shader #1 over the one on air. Instances now resolve through a 1 s `/api/playlist/state` poll (active item per layer); selection follows the channel-layer across hops; cgName re-hosts the right shader | [WO-403](./work-orders/403_WO_SHADER_LIVE_PLAYLIST_FOLLOW.md) | ✅ Done 06.08 — deploy leg completed in today's batch; QA tick on checklist06.08.26 |
| WO-404 | issues01.08 item 2: compose preview "loses signal" on any sources-panel drag — interaction suppression DELETEd the operator layout, which STOPs all compose route producers; restore re-PLAYed them (real re-acquire). Suppression now rides WO-319's `suppressHoles` on the same cells: holes close, routes keep playing, restore is instant. Remote clients' modals also stop clobbering the shared layout | [WO-404](./work-orders/404_WO_COMPOSE_PREVIEW_DRAG_BLACKOUT.md) | ✅ Done 06.08 — deploy leg completed in today's batch; QA tick on checklist06.08.26 |
| WO-396 | todos30.07: Devices tab 1–2 s load = cold `GET /api/device-view` re-probing DeckLink every time: inventory collector only read TODAY'S Caspar log (device block is at Caspar's last start → `[]` after midnight), then the 1.2 s live ffmpeg probe (always fails, Caspar holds the cards) ran BEFORE the 459 ms log parse that works. Collector now multi-day; fallback log-first; result cached 10 min (`fresh=1` clears) | [WO-396](./work-orders/396_WO_DEVICE_VIEW_COLD_LOAD_DECKLINK_PROBE.md) | ✅ Done 30.07 (suite 1745/0/2; live: cold 2.47 s → 0.455 s, warm 54 ms, connectors intact) |
| WO-405 | todos03.08 item 1 (perf round 2): the owed WO-401/403/404 **post-show deploy is DONE 03.08** (server live via 09:55 restart; `build:client` 10:13 + kiosk XTEST F5 — dist-web had been stale since 30.07 while client fixes landed 01.08). Round-2 research not started: fresh baseline with round 1 actually live, delta-WS verify, caspar-internal pass (431 % CPU), revisit deferred F10/F11/F13/F15 | [WO-405](./work-orders/405_WO_PERFORMANCE_ROUND_2.md) | 🟡 Deploy leg done 03.08; idle baseline captured 04.08 (casparcg 8 %, node 1 %, GPU 12 % on the post-produce 2-channel config — the reference floor); caspar-internal pass waits for a real show config |
| WO-406 | todos03.08 item 2 (headphone monitoring + solo): the solo system already EXISTS — `/api/audio/solo` (routes-audio.js:284), audio-preview config model, `buildMonitorChannelXml`, SOLO buttons with Ctrl multi-solo in the mixer — but NOTHING enables it: no UI sets `audio_preview_enabled`/`monitor_channel_enabled`, live config has no monitor channel, so the buttons no-op ("Solo API not supported"). Work = enablement surface + system-audio device enumeration (DM3 is single-open — monitor must be a different device) + end-to-end verify. Owner A406.1: which physical output for headphones? | [WO-406](./work-orders/406_WO_AUDIO_MONITOR_SECOND_OUTPUT_SOLO.md) | 🟡 2nd round 03.08: owner heard "stuttery noise" → root-caused as the 25 fps monitor channel dropping every other 50 fps route frame (route_producer bounded queue) — ch5 now rate-matched **720p5000**, live (`oal[5|720p5000]`, route://2 playing); stored entry flipped to type **system-audio** (owner was right, it said portaudio) + Monitor checkbox now forces the type; `~/.alsoftrc` mixes at 48 k. Owner: LISTEN + SOLO. NDI hostChannel-5 collision stays with WO-377/381 (WO §5) |
| WO-407 | todos03.08 item 3 (PGM screen-consumer micro-stutter, operator GUI smooth): producer side exonerated by the smooth GUI path — suspects are presentation-side: (1) PGM display at 60 Hz vs 50 fps channel with vsync (3-2 cadence), (2) two vsync'd GL windows on one X screen beating, (3) caspar load (431 %). Anomaly on file: xrandr reported Screen 0 `current 8 x 8`, NO active mode on DP-0/DP-4 while kiosk+consumers ran. Diagnosis needs the box on-glass: owner names the stuttering output, live xrandr, cadence check, then single-variable tests (force 50 Hz → ch3 vsync off → ch1 vsync off) | [WO-407](./work-orders/407_WO_SCREEN_CONSUMER_MICRO_STUTTER.md) | ✅ Done 03.08 — root cause: GL swaps synced to the primary head (operator DP-4) while PGM is DP-0 → cross-panel vblank beat; fixed with `__GL_SYNC_DISPLAY_DEVICE` — **auto** per owner follow-up (caspar-env machine-generated each Apply from the layout plan's screen-1 sysId; override `caspar_gl_sync_display`); owner-confirmed smooth; explainer doc `multi-head-gl-vblank-sync.md`. Re-check once after next Apply (ch3 vsync regenerates true) |
| WO-408 | todos03.08 items 4-6 batch: live-source compose tiles wore the PGM-red label bar (no rule for `def.sourceTile` tiles → blue `#2563eb`); PRT/CAPTURE snapshot buttons unified on CAPTURE (same PRINT action, label-only drift); test pattern printed the resolution twice because custom modes are NAMED by their resolution and config-compare appended the mode id unconditionally | [WO-408](./work-orders/408_WO_TODOS0308_COMPOSE_UI_BATCH.md) | ✅ Done 03.08 (suite 1781/0/2, built+F5+service restart) — owner eyeball |
| WO-409 | todos03.08 item 7: PRV bus strips in the audio mixer — row model was PGM-only; now each screen gets a `prv:<ch>` master strip (meters via widened key regex, fader, SOLO). PRV SOLO = whole-channel solo: new layer-less `/api/audio/solo` target → `route://<ch>` on the WO-406 monitor bus; shared `soloKeyToTarget` replaces two inline parsers that would have sent `route://ch-NaN` | [WO-409](./work-orders/409_WO_PRV_MIXER_STRIPS.md) | ✅ Done 03.08 (suite 1781/0/2) — listening leg gated on WO-406 Apply+restart |
| WO-410 | Operator GUI holes empty after a caspar restart: the apply flow's route re-plays landed on the DYING instance, then the reconnect re-apply skipped every PLAY — `lastAppliedRouteByChannel` (module memory) described the dead caspar. Cache now forgotten on every (re)connect (multiview checked: safe). Live-verified: PLAY 3-10/3-11 re-fired, producers back | [WO-410](./work-orders/410_WO_OPERATOR_HOLES_EMPTY_AFTER_CASPAR_RESTART.md) | ✅ Done 03.08 (suite 1782/0/2) — owner eyeball |
| WO-411 | todos03.08 additions: resolution STILL appeared a second time — in the "Output:" line (`connectorLabel += ' · screen · 1728x960'`, routes-led-test-card + startup fallback; mode suffix dropped, "once at the top is enough"); bouncing character collided as a 250×250 square while the glyph aspect-fits inside it → early left/right bounces; collision now uses the drawn rect (live `led_grid_test-render.js` + orphan patterns copy kept in sync) | [WO-411](./work-orders/411_WO_TEST_PATTERN_OUTPUT_LINE_AND_BUMP_BOX.md) | ✅ Done 03.08 (+round 2: bounce character size now RELATIVE to the screen — 26% of the shorter dimension, ≈ the old 250px on the 1728×960 main; speed scales with it; both template copies in sync) — owner: pop the test pattern once, incl. a small output |
| WO-412 | todos03.08 follow-ups: the port inspector's "NVIDIA sync to display" tick now ALSO drives caspar's GL swap vblank (one tick → nvidia policy + `__GL_SYNC_DISPLAY_DEVICE`; box's existing port-1 tick → DP-0, same as running); cabling the operator GUI to a port now persists `operator_monitor`/`always_on_top:false`/`interactive:true` into the APP config at Apply (was merged-config-only — the runtime session and the GUI tick never saw it) | [WO-412](./work-orders/412_WO_SYNC_TICK_DRIVES_CASPAR_AND_OPGUI_CABLE_IMPLIES_MONITOR.md) | ✅ Done 03.08 — owner: next Apply shows the port-3 Operator-monitor tick ticked |
| WO-413 | todos03.08 addition: USB drives auto-mount on plug — the WO-29 stack mounted only from a UI click, so "plug and import" never worked without terminal. New node watcher polls unmounted removable partitions every 3 s → udisksctl mount each (multiple drives independent); eject ≠ replug (no remount until physically re-inserted); failure backoff; `usbIngest.autoMount` gate (default on, live-read). Polkit already authorizes (pkcheck 0) | [WO-413](./work-orders/413_WO_USB_AUTOMOUNT.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| WO-414 | owner follow-up 03.08: PGM strip had no SOLO (UI-only gap, WO-409 rendered it PRV-only) + PRV audio metered on the macbook NDI strip — the WO-406 §5 collision proven live: stored NDI pin ch5 AND monitorCh both = 5 (generator clobbered the NDI block, mixer metered monitor audio as NDI). Fix: routing-map dynamic allocation flows AROUND stored host-live pins (`allocCh()` skips them; pins never move per WO-377/381); SOLO on every master strip | [WO-414](./work-orders/414_WO_PINNED_HOST_CHANNEL_COLLISION_AND_PGM_SOLO.md) | ✅ Done 03.08 live (`oal[6|720p5000]`, ch5 = NDI again, route://2 on ch6; suite 1801/0/2) — owner: SOLO the PGM strip |
| WO-415 | INCIDENT 03.08 12:54: plugging the flashed live-USB back in triggered exfat-sync `usb-modular-config` (`to_project`, stick wins) → stick's stale `configs/` clobbered live `config/` (destinations/connectors/multiview/streams gone, server now runs "no assigned outputs"; WO-237 smoke correctly red 1799/2). Also hit `projects/wesele.json` (untracked!) + shader templates; node_modules pruned same window (restored). Restore point: `c45b17a` (12:17, live-verified — owner 04.08: restore NOT wanted). Hardening options in WO | [WO-415](./work-orders/415_WO_usb_stick_insert_clobbers_live_config.md) | ✅ CLOSED 04.08 — NOT an incident (owner: normal eggs-produce behavior) |
| WO-416 | `build-produce-flash-stick.sh` dies in phase 2/5 (`parted rm 3` → "Partition doesn't exist" + "unable to inform the kernel… in use"): **WO-413's USB auto-mount poller re-mounts the stick mid-flash**. `usb_mask_exfat_automount` only masks WO-47 *systemd* units, but the poller is a 3 s node timer inside highascg.service that `udisksctl mount`s to `/media/casparcg/<LABEL>`; udisks log shows unmount→remount 1–3 s later "on behalf of uid 1000", no desktop automounter on the box. Regression from WO-413 (landed same day); also explains the earlier half-flashed ISO-only stick (no HIGHASCGEXF slice). Workaround: stop highascg (or `autoMount:false`) + reboot/replug before re-running | [WO-416](./work-orders/416_WO_usb_automount_fights_flash_script_partitioning.md) | ✅ CLOSED 04.08 — owner QA passed: reflash ran through all 5 phases ("i already did reflash and install and it worked") |
| WO-417 | Calamares "failed to install boot loader" on target machine (03.08 ISO): squashfs audit of the actual artifact shows ALL known bootloader fixes present (full-path bootloader.conf, BIOS+EFI before_bootloader branches, grub-pc/efi+signed installed, i386-pc + x86_64-efi module trees) — root cause needs the session log, which dies with the live tmpfs because `shellprocess@logs` runs AFTER `bootloader` in the exec sequence. Fixed the evidence gap: live-only systemd unit mirrors session.log → `HIGHASCGEXF:/logs/` every 20 s, baked in via fix-calamares-shellprocess + ISO verify gate | [WO-417](./work-orders/417_WO_calamares_bootloader_install_failed.md) | ✅ CLOSED 04.08 — owner: install on the target machine WORKED; bootloader failure gone with the 03.08+ fixes; session-log rescue stays as permanent evidence plumbing |
| WO-418 | Review 2026-08-03 "fix first" rows 1–5, all implemented 04.08: project-slug traversal closed at every entry point incl. the persisted-autosave write primitive (`isSafeProjectSlug`); AMCP CR/LF injection killed in `param()`/all escapers + preset whitelists in both ffmpeg-args builders (adapter untouched — it legitimately carries multi-line batches); `'error'` handlers on all 5 crash-the-server sites (ingest upload cleans up partial files + reports per-file errors); take Phase-B failure now logs (log-only by design — teardown-timing change needs its own rehearsed WO); `profiler_chN_healthy` inversion fixed + WO-401 smoke repointed to the correct contract. Rows 6–10 + auth-off exposure remain open | [WO-418](./work-orders/418_WO_review0308_fix_first_batch.md) | ✅ Done 04.08 — server restarted, live probe passed (`GET /api/project/file/..%2fconfig%2fgeneral` → 404) |
| WO-419 | Review 2026-08-03 "fix first" rows 6, 7, 10, all implemented 04.08: the three autosave latches un-latched (`project_gone` now exits via any successful save/load/adopt; resync gate un-inverted so a failed bootstrap RETRIES on WS reconnect, with an in-flight guard against double-bootstrap; fresh-server `200 {}` no longer falls through to the throwing 404 POST, so seeding autosave works on a new box); USB import API resurrected (router now registers `/api/usb/*` matching handler+client, parsed query passed through — the old re-parse got an already-stripped path); logs-modal eye-click close runs the full `close()` teardown (was leaking a 2 s poll + WS listener per toggle; `parseMarkdownBasic` moved to `logs-modal-shared.js` for the 500-line gate). Rows 8–9 stay parked on the owner's WO-415 hardening pick | [WO-419](./work-orders/419_WO_review0308_fix_first_rows_6_7_10.md) | ✅ Done 04.08 — server restarted, row 7 live-verified (`GET /api/usb/drives` → 200) |
| WO-420 | CI red on EVERY main push since 30.07 — not the WO-415 box reds: the lint warning ratchet was breached (225 then 226 vs cap 224, 0 errors), which MASKED two more failures behind it (`check-tdz-reads.js` fails Prettier since WO-383; npm-audit gate blocks on `brace-expansion` high). Fixed 04.08: 8 dead-code warnings deleted (4 stale router.js requires from the WO-119 split, dead `cancelFn`, 2 unused imports, 1 unused param), cap ratcheted 224→218, prettier-fixed the tdz tool (its smoke 6/6 unmodified), `npm audit fix` (brace-expansion + undici patch bumps; suite + boot re-verified after) | [WO-420](./work-orders/420_WO_ci_red_since_3007_lint_ratchet_prettier_audit.md) | 🟢 Done 04.08 — CI GREEN on fc21b64 (first green main run since 29.07) |
| WO-421 | Config durability + monitor-fps fallback, un-parked by the owner reframe (WO-415 = normal produce behavior): corrupt-but-present config files now QUARANTINED to `.corrupt-<stamp>` before defaults can be saved over them (review §1 amplifier); exFAT sync copies via tmp+rename (review §3; staleness guard §2 deliberately skipped — stick-wins is intended); monitor bus fps falls back to the operator-GUI main (the box's 50 fps GUI channel was getting a 25 fps monitor bus — the WO-237 chop condition, a REAL generator gap the "config drift" reds were hiding); WO-237/406 smokes repointed to the mains that exist | [WO-421](./work-orders/421_WO_config_durability_and_monitor_fps_fallback.md) | ✅ Done 04.08 — smoke 4/4, suite **1815 / 0 fail / 2 skip** (first 0-fail since produce), server restarted |
| WO-422 | Batch COMMIT-ack timeout no longer replays the transaction sequentially (review engine §3 — the replay double-executed every PLAY on air, clips restarting from frame 0 mid-transition; the 07-19 ack-drift incident was exactly a commands-ran-acks-lost case). Timeout rejection now carries `amcpPayloadSent`; the fallback refuses to resend it and logs at warn; all pre-send failures keep the sequential fallback. **Closes the last open 03.08 review item** | [WO-422](./work-orders/422_WO_batch_commit_timeout_no_sequential_replay.md) | ✅ Done 04.08 (smoke 2/2, neighbors 21/21, suite 1820/0/2, server restarted) |
| WO-423 | Owner 04.08: Settings → Install-to-disk must leave ONLY Calamares on screen — `launch-calamares.sh` now re-execs into a transient systemd unit (so stopping its own parent service can't kill it), stops casparcg-server + scanner + highascg right before launch, and restarts all three when the installer exits (cancelled install un-bricks itself); confirm dialog warns the GUI will close | [WO-423](./work-orders/423_WO_calamares_launch_stops_stack.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| WO-424 | Owner 04.08: "update system built in" — it EXISTED (WO-188: Settings tab, check/apply API, sudo helper) but was dead end-to-end: the checker read `/releases/latest` which never returns the PRERELEASE server drops, and the stamp comparator sorted `2026.05.20` ABOVE `2026-06-28…` (`-`<`.` in ASCII). Checker now scans the release list; comparator normalizes separators; download path hardened (review §5: stream errors, stall wedge, redirect bound, partial-file cleanup) | [WO-424](./work-orders/424_WO_gui_update_flow_unbricked.md) | ✅ Done 04.08 — LIVE-verified: check now answers `updateAvailable:true` for the 2026-06-28 release on this box; publish workflow = `npm run release:github-server` (dry-run verified) |
| WO-425 | Owner 04.08: monitor output must be "Audio 1/2 + Monitor tickbox", not "USB headphones", and absent from defaults — the label was never a code default, only THIS box's config; relabeled to `Audio 2` (role/device untouched, monitor bus resolves identically, live-verified); factory config proven FUNCTIONALLY to ship zero audio outputs | [WO-425](./work-orders/425_WO_monitor_output_generic_label.md) | ✅ Done 04.08 |
| WO-426 | Owner 04.08: nothing may hardcode 50 fps (60 Hz regions) — full sweep classified: rates derive from mode ids/config everywhere load-bearing, 60-family modes all present, ~25 fallback-only 50s kept as defaults. 2 real defects fixed: client `videoModeToResolution` returned fps 50 for "1080p6000" (prefix match ignored the rate digits), mixer settle timer converted frames→ms at hardcoded 50 | [WO-426](./work-orders/426_WO_no_hardcoded_50fps_audit.md) | ✅ Done 04.08 (audit + fixes, smoke pinned) |
| WO-427 | Owner 04.08: easy DeckLink driver install — Blackmagic's EULA forbids shipping it, so the user supplies the free download once, now via BROWSER UPLOAD: new `/api/system/decklink/upload` (whitelisted, streamed to `vendor/decklink/`, git+Syncthing-ignored), local dir scanned first by both install halves, and the WO-188 Install button RESURRECTED (it was never in the DOM and its listener never called). Live-probed: upload→`vendorAvailable:true`→Install enables | [WO-427](./work-orders/427_WO_decklink_driver_install_ux.md) | 🟡 Implemented — owner tick on checklist06.08.26_close_all_wos.md |
| WO-428 | Owner 04.08: phantom DeckLink card on a driverless fresh install + card flexibility — suggest now gates every DeckLink port on the snapshot's `decklink.detected` (merged ffmpeg→OS probe; a PCI card counts even driverless; fails open with no probe data, preserving the Caspar-offline fallback). Flexibility was already detection-driven (lspci model names, port count follows hardware, ≤8 slots) — the phantom was cloned config keys echoing. BONUS FIND while testing WO-427 with the REAL 16.2 package: the WO-188 tar.gz path NEVER worked — the real archive nests under `Blackmagic_…_16.2/` and the `^deb/x86_64` anchor matched nothing; extractor now layout-agnostic, proven against the real 2 GB file (16.2a1 pair found). 16.2 staged on this box, Install armed | [WO-428](./work-orders/428_WO_decklink_phantom_card_and_real_archive.md) | ✅ CLOSED 04.08 — owner QA: driver install SUCCEEDED through the GUI (16.0.1→16.2, real 2 GB package, after 3 fix rounds: stderr parse, nested archive, pipefail SIGPIPE) |
| WO-429 | Owner 04.08: eggs must exclude DeckLink driver packages + all logins (tailscale/nomachine/syncthing/…) — audit: identity material was already covered (WO-142/168), but **the GitHub OAuth token (`~/.config/gh`) shipped in every ISO to date**, plus shell history; both closed in BOTH fragments along with gitconfig/netrc/npmrc/keyrings and the 2 GB `vendor/` upload dir; ISO verifier now fails a squashfs containing token/history/vendor. Installed-driver files were already masked (decklink fragment), so ISOs ship driver-free = EULA-clean with the WO-427 GUI flow | [WO-429](./work-orders/429_WO_eggs_excludes_credentials_vendor.md) | ✅ Done 04.08 (smoke 2/2) — next produce is clean; **rotate the gh token if any prior ISO left the building** |
| WO-430 | Owner 05.08: second fresh install (192.168.0.34) shows the DeckLink in the devices tab "without drivers" — the DISPLAY is intended (card physically present, lspci-driven, WO-428) but the probe revealed the ISO was NOT driver-free: **merged-usr (`/lib -> usr/lib`) made the fragment's anchored `lib/modules/*` excludes match nothing**, so the DKMS kernel modules shipped in every ISO and autoload via PCI modalias (live: "16.2a1 loading" + `/dev/blackmagic/io0-3` on the driverless box; userspace correctly masked). Fragment + verifier now cover `usr/lib/modules/*/updates/dkms/blackmagic*` and the usr/lib udev rule | [WO-430](./work-orders/430_WO_decklink_kernel_module_ships_merged_usr.md) | ✅ Done 05.08 (smoke 4/4) — squashfs-PROVEN on the owner's 10:32 produce (verifier full pass); ISOs before 05.08 10:32 all carry the kernel module |
| WO-431 | Owner 05.08: GUI DeckLink Install on the fresh second box answered "already installed" — eggs clones ship the build host's `/var/lib/dpkg/status` (a single record can't be excluded) while the fragment masks all payload + `info/desktopvideo.*`, so `dpkg-query` reports a PHANTOM install and the version-only gate in `decklink-install-lib.sh` skipped. Gate now also requires the payload binary (`/usr/lib/blackmagic/DesktopVideo/DesktopVideoHelper`). Unblock .34 today: `sudo dpkg --remove desktopvideo-gui desktopvideo` then Install again | [WO-431](./work-orders/431_WO_decklink_install_phantom_dpkg_already_installed.md) | ✅ Done 05.08 (smoke 6/6) — owner: run the .34 one-liner; this box still owes `install-exfat-systemd-units.sh` (WO-427 debt) |
| WO-432 | Owner 05.08: (1) Settings→USB video tab removed (WO-121 panel; redundant with Sources→Live add + device-view tile remove; panel file deleted, no new orphan exports); (2) Updates showed server stamp 26.05.20 because the eggs produce chain NEVER wrote `BUILD_STAMP` — only the GitHub-release script did; `build-produce-flash-stick.sh` now stamps `date -u +%F_%H%M%S` before Phase 1 (fragments deliberately don't mask it); (3) same script restores `npm install --include=optional` right after the produce prune so the build box keeps its dev tooling | [WO-432](./work-orders/432_WO_usb_video_tab_removed_produce_build_stamp_restore.md) | ✅ Done 05.08 (suite 1835/0/2, lint 216 warn/0 err, client rebuilt + kiosk reloaded) — stamp/restore prove out on the next produce; 10:32 ISO still shows 26.05.20; **mirror `BUILD_STAMP` into the Mac .stignore** |
| WO-433 | Owner 05.08: DeckLink Install on .34 died with a long error — reproduced live: `linux-headers "already the newest version"` yet `/lib/modules/…/build missing`. Fragments stripped ALL of `usr/src` (defeating `install-kernel-headers-for-dkms-iso.sh`) while the shipped dpkg status claimed the packages, and the verifier's status-grep was phantom-blind. Fragments now ship headers (mask only `usr/src/{nvidia,blackmagic}-*`), installer self-heals phantoms via apt `--reinstall` (both header pkgs), verifier checks REAL files. Plus UX: `~/Downloads` scanned by both install halves so no upload step when the tar.gz was downloaded on the box (WO-429 keeps Downloads out of ISOs) | [WO-433](./work-orders/433_WO_decklink_install_headers_phantom_downloads_scan.md) | ✅ Done 05.08 + addendum — 13:46 produce: verifier false-FAIL was a stale `usr/src/linux-headers` absence entry (fixed, verifier passes the real squashfs); **13:46 ISO poisoned, do not flash**: inserted stick's 04.08 `drop-update/` auto-applied over the live repo 12 s into the produce (old API file, old dist-web incl. USB tab, BUILD_STAMP 2026.05.20, 544 exec-bit flips, node_modules pruned) — tree restored to HEAD, re-produce needed; owner decision open: drop-apply opt-out or older-stamp guard on the BUILD box (WO-415 ruled the config cousin intended); .34 one-liner still pending |
| WO-473 | Owner 11.08: "the audio fix didnt land — the other machine started with 2 audio outputs already created and filled with a device. fresh project shouldnt have audio outputs created as well as record output". THREE carriers, WO-470 closed only one. (a) The bridge/stick `configs/` still held this box's `audio_1` (hw:0,0) + `audio_monitor_usb` (sc60mon) and the modular-config sync pairs copied them BOTH ways — WO-470 left that re-seed as manual owner action, so it kept coming back; both pairs now exclude `audio_outputs.json` + `audio_capture.json` (exclude applies on push and pull), bridge copy blanked. (b) Committed `device_graph.json` still shipped SEVEN connectors — rec_1, audio_1, audio_monitor_usb→sc60mon, gpu_p0-3 — invisible to WO-470's gate because it scanned the WORKING TREE and this box's live graph has none; now ships the factory graph. (c) `defaults-core` shipped rec_1 and four sites re-materialised it when the key was absent (settings-get, device-graph-suggest, the client's initial settings state, and the Add button, which would have created Rec2 first) — all now `[]`. Gates moved to judge the STAGED slice, not the working tree, which also clears the two failures the suite had been carrying all day | [WO-473](./work-orders/473_WO_FRESH_BOX_STILL_SHIPS_OUTPUTS_AUDIO_AND_REC.md) | ✅ Done 11.08 (suite 1947 tests, 1945 pass, 0 fail, 2 skip; eslint 0 errors; dist-web rebuilt) — owner QA: next fresh install + one New project; stick still holds the old copy, blank on next insert |
| WO-472 | Owner 11.08: "even though the media files are both in exfat (stick) and bridge the gui only shows the media in bridge folder" — WO-469 one day later, same symptom, but NOT a regression of it: that fix was only half the path. The client re-merges WS `state.media` with GET `/api/media` in `mergeMediaProbeOverlay`, and `mediaCatalogMergeKey` keyed FILE rows on `normalizeMediaIdForMatch`, which strips the directory (it must — `findMediaRow` matches a path-less layer value). So `exfat/TALK2.mp4` and `bridge/TALK2.mp4` both keyed `file:talk2` again, and `preferMergedMediaId`'s `b.length >= a.length` tie-break keeps the LONGER id — `bridge/` is one char longer than `exfat/`, so bridge won all eight rows deterministically; the `dir:`-keyed folder row has no collision and survives, hence a folder that opens onto nothing. File keys are now scoped to their lowercased folder, mirroring the server's `canonicalMediaRowKey`, so CLS `BRIDGE/TALK2` still folds onto `bridge/TALK2.mp4` | [WO-472](./work-orders/472_WO_GUI_STILL_EMPTIES_EXFAT_FOLDER_CLIENT_MERGE.md) | ✅ Done 11.08 (repro before/after in node against the client module, new smoke 4/4 registered, suite + eslint green, dist-web rebuilt) — owner QA: stick in, server up, F5, check both folders |
| WO-471 | Owner 10.08: Updates showed `2026.05.20` on a same-day ISO + "make sure the timestamp is printed on the iso even when only producing eggs, because thats the main workflow now" + boot-to-GUI doubled. THREE fixes: (1) WO-432 wrote BUILD_STAMP only in `build-produce-flash-stick.sh`, but `npm run eggs:build` is the main workflow and bypasses it — clones fell through build-stamp.js precedence to the package-version-era legacy file, and `compareBuildStamps` then judged updates on a bogus value (WO-424 class again). Write moved into `build-highascg-egg.sh` before `eggs produce`; wrapper exports `HIGHASCG_BUILD_STAMP` so there is one writer. (2) `critical-chain` proved decklink-install took **54ms** — the 30s was the exfat script polling 1s/iteration for an absent stick, with casparcg-server ordered behind it via 13-caspar-systemd-units.sh's `After=`. Now `udevadm settle` + a 5s grace when lsblk reports no USB disk at all; a present stick keeps the full wait. (3) The decklink gate's last branch queued DKMS whenever dpkg didn't report desktopvideo — but WO-431 says clones ship dpkg/status whole (this box: dpkg `ii`, `dkms status` empty), so a working box could rebuild every boot; now gates on `lsmod`/`lspci`. **TRAP:** the boot script exists twice and `pick_src`/the installer both prefer `scripts/exfat/` — the tools/runtime copy alone would have been a no-op; both patched and pinned | [WO-471](./work-orders/471_WO_PRODUCE_STAMP_AND_BOOT_TIME_TO_PLAYOUT.md) | ✅ Done 10.08 (suite 1942/0/2, eslint 0, bash -n clean) — owner QA: one produce + a stickless boot |
| WO-470 | Owner 10.08: "i told you not to hardcode the sc60 as a monitor — why is it even in the fresh install on another machine". `git show HEAD:config/audio_outputs.json` carried this box's headset (`audio_monitor_usb` / `system-audio` / `sc60mon` / `role:monitor`) — **committed**. WO-425 diagnosed this exact class but renamed only the label and explicitly left "Role/device untouched", and its smoke then PINNED the entry as required, locking the leak in. No gate either: `config:write-defaults` (which sets `audioOutputs=[]`) is manual, referenced nowhere in CI. Vector confirmed NOT the ISO (`write-iso-default-config.js` strips `config/*.json`; `casparcg.config.iso` is clean) but the stick+bridge `configs/` snapshots, applied 32 min after boot. Default blanked to `[]`; new standing gate rejects any committed `role:"monitor"` output or `sc60` reference under `config/`; WO-425's assertion superseded (stricter). **Stick/bridge `configs/` still carry it — re-seed owed, or the next insert re-applies it** | [WO-470](./work-orders/470_WO_SC60_MONITOR_LEAKS_INTO_FRESH_INSTALLS.md) | ✅ Done 10.08 (suite 1935/0/2, eslint 0) — owner: re-seed stick/bridge configs |
| WO-469 | Owner 10.08: "in the gui the exfat (stick) folder is empty even though the media is in both". `/api/media` returned the exfat folder row with zero children while all 8 bridge files listed. NOT the scanner (`projects/` walked fine) and NOT the mount — service and shell share one namespace (`mnt:[4026531841]`), both binds present in `/proc/<MainPID>/mountinfo`, `PrivateMounts/ProtectHome/ProtectSystem=no`; two earlier hypotheses disproved by that output. Real cause: `dedupeMediaList` keyed on `canonicalMediaBasenameKey`, which strips the directory — `exfat/TALK2.mp4` and `bridge/TALK2.mp4` both key `talk2` and the localeCompare tie-break keeps `bridge/`, so every exfat file was swallowed once the payload got copied onto the bridge (WO-468 §3). 18 rows → 10 keys before, 18 after. Added folder-scoped `canonicalMediaRowKey` for the browser dedupe only; the loose key stays directory-blind for caspar-cls-id + playback-tracker bare-name matching | [WO-469](./work-orders/469_WO_MEDIA_BROWSER_DEDUPE_SWALLOWS_DUPLICATE_FOLDER.md) | ✅ Done 10.08 (suite 1935/0/2, eslint 0) — owner: reload GUI after deploy |
| WO-468 | Owner 10.08, fresh install on highascg7579 (192.168.0.27): "no ethernet port found" + "operator gui and pgm2 displaying both the gui". TWO root causes, both fixed: (1) `IFACE_RE = /^(eth\|enp\|eno)[0-9]+$/` accepted digits-only suffixes, so every `enoN` box passed while this box's PCI-named `enp3s0` was filtered out → `interfaces:[]` → GUI "(no Ethernet found)"; (2) `applyMappingGpuPlacementOffsets` SHIFTED screens (`x += offX`) but CLAMPED multiview/prv (`x = max(x, offX)`), discarding the relative offset and parking the operator-GUI head on the same origin as screen_2 (both `6144,0` → two xrandr outputs scanning identical pixels). Fixing it exposed a second defect the clamp had hidden: the multiview offset block ran unconditionally, so vertical layouts moved heads down AND right — the two placements are now one mutually-exclusive axis pass. NOT defects: mounts correct by label; DeckLink driver healthy (profile was the input issue, owner fixed); `REMOVE FAILED` flood = WO-415 reconcile noise. Still open: bridge seeded with stick payload, `led_grid_test` cleared ~3s after PLAY, `sc60mon` audio device absent, ch1 decklink subregion 3840x2160 from a 6144x1536 canvas | [WO-468](./work-orders/468_WO_NEW_BOX_NIC_NAME_AND_GUI_PGM2_OVERLAP.md) | 🟡 Fixed + verified offline (suite 1933: 1931 pass / 0 fail / 2 skip; eslint 0; new `smoke-os-layout-offset-overlap.test.js` + iface cases, both registered in curated FILES — `smoke-project-fps-network.test.js` had never gated CI). NOT deployed to .27; owner QA owed — framebuffer widens 8064 → 9984 |
| WO-467 | Owner: "create and push a fresh latest release to github **with the starter layouts zips**". Published **[2026-08-10_143258](https://github.com/mko1989/highascg/releases/tag/2026-08-10_143258)** — verified via API: `prerelease:false`, targets `main`, 3 assets (server tarball + both starter zips). Tooling: zips now attached **automatically and rebuilt** (they were hand-added before, and `docs/guides/stick/` copies are documented as lagging snapshots), plus a `--latest` flag (`RELEASE_LIB_LATEST=1`) since GitHub never points `releases/latest` at a prerelease. Tree pruned to production first — the previous 46 MiB tarball had shipped devDeps; this one is 13.8 MiB | [WO-467](./work-orders/467_WO_release_carries_starter_zips_and_latest.md) | ✅ Done + verified on GitHub |
| WO-466 | todos10.08: stick docs moved to **Ventoy + reserved exFAT partition at the end** (p1 `Ventoy` ISOs / p2 `VTOYEFI` / p3 `HIGHASCGEXF`) — **reverses** WO-458's "no extra partition" ruling, and that WO now points here. `STICK_QUICK_START.md` rewritten as the single procedure, then narrowed to **Windows-only using standard Disk Management** and a **32 GiB** stick (reserve 10000 MB) per owner. That also **reverses WO-457**, which banned Disk Management because it has no offset option — under Ventoy the reserved space is the only free region, so the objection is gone and the guide says why. MANUAL_STICK narrowed to contents-only, USB_STICK_AFTER_FLASH superseded, HOW_TO/BUILD_AND_FLASH/docs README updated; 3 duplicate procedures → 1. Nine screenshot markers left for the owner | [WO-466](./work-orders/466_WO_stick_docs_ventoy_three_partition.md) | ✅ Done in repo — owner: walk it once on a fresh stick + drop in the screenshots |
| WO-465 | Owner request: build script now ends with `npm install --include=optional`. `install-iso-defaults.sh` prunes dev+optional deps for the squashfs (correct) but the prune outlives the build, so the offline suite then dies `Cannot find module 'acorn'` — hit 3× in one afternoon, once after a build that failed at the audit and produced no ISO. Restore runs AFTER `eggs produce` (so nothing reaches the ISO), as `casparcg` not root (npm as root poisons node_modules/~/.npm ownership), and warns instead of failing. WO-462 sidecar stays last. Suite 1929/0 | [WO-465](./work-orders/465_WO_build_restores_dev_deps.md) | ✅ Done in repo — exercised by the next build |
| WO-464 | Owner: "now eggs produce fails" — `audit-eggs-clone-host.sh` correctly refused a 2nd produce in one boot (the 12:21 build left LIVE bind mounts under `/home/eggs/liveroot`; rm/umount through them has erased /usr before — see RECOVER_DESTROYED_USR.md). **The guard is right; the timing was wrong**: it only ran at build line 92, after apt + `npm ci` + vite + prune + Companion + Calamares had already run (~5 min and a mutated host tree) to report something true at second 0. Same check now runs at the top, before any mutation, exits 1 clean. Rule: **one produce per boot** | [WO-464](./work-orders/464_WO_liveroot_guard_fires_after_five_minutes_of_work.md) | 🟡 Early guard + 4 tests — **owner: REBOOT then re-run the build (not a code failure)** |
| WO-463 | Operator box journal (live from Ventoy stick): `USB auto-mount failed for /dev/sdb1 … already mounted or mount point busy`, once per poll tick forever. `usb-drives-discovery.js:65` checked `hasChildren` for whole disks but NOT for partitions, so the Ventoy boot partition — unmounted, removable, and held `O_EXCL` by the dm map of the booted ISO — looked like a candidate udisks could never mount. A partition with children is never the mountable object (Ventoy/LVM/LUKS: you mount the mapper device). Same mechanism as WO-458, other side. Suite 1920/0 | [WO-463](./work-orders/463_WO_automount_loops_on_ventoy_partition.md) | 🟡 Fixed + 5 tests on the box's real topology — owner QA on the next ISO |
| WO-462 | todos10.08: eggs ISO "invalid magic number / you need to load the kernel first" under Ventoy — **twice, on two different sticks**. Source ISOs are provably good both times (blocks×2048 = size, kernel is a valid bzImage); both stick copies had the identical size but a garbage kernel (`file` → `data`), diverging at 256 MiB then 2.37 GiB. **Root cause: a build/copy race** — `eggs produce` creates the file (its NAME timestamp = produce time) and then `patch-iso-squashfs-calamares.sh` + `inject-iso-boot-branding.sh` re-pack the whole image for ~5 more min (1221 build: name 12:21, final bytes 12:26:00.662). Copying as soon as the file appears reads two different generations of the image. Fix: build writes `<iso>.sha256` LAST as the ready marker + "DO NOT copy before this point"; `verify-stick-iso.sh` consumes it. Round-1 "unflushed copy" diagnosis superseded. Suite 1915/0 | [WO-462](./work-orders/462_WO_ventoy_iso_invalid_magic_number.md) | 🟡 Guard landed — **owner: recopy the 1221 ISO (cmds in WO §Remains), and never copy before the build prints `Done. ISO:`** |
| WO-461 | todos10.08: branded GRUB menu's selected entry invisible. `selected_item_color` was byte-identical to `desktop-color`; the intended blue highlight bar never existed because GRUB's boot_menu has **no background-colour properties** — `menu_bg_color`/`selected_item_bg_color` are unknown and silently ignored (only `*_pixmap_style` paints). Worse, `verify-iso-boot-branding.sh` *rewarded* `menu_bg_color` with an "ok visible labels" line, so the gate greenlit the broken ISO. Now: white-on-dim selection + a real contrast/luminance gate + 5 tests. Suite 1910/0 | [WO-461](./work-orders/461_WO_grub_selected_entry_invisible.md) | 🟡 Fixed in repo — **owner: visible after the next `build-highascg-egg.sh` (build now aborts if contrast regresses)** |
| WO-460 | todos10.08: Settings → Danger zone gained a **Shut down host** button next to Reboot host. Trap found first: `reboot` is the FALLTHROUGH at the bottom of `handlePost()`, so adding the path to `nuclearPaths` without its own branch would have rebooted the box while replying `action:"reboot"` — the shutdown branch is placed (and positionally asserted) before it. `poweroff` was also absent from the sudoers allowlist; `12-passwordless-sudo.sh` gained 2 lines, docs synced. Client rebuilt. Suite 1905/0 | [WO-460](./work-orders/460_WO_shutdown_host_button.md) | 🟡 Implemented + 5 tests — **owner: `sudo bash scripts/setup/12-passwordless-sudo.sh` once per box (else 502), then click it once** |
| WO-459 | todos10.08 §1: Calamares slideshow still read "Broadcast graphics playout" — the todos03.08 §30 "everything says media server" sweep missed it because the slide renders ONLY mid-install (invisible on a running box / on the stick) and a repo grep for "broadcast" drowns in `_wsBroadcast`/`broadcastProject`. `show.qml:76/92` → "Media server — installing to this machine" / "One-box media server"; `verify-iso-boot-branding.sh` now FAILS the squashfs on `broadcast|playout` (it only checked "is it HighAsCG" before, so a produce would reship it forever) + 3 offline guards. Suite 1900/0 | [WO-459](./work-orders/459_WO_installer_slideshow_says_broadcast.md) | 🟡 Repo fixed — **owner: `sudo HIGHASCG_NVIDIA_DRIVER=595 bash tools/eggs/live-usb/build-highascg-egg.sh` (resyncs the stale /etc branding itself via prepare→install-eggs-calamares; build aborts if the wording returns)**. map.html closed: owner "doesnt matter at all" |
| WO-458 | Ventoy stick: `~/exfat` never mounts. Label was fine (`HIGHASCGEXF -> sdb1`, `TYPE=exfat`) — on Ventoy the data partition IS the boot medium, so dm holds `sdb1` `O_EXCL` for the ISO map and every mount returns **EBUSY (rc=32)**. Ventoy publishes a 1:1 full-partition map (`/dev/mapper/sdb1`); new `resolve_usb_dev()` picks it by size+fstype (rejects the 3.5 G `ventoy` ISO map). Also fixed: `systemctl start --no-block` always exits 0, so the direct-mount fallback had been **dead code**. Hostname `highascg7579` is MAC-derived, NOT inherited from the eggs host — working as designed. Suite 1897/0 | [WO-458](./work-orders/458_WO_ventoy_stick_exfat_never_mounts.md) | 🟡 Implemented + 6 new tests — **owner: boot a Ventoy stick after `patch-wo47-exfat-boot-scripts.sh --restart` (dm branch unproven on hardware); layout SETTLED = plain Ventoy, extra partition rejected** |
| WO-457 | Windows stick prep: diskpart/Disk Mgmt fail with "system cannot find the file specified" formatting the exFAT tail. Cause: VDS has no volume attached to the fresh partition (+ Windows cannot see the hybrid ISO extent, so offset-less create lands INSIDE the boot image). STICK_QUICK_START.md hardened: fixed 6 GiB offset always (6291456 KB / sector 12582912), format via `format.exe` OUTSIDE diskpart, recovery steps + troubleshooting row; persistence stays deprecated, no .ps1 | [WO-457](./work-orders/457_WO_windows_diskpart_exfat_format_error_stick_guide.md) | 🟡 Doc pushed 07.08 — owner QA: retry exFAT format on the Windows box |
| WO-456 | Starter zips: exFAT snapshot refreshed to canonical tree (network/, decklink/, .private/ now in the zip); FIRST bridge HIGHASCGDAT-starter-layout.zip (+ `npm run bridge:starter-zip`); bridge seed gained drop-update/ (update helper stages there). Factory-config check: no secrets in published zips | [WO-456](./work-orders/456_WO_starter_zip_refresh_bridge_zip.md) | ✅ Done 07.08 — zips on the release + docs/guides/stick/ |
| WO-455 | todos07.08 addendum: GUI update on installed box → EACCES mkdir /var/cache/highascg/updates. Eggs ships `var/cache/*` empty; node (casparcg) can't mkdir under root-owned /var/cache. Fixed: /tmp/highascg-updates fallback (already whitelisted by deployed sudo helpers), root-side self-heal in the apply helper, tmpfiles.d entry deployed by the exFAT installer. Suite 1891/0 | [WO-455](./work-orders/455_WO_gui_update_eacces_var_cache.md) | ✅ Done in repo — **owner: run the one-liner on machine 2 (WO §Remains) + install tmpfiles.conf on this box** |
| WO-454 | Mode-only churn: 472 uncommitted + 1039 COMMITTED files flipped to 755 by exFAT-round-trip perms relayed through Syncthing (ignorePerms was false). 739 non-scripts normalized back to 644 (2 commits, zero content staged); Syncthing highascg folder ignorePerms=true via REST (verified). | [WO-454](./work-orders/454_WO_exec_bit_pollution_syncthing_perms.md) | ✅ Done 07.08 — **owner: set Ignore Permissions on peers before their first rescan when powered on** |
| WO-453 | todos07.08: simulator parity + CG Studio unify + Mac/Win stick guides. `--no-caspar` never reached the sim stack (appCtx.amcp null → silent no-op takes, empty catalogs, no OSC → dead timers/playlists); AmcpSimulated rewritten stateful (INFO CONFIG = generator XML, CLS = real media scan, 2.6-dev stage XML) + sim-osc-feeder drives the production OscState pipeline (LOADBG AUTO promotes → playlist advance works). Launcher un-rotted (repo-root probe, portable:sim restored, .cmd/.command fixed); CG Studio launcher mirror+:4300 host DELETED — button opens server-hosted `/cg-studio/` (mirror was 1 commit stale: exports 404'd `lt-engine-controls.js` on air); 7 stick guides repaired to WO-47 canonical `drop-update/`, mac/win seed scripts aligned. Verified live on box (:8091 isolated sim: 2ch/63 media/73 templates, timers advance, pause freezes) + suite 1888/0 | [WO-453](./work-orders/453_WO_simulator_parity_cg_studio_unify_stick_guides.md) | ✅ Done 07.08 — CI green, release 2026-08-07_120130 published; owner QA: run sim on a prep laptop |
| WO-452 | Owner: "commits flagged as some random guy" + "actions fail at every push". `.git/config` carried Antigravity IDE's identity (`antigravity@google.com` → GitHub account "shimonenator"); fixed to mko1989, history NOT rewritten (Syncthing peers). Actions red = Aug 6 ~11:30–17:00 UTC GitHub incident (Pages deploy 10-min poll timeouts, then hosted-runner starvation), not code: Pages rerun green in 1m30s. Head CI run 31121973364 is an un-cancellable zombie (409 on force-cancel) → `workflow_dispatch` added to ci.yml | [WO-452](./work-orders/452_WO_ci_incident_aug6_and_commit_attribution.md) | ✅ Done 07.08 — CI+Pages green on 70b96b0, GitHub shows mko1989 (run also verified WO-450 r3–5) |
| WO-451 | todos06.08 line 26: GPU layout editor — one-slot drag was a silent no-op (insert-before-only arithmetic), and the "Detected GPU wiring differs" banner compared rows INCL. slotOrder so any deliberate reorder alarmed forever. Drop targets now split top/bottom half; mismatch compares the jack SET (`topologyPairSetDiffers`). Verified live: `topologyMismatch:false` on the owner's reordered layout after restart | [WO-451](./work-orders/451_WO_gpu_layout_reorder_and_mismatch_banner.md) | 🟡 Implemented (built+F5, restarted) — owner QA: one-step drag + banner stays gone |
| WO-450 | todos06.08 line 15: companion "false connected flag" + dead picker. Ground truth (netcat): Companion 5.0.2 IS up but `CAPS SUBSCRIPTIONS=0` — **owner must enable Button Subscriptions API in Companion Settings** for previews. Fixes: status endpoint now live-probes the CAPS handshake (was echoing the passive client = wrong whenever no picker open); picker renders a clickable blank grid without previews; CAPS-after-BEGIN race fixed. Verified against live Companion post-restart | [WO-450](./work-orders/450_WO_companion_status_truth_and_picker.md) | 🟡 Implemented — **owner: tick Button Subscriptions API in Companion** |
| WO-449 | todos06.08 line 13: extending a playing clip past its media length blanked at file end — loop requirement flip (implicitLoop/clip.loop) was invisible to timelineClipTransportStale, so no re-PLAY LOOP was sent. loopStale detection added; smoke proves PLAY→extend→PLAY LOOP→no churn | [WO-449](./work-orders/449_WO_clip_extend_loop_replay.md) | 🟡 Implemented (restarted) — owner QA: extend a playing clip past source length |
| WO-448 | todos06.08: timeline was BORN routed to PGM on pgm_only screens (client coerced program:true + engine `_channelsFor` fell back to programCh(1) for PRV-only requests) — playhead jumps painted over the live look. Now unrouted until the PGM tick, timeline Take, or look playback; PGM checkbox no longer force-checked/disabled. smoke-preview-amcp-channel assertions repointed | [WO-448](./work-orders/448_WO_timeline_unrouted_until_take.md) | 🟡 Implemented (built+F5, restarted) — owner QA: scrub with a look playing |
| WO-447 | Owner mid-session: screen-consumer vsync default ON → OFF (GL sync owns pacing; "with the gl sync and vsync off the playback is perfect"). 3 default sites flipped; seeded screen_2/4_vsync:true healed via live settings API (all four screens now false); vsync-nvidia doc marked superseded for that row; multiview + operator-GUI vsync untouched (part of the verified-perfect state) | [WO-447](./work-orders/447_WO_vsync_default_off_screen_consumers.md) | ✅ Done 06.08 — XML updates on the owner's next Apply |
| WO-446 | WO-445 §2b consolidations: which.js reimpl deduped (130→87); artnet↔sacn receivers extracted to DmxBorderReceiverBase (~250 shared lines) — which exposed **the sixth lost-wiring find: SacnReceiver was never instantiated** (WO-179 T179.4 shipped class+UI select, no dispatch); new LightingInputReceiver facade wires it with a 2-line index.js change and fixes two latent sACN status bugs. Mixer/template/config-generator dedupes deferred (owner todos took priority) | [WO-446](./work-orders/446_WO_DUP_CODE_CONSOLIDATIONS.md) | 🟡 Implemented (restarted) — sACN input QA needs a sACN source; §2.9 open |
| WO-445 | todos06.08 item: full-project audit — 500-line violations, dead + duplicate code. **CI on main is RED** at the 500-line step (`device-view-inspector-gpu-video-modeline.js` 507, WO-442 push), masking two more red gates behind it: new orphan export `openSaveDeviceSnapshotModal` (WO-367 gate) and an eslint NBSP error in the WO-440/441 smoke. Dead files found: `template/led-grid-test-{core,patterns}.js` (~500 lines, WO-411's known orphans still hand-synced) + `client/components/device-view-gpu-source-inherit.js` (183, lib copy is the live one). 129 duplicate groups scanned; top actionable: gpu-modeline inspector family (~165 lines, overlaps the gate-breaker — one dedupe fixes both), caspar config generators (~220), artnet↔sacn (~63) **Fixes landed same day (owner "go"):** modeline event wiring moved to -preview.js (507→475), Save-device-snapshot wired into Devices header (WO-49's missing entry point — the -apply/-os pair over the dedupe turned out DEAD and was deleted with the other 3 files, 1,034 lines), NBSP escaped in the WO-441 smoke. All gates green locally, suite 1865/0/2, built+F5 | [WO-445](./work-orders/445_WO_500_LINE_DEAD_DUP_CODE_AUDIT.md) | 🟡 Implemented — CI green pending push; owner QA: ⭳ snapshot button; §2b follow-ups open |
| WO-444 | Owner "how can i check if the gl sync is applied?" → it WASN'T, despite a perfect 14:03 Apply (env file got DP-0, caspar relaunched 6s later, process had NO var): run.sh sourced caspar-env once at SUPERVISOR start (09:49, pre-var) and respawned the binary in that stale env. Sourcing now per-launch inside run_caspar (clear-before-source; absent line clears the export); replaced atomically (cp→sh -n→mv, running sh keeps old inode). **Owner: `sudo systemctl restart casparcg-server` ONCE** (permission layer blocked it here), then every Apply carries env changes. Completes WO-407→439→440 | [WO-444](./work-orders/444_WO_caspar_env_sourced_per_launch.md) | ✅ Done 06.08 (suite 1865/0/2) — pending the one-time service restart |
| WO-443 | todos06.08 (2nd batch, late add): monitor-role audio outputs no longer ride in projects — save filters `role:'monitor'` out of the hardware slice; load keeps the BOX's monitor entries and drops any the project carries (legacy projects embed this box's `sc60mon` entry). Same box-local principle as WO-425 | [WO-443](./work-orders/443_WO_monitor_audio_output_not_project_scoped.md) | ✅ Done 06.08 (suite 1863/0/2, service restarted) — owner QA on project save/reload |
| WO-442 | Owner follow-up on WO-441: the 1920/1080 in the custom W/H boxes were WO-437-bug fossils — cable-time inherit seeding (`gpuScreenInheritedSettingsPatch`) wrote `screen_N_custom_*` from the corrupt feed pair (mode 2160p5000 + width 1920) the pre-437 resolver returned. Config healed to 3840/2160/50 via settings API; the custom row now exists only when mode=Custom and prefills from the previously active mode, never stale keys | [WO-442](./work-orders/442_WO_custom_dims_fossil_keys.md) | ✅ Done 06.08 (suite 1860/0/2, built+F5) — owner eyeball |
| WO-441 | todos06.08 (2nd batch) items 1-2: GPU inspector "Native mode" split mid-word (`word-break: break-all` on kv-val licensed a break inside "Hz") → word-boundary wrap + NBSP-joined mode string; custom W/H/FPS number inputs kept natural ~150px width and overflowed the sidebar → `flex:1 1 0; min-width:0` three-equal-columns | [WO-441](./work-orders/441_WO_gpu_inspector_native_mode_wrap_and_input_fit.md) | ✅ Done 06.08 (suite 1858/0/2, built+F5) — owner eyeball |
| WO-440 | todos06.08 (2nd batch) item 3: Apply button hit the WO-337 unchanged-gate ("no changes — no restart") which is blind to caspar-env drift (GL sync lives OUTSIDE the compared XML), and the button never sent the already-plumbed `force`. Button now always sends `force:true` (owner ruling: Apply = restart, every time); background project-hardware apply deliberately keeps the fast path. **Owner: press Apply once** → caspar-env gains `CASPAR_GL_SYNC_DISPLAY=DP-0`, relaunched Caspar gets the var (session's permission layer blocked firing the apply directly) | [WO-440](./work-orders/440_WO_apply_button_always_restarts_caspar.md) | ✅ Done 06.08 (suite 1858/0/2, built+F5) — GL sync live after the owner's next Apply click |
| WO-439 | Owner escalation on WO-437: the "NVIDIA sync to display" tick WAS set and WAS read, but its port→name lookup only knew screen_N_system_id/plan.screens (empty on this mapping-only rig) — tick silently dropped for BOTH consumers (caspar GL sync env AND HIGHASCG_NVIDIA_SYNC_OUTPUT policy export). New `resolveGpuPortIndexToXrandrOutput` resolves the ticked port via the graph's gpu_p{N-1} connector; precedence now tick → screen assignment → mapping wall origin. Verified DP-0 via the tick against the live merged config | [WO-439](./work-orders/439_WO_nvidia_sync_tick_dropped_on_mapping_rig.md) | ✅ Done 06.08 (suite 1853/0/2, service restarted) — caspar-env var lands on next Apply + caspar restart |
| WO-438 | todos06.08 item 4: practical Pixel-Map-destination test guide — `docs/WALKTHROUGH_PIXELMAP_DESTINATION_TEST.md` (mapping-node vs pixelmap disambiguation, end-to-end mechanism, universe math, exact UI steps, hardware-free python3 ArtDMX loopback test — box has no tcpdump). Serves WO-242's still-open A242.1 | [WO-438](./work-orders/438_WO_pixelmap_destination_test_guide.md) | ✅ Done 06.08 (doc-only) — owner: run the loopback test |
| WO-437 | todos06.08 items 2+3: GPU ports reported 1080p because outputs stored stale width:1920/height:1080 next to mode 2160p5000 (pre-437 saveCustom sent the hidden custom inputs with every standard-mode pick; resolvers preferred stored dims — display-only, OS layout was saved by the mapping rects). Standard mode now authoritative over stored dims (client+server resolvers unified), saveCustom derives dims from the mode, live graph healed via API (both outputs 3840×2160). GL sync: NOT set live (empty plan.screens on this mapping-only rig blinded WO-407 auto) — auto now falls back to the leftmost mappingGpuOutputs head, resolves DP-0; lands in caspar-env on next Apply + caspar restart | [WO-437](./work-orders/437_WO_mapping_dims_stale_and_gl_sync_mapping_rig.md) | ✅ Done 06.08 (suite 1849/0/2, built+F5+service restart) — GL-sync var live after owner's next Apply |
| WO-436 | todos06.08 item 1: mapping-node inspector values reverted on first entry — two stacked caches: bare `load()` after every save re-rendered the 5s-cached pre-edit snapshot (visual revert), and `fetchDeviceView()`'s read-modify-write hit the browser HTTP cache (max-age=3) on fast retries, saving a pre-first-save graph back (REAL server-side revert). All 9 inspector reloads now `forceRefresh: true`; `fetchDeviceView` busts with `_ts`; new smoke pins both | [WO-436](./work-orders/436_WO_mapping_node_inputs_revert_first_entry.md) | ✅ Done 06.08 (suite 1844/0/2, built+F5) — owner QA on feel |
| WO-435 | todos06.08 item 5: Devices-tab Refresh + Save/Load-snapshot buttons removed (Refresh auto on tab-in + every config-changed signal; snapshots ride in the project file; snapshot modals stay reachable via project-hardware-reconcile). BONUS: WO-433's `~/Downloads` scan in `src/api/system-hardware-decklink.js` found REVERTED by an uncommitted working-tree deletion (Syncthing peer-revert signature) — restored from HEAD, WO-433 smoke green again; watch for re-clobber | [WO-435](./work-orders/435_WO_devices_tab_redundant_buttons.md) | ✅ Done 06.08 (suite 1842/0/2, built+F5) — owner eyeball |
| WO-434 | Owner 05.08: "why does it even seed the drop update on a fresh stick" — right: on embed-server ISOs the seeded drop is a byte-identical copy of the squashfs (dead weight) that turns into a downgrade grenade as the stick ages (the WO-433 poisoning mechanism). Flash now seeds only for WO-47 exFAT-only ISOs or explicit `HIGHASCG_SEED_DROP_UPDATE=1` (sneakernet update stick); stick QA test-04 accepts no-drop when the squashfs embeds the server; the orphaned exFAT-rsync fix in the seed script (uncommitted since an earlier session) committed alongside. Insert-time apply of OLD sticks unchanged — that decision stays open in WO-433 | [WO-434](./work-orders/434_WO_no_drop_update_seed_on_embed_server_sticks.md) | ✅ Done 05.08 (smoke 3/3) — proves out on the next flash: expect "skip: embed-server ISO" at step 4/5 and test-04 green without a drop |
| REVIEW-2026-08-03 | Full codebase review wave (7 read-only reviewers, ~265k lines). Top verified items: **path traversal** on `/api/project/file/<slug>` (LIVE probe returned config/general.json; `/api/project/load` + `../` slug makes autosave a config-overwrite primitive), **AMCP CR/LF injection** via `param()` (template data / encoderPreset → second command, e.g. `KILL`), **5 missing stream/spawn `'error'` handlers** (uncaughtException → process.exit(1) = full outage on disk-full), **take phase `catch (_) {}`** (AMCP hiccup → black channel, no log), **inverted `profiler_ch{N}_healthy`**, **3 one-way latches that kill autosave silently**, **USB import API entirely dead** (route prefix mismatch, 404 verified), exFAT sync has no staleness guard + non-atomic writes (= WO-415 mechanism), 18 rotted tests outside the curated gate. Box runs `enforceAuth:false` on `0.0.0.0:4200` → all API findings are LAN-reachable | [reviews/2026-08-03-SUMMARY](./reviews/2026-08-03-SUMMARY.md) | 🟡 Rows 1–5 (WO-418) + 6, 7, 10 (WO-419) fixed, restarted, live-probed 04.08. **Owner decisions 04.08: API auth stays OFF** (exposure note closed as won't-do; box relies on network trust), **config restore not needed** ('does not matter at all' — the 2 WO-237 suite reds will persist until the box is reconfigured or the smoke is repointed). Rows 8–9 resolved 04.08 (owner reframe + WO-421); engine §3 closed by WO-422. **Review wave fully closed 04.08** — every numbered row fixed, won't-do'd with reason, or reframed |
| DOC | WO-407 explainer written for future reference: `docs/reference/multi-head-gl-vblank-sync.md` (why per-display vblank clocks beat on a multi-head X screen, how the fix is wired box-locally, diagnosis recipe); cross-linked from the vsync doc whose recipe was necessary-but-not-sufficient | [multi-head-gl-vblank-sync.md](../docs/reference/multi-head-gl-vblank-sync.md) | ✅ 03.08 |

Owner decision 30.07 (todos30.07 §xcompmgr): compositor-over-holes idea **rejected** ("not worth it") — X SHAPE hole punching stays; no WO opened.

Last updated: 2026-08-04 (evening) — queue swept: the 03.08 review wave is FULLY CLOSED (rows 1–5 WO-418, rows 6/7/10 WO-419, rows 8–9 WO-421 after the owner's reframe, engine §3 WO-422 — each live on the box, restarts done). WO-415 closed NOT-AN-INCIDENT (normal eggs-produce config reset, owner). CI un-broken after 6 days red (WO-420: lint ratchet 226>224 masking a Prettier failure masking an npm-audit block — cap now 218, all green). Suite **1820 pass / 0 fail / 2 skip** — first 0-fail since the produce run, after the WO-237/406 smoke repoints AND a real fix (monitor bus now rate-matches the operator-GUI main; was the 25-vs-50 fps chop condition). WO-368 option B done (42-file shader commit; post-produce config/ committed as baseline; `git status` CLEAN, 0 entries — nginx conf restored from repo, produce had reverted the WO-402 fix on disk). WO-371 option C done (⏮/⏭ step the PRV render, schedule-free). WO-399 row corrected (was done 30.07). WO-405 idle baseline captured (caspar 8 %, node 1 %). Remaining open items ALL need the owner: WO-366 ticks, WO-402 sudo nginx reload, WO-416/417 stick/target retries, WO-405 round-2 (needs a show config), owner-QA rows.

Previously: 2026-07-29 (later) — WO-387 (installed-app launcher) implemented, built, server restarted and kiosk reloaded; suite **1712 pass / 0 fail / 2 skip**. Owner decisions recorded from `DECISIONS_PENDING_2026-07-28.md`: **WO-371 = C** (PRV stays paused, ⏮/⏭ step the preview), **WO-368 = B** (commit the shaders now in the folder) — both unblocked, NOT implemented. WO-366's seven yes/no ticks are still blank. Earlier today: WO-381 implemented in two rounds, built, server restarted and kiosk reloaded on the box; suite **1678 pass / 0 fail / 2 skip**, unwired-exports baseline shrunk 695→693. WO-210's CSS guard and WO-226's T226.5 test were repointed to the surviving controls (reason recorded inline). Lint not runnable on this box (no local eslint) — CI covers it.

Previously: 2026-07-28 (late) — implemented and committed: WO-370, WO-365, WO-369, WO-367, WO-373, WO-344, plus first-ever test coverage for WO-343. WO-372 partly (owner Companion restart owed). **WO-371 decided by the owner: option B** (preview plays every item and loops) — unblocked, not implemented. Stale statuses corrected this batch: WO-343, WO-344 (both were already implemented; WO-361 corrected to CORRECTED). Client rebuilt + kiosk reloaded; **the server halves of WO-373 and WO-344 need `kill -TERM $(systemctl show -p MainPID --value highascg)`**. Suite 1624: **1622 pass / 0 fail / 2 skip**; lint 0 (capped 224); `check-unwired-exports` CI gate with a 695-entry shrink-only baseline. Owner decisions still open: WO-366 (seven yes/no), WO-368 (A/B/C) — see `work/DECISIONS_PENDING_2026-07-28.md`. Commits await `git push origin main`.

Previously: 2026-07-28 — completeness audit of todos 14.07→28.07 (see `work/checklist28.07.26_manual_verify.md`) + checklist27 note sweep; WO-365..373 opened, none implemented. Stale statuses corrected: WO-342, WO-345. Suite 1559 pass / 0 fail / 2 skip at 637965c.

Previously: 2026-07-15 evening (WO-242 committed 74990aa: gate 320/318/0-fail re-verified, vite build OK; config/*.json left dirty pending CONFIG-APPLY-CLOBBER decision; **highascg restart still pending for WO-239 frozen-variables fix**; reload browsers; local commits await `git push origin main` by owner)
