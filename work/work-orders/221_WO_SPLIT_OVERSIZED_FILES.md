# WO-221 — Split all code files exceeding the 500-line limit into smaller modules

**Status:** DONE (06.08.26 — superseded piecemeal: every census file was split by WO-114/118/122/140-era work, and the rule is CI-enforced by `tools/ci/check-max-file-lines.js` (WO-445). Census re-run today: 0 files over 500.)
**Priority:** Medium (hygiene; owner rule: 500 LOC max across ALL code files incl. frontend CSS and templates)
**Date:** 2026-07-15
**Source:** owner loop directive 2026-07-15 ("check all highascg code files including frontend css and templates... write wo to split... spawn sonnet for it").

---

## 1. Census (2026-07-15, excludes node_modules/dist-web/vendor)

**Phase A — production client JS (7):** timer-control-panel.js 684, settings-modal-mount-hardware.js 672, scene-state.js 615, scenes-editor.js 543, scenes-compose.js 537, preview-canvas-draw-stacks.js 526, device-view-cables.js 516 (all under client/components|lib).
**Phase B — server (3):** index.js 515, src/engine/multiview-apply.js 502, src/api/router.js 502.
**Phase C — CSS (5):** styles/07b-audio-mixer-modal-shell.css 878, 08c-modals-misc.css 702, 01a-base-theme-header-connection.css 613, 06c-inspector-effects-pip.css 545, 02c-timeline-multiview-sources-sidebar.css 525. (Note: 01a already has partial splits 01a1/01a2/01a3 — CHECK whether 01a still duplicates their content or they are additive; dedupe as part of the split.)
**Phase D — templates (2):** template/lower-thirds/lt-engine.js 578, template/led_grid_test.js 522.
**Phase E — tools/tests (5, lowest value):** tools/smoke/smoke-config-generator-routing.js 762, smoke-mapping-gpu-os-layout.js 675, smoke-wo210-screen-timers.test.js 615, tools/map/ast-scanner.js 590, client/tools/electron-launcher/index.html 562.

## 2. Rules (non-negotiable)

- **Behavior-preserving mechanical extraction only** — move code, add imports/exports; zero logic changes. Follow the repo's established split patterns: JS → sibling `-<topic>.js` modules (cf. scene-take-lbg / -jobs / -merge / -helpers; audio-mixer-panel-* family); CSS → new numbered sheets + an `@import` line in [client/styles.css](../../client/styles.css) in the SAME cascade position (order matters).
- After EVERY file's split: node --check (or eslint for ESM), the full gate (`node tools/ci/run-offline-tests.js`), and `npx vite build` when client files changed. A split that changes any test outcome is wrong — revert and re-cut.
- Templates (Phase D) are LIVE Caspar CG assets: lt-engine.js is loaded by lower-third templates — verify how it is included (script src) before splitting; if splitting requires multiple script tags in template HTMLs, update every including template. led_grid_test.js is a test asset — lowest risk.
- Server Phase B: index.js and router.js are boot-critical — extract cohesive blocks (e.g. router.js: move the `/api/timers|countdown|system` registration clusters into `router-registrations-<domain>.js` functions called from router.js; index.js: extract the onAfterInfoConfigReady hooks into `src/engine/startup-hooks.js`). The registration EXECUTION ORDER must not change.
- Target: every touched file lands under 500 lines INCLUDING the new modules (do not create a new >500 file).

## 3. Tasks

- [x] T221.A Phase A (7 client JS files) — one sub-batch per file, gate+build between files. Done 2026-07-15.
- [ ] T221.B Phase B (3 server files).
- [x] T221.C Phase C (5 CSS sheets, dedupe 01a vs 01a1-3). Done 2026-07-15.
- [ ] T221.D Phase D (2 templates).
- [ ] T221.E Phase E (5 tools/tests).
- [ ] T221.F Final: re-run the census command from §1 — zero files >500 outside node_modules/dist-web; full gate; vite build; work log updated with the new module map.

## 4. Acceptance criteria

- [ ] A221.1 Census clean (no code file >500 LOC); gates green; no behavior change (owner smoke on the UI).

## 5. Work log

- 2026-07-15 — WO created from census (22 files).
- 2026-07-15 — T221.A Phase A complete (7 client JS files). Re-measured before cutting — all 7 had grown since the original census (some other agent's edits landed on `timer-control-panel.js` and `settings-modal-mount-hardware.js` in the interim); no file was found already under 500. `settings-modal-mount-hardware.js` was re-read before every edit to check for concurrent-churn; it was stable (unchanged md5) at both checkpoints, so it was NOT skipped. Gate (`node tools/ci/run-offline-tests.js`: 210 pass / 2 skipped, unchanged) and `npx vite build` were run and stayed green after EVERY file (7/7 individual runs + 1 final combined run). `./node_modules/.bin/eslint --quiet` clean on every old+new file (0 errors; pre-existing unrelated warnings such as the dead `resolveSourceThumbnailUrl` import in scenes-compose.js and innerHTML-escape warnings were left untouched — out of scope for a mechanical split). No smoke needed updating: the WO-196/WO-210 smokes that source-grep `timer-control-panel.js` for `helpText`, `Screen timers`, `/api/timers/list`, `/api/timers/cmd`, `/api/timers/visible` were checked first and the extraction was deliberately routed around those literals (kept `refreshTimerList`/`onTimerAction`/`onToggleVisible` and the `helpText` constant in the main file); re-ran both smoke files directly after the cut (30/30 pass). The WO-223/B150 smokes that load `scene-state.js` directly (`smoke-look-preset-operator-bugs.test.js`) were also re-run directly (14/14 pass) since that file's split touched preset/timer/border method locations.

  Module map (old file LOC → new LOC, + new/extended sibling modules):
  - `client/components/device-view-cables.js` 517 → 298 + `device-view-cables-physics.js` 223 (new: cable Verlet-physics simulation + color hashing, `getCableColor`/`getOrBuild` exported).
  - `client/components/settings-modal-mount-hardware.js` 673 → 351 + `settings-modal-mount-hardware-summary.js` 327 (new: `renderHardwareSummary`/`formatBytes`, the System-pane hardware summary DOM builder).
  - `client/lib/scene-state.js` 616 → 409. Split via the mixin-into-prototype pattern already established (and documented in WO-114's log) by `scene-state-layer-ops.js` — that module existed on disk but had been silently un-wired (nothing imported `mixinSceneStateLayerOps`; the 6 methods it contained were duplicated directly on the class). Re-wired it (added `setLayerNumber`, previously missing) and extended the same pattern with 3 new sibling mixins: `scene-state-layer-ops.js` 94 → 101 (now actively used again: `nextLayerNumber`/`addLayer`/`removeLayer`/`reorderLayers`/`setLayerNumber`/`setLayerSource`/`patchLayer`/`setDefaultTransition`), `scene-state-global-border-ops.js` 53 (new: 8 global-border wrapper methods), `scene-state-preset-ops.js` 73 (new: 14 layer/look preset wrapper methods), `scene-state-timer-ops.js` 50 (new: 6 timer-instance wrapper methods, WO-208). All 4 mixins are called via `Object.assign(SceneState.prototype, {...})` right after the class body — behavior identical to defining the methods inline, just physically relocated; none of the new modules import `scene-state.js` back (no circular import).
  - `client/components/scenes-editor.js` 543 → 490 + `scenes-editor-deck-ingest.js` 60 (new: `ingestDeckDroppedFiles` deck-drop upload/poll-media-list helper, previously a closure with no state-store dependencies).
  - `client/components/scenes-compose.js` 537 → 409 + `scenes-compose-layer-thumb.js` 156 (new: `buildComposeLayerContent`/`makeLayerSourcePlaceholder`, the per-layer thumbnail/placeholder/live-thumb DOM builder used inside the compose layer loop).
  - `client/components/preview-canvas-draw-stacks.js` 526 → 378 + `preview-canvas-draw-placeholder.js` 160 (new: `drawPlaceholderFill`/`drawAudioOnlyPreviewFill`/`drawPreviewStatusText`/`sourceFallbackLabel`, pure canvas-drawing helpers shared by `drawSceneComposeStack` and `drawTimelineStack`).
  - `client/components/timer-control-panel.js` 691 → 470 + `timer-control-panel-display.js` 85 (new: `DEFAULT_TIMER_CONFIG`/`computeDisplayTime`/`formatDisplayTime`, pure no-DOM helpers) + `timer-control-panel-settings-form.js` 154 (new: `buildTimerSettings`, the per-timer settings-panel form builder, taking `refreshTimerList` as an injected dependency to avoid a circular closure).

  Gate/build results: `node tools/ci/run-offline-tests.js` → 212 tests, 210 pass, 0 fail, 2 skipped (unchanged from baseline; the 2 skips are pre-existing local-only tests gated behind `CI=1`). `npx vite build` → success after every file, no new warnings (the two `INEFFECTIVE_DYNAMIC_IMPORT` warnings are pre-existing and unrelated to Phase A). Nothing was skipped in Phase A.

- 2026-07-15 — T221.C Phase C complete (5 CSS sheets). Re-measured before cutting — all 5 had grown since the original census: 07b 878→906, 08c 702→727, 01a 613→613 (unchanged), 06c 545→545 (unchanged), 02c 525→525 (unchanged).

  **Pre-existing orphan discovery (important, expands on the WO's 01a-only note):** `client/styles.css`'s `@import` list only references the 5 named parent sheets — it does NOT import `01a1/01a2/01a3`, `02c1/02c2/02c3`, `06c1/06c2/06c3`, or `08c1/08c2/08c3/08c4`, all of which already existed on disk (added wholesale by commit `89b1e68`, 2026-07-04, an unrelated 416-file "Improve timeline editor…" commit — not this WO). Grepped every `.css`/`.js`/`.html` file for each of those 13 basenames: zero references anywhere. So all 13 were **dead, unimported code**, not part of the live cascade. Verified their actual relationship to the current parent content before trusting them as a starting point:
  - `02c1+02c2+02c3` (68+15+442=525 lines) — byte-for-byte identical to `02c-timeline-multiview-sources-sidebar.css` (`diff` exit 0, no filtering). A correct, complete, stale-free split that was simply never wired in. **Reused verbatim.**
  - `01a1+01a2+01a3`, `06c1+06c2`, `08c1-4` — NOT clean duplicates: `diff` showed drift in both directions (e.g. old `01a2/01a3` carried a `.header-audio` block that does not exist in the current live `01a` at all — it only exists in the live `08c-modals-misc.css` — while current live `01a` has a `.header-bar__map-link` block the old `01a2` was missing entirely; similar drift in `06c`/`08c`). Trusting these old files would have either duplicated `.header-audio` into two live cascade positions or dropped current rules. **Not reused** — instead cut fresh siblings directly from the current parent's actual content (see maps below), which mechanically satisfies the WO's dedupe instruction ("keep exactly one copy in the correct cascade position, sourced from the live winner"). The stale `01a1/01a2/01a3` filenames were overwritten in place with correct fresh content (same names, continuing the naming per the WO's "if additive, continue naming" branch, since they are otherwise unreferenced). The stale `06c1-inspector-pip.css`, `06c2-inspector-effects.css`, `08c1-modals-led-test.css`, `08c2-modals-load-project.css`, `08c3-modals-usb-import.css`, `08c4-modals-reconcile-banners.css` could **not be deleted** — the harness's auto-mode classifier blocked an `rm` of pre-existing files as "irreversible local destruction" outside this task's authorized scope (creating new sheets, not removing old ones). They remain on disk, unreferenced by `styles.css` or any other file (re-verified after the split — zero references), functionally inert. **Flagged for a follow-up cleanup WO** to delete them with explicit user sign-off.

  Split method: contiguous, in-original-order cuts only (no reordering across files) so the global `styles.css` cascade order is preserved exactly — each new sibling `@import` was inserted immediately after its parent's line. `npx vite build` was run after every single sheet's edit and produced a **byte-identical** `dist-web/assets/main-*.css` (173.13 kB / gzip 29.17 kB) every time, from the very first split through the last — strong evidence of zero computed-CSS drift. `node tools/ci/run-offline-tests.js` stayed at 284 pass / 0 fail / 2 skipped (unchanged) after every sheet.

  Module map (old file LOC → new LOC, + new sibling sheets, in cascade order):
  - `client/styles/07b-audio-mixer-modal-shell.css` 906 → 453 (audio-mixer inspector shell only) + `07b1-modal-shell-base.css` 59 (new: generic `.modal-overlay`/`.modal-content`/`.modal-header`/`.modal-close`) + `07b2-timer-control-panel.css` 402 (new: all `.timer-control-panel__*` rules, WO-186/WO-210/WO-226).
  - `client/styles/08c-modals-misc.css` 727 → 266 (LED test modal, scenes-rundown-playback, global `.btn` variants, load-project modal) + `08c2-modals-header-audio-usb-import.css` 237 (new: `.header-audio*` dropdown + `.usb-import-modal*`) + `08c3-modals-hardware-reconcile-banners.css` 233 (new: `.hardware-reconcile-modal*`, `.hardware-reconcile-banner*`, `.media-reconcile-banner*`, `.replication-status-banner*`, `.timer-inspector-modal*` WO-226 T226.4).
  - `client/styles/01a-base-theme-header-connection.css` 613 → 10 (stub: header comment only, all content deduped out) + `01a1-base-theme-tokens.css` 64 (font-face, `:root` tokens, `*`/`body`/form-element resets, `.app`) + `01a2-header-bar.css` 429 (`.header`, all `.header-*` indicator/status/PGM-timer rules, including `.header-bar__map-link` which the old stale file was missing) + `01a3-connection-chrome.css` 128 (`.server-config-strip*`, `.status-dot*`, `.connection-eye*`, `@keyframes pulse`; deliberately excludes `.header-audio*`, which stays solely in `08c2` where it already lived live).
  - `client/styles/06c-inspector-effects-pip.css` 545 → 137 (Inspector Effects cards + PIP overlay inspector + `.inspector-btn*`) + `06c2-inspector-layer-style-presets.css` 117 (new: layer-style clipboard, look/layer presets, `panel-inspector-mode*` hints, `panel-presets-import*`) + `06c3-inspector-lt-roster.css` 300 (Lower Third inspector fields + roster table, overwriting the stale 408-line version which had drifted from current content).
  - `client/styles/02c-timeline-multiview-sources-sidebar.css` 525 → 5 (stub: header comment only) + `02c1-timeline-sidebar.css` 68 + `02c2-multiview-sidebar.css` 15 + `02c3-sources-sidebar.css` 442 (all three reused verbatim, unchanged — see dedupe note above).

  `client/styles.css` `@import` edits: 12 new `@import` lines added, each immediately after its parent's existing line, preserving cascade order exactly (see diff in the file itself).

  Smoke updates (source-grep smokes that referenced moved classes, per the WO's instruction to update and log every one):
  - `tools/smoke/smoke-wo210-screen-timers.test.js` — `cssPath` for the `timer-control-panel__*` class-presence test changed from `07b-audio-mixer-modal-shell.css` to `07b2-timer-control-panel.css` (all 9 asserted classes live there now). Re-ran directly: 23/23 pass.
  - `tools/smoke/smoke-wo226-timer-overlay.test.js` — `modalsCss` path for the `.timer-inspector-modal`/`.timer-inspector-modal__fade-row` checks changed from `08c-modals-misc.css` to `08c3-modals-hardware-reconcile-banners.css`. The `audioCss` checks (`.audio-mixer__timers-compact`, `.audio-mixer__timer-compact-btn`) needed no change — those rules stayed in `07b` (they're part of the audio-mixer-inspector shell, not the timer-control-panel or modal-shell blocks that moved). The `scenesCss` check (`06a3-scenes-deck-multi.css`) is an untouched file, out of scope. Re-ran directly: 8/8 pass.
  - Searched for WO-219/WO-227 smokes mentioned in the task brief as potentially grepping these 5 files: no WO-219 smoke exists in `tools/smoke/`; the WO-227 smoke (`smoke-wo227-mixer-dense.test.js`) greps `07c2-audio-mixer-view-console-layout.css`, an already-split sibling untouched by this work — no update needed.

  Final verification: all 5 originally-oversized sheets and all 8 new/reused sibling sheets are under 500 lines (largest is `02c3-sources-sidebar.css` at 442 and `01a2-header-bar.css` at 429). Repo-wide scan of `client/styles/*.css` confirms zero files ≥500 lines (the next-largest, `07a-scenes-compose-canvas.css` at 487, is pre-existing and out of this WO's scope). `npx vite build` and `node tools/ci/run-offline-tests.js` (284/284 non-skipped pass) both green on the final combined state. No JS, `template/`, or server files were touched — only `client/styles/*.css`, `client/styles.css`, and the 2 smoke test files listed above.

  Note on concurrent repo activity: mid-task, `git log`/`git show HEAD` revealed another agent (`Antigravity`, commit `7802409` "WO-235: new-Caspar protocol compat fixes…") committed to this same working tree while this split was in progress — that commit's diff shows it swept up this task's earliest edits (the `02c1-3`/`07b1-2` `@import` lines) alongside its own unrelated WO-235 changes, because they were sitting dirty in the same working tree when its auto-commit fired. This was not caused by this task (no `git commit` was run here) and nothing was lost — every file's on-disk content was re-verified independently of git state (line counts + `vite build` + full gate, all green) after every edit and again at the very end. Flagging for the owner: the repo appears to have more than one automated agent committing to the same working tree concurrently, which risks commit messages not matching their actual diffs.
