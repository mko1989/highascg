in the compose preview top bar, there is a prt pgm button that needs to be removed and replaced by the reset button for reset the compose prv windows.
all live sources inside the compose preview need to have a prt/capture button. the prt button is weirdly not consitetnt with the rest of the buttons and on the pgm there is capture and prt button.


playlists panel in bottom rigth needs to show all playlists that are created in looks. as an operator i need to be able to set an item of the playlist before playout.

i messed with some parameters and now it stopped displaying at all. it needs a revert back to default next to each parameter. 
list the parameters in two columns, the sliders dont have to be this long.

each time i do git push origin main i get mail from github that some action failed.


Annotations
1 error and 1 warning
verify
Process completed with exit code 1.
verify
Node.js 20 is deprecated. The following actions target Node.js 20 but are being forced to run on Node.js 24: actions/checkout@v4, actions/setup-node@v4. For more information see: https://github.blog/changelog/2025-09-19-deprecation-of-node-20-on-github-actions-runners/

on that note id like to implement some actual github actions that do some real checks wheter everything works. especialy when there will be others contributing.

the compose preview top bar change to half the current height.
on top of this bar there is a progress bar. it has too much spacing on the top.
the top bar of the screen's looks list is also to high.
the mix duration type takes the full wdith it has when it should be small adjusted to right.


the screen that displays after reboot to choose operator monitor is too bright and colorfull. black bg and white text in the middle. also when the monitor is chosen this way it needs to be cabled correctly in the devices tab already.


the taskbar in the top bar of operator gui, uses apps system icons inside the cirlce.
when i click back to gui, the browser hides under the casparcg screen, but after around 2s. but then i cant recall to the front the browser again.

 the playlist workflow the items in the list have teribble spacing. the move dots are huge, then there is some rectangle, 4characters of the item name, big box for seconds. and big trashcan. minimalizm is the key. the most importnat part is the name. change trashcans to xs. check if they appear anywhere else like layer list.


 ****
 the font of current item under a screen window inside compose preview should have bigger font.
 the play and cut buttons should be adjusted to left.
 the shaders thumbnail only worked for one of my looks.
 i did changes in a playlist of look 6 and after it played the list was correct, but when it went back to preview it played the old version.
 also a while ago when i had shaders in the playlist mixing between them worked and now it cuts.


 in the compact playlist the next prev buttons have blue bg under icons, i dont want it.
 the shaders thumbs are back to not filling the screen even though the layer they are on is set to screen's dimmensions.



 the playlist workflow of shaders now uses preview channel. why? everything can be done inside the pgm channel only.
the playlist continues even though its look was taken from the pgm......

i want to expand the shaders live editor. first the drop down which to edit is acting out by blinking.
the right side of the parameters is truncated.
there needs to be a way to decode what each of the parameters do and label them. also the color pickers are gone. right now its all giberish.

 current time with seconds small between eyes on the right and the small progress bar.