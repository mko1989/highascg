i edited a look for pgm 2 hit the global play button (still inside this looks editor) and it took this look to pgm1.

for the timer template. 
the thing is we need to change where the controls for the timer appear. having them inside the layers inspector only is not flexible enough.
when a timer is used it should create timer #1 and add a small collapsible timer display and controll in the right bottom next to the compact audio mixer. there should also be a drop down at the top of it to select which timer to control/display.
also inputing duration in seconds is not good. should be 3 seperate boxes for hh mm ss 
timers default should be center.


i need a way to somehow show the templates in the looks editor. i had an idea to just grab a sort of thumbnail/snapshot from the template (when there are animations for lower thirds then from fully "shown") to display in the editor as a layer.





i need you to check the auto systems for drop-update and decklink install.
my idea is for a usb stick boot. while system is booting a service checks if exfat usb exists, checks the content of drop-update if there is something there copies it over into the highascg workdir. (make sure the copy overwrites only existing files, and does not somehow delete others) so highascg can run with the latest version without rebuilding the stick.
for decklink simliar but when the debs are in the decklink folder the service needs to install it. so passwordless sudo for this is needed.

for installed system similar case, but it should check first if the update or decklink is not already installed.

i also need a way for installed systems to easily from the web gui check for updates (realease on github) download it and install (once the download is complete prompt the user that highascg needs to restart press yes/no to restart)

do a full github comit and push.

in the system tab of the settings modal do a display of the hardware that is on the system.



weird issue the cropped layer with a border displays correctly on the main output but on multiview it displays without the top crop.

the timers on the multiview need to show the timers and progress bar for every layer, but only those that actually have a video/audio file that has some running time to show. the rest should be just displayed. 
all should have just the short version of layer label ex:L10 at the top should be the highest layer and then descending to bottom.


the timer display in the small corner control does not display the time. after playing other set time the setting defaults back to 5min. under the time set add a couple of presets for 5min 10min 15min 20min 25m 30m 45m 60m (should be very small buttons).

we need to add a system setting in the settings modal for setting the system time.

check the workflow for auto creation of the hostname based on the mac address. on usb boot it needs to happen each time it boots.

****NEW***
still the top layer of my test look is pip-edge-strip.html which just shows with 00 and empty progess bar on the multiview. 
actually the pip borders should be linked with their layers.
no need to make the top layer bigger in the timers. the labels should be L## file name.
the prv channel timers doesn't work correctly.
the timers show stale leftover layers sometime.

i had a timer template in two looks on pgm 2 and after transitioning to a look without the timer stays on the screen and it shouldnt.
whats the workflow to create multiple timers? id like to be able to use the same timer on different looks, and be able to use a completly different one on other looks.
the  compact timer disppeared. it also didnt show the current timer.

****NEWNEW***

the modal that opens when connection eyes are clicked.
the logs and shortcuts should be tabs, minmal. and changing them should change the view of the modal. now the shortcus display in a super small bar at the bottom of the logs....

doing changes in the look editor does not move the prv channel of caspar...
the crop and border issue is now preset only on prv1 ch2.
the prv screen needs to be treated as such. meaning the changes made in the web ui need to be reflected on the prv screen asap. 

i also noticed that the jpeg hack preview in the ui now has higher latency than i noticed at the begging.
still something wrong with this
Image corrupt or truncated. 1.jpg
Image corrupt or truncated. 2.jpg
Image corrupt or truncated. 1.jpg
Image corrupt or truncated. 2.jpg
Image corrupt or truncated. 2.jpg
Image corrupt or truncated. 3.jpg
Image corrupt or truncated. 1.jpg
GET
http://192.168.0.35/api/compose-preview/3.jpg?v=W/"1784026611284-23003"
[HTTP/1.1 404 Not Found 9ms]

Image corrupt or truncated. 3.jpg

​****NEWNEWNEW***

the devices tab takes a while to render. something is holding it up or its not optimized correctly.

the webui doesnt stoped working due to trying to change something on the multiview, after changes that didnt do anything i reset the layout but the caspar output still shows the old mv. the compose preview is now like 2-3fps and i have it set to 25fps and  

Content-Security-Policy: The page’s settings blocked a JavaScript eval (script-src) from being executed because it violates the following directive: “script-src 'self' 'unsafe-inline'” (Missing 'unsafe-eval') 48 device-view-CJr4CM-R.js:11:25340
Content-Security-Policy: The page’s settings blocked a JavaScript eval (script-src) from being executed because it violates the following directive: “script-src 'self' 'unsafe-inline'” (Missing 'unsafe-eval') 6 device-view-CJr4CM-R.js:11:25340

​

the compact timer doesnt display the timer, the display is stuck at 00. the set timer resets to 5min, even though the actual timer works as expected so in my case restes to set before 20 min.

playing look on screen 2 does nothing on the actual output and mv.....



now there is a loading devices... and it never loads!!!!!

also need a new feature to be able to adjust the size of the multiview timers

also an ability to highlight the top layer with active running media (with timers)




the highligth is not a great color on black bg. the 200% size should be 100%, as 200% is actualy readable.
the timers can span the whole width of the window. when a filename is too long to display it should be truncated.

timers on the mv of the prv channel does not work. and also the prv channel does not display the look exactly how it ends up being on the pgm, which is correct. but the prv shows something different.
the live prv editing is still bugged as hell. it doesnt actually move the layers on the prv output observed on the multiview screen (casparcg consumer).

****NEWNEWNEW***

the multiviewer is bugged. changes made on it doesnt refresh, unless i reset the multiview and rebuild it.
the font on the timers is a bit too big in the output, make it adjustable too.
it also shows my pgm1 and prv1 as 1920x1080 channels when in reality they are 3072x1728.

the incorrect cropping is now on the pgm channel. the prv still doesnt auto update when editing a look.

